schema = 1

[[contract]]
id = "a"
title = "図の生成の口 3 つ（検査・描画・埋め込み）を 1 便で置く"
req = ["FR15"]
section = "1"
verify = ["cargo nextest run -p folio figure"]
size = "M"
done = "型付き記述の見本 1 本が仕上がりの段を通り図の本体が部品として埋まる"
goal = "文書の正本の図の節にある型付き記述から、検査に通った図の本体（SVG）だけを部品として埋める。 検査を通らなかった図は生成せず、前の生成物も上書きしない（FR15・P-4.1）。 検査を通らなかった図の生成物は書き出してはならない（FR15）。 この節の文のうち規範の印を持つのは直前の 1 文だけで、参照 id を持ち数と単位を持たないので散文の門（R-16）を通る。"
