# folio2 判断の記録 ADR-21 — 正本（1 判断 = YAML 1 file）。欄の決まりは adr/schema.yaml。人が読む面は判断の記録の面（adr-21.html・生成物・手で直さない）。
# 状態: accepted（起草 2026-09-24・改訂 b〜d 2026-09-24・orchestrator 席 fable 5.1 の起草役・発効 2026-09-24 22:38 JST 持ち主「承認する」= 本判断の承認要求〔問 2 = v3 の合流先は新しい repo（scribe3）・問 1 = 発効（案 a）〕への回答・推奨どおり）。憲法の条文は 1 字も変えない（条文を改訂しない判断）。規則の表の行も足さない。
# 起点: scribe2 の orchestrator 席からの連絡（~/.local/share/folio2/handoff-2026-09-24/scribe2-v3-direction.md・scribe2 の持ち主の裁定 2026-09-24T08:40Z・逐語は scribe2 の台帳 s2-07l.214 notes）と、その控え 台帳 f2-648.188。
# 持ち主の裁定: 台帳 f2-648 notes 2026-09-24 21:33 JST（対話面 R-8・逐語「すべて承認する」）の 🔴 問 6 = 判断の記録 ADR-16 の M3 の出口の利用者を scribe2 から v3 の置き場へ読み替える改訂を「起こす」。これは起草してよいの裁定で、発効は本判断の承認要求（docs/design/adr-21.md）で別に問う（条 A-2.1 の形に倣う）。
# 反証の試み = 起草役の自己反証 docs/design/adr-21-grill.md と、独立の検証役の検証 ~/.local/share/folio2/handoff-2026-09-24/adr21-verify.md（別の文脈・opus 5.5・条件付き支持・blocking 4）。起草の記録 = ~/.local/share/folio2/handoff-2026-09-24/adr21-draft.md。
# 改訂 b = 改訂の欄の 5 行を先例と同じ 1 行の形に（歯 f101）。改訂 c = 独立の検証の blocking 2〜4 と文面を席の裁定（2026-09-24）で当てた（v3 の置き場は既に立っている・器の受け口は着地済み・合流先の repo を 🔴 問 2 で問う）。
# 実測は本流 ca60873 の組み立て（target/debug/folio）を scratchpad の空の版管理の写しに撃った（folio2 の repo には書いていない）。外の置き場の実測 = 同 dir の folio2-v3-entry-notes.md。

id: ADR-21
title: M3 の出口の利用者を scribe2 から器の次の世代（scribe v3）の設計の置き場へ読み替え、scribe2 の設計文書の移行に掛かる論点を閉じる

status: accepted
date: 2026-09-24

plain: |
  folio2 の次の段（M3）は、「folio2 以外の場所で、folio2 の道具の検査に合格できるか」を確かめる段です（判断の記録 ADR-16）。
  その「folio2 以外の場所」には、scribe2 の repo を選んでいました。scribe2 の設計文書のうち、便の約束の表（契約表）を持つものを folio2 の形に移して、検査に掛ける計画でした。
  ところが 2026-09-24 に、scribe2 の持ち主の裁定で、この移行は取りやめになりました。
  scribe2 は残りの仕事を前の版の道具（folio v1）のまま片付けます。folio2 の形へ移るのは、器の次の世代（scribe v3・以下 v3）を作るときです。
  v3 は、scribe2 の中心の部分と folio2 と画面の層を 1 つの repo にまとめる世代で、v3 の設計文書は最初から folio2 の形で書きます（folio2 の道具の init 命令が書く骨格から始めます）。
  その置き場は 2026-09-24 の夕方に、新しい repo（名は scribe3）の中にもう立っています。
  このままでは、ADR-16 の出口は「scribe2 が文書を移して合格したとき」のままで、その日は来ません。

  この判断では、出口の相手を scribe2 から「v3 の設計の置き場」に読み替えます。
  出口の 3 つの確かめ（その置き場で道具の検査に合格する・便の約束の写しがそこに入ってずれが 0 と数える・便を運ぶ道具がその写しを読んで通す）は、中身を変えずに v3 の置き場で確かめます。ただし 3 つ目の確かめの記録には、どの器が読んだか（器の repo と commit）を名指すことを足します。v3 の器は今の器（scribe2）から v3 の中の器へ替わりうるので、名指さないと確かめ直せないからです。この分だけ、3 つ目の合格の条件は前より狭くなります。
  「移す」の話は無くなります。v3 は移すのでなく新しく書くからです。
  置き場の形も v3 には当てません。ADR-16 は、scribe2 の repo の中の design-intent/folio2 という場所に置き、憲法を「床の受付の宣言」と呼んで scribe2 の本来の憲法の条を指す形を決めていました。
  この形は、同じ repo に本来の憲法を別に持つ利用者に限ります。v3 の置き場の場所は v3 が選び、骨格が書く憲法は v3 の憲法そのものです。
  v3 の持ち主が、骨格の「本来の憲法を指す」字を v3 の条に書き換えてから第 1.0 版を承認し、その後に始まりの凍結（基準の写しを最初に作る命令）を動かします。
  ADR-16 の決定の文そのものは書き換えません。発効した判断の記録なので、この判断の「改訂の欄」と、ADR-16 の注の 1 段で断ります（今までの判断の記録と同じやり方です）。

  あわせて、scribe2 の移行のためだけにあった話を閉じます。
  scribe2 の文書の番号の書き方（9 種類）に合わせる話、章が多すぎる文書の話、scribe2 の側の仮の検査を外す話です。
  v3 は最初から folio2 の番号の書き方と章の上限で書くので、どれも要らなくなりました。
  番号の書き方を置き場ごとに変える仕組み（ADR-16 の決定 (6)）の考え方は残しますが、そのための便はいまは起こしません。

  v3 の置き場で検査を通すまでに直す所（骨格のままだと画面が作れない・「folio2」という名前が画面に焼き込まれている、など）は、別の控え 8 件（folio2 の台帳の項 f2-648.189〜f2-648.196）に書いてあります。
  その順番は席が決めるので、この判断には並べません。

  やめる条件も決めます。folio の実装そのものが v3 の repo へ移ってしまい、v3 が「外の利用者」と言えなくなったら、あなたに問い直します。
  v3 の設計が「便の約束は写しの file でなく型で渡す」と決めたら、出口の 3 つの確かめのうち後ろの 2 つを問い直します。
  発効から 30 日たっても v3 の置き場で検査の合格が記帳されないとき、scribe2 がやはり移行を再開したときも、あなたに問い直します。

  あなたは scribe2 の側で「それを folio2 で作ってはだめなのか」と問うていました。その答えはまだ記録に見つかりません。
  v3 を folio2 と別の repo で作るか、folio2 の repo を v3 に育てるかを、別の問いであなたに聞きます。
  folio2 の repo で作るなら、v3 は「外の利用者」になれないので、この判断は発効させず保留にします。

  （発効の後の読み方）この判断は 2026-09-24 に発効しました。あなたは推奨どおり承認し、v3 は新しい repo（scribe3）で作り、folio の実装は後から取り込む（folio2 の repo は v3 に育てない）と決まりました。
  だから v3 の置き場は「外の利用者」に数えられ、上の段落の保留は起きませんでした。上の段落は、承認をお願いしたときの字のまま残しています。

context: |
  (1) 出所。scribe2 の orchestrator 席からの連絡（2026-09-24 17:43 JST 着・~/.local/share/folio2/handoff-2026-09-24/scribe2-v3-direction.md）。
  scribe2 の持ち主の裁定（2026-09-24T08:40Z・逐語は scribe2 の台帳 s2-07l.214 notes）の要旨は次のとおり。
  (ア) scribe2 の設計文書（docs/design の 22 本）を folio2 の設計ノートへ移す段（判断の記録 ADR-16 の M3 の消費側）は取りやめ、scribe2 は残りの契約を前の版の道具（folio v1）のまま畳む。
  (イ) folio2 の形への移行は器の次の世代（scribe v3・以下 v3）を作るときに行う。v3 は scribe2 の中心の部分と folio2 と面の層（画面と、席が制御する表示面）を
  1 つの repo・1 つの組み立ての束（workspace）・1 つの憲法・1 つの台帳に合流させる世代で、白紙の書き直しではない。
  組み立ての単位（crate）は 設計の正本と床の単位（folio）と器の単位（scribe）を分けたまま、境界は「設計ノートの行 → 契約」の型 1 本とする。
  (ウ) v3 の設計は folio2 の形で、folio init --dir の骨格から起こす。M3 の口（骨格の命令・導出物の命令・置き場の憲法の値域の床）は v3 の入口として据え置く。
  (エ) scribe2 が送っていた論点（scribe2 の 9 形の参照 id・章の上限 12 を超える文書 10 本・器の暫定の散文の門〔scribe2 の側の要件が持つ門〕の床の引き継ぎ）は閉じてよい。
  folio2 の台帳には控え f2-648.188 を起こし、判断の記録 ADR-16 の状態は動かさなかった（条 P-12.1・P-12.3）。
  scribe2 の持ち主の同じ裁定の逐語（s2-07l.214 notes）は、4 問目への答えの後段に「それをfolio2で作ってはだめなのか？」の問いを持つ。連絡はこの後段を写していない。
  v3 の置き場の討論の起こしの文書（scribe3 の docs/handoff/2026-09-24-v3-kickoff.md）にも同じ逐語が在るが、この問いへの答えは起こしにも台帳にも見つからなかった（独立の検証の読み）。

  (2) 持ち主の裁定。台帳 f2-648 notes 2026-09-24 21:33 JST（対話面 R-8・逐語「すべて承認する」）の 🔴 問 6 は
  「ADR-16 の M3 の出口の利用者を scribe2 から v3 の置き場へ読み替える改訂を起こす」を推奨どおりに承認した。問うたのは改訂を起こすことで、本判断の発効は別に問う。

  (3) ADR-16 の今の状態。ADR-16 は 2026-09-23 17:39 JST に発効した（逐語「裁定は両方承認する」）。決定 (7) の便のうち、
  ① 要件書の M3 の範囲の節（第 1.34 版）・② 列の根の表と始まりの凍結の命令（便 121・要件 FR23 / FR24）・③ 値域の部分集合の床（便 122・FR25）・
  ④ 骨格の命令と器の導出 file の解き方（便 123・便 125・FR22）・⑤ 導出物の独立の命令（便 119・FR11）は、どれも 2026-09-24 までに着地した（要件書の各要件の注）。
  残るのは ⑦（列の根の表に利用者の行を足す便）だけで、これは利用者が骨格を書き、持ち主が骨格を承認し、始まりの凍結の命令で要約値を得た後に起こす。
  出口の判定点 ①〜③ の記帳はまだ無い。判定点 ③ の前提の器の受け口 s2-07l.473 は、契約 s2-07l.586 へ引き継いで閉じ、.586 は scribe2 の本流の 版（0337cf1・2026-09-24 07:40 JST）に着地した＝今の器は導出物の表の行の目標の欄（goal）を受ける（scribe2 の台帳・独立の検証の読み）。
  要件 FR11 の注の「s2-07l.473 は 2026-09-24 に器の台帳で再開された」の字はこの事実と食い違う（承認の後に同じ版で直す）。判定点 ③ は今の器で測れる状態に近づいた。

  (4) ADR-16 のうち利用者を scribe2 と読んでいる所。
  決定 (1) は M3 を「外部の利用者 1 つ（scribe2）」の段と定め、判定点の記帳を「利用者の台帳（scribe2 は s2- の項）」に置き、利用者の依存として folio を固定する裁定を scribe2 の憲法の条（A3）に結ぶ。
  決定 (3) は scribe2 の置き場を design-intent/folio2 とし（前の版の道具の文書群が design-intent を使っているため・ADR-16 文脈 (4)）、骨格の憲法を「床の受付の宣言」と題して
  規範文が利用者の本来の憲法の条を番号で指し、根拠の種別を値域の既存の値（scribe2-article）で本来の条に結ぶ形にした。
  決定 (6) は散文の門の参照 id の形の scribe2 の値（器の 9 形）と、その種の振り分け・数と単位の読み方の揃え・器の暫定の門の撤去の条件と、撤去の前提の便（参照 id の形を行 R-16 から読む便）を定めた。
  決定 (7) は、その撤去の前提の便と、利用者の側の便（読む file の述語・導出物の差分の歯・要件書の最小の写しの導出・暫定の門の撤去）を列に置いた。
  撤退条件 (1) は、洗い出しの漏れを数える窓を「本判断の便が揃った後から M3 の出口（判定点 ③）まで」とした。
  決定 (2) の 7 つの扱い（列の根・始まりの凍結・値域・部品目録・床が名指す id・欄の決まりの出所・器の導出 file の置き場）と決定 (4)(5) は、利用者の名に依らない。

  (5) 外の置き場の実測（folio2 席・2026-09-24・本流 ca60873・~/.local/share/folio2/handoff-2026-09-24/folio2-v3-entry-notes.md）。
  別の repo の design-intent に骨格を書いて commit すると、床は違反 0・「まだ分からない」2（凍結 anchor 0 本・id の一覧の基準が無い）だった。
  骨格のままだと落ちる命令は 7 つ（注入・配信の組み立て・面・天井の束・天井の印と索引の印字・凍結・部品の検査）で、6 つは骨格に中身を足せば通る。
  凍結は列の根の表に行を足した組み立てで初めて通り、そのとき床は合格（違反 0・まだ分からない 0）まで届いた。folio2 専用の値が焼かれた箇所は 13 項目あった。
  そのうち床の結果（違反か「まだ分からない」）に効くのは、列の根の表（ADR-16 の (ア)）・器の導出 file の置き場（同 (キ)）と、裁定 id の形（台帳の接頭辞が「英字 1 字と数字 1 字」の形でないと承認欄の裁定 id が落ちる）の 3 つである。
  最後の 1 つは (ア)〜(キ) に無い種類で、v3 の台帳の接頭辞がその形なら当たらない。面の題に出る「folio2」の名・支度表の id・里程の名・役者の語・様式の file・図の道具の置き場は、面の側（M3 の外）に効く。
  行 R-17 が無い置き場で散文の言及の歯が黙ることは、床の結果ではなく結果の不在である。控えは台帳 f2-648.189〜.195 の 7 件に起こし、本判断の起草の所見（文脈 (6)）を f2-648.196 に足して 8 件になった。

  (6) 骨格の形の実測（本判断の起草・本流 ca60873・scratchpad の空の版管理の写し）。骨格の命令が書く憲法の file は、名を床の受付の宣言（floor-declaration）と名乗り、
  前文の順位と唯一の条の規範文が「利用者の本来の憲法」を指し、根拠の種別が値域の既存の値（scribe2-article）である＝ADR-16 決定 (3) の scribe2 の形がそのまま雛形に焼かれている。
  commit の後の床は違反 0・「まだ分からない」2 で、外の置き場の実測と一致した。
  v3 は「1 つの憲法」を持ち、指す先の別の憲法を持たないので、この雛形の字は v3 では指す先が無い。雛形の字は利用者が凍結の前に書き換えられ、床はそれを止めない。

  (7) 外部の利用者という前提。条 P-13.4 は、自分のコードの変更を自分の便で運ぶ運用（self-host）を、外部の利用者で受入が通った後に始めると定める。
  ADR-16 文脈 (1) は M3 をその「外部の利用者の受入」の最初の段とした。v3 は folio2 を合流させる世代なので、いずれ folio の実装を同じ repo に持つ。
  ADR-16 は外部の利用者の性質を列挙していない（文脈 (1) の「最初の外部の利用者」と案 a の「外側の証人」だけ）。ADR-16 の文脈 (1) と決定 (2)（道具は 1 つの組み立て）から本判断が組み立てた読みでは、
  合流の前の v3 の置き場は、folio の実装を持つ repo（folio2）と別の repo に在り、憲法の名も folio2 の憲法の名と違い、本流の 1 つの組み立てで検査されるので、scribe2 に認めたのと同じ程度に「よそ」である。
  合流の時機は連絡にも v3 の起こしにも無い。v3 の起こしの論点 E は、v3 の憲法を scribe2 の憲法と folio2 の憲法の合流とし、どちらを母体にするかを未決にしている。folio2 の憲法を母体にして名を引き継ぐと、憲法の名の限定も崩れる。
  持ち主の「それをfolio2で作ってはだめなのか？」（文脈 (1)）の答えが「folio2 の repo を v3 に育てる」なら、v3 の置き場は最初から folio の実装と同じ repo に在り、外部の利用者と数えられない。

  (8) 境界の形。連絡は v3 の 2 つの組み立ての単位の境界を「設計ノートの行 → 契約」の型 1 本と書いた。ADR-16 の判定点 ②③ は、契約表から器が読む形の file（導出物）を版管理に置き、器がそれを読むことを確かめる。
  v3 の器が導出物の file を読むのか、同じ組み立ての束の中で型を直に受けるのかは、v3 の設計の討論（新しい置き場（scribe3）の起こしの版（06cd976）から）でまだ決まっていない。
  起こしの論点 A は、scribe2 席の推奨として境界の型を「設計ノートの行 → 契約（toml）」1 本と書き、採否は討論で決めるとしている＝導出物の file の側の見込みである。
  v3 の最初の便を運ぶのは今の器（scribe2 の組み立て）である見込みが高く、今の器は導出物の file を読み、表の行の目標の欄（goal）も受ける（文脈 (3)・s2-07l.586）。

  (9) v3 の置き場の実在（独立の検証と本判断の改訂 c の読み・読むだけ）。v3 の置き場は、新しい置き場（~/projects/local-projects/scribe3）の repo の design-intent に在る。
  骨格は版（5091abb・2026-09-24 17:45:49 JST・「folio2 の骨格で v3 の設計の置き場を起こす」）で、本判断の起草と問 6 の承認より前である。
  その後に討論の起こし（版 06cd976・21:33 JST）・論点 D の討論（版 d65646c）ほかの commit が続く。遠くの写し（remote）も台帳も無い。憲法の名は床の受付の宣言（floor-declaration）のままである。
  本流の組み立てで撃った床は違反 0・「まだ分からない」2 だった。folio の組み立ての単位（crates）はまだ無い。

decision: |
  (0) 本判断は条文を改訂しない。規則の表の行を足さない。決めるのは、判断の記録 ADR-16 の決定のうち利用者を scribe2 と読んでいた所の読み替えと、
  scribe2 の設計文書の移行のためだけに置いた論点の閉じ方（判断の記録 ADR-3 決定 (7) の撤去の句を含む）と、ADR-16 の撤退条件 (1) の窓の起点である。ADR-16 の決定 (2)(4)(5) は変えない。

  (1) **M3 の利用者を v3 の設計の置き場へ読み替える（ADR-16 決定 (1) を狭める）。**
  本判断は、v3 の合流先の repo が folio2 の repo と別の repo である（今の置き場は新しい置き場（scribe3）の repo・文脈 (9)）ことを前提とする。持ち主がこの前提を否とする（folio2 の repo を v3 に育てる）なら、本判断は発効させない（承認要求の 🔴 問 2・案 c）。
  ADR-16 決定 (1) の「外部の利用者 1 つ（scribe2）」は、本判断の発効の後、「v3 の設計の置き場（v3 の repo の下に folio init --dir で書いた置き場）」と読む。
  M3 の出口は、その置き場の 1 本目の契約表を持つ設計ノートが判定点 ①〜③ を通ったときである。判定点の中身は ADR-16 決定 (1) のまま、場所を v3 の置き場とする＝
  ① v3 の置き場で床が合格する（撃った folio の組み立ての要約値を記帳に添える）、② 導出物が v3 の repo に着地して差分 0 を数えられる、③ v3 の便を運ぶ器の受付がその導出物を読んで通す。
  判定点 ③ の記帳は、読んだ器の組み立て（器の repo と commit）を名指す。v3 の器は今の器（scribe2）から v3 の中の器へ替わりうるので、どの器が読んだかを特定しないと判定点 ③ を確かめ直せないためである。
  判定点の記帳は v3 の置き場の台帳が持ち、folio2 の台帳 f2-648 はその id を名指す。記帳が無いあいだ判定点は「まだ分からない」である（要件書の受入基準 AC23〜AC25 の形のまま）。
  M3 に入れないもの（ADR-16 決定 (1) の列）は変えない。そのうち「利用者の憲法・判断の記録・調べものの文書の移行」は、v3 が移さず新しく書くので当たる物が無い。
  面の生成と配信・注入は、v3 の置き場でも M3 の出口に入れない（外の置き場の実測で骨格のままでは落ちる命令が在るが、それは v3 の入口の便で扱う・帰結）。
  利用者の依存として folio を固定する裁定を scribe2 の憲法の条（A3）に結んだ字は、v3 では v3 の設計が決める（同じ組み立ての束に入れば依存の追加ではなくなる）。
  v3 の置き場を M3 の「外部の利用者」と数えるのは、その置き場が folio の実装を持つ repo と別の repo に在り、その置き場の憲法の名が folio2 の憲法の名と違うあいだとする（文脈 (7)）。

  (2) **置き場と骨格の形を、本来の憲法を別に持つ利用者に限る（ADR-16 決定 (3) を狭める）。**
  決定 (3) の「scribe2 の置き場は design-intent/folio2 とする」と、骨格の憲法を「床の受付の宣言」と題して規範文と前文が利用者の本来の憲法を指し根拠の種別を値域の既存の値（scribe2-article）で結ぶ形は、
  同じ repo に本来の憲法（前の版の文書群）を別に持つ利用者に限って当てる。v3 の置き場の場所は v3 が選ぶ（連絡の字は v3 の repo の design-intent）。
  v3 の骨格の憲法は v3 の憲法そのものとし、v3 の持ち主が骨格の雛形の「本来の憲法を指す」字を v3 の条へ書き換えてから第 1.0 版を承認し、その後に始まりの凍結を撃つ（文脈 (6)）。
  骨格の命令の振る舞いと雛形は本判断では変えない（便を起こさない）。本来の憲法を持たない利用者向けの雛形を持つかは、v3 の入口の便の論点として残す（帰結）。
  決定 (3) のうち、既に別の文書群が在る置き場には書かないこと・雛形に folio2 の口座名と席とモデルの名と持ち主の逐語を写さないこと・相談窓口の雛形の扱い・案内の 1 行は変えない。

  (3) **scribe2 の移行のためだけの論点を閉じる（ADR-16 決定 (6) を狭める）。**
  決定 (6) のうち、scribe2 の値（器の 9 形）・種の振り分けの初版・数と単位の読み方を scribe2 が folio の読みに揃えること・器の暫定の散文の門（scribe2 の側の要件が持つ門）の撤去の条件・
  形だけ認める形の字の並びを器の歯の 12 例に合わせる fixture は、scribe2 の設計文書が folio の置き場へ移らないので閉じる。
  参照 id の形を行 R-16 から読む便（撤去の前提の便）は起こさない。要件 FR12 の規範文の直し（参照 id の 4 種の列挙を行 R-16 の値の形へ）もその便と同じ版の予定だったので行わない。FR12 の規範文は今の 4 種のままである。
  決定 (6) の形（参照 id の形は道具の閉じた一覧から置き場ごとに行 R-16 の値で選び、形ごとに解決か形だけ認めるかを持つ）は向きとして残す。
  folio2 の形の外の参照 id を要る利用者が出たときに、その便を改めて起こす。v3 は最初から folio2 の参照 id の形で書く（連絡の (エ)）ので、M3 の出口はこの便を要らない。
  folio2 の行 R-16 の値と folio2 の散文の門は 1 字も変わらない。
  器の暫定の散文の門の撤去は、folio2 の判断の記録が条件を持たない器の側の扱いとする。判断の記録 ADR-3 決定 (7) の「M1 が引き取ったら撤去する」は、ADR-16 決定 (6) が条件を定め直した字で、
  その定め直しを本判断が閉じても元の字へは戻らない（改訂の欄で ADR-3 決定 (7) の撤去の句も狭める）。器は利用者の設計文書を folio の床に掛けないまま前の版の道具で畳むので、撤去の時機は器の持ち主が決める。
  章の上限 12 を超える scribe2 の文書の論点（ADR-16 文脈 (8)・案 i）は、決定 (5) が導出物を配信の組み立てから切り離したことで M3 の判定点から既に外れており、利用者が v3 に替わっても変わらない。v3 は章の上限の中で書く。

  (4) **便の列から scribe2 の側の便を外す（ADR-16 決定 (7) を狭める）。**
  決定 (7) のうち、参照 id の形を行 R-16 から読む便と、利用者の側の便（読む file の述語・導出物の差分の歯・要件書の最小の写しの導出・暫定の門の撤去）は、列から外す。
  ⑦（列の根の表に利用者の行を足す便）は残し、利用者を v3 の置き場と読む。起こすのは、v3 の持ち主が v3 の憲法の第 1.0 版を承認し、始まりの凍結の命令が要約値の全桁を出して断った後である（規則の表の行 D-11 の手順）。
  運ぶのは、そのとき folio の実装を持つ repo の便である（合流の前は folio2 の便）。
  v3 の入口に要る便（台帳 f2-648.189〜.196 の 8 件の控え）は M3 の列に入れない。帰結で名指すだけで、順は席が決める。

  (5) **ADR-16 の撤退条件の数え方（ADR-20 決定 (4) と同じ形・撤退条件の字は変えず ADR-16 の注で断る）。**
  撤退条件 (1)（洗い出しの漏れ）の窓の起点は、v3 の置き場が立った時点＝v3 の repo に骨格の命令が書いた置き場を載せた最初の commit の時刻とする。
  その時点は新しい置き場（scribe3）の repo の版（5091abb・2026-09-24 17:45:49 JST）で、発効の時点では窓は既に始まっている。席はその repo と commit と時刻を台帳 f2-648 に記帳する。窓の終わりは M3 の出口（判定点 ③ の記帳）のまま。
  基準の 7 種類（ADR-16 文脈 (2)(3) の (ア)〜(キ)）と閾値（3 種類）と戻し先は変えない。外の置き場の実測（文脈 (5)）は時刻では窓の中だが、v3 の repo でなく写しの上の実測なので数えない。
  床に効く種類の候補（裁定 id の形）が 1 つ在ったことを記帳の最初の行に添える。
  撤退条件 (2)(3)(4) は字のまま数え、「利用者」を v3 の置き場と読む。

  (6) **発効した決定の本文は書き換えない。** ADR-16 の決定・文脈・案・撤退条件・承認欄と見出しの字は変えない。範囲の改訂は本判断の改訂の欄が持ち（欄の決まりのとおり改訂する側の片側だけ）、
  ADR-16 の注に 1 段で本判断の読みと改訂の欄の在り処を断る。ADR-16 の平易文の末尾に、利用者の読み替えの 1 段を足す（決定の字ではなく平易文なので、先例 ADR-20 が ADR-18 の平易文を直した形に倣う）。
  要件書と規則の表の行 R-16 の注の字の揃えは、承認の後に席が書く（承認要求 docs/design/adr-21.md の §3）。

  (7) 憲法の条文は 1 字も改訂しない。条文の改訂が要ると分かったら、その時点で本判断を止めて条 A-2 の手順へ移す。

options:
  - id: a
    name: ADR-16 を生かしたまま、利用者を v3 の設計の置き場へ読み替え、scribe2 の移行のためだけの論点を閉じる（採る）
    text: |
      決定 (1)〜(7) の形。ADR-16 の決定 (1)(3)(6)(7) を改訂の欄で狭め、決定の本文は書き換えない。
      判定点 ①〜③ はそのまま v3 の置き場で確かめ、撤退条件 (1) の窓の起点を v3 の置き場が立つ時点へ動かす。
    verdict: adopted
    reason: |
      ADR-16 の決定 (2) の 7 つの扱いと、決定 (7) の便 ①〜⑤ の着地（要件 FR11・FR22〜FR25）は利用者の名に依らず、v3 の置き場でもそのまま使える（連絡の (ウ)・外の置き場の実測）。
      変わるのは利用者の名と、scribe2 の移行に結んだ所だけなので、判断の記録の欄の決まりの改訂の欄（発効した判断が生きたまま、その決定の範囲が別の判断で変わるとき）の形に合う。
      持ち主が問 6 で承認した向きそのものである。
  - id: b
    name: ADR-16 を退役させ、v3 の置き場を利用者とする新しい M3 の判断の記録を起こす
    text: |
      ADR-16 の状態を退役（retired）にし、後継の判断の記録（本判断）が置き換えの欄（supersedes）で置き換える。決定 (2) の 7 つの扱いと便の列は新しい判断に書き直す。
    verdict: rejected
    reason: |
      変わるのは利用者の名と scribe2 に結んだ所だけで、決定 (2)(4)(5) と着地した便 ①〜⑤ は生きている。
      丸ごと置き換えると、生きている決定 (2)(4)(5) を 1 字も変えずに後継へ写し、ADR-16 の改訂の欄 2 行（ADR-2 決定 (3) を広げる行と ADR-3 決定 (7) を広げる行）も後継へ移すことになる。移さないと列の根の表の根拠が退役した判断に残る。
      要件書の判断の記録の欄は直さなくても床は合格する（退役した判断は名指しを解決し続ける・独立の検証の写しの実測）。
      決定の生きている判断を廃止の状態にすることは、条 P-7.2 の状態の意味とも合わない。得るもの（字の上で新しい 1 本にまとまること）は注の 1 段と改訂の欄で足りる。
  - id: c
    name: 何も変えず、v3 の置き場で最初の設計ノートが書かれるまで保留する
    text: |
      ADR-16 の字も要件書も触らず、v3 の置き場で最初の設計ノートが書かれた時点で、改めて読み替えを問う。scribe2 の論点も開いたまま残す。持ち主が v3 を folio2 の repo で作ると答えたときは、この案に倒す。
    verdict: rejected
    reason: |
      ADR-16 の出口は「scribe2 が契約表を持つ設計ノートを置いて合格したとき」のままで、scribe2 の持ち主が移行を取りやめたので、その日は来ない。
      出口に届かない M3 が発効したまま残り、条 P-13.4 の「外部の利用者で受入が通った後」の物差しが指す先を持たない。
      撤退条件 (1) の窓は字の上では便が揃った時点から始まっているが、数える相手の置き場を ADR-16 は名指さない。
      scribe2 が閉じてよいと言った 3 つの論点（9 形の参照 id・章の上限・器の暫定の門の撤去）も、持ち主の裁定と判断の記録なしには閉じられない（条 P-13.3）。
      費用は小さいが、要件書の受入基準 AC23〜AC25 の平易文が「利用者（scribe2）」のまま読まれ続ける。判定点 ① の記帳が来ない場合の扱いは本判断の撤退条件 (3) が持つ。
  - id: d
    name: 読み替えに加えて、M3 の出口を判定点 ①（v3 の置き場での床の合格）だけに縮め、判定点 ②③ を v3 の境界の設計へ移す
    text: |
      v3 の境界は「設計ノートの行 → 契約」の型 1 本になるので、導出物の file と器の読みの確かめ（判定点 ②③）を M3 から外し、M3 の出口を床の合格だけにする。
    verdict: rejected
    reason: |
      v3 の器が導出物の file を読むか型を直に受けるかは、v3 の設計の討論でまだ決まっていない（文脈 (8)）。今ここで ②③ を外すと、v3 の設計を先に決めることになる。
      討論の起こしの推奨は「設計ノートの行 → 契約（toml）」で、導出物の file の側である。v3 の最初の便を運ぶのは今の器で、今の器は導出物の file を読み、表の行の目標の欄（goal）も受ける（s2-07l.586 着地）。導出物の口（要件 FR11・便 119）は着地しており、受入基準 AC24・AC25 の形も在る。
      床の合格だけでは、条 P-13.4 が求める「外部の利用者の受入」のうち、器が folio の出力を実際に使える面が確かめられない。
      v3 の設計が型の境界を選んだときに ②③ を問い直すことは、本判断の撤退条件 (2) に置いた。
  - id: e
    name: 利用者を scribe2 のまま残し、scribe2 の移行の再開を待つ
    text: |
      ADR-16 の字を変えず、scribe2 が v3 の前に移行を再開するのを待つ。
    verdict: rejected
    reason: |
      scribe2 の持ち主の裁定（2026-09-24T08:40Z）は、scribe2 の開発は前の版の道具で残りを全部こなせるとして移行を v3 へ送った。
      folio2 の持ち主も問 6 で v3 の置き場への読み替えを承認した。待つ相手の予定が無い。scribe2 が移行を再開した場合は、本判断の撤退条件 (4) で元の読みへ戻すかを問う。

basis: [P-4, P-6, P-7, P-8, P-11, P-12, P-13, A-2, N-3, R-7, R-8, R-16, R-17, D-11, FR11, FR12, FR22, FR23, FR24, FR25, AC23, AC24, AC25, CON8, ADR-2, ADR-3, ADR-13, ADR-16, ADR-18, ADR-20]

revises:
  - {target: ADR-16, decision: (1), kind: narrow, summary: M3 の出口の利用者を「外部の利用者 1 つ（scribe2）」から v3 の設計の置き場に限り、設計文書を移す話を範囲から外し、判定点 ③ の記帳に読んだ器の組み立てを名指すことを足す（判定点 ①〜③ の中身と M3 に入れないものの列は変えない）}
  - {target: ADR-16, decision: (3), kind: narrow, summary: 置き場を design-intent/folio2 とする字と、骨格の憲法を本来の憲法を指す「床の受付の宣言」にし根拠の種別を値域の既存の値（scribe2-article）で結ぶ形を、同じ repo に本来の憲法を別に持つ利用者に限る（v3 の置き場の場所は v3 が選び、骨格の憲法は v3 の憲法そのものとして v3 の持ち主が書き換えてから承認する・骨格の命令は変えない）}
  - {target: ADR-16, decision: (6), kind: narrow, summary: scribe2 の値（器の 9 形）・種の振り分け・数と単位の読みの揃え・器の暫定の門の撤去の条件と、撤去の前提の便（参照 id の形を行 R-16 から読む便）と要件 FR12 の規範文の直しを閉じる（参照 id の形を置き場ごとに選ぶ向きは残し、folio2 の行 R-16 と門は変えない）}
  - {target: ADR-3, decision: (7), kind: narrow, summary: 器の暫定の散文の門を「M1 が引き取ったら撤去する」の句（ADR-16 決定 (6) が条件を定め直した字）を folio2 の判断の記録から外し、撤去の時機を器の持ち主の扱いとする（散文の門の式と folio2 の門は変えない）}
  - {target: ADR-16, decision: (7), kind: narrow, summary: 便の列から参照 id の形を読む便と利用者の側の便を外し、⑦ の利用者を v3 の置き場と読む（⑦ は v3 の憲法の第 1.0 版の承認と始まりの凍結の断りの後・folio の実装を持つ repo の便）}

consequences:
  - M3 の出口の判定点 ①〜③ は v3 の置き場で数える。v3 の置き場は新しい置き場（scribe3）の repo に立っているが判定点の記帳はまだ無く、記帳が在るまで判定点は 3 つとも「まだ分からない」のままである（要件書の受入基準 AC23〜AC25 の形は変わらない）。
  - ADR-16 の撤退条件 (1) の窓は v3 の置き場が立った時点から始まり、発効の時点で既に始まっている。その時点は新しい置き場（scribe3）の骨格の版（5091abb・2026-09-24 17:45:49 JST）である。外の置き場の実測は写しの上なので数えない。そこで見た床に効く新しい種類の候補は 1 つ（裁定 id の形・台帳の接頭辞）で、閾値の 3 に届いていない。
  - v3 の入口に要る便は、台帳 f2-648.189〜.196 の 8 件の控え（骨格と面の生成器の食い違い・規則の表の引用符付きの id と索引の読み手・面と生成物に焼かれた「folio2」の名・外の置き場の始まりの凍結の手順と列の根の表・始まりの凍結が空の承認欄を凍結する罠・行 R-17 が無いと散文の言及の歯が黙ること・値域を狭める変更が黙って通ること・骨格の憲法の雛形が scribe2 の形であること）である。M3 の出口の判定点に直接効くのは列の根の表の行（⑦ = f2-648.192）と承認欄の罠（f2-648.193）で、ほかは面の側か黙りの側である。順は席が決める。
  - 骨格の命令が書く憲法の雛形は scribe2 の形（本来の憲法を指す・根拠の種別は値域の既存の値（scribe2-article）・名は床の受付の宣言（floor-declaration））である。v3 の持ち主は第 1.0 版の承認と凍結の前に、条と前文の字を v3 の字へ、憲法の名を v3 の名へ書き換える。名を凍結の後に変えると列の根の表の行と合わなくなる（ADR-16 の帰結）。本来の憲法を持たない利用者向けの雛形を道具に持つかは、v3 の入口の便の論点である（控え f2-648.196）。新しい置き場（scribe3）の憲法の名は今も床の受付の宣言（floor-declaration）である。
  - 散文の門の参照 id の形は folio2 の 4 種（条・規則の行・要件・契約）のまま、どの置き場でも同じになる。v3 は最初から folio2 の形で書く。参照 id の形を行 R-16 から読む便は起こさないので、要件 FR12 の規範文は今のまま残る。
  - 器（scribe2）の暫定の散文の門は scribe2 の中に残り、scribe2 が前の版の道具で残りを畳むあいだ scribe2 の門として働く。folio2 はその撤去の条件を持たない。規則の表の行 R-16 の注の「揃える向き・撤去の条件は ADR-16 決定 (6) の便が運ぶ」の字は、裁定 id を付けて閉じた字へ直す（承認の後に席が書く）。
  - 要件書の版が 1 つ上がる（M3 の範囲の節の指し先に本判断・要件 FR22 の注・受入基準 AC23〜AC25 の平易文と固定の材料の「scribe2」の字・制約 CON8 の末尾・要件 FR11 の注の s2-07l.473 の字・要件 FR12 の注の撤去の条件の字）。規範文と番号と段の欄は変わらない（条 P-7）。
    受入基準 AC25 の判定の文は、読んだ器の組み立てを名指す記帳を求める字になり、受入の判定が狭まる（決定 (1)・承認要求の 🔴 問 1 に載せた）。
  - 台帳 f2-648.93（器の暫定の散文の門の撤去を器へ要求する）は、folio2 が撤去の条件を持たなくなるので閉じる（決定 (3)）。
  - 判断の記録 ADR-16 の決定の本文は変わらない。読み方は本判断の改訂の欄と、ADR-16 の注の 1 段と平易文の末尾の 1 段が持つ。
  - v3 が folio の実装を同じ repo に合流させると、v3 の置き場は M3 の「外部の利用者」の性質を失う。その時機が判定点 ①〜③ の記帳より先に来たら、本判断の撤退条件 (1) で持ち主へ問う。
  - 本判断の発効は、判断の記録の状態の欄として周の引き金を動かす（どの判断の記録の発効も同じ）。便は起こさない。

retreat:
  kind: measure
  condition: |
    数えるのは orchestrator 席で、台帳 f2-648 の notes に記帳する。v3 の repo は読み取りだけで見る。v3 の台帳の記録は id で名指して写す。
    (1) 外部の前提が崩れる: folio の実装（folio の組み立ての単位のソース）を v3 の repo へ移す取り込みが、M3 の出口の判定点 ①〜③ の記帳が揃う前に着地した回を数える。
    数える口は、v3 の repo の統合先の履歴に folio の組み立ての単位の置き場が現れた最初の commit と、判定点 ③ の記帳の時刻の前後である。
    v3 の置き場の憲法の名（meta.id）が folio2 の憲法の名（folio2-constitution）と同じになった commit が、判定点 ①〜③ の記帳が揃う前に統合先に入った回も、同じ (1) に数える（決定 (1) の 2 つ目の限定）。
    1 回で、条 P-13.4 の「外部の利用者で受入が通った後」の物差しを v3 の置き場で満たせるかを持ち主へ問い、案 b（新しい M3 の判断の記録）と比べ直す判断の記録を起こす。
    (2) 境界が導出物の file を使わない: 判定点 ③ の記帳より前に、v3 の設計文書で「器は契約を導出物の file でなく型で受ける」と決めた判断の記録か設計ノートが発効した回を数える。
    1 回で、判定点 ②③ の物差しを問い直す判断の記録を起こす（案 d と比べる）。
    (3) 判定点 ① が来ない: 本判断の発効の時刻から 30 日のあいだに、判定点 ① の記帳（v3 の置き場の床の合格と撃った folio の組み立ての要約値）が台帳 f2-648 の notes に名指されなかったら数える。
    数える口は台帳 f2-648 の notes である。1 回で持ち主へ問い、案 c（保留）と比べ直す。30 日は、ADR-16 の便 ①〜⑤ が 2026-09-24 に揃い、M3 の口が使われないまま置かれる期間の上限として置く（推測の閾値）。
    (4) scribe2 の移行の再開: scribe2 の台帳が scribe2 の設計文書の folio2 の形への移行の再開を記帳した回（s2-07l.214 か後継の項・folio2 の席が読んで写す）を数える。
    1 回で、ADR-16 の元の読み（利用者 = scribe2）へ戻すかを持ち主へ問う。戻すなら本判断の改訂の欄を取り下げる判断の記録を起こす。
    (5) 数えられない回（v3 の repo が読めない・v3 の台帳が読めない・scribe2 の台帳が読めない）が数値の表の行 R-7 の回数を超えたら、本判断の撤退条件の判定を「まだ分からない」に落として持ち主へ渡す（条 P-4.2・P-11.1）。
    数える時機は、v3 の repo の統合先に新しい commit が入った後の最初の本流の周の前に 1 回とし、(1)(2)(4) もこの時機に数える（(3) は発効から 30 日の時点で 1 回数える）。数えられない回は、どの条件でもその機会ごとに 1 回と数える。v3 の台帳が立つ前の「v3 の台帳が無い」は、数える物が無いとして数えない。
    「同じ」とみなすのは（撤退条件の番号・数えられなかった理由）の 2 つ組が一致する回である。

grill: {when: 2026-09-24, who: 独立の検証役（起草役とは別の文脈の AI・opus 5.5・読み取りと写しの上の実測だけ）と起草役の自己反証, where: ~/.local/share/folio2/handoff-2026-09-24/adr21-verify.md（再実行の道具 adr21-verify-scripts/）と docs/design/adr-21-grill.md（自己反証と改訂 c の当て方の一覧）, summary: 論点 8 つを崩しにかかった。中心（ADR-16 を生かしたまま利用者を v3 の置き場へ読み替え・判定点 ①〜③ を保つ・scribe2 の移行のためだけの論点を閉じる）は支持。条件付きの所は 3 つで、v3 を外部の利用者と数える条件（合流の前に限る）を決定 (1) と撤退条件 (1) に・判定点 ③ で読んだ器の組み立てを名指すことを決定 (1) に・骨格の雛形が scribe2 の形であることを帰結に書いた。残る危うさは v3 の境界の形と合流の時機で、撤退条件 (1)(2) が持つ。独立の検証の総合は条件付き支持で、中心の判断と改訂の欄 5 行の対と向きは支持した。穴は blocking 4（改訂の欄の形が歯を落とす〔改訂 b で閉じた〕・v3 の置き場は既に立ち器の受け口は着地済みという事実の誤り・持ち主の「folio2 で作ってはだめなのか」を問うていない・承認の後の欄の漏れ 4 行）と文面 7 と低い重さ 5 で、改訂 c で全部を席の裁定どおりに当てた。改訂 c の再検証は支持（blocking 0・新しい穴 3）で、改訂 d で当てた。}

approval: {who: 持ち主, date: 2026-09-24, ruling: f2-648 notes 2026-09-24 22:38 JST（発効の承認・本判断の承認要求〔問 2 = v3 の合流先は新しい repo（scribe3）で folio の実装は後から取り込む・問 1 = 発効（案 a）〕への回答・推奨どおり）, verbatim: 承認する, surface: R-8}
note: |
  【出所】scribe2 の orchestrator 席からの連絡（~/.local/share/folio2/handoff-2026-09-24/scribe2-v3-direction.md）と控え 台帳 f2-648.188。
  持ち主の起草の裁定 = 台帳 f2-648 notes 2026-09-24 21:33 JST（🔴 問 6・逐語「すべて承認する」）。発効は本判断の承認要求（docs/design/adr-21.md）で別に問う。
  v3 の入口の材料 = 外の置き場の実測 ~/.local/share/folio2/handoff-2026-09-24/folio2-v3-entry-notes.md と控え 台帳 f2-648.189〜.196。
  【改訂の欄の向き】改訂の欄の向きは 2 つの値しか持たない。決定 (1) の改訂は利用者の名を替えるが、範囲からは設計文書を移す話が外れ、出口の記帳には読んだ器の名指しが足される（出口と数える状態が減る）ので、狭める向きとした。
  決定 (3)(6)(7) と ADR-3 決定 (7) の撤去の句は、scribe2 に結んだ字を当てる範囲から外すので狭める向きである。向きが本当かは人が読む（欄の決まりの revises_note）。
  【撤退条件 (1) の窓】ADR-16 の撤退条件は決定ではないので改訂の欄の対象にならない。窓の起点の読みは決定 (5) に書き、ADR-16 の注で断る（ADR-20 決定 (4) が ADR-13 と ADR-18 の撤退条件の数え方を決めた形に倣う）。
  【改訂 b（2026-09-24）】改訂の欄の 5 行を、先例と同じ 1 行の形に直した（歯 f101）。値は変えていない。
  【改訂 c（2026-09-24・席の裁定）】独立の検証（adr21-verify.md・条件付き支持）の blocking 2〜4 と文面を当てた。
  v3 の置き場は新しい置き場（scribe3）の repo に既に立っている（版 5091abb）ので、文脈 (8)(9)・決定 (5)・帰結をその事実に直し、撤退条件 (3) を「発効から 30 日のあいだに判定点 ① の記帳が無い」に替えた。
  器の受け口 s2-07l.473 は s2-07l.586 へ引き継いで着地済み（版 0337cf1）なので、文脈 (3) と案 d の理由を直した。
  持ち主の「それをfolio2で作ってはだめなのか？」を文脈 (1)(7) に引き、合流先の repo を承認要求の 🔴 問 2 で問う形にし、決定 (1) にその前提を書いた。撤退条件 (1) に憲法の名の限定を足した。
  文脈 (7) の性質の列挙を本判断が組み立てた読みと書き直し、案 b の費用の理由を実測の字にし、撤退条件 (5) に数える時機を書いた。当て方の一覧は grill の §6。
  【改訂 d（2026-09-24・席の裁定）】再検証（支持・blocking 0・新しい穴 3）を当てた。案 c の名を text に揃え、撤退条件 (5) の数える時機が (1)(2)(4) にも掛かり (3) は 30 日の時点で数えることを書いた。問 2 の答えを v3 の席へ返す行と、問 2 を「まだ決めない」と答えたときの扱い、要件 FR11 の注の頭の文の直しは承認要求の側に書いた。
  【発効】2026-09-24 22:38 JST、持ち主が対話面 R-8 で「承認する」と答えた（本判断の承認要求 docs/design/adr-21.md の 🔴 2 問への回答・どちらも推奨どおり）。
  問 2 の答え = v3 の合流先の repo は新しい repo（scribe3）で、folio の実装は後から取り込む（folio2 の repo は v3 に育てない）。決定 (1) の前提は満たされた。問 1 の答え = 本判断の発効（案 a）。
  逐語と日付は承認欄に置き、台帳 f2-648 の notes にも同じ逐語で記帳する。同じ承認で、承認要求 §3 の欄（ADR-16 の注と平易文・ADR-3 の注・要件書 第 1.40 版・規則の表の行 R-16 の注と裁定・語彙の M の項）を書いた。
  問 2 の答えは v3 の席へ ~/.local/share/folio2/handoff-2026-09-24/folio2-v3-answer.md で返した（§3 #20）。ADR-16 と ADR-3 の決定の本文は書き換えていない。
  撤退条件 (3) の起点は本判断の発効の時刻（2026-09-24 22:38 JST）で、ADR-16 の撤退条件 (1) の窓の起点は scribe3 の骨格の版（5091abb・2026-09-24 17:45:49 JST）である。
  【天井の 32 周目の所見への応答（一括 19・2026-09-24 起草・持ち主の承認 2026-09-25 07:03 JST「すべて承認する」）】平易文を 4 か所直した。① 出口の 3 つの確かめを「そのまま」確かめるとだけ言い、決定 (1) が判定点 ③ の記帳に読んだ器の組み立ての名指しを足して受入の判定が狭まったことを落としていた（天井の 32 周目 忠実さ F-2）。
  ② 決定 (2)（ADR-16 決定 (3) を狭める＝置き場と「床の受付の宣言」の形を同じ repo に本来の憲法を別に持つ利用者に限り、v3 の置き場の場所は v3 が選び、v3 の持ち主が骨格の憲法を v3 の条へ書き換えてから第 1.0 版を承認する）が平易文に 1 文も無かった（同 忠実さ F-3）。
  ③ 平易文は発効の前の字（答えはまだ記録に見つからない・発効させず保留）で終わり、発効と問 2 の答えが注にしか無かった（同 読みやすさ F-5）。末尾に発効の後の読み方の段を足し、前の段落は承認要求の時の字のまま残した。④「別の控え 8 件」に台帳の項の id を添えた（同 読みやすさ F-5）。
  見出し・文脈・決定・案・帰結・撤退条件・改訂の欄・承認欄の字は変えない。
  【判断の記録 ADR-22（2026-09-25 発効）】決定 (0) の「scribe2 の設計文書の移行のためだけに置いた論点の閉じ方」は、判断の記録 ADR-3 決定 (8)（M1 で契約表を移す順序と、移す範囲の論点）と ADR-16 決定 (1) の末尾の問う句を名指していなかった（天井の 32 周目 整合 F-1）。この 2 つは判断の記録 ADR-22 の改訂の欄が狭めた。本判断の決定と改訂の欄の 5 行は変えない。
  【天井の 33 周目の所見への応答（一括 20・2026-09-25 起草・持ち主の承認待ち）】① 見出しを前半だけにした（天井の 33 周目 読みやすさ F-2 が、判断の記録 ADR-22 の長い見出しと同じ傾向として本判断の見出しも名指した）。
  前の見出しの逐語＝「M3 の出口の利用者を scribe2 から器の次の世代（scribe v3）の設計の置き場へ読み替え、scribe2 の設計文書の移行に掛かる論点を閉じる — 判断の記録 ADR-16 の決定 (1)(3)(6)(7) と ADR-3 の決定 (7) の撤去の句の範囲を狭める」。外した後半（狭める決定の番号）は改訂の欄の 5 行が持つ。見出しは周の引き金の欄に入らず、判断の記録の改訂にも当たらない（読みは判断の記録 ADR-22 の注の同じ見出しの段と同じ）。
  ② 帰結の 4 つ目の末尾「新しい置き場（scribe3）の憲法の名は今も床の受付の宣言（floor-declaration）である」は、発効の時点（2026-09-24）の字である（天井の 33 周目 実態 F-2）。
  その後、利用者の憲法 第 1.0 版（利用者の repo の版 e52d24c・2026-09-25 発効）は名を scribe3-constitution とし、便 133（台帳 f2-648.202・main 3269ff4）がその名を鍵に列の根の表へ行を足した。
  続いて利用者の持ち主が project の名を scribe3 から tsuzuri に改め、憲法の名は tsuzuri-constitution になった（利用者の repo の版 1c93e70・台帳 f2-648.192 notes 2026-09-25 09:48 JST）。名は列の根の要約値の外なので値は変わらず、表の鍵は便 134（台帳 f2-648.204・2026-09-25 着地・main 33a7dd4・契約 docs/design/delivery-134.md）で tsuzuri-constitution に改めた。
  帰結の 3 つ目が判定点に直に効くと名指す「列の根の表の行（⑦ = f2-648.192）」は、表の行は揃ったが、利用者の始まりの凍結（利用者の側の手番）の記帳まで開いている。今の利用者の名と鍵の段取りの正は要件 FR23 の注である。
  見出しのほかの本文（文脈・決定・案・帰結・撤退条件・改訂の欄・承認欄）の字は変えない。
