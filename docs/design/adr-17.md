# 判断の記録 ADR-17 の承認要求 — 憲法 第 1.3 版（条 N-6 の見出しと前文の根拠の行の字の直し）

- 状態: 承認済み・発効（2026-09-24 11:51 JST「全部推奨で」・台帳 f2-648 notes・対話面 R-8）。憲法は第 1.3 版になった。
- 対象: `design-intent/adr/ADR-17.yaml`（status accepted・起草 2026-09-24・作成 = orchestrator 席〔AI・fable 5.1〕の起草役）。
- 出所: 台帳 f2-648.168。一括 15 の 🔴 問 6（持ち主の承認 2026-09-24 07:46 JST「承認する」＝2 つを束ねた 1 本の判断の記録を起こす）。
- 反対側からの検証（条 A-2.3）: `docs/design/adr-17-grill.md`（検証役・AI・opus・独立の文脈）。総合は **改訂を支持（条件付き）**。条件 10 件は全部、判断の記録へ反映した（同じ file の末尾の表）。
- 起草の時点では憲法の正本を 1 字も変えなかった。承認の後に、下の「承認の後に席が書く欄」の表の全部を 1 つの commit で置いた。

## 要旨（やさしく言うと）

憲法の字を 2 か所だけ直します。守らせる決まりの中身は 1 つも変わりません。

1. **「見せる先」の条（N-6）の見出し。** 今の字は「見せる先を同じ端末の中と tailnet の外へ広げない」です。これは「同じ端末の中へも広げない」とも読めて、本文と逆になります。そこで「見せる先を、同じ端末の中と tailnet の中より外へ広げない」に改めます。要件書の図の名札は、一括 15 で同じ字に揃えてあります。
2. **前書きの「迷ったらどちらへ倒すか」の根拠の行。** 英語の語（walk）を「持ち主が実際に開いて読み、承認済み」という日本語に置き換えます。
3. **前の判断の記録（ADR-12）。** 古い見出しの字が当時の記録として残ります。そこは書き換えず、読み方の断りを注に 1 段落足します。

見出しも前書きも、1 字でも変えるには憲法の改訂の手順が要ります。手順を 2 回に分けないよう、1 回にまとめました。

---

## 🔴 問 1. 判断の記録 ADR-17 を発効し、憲法を第 1.3 版に上げてよいか

**やりたいこと。** 判断の記録 ADR-17 の決定 (1)〜(6) を発効する。字は次の 2 つで、どちらも逐語である。

| 場所 | 今の字（第 1.2 版） | 新しい字（第 1.3 版） |
|---|---|---|
| 条 N-6 の見出し | 見せる先を同じ端末の中と tailnet の外へ広げない | 見せる先を、同じ端末の中と tailnet の中より外へ広げない |
| 前文の根拠の行の 1 行目 | 見本 v4 §0「迷ったらどちらへ倒すか」（walk 承認済み・2026-09-12） | 見本 v4 §0「迷ったらどちらへ倒すか」（持ち主が実際に開いて読み、承認済み・2026-09-12） |

条の id・段・縛る相手・規範文 N-6.1 と N-6.2・前文の規範文と平易文は 1 字も変えない。拒む先の範囲も変わらない。判断の記録 ADR-12 の決定の本文と改訂の欄は変えず、注に断りの段落を 1 つ足す（ADR-17 の決定 (3) の逐語）。

**理由。**

- 見出しの今の字は、「同じ端末の中」と「tailnet の外」を並べた句にも読める。そう読むと「同じ端末の中へも広げない」になり、規範文 N-6.1 と平易文の逆を言う（天井 27 周目の読みやすさ F-4）。
- 見出しは平易文の無い場所にも出る。本流 2d90e8d の上の組み立てで、憲法の面の条 A-1 の関係の欄、判断の記録 ADR-4・ADR-5・ADR-6・ADR-12 の根拠の一覧、要件書 FR7 の根拠に出ると測った。入口の面には出ない。
- 根拠の行の英字の語は語彙に無く、面の用語集で引けない（天井 24 周目の読みやすさ F-1）。一括 12〜15 が 4 回続けて持ち越した。
- 見出しは条文の 5 欄の 1 つで、前文は改訂の範囲の節である。どちらも凍結 anchor の写しに入るので、条 A-2 の手順が要る。持ち主は一括 15 の問 6 で、2 つを束ねて 1 回で運ぶことを承認済みである。

**代替案。** どれも判断の記録の案の欄に、退けた理由付きで在る。

- **(b) 見出しを肯定の形「見せる先を、同じ端末の中か tailnet の中に限る」にする。** 読み違いは起きにくい。しかし段が「絶対にやらない」の条 6 本で 1 本だけ肯定の形になり、要件書の図の名札とも向きが違う。
- **(e) 見出しに規範文 N-6.1 の字を写す（「同じ端末の中でも tailnet の中でもない先へ広げない」）。** 読み違いを最も強く塞ぐ。しかし否定が 2 つ重なり、6 本の見出しで最も長い。撤退条件 (1) の乗り換え先に置いた。
- **(g) 根拠の行を受入基準 AC5 と同じ形（「持ち主が実際に開いて読み（walk）、承認済み」）にする。** 括弧が入れ子になる。受入基準 AC5 が指すのは 2026-09-18 の 3 面の承認で、根拠の行の 2026-09-12 とは別の出来事である。
- **(c) 改訂しない。** 手順も承認も要らない。しかし「絶対にやらない」の条の見出しが 1 つの読みで本文と逆を言う欠陥がそのまま残る。
- **(d) 2 本に分ける。** 一括 15 の問 6 で持ち主が退けた。

**コスト。**

- 持ち主の承認 1 回（この問）。
- 席の取り込みの要求 1 本（下の書き込みの表）。便は要らない。
- 凍結 anchor 1 本（605 行・生成物）が増え、歯 1 本の値が 4 から 5 に変わる。
- 天井の印は正本が変わるので古くなり、次の周で憲法が読み直される。

**部分承認。** 承認は 1 回で求めるが、3 つは別々に退けられる。どれを退けても、残りで第 1.3 版に上げる。

| 退けるもの | 外すもの |
|---|---|
| (1) 条 N-6 の見出し | 改訂の欄の 1 行目・決定 (1) と (3)・撤退条件 (1) と (3)・条 N-6 の改訂来歴の 2 件目・ADR-12 の注の段落・歯 1 本の更新 |
| (2) 前文の根拠の行 | 改訂の欄の 2 行目・決定 (2)・撤退条件 (2)・改訂来歴の理由の同梱の 1 文 |
| (3) ADR-12 の注の断りだけ | 決定 (3) と撤退条件 (3)。ADR-12 は 1 字も変えない |
| (1) と (2) の両方 | 改訂は起こさない。判断の記録 ADR-17 は proposed のまま退役の扱いを別に問う |

**推奨 = 承認する（3 つとも）。**

---

## 承認の後に席が書く欄

全部を 1 本の取り込みの要求で置く。凍結の前に、その時点の本流の上で `folio check --emit-amends` の 2 行と歯の本数を測り直し、判断の記録の改訂の欄と一致することを確かめる。

| file | 欄 | 書く値 |
|---|---|---|
| `design-intent/constitution.yaml` | 先頭の注の 1 行目 | `（v1.2・` を `（v1.3・` に。版の来歴の末尾に `第 1.3 版 <発効日> = 判断の記録 ADR-17（条 N-6 の見出しと前文の根拠の行の字の直し・規範文は 0 本）` を足す |
| 同 | 先頭の注の「状態」の行 | 版の来歴に `、第 1.3 版は条 N-6 の見出しと前文の根拠の行（判断の記録 ADR-17・承認 <発効日>・規範文は 0 本）` を足す |
| 同 | `meta.version` | `v1.3` |
| 同 | `precedence.rationale` の 1 行目の `ref` | 上の表の新しい字 |
| 同 | 条 N-6 の `title` | 上の表の新しい字 |
| 同 | 条 N-6 の `amended_by` | 2 件目を足す。欄の値は判断の記録 ADR-17 の注の「置く 1 件の欄」の逐語で、`ruling` だけ承認の裁定 id と時刻にする。1 件目の ADR-12 の行は変えない |
| `design-intent/adr/ADR-17.yaml` | `status` | `accepted` |
| 同 | `approval` | `{who: 持ち主, date: <発効日>, ruling: f2-648 notes <日付 時刻> JST（…）, verbatim: <逐語>, surface: R-8}` |
| 同 | 先頭の注の「状態」の行 | 発効の日と承認の逐語 |
| `design-intent/adr/ADR-12.yaml` | `note` の末尾 | ADR-17 の決定 (3) の段落の逐語。`<日付 時刻>` を承認の時刻にする |
| `design-intent/anchors/constitution-v1.3.yaml` | 新しい file | `folio check --dir design-intent --freeze-anchor` が書く（手で書かない） |
| `design-intent/anchors/index.yaml` | 末尾 | 同じ命令が 1 項（3 行）を追記する |
| `crates/folio/tests/face_constitution.rs` | 歯 `f80_amendment_rows_carry_the_previous_text` | `assert_eq!(total, 4, …)` の 4 を 5 に |
| 台帳 f2-648 と f2-648.168 の notes | — | 承認の逐語・日付・裁定 id（条 P-12.2） |

## 承認の後の取り込みの要求の書き込みの見積（写しで実測）

本流 2d90e8d の写しで、上の表を全部置いて凍結まで模した。承認の欄と改訂来歴の `ruling` は模擬の字である。

| file | 変わる行 |
|---|---|
| `design-intent/constitution.yaml` | 11 行（足す 6・消す 5） |
| `design-intent/adr/ADR-17.yaml` | 5 行 |
| `design-intent/adr/ADR-12.yaml` | 1 行（注は 1 行の欄） |
| `design-intent/anchors/constitution-v1.3.yaml` | 605 行（新しい file・生成物） |
| `design-intent/anchors/index.yaml` | 3 行 |
| `crates/folio/tests/face_constitution.rs` | 1 行 |
| `CLAUDE.md` | 0 行 |

- **凍結 anchor の写し。** `tests/fixtures` の下の憲法の写しは 26 本在る（`find tests -name 'constitution*.yaml'` の全数）。どれも本物の憲法の写しではない最小の手書きの fixture で、変える字（第 1.2 版の見出しと根拠の行）を持つものは 0 本である。`tests/fixtures/floor_base/` の 2 本は英字の語を含むが、第 1.0 版の形を凍結した fixture なので変えない。書き直す写しは 0 本である。
- **床の事例の期待（`tests/floor_cases.yaml`）。** 0 行。事例は `floor_base` の fixture の上の合成で、本物の憲法を読まない（模擬で `floor_cases_all_pass_with_folio` は合格）。
- **面の fixture。** 0 本。面の歯は本物の憲法を組み立てて数える形で、値を固定しているのは上の 1 本だけだった。
- **生成区間（`CLAUDE.md`）。** 見出しと前文の根拠の行は生成区間に写らない。模擬で `folio inject --check` は 68 行・7912 byte で一致した。

## 凍結 anchor への影響（実測）

**列の根の要約値は変わらない。列の 4 枚目が増えるだけである。**

| 項目 | 今（第 1.2 版） | 承認の後（模擬） |
|---|---|---|
| 索引の項の数 | 3（v1.0・v1.1・v1.2） | 4（v1.3 の previous = v1.2） |
| 根（v1.0）の要約値 | acb52acd…866ed | 同じ |
| 床の定数 `root_digest`（`adr/schema.yaml` と実装の 2 か所） | acb52acd…866ed | 同じ |
| v1.3 の anchor の要約値 | — | 230e2d68…7cecf（模擬の承認の字のとき） |

- v1.2 と v1.3 の anchor の差は 6 つの塊である。先頭の注の版、`version` と `previous`、承認一覧の 1 項（ADR-12 から ADR-17 へ）、前文の根拠の行、条 N-6 の見出し、要約値。
- v1.3 の要約値は承認一覧の字（裁定 id と日付）を覆うので、本物の承認の字で凍結すると模擬の値とは違う値になる。値は `--freeze-anchor` が決め、手で書かない。
- 改訂の欄の 2 行は、`folio check --emit-amends` の印字と 1 字も違わない（版の字だけ v1.2 を v1.3 に替えた）。凍結の後の床は、差分 2 件を改訂の欄 2 行と 1 対 1 に消し込んで合格した。
- 前文は改訂来歴の欄を持たないので、前文の改訂は判断の記録の改訂の欄と憲法の先頭の注の版の行だけが持つ（`adr/schema.yaml` の `limits_note`）。

## 床と歯の結果（2026-09-24 実測）

| 写し | `folio check` | `schema --check` | `inject --check` | 歯（nextest） |
|---|---|---|---|---|
| 本 PR の枝（本流 2d90e8d + ADR-17 proposed） | 合格（違反 0・まだ分からない 0） | 一致（9 file） | 68 行・7912 byte 一致 | 796 本中 796 本 合格 |
| 承認の後の模擬（凍結まで・歯の値を 5 に直した） | 合格（違反 0・まだ分からない 0） | 一致 | 68 行・7912 byte 一致 | 796 本中 796 本 合格 |
| 同じ模擬で歯の値を 4 のまま | — | — | — | 796 本中 795 本 合格。落ちるのは `f80_amendment_rows_carry_the_previous_text` の 1 本（改訂来歴の項 5 と 4 の食い違い） |

凍結の前の模擬の床は「版を上げたのに凍結していない」の 1 件だけで落ち、`--freeze-anchor` の後に合格した。第 1.2 版のときと同じ手順である。

## まだ分からない点

- v1.3 の anchor の本物の要約値。承認の字で決まるので、凍結するまで分からない。
- 次の天井の周が新しい見出しをどう読むか。検証役は、並べた句の読みが文法の上では残ると書いた。撤退条件 (1) がその 1 件を数える。
- 28 周目の束（`~/.local/share/folio2/ceiling/2026-09-24-round28/findings-all.txt`・10:17 JST 時点の 30 行）には、条 N-6 の見出しと前文の根拠の行への所見は無い。周はまだ閉じていないので、承認の前にもう 1 度見る。
