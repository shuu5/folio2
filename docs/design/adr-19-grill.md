# 判断の記録 ADR-19 の反証の試み（条 A-2.3 の形）

- 対象: 判断の記録 ADR-19「不合格の後の往復は、直しの一括の枝の上で周を回し直して閉じる」（proposed・`design-intent/adr/ADR-19.yaml`）。
- 実施: 2026-09-24。**§1〜§4 は独立の検証役ではない。** 席の指示で別の文脈の AI を起こさず、起草役が論点ごとに自分の案を崩しにかかった記録である。起草役と同じ文脈なので、見落としの向きが揃っている恐れがある。承認の前に、席が別の文脈の検証役（読み取りだけ）を起こして数え直させることを勧める（§4）。
- **改訂 b（2026-09-24）**: 席が起こした独立の検証役（別の文脈・opus 5.5）の報告 `~/.local/share/folio2/handoff-2026-09-24/adr19-verify.md`（総合 条件付き支持・穴 9・文面 7）を、席の裁定で全部当てた。当て方の一覧は §5。§1〜§4 は起草の時点の記録として残し、改訂 b で覆った所には「（改訂 b: …）」を添えた。
- 本判断は憲法の条文を改訂しないので、判断の記録の欄の決まりは反証の記録を必須にしていない。それでも、往復の運び方と持ち主の承認の置き方を変えるので、持ち主の指示どおり前置した。
- 数えの script: `~/.local/share/folio2/handoff-2026-09-24/adr19-scripts/`（load.py が周の記録を rounds19.json に落とし、ほかの script が数える）。再実行できる。
- 実測は周 1〜28（28 周目は 2026-09-24 01:17 UTC に終わった）と、main 8066e4c の版管理と、GitHub の取り込みの記録。

## 1. 改訂が主張する利益と、失うもの

- 主張する利益: 不合格の後の往復ごとに挟まる持ち主の承認待ちが消え、承認は連鎖ごとに 1 回になる。持ち主が承認する字が、天井に合格した字になる。
- 失うもの（と疑われるもの）: 持ち主が途中で方向を変える機会。1 回の承認要求が大きくなることによる、持ち主の読みの質。

## 2. 数え直し（別の道で数えた値との突き合わせ）

| 項目 | 本判断の数え（load.py 系） | 別の道（recount.sh・grep で数える） | ADR-18 の数え（1〜27 周） | 結果 |
|---|---|---|---|---|
| 所見 file | 112（28 周 × 4 観点） | 112 | — | 一致 |
| 所見の総数 | 704（止める 40・直す 565・参考 99） | 40・565・99 | 674（28 周目の 30 件を足すと 704） | 一致 |
| 止める の反証 | 支持 32・退けた 8 | 結果 file 31 + 所見 file の欄 1 = 32・4 + 4 = 8 | 支持 32・退けた 8 | 一致 |
| 4 観点とも合格の周 | 8（6・14・15・18・21・23・26・28） | 8 | 7（1〜27 周） | 一致（28 周目が加わった） |
| 周の base | 依頼文の「main <7 桁>」 | 束の写しと版管理の file の byte 一致が最多の commit | — | 2〜28 周の全部で一致（1 周目は依頼文に字が無く、束の写しを正本とした） |

1 周の所要は、ADR-18 が「審査の開始から反証の終わり」で中央値およそ 8 分、本判断が「束を組んでから反証の終わり」で中央値およそ 18 分と測った。始点が違うだけで、どちらも周と周の間（中央値およそ 1.3 時間）より 1 桁小さい。

## 3. 反証の試みと応答

### ① 利益の主張は本当か — 条件付き支持

- 反証の試み: 節約は見かけより小さい。承認待ちの和 9.2 時間のうち 5.75 時間は 10 周目の後の夜の 1 回で、残りは 1.68・1.01・0.46 時間ほか。2026-09-21 以降の連鎖は往復が 1〜2 本で短く、往復が 1 本で閉じる連鎖では何も節約しない。周の回数も 1 周の所要も変わらない。さらに、2026-09-20 までの一括は承認の後に取り込みの依頼を開いていたので、その間の承認待ちは起草の時間と分けられず、9.2 時間は下限である（上にずれるので節約の否定にはならないが、数としては不確か）。
- 応答: 受け入れた。平易文・文脈 (3)・帰結の 1 つ目に「周の回数は減らない」「往復が 1 本で閉じる連鎖では節約はほとんど無い」「9.2 時間は下限で、大半は夜の間の 1 回」と書いた。それでも利益は 2 つ残る。承認の回数（過去の 7 連鎖で 18 回が 7 回）は、連鎖の長さにかかわらず減る（改訂 b: この数は決定 (3) の止める場合を当てておらず過大だった。独立の検証の値はおよそ 16 回 → 10〜11 回・消える承認待ち 1.6〜7.4 時間。§5 の 1）。持ち主が眠っている間も席が枝の上で往復を進められるので、夜をまたぐ連鎖ほど効く。節約が小さいことを撤退の理由にしないのは、一括の行が実装の仕掛けを持たず、残して害が無いためである（撤退条件の末尾）。

### ② 精度を落とさないか — 条件付き支持（1 文を足した）

- 反証の試み 1: 枝の上で持ち主抜きに回し直すのは、偶然の合格が出るまで周を引き直す形になりうる。4 観点の合格は欠陥の無いことを保証しない（支持の 止める 32 件のうち 15 件は、4 観点とも合格した周をまたいで残っていた）。持ち主の承認が往復ごとに挟まっている今は、引き直しの回数に自然な歯止めがある。
- 応答: 受け入れた。決定 (1) ③ に「不合格の周を、直しを足さずに回し直すことはしない」を足した。直さずに回し直してよいのは、実行できなかった回（起動の記録が無い・束が古い・反証が未・止める が全部退けられた再判定待ち）だけにした。回し直しの歯止めは、同じ種類の失敗が規則の表の行 R-7 の回数を超えたら止めて持ち主へ渡すこと（決定 (3) (ア)・条 P-11.1）で持つ（改訂 b: 「直さずに」を禁じるだけでは、止める と関係の無い小さな直しで引き直せた。支持の 止める のそれぞれに直しを当ててから回し、直しの無い回し直しは窓の中で 1 回までに強めた。§5 の 4）。今の形も、直してから回し直す点は同じで、持ち主の承認が引き直しを止めていたわけではない（持ち主の回答は全部推奨どおりだった）。
- 反証の試み 2: 周が読む字は同じでも、承認の前に回すことで、持ち主が推奨と違う案を選んだときに天井が読んでいない字が本流へ入る。
- 応答: その場合は周の引き金の欄が変わるので、門が「まだ分からない」を返し、本流で周を回す（決定 (2)・今と同じ）。引き金の外（注・平易文）だけが変わった場合は、判断の記録 ADR-18 の形のとおり次の引き金の周が読み、門の理由の行と面の名札が「まだ読まれていない」と出す。
- 精度が上がる側: 一括そのものが持ち込んだ字に立った支持の 止める 4 件（9 周目 忠実さ F-1・13 周目 忠実さ F-4 と実態 F-1・16 周目 実態 F-1）は、持ち主が天井に掛かる前に承認していた。本判断では承認の前に枝の上の周が見つける。

### ③ 判断の記録 ADR-18 に依らずに立つか — 立たない（前提として決定 (6) に書いた）

- 反証の試み: ADR-18 が今のまま（設計文書が 1 字でも変われば印が古い）なら、承認の記帳（承認欄・版の来歴・規則の表の裁定の欄）の取り込みだけで枝の上の印が古くなる。本流でもう 1 周が要り、その周が 4 観点とも合格する割合は過去の実測でおよそ 0.29（28 周のうち 8 周）である。合格しなければ新しい連鎖が始まり、もう 1 回承認が要る。本判断は周を 1 本増やし、承認はほとんど減らさない。
- 応答: 受け入れた。決定 (6) で、ADR-18 の案 a が発効しないなら本判断は発効させず取り下げる、と書いた。承認の記帳が ADR-18 の規範の欄の外に在ることは、一括の枝に残っている承認の記帳の取り込み 4 本で確かめた（approval_diff.py）。一括 10 と 11 は変えた欄の全部が規範の欄の外だった。一括 12 は判断の記録 ADR-14 の発効（状態の欄）を含み、一括 13 は本流の取り込みが天井の正本の文書の一覧を動かしていた。前者は判断の記録の発効を含む承認で、決定 (3) の末尾のとおり承認の後の周が残る型である。後者は本流が動いた型で、決定 (2) のとおり本流で周を回す。
- 依存は片向きである。ADR-18 は本判断に依らないので、ADR-18 だけを発効させることはできる。

### ④ 憲法と先の判断に反しないか — 支持

- 条 P-12（承認は対話面 R-8 を通ったものだけ）: 枝の上で推奨の案を書くのは承認ではなく起草である。本流へは承認の後にしか取り込まない。
- 条 P-1.2（folio は採否を代行しない）: folio の口は変えない。運ぶのは席の手順である。
- 条 P-3.3（下位の合格を完成として扱わない）: 枝の上の合格は、本流の完成の判定にも門の判定にも数えない（決定 (4)）。
- 条 P-11.1: 決定 (3) (ア) が行 R-7 で止める。
- 条 N-3.1（例外の口）: 旗も「今回だけ」の口も足さない。手順は一括の全部に同じく掛かる。
- 条 N-2.1: 一括の運び方は今、席の作法と記憶にしか無い（判断の記録 ADR-13 の注 (f) が「規則として縛るなら行を足すことになるが、本判断はそれを求めない」と書いた状態）。本判断はそれを規則の表の行に置く。
- 判断の記録 ADR-8 の決定 (4)（天井が合格でない間は設計文書の便の着地を止める）: 枝の上の往復は便の着地ではなく、本流の印は不合格か古いままで、便は今と同じく門で止まる（改訂 b: 合格の周の後に始まる連鎖では本流の印が新しい合格のままで門が通すので、この応答は偽だった。窓の間は門に依らず設計文書の便を受け付けない、を決定 (4) に置いた。§5 の 2）。
- 判断の記録 ADR-13 の決定 (7)（前の周の判定を持ち越さない）: 変えない。案 a を退けた理由の 1 つである。
- 条 A-1・A-4: 枝の上の作業は取り込みの前で、取り消せる。消す操作が要るときは決定 (3) (ウ) で止める。

### ⑤ 持ち主の判断を先回りしないか — 条件付き支持（残る危険を帰結に書いた）

- 反証の試み: 席が推奨の案で直しを重ねるので、持ち主は途中で方向を変えられない。往復が長い連鎖ほど、1 回の承認要求が大きくなり、持ち主が読み切れずに通す危険が上がる。撤退条件 (1)（推奨と違う案を選んだ一括を数える）は、持ち主が異を唱えた回を数えるだけで、読み切れずに通した回は数えられない。
- 応答: 一部を受け入れた。承認要求には枝の上で回したすべての周の所見と直しと結果を載せる（決定 (1) ⑤）。推奨を 1 つに決められない問いと、条文の改訂・判断の記録の発効か廃止・消す操作は、その場で止めて問う（決定 (3) (イ)(ウ)）。「読み切れずに通した」ことを機械で数える方法は無いので、帰結に残る危険として書いた。過去の往復の最長は 7 本（7〜14 周）で、行 R-7 の止め方があるので、枝の上の往復がそれを大きく超えることは起きにくい。

### ⑥ 案 a（合格した観点の持ち越し）を救えないか — 救えない

- 反証の試み: 観点の束の写しが前の周と同じときだけ持ち越せば、精度の損は無い。節点の要約値が同じ節だけを持ち越せば、節約も出る。
- 応答: 前の周に合格した観点の束の写しが次の周でも同じだった回は、不合格の後の 51 回のうち 0 回だった（直しの一括は、どの観点も読む要件書を必ず動かす）。節点の単位で持ち越すと、持ち越した観点が実際に出していた支持の 止める 8 件のうち前から在った字の 6 件を見逃す。束を変わった所に絞る形は、判断の記録 ADR-13 の決定 (5)（束を影響集合で切らない）と (7) の改訂にもなる。

### ⑦ 案 b（総点検の前置）を救えないか — 構造としては救えない・1 回の候補として残す

- 反証の試み: 2026-09-20 の平易文の総点検の後、前から在った字への忠実さの支持の 止める は 9〜28 周で範囲の外の 1 件しか出ていない。効く範囲はある。
- 応答: 効く範囲はある。しかし約束と実態の総点検の後も、前から在った字への実態の支持の 止める が 5 件残った。2 回の総点検の後も連鎖は 5 周続き、総点検の直しの一括そのものが 9 周目の 止める を持ち込んだ。周のたびに総点検を前置すると手間と承認が増える。範囲を決めた 1 回の総点検（一度も総点検していない文書どうしの整合）は、本判断の外の候補 ④ として注に残した。

### ⑧ 案 c（退けた 止める の読み替え・読みやすさの重さ）と案 f（多重読み）— 退けを保つ

- 案 c: 過去 28 周で、この読み替えで 4 観点とも合格に変わる周は 0 周だった。読みやすさの 止める 5 件は全部退けられたが、4 件は 1 周目で観点の判定は合格のまま、1 件（10 周目）の周は整合と実態も不合格だった。節約は反証の起動の数だけである。
- 案 f: 模型（model_f.py・独立を仮定した上限側の見込み）で、前から在る欠陥が 4 件のとき、不合格の後の周は k = 1 で平均 0.8 周、k = 3 で 1.8 周に増え、合格の周に残る欠陥は 3.2 件から 1.4 件に減る。往復を短くする問いとは逆向きである。ただし本判断の形では往復が席の手だけで閉じるので、多重読みで往復が増えても持ち主の承認は増えない。精度の側の手として、影の 2 本目の審査役で実測する候補 ③ を注に残した。

### ⑨ 撤退条件が数えられるか — 支持

- (1) 持ち主の回答の逐語と承認要求の推奨の突き合わせで数えられる。(2) 決定 (3) (ア) で止めた連鎖は台帳に記帳する。(3) ADR-18 の便アの着地の後の門の返事の理由の行で数えられる。窓の起点（一括の行の裁定の時刻）と終わり（7 日か連鎖 3 本の遅い方）を書いた。発効の当日に満たされる条件は無い（改訂 b: 起点を ADR-18 の便アの着地に移し、(2) は窓の中の通しの数えで決定的に数えられるようにした。§5 の 3・9）。

## 4. 残ること

- この記録は起草役の自己反証で、独立ではない。承認の前に、席が別の文脈の検証役（読み取りだけ）を起こし、`adr19-scripts/` を見ずに次の 4 点を数え直させることを勧める。① 往復の承認待ちの和（取り込みの依頼を開いた時刻から取り込みまで）② 支持の 止める の字の生まれ ③ 案 a の再生 ④ 承認の記帳の欄の分類。
- 検出の割合の推定 0.17 は、字の生まれを欠陥の生まれの上限の目安として使ったので低めに出ている。

## 5. 改訂 b — 独立の検証の穴 9 と文面 7 の当て方（2026-09-24・席の裁定）

- 検証: `~/.local/share/folio2/handoff-2026-09-24/adr19-verify.md`（別の文脈の検証役・総合 条件付き支持）。数えの script は同じ置き場の `adr19-verify-scripts/`。
- 当て方は席の裁定（2026-09-24）に従った。判断の骨（周の中身を変えない・承認する字を天井に合格した字にする・ADR-18 の案 a への片向きの依存）は変えていない。

| # | 検証の穴 | 当てた所 | 当て方 |
|---|---|---|---|
| 1 | 利益の数が過大（決定 (3) を再生に当てていない・承認でない取り込みを 2 本数えた） | 平易文・文脈 (3)(6)・案 d と e の理由・帰結・承認要求 §1 §2・注の【利益の数の改め】 | 検証役の値へ置き換えた＝承認およそ 16 回 → 10〜11 回・消える承認待ち 1.6〜7.4 時間・止める所は発効 3 か所と (ア) の 3 つ組 1 か所。決定 (3) (ア) の通しの数えでは下限の 1.6 時間と明記。起草役の 18 → 7 回・9.2 時間は「再生の当て方が違った」と注に残した |
| 2 | 合格の周の後に始まる連鎖で、本流の印が新しい合格のままなので門が設計文書の便を通す | 決定 (4)・行 D-14 の what と note | 窓の間は、門の判定に依らず、本流の設計文書を書き換える便を受け付けない（順序の規律）。ADR-8 決定 (4) の趣旨を保つためと理由を書いた |
| 3 | 決定 (3) (ア) の窓と再開が未定 | 決定 (1)（窓の定義と ⑦ 再開）・決定 (3) (ア) の数え方・行 D-14 | 窓 = 一括の枝を切ってから本流へ取り込むまで。窓の間に本流の規範の欄の要約値が動いたら枝を載せ替えて回し直す（再開）。動いたかは要約値の比較で決める（便アの前は印の正本の全体の要約値）。(ア) は窓の中で通して数え、きっかけの周を含め、再開でも戻さない。止めて出した枝は承認の後に窓を閉じ、次の窓で数え直す |
| 4 | 決定 (1) ③ は「直さずに」を禁じるだけで、関係の無い小さな直しで引き直せる | 決定 (1) ③・(3) (エ)・行 D-14 | 理由になった支持の 止める のそれぞれに直しを当ててから回す。直しの無い回し直しは実行できなかった回だけで窓の中で 1 回まで。2 回目は往復を止めて持ち主へ |
| 5 | 承認待ちの間に着地した実装を周が読まない（「精度を 1 つも落とさず」は言い過ぎ） | 案 e の理由・帰結・承認要求 §2 | 「精度を 1 つも落とさず」を消した。窓の間は設計文書の便を止めるので、読まない対象は実装の便だけ＝実態の観点は着地の時点の本流を次の引き金の周が読む、と帰結に書いた |
| 6 | 1 回の承認要求を分ける基準が無い | 決定 (1) ⑤・行 D-14・帰結 | 新しい閾値行は足さない。1 回の承認要求 = 本流の 1 つの周の所見から切った 1 つの一括（周をまたいで束ねない）を行 D-14 に置いた |
| 7 | 注だけの是正を承認なしで取り込む今の作法を残すか | 決定 (1) ⑧・行 D-14・語彙の文案 | 残す。持ち主に問う項を持たず規範の欄の要約値を動かさない一括は、枝の上の周の合格の後に承認を求めず記帳して取り込む。区別は要約値の比較で決め、席が選ぶ旗を持たない（条 N-3.1）。枝の上で周を回すことはこの作法と衝突しない（周が読むのは枝の正本）と明記した |
| 8 | 便でしか直せない所見（生成区間）の手順が無い | 決定 (1) ⑨・(3) (オ)・行 D-14 | 台帳に控えとして起票した 1 件に束ね、窓を閉じた後、次の周の前に便で運ぶ（今の運用・f2-648.171 の形）。その型の 止める で枝の上の周が合格できないときは止めて持ち主へ（窓の間は設計文書の便を受け付けないため） |
| 9 | 撤退条件の窓の起点が行の裁定の時刻で、便アより前に閉じうる | 撤退条件 | 数える期間の起点を ADR-18 の便アの着地の時刻に移した（決定 (1) の「窓」と紛れないよう「数える期間」と呼ぶ） |
| 10 | 行 D-14 の条と関係の欄（写しの床で 3 欄が欠けた）・`stage: post` が行の欄に無い | 決定 (5)・承認要求 §3 §4・帰結 | 行の条は P-11 のまま、憲法の条 P-11・P-3・P-12 の relations.rules に D-14 を足す。FR20 の rules を [D-12, D-14] に。D-14 の refs に ADR-13 と P-3・P-12。`stage: post` と「段 post」を外した |
| 11 | 取り込みの順と床 | 承認要求の冒頭と §5 | 取り込みの順 ADR-17 → ADR-18 → ADR-19 を明記した（歯の 1 本が判断の記録の番号の連番を仮定）。この歯の仮定と、床が行の欄の閉じた一覧を数えていない件は、席が別に起票する（本判断には書かない） |
| 12 | 文面 7 点 | 各所 | ① 数（上の 1）② 精度の言い過ぎ（上の 5）③ `stage: post` を外した（上の 10）④ note の例外の定めを what へ移した ⑤ FR20 の注の 1 文に「便アの着地の後は…・便アの着地までは承認の後に本流で周を回す」の断りを入れた ⑥ §3 に 3 行を足し「末尾の注」を「末尾のコメント」に直した ⑦ 比べる母集団を揃えた（不合格の後の 20 周の所要 6.3 時間・およそ 9 倍） |

### 床で確かめたこと（改訂 b）

- 枝（docs/adr18 の上・本流 628c547 を取り込み済み）で `folio check --dir design-intent` 合格（違反 0・まだ分からない 0）・`folio schema --check` 一致・`folio inject --check` 一致。`folio build` で adr-19.html が組める。
- 承認の後に席が書く欄（§3・§4 の文案そのもの）を写しの上で全部当てても、同じ 3 つが合格・一致だった（写しを当てる script は起草の記録 adr19-draft.md に置き場を書いた）。

## 6. 改訂 c — 改訂 b の再検証の残った穴 4 と文面 6 の当て方（2026-09-24・席の裁定）

- 再検証: 同じ報告 `adr19-verify.md` の「改訂 b の再検証」の節（総合 条件付き支持・前回の穴 9 のうち 6 が閉じ・残り 4・文面 6）。

| # | 残った穴 | 当てた所 | 当て方 |
|---|---|---|---|
| 1 | 利益の数に止める場合 (オ) を当てていない（4 周目・22 周目の実態の 止める は生成区間の字で、便で直していた） | 平易文・文脈 (6)・案 d と e の理由・帰結・注の【利益の数の改め（改訂 c）】・承認要求 §1 §2 §6 | 承認はおよそ 16 回 → 12 回（(ア) が当たらなければ 11 回）。消える承認待ちは 1.6 時間から (オ) の新しい待ちを引いた値（その長さは過去の記録に無い）。本判断の字の値を組で書いた |
| 2 | 決定 (4) が 2026-09-22 00:0x JST の持ち主の裁定（設計文書を触る便は印が古くても門を経ずに出す・便 101・103・117・119・123 に適用）とぶつかる | 決定 (4)・注の【2026-09-22 の裁定との関係】・帰結・承認要求 §1 §2（🔴 に「承認すると置き換わるもの」）・行 D-14 の note | 「本判断の発効で 09-22 の裁定を置き換える＝窓の外では ADR-18 の案 a の門で判定し、窓の中では設計文書を書き換える便を受け付けない。09-22 の裁定は本判断の発効まで有効」を決定 (4) に書き、出所（台帳 f2-648 notes 2026-09-22・持ち主の逐語）を注に残した。「不合格の周から始まった窓では門でも止まる」は事実と違うので消した。便アの着地までは門が今の式で、印が古いと設計文書の便が止まることも帰結に書いた |
| 3 | 器の列に在る設計文書の便が窓の間に自動で起動しうる | 決定 (4)・行 D-14 の what と note | 窓を開くとき、席は設計文書を書き換える便の台帳の件を器の列で保留にし、窓を閉じたら解放する（便 125 を保留にした形と同じ・席の手順・器の仕掛けは増やさない） |
| 4 | (ア) の数え方が行の note にしか無く、「所見」の範囲が未定 | 決定 (3) (ア)・行 D-14 の what | 数える失敗 = 反証で支持された 止める の所見で、観点と場所の組で同じと見る。場所は所見の置き場の file、判断の記録と設計ノートは場所の頭の id が指す file。1 つの周の中の同じ組は 1 と数える。通しの数え・きっかけの周を含む・載せ替えでも戻さない・止めた後は次の窓で数え直す、を what へ移した |
| 5 | 読み方 3（FR20 の adrs に ADR-18）の理由 | 承認要求 §4 | 床は求めない。ADR-18 が FR20 を改訂する判断なので、その改訂で入る、と理由を直した |
| 6 | 文面 6 点 | 各所 | ① 数（上の 1）② 09-22 の裁定と「門でも止まる」の直し（上の 2）③ 列の保留（上の 3）④ (ア) の数え方を what へ（上の 4）⑤ 帰結の歯止めを「(ア)(エ) だけ・⑤ は並行する窓を束ねないだけ」に直し、(ア)〜(オ) で止めて出す枝は 4 観点合格でない例外と決定 (3) と行 D-14 に書いた ⑥ §4 の理由（上の 5）と、直しを当てない回し直しを 1 回までにした理由を注と行 D-14 の note に書いた |

### 床で確かめたこと（改訂 c）

- 枝で `folio check --dir design-intent` 合格・`folio schema --check` 一致・`folio inject --check` 一致。承認の後に席が書く欄（§3・§4 の文案そのもの）を写しに当てても 3 つとも合格・一致。
