# 判断の記録 ADR-20 の承認要求 — 周の引き金の「辺」の断りを狭め、憲法の前文と schema 節を引き金の一覧に入れる

- 状態: 起草・承認待ち（2026-09-24・改訂 b 2026-09-24・orchestrator 席 fable 5.1 の起草役）。
- 正本: `design-intent/adr/ADR-20.yaml`（status: proposed）。反証の試み: 独立の検証役の検証 `~/.local/share/folio2/handoff-2026-09-24/adr20-verify.md`（別の文脈・総合 条件付き支持・blocking 4・文面 6・低い重さ 3）と、起草役の自己反証 `docs/design/adr-20-grill.md`。改訂 b は検証の指摘を席の裁定で全部当てた（当て方の一覧は grill の §6）。
- 起点: 天井の 30 周目（一括 17 の枝の上）の文書どうしの整合 F-1（止める・反証で支持）・F-4（直す）・F-7（直す）と、一括 17 の問 8（`docs/design/batch17-triage.md`）。同じ食い違いを **31 周目（本流 `ca60873`）の整合 F-1（止める・反証で支持）と F-2** も拾った。問 8 であなたが承認したのは**解く向き**で、正本の字はまだ 1 字も動いていない。
- 枝: `docs/adr20` は本流 `ca60873`（一括 17 の取り込み・PR #306）の上に在る（起草は一括 17 の枝の承認の記帳 `0e7901c` の上で行い、取り込みの後に載せ替えた）。参考値の base は `ca60873`。一括 18（枝 `docs/batch18`）はこの枝を土台にしている。
- 憲法の条文・前文・schema 節は 1 字も変えない。**判断の記録 ADR-18 と ADR-13 の決定の本文も書き換えない**（改訂 b）。発効の後に便が 1 本要る（M・§5）。
- **🔴 は 2 問**（問 1 = 発効してよいか・問 2 = 発効の後の便を、門が まだ分からない のまま器へ出してよいか）。

## 1. 要旨（平易に）

- 天井の周をいつ回すかの決まり（判断の記録 ADR-18）の中で、2 つの文が食い違っていた。
  - 決定 (1) は、受入基準が「どの要件を確かめるか」、要件が「どの受入基準で確かめるか」、規則の表の行が「どの条を縛るか」を、周を回すきっかけ（規範の欄）に数える。
  - 決定 (2) は、「つながり（辺）を足すだけの変更では周を回さない」と言う。ところがこの 3 つは、設計文書の索引ではつながりの欄として扱われている。
  - だから同じ変更に、決定 (1) は「回す」、決定 (2) は「回さない」と逆の答えを出していた。機械（天井の門）はもう決定 (1) のとおりに動いている。**30 周目と 31 周目が続けてこの食い違いで止まった。**
- この判断では、**決定 (1) を正しい側とし、決定 (2) の断りを狭める。** 3 つの欄を書き換えたら周を回す。ほかのつながり（根拠・参照・条どうしの関係など）に番号を足すだけなら、今までどおり回さない。
- あわせて、**憲法の前文（条が衝突したときの順位）と憲法の決まりの節（schema 節）を、周のきっかけの一覧に入れる。** 今は一覧に無く、書き換えるときに必ず伴う判断の記録の発効がきっかけになって、間接に周が回っていた。一覧に書いて機械が直接数えるようにする（便が 1 本要る）。
- **ADR-18 の決定の文そのものは書き換えない。** 発効した判断の記録なので、この判断の「改訂の欄」と ADR-18 の注の 1 段で断る。一括 17 の問 8 で説明したとおりで、今までの判断の記録のやり方と同じである。
- 周のきっかけが漏れたときの撤退条件（ADR-13 と ADR-18 に 3 つ）は、どれも字のまま数え、同じ件は両方に数える。

**先に言っておくこと: この判断で回す周の数は変わらない。** 過去 29 回の周と周の間に当てると、3 つの欄だけが変わった間は 1 度も無く、前文が変わった 2 回はどちらも判断の記録の発効と一緒だった。この判断は、文書の中の食い違いを消して、次の周が同じ所で止まらないようにするものである。独立の検証が実物の道具で別に数え直し、数は全部一致した。

| 過去 29 区間（周 2〜31）への当てはめ | 値 |
|---|---|
| 3 つの欄（確かめる要件・確かめ方の中の受入基準・行の条）が変わった区間 | 11 |
| そのうち、3 つの欄**だけ**が変わった区間（この判断で周の数が変わりうる区間） | 0 |
| 前文か憲法の決まりの節が変わった区間 | 2 |
| そのうち、判断の記録の発効と同じ区間 | 2 |
| 周のきっかけが動かなかった区間のうち、ほかのつながりの欄が変わった区間 | 0 |

## 2. 🔴 持ち主へ問う項（2 問）

### 🔴 問 1: 判断の記録 ADR-20 を発効してよいか

- **やりたいこと**: 判断の記録 ADR-20 を発効する（案 d）。発効の後に、席が §3 の欄を書き、§5 の便 1 本を起こす。
  - ADR-18 の決定 (2) の「辺を足すだけの変更を引き金に数えないことは変えない」を、ADR-20 の改訂の欄（狭める向き）で「規範の欄に入らない辺の欄に限る。3 つの欄は引き金に数える」と読む。
  - ADR-18 の決定 (1) ① に、ADR-20 の改訂の欄（広げる向き）で前文と憲法の schema 節を足し、引き金の一覧の型付きの定数の憲法の写しの範囲を「条文だけ」から「schema 節・前文・条文」へ広げる。
  - ADR-18 と ADR-13 の決定の本文は書き換えず、両者の注に 1 段ずつ断りを書く。ADR-18 の改訂の欄の ADR-13 決定 (8) の要約の括弧だけは、問 8 の推奨の字どおりに狭める。
  - 引き金の漏れの撤退条件（ADR-13 の (6)・ADR-18 の (1)・(3)）は字のまま数え、同じ件は両方に数える。
- **理由**:
  - どの受入基準がどの要件を確かめるか・規則の行がどの条を縛るかは、完成の物差しと規則の縛る先そのもので、読まずに通すと精度が落ちる（ADR-18 決定 (0)）。実装はもうこの側で動いている。
  - 前文と schema 節を型付きの一覧に入れると、ADR-18 決定 (1) の「一覧の正本は実装の型付きの定数」と一覧の中身が 1 対 1 になる。入れない形（案 a）は、同じ決定の中に「一覧の正本は定数」と「前文と schema 節は定数に入れない」を並べる新しい食い違いを生む。問 8 のコストの欄であなたに見せた形も、定数に足す便だった。
  - 決定の本文を書き換えないのは、問 8 の説明（発効した ADR-18 の決定の字は書き換えない）と、判断の記録の欄の決まり（範囲の改訂は改訂する側の片側に置く）と、同じ日の先例 ADR-17（ADR-12 の決定を書き換えず注で断った）に揃えるためである。31 周目の反証は「後の決定が前を上書きする形になっていない」ことを解けない理由に挙げた。改訂の欄の狭める向きで上書きの形にすれば解ける見込みが高い（確率的な審査なので保証ではない）。
- **精度の損は 0 ではない**: 便の着地までは、索引の注の「この欄に id を足すだけの変更は周の引き金にならない」の古い字が残る。便の前に周を回すと同じ型の 止める が立つ見込みが高いので、問 2 で便を先に出す道を問う。
- **代替案**:

| 案 | 中身 | 過去への当てはめ | 費用 | 失うもの |
|---|---|---|---|---|
| **d（推奨）** | 決定 (2) の断りを 3 つの欄だけ狭める。前文と schema 節を型付きの一覧に足す。決定の本文は書き換えない | 周の数は変わらない | 便 1 本（M・約 29 file）。発効の後の周 1 本（どの案でも要る） | 無し（定数の範囲を広げる上乗せは、門の歯 6 本と定数と生成区間の 1 行） |
| a | d と同じだが、前文と schema 節は定数に足さず、発効した判断の記録の改訂の欄との結び付きで数える | 周の数は変わらない（前文の 2 区間は結び付きで回った） | 便 1 本（約 29 file・注の字だけ）。発効の後の周 1 本 | 決定 (1) の中に新しい食い違い。床を経ない取り込みでは前文の変更が周を起こさない |
| f | d と同じ中身で、ADR-18 の決定 (1) ① と (2) の本文そのものを書き換える | 同上 | d と同じ | 問 8 の説明と先例に反する。ADR-13 決定 (8) の同じ趣旨の字を書き換えない扱いと両立しない |
| b | 3 つの欄を周のきっかけから外す（決定 (2) と語彙と索引の注はそのまま） | 周の数は変わらない（3 つの欄だけの区間 0） | 実装の便 1 本 | 完成の物差しの結び付きの変更を読まずに通す |
| c | つながりの断りを全部外す（根拠・参照に番号を足しても周を回す） | 周の数は変わらない（その区間 0） | 実装の便 1 本 | つながりを書き足す作業のたびに周が要る（2026-09-22 のあなたの裁定 ④ への答えを覆す） |
| e | 決定の字を変えず、注で「(1) が (2) に優先」と断る | — | 便 0 | 両立しない 2 文と、語彙・索引の注の逆の答えが残る（問 8 で退けた） |

- **コスト**: この承認は 1 回（一括 18 と同じ回で求める）。承認の後に、席が §3 の欄を書き（要件書の版が 1 つ上がる・規範文と番号は不変）、§5 の便 1 本（M）を起こす。ADR-20 の発効と便の定数の変更は周のきっかけなので、便の着地の後に本流で周が 1 本要る（どの判断の記録の発効も同じ）。
- **推奨**: d。

回答の例: 「推奨で」「a で（前文と schema 節は定数に足さない）」「f で（ADR-18 の本文も書き換える）」「b で」。

### 🔴 問 2: 発効の後の便を、門が まだ分からない のまま器へ出してよいか

- **やりたいこと**: §5 の便（生成区間の注の字と、案 d の定数と関数）を、天井の門（`folio ceiling --gate`）が「まだ分からない（印が古い）」を返したまま器へ出す。規則の表の開発規律行 D-12 の「まだ分からない のときは出さず、所見の解消か持ち主の裁定を先に取る」の、持ち主の裁定の道を使う。裁定の範囲はこの 1 本の便だけとする。
- **理由**:
  - 本流の印は 28 周目のもので、引き金の要約値の欄を持たない。門は今も、ADR-20 の発効の後も、rc 2（まだ分からない・印が古い）を返し続ける（独立の検証の実測）。便は設計文書の置き場の生成区間（graph.yaml・ceiling.yaml）を書き換えるので、門を経ずには出せない。
  - 便の前に周を回すと、索引の注の古い字（「この欄に id を足すだけの変更は周の引き金にならない」）が ADR-18 の新しい読みと食い違ったまま読まれる。30・31 周目の F-1 はこの注を食い違いの側に引いたので、同じ型の 止める が立つ見込みが高い。そうなると行 D-14 の「枝に書けない所見の 止める で合格できない」で、またあなたへ戻る。
  - 2026-09-22 のあなたの裁定（設計文書を触る便は印が古くても門を経ずに出す）は、判断の記録 ADR-19 決定 (4) により ADR-18 の便アの着地（2026-09-24）の時点で置き換わった。前例として当てることはできないので、改めて問う。
- **代替案**: 先に本流で 32 周目を回し、印を付け直してから便を門に通す。印が付くには 4 観点とも合格が要り、索引の注の古い字で整合が 止める を出す見込みが高いので、周が 1〜2 本増え、あなたへの問いがもう 1 回増える見込みである。
- **コスト**: この裁定は 1 回。便が門を経ずに着地するので、便の中身（生成区間の注と定数）は着地の後の 32 周目が初めて読む。その周の前に床 4 本と歯の全部は通す。
- **推奨**: 出す裁定（この 1 本の便に限る）。

回答の例: 「問 2 も推奨で」「問 2 は先に 32 周目を回す」。

## 3. 承認の後に席が書く欄（本判断の枝ではまだ書かない）

| # | file | 欄 | 書くこと |
|---|---|---|---|
| 1 | `design-intent/adr/ADR-20.yaml` | status・approval・note・頭の注釈 | accepted と、あなたの逐語・日付・裁定 id・対話面 R-8。note に【発効】の段 |
| 2 | `design-intent/adr/ADR-18.yaml` | revises の ADR-13 (8) の summary の括弧 | 「（辺を足すだけの変更を数えないことは変えない）」を「（辺を足すだけの変更を数えないことは、規範の欄に入らない辺の欄に限って残す・判断の記録 ADR-20）」へ（問 8 の推奨の字どおり） |
| 3 | 同 | plain（平易文）の規範の欄の並び | 「憲法の条文、発効した判断の記録（…」を「憲法の条文と前文と schema 節、発効した判断の記録（…」へ（§4 の 1） |
| 4 | 同 | note | 30 周目の段の「その向きの新しい判断の記録（次の番号）を起草する」を「その向きの判断の記録 ADR-20 を起草した」へ。末尾に【判断の記録 ADR-20 による改訂】の段（§4 の 1）。**決定 (1) ① と (2) の本文は書き換えない**。見出し・文脈・決定・案・帰結・撤退条件・承認欄の字は変えない |
| 5 | 席の判断 | ADR-18 の頭の注釈 | 書き換えない（注の段で足りる） |
| 6 | `design-intent/adr/ADR-13.yaml` | note の 30 周目の段の末尾 | ADR-20 決定 (4) の数え方と、決定 (8) の辺の断りを ADR-20 の改訂の欄で狭めて読むことを 1 段（§4 の 2）。**決定 (8) と撤退条件の本文は書き換えない** |
| 7 | `design-intent/vocabulary.yaml` | 周の引き金（round-trigger）の定義の末尾の文 | §4 の 3。定義の本体の一覧（「憲法の条文・…」）は ADR-18 決定 (1) を指す要約なので触らず、末尾の文に前文と schema 節を書く |
| 8 | `design-intent/srs.yaml` | 要件 FR20 の注・adrs・meta（版・状態の注・承認欄） | 注の末尾に §4 の 4 の 1 文。adrs に ADR-20。版を 1 つ上げる（規範文と番号は不変・条 P-7）。**一括 18 の第 1.39 版の後の字に当てる**（版は記帳の時に席が決める） |
| 9 | 便（§5） | 引き金の定数と関数・`graph.yaml` の edge_fields_note・`ceiling.yaml` の trigger_note と引き金の一覧の写し（生成区間） | 字の正本（実装の文字列と定数）と凍結の写しと歯を直し、`folio schema --write` で生成区間を書き直す（§4 の 5） |
| 10 | 台帳（`bd`） | f2-648 notes・f2-648.181 | 承認の逐語と日付（条 P-12.2）・問 2 の裁定。撤退条件を数える期間の起点 = 便の着地の時刻。f2-648.181 を閉じ、便の項を起こす |
| 11 | `docs/design/state.md` | 判断の記録の行・次の一手 | ADR-20 の発効と、便と、便の後の本流の 32 周目 |
| 12 | `design-intent/srs.yaml` | 要件 FR20 の plain（平易文）の規範の欄の並び | **一括 18 の後の字**（読みやすさ F-4 で並び替えた字）の「憲法の条文」の後に「と前文と schema 節」を足す |

- #2〜#4・#6〜#8・#12 を写しの上で当てて床を撃つと、合格（違反 0・まだ分からない 0）で、`schema --check` と `derive --check` は一致だった（模擬の script `~/.local/share/folio2/handoff-2026-09-24/adr20-scripts/post_approval_sim.py`・改訂 b の字・要件書の版上げと #9 は当てていない・#12 は今の本流の字に当てた）。
- 独立の検証は、起草の時点の §3（決定の本文の書き換えを含む）に要件書の版上げを足して模擬し、床 4 本が rc 0・nextest 855 / 855 だった。

## 4. 文案（承認の後に正本へ入れる字）

### 1. ADR-18 の平易文と注（#3・#4）

- 平易文: 「憲法の条文、発効した判断の記録（」→「憲法の条文と前文と schema 節、発効した判断の記録（」
- 注の末尾の段:

> 【判断の記録 ADR-20 による改訂（<日付> 発効・持ち主の承認 <時刻> JST「<逐語>」）】決定 (1) ① の規範の欄は、判断の記録 ADR-20 の改訂の欄（広げる向き）で、条 A-2 の改訂の範囲に在る憲法の前文と schema 節を含む（引き金の一覧の型付きの定数の憲法の写しの範囲を ADR-20 の便で改訂の範囲へ広げた）。決定 (2) の末尾の文「辺を足すだけの変更を引き金に数えないこと（同 決定 (8)）は変えない。」は、ADR-20 の改訂の欄（狭める向き）で、規範の欄に入らない辺の欄（憲法の条の関係の欄・規則の表の行の参照の欄・要件書の根拠と目的と規則と判断の記録の欄・判断の記録の根拠と帰結の欄）に限って読む。受入基準の確かめる要件（verifies）・要件と非機能要件の確かめ方の中の受入基準（verify.ac）・規則の表の行の条（article）へ id を足すだけの変更は引き金に数える（天井の 30 周目 整合 F-1・F-4 と 31 周目 整合 F-1・F-2）。決定の本文は発効した判断の記録の字なので書き換えず、改訂の欄は ADR-20 の側に在る（判断の記録の欄の決まりのとおり片側だけ）。平易文の規範の欄の並びに前文と schema 節を足した。見出し・文脈・決定・案・帰結・撤退条件・承認欄の字は変えない。

### 2. ADR-13 注の 30 周目の段の末尾（#6）

> その後、判断の記録 ADR-20（<日付> 発効）の決定 (4) が数え方を決めた＝(6) は字のまま自分の数えで数え（効力は変えない）、ADR-18 の撤退条件 (1)・(3) も字のまま数える。同じ件が (6) と ADR-18 の (1) の両方に当たれば両方に数える。(3) も字のまま数える。決定 (8) の「辺を足すだけの変更は引き金に数えない」は、ADR-20 の改訂の欄（広げる向き）で、規範の欄に入らない辺の欄に限って読む（受入基準の確かめる要件・要件の確かめ方の中の受入基準・規則の表の行の条へ id を足す変更は引き金に入る）。決定と撤退条件の本文は変えない。

### 3. 語彙「周の引き金」の末尾の文（#7）

- 今: 「辺を足すだけの変更は引き金に数えない（判断の記録 ADR-13・ADR-18）。」
- 改めた後: 「辺を足すだけの変更は、規範の欄に入らない辺の欄（根拠・参照・条どうしの関係など）なら引き金に数えない。受入基準の確かめる要件・要件の確かめ方の中の受入基準・規則の表の行の条は規範の欄に入るので数える。憲法の前文と schema 節も規範の欄に入る（判断の記録 ADR-13・ADR-18・ADR-20）。」

### 4. 要件 FR20 の注に足す 1 文（#8・規範文は変えない）

> 引き金の規範の欄のうち受入基準の確かめる要件・要件の確かめ方の中の受入基準・規則の表の行の条は、索引では辺の欄だが引き金に入り、憲法の前文と schema 節も引き金の一覧に入る（判断の記録 ADR-20・天井の 30 周目と 31 周目の整合の所見）。

### 5. 便で直す生成区間の字（#9・字の正本は実装の文字列と定数）

- 索引の欄の決まりの `edge_fields_note`（字の正本 `crates/folio/src/graph.rs` の床の木の文字列）:
  - 今の 2 文目「節点の要約値はこの欄を落とした本文だけを数えるので、この欄に id を足すだけの変更は周の引き金にならない（判断の記録 ADR-13 決定 (8)）。」を、
  - 「節点の要約値はこの欄を落とした本文だけを数えるので、この欄に id を足すだけの変更は節点の要約値を動かさない。周の引き金になるかは天井の正本の引き金の一覧が決め、この欄のうち受入基準の verifies・要件の verify.ac・規則の表の行の article は規範の欄として引き金に入る（判断の記録 ADR-18 決定 (1)・ADR-20）。ほかの辺の欄に id を足すだけの変更は周の引き金にならない（ADR-13 決定 (8)）。」へ。残りの文は変えない。
- 天井の正本の引き金の一覧（字の正本 `crates/folio/src/ceiling.rs` の定数と床の木）:
  - 憲法の行に写しの範囲（schema・precedence・articles）を足す（行の形は契約で決める）。
  - `trigger_note` の「constitution は凍結 anchor の写しと同じ条の欄と規範文の欄。」を「constitution は凍結 anchor の写しと同じ範囲（schema 節と前文は丸ごと・条は条の欄と規範文の欄）。」へ。末尾の「何にするかの裁定の正本は判断の記録 ADR-18 決定 (1)」を「…ADR-18 決定 (1)（ADR-20 が改訂）」へ。

## 5. 便の見積と順序

### 引き金の式で変えるのは憲法の写しの範囲だけ（実測）

`crates/folio/src/gate.rs` の `trigger_digest` と `crates/folio/src/ceiling.rs` の引き金の定数を読み、実物の道具で確かめた（`trigger_probe.sh`）。

| 決めること | 今の実装 | 実物の確かめ | 便で変えるか |
|---|---|---|---|
| 受入基準の確かめる要件を引き金に数える | 受入基準の欄の定数が id・題・確かめる要件・判定の文を持つ | AC18 に id を足すと引き金の要約値が動く | 変えない |
| 要件の確かめ方の中の受入基準を数える | 要件の欄の定数が確かめ方を丸ごと持つ | FR20 の確かめ方に id を足すと動く | 変えない |
| 規則の表の行の条を数える | 規則の表の欄の定数が条を持つ | D-12 の条を替えると動く | 変えない |
| ほかの辺の欄を数えない | どの定数にも根拠・参照・関係・目的・規則・判断の記録・帰結の欄が無い | 参照・根拠に id を足しても動かない | 変えない |
| 前文と schema 節を数える | 憲法は凍結 anchor の写しの関数を範囲 articles だけで通す | 前文・schema 節の変更は動かさない（床は N-4 で落とす） | **変える**（範囲を schema・precedence・articles へ） |

### 便 1 本（M・約 29 file・独立の検証の模擬で数えた）

- 書き換える file: `design-intent/graph.yaml`・`design-intent/ceiling.yaml`（生成区間）・`crates/folio/src/graph.rs`・`crates/folio/src/ceiling.rs`・`crates/folio/src/gate.rs`・天井の正本の写し 20 本（`tests/fixtures/**/ceiling.yaml`・歯 f102 が凍結の区間との byte 一致を求める）・`crates/folio/tests/schema_docs.rs` の定数 4 つ（天井と索引の生成区間の byte 数と要約値）・`tests/fixtures/schema/node-digest-anchor.txt`・`crates/folio/tests/graph.rs` の定数（f99）・`crates/folio/tests/gate.rs` と印の fixture（案 d の範囲で動く門の歯 6 本）・凍結の写し `tests/fixtures/schema/graph-region.txt` と `ceiling-region.txt`。
- 独立の検証の模擬: 字だけ（6 file）では 848 / 855（7 本落ち）、天井の正本の写し 20 本と生成区間の定数を足して 854 / 855（f99 が残る）、案 d の範囲だけでは門の歯 6 本が落ちた。先例の便ア（台帳 f2-648.178）は 36 file だった。
- 契約は発効の後に書く。生成区間の変更で索引の欄の決まりと天井の正本の版を上げるかは、便アの扱いに倣って契約で決める。

### 順序

1. 独立の検証と改訂 b（済み）。
2. **一括 18（枝 `docs/batch18`・`docs/design/batch18-triage.md`・本枝を土台にする）と同じ回で承認を求める。** 31 周目は整合 不合格で、行 R-7 に達し、行 D-14 の止める場合 (イ) で往復を止めて出している。ADR-20 はその 止める の答えでもある。
3. 承認 → 席が §3 の #1〜#8・#10〜#12 を書く → **ADR-20 → 一括 18 の順で本流へ取り込む**（#8・#12 の要件書は一括 18 の第 1.39 版の後の字に当てる）。
4. 問 2 の裁定で便（#9）を器へ出し、着地させる。
5. **便の着地の後に本流で 32 周目を回す**（ADR-20 の発効・定数の変更・一括 18 の引き金の欄の変更をまとめて読む）。撤退条件を数える期間は便の着地の時刻から 14 日（(1) だけは期間を切らない）。

## 6. 実測の要点（数え方と script は ADR-20.yaml の注と adr-20-grill.md）

| 項目 | 値 |
|---|---|
| 索引の辺の欄と引き金の一覧の重なり | 3 つ（確かめる要件・確かめ方の中の受入基準・規則の表の行の条）。独立の検証も 3 つ |
| 周と周の間の区間（周 2〜31） | 29 |
| 引き金が動いた区間／動かなかった区間 | 23／6（独立の検証が実物の道具で一致） |
| 3 つの欄が動いた区間／そのうち 3 つだけ | 11／0（同） |
| 引き金が動かなかった区間でほかの辺の欄が動いた区間 | 0（同） |
| 前文か schema 節が動いた区間／そのうち判断の記録の欄も動いた区間 | 2／2（同） |
| 凍結 anchor の列での前文の変更 | 2 度（v1.2 の平易文・v1.3 の根拠の行）。schema 節は 0 度 |
| 1〜31 周の所見のうち場所が 3 つの欄に在るもの | 1（直す）・止める 0 |
| 本流 `ca60873` と枝の引き金の要約値 | 同じ（提案中は引き金を動かさない）。発効の模擬で動く |
| 門（本流の印） | 今も発効の後も まだ分からない（rc 2・印に引き金の要約値の欄が無い） |

## 7. まだ分からないこと

- **次の周が、改訂の欄と注だけで食い違いを解けたと読むか。** 決定の本文は書き換えないので、ADR-18 決定 (2) と ADR-13 決定 (8) の字は残る。31 周目の反証は「後の決定が前を上書きする形になっていない」を理由に挙げたので、改訂の欄の狭める向きでその形になれば解ける見込みだが、審査は確率的である。拾われたら撤退条件 (3) で数え、決定の本文を書き換える案 f を含めてあなたへ問う。
- **便の中で、憲法の写しの範囲を広げたときの印と門の歯の直し方。** 独立の検証の模擬では門の歯 6 本が落ちた。契約で歯の期待を独立の実装から組み直す。
- **生成区間の注の便で、索引の欄の決まりと天井の正本の版を上げるか。** 契約で先例を見て決める。
- **同じ型の穴が憲法のほかの節に在るかもしれない。** 憲法の目的の節（north_star）・改訂の手続きの章・規則と用語集の指し先・出所は、改訂の範囲の外で、ADR-18 の決定 (1) の一覧にも決定 (2) の引き金の外の列にも名指されていない。目的の節は初版から 1 度も変わっていない。改訂の手続きの章と規則と用語集の指し先は 10→11 周の区間で、出所は 4 つの区間で、判断の記録の発効なしに変わった（どの区間もほかの欄で引き金は動いていたので漏れは無かった・独立の検証の数え）。本判断に混ぜず、次の周の所見か別の問いで扱う。
- **本判断の見出しは起草の時点の字のままである。** 見出しの後半「憲法の前文と schema 節は判断の記録の改訂の欄を通して引き金に数える」は案 a の形を言い、改訂 b の推奨（案 d）とずれる。見出しは id ではないが、席の裁定で変えていない。
- **数えの道具の置き場（`~/.local/share/folio2/handoff-2026-09-24/`）は版管理の外に在る。** 端末を移るときは写してから数える。
