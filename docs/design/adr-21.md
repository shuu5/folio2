# 判断の記録 ADR-21 の承認要求 — M3 の出口の相手を scribe2 から v3 の設計の置き場へ読み替え、scribe2 の移行のためだけの論点を閉じる

- 状態: 起草・承認待ち（2026-09-24・改訂 b・c 2026-09-24・orchestrator 席 fable 5.1 の起草役）。
- 正本: `design-intent/adr/ADR-21.yaml`（status: proposed）。反証の試み: 独立の検証役の検証 `~/.local/share/folio2/handoff-2026-09-24/adr21-verify.md`（別の文脈・opus 5.5・**総合 条件付き支持**・blocking 4・文面 7・低い重さ 5）と、起草役の自己反証 `docs/design/adr-21-grill.md`。改訂 b は歯の赤（blocking 1）を閉じ、改訂 c は残りの blocking 2〜4 と文面と低い重さを席の裁定で全部当てた（当て方の一覧は grill の §6）。起草の記録: `~/.local/share/folio2/handoff-2026-09-24/adr21-draft.md`。
- 出所: scribe2 の orchestrator 席からの連絡（`~/.local/share/folio2/handoff-2026-09-24/scribe2-v3-direction.md`・scribe2 の持ち主の裁定 2026-09-24T08:40Z）と控え 台帳 f2-648.188。あなたは 2026-09-24 21:33 JST の 🔴 問 6（逐語「すべて承認する」）で、**この改訂を起こすこと**を承認した。発効はこの承認要求で別に問う。
- 枝: `docs/adr21` は本流 `249a7a2`（ADR-20 の発効と一括 18・要件書 第 1.39 版・PR #308）の上に在る（起草は枝 `docs/batch18` の `1498aee` の上で行い、取り込みの後に載せ替えた）。
- 憲法の条文・前文・schema 節は 1 字も変えない。規則の表の行を足さない。**判断の記録 ADR-16 と ADR-3 の決定の本文も書き換えない**（改訂の欄と注で断る・ADR-17 / ADR-20 と同じ形）。便は起こさない。
- **🔴 は 2 問**（問 2 = v3 をどの repo で作るか・問 1 = 本判断を発効してよいか）。**問 1 の答えは問 2 の答えに依る。** 問 2 を先に読んでほしい。

## 1. 要旨（平易に）

- 次の段（M3）は「folio2 以外の場所で、folio2 の道具の検査に合格できるか」を確かめる段で、その相手を scribe2 にしていた（判断の記録 ADR-16）。
- 2026-09-24 に、scribe2 の持ち主の裁定で、scribe2 の設計文書を folio2 の形へ移す計画は取りやめになった。folio2 の形へ移るのは、scribe の次の世代（v3）を作るときになった。v3 は scribe2 の中心と folio2 と画面の層を 1 つの repo にまとめる世代で、設計文書は最初から folio2 の形で書く。
- **v3 の設計の置き場は、もう立っている。** 新しい repo `~/projects/local-projects/scribe3` の骨格の commit `5091abb`（2026-09-24 17:45 JST）である。台帳と remote はまだ無く、検査は違反 0・まだ分からない 2 だった。
- このままだと、ADR-16 の出口（scribe2 が文書を移して合格したとき）の日は来ない。**この判断で、出口の相手を「v3 の設計の置き場」に読み替える。** 出口の 3 つの確かめ（検査に合格・約束の写しが入ってずれ 0・便を運ぶ道具が写しを読んで通す）は、そのまま v3 の置き場で確かめる。3 つ目の前提だった scribe2 の受け口（s2-07l.473 → .586）は、もう着地している。
- **scribe2 の移行のためだけにあった話を閉じる。** scribe2 の番号の書き方 9 種類・章が多すぎる文書・scribe2 の仮の検査を外す条件。v3 は最初から folio2 の書き方で書くので要らない。folio2 の検査は 1 字も変わらない。
- 着地済みの M3 の口（骨格の命令・列の根の表・始まりの凍結・値域の床・導出物の命令）は、v3 の入口としてそのまま使う。v3 の入口で直す所（控え 8 件）の順は席が決め、この判断には並べない。
- **やめる条件**: 次のどれかが 1 回起きたら、あなたへ問い直す。
  - folio の実装そのものが v3 の repo へ入るか、v3 の憲法が folio2 の憲法の名を名乗るかして、v3 が「外の利用者」と言えなくなる。
  - v3 が「約束は写しの file でなく型で渡す」と決める。
  - 発効から 30 日たっても、v3 の置き場で検査の合格が記帳されない。
  - scribe2 が移行を再開する。

**先に言っておくこと: あなたの問い「それをfolio2で作ってはだめなのか？」に、まだ答えが記録されていない。** あなたは scribe2 の側（s2-07l.214 notes・2026-09-24T08:40Z）で、4 問目への答えの後にこう問うた。scribe2 席の連絡はこの後段を写しておらず、v3 の起こしの文書にも台帳にも答えは無い（独立の検証の読み）。v3 を folio2 の repo で作る（folio2 の repo を v3 に育てる）なら、v3 の置き場は最初から folio の実装と同じ repo に在り、「外の利用者」と数えられない。だから問 2 で先に問う。

| M3 の口（ADR-16 決定 (7)） | 状態 | v3 の置き場で使うか |
|---|---|---|
| ① 要件書の M3 の範囲の節 | 着地（第 1.34 版） | 使う（指し先に本判断を足す） |
| ② 列の根の表と始まりの凍結（便 121） | 着地 | 使う（v3 の行は ⑦ で足す） |
| ③ 値域の部分集合の床（便 122） | 着地 | 使う |
| ④ 骨格の命令と器の導出 file の解き方（便 123・125） | 着地 | 使う（雛形の字は v3 の持ち主が書き換える） |
| ⑤ 導出物の独立の命令（便 119） | 着地 | 使う |
| ⑦ 列の根の表に利用者の行を足す | 未（v3 の憲法の第 1.0 版の後） | 使う（利用者 = v3・f2-648.192） |
| 器の受け口（s2-07l.473 → .586） | 着地（scribe2 本流 `0337cf1`・07:40 JST） | 判定点 ③ の前提が揃った |
| 参照 id の形を行 R-16 から読む便 | 未 | **起こさない**（scribe2 の門を外す前提だった） |
| 利用者の側の便（scribe2 の 4 本） | 未 | **外す** |

## 2. 🔴 持ち主へ問う項（2 問・問 2 を先に）

### 🔴 問 2: v3 の合流先の repo は、新しい repo（scribe3）か、folio2 の repo を v3 に育てるか

- **やりたいこと**: v3 の合流先の repo を、今 v3 の設計の置き場が在る新しい repo（`scribe3`・private・folio の実装は後から取り込む）と決める。folio2 の repo は、合流までは folio の実装と folio2 の設計文書の置き場のまま残る。
- **理由**:
  - あなたは scribe2 の側で「それをfolio2で作ってはだめなのか？」と問うた（s2-07l.214 notes・2026-09-24T08:40Z）。この問いへの答えは、scribe2 席の連絡にも、v3 の起こしの文書（scribe3 の `docs/handoff/2026-09-24-v3-kickoff.md`）にも、台帳にも見つからなかった。
  - 答えで ADR-21 の前提が変わる。新しい repo なら、合流の前の v3 の置き場は folio2 と別の repo・別の憲法の名・本流の 1 つの組み立てで検査される置き場になり、条 P-13.4 の「外部の利用者で受入が通った後に self-host を始める」の順がそのまま成り立つ（合流の前の v3 で受入 → 合流 → v3 が自分の便で自分を運ぶ）。
  - folio2 の repo を v3 に育てるなら、v3 の置き場は最初から folio の実装と同じ repo に在り、外部の利用者と数えられない。そのとき ADR-21 は発効の直後にやめる条件 (1) に当たるので、発効させず保留（案 c）に倒す方が、判断の記録 1 本の往復を無駄にしない。
  - 新しい repo の置き場は既に在る（`5091abb`・その後に討論の起こしと論点 D の討論の commit）。
- **代替案**: folio2 の repo を v3 に育てる。folio の実装と v3 の設計が最初から同じ repo に入り、合流の手間は減る。代わりに、M3 の「外部の利用者」は v3 では立たず、P-13.4 の物差しを別に立てる判断（新しい M3 の判断の記録・案 b の形）が要る。scribe3 の置き場の中身は folio2 の repo へ移すことになる。
- **コスト**: この答えは 1 語で済む。新しい repo なら、問 1 へ進む。folio2 の repo なら、問 1 は案 c（保留）になり、M3 の物差しを立て直す判断の記録を後で起こす。
- **推奨**: 新しい repo（scribe3）。置き場は既に在り、ADR-21 の限定と P-13.4 の順がそのまま成り立つ。

- **「まだ決めない」と答えたとき**: v3 の合流の形は v3 の討論の論点 A（scribe3 の起こしの文書 §2）で決めることになっている。「まだ決めない（v3 の論点 A で決める）」の答えなら、問 1 は案 c（保留）として扱い、発効させない。論点 A の答えが v3 の席から着いたときに、問 1 を改めて問う。

回答の例: 「問 2 は推奨で（scribe3）」「v3 は folio2 の repo で作る」「まだ決めない（v3 の論点 A で決める）」。

### 🔴 問 1: 判断の記録 ADR-21 を発効してよいか（問 2 が「scribe3」のとき）

- **やりたいこと**: 判断の記録 ADR-21 を発効する（案 a）。発効の後に、席が §3 の欄を書く。便は起こさない。
  - ADR-16 決定 (1) の「外部の利用者 1 つ（scribe2）」を「v3 の設計の置き場」と読む。判定点 ①〜③ はそのまま v3 の置き場で数え、判定点 ③ の記帳に読んだ器の組み立て（器の repo と commit）を添える（受入基準 AC25 の判定の文が狭まる）。v3 を外部の利用者と数えるのは、folio の実装を持つ repo と別の repo に在り、憲法の名が違うあいだに限る。改訂の欄の向きは「狭める」とした。利用者の名の置き換えを 2 値の向きに近似した読みで、範囲から「移す話」が外れ、出口の記帳に条件が足されるので出口と数える状態が減る、という理由である。
  - ADR-16 決定 (3) の scribe2 の置き場（design-intent/folio2）と、骨格の憲法を「本来の憲法を指す宣言」にする形を、本来の憲法を別に持つ利用者に限る。v3 の骨格の憲法は v3 の憲法そのものとして、v3 の持ち主が書き換えてから承認する。
  - ADR-16 決定 (6) の scribe2 の値と器の暫定の門の撤去の条件を閉じ、参照 id の形を読む便と要件 FR12 の直しを起こさない。ADR-3 決定 (7) の「M1 が引き取ったら撤去する」の句も閉じ、器の門の撤去は器の持ち主の扱いにする。
  - ADR-16 決定 (7) の便の列から scribe2 の側の便を外す。
  - ADR-16 の撤退条件 (1) の窓の起点を、v3 の置き場が立った時点（`5091abb` の 2026-09-24 17:45:49 JST）と読む。窓は既に始まっている。
- **理由**:
  - scribe2 の持ち主の裁定で、ADR-16 の出口の相手が消えた。字のままでは M3 が届かない段として残り、条 P-13.4 の「外部の利用者で受入が通った後」の物差しが指す先を持たない。
  - ADR-16 の決定 (2)（道具は 1 つの組み立て・列の根の表・値域・部品目録ほか 7 つ）と便 ①〜⑤ の着地は利用者の名に依らず、v3 の置き場でも使える（外の置き場の実測で、列の根の表に行を足した組み立てなら床は合格まで届いた）。変えるのは利用者の名と scribe2 に結んだ所だけなので、発効した判断を生かしたまま範囲を変える形（改訂の欄）が合う。
  - scribe2 が閉じてよいと言った 3 つの論点は、判断の記録なしには閉じられない（条 P-13.3）。ADR-3 決定 (7) の撤去の句は、ADR-16 の定め直しだけを閉じると元の字（M1 が引き取ったら撤去）が生き返って「器は今すぐ門を外せ」と読めるので、一緒に閉じる。
  - 判定点 ③ の前提の器の受け口は着地済みで、今の器は導出物の file を読む。v3 の起こしの推奨も境界を「設計ノートの行 → 契約（toml）」と書く。判定点 ②③ を保つ理由は起草の時点より強い。
- **精度の損は 0 ではない**: v3 を外部の利用者と数える根拠は「合流の前」に限られ、その時機は分からない。v3 の憲法の母体（scribe2 か folio2 か）は v3 の討論の論点 E で未決で、folio2 の憲法の名を引き継げば限定が崩れる。判定点 ②③ は、v3 が型で受ける境界を選べば相手を失う。どれも撤退条件 (1)(2) が 1 回であなたへ上げる。
- **代替案**:

| 案 | 中身 | 費用 | 失うもの |
|---|---|---|---|
| **a（推奨）** | ADR-16 を生かしたまま、利用者を v3 の置き場へ読み替え、scribe2 の論点を閉じる（改訂の欄と注） | 席の記帳（§3）・要件書の版上げ 1 つ | 「外部の利用者」が合流の前に限られる（撤退条件 (1)） |
| b | ADR-16 を退役させ、新しい M3 の判断の記録で置き換える | 生きている決定 (2)(4)(5) と、ADR-16 の改訂の欄 2 行（ADR-2 (3)・ADR-3 (7) を広げる行）を後継へ写す（要件書の欄は直さなくても床は合格する・独立の検証の実測） | 決定の生きている判断を「廃止」の状態にする（条 P-7.2 の状態の意味と合わない） |
| c | 何も変えず保留する（問 2 が「folio2 の repo」ならこれ） | 0 | 出口に届かない M3 が残る。受入基準 AC23〜AC25 が「利用者（scribe2）」のまま読まれる。scribe2 の論点が開いたまま |
| d | a に加えて、出口を判定点 ①（v3 での検査の合格）だけに縮める | a と同じ＋受入基準の組み替え | v3 の境界の形を先に決めることになる。器が folio の出力を使える面が確かめられない |
| e | 利用者を scribe2 のまま残し、移行の再開を待つ | 0 | 待つ相手の予定が無い |

- **コスト**: この承認は 1 回。承認の後に席が §3 の欄を書き（要件書の版が 1 つ上がる・規範文と番号と段は不変・規則の表の行 R-16 の注に裁定 id を付けて直す・台帳 f2-648.93 を閉じる）、台帳と状態の 1 枚を書く。便は 0 本。本判断の発効は周の引き金を動かすので、本流の次の周が読む（どの判断の記録の発効も同じ）。
- **推奨**: a。

回答の例: 「どちらも推奨で」「問 2 は推奨・問 1 は c（保留）」「v3 は folio2 の repo で作る（問 1 は c）」「問 2 はまだ決めない（問 1 は c・論点 A の後に問い直す）」「問 1 は d で（出口を検査の合格だけに）」。

## 3. 承認の後に席が書く欄（本判断の枝ではまだ書かない）

| # | file | 欄 | 書くこと |
|---|---|---|---|
| 1 | `design-intent/adr/ADR-21.yaml` | status・approval・note・頭の注釈 | accepted と、あなたの逐語・日付・裁定 id・対話面 R-8。note に【発効】の段（問 2 の答えも） |
| 2 | `design-intent/adr/ADR-16.yaml` | note の末尾 | 【判断の記録 ADR-21 による改訂】の段（§4 の 1）。**決定・文脈・案・撤退条件・承認欄と見出しの字は変えない** |
| 3 | 同 | plain（平易文）の末尾 | 読み替えの 1 段（§4 の 2）。先例 = ADR-20 の発効で ADR-18 の平易文を直した形。上の段落は発効の時の字のまま残す |
| 4 | 席の判断 | ADR-16 の頭の注釈 | 書き換えない（注の段で足りる） |
| 5 | `design-intent/adr/ADR-3.yaml` | note の末尾 | 決定 (7) の撤去の句を ADR-21 の改訂の欄で閉じたことと、ADR-16 の広げる行の扱いを 1 段（§4 の 3）。**決定 (7) の本文は書き換えない** |
| 6 | `design-intent/srs.yaml` | M3 の範囲の節（scope_m3）の作るの 1 行目と作らないの 1 行目 | 指し先に ADR-21 決定 (1) を足す（§4 の 4）。段の欄と規範文は変えない |
| 7 | 同 | 受入基準 AC23・AC24・AC25 の平易文・固定の材料（red_test.fixture）・注、AC25 の判定の文（red_test.sentence） | 「利用者（scribe2）」「scribe2 は s2- の項」「scribe2 が読む形」を v3 の置き場の字に。AC25 の判定の文に読んだ器の組み立てを足す（§4 の 5・決定 (1)） |
| 8 | 同 | 要件 FR22 の注・判断の記録の欄（adrs） | 決定 (2) の限定を 1 文（§4 の 6）。adrs に ADR-21 |
| 9 | 同 | 制約 CON8 の本文の末尾 | 取りやめと読み替えの 1 文（§4 の 7） |
| 10 | 同 | meta（版・状態の注・承認欄） | 要件書の版を 1 つ上げる（本流の第 1.39 版の後の第 1.40 版）。規範文と番号と段は不変（条 P-7） |
| 11 | 席の判断 | 要件 FR11 の平易文と受入基準 AC9 の平易文の「scribe2 が読む形」、M3 の範囲の節の note の「この節は判断の記録 ADR-16 決定 (1) を指す節」 | 変えない（今の器は scribe2 のまま。器が v3 の中へ替わったら直す。note は #6 の指し先で足りる） |
| 12 | `design-intent/rules.yaml` | 行 R-16 の note・ruling・ruled_at | 注を §4 の 8 の字へ。**ruling の頭に本判断の発効の裁定（台帳 f2-648 notes の時刻と逐語）を足し、前の裁定を「前の裁定 = …」で畳む**。ruled_at を発効の日へ（条 P-17.1）。値・種別・段・母集団は不変。床は裁定の欄の書き忘れを落とさない（空でないことしか見ない）ので、手順で守る |
| 13 | 台帳（`bd`） | f2-648 notes・f2-648.188・.189〜.196 | 承認の逐語と日付（条 P-12.2）と問 2 の答え。f2-648.188 を閉じる。.189〜.196 の 8 件の昇格条件の「問 6 の裁定」は満たされたと記帳し、順は席が決める。撤退条件 (3) の起点 = 本判断の発効の時刻。ADR-16 の撤退条件 (1) の窓の起点 = scribe3 の `5091abb`（17:45:49 JST）を記帳する |
| 14 | `docs/design/state.md` | 判断の記録の行・次の一手 | ADR-21 の発効と、M3 の出口の相手が v3 の置き場になったこと |
| 15 | scribe2 席への返し | `~/.local/share/folio2/handoff-2026-09-24/` の file | お願い 2 の 3 論点を閉じたこと（版管理の外・口座名を書かない） |
| 16 | `design-intent/srs.yaml` | 要件 FR11 の注 | 頭の文「解除の前提とした器の口 s2-07l.473（…）は着地していない＝…判定点 ③（…）は器の受け口の着地が要る。」と、後ろの文「s2-07l.473 は 2026-09-24 に器の台帳で再開された（契約 1 本・S 見込み）。s2-07l.473 の着地は判定点 ③ の前提（受け口）である。」の両方を、.586 の着地の事実の字へ（§4 の 9）。第 1.40 版に同居 |
| 17 | 同 | 要件 FR12 の注 | 「撤去の条件は、ADR-3 決定 (7) の…を判断の記録 ADR-16 決定 (6) が定め直した。」と「撤去をいつ器へ要求するかの正本は規則の表の行 R-16 の注」を閉じた字へ（§4 の 10）。第 1.40 版に同居 |
| 18 | 台帳（`bd`） | f2-648.93（器の暫定の散文の門の撤去を器へ要求する） | 閉じる（理由 = ADR-21 決定 (3)・folio2 は撤去の条件を持たない） |
| 19 | `design-intent/vocabulary.yaml` | M（段階）の項（milestone）の定義の「（M3 は判断の記録 ADR-16 決定 (1)）」 | 「（M3 は判断の記録 ADR-16 決定 (1)・利用者の読み替えは ADR-21 決定 (1)）」へ（§4 の 11） |
| 20 | v3 の席（s3 の設計の席・scribe3 の repo）への返し | `~/.local/share/folio2/handoff-<日付>/` の file | 問 2 の答え（と問 1 の答え）を file で返す（版管理の外・口座名を書かない）。v3 の討論の論点 A が合流先の repo を決める場なので、folio2 の対話面で出た答えを v3 の側へ届ける。返したことは台帳 f2-648 の notes にも記帳する |

- #1〜#3・#5〜#9・#12（注の字）・#16・#17・#19 を写しの上で当てて床を撃つと、合格（違反 0・まだ分からない 0）で、`schema --check`・`derive --check`・`inject --check` は rc 0、`cargo nextest run --workspace` は 855 / 855 だった（模擬の script `~/.local/share/folio2/handoff-2026-09-24/adr21-scripts/post_approval_sim.py`・改訂 c で #8・#16・#17・#19 を足した・#2 と #5 は段を短くした字で当てた・#10 の版上げと #12 の裁定の欄は当てていない）。独立の検証の `full_sim.py`（#8・#10・#12 の ruling と ruled_at・§4 の全文）でも床 4 本 rc 0・nextest 855 / 855 だった。

## 4. 文案（承認の後に正本へ入れる字）

### 1. ADR-16 の注の末尾の段（#2）

> 【判断の記録 ADR-21 による改訂（<日付> 発効・持ち主の承認 <時刻> JST「<逐語>」）】scribe2 の設計文書を folio2 の形へ移す段は、2026-09-24 に scribe2 の持ち主の裁定で取りやめになった（scribe2 の台帳 s2-07l.214 notes・folio2 の控え 台帳 f2-648.188）。決定 (1) の「外部の利用者 1 つ（scribe2）」は、判断の記録 ADR-21 の改訂の欄（狭める向き）で、器の次の世代（scribe v3）の設計の置き場（新しい置き場（scribe3）の design-intent）と読む。判定点 ①〜③ はその置き場で数え、判定点 ③ の記帳は読んだ器の組み立てを名指す。v3 の置き場を外部の利用者と数えるのは、folio の実装を持つ repo と別の repo に在り、憲法の名が folio2 の憲法の名と違うあいだに限る。決定 (3) の scribe2 の置き場と骨格の宣言の形は、同じ repo に本来の憲法を別に持つ利用者に限る。決定 (6) の scribe2 の値・器の暫定の門の撤去の条件・参照 id の形を行 R-16 から読む便・要件 FR12 の規範文の直しは閉じた。決定 (7) の便の列から scribe2 の側の便を外し、⑦ の利用者を v3 の置き場と読む。撤退条件 (1) の窓の起点は、v3 の置き場が立った時点（版 5091abb・2026-09-24 17:45:49 JST）と読む（ADR-21 決定 (5)）。撤退条件 (2)〜(4) は字のまま数え、利用者を v3 の置き場と読む。決定の本文は発効した判断の記録の字なので書き換えず、改訂の欄は ADR-21 の側に在る（判断の記録の欄の決まりのとおり片側だけ）。見出し・文脈・決定・案・帰結・撤退条件・承認欄の字は変えない。

### 2. ADR-16 の平易文の末尾に足す段（#3）

> （2026-09-24 の後の読み方）scribe2 の設計文書を移す計画は、scribe2 の持ち主の裁定で取りやめになりました。この判断の「scribe2」は、判断の記録 ADR-21 で、器の次の世代（scribe v3）の設計の置き場に読み替えています。v3 は設計文書を最初から folio2 の形で書くので、scribe2 の番号の書き方に合わせる話と、scribe2 の側の仮の検査を外す話は閉じました。上の段落は、発効したときの字のまま残しています。

### 3. ADR-3 の注の末尾の段（#5）

> 【判断の記録 ADR-21 による改訂（<日付> 発効）】決定 (7) の「M1 が引き取ったら撤去する」（器の暫定の散文の門の撤去の句）は、判断の記録 ADR-16 決定 (6) が撤去の条件を定め直し、その定め直しを判断の記録 ADR-21 が scribe2 の設計文書の移行の取りやめで閉じた。ADR-21 の改訂の欄（狭める向き）で、この句を folio2 の判断の記録から外し、器の門の撤去の時機は器の持ち主の扱いとする（元の字へは戻らない）。ADR-16 の改訂の欄の広げる行（参照 id の形を行 R-16 の値へ）は向きとして残り、それを運ぶ便は起こさない（ADR-21 決定 (3)）。散文の門の式と folio2 の門は変えない。決定の本文は書き換えない。

### 4. 要件書の M3 の範囲の節（#6）

- 作るの 1 行目: 「…の正本は判断の記録 ADR-16 決定 (1) である（ここには写さない・条 P-6.3）」→「…の正本は判断の記録 ADR-16 決定 (1) と、その利用者を器の次の世代（scribe v3）の設計の置き場へ読み替えた判断の記録 ADR-21 決定 (1) である（ここには写さない・条 P-6.3）」
- 作らないの 1 行目: 「M3 に入れないものの正本も判断の記録 ADR-16 決定 (1) である（ここには写さない）。」の後に「判断の記録 ADR-21 はこの列を変えない。」を足す。

### 5. 受入基準 AC23〜AC25（#7）

- AC23 平易文: 「利用者（scribe2）の repo で、」→「利用者（v3 の設計の置き場）の repo で、」。固定の材料: 「（scribe2 は s2- の項）」→「（v3 の置き場の台帳の項）」。注の「判断の記録 ADR-16 決定 (1)(5)」→「判断の記録 ADR-16 決定 (1)(5)・ADR-21 決定 (1)」。
- AC24 平易文: 「利用者（scribe2）の 1 本目の設計ノートの契約表から作った scribe2 が読む形の file が、」→「利用者（v3 の設計の置き場）の 1 本目の設計ノートの契約表から作った器が読む形の file が、」。固定の材料は AC23 と同じ直し。
- AC25 平易文: 「scribe2 の受付が、利用者の repo に入った scribe2 が読む形の file を実際に読んで通した記録が利用者の台帳に在れば、」→「v3 の便を運ぶ器の受付が、利用者の repo に入った器が読む形の file を実際に読んで通した記録が、読んだ器の組み立てを名指して利用者の台帳に在れば、」。判定の文: 「器の受付がその導出物を読んで通した記録が在れば着地」→「器の受付がその導出物を読んで通した記録が、読んだ器の組み立て（器の repo と commit）を名指して在れば着地」。固定の材料は AC23 と同じ直し。

### 6. 要件 FR22 の注の末尾に足す文（#8）

> 判断の記録 ADR-21 決定 (2) で、scribe2 の置き場（design-intent/folio2）と骨格の憲法の宣言の形（本来の憲法を指す・根拠の種別 scribe2-article）は、同じ repo に本来の憲法を別に持つ利用者に限った。v3 の設計の置き場の場所は v3 が選び、骨格の憲法は v3 の持ち主が v3 の条と名へ書き換えてから第 1.0 版を承認する。骨格の命令の振る舞いと雛形は変えない。

### 7. 制約 CON8 の末尾に足す文（#9）

> scribe2 の設計文書の移行は 2026-09-24 に scribe2 の持ち主の裁定で取りやめになり、M3 の利用者は判断の記録 ADR-21 で器の次の世代（scribe v3）の設計の置き場へ読み替えた。scribe2 の設計文書について移す範囲を問い直す論点は閉じた。

### 8. 規則の表の行 R-16 の注（#12）

- 今: 「揃える向き（揃えるのは器の側・folio2 の読みと今の値は動かない）と、参照 id の形をこの行の値に持たせる直しは ADR-16 決定 (6) の便が運ぶ。撤去の条件は、ADR-3 決定 (7) の「folio2 が引き取ったら」を ADR-16 決定 (6) が定め直した（契約表を持つ利用者の設計文書が全部移り、利用者の自動の検査が folio の床を動かすようになったとき）。」
- 改めた後: 「揃える向きと、参照 id の形をこの行の値に持たせる直しを運ぶはずだった ADR-16 決定 (6) の便は、scribe2 の設計文書の移行の取りやめで起こさない（判断の記録 ADR-21 決定 (3)）。器の側の門の撤去の時機は器の持ち主が決め、folio2 の判断の記録は条件を持たない（同・ADR-3 決定 (7) の撤去の句も閉じた）。」
- 値・種別・段・母集団は不変。ruling の頭に本判断の発効の裁定を足し、前の裁定を畳む。ruled_at を発効の日にする。

### 9. 要件 FR11 の注（#16）

- 頭の文の今: 「解除の前提とした器の口 s2-07l.473（導出物の欄 goal の受け口）は着地していない＝s2-07l.473 は 2026-09-22 に memo として閉じられ未着地（folio2 席の返答「保留・閉じてよい」）。器の読み手は導出物の欄 goal を未知の key として断る（scribe2 main 596989b・2026-09-24 実測）ので、判定点 ③（器が導出物を読む・ADR-16 決定 (1)）は器の受け口の着地が要る。」
- 頭の文の改めた後: 「解除の前提とした器の口 s2-07l.473（導出物の欄 goal の受け口）は、2026-09-22 に memo として閉じられた（folio2 席の返答「保留・閉じてよい」）後、契約 s2-07l.586 へ引き継がれ、.586 が着地した（下の段）。着地の前の scribe2 main 596989b（2026-09-24 07:04 JST）の実測では、器の読み手は導出物の欄 goal を未知の key として断っていた。」
- 後ろの文の今: 「s2-07l.473 は 2026-09-24 に器の台帳で再開された（契約 1 本・S 見込み）。s2-07l.473 の着地は判定点 ③ の前提（受け口）である。」
- 後ろの文の改めた後: 「.586 は scribe2 の本流の版（0337cf1・2026-09-24 07:40 JST）に着地した＝器の読み手は表の行の目標の欄（goal）を受ける。受け口の着地は判定点 ③ の前提で、満たされた。判定点 ③ の測定値ではない（第 1.40 版で「着地していない」「再開された」の字を直した・判断の記録 ADR-21 文脈 (3)）。」

### 10. 要件 FR12 の注（#17）

- 今: 「撤去の条件は、ADR-3 決定 (7) の「folio2 が引き取ったら」を判断の記録 ADR-16 決定 (6) が定め直した。」
- 改めた後: 「器の側の門の撤去の時機は器の持ち主が決め、folio2 の判断の記録は条件を持たない（判断の記録 ADR-21 決定 (3) が、ADR-16 決定 (6) の撤去の条件と ADR-3 決定 (7) の撤去の句を閉じた）。」
- あわせて「違いの中身と、撤去をいつ器へ要求するかの正本は規則の表の行 R-16 の注で、」→「違いの中身の正本は規則の表の行 R-16 の注で、」。

### 11. 語彙の M（段階）の項（#19）

- 「（M3 は判断の記録 ADR-16 決定 (1)）」→「（M3 は判断の記録 ADR-16 決定 (1)・利用者の読み替えは ADR-21 決定 (1)）」

## 5. 実測の要点

| 項目 | 値 |
|---|---|
| v3 の設計の置き場 | 新しい repo `scribe3` の design-intent。骨格 `5091abb`（2026-09-24 17:45:49 JST）。remote・台帳は無い。憲法の名 floor-declaration。床は違反 0・まだ分からない 2（独立の検証・読むだけ） |
| 器の受け口 | s2-07l.473 は .586 へ引き継いで閉。.586 は scribe2 本流 `0337cf1`（07:40 JST）に着地（`596989b` の実測より後） |
| 骨格のまま（commit 後）の床 | 違反 0・まだ分からない 2（外の置き場の実測・本判断の起草の撃ち直し・scribe3 の実物で一致） |
| 骨格の憲法の名・前文と条の指す先・根拠の種別 | floor-declaration・利用者の本来の憲法・scribe2-article（scribe2 の形・控え f2-648.196） |
| ADR-16 の便 ①〜⑤ | 全部着地（2026-09-24 まで）。残りは ⑦ だけ |
| 外の置き場の 13 項目のうち、床の結果に効く新しい種類の候補 | 1（裁定 id の形・台帳の接頭辞）。ADR-16 撤退条件 (1) の閾値は 3。写しの上の実測なので窓の数えには入れない |
| 列の根の表に行を足した組み立てでの床 | 合格（違反 0・まだ分からない 0・外の置き場の実測） |
| 本枝の床 4 本と歯 | 床 4 本 rc 0・`cargo nextest run --workspace` 855 / 855（改訂 b `b1ccbef` と改訂 c の両方） |

## 6. まだ分からないこと

- **v3 の合流先の repo。** 問 2 で問う。scribe3 に置き場は在るが、あなたの「それをfolio2で作ってはだめなのか？」への答えは記録に無い。
- **合流の時機と、v3 の憲法の母体。** folio の実装が scribe3 へ入るのが判定点 ①〜③ の記帳の前か後か。v3 の憲法を scribe2 と folio2 のどちらを母体に合流させるか（v3 の討論の論点 E）。folio2 の憲法の名を引き継げば、撤退条件 (1) に当たる。
- **v3 の境界の形。** 起こしの推奨は「設計ノートの行 → 契約（toml）」1 本（論点 A）だが、採否は討論で決める。型で受けると決めたら判定点 ②③ を問い直す（撤退条件 (2)）。
- **今の器が scribe3 の便を運べるか。** scribe3 は remote を持たず、器の着地の手順は取り込みの要求を前提にしている。判定点 ③ を今の器で測る道がまだ無い。
- **v3 の台帳の接頭辞。** v3 の台帳はまだ無い（論点 F）。承認欄の裁定 id の形（英字 1 字と数字 1 字の接頭辞）に合わなければ、床が承認欄を落とす（ADR-16 の (ア)〜(キ) に無い種類）。
- **撤退条件 (3) の 30 日。** 根拠は M3 の口が使われないまま置かれる期間を区切ることだけで、推測の閾値である。
