# 判断の記録 ADR-22 の承認要求 — 契約表を「M1 で移す」とした古い順序を閉じる（ADR-3 の決定 (8) を狭める）

- 状態: **承認待ち**。起草 2026-09-24・改訂 b・c 2026-09-24・orchestrator 席 fable 5.1 の起草役。
- 正本: `design-intent/adr/ADR-22.yaml`（status: proposed）。反証の試み: 独立の検証役の検証 `~/.local/share/folio2/handoff-2026-09-24/adr22-verify.md`（別の文脈・opus 5.5・**総合 条件付き支持**・blocking 1・文面 6・低い重さ 5）と、起草役の自己反証 `docs/design/adr-22-grill.md`。改訂 c で blocking と文面を全部当てた（当て方の一覧は grill の §7）。起草の記録: `~/.local/share/folio2/handoff-2026-09-24/adr22-draft.md`。
- 起点: 天井の 32 周目（本流 `68196cf`）の文書どうしの整合 F-1（止める・反証で支持）。同じ根の F-2（要件書 CON8 の頭の文）と F-3（要件書 M3 の範囲の節の問う句）も、承認の後の字の直し（§3）で一緒に解く。
- 枝: `docs/adr22` は本流 `68196cf`（ADR-21 の発効・PR #309）の上に在る。
- 規則の表の行 D-14 の止める場合のうち「判断の記録の発効が要る」に当たる。**一括 19（枝 `docs/batch19`）と同じ回で承認を求める。**
- 憲法の条文・前文・schema 節は 1 字も変えない。規則の表の行を足さない。便は起こさない。**判断の記録 ADR-3 と ADR-16 の決定の本文も書き換えない**（改訂の欄と注で断る・ADR-17 / ADR-20 / ADR-21 と同じ形）。
- **🔴 は 1 問**（発効してよいか）。

## 1. 要旨（平易に）

- 2026-09-16 に発効した判断の記録 ADR-3 の決定 (8) は、「scribe2 の便の約束の表（契約表）は、いまの形で先に使い始め、folio2 の段 M1 で設計ノートの YAML の正本と、そこから作る写し（導出物）へ移す」と決めていた。「scribe2 の設計文書のどこまでを移すか」は、あとであなたに聞くことにしていた。
- その後、次のことが決まった。どれも発効済みである。
  - 導出物を作る口は M1 では作らず、M3 で作った（判断の記録 ADR-16・要件 FR11）。
  - 2026-09-24 に scribe2 の設計文書の移行は取りやめになった。scribe2 は残りを前の版の道具のまま片付ける（判断の記録 ADR-21）。
  - M3 の相手は v3 の設計の置き場になった。v3 は設計文書を最初から folio2 の形で書くので、移すものが無い（ADR-21）。
- **ところが、ADR-3 の決定 (8) を狭める記録がどこにも無かった。** ADR-16 も ADR-21 も (8) を名指していない。判断の記録の書き方の決まりでは、発効した決定の範囲を後から変えるときは、変える側の記録の「改訂の欄」に書く。天井の 32 周目がこの食い違いを「止める」と指摘し、反証も支持した。
- **この判断で (8) を狭める。** 中身は ADR-16 と ADR-21 が既に決めたことを (8) の側に記帳するだけで、新しい向きは足さない。
  - 「M1 で移す」の順序は閉じる。導出物の口は M3 に在る。
  - scribe2 の契約表は移さない。いまの形のまま使い終わる。
  - 「契約表は設計ノートの YAML の正本に置き、器が読む写しをそこから作る」という向きは残す。当てる相手は、v3 の設計の置き場が自分の repo で最初から書く契約表である。
  - 「どこまで移すか」の論点は、移す相手が無いので閉じる。ADR-16 の決定 (1) の「契約表を持たない設計文書の移行は M3 の後に改めて問う」の句も、同じ理由で閉じる。
- **やめる条件**: 次のどれかが起きたら、あなたへ問い直す。
  - scribe2 が移行を再開する（1 回で）。
  - 設計文書を folio2 の形でなく持つ外部の利用者が現れ、それを移すと記帳される（1 回で）。
  - v3 の置き場で、契約表から導出物を作れない同じ失敗が 2 回を超える。
  - 数えられない回が 2 回を超えたら、判定を「まだ分からない」に落として渡す。

| 所（ADR-3 の決定 (8) を引く字） | 今 | この判断の後 |
|---|---|---|
| ADR-3 決定 (8) | 「M1 で YAML 正本と導出物へ移す」 | 字は残す。改訂の欄（1 行目）と ADR-3 の注の段で狭める |
| ADR-16 決定 (1) の末尾 | 「契約表を持たない設計文書の移行は CON8 のとおり M3 の後に改めて問う」 | 字は残す。改訂の欄（2 行目）で閉じる |
| ADR-3 の平易文 | 「移すのは M1 で、どこまで移すかはそのとき改めてあなたに聞きます」 | 読み方の 1 段を足す |
| ADR-16 の文脈 (1)・案 d の理由・注 (e) | (8) を生きた向きとして引く | 字は残す。ADR-16 の注の 1 段で読みを断る |
| 要件書 制約 CON8 | 頭の文が「M1 で…導出物へ移す」と言い切る（32 周目 F-2） | 頭の文を第 1.1 版の字の記録にし、今の制約を書く |
| 要件書 M1 の範囲の節の注 | 「移す範囲は CON8 の別の論点」 | 閉じた字へ |
| 要件書 M3 の範囲の節 | 「CON8 のとおり M3 の後に改めて問う」（32 周目 F-3） | 閉じた字へ |

## 2. 🔴 持ち主へ問う項（1 問）

### 🔴 問 1: 判断の記録 ADR-22 を発効してよいか

- **やりたいこと**: 判断の記録 ADR-22 を発効する（案 a）。発効の後に、席が §3 の欄を書く。便は起こさない。改訂の欄は 2 行である。
  - **1 行目**: ADR-3 決定 (8) を狭める。「folio2 の M1 で設計ノートの YAML 正本と導出物へ移す」を M1 の段の義務として読まない。scribe2 の設計文書の中の契約表を、移す相手から外す。YAML 正本の節から導出物を作る向きは、v3 の設計の置き場が自分の repo で最初から書く契約表に当てる。設計文書を移す範囲の論点を閉じる。
  - **2 行目**: ADR-16 決定 (1) の末尾「契約表を持たない設計文書の移行は、要件書 CON8 のとおり M3 の後に改めて問う」を閉じる。問う相手が無いためである。M3 に入れないものの列と、出口の判定点 ①〜③ は変えない。
  - ADR-21 の改訂の欄の 1 行目も同じ ADR-16 決定 (1) を狭めている。それが外したのは範囲の文の中の「移す話」で、末尾の問う句は名指していない。2 行目はその句だけを閉じるので、重ならない。
- **理由**:
  - 32 周目の整合 F-1 は 止める で、反証も支持した。退けた読みは 3 つある。「ADR-21 決定 (0) が (8) も閉じた」「ADR-16 の注 (e) が (8) を狭めた」「M1 を過ぎたので (8) は失効した」である。判断の記録の失効と範囲の改訂の口は、状態と置き換えの欄と改訂の欄だけである。
  - 中身は ADR-16（導出物は M3）と ADR-21（scribe2 は移らない・相手は v3）の帰結で、新しい向きを足さない。向きは「狭める」である。M1 の義務を外し、scribe2 の相手を外し、論点を閉じるだけで、M3 に義務を足さない。
  - 2 行目を足した理由がある。(8) だけを閉じると、ADR-16 決定 (1) の「改めて問う」と要件書の「閉じた」が並んで残る。次の周が同じ形の 止める を拾う見込みが高い（32 周目の F-3 がもうこの句を名指している）。
  - 29〜31 周目の ADR-13 決定 (8) の件も同じ形だった。そのときは注では解けず、ADR-20 の改訂の欄で解いた。
- **精度の損**: ほぼ 0。変わるのは発効した記録の読み方の記帳だけで、道具の振る舞いと、M3 の出口の判定点は変わらない。失うのは「scribe2 の設計文書のどこまでを移すか」の論点の置き場である。scribe2 が移行を再開したら、やめる条件 (1) で戻すかを問う。
- **代替案**:

| 案 | 中身 | 費用 | 失うもの |
|---|---|---|---|
| **a（推奨）** | 改訂の欄 2 行で狭め、決定の本文は書き換えず注で断る | 判断の記録 1 本・注 3 段・平易文 1 段・要件書の字 3 か所 | 無い（読み方の記帳だけ） |
| b | ADR-3 を退役させ、(8) を除いた後継で置き換える | 生きている決定 (1)〜(7)(9) を後継へ写す。ADR-16 と ADR-21 の改訂の行（ADR-3 決定 (7) 行き）・要件書・設計ノートの欄の決まり・規則の表の出所の付け替えを判断する | 決定の生きている判断を「廃止」にする（条 P-7.2 の状態の意味と合わない） |
| c | 判断の記録を起こさず、注だけで断る | 小さい | 欄の決まりと ADR-21 決定 (6) に反する（範囲を変える口は改訂の欄）。32 周目の反証が注 (e) を退けたのと同じ理由で、次の周で 止める に戻る見込みが高い |
| d | ADR-3 決定 (8) の本文を書き換える | 小さい | 承認の逐語が承認した字と今の字が食い違う（ADR-17 / 20 / 21 と一括 17 の問 8 の「書き換えない」の先例に反する） |
| e | 発効した ADR-21 の改訂の欄に 6 行目を足す | 小さい | ADR-21 の承認は 5 行を承認した。同じ回に問えば承認は得られるが、どの承認がどの行を認めたかが 1 対 1 でたどれなくなる |

- **コスト**: この承認は 1 回。一括 19 と同じ回で答えてよい。承認の後に席が §3 の欄を書く（要件書の版が 1 つ上がる。一括 19 と同じ回なら一括 19 の版に同居させる。制約の番号・要件の規範文と番号と段は不変）。便は 0 本。本判断の発効は周の引き金を動かす（どの判断の記録の発効も同じ）。
- **推奨**: a。

回答の例: 「推奨で」「承認する」「c で（注だけ）」「1 行目だけで（2 行目は外す）」。

- **「1 行目だけで」と答えたとき**: ADR-16 決定 (1) の末尾の問う句は生きたまま残る。§3 の #7（M3 の範囲の節の問う句の直し）と #4 の 1 文目（ADR-16 の注で問う句を閉じたと書く文）は書けないので、書かない。32 周目の整合 F-3 はそのまま残り、次の周が「要件書 CON8 は閉じた・ADR-16 決定 (1) は改めて問う」の同じ形の所見を拾う見込みが高い。そのときは、問う句を閉じるかを別の判断の記録で改めて問う。

## 3. 承認の後に席が書く欄（本判断の枝ではまだ書かない）

| # | file | 欄 | 書くこと |
|---|---|---|---|
| 1 | `design-intent/adr/ADR-22.yaml` | status・approval・grill・note・頭の注釈 | accepted と、あなたの逐語・日付・裁定 id・対話面 R-8。grill の欄の who・where・summary を「独立の検証役（opus 5.5・`adr22-verify.md`・`adr22-verify-scripts/`）と起草役の自己反証（`adr-22-grill.md`）」の字に書き換える（先例 ADR-20・ADR-21）。note に【発効】の段 |
| 2 | `design-intent/adr/ADR-3.yaml` | note の末尾 | 【判断の記録 ADR-22 による改訂】の段（§4 の 1）。**決定 (8) の本文は書き換えない** |
| 3 | 同 | plain（平易文）の末尾 | 読み方の 1 文群（§4 の 2）。先例 = ADR-21 の発効で ADR-16 の平易文に読み方の段を足した形。前の文は発効の時の字のまま残す |
| 4 | `design-intent/adr/ADR-16.yaml` | note の末尾 | 【判断の記録 ADR-22 による改訂】の段（§4 の 3）。決定 (1) の末尾の句を閉じたこと、文脈 (1)・案 d の退けた理由・注 (e) の (8) の引きの読み。**決定・文脈・案・撤退条件・承認欄と見出しの字は変えない** |
| 5 | `design-intent/srs.yaml` | 制約 CON8 の題・本文・出所 | 頭の文を第 1.1 版の字の記録にし、今の制約を書く。題を「契約表の段と移す範囲」へ。出所に ADR-22（§4 の 4・32 周目 整合 F-2） |
| 6 | 同 | M1 の範囲の節（scope_m1）の note | 「移す範囲は CON8 の別の論点」を閉じた字へ（§4 の 5・F-3 が名指した行） |
| 7 | 同 | M3 の範囲の節（scope_m3）の作らないの 1 行目 | 「CON8 のとおり M3 の後に改めて問う」を閉じた字へ（§4 の 6・32 周目 整合 F-3） |
| 8 | 同 | meta（版・状態の注・承認欄・来歴の変更の行） | 要件書の版を 1 つ上げる。一括 19 と同じ回で承認されたら一括 19 の版に同居させ、来歴の行に本判断の分を書く。制約の番号・要件の規範文と番号と段は不変（条 P-7） |
| 9 | 席の判断 | `design-intent/adr/ADR-21.yaml` の note の末尾 | 決定 (0) が (8) と ADR-16 決定 (1) の問う句を名指していなかったことと、ADR-22 が狭めたことの 1 段（§4 の 7）。**決定と改訂の欄の 5 行は変えない** |
| 10 | 席の判断 | 変えない所 | 設計ノートの欄の決まりの対応表の行（ADR-3 決定 (8) → CON8・指し先が閉じた字を持つ）・要件書の来歴の欄（第 1.1 版からの変更の行）・docs/design の便の設計ノート 43 本の置き場の行と `delivery-23.md`・`delivery-28.md` の CON8 の引用（folio2 自身の設計ノートの起草の時点の記録）・仕分けの記録・歯の fixture（`tests/fixtures/floor_base/`） |
| 11 | 一括 19 の起草役への連絡 | 席が伝える | 32 周目の整合 F-2（CON8）と F-3（M3 の範囲の節・M1 の範囲の節の注）は ADR-22 の #5〜#7 で直す。一括 19 の直しの一覧では「ADR-22 で直す」と仕分け、同じ字を 2 か所で直さない |
| 12 | 台帳（`bd`） | f2-648 notes | 承認の逐語と日付（条 P-12.2）。32 周目 整合 F-1〜F-3 の閉じ方。撤退条件を数える時機は ADR-21 と同じ |
| 13 | `docs/design/state.md` | 判断の記録の行 | ADR-22 の発効 |

- **注の段の順（一括 19 と同じ回で承認されたとき）**: ADR-16・ADR-21 の注の末尾には、一括 19 も【天井の 32 周目の所見への応答（一括 19）】の段を足す。ADR-22 の段は一括 19 の段の後ろ（注のいちばん最後）に置く。順は、所見への応答（一括）→ 判断の記録による改訂、とする。ADR-16 の注で【天井の 31 周目の所見への応答（一括 18）】→【ADR-21 による改訂】と並べた先例と同じ向きである。床と歯はどちらの順でも通る（独立の検証の写し `comb/` の実測）。模擬の script は段を file の末尾に足す形に直した。

- #1〜#7・#9 を写しの上で当てて床を撃つと（grill の欄の書き換えは当てていない）、合格（違反 0・まだ分からない 0）だった。`schema --check`・`derive --check`・`inject --check` は rc 0、`cargo nextest run --workspace` は §5 の数だった（模擬の script `~/.local/share/folio2/handoff-2026-09-24/adr22-scripts/post_approval_sim.py`・#8 の版上げは一括 19 と同居しうるので当てていない）。
- 模擬で 1 つ直した所がある。ADR-3 の注の段に「要件書 CON8」と書くと、床の行 R-17（散文の言及は型付きの欄にも要る）が落とす（ADR-3 の型付きの欄に CON8 が無い）。§4 の 1 の字は CON8 を名指さない形にした。

## 4. 文案（承認の後に正本へ入れる字）

`<日付>`・`<時刻>`・`<逐語>` は承認の後に埋める。

### 1. ADR-3 の注の末尾の段（#2）

> 【判断の記録 ADR-22 による改訂（<日付> 発効・持ち主の承認 <時刻> JST「<逐語>」）】決定 (8) の「folio2 の M1 で設計ノートの YAML 正本と導出物へ移す」は、判断の記録 ADR-22 の改訂の欄（狭める向き）で、M1 の段の義務として読まない。導出物の口の段は判断の記録 ADR-16 決定 (5)(7) と要件 FR11 の段の欄（M3）が持つ。scribe2 の設計文書の中の契約表は移す相手から外れ（scribe2 は前の版の道具のまま残りの契約を畳む・判断の記録 ADR-21）、今の形のまま使い終わる。
> 契約表を設計ノートの YAML 正本の節に置き、器が読む導出物をそこから道具が作る向きは残る。その向きを M3 の出口の利用者（器の次の世代〔scribe v3〕の設計の置き場）が自分の repo で最初からその形で書く契約表に当てるのは ADR-16 決定 (1)(5) と ADR-21 決定 (1) で、ADR-22 は決定 (8) の側で相手を足さない。設計文書を YAML へ移す範囲の論点（決定 (8) の後ろの文）は、移す相手が無いので閉じた。決定の本文は書き換えない（天井の 32 周目 整合 F-1）。

### 2. ADR-3 の平易文の末尾に足す文（#3）

ADR-3 の平易文は 1 行の文字列なので、末尾に続けて足す。

> （2026-09-24 の後の読み方）scribe2 の設計文書を folio2 の形へ移す計画は、scribe2 の持ち主の裁定で取りやめになりました。scribe2 の契約表は今の形のまま使い終わり、移しません。契約表を設計ノートの形で持ち、器が読む写しをそこから作るのは次の段（M3）で、相手は器の次の世代（v3）の設計の置き場が最初からその形で書く契約表です。どこまで移すかをあなたに聞く話も、移す相手が無くなったので閉じました（判断の記録 ADR-21・ADR-22）。ここより前の文は、発効したときの字のまま残しています。

### 3. ADR-16 の注の末尾の段（#4）

> 【判断の記録 ADR-22 による改訂（<日付> 発効・持ち主の承認 <時刻> JST「<逐語>」）】決定 (1) の末尾「契約表を持たない設計文書の移行は、要件書 CON8 のとおり M3 の後に改めて問う」は、判断の記録 ADR-22 の改訂の欄（狭める向き）で閉じた。scribe2 の設計文書は移らず、器の次の世代（scribe v3）は設計文書を最初から folio2 の形で書くので、問う相手が無い。M3 に入れないものの列と判定点 ①〜③ は変えない。
> 文脈 (1) の「判断の記録 ADR-3 決定 (8) と要件書 CON8 が…と裁定済み」と注 (e) の「ADR-3 決定 (8) は移す順序を決め」の決定 (8) は、ADR-22 の改訂の欄で M1 の段の義務と scribe2 の契約表の相手を外した。案 d の退けた理由の「ADR-3 決定 (8) が「M1 で設計ノートの YAML 正本へ移す」と決めた向き」は、向き（契約表を設計ノートの YAML 正本の節に置き、器が読む導出物をそこから作る・器の設計文書の散文を道具が直に読まない）が ADR-22 の後も生きているので、理由として立つ。「M1 で」の段の字は ADR-22 で閉じた。
> 決定の本文と案の字は書き換えない（天井の 32 周目 整合 F-1・F-3）。

### 4. 要件書 制約 CON8（#5）

- 題: 「M1 の順序と移す範囲」→「契約表の段と移す範囲」（本文の頭が第 1.1 版の記録になるので、題を今の中身に合わせる。番号は変えない）
- 本文の改めた後:

> 第 1.1 版は、契約表を今の形（scribe2 の設計文書の中の機械が読む区間）で着地させ、folio2 の M1 で設計ノートの YAML 正本と導出物へ移す、と定めた（判断の記録 ADR-3 決定 (8)）。scribe2 の設計文書のどこまでを YAML へ移すかは同じ版の承認時に持ち主へ出す別の論点とし、その裁定は、最初の範囲は契約表を持つ設計文書だけ（他の設計文書の移行は M1 の後に改めて問う）だった。その後、導出物の口の段は M3 になり（判断の記録 ADR-16 決定 (5)(7)）、他の設計文書の移行を問い直す時機は M3 の後になった（ADR-16 決定 (1)・第 1.33 版で追随）。scribe2 の設計文書の移行は 2026-09-24 に scribe2 の持ち主の裁定で取りやめになり、M3 の利用者は判断の記録 ADR-21 で器の次の世代（scribe v3）の設計の置き場へ読み替えた。今の制約は次のとおりである（判断の記録 ADR-22 が ADR-3 決定 (8) と ADR-16 決定 (1) の末尾の問う句を狭めた）＝scribe2 の契約表は今の形のまま使い終わり、移さない。契約表を設計ノートの YAML 正本の節に置き器が読む導出物をそこから作るのは、M3 の出口の利用者が自分の repo で最初からその形で書く契約表である。設計文書を移す範囲を問う論点は、移す相手が無いので閉じた。

- 出所の欄: 頭の「判断の記録 ADR-3 決定 (8)・」の後に「判断の記録 ADR-22（M1 の順序・scribe2 の契約表の相手・範囲の論点を閉じた）・」を足す。basis は変えない。

### 5. 要件書 M1 の範囲の節の注（#6）

- 今: 「M1 の実装の着手は CON1 の順序に従う。設計文書（scribe2 の docs/design）を YAML 正本へ移す範囲は CON8 の別の論点。」
- 改めた後: 「M1 の実装の着手は CON1 の順序に従う。設計文書（scribe2 の docs/design）を YAML 正本へ移す範囲の論点は CON8 に在ったが、scribe2 の設計文書の移行の取りやめで閉じた（判断の記録 ADR-21・ADR-22）。」

### 6. 要件書 M3 の範囲の節の作らないの 1 行目（#7）

- 今の末尾: 「…判断の記録 ADR-21 はこの列を変えない。契約表を持たない設計文書の移行は要件書 CON8 のとおり M3 の後に改めて問う」
- 改めた後の末尾: 「…判断の記録 ADR-21 はこの列を変えない。判断の記録 ADR-16 決定 (1) の末尾の「契約表を持たない設計文書の移行は M3 の後に改めて問う」は、判断の記録 ADR-22 で閉じた（scribe2 の設計文書は移らず、v3 は最初から folio2 の形で書く・要件書 CON8）」

### 7. ADR-21 の注の末尾の段（#9・席の判断）

> 【判断の記録 ADR-22（<日付> 発効）】決定 (0) の「scribe2 の設計文書の移行のためだけに置いた論点の閉じ方」は、判断の記録 ADR-3 決定 (8)（M1 で契約表を移す順序と、移す範囲の論点）と ADR-16 決定 (1) の末尾の問う句を名指していなかった（天井の 32 周目 整合 F-1）。この 2 つは判断の記録 ADR-22 の改訂の欄が狭めた。本判断の決定と改訂の欄の 5 行は変えない。

## 5. 実測の要点

| 項目 | 値 |
|---|---|
| ADR-3 決定 (8) を名指す改訂の行（判断の記録 21 本） | 0（ADR-3 行きは ADR-16 と ADR-21 の決定 (7) の 2 行だけ） |
| (8) を生きた向きとして引く所 | 9 か所（ADR-3 決定 (8) と平易文・ADR-16 の文脈 (1)・決定 (1) の末尾・案 d・注 (e)・要件書 CON8・M1 の範囲の節の注・M3 の範囲の節）。改訂の欄が 2、承認の後の字が 7 |
| 変えない所 | 5 種（設計ノートの欄の決まりの対応表の行・要件書の来歴・folio2 自身の便の設計ノート 43 本と CON8 の引用 2 本・仕分けの記録・歯の fixture） |
| 本枝の床 4 本 | `check` 合格（違反 0・まだ分からない 0）・`schema --check`・`derive --check`・`inject --check` rc 0 |
| 本枝の歯 | `cargo nextest run --workspace` 861 / 861 |
| 承認の後の模擬（#1〜#7・#9） | 床 4 本 rc 0（合格・違反 0・まだ分からない 0）・`cargo nextest run --workspace` 861 / 861 |
| 改訂 c の後の本枝（`8ef06c9`） | 床 4 本 rc 0（合格）・`cargo nextest run --workspace` 861 / 861 |
| 改訂 c の後の模擬（本枝 + #1〜#7・#9） | 床 4 本 rc 0（合格）・861 / 861 |
| 一括 19（`5b913f0`）に本枝の 6 commit を載せて模擬を当てた写し | 床 4 本 rc 0（合格）・861 / 861。ADR-16 の注は 一括 18 → ADR-21 → 一括 19 → ADR-22 の段の順になった |

## 6. まだ分からないこと

- **一括 19 との同居の形。** 要件書の版上げを一括 19 と同じ版に置くかは、一括 19 の起草と取り込みの順で決まる。席が決める。
- **folio2 自身の設計ノートの移行の根拠。** docs/design の便の設計ノート 43 本は「M1 で YAML 正本へ移す」と書き、`delivery-23.md` は要件書 CON8 の順序を引いた。(8) の相手は scribe2 の契約表で、folio2 自身の設計ノートではない。folio2 自身の設計ノートを YAML 正本へ移す時機と範囲を決めた判断の記録は、起草の読みでは見つからなかった（語彙の「設計文書」の項の注が「移行中」と書くだけ）。本判断の外の論点である。
- **判断の記録 ADR-6 決定 (2) ③「最初は 0 本の棚」は、本判断に入れず別の判断の記録で扱う（改訂 b・2026-09-24 の判定）。** 32 周目の整合 F-5（直す）が名指した食い違いで、ADR-16 決定 (2)(カ)（骨格の命令が最初の判断の記録を書く）と改訂の欄で結ばれていない点は F-1 と同じ種類である。入れない理由は 3 つある。
  - 根が違う。本判断が狭めるのは、契約表を移す順序と移す範囲の論点（ADR-3 決定 (8) と、その時機を定め直した ADR-16 決定 (1) の句）で、根は scribe2 の設計文書の移行の取りやめである。ADR-6 ③ は相談窓口の棚の最初の中身で、根は ADR-16 決定 (2)(カ) の骨格の命令である。
  - 向きが決まらない。棚を「0 本」から「骨格が書く 1 本から始まる」に変えるのは、中身を足す向きにも読める。狭める向きと言えるかは、それだけで反証の論点になる。
  - 重さが違う。F-5 は 直す で、行 D-14 の止める場合に当たらない。一括 19 は相談窓口の質問 q3 の字と ADR-6 の注を直した。改訂の欄の行は、F-5 の扱いを決める別の判断の記録（ADR-16 決定 (2)(カ) の帰結として ADR-6 決定 (2) ③ を改める記録）で起こす。起こすかと時機は席が決める。控えは台帳 f2-648.198 に在る。
- **独立の検証。** 独立の検証役の検証（`adr22-verify.md`・条件付き支持）を改訂 c で当てた。改訂 c の字の再検証（08a1a1b）は支持・blocking 0 で、そこで出た低い重さ 1 件を改訂 d（952e00b）で当てた。改訂 d は字の直しだけである。
