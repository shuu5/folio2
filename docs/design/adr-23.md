# 判断の記録 ADR-23 の承認要求 — 憲法 第 1.4 版（schema 節の機構の段の規則と前文の平易文の字の直し）

- 状態: 起草・持ち主の承認待ち（2026-09-24 起草・orchestrator 席〔AI・fable 5.1〕の起草役〔opus 5.5〕・作業場所 `.worktrees/planner-adr23`・枝 `docs/adr23`・土台 = 本流 68196cf）。持ち主の逐語はこの文書にも正本にも書いていない（条 P-12.3）。
- 改訂 b（2026-09-24）: 一括 19 の起草役と検証役の申し送りを当てた（判断の記録 461487a・検証の記録の問 11〜13）。中の案を代替案 (d) に、6 条の段の値 M1 を変えない判定を決定 (1) ⑤ に、面の名札の便を承認の後の欄 17 に置いた。規則の字と改訂の欄は変えていない。
- 改訂 c（2026-09-24）: 独立の検証（`~/.local/share/folio2/handoff-2026-09-24/adr23-verify.md`・条件付き支持・案 a 支持・blocking 3・文面 9）を当てた。規則の字の「床の出力」を「folio check の出力」に、根拠の括弧を（P-3.3・P-4.1）に改めた（改訂の欄の 2 行目を `--emit-amends` で撃ち直した）。面の名札の便を決定 (4) の周の前提に入れた。一括 19 の欄の番号の指しを外した。応答の一覧は検証の記録の §独立の検証役の指摘と応答。
- 対象: `design-intent/adr/ADR-23.yaml`（status proposed）。
- 出所: 天井の 32 周目の実態 F-1（止める・反証 支持）と読みやすさ F-1（直す）。一括 19 の仕分け（`docs/design/batch19-triage.md`・枝 `docs/batch19`）の 🔴 問 4 は、この文書の 🔴 問 1 を指す。
- 反対側からの検証（条 A-2.3）: `docs/design/adr-23-grill.md`（起草役の自己反証）と、独立の検証役の報告 `~/.local/share/folio2/handoff-2026-09-24/adr23-verify.md`（別の文脈・opus 5.5・条件付き支持・blocking 3 は改訂 c で全部当てた）。以下は起草の時点の字： 総合は改訂を支持（条件付き・条件 4 つは全部判断の記録に書いた）。独立の検証役へ渡す問い 5 つを同じ file の末尾に置いた。
- 起草の時点では憲法の正本を 1 字も変えない。承認の後に、下の「承認の後に席が書く欄」の表の全部を 1 本の取り込みの要求で置く。

## 要旨（やさしく言うと）

憲法の決まりの 1 つが、道具の実際の動きと食い違っています。その字を、実際に守れる形に直します。

1. **仕掛けがまだ無い条の扱い。** 6 つの条（P-11・P-13・P-15・P-17・P-18・A-3）は、守らせる仕掛けがまだ作られていません。憲法の型の節は「その条は検査の結果を「まだ分からない」として出す」と約束しています。でも検査の道具は何も言わずに「合格」を返します。道具に「まだ分からない」を出させると、仕掛けができるまで毎日の確かめが全部止まります。そこで約束を「合否の数には入れず、検査の出力と憲法のページに条ごとに並べて見せる」に直します。見せる 1 行を道具に足す小さな便を、あとで 1 つ出します。憲法のページの「M1 で動く」という札も「仕掛けがまだ無い」に直す便を出し、2 つが済んでから次の見直しの周を回します。
2. **前書きの「迷ったら」の文。** 今は「全部そろえる」より「そろっている」で、同じ言葉が 2 回出て違いが分かりにくいです。「何でも漏れなく入れる」より「同じ形にそろっている」に直します。

条の本文と見出しは 1 字も変わりません。どちらの字も、変えるには憲法の改訂の手順が要るので、1 回にまとめました。

---

## 🔴 問 1. 判断の記録 ADR-23 を発効し、憲法を第 1.4 版に上げてよいか

**やりたいこと。** 判断の記録 ADR-23 の決定 (1)〜(5) を発効する。憲法の字は次の 2 つで、どちらも逐語である（改訂の欄の 2 行と同じ字）。

| 場所 | 今の字（第 1.3 版） | 新しい字（第 1.4 版） |
|---|---|---|
| schema 節の機構の段の規則（`schema.mechanism_live_rule`） | kind が reject / build-check でも live が now でない条は、その検査結果を「まだ分からない」として出す（P-4.2）。 | kind が reject / build-check でも live が now でない条は、機構がまだ無い条として床の判定（違反・まだ分からない）に数えず、folio check の出力と面の機構の欄に条ごとに表に出す（P-3.3・P-4.1）。live は実在する検査を止める旗ではなく、機構が実在した版で now に改める。 |
| 前文の平易文（`precedence.plain`）の 1 文目の頭 | 迷ったら「全部そろえる」より「そろっている」、 | 迷ったら「何でも漏れなく入れる」より「同じ形にそろっている」、 |

前文の平易文の残り・前文の規範文・根拠の行、条の id・見出し・段・縛る相手・規範文、schema 節のほかの欄、6 本の条の機構の欄は 1 字も変えない。6 本の条の実在の段の値（M1）も変えない。値域に今の実態を正しく言う値が無く、値域を足すのは別の改訂になるからである（決定 (1) ⑤。値だけを変えるなら条 A-2.2 の機構の欄の外で改訂には当たらない）。

あわせて、実装の便を 1 本起こす（決定 (3)）。`folio check` が、機構の種別が reject か build-check で実在の段が now でない条を、標準エラーに 1 行で並べる。判定・数・終了コード・標準出力（要約の行を含む）は 1 byte も変えない。folio2 自身の憲法の上でその一覧（今は 6 本）を固定する歯を 1 本置く。もう 1 本、憲法の面の名札「M1 で動く」を「機構がまだ無い（床は判定しない）」の向きへ改める便を起こす（承認の後の欄 17）。天井の周は、この 2 本が着地した後に本流で回す（決定 (4)）。

```
# 機構がまだ無い条（床の判定の外・憲法 schema.mechanism_live_rule）: P-11（M1）・P-13（M1）・P-15（M1）・P-17（M1）・P-18（M1）・A-3（M1）
folio check: 合格（違反 0・まだ分からない 0）
```

**理由。**

- 文書が「出す」と約束し、道具が 0 件を出すのは、実態と逆の約束である（天井 32 周目 実態 F-1・止める・反証 支持）。
- 道具に出させる案（案 b）では、床は 6 本の機構が全部できるまで終了コード 2 を返し続ける。6 本の機構はどれも便の列に無い。便の確かめ・受付の前の確かめ・天井の門は床の終了コード 0 で受けるので、全部止まる。受け流す運びにすると、本物の「まだ分からない」が同じ終了コードに埋もれる。
- 案 a なら床は今のまま運べ、機構がまだ無い条は検査の出力と憲法の面の 2 か所で見える。判定の 3 値（合格・不合格・まだ分からない）の意味も割らない。
- 初日の床（day-1・814366c）も 1 つの条を判定に数えなかった。ただし名は「まだ分からない」で、在る計算の結果（編集時の本数 0）を段で抑えていた（案 d の形）。案 a は名を「機構がまだ無い条」に変え、段で在る検査を抑えることを字で禁じる。
- 規則の字は `folio check` を名指す。`folio build` の床の行は触らない（「床の出力」と書くと build の行も含むと読め、約束した出力が無いという同じ形の所見が立つ）。
- 前文の平易文は条 A-2 の改訂の範囲に在り、別に直すと改訂が 2 回になる（読みやすさ F-1・入口の「はじめて読む人」の最初の行き先）。

**代替案。** どれも判断の記録の案の欄に、退けた理由付きで在る。

- **(b) 床に入れる（6 件の「まだ分からない」を判定に数える）。** 約束の字は保てる。上の理由で床の運びが全部止まる。床の事例のうち終了コード 0 を期待する 21 件も全部終了コード 2 に変わり、凍結した期待値の書き直しが要る。
- **(c) 6 本の条の注で断るだけ。** 一番軽い。規則の字が「出す」のまま残り、次の周が同じ 止める を立てる見込みが高い。
- **(d) 中の案＝条ごとに「まだ分からない」を判定とは別の行に出し、要約に別枠で数える（判定と終了コードは変えない・一括 19 の起草役の推奨）。** 床は運べ、「まだ分からない」の字が出る。しかし要件書の 3 値の定め（まだ分からない＝検査が動かせなかった・合格として表示しない）と要件 FR5 と割れる読みが立ち、割れないようにすると判定に入る「まだ分からない」と入らない「まだ分からない」の 2 種ができる（条 N-3.1 の例外の口に触れる恐れ・要件書にも断りが要る）。「改訂なしで充たせる」読みも、規則の字が 3 値の検査結果として出すと言うので成り立ちにくい（検証の記録の問 11）。便は M の見込み。
- **(e) 要約の行に、まだ分からないと別の名の数を足す。** 見える力は強い。要約の字の形を見る歯は約 21 か所・10 file（逐語だけなら 15 か所・9 file）で、多くの契約表も引いており、便は M になる。数だけでは条が分からない。撤退条件 (3) の乗り換え先に置いた。
- **(f) 6 本の機構の種別を「人が目で確かめる」か「機構なし」に書き換える。** 手順が要らない。設計した守らせ方と極性一覧の元データが消え、戻し忘れても何も落ちない。
- **(g) 前文の直しを載せない。** 問いは単純になる。前文を直すのに改訂がもう 1 回要る。下の部分承認で (2) だけを退けられる。

**コスト。**

- 持ち主の承認 1 回（この問）。
- 席の取り込みの要求 1 本（下の書き込みの表）。凍結 anchor 1 本（605 行・生成物）と索引の 3 行が増える。歯は版上げでは 1 本も動かない。
- 便 1 本（大きさ S の見込み・試作は 1 つの関数の 20 行ほどで既存の歯 861 本は 1 本も落ちなかった）。
- 天井の印は正本が変わるので古くなる。次の周は便の着地の後の本流で組む（決定 (4)）。
- 本判断の取り込みは、1 つ前の番号の判断の記録（枝 `docs/adr22`）の取り込みの後に置く（判断の記録の面の歯が番号の並びを前提にする・下の「床と歯の結果」）。

**部分承認。** 承認は 1 回で求めるが、2 つは別々に退けられる。どちらを退けても、残りで第 1.4 版に上げる。

| 退けるもの | 外すもの |
|---|---|
| (1) 機構の段の規則と便 | 改訂の欄の 2 行目・決定 (1)(3)(4)・撤退条件 (1)(2)(3)(5)・案 a〜f の字。実態 F-1 は一括 19 の問 4 の代替案 (A)（床に入れる便）か (B)（注で断る）を改めて問う |
| (2) 前文の平易文 | 改訂の欄の 1 行目・決定 (2)・撤退条件 (4)・案 g。読みやすさ F-1 の控えを台帳に起こす（下の欄 19） |
| (1) と (2) の両方 | 改訂は起こさない。判断の記録 ADR-23 は proposed のまま退役の扱いを別に問う |

**推奨 = 承認する（2 つとも）。**

---

## 承認の後に席が書く欄

全部を 1 本の取り込みの要求で置く（決定 (3) の便と欄 17 の便は別）。**順。** 凍結 anchor は床が違反 0 のときだけ作れるので、同じ回に承認する一括 19 と枝 docs/adr22 の判断の記録の記帳を先に書き、床が合格してから本判断の版上げ（欄 1〜5）と凍結（欄 10・11）を書く（一括 19 の問どうしの依存の表の順）。凍結の前に、その時点の本流の上で `folio check --emit-amends` の 2 行を測り直し、判断の記録の改訂の欄と一致することを確かめる。

| # | file | 欄 | 書く値 |
|---|---|---|---|
| 1 | `design-intent/constitution.yaml` | 先頭の注の 1 行目 | `（v1.3・` を `（v1.4・` に。版の来歴の末尾（`・orchestrator 席 fable 5.1）` の前）に `・第 1.4 版 <発効日> = 判断の記録 ADR-23（schema 節の機構の段の規則と前文の平易文の字の直し・規範文と見出しは 0 本）` を足す |
| 2 | 同 | 先頭の注の「状態」の行 | 版の来歴の末尾（`規範文は 0 本）。` の `。` の前）に `、第 1.4 版は schema 節の機構の段の規則と前文の平易文（判断の記録 ADR-23・承認 <発効日>・規範文と見出しは 0 本）` を足す |
| 3 | 同 | `meta.version` | `v1.4` |
| 4 | 同 | `schema.mechanism_live_rule` | 上の表の新しい字 |
| 5 | 同 | `precedence.plain` | 上の表の新しい字（1 文目の頭だけが変わる・残りは同じ字） |
| 6 | `design-intent/adr/ADR-23.yaml` | `status` | `accepted` |
| 7 | 同 | `grill` | `{when: 2026-09-24, who: …, where: docs/design/adr-23-grill.md, summary: …}`（起草役の自己反証と独立の検証役〔adr23-verify.md・条件付き支持・blocking 3 は改訂 c で当てた〕の両方の要旨） |
| 8 | 同 | `approval` | `{who: 持ち主, date: <発効日>, ruling: f2-648 notes <日付 時刻> JST（…）, verbatim: <逐語>, surface: R-8}` |
| 9 | 同 | 先頭の注の「状態」の行 | 発効の日と承認の逐語 |
| 10 | `design-intent/anchors/constitution-v1.4.yaml` | 新しい file | `folio check --dir design-intent --freeze-anchor` が書く（手で書かない） |
| 11 | `design-intent/anchors/index.yaml` | 末尾 | 同じ命令が 1 項（3 行）を追記する |
| 12 | `docs/design/adr-23.md` | 頭の状態の行 | 「承認済み・発効（<逐語>・<日付 時刻> JST）」に |
| 13 | `docs/design/batch19-triage.md` | 🔴 問 4 と §承認の後に席が書く欄 | 問 4 の答えを本問の答えに結ぶ字に。実態 F-1 の後の便を書く行（改訂 f で「folio check が標準エラーに 1 行・要約の行と終了コードと標準出力は不変」の字に揃えた行）が、決定 (3) の便と欄 17 の便の 2 本を指すことを確かめる。一括 19 の欄の番号はこの文書から指さない |
| 14 | `docs/design/state.md` | 憲法の版の行・判断の記録の行・次の一手 | 憲法 = 第 1.4 版（発効日・逐語・ADR-23）／決定 (3) の便の契約表の起草と受付／便の前は天井の周を回さない |
| 15 | 台帳（`bd`） | f2-648 の notes | 承認の逐語・日付・裁定 id（条 P-12.2）・決定 (3) の便の件を起こしたこと・決定 (4) の順 |
| 16 | 台帳（`bd`） | 決定 (3) の便の件 | 新しい件を起こし、契約表（`docs/design/delivery-<n>.md`）の行を起草する |
| 17 | 台帳（`bd`）と契約表 | 面の名札の便（決定 (4) が天井の周の前提に置く） | 実在の段が now でない条の憲法の面の機構の欄の名札「M1 で動く」（`crates/folio/src/face_labels.rs` の mechanism_live_label）を、「機構がまだ無い（床は判定しない）」の向きの字へ改める便の件を起こす。「まだ分からない」の字は使わない（検証の記録の問 11）。決定 (3) の便と 1 本に束ねるかは契約で決める（束ねると面の fixture `tests/fixtures/face/expected.html` も write-set に入る）。面の見た目の直しの便の数え（判断の記録 ADR-5 の撤退条件 ②）に当たるかは契約で確かめる |
| 18 | `design-intent/constitution.yaml` | 条 P-18 の機構の注（`mechanism.note`） | 「それまで P-18.3 / P-18.4 の充足は「まだ分からない」として扱う（P-4.2）」を、P-18.3 は極性一覧を出す口が無い・P-18.4 は編集時の guard が 0 本で分かっている未達（床は判定しない）の字へ直す（決定 (1) ①・機構の注は条文の 5 欄の外で改訂に当たらない・条 P-13.3 の記録は本判断）。欄 10 の凍結より前に置く |
| 19 | 台帳（`bd`） | (2) を退けたときだけ | 読みやすさ F-1（前文の平易文の言い換え）の控えの件を起こし、次の憲法の改訂に載せる |
| 20 | `docs/design/adr-23-grill.md` | 末尾 | 独立の検証の後の再検証があればその節を足す（改訂 c の時点で §独立の検証役の指摘と応答は在る） |

- **条の改訂来歴（amended_by）は置かない。** schema 節と前文は改訂来歴の欄を持たない（`adr/schema.yaml` と憲法 schema の precedence.optional が空）。改訂は本判断の改訂の欄と憲法の先頭の注の版の行だけが持つ（判断の記録 ADR-17 の前文の直しと同じ形）。
- **CLAUDE.md は触らない。** 模擬で `folio inject --check` は 68 行・7912 byte で一致した。
- **要件書・語彙・規則の表は触らない。** 規則の字は憲法の schema 節にだけ在る（`git grep mechanism_live_rule` の全数 = 憲法・凍結 anchor 4 本・床の fixture 2 本と床の事例・設計ノート 2 本）。床の事例（`tests/floor_cases.yaml`）の `mlr_old` と fixture の写しは第 1.0 版の形の凍結で、変えない。

## 決定 (3) の便の要点（契約表の起草の材料）

| 項目 | 値 |
|---|---|
| 書き換える所 | `folio check` の出力（`crates/folio/src/main.rs` の要約の行を出す所か、`check.rs` の材料）と歯 1〜2 file（例 `crates/folio/tests/check.rs`）。幅 120 で正規化した行数は `main.rs` 655・`check.rs` 1002（独立の検証の測り）で、上限との差は契約で測る |
| 要件の当て先 | FR5 の規範文は `folio build` の 3 値の口で、この行を求めていない。契約表の行の要件は、FR5 を当てて注で「3 値の外の 1 行・判定は不変」と断るか、条 P-3・P-4 の歯として置くかを契約で決める |
| 出す条 | 置き場の憲法の条のうち、機構の種別が reject か build-check で、実在の段が now でないもの（憲法の条の並び） |
| 出す字 | `# 機構がまだ無い条（床の判定の外・憲法 schema.mechanism_live_rule）: <id>（<段>）・…`（下限）。0 本なら出さない |
| 出す所 | 標準エラー・要約の行の直前。凍結の口と差分の印字の口でも同じ。凍結を断る道（`main.rs` の After::Refused・要約の行より前に終了コード 1）では出さない |
| 変えないもの | 判定・違反とまだ分からないの数・終了コード・標準出力の全部（要約の行を含む）・folio build の床の行・面 |
| 歯 | 当たる条を持つ fixture で行が出る／0 本の fixture で出ない／どちらでも判定と終了コードと標準出力が同じ／folio2 自身の憲法で一覧が 6 本（P-11・P-13・P-15・P-17・P-18・A-3・段 M1）に一致。便は新しい dir を作れないので、当たる側は床の事例の土台 `tests/fixtures/floor_base/`（第 1.0 版・now でない条を持つ）を一時 dir へ写し、0 本の側は `folio init` の骨格（唯一の条が now）で組む。一覧を固定する歯は、段の欄の変更を審査に引き出すための意図した 2 つ目の写しと歯の注で断る |
| 試作の実測 | 写し `adr23clone2` に 1 つの関数の 20 行ほどを当て、上の 2 行の出力と終了コード 0 を得た。既存の歯 861 本は 1 本も落ちなかった |
| 門 | 書き換えが実装と歯だけなので、印が古くても門は受付を止めない（`gate.rs` の run・設計文書の正本が書き換えの一覧に無ければ通す） |

## 承認の後の取り込みの要求の書き込みの見積（写しで実測）

本流 68196cf に 1 つ前の番号の判断の記録（枝 `docs/adr22` の file・提案中）を足した写しで、上の表の 1〜11 を置いて凍結まで模した。承認の欄は模擬の字である。

| file | 変わる行 |
|---|---|
| `design-intent/constitution.yaml` | 10 行（足す 5・消す 5）。欄 18 の条 P-18 の注の直しでさらに 1 行 |
| `design-intent/adr/ADR-23.yaml` | 状態・検証・承認の 3 欄と先頭の注の 1 行 |
| `design-intent/anchors/constitution-v1.4.yaml` | 605 行（新しい file・生成物） |
| `design-intent/anchors/index.yaml` | 3 行 |
| `CLAUDE.md` | 0 行 |
| 歯 | 0 本 |

## 凍結 anchor への影響（実測）

**列の根の要約値は変わらない。列の 5 枚目が増えるだけである。**

| 項目 | 今（第 1.3 版） | 承認の後（模擬） |
|---|---|---|
| 索引の項の数 | 4（v1.0〜v1.3） | 5（v1.4 の previous = v1.3） |
| 根（v1.0）の要約値と床の定数 `root_digest` | acb52acd…866ed | 同じ |
| v1.4 の anchor の要約値 | — | 5670502a…a7018（模擬の承認の字のとき） |

- v1.3 と v1.4 の anchor の差は 6 つの塊である。版と直前、承認一覧の 1 項（ADR-17 から ADR-23 へ）、`mechanism_live_rule`、前文の `plain`、要約値。
- v1.4 の要約値は承認一覧の字を覆うので、本物の承認の字で凍結すると模擬の値とは違う値になる。値は `--freeze-anchor` が決め、手で書かない。
- 改訂の欄の 2 行は `folio check --emit-amends` の印字と 1 字も違わない。凍結の後の床は、差分 2 件を改訂の欄 2 行と 1 対 1 に消し込んで合格した。

## 床と歯の結果（2026-09-24 実測）

| 写し | `folio check` | `schema --check` | `derive --check` | `inject --check` | 歯（nextest） |
|---|---|---|---|---|---|
| 本流 68196cf | 合格 | 一致 | 一致 | 68 行・7912 byte 一致 | 861 本中 861 本 合格 |
| 起草の版（d54a9bc・本流 68196cf の上） | 合格（違反 0・まだ分からない 0） | 一致 | 一致 | 一致 | 861 本中 860 本 合格。落ちるのは `site_on_the_real_sources_passes_parts_check_and_face_check` の 1 本（判断の記録の面が番号 1〜22 でそろう前提・枝 docs/adr22 の記録が無い） |
| 本枝（改訂 c・枝 docs/adr22 の先端 14661f4 の上） | 合格（違反 0・まだ分からない 0） | 一致 | 一致 | 一致 | 861 本中 861 本 合格 |
| 承認の後の模擬（凍結まで） | 合格 | 一致 | 一致 | 一致 | 861 本中 861 本 合格 |
| 承認の後の模擬 + 決定 (3) の試作 | 合格（標準エラーに 1 行） | — | — | — | 861 本中 861 本 合格 |

## まだ分からないこと

- v1.4 の anchor の本物の要約値。承認の字で決まるので、凍結するまで分からない。
- 次の天井の周が新しい規則の字をどう読むか。特に、忠実さか整合の観点が「schema 節が条 P-4.2 を狭めた」と読むか（撤退条件 (3)・検証の記録の問 2）。
- 面の名札「M1 で動く」と実在の段の意味（「M1 で実在する」）が、M1 の段を過ぎた今「もう動いている」と読まれるか。名札は欄 17 の便で改め、その便の着地までは天井の周を回さない（決定 (4)）。実在の段の値と意味は変えない（決定 (1) ⑤）。
- 面の名札の便が ADR-5 の撤退条件 ②（面の見た目の直しの便の数え）に当たるか。契約で確かめる。

条 P-18.4・P-17.2 の未達は「まだ分からない」ではなく分かっている事実なので、判断の記録の帰結に置いた（本判断で閉じない・閉じるのは P-18・P-17 の機構の便）。
- 決定 (3) の便の書き換える file の余地（行の上限との差）。契約表の起草のときに測る。
