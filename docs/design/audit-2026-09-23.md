# 設計ノート: 総点検（2026-09-23）— 現状の整理・台帳の整理・契約と歯の負債の是正

- 状態: 起草（承認待ち）。持ち主の指示（2026-09-22 23:3x JST・台帳 f2-648 notes）「次の切りの良いところで一旦ここまでの実装を見直し現状の整理とタスクおよび台帳の整理と設計のリファクタリングを行いたい。…根本的な設計の部分は古い scribe2 が作っていてそこに大量に問題が隠されているのではないかと疑っている。…beads 台帳を徹底的にチェックし実装とも整合性を照らし合わせつつ契約の問題点についてリファクタリングしたい」への応答。
- 切り: 天井の 26 周目の印（main aff1b32・4 観点 合格・門 通す）。以後、本ノートの裁定が出るまで新しい便は出さない。
- 材料: 材料集め役（AI・opus）が読むだけで作った棚卸し `~/.local/share/folio2/handoff-2026-09-23/audit-inventory.md`（571 行・8 表・基準 main aff1b32）。本ノートの数はすべてそこから引く（数え方は棚卸しの各表の前の 1 行）。
- 置き場: この文書は folio2 の設計ノート。規則を置かない（P-5.4・N-2.1）。ここで決まったことは、判断の記録（ADR-15）・要件書の版・規則の表の行・台帳の起票へそれぞれ写す。
- 読み方: §1 が「何が分かったか」、§2 が「持ち主の疑いにどう答えるか」、§3 が「何をどの順で直すか」、§4 が持ち主への問い（🔴 6 つ）。

## 1. 現状の整理（棚卸しの要点）

### 1-a. 揃っているもの（直す対象ではない）

| 何 | 実測 |
| --- | --- |
| 台帳と契約と着地の対応 | 台帳 142 件（親を除く）のうち 107 件が契約表 105 本の行に当たり、着地 commit 105 本と 1 対 1。契約の `done` が言う歯の本数と main の実際の本数が食い違うのは 便 97 の 1 本だけ（3 本と書いて 1 本・残り 2 本は便 98 が畳んだ） |
| 床と天井 | 床は合格（違反 0）。26 周目は 4 観点 合格・止める 0。印は 便 104 で門と同じ規則になり、門は 通す |
| 実装の大きさ | src 44 file・正規化 26,160 行（上限 60,000）。歯 38 file・22,134 行（対 src 比 84.6%・上限 100%）。`#[test]` 772 本（tests 635・src 122・退役 15） |
| 外部依存 | 直接 2 crate（clap・yaml-rust2）。R-6 の範囲 |

**設計文書（design-intent）そのものは、天井の周 26 回と床で毎回検査されており、持ち主の疑う「大量の問題」はそこには無い。** 問題は、設計文書の外側 = 契約表・歯・台帳・実装の module の切り方に在る。

### 1-b. 器（scribe2）の制約が形を決めた跡

| 跡 | 実測 | どこが問題か |
| --- | --- | --- |
| 行数の上限 1,500（幅 120 正規化）のために、中身を変えずに file を割った便 | 4 本（便 35・87・89・100）。便 76 は上限のために write-set から file を外した | module の境界が「責務」でなく「上限」で決まっている |
| 今、上限を超えている歯の file | `tests/face.rs` 1,785（余地 **−285**）・`tests/face_index.rs` 1,528（余地 −28） | この 2 本を write-set に含む便は受付で断られる＝面の生成器の歯を足せない |
| 余地 300 未満で size M が通らない src | 4 本（adr.rs 110・face_index.rs 122・note.rs 138・face_constitution.rs 206） | 判断の記録・入口・設計ノート・憲法の面に M の便を運べない |
| 契約に要約値（sha256）や残差の byte を写した契約 | 44 本 / 105 本（上位: 便 7 が 15・便 99 が 15 + 残差 22・便 38 が 13・便 102 が 11 + 残差 8） | 先行の便が着地すると値が古くなり、審査 FAIL か改訂を生む（便 100〜104 で実際に起きた） |
| 改訂を持つ契約 | 24 本・41 回（便 68 以降に全部集中・便 0〜67 は 0 回）。本文に理由が在る 20 件のうち、便の中身の誤りは 0 件。検証役への応答 10・受付の断り（名指し）2・審査の字面 2・器の質問 2・行数の測り直し 2・門 1・着地後の注記 1 | 改訂の理由は「器が契約を読む形」に偏る。便 104 は 3 回改訂し、うち 2 回は `型::項目` の名指しが base に解けないことだけが理由 |
| write-set の重なりで順序を待った便 | 6 組（便 73・84・98・102・103 ほか） | 器の dispatcher は待ちの便を先行の着地直後に自動で起動するので、契約の数値が着地前に古くなる |
| 歯の pin | 実の design-intent を読む歯 65 本・64 桁の要約値を持つ歯 23 か所（fixture には 111 か所）・4 桁以上の数を持つ歯 28 本 | 一括 12 の申し送り「数や字面で pin する歯を性質で当てる形へ」が未起票のまま |

### 1-c. 設計と実装の穴

| 穴 | 実測 |
| --- | --- |
| 要件の未実装 | FR11（導出物の差分 0）・FR13（着地は印だけから読む）は契約 0 本。注は「器の側の口が入るまで便を起こさない」（s2-07l.473 待ち）。NFR1（AI へ常時渡す説明文の量）は測る口が無く「未達」 |
| 要件と契約の線の欠け | FR21（全体像の短い出力）は口が着地済み（便 96・`folio graph --digest`）だが、契約の `req` は FR14 / FR3 のまま。FR21 は便の後に新設された要件で、契約から要件への線が 0 本 |
| ADR-13 決定 (16) の便の列 12 本 | 8 本 着地・**4 本は開いた bead も閉じた bead も無い**（⑤ の 2 本目 = 周の引き金の判定・⑥ 注意の先の一覧・⑨ 実装の状態の導出・⑩ 門の単位の変更）。台帳 143 件を全数検索して 0 件 |
| ADR-11 決定 (4) ⑥⑦⑧ | 台帳 `.74` / `.75` / `.76` が 2026-09-19 の起票のまま更新 0（4 日）。3 本とも今も有効（実装に `const YES`・規則の表に無い閾値・生成区間に無い写し が残る） |
| ADR-2 の帰結 | 条の廃止（status）の機構の時機 = `.100` が開いたまま。憲法 27 条のどれにも status の欄が無い |
| ADR-3 決定 (7) | 器の暫定の散文の門の撤去 = `.93` が `.88` に塞がれたまま |

### 1-d. 台帳の状態

| 何 | 実測 |
| --- | --- |
| 開いた子 bead | 7 本。**7 本とも notes が空・受入欄が空・起票日から更新 0**。その間に便 90〜104 の 15 本が着地 |
| 塞ぎの旗が古い | `.126`（ids anchor の写しを生成区間へ）は `.127`（便 89）に塞がれた表示のままだが、`.127` は 2026-09-22 に着地して閉じている |
| 題が古い | `.88`「FR11・FR13・FR14」のうち FR14 は着地済み。残るのは FR11 と FR13 |
| 親 `f2-648` の notes | 544 行・302 KB。【次の一手と未決】が 46 回散在。見出しは 0。復元の入口は末尾の 1 件 |
| 全 bead の notes | 623 KB（88 / 143 件が notes を持つ） |

### 1-e. 退役の残骸

| 置き場 | 中身 |
| --- | --- |
| `crates/folio/retired/` | `render.rs` 1,380 行 + `render-tests.rs` 348 行（`#[test]` 15）。Cargo.toml からも build.rs からも参照されない |
| `scripts/retired/` | 7 file（Python の床・parity・見本の生成器） |
| `design-intent/preview/retired/` | 4 file（見本 3 面 + readable.html） |
| `readable.html` の字 | design-intent に 10 か所・docs/design に 16 file（退役の記録として残る。src は 0） |

## 2. 持ち主の疑いへの答え

**疑い: 「根本的な設計の部分は古い scribe2 が作っていてそこに大量に問題が隠されている」。**

答えは「半分そのとおり」である。設計文書と契約を書いたのは器ではなく席（AI）だが、**器の制約が席の書く形を決めた**跡が §1-b のとおり残っている。隠れていた問題の実例は、今日の切りの直前に 1 つ出た＝便 98 の契約は「天井の印も門も変わらない」と書いたが、印の側は「写しは全観点で同一」という便 98 以前の前提を持っており、凍結の土台では 4 観点の写しが偶然そろっていたので歯が拾えず、印が書けなくなった（便 104 で是正）。**契約が主張する「当たらない」の範囲を、凍結の土台で確かめる歯が無かった**のが穴で、同じ形の穴は §1-b の pin の歯 65 本の外側にも在りうる。

一方で、設計文書（憲法・規則・要件書・語彙・判断の記録）は 26 周の天井と床で毎回検査され、意味の食い違いはその都度一括で直してきた。**設計文書の中身をリファクタリングする必要は、いま見つかっていない。** 直すのは次の 4 つの層である。

1. **module の境界**（実装）: 上限で割った file を責務で割り直す。
2. **契約と歯の書き方**（開発の作法）: 数値でなく手順・性質で書く。契約の PR の前に受付を撃つ。
3. **台帳**: 開いた bead を今の実態に合わせ、ADR-13 の残り 4 本の行き先を決め、復元の入口を 1 枚にする。
4. **要件の穴**: FR11 / FR13 / NFR1 / FR21 の状態を要件書に明示し、いつ埋めるかを決める。

## 3. 直す順と便の列（案）

順は「戻せる・小さい・他を塞がない」ものから。**§4 の裁定が出るまで 1 本も起こさない。**

### 3-a. 台帳の整理（席だけで済む・便は無い）

| # | 何 | 形 |
| --- | --- | --- |
| L1 | `.126` の塞ぎを外す（`.127` は着地済み） | `bdw dep remove` |
| L2 | `.88` を FR11 / FR13 の 2 本に割り、FR14 の分を close reason に書いて閉じる | `bdw create` × 2・`bdw close .88` |
| L3 | `.74` / `.75` / `.76` に「今も有効」の実測を notes に記帳し、優先度を §3-c の列に合わせる | `bdw update --append-notes` |
| L4 | ADR-13 の残り 4 本（⑤-2・⑥・⑨・⑩）を起票する（🔴 5 の裁定で park なら park と記帳） | `bdw create` × 4 |
| L5 | 復元の入口を 1 枚にする: `docs/design/state.md`（今の状態・次の一手・未決だけ・毎回上書き・散文の規則は置かない）を席が保守し、`f2-648` notes の【次の一手と未決】はそこへの参照 1 行にする | 設計ノート 1 本 |

親の notes 302 KB は消さない（消すのは A-1・記帳は残す）。以後の記帳を短くし、復元は L5 の 1 枚で行う。

### 3-b. 契約と歯の作法（規則の表の行 1 本 + 型 1 本）

| # | 何 | 形 |
| --- | --- | --- |
| P1 | 契約の本文に要約値・byte 数・行数を**写さない**。書くのは「組み直す手順」（独立の script の名と撃ち方）と「変わらないものの理由」。数を書くなら「参考値（base <sha> の実測）」と断る | 開発規律の行 D-13（P-17.1 の裁定 id が要る） |
| P2 | 契約の PR を merge する前に、席が worktree で `pipe preflight --repo <worktree>` を撃って受付の断り（名指し・余地・write-set）を先に潰す | admit.sh に 1 段足す（規則にはしない・手順） |
| P3 | 契約の「当たらないもの」の主張は、凍結の土台で確かめる歯を 1 本持つ（便 98 の穴の型） | 契約の型（docs/design/delivery-template.md・新設）に欄を足す |
| P4 | done の歯の接頭辞 `f<NN>_` を便 0〜73 にも遡って当てない（74 本は数え直せない）。代わりに §3-c の便で歯を触るとき、その便の接頭辞に改める | 便の中で |

### 3-c. 便の列（器で運ぶ・振る舞い不変を先に）

| 順 | 便 | 中身 | 塞ぎ |
| --- | --- | --- | --- |
| 1 | 105 | `tests/face.rs`（−285）と `tests/face_index.rs`（−28）を面ごとに割る（振る舞い不変・歯の本数不変） | 無し。これを先にしないと面の歯を触る便が全部止まる |
| 2 | 106〜107 | src の割り直し 1 本目: `adr.rs`（余地 110）と `note.rs`（138）を「読む・検査する・面に出す」の責務で割る（ADR-15 の module 図に従う） | ADR-15 の発効 |
| 3 | 108〜109 | src の割り直し 2 本目: `face_index.rs`（122）と `face_constitution.rs`（206） | 同上 |
| 4 | 110〜 | pin の歯を性質へ: 実の design-intent の数や字面を固定する歯（4 桁数 28 本・要約値 23 か所）を「凍結の土台に対する厳密な値」と「実の正本に対する不変条件」に分ける（便 43 / 44 と同じ向き・一括 12 の申し送り） | 便 105 |
| 5 | — | ADR-11 決定 (4) ⑥⑦⑧（`.74` / `.75` / `.76`）。⑦ は規則の表の行を足すので裁定 id が要る | §4 🔴 5 |
| 6 | — | ADR-13 の残り 4 本（⑤-2 周の引き金・⑩ 門の単位・⑥ 注意の先・⑨ 実装の状態） | §4 🔴 5 |
| 7 | — | 退役の残骸の撤去（`crates/folio/retired/`・`scripts/retired/`・`preview/retired/`）— git の履歴に残るので戻せるが、A-1 の「消す」に当たる | §4 🔴 4 |

要件の穴（FR11・FR13・NFR1・FR21）は便でなく要件書の版と台帳で扱う（§4 🔴 2）。

### 3-d. 判断の記録（ADR-15・案）

「module の境界は責務で決め、器の行数の上限は境界の理由にしない」を判断として書き、module 図（読む → 検査する → 導出する → 面に出す → 配る の 5 層と、各 file の責務 1 行）を付ける。撤退条件 = 器の上限が外れても境界を戻さない（境界は責務で決めたので）。grill を先に行う（A-2.3 の趣旨・憲法の改訂ではないので A-2.1 は要らない）。

## 4. 持ち主への問い（🔴 6 つ）

### 🔴 1. 総点検の範囲

〔やりたいこと〕§2 の 4 層（module の境界・契約と歯の作法・台帳・要件の穴）を範囲にする。設計文書の中身（憲法・要件書・語彙・判断の記録の本文）は範囲に入れない。
〔理由〕設計文書は 26 周の天井で毎回検査されており、今日の棚卸しでも意味の食い違いは出ていない。問題は外側の 4 層に集中している。
〔代替案〕(A) 設計文書の本文も範囲に入れ、条・要件を 1 本ずつ実装と突き合わせる総点検を別に行う（周 1 回分の時間・所見は天井の周と重なる）。(B) 4 層のうち module の境界だけを先にやり、残りは M3 へ。
〔コスト〕4 層で便 6〜8 本 + 席の作業 + 判断の記録 1 本。周は増えない（便が着地するたびに門は印で通す）。
〔推奨〕4 層を範囲にする。

### 🔴 2. 未実装の要件（FR11・FR13・NFR1）と線の欠け（FR21）

〔やりたいこと〕要件書の次の版で、FR11・FR13・NFR1 の状態欄を「保留（器の口 s2-07l.473 の着地後）」と明示し、M3 の入口で再判定する。FR21 には注で「口は便 96 で着地」と線を引く。
〔理由〕3 本とも「未実装」の断りが注と台帳と判断の記録の 3 か所に散っていて、読む人によって「未着手」「park」「捨てた」に読み分かれる。要件の状態は要件書の欄で 1 つに定めるのが P-6.3。
〔代替案〕(A) 今すぐ実装する（FR11 は器の口が無いので実装できず、NFR1 は測る対象の定義が要る）。(B) 廃止（status）にする（P-7.2 の形。ただし FR13 は ADR-3 の決定 (5)(6) の実体で、捨てると判断の記録の改訂が要る）。
〔コスト〕要件書の版 1 つ（承認 1 回）。便は無い。
〔推奨〕保留と明示し、M3 の入口で再判定する。

### 🔴 3. module の境界を責務で割り直す（ADR-15 + 便 106〜109）

〔やりたいこと〕上限で割られた面の生成器と、余地が 300 未満の src 4 本を、責務（読む・検査する・導出する・面に出す・配る）で割り直す。振る舞いは変えない。先に判断の記録 ADR-15 で module 図を発効する。
〔理由〕今の境界は「上限に当たったところ」で決まっており、次に触る便が size M を運べない file が 4 本ある。責務で割れば、便の write-set が責務の単位で閉じ、重なりと anchor の巻き添えも減る。
〔代替案〕(A) 器の上限 1,500 を緩めてもらう（器の規則の改訂・folio2 の都合で器を変える形になる）。(B) 触る便が来るたびにその場で割る（今までどおり・境界が上限で決まり続ける）。
〔コスト〕判断の記録 1 本（grill 込み）+ 便 4 本（振る舞い不変・歯の本数不変）。着地のたびに門は通す（設計文書を触らない）。
〔推奨〕ADR-15 を発効して 4 本運ぶ。

### 🔴 4. 退役の残骸の撤去（A-1「消す」）

〔やりたいこと〕`crates/folio/retired/`（1,728 行）・`scripts/retired/`（7 file）・`design-intent/preview/retired/`（4 file）を repo から消す。git の履歴には残る（回復可能・N-1.1 の「回復不能」には当たらない）。
〔理由〕参照が無い code 1,728 行が crate の下に在り、材料集め役や天井の審査役が読む母集団を広げている。退役は可逆な移動で済ませたが、移動先が今も repo の中で、検査の母集団から外れていない。
〔代替案〕(A) そのまま置く。(B) repo の外（`~/.local/share/folio2/retired/`）へ移す（git の履歴からは辿れなくなる＝(A) より戻しにくい）。
〔コスト〕席の PR 1 本。便は無い。承認 1 回（A-1）。
〔推奨〕消す（履歴に残る形で）。

### 🔴 5. 判断の記録の未消化の決定（ADR-11 ⑥⑦⑧・ADR-13 の 4 本）

〔やりたいこと〕ADR-11 決定 (4) ⑥⑦⑧ は §3-c の列の 5 番目に入れて運ぶ。ADR-13 の残り 4 本は起票し、⑤-2（周の引き金の判定）と ⑩（門の単位）を先、⑥（注意の先）と ⑨（実装の状態）を後の順で列に入れる。
〔理由〕発効した判断の決定に bead が無いのは、台帳が判断の記録を追えていないことの実例（P-5.4 の台帳の役割）。⑤-2 と ⑩ は周の回数と門の止め方を変えるので、持ち主の目的関数（精度・実時間）に直接効く。
〔代替案〕(A) ADR-13 の 4 本を「M3 へ」と注で印し、起票しない。(B) ADR-11 の 3 本を閉じて判断の記録の注で「取り下げ」と記帳する。
〔コスト〕起票 7 本。便は §3-c の 5・6 番目（時期は §3-c の 1〜4 の後）。
〔推奨〕全部起票し、順を §3-c のとおりにする。

### 🔴 6. 契約と歯の作法を規則にする（D-13）

〔やりたいこと〕開発規律の行 D-13「契約の本文に要約値・byte 数・行数の実測値を写さない。組み直す手順と、変わらないものの理由を書く。数を書くなら参考値と断る」を規則の表に足す（裁定 id 付き・P-17.1）。あわせて手順として、契約の PR の前に席が worktree で受付を撃ることを admit.sh に足す。
〔理由〕改訂 41 回のうち内容の誤りは 0 で、器が契約を読む形に起因する改訂が大半。数値を写す形は先行の便の着地で必ず古くなる。
〔代替案〕(A) 規則にせず教訓（記憶）に留める（席が替わると失われる・N-2.1 の「散文にしか無い規則」）。(B) 器の側に「参考値の欄」を要望する（s2 の手番・時期が読めない）。
〔コスト〕規則の行 1 本（裁定 id = この承認の逐語と時刻）。便は無い。
〔推奨〕D-13 を足す。

## 5. 本ノートの後の段取り

1. 持ち主の裁定（🔴 1〜6）→ 逐語と時刻を台帳と本ノートに記帳。
2. 台帳の整理（§3-a・席だけ）→ `state.md` の新設。
3. ADR-15 の起草 → grill → 発効（承認 1 回・要件書の版と D-13 と同じ承認にまとめる）。
4. 便 105 → 106〜109 → 110〜（各便は起草役 + 検証役の対・P2 の受付の先撃ち）。
5. その後に一括 14（26 周目の直す 14 件 + 便 102 の 🔴 1）→ 27 周目。

## 6. 数の出所

棚卸し `~/.local/share/folio2/handoff-2026-09-23/audit-inventory.md` の §1〜§8。本ノートの数はそこからの写しで、独立の再現は §4 の裁定の後に検証役が行う（数が変わっても本ノートの結論は変わらない見込みだが、変われば改訂 b で直す）。
