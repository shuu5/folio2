# 一括 13 の仕分け — 天井の 25 周目の所見 20 件

**状態: 承認待ち（2026-09-22 起草）。** 下の 問 1 と 問 2 が持ち主への承認要求である。承認の記帳（要件書の承認欄・判断の記録の注の断り）は、持ち主が答えた後に席が同じ枝の別の commit で正本の側へ書く。**この文書にも正本にも、持ち主の逐語はまだ 1 字も入っていない。**

起草 2026-09-22・orchestrator 席の起草役（AI・fable 5.1）・作業場所 `.worktrees/planner-batch13`（branch `docs/batch13`・`origin/main` 278ba59 から切った）。

材料は 1 つだけである。**天井の 25 周目の所見 20 件**（周 = `~/.local/share/folio2/ceiling/2026-09-22-round25/`・base main 278ba59・観点 4・審査役 opus）。一括 12 と違って便の自己申告（🔴）は無い＝便 102 以降の契約表は 🔴 を 1 つも上げていない。

内訳は **忠実さ 合格**（直す 3・参考 1）・**読みやすさ 合格**（直す 4・参考 2）・**実態との整合 合格**（直す 4）・**文書どうしの整合 不合格**（止める 1・直す 5）。合計 20 件（止める 1・直す 16・参考 3）。

**周の結論はまだ出ていない。** 整合の「止める」1 件の反証（refute）は本書の起草の時点で実施中で、`~/.local/share/folio2/ceiling/2026-09-22-round25/refute-notes/` はまだ空である。**印は打たない**ので、この枝の PR は天井の門が「まだ分からない」の側で返す（規則の表の開発規律行 D-12 のとおり席が受付の手順で運ぶ）。

## この一括に入れなかった 3 件（便 103 が実装側で直す）

3 件はどれも**生成区間**（実装の型付きの定数から `folio schema --write` が導出する範囲）に在る。生成区間は手で直せず、直すには実装の定数を書き換える便が要る（条 P-6.2・判断の記録 ADR-9）。**この一括は設計文書の側だけを動かすので、3 件は便 103 の write-set に置く。**

| 観点 | id | 重さ | place | 直す先（実装の定数） |
|---|---|---|---|---|
| 整合 | F-1 | **止める** | `design-note` の `schema.index.index_note` | `crates/folio/src/note.rs` の `index_note`。索引の中身を「文書・要件・契約の id と 1 行の題」と書き、口を未実装と書く。要件 FR14 の第 1.28 版と判断の記録 ADR-14 決定 (1) は同じ索引を節点と辺で定め、口は 2026-09-22 に着地したと書く |
| 実態 | F-1 | 直す | 同上 | 同上（同じ 1 か所。整合と実態が別の理由で同じ字を指している） |
| 実態 | F-3 | 直す | `design-note` の `schema.guards.post` | `crates/folio/src/note.rs` の事後の guard の一覧。便 93 で着地した規則の表の行 R-17 の床の歯（`crates/folio/src/mentions.rs`）が一覧に無く、事後の側の本数が実態より少なく出る |

**整合 F-3 の 2 か所目も同じ形である**（判断の記録の欄の決まり `design-intent/adr/schema.yaml` の `limits_note`・正本は `crates/folio/src/adr.rs`）。下の表では「便」と書き、便候補として `docs/design/face-deliveries-plan.md` §11 に置いた。**便 103 に同梱するのが安い。**

## 仕分けの表（20 件）

仕分けの記号は一括 10〜12 と同じ向き。**B** = 本一括で正本を直した（持ち主の承認が要る）／**承認不要** = 本一括で直したが承認欄を持たない／**C** = 便候補（直す先が実装）／**便 103** = 上の節／**参考** = 直さない。

| 観点 | id | 重さ | place | 是正の内容 | 仕分け | 裁定の種類 | 問 |
|---|---|---|---|---|---|---|---|
| 整合 | F-1 | **止める** | `design-note` `schema.index.index_note` | 索引の中身と状態を着地した実装に合わせる | 便 103 | 実装の定数の便 | — |
| 整合 | F-2 | 直す | `vocabulary` `terms.design-intent.def` | 設計文書の中身の列挙に**索引の欄の決まりの正本**（`design-intent/graph.yaml`）を足す。便 95 で着地し、要件 FR19 と受入基準 AC17 の対象に入っているのに、列挙にも数えない例外 2 つにも無かった。門が数える正本の範囲もこの項が定めるので、足すと「門はこの file を数える」がこの項から引ける（実装 `crates/folio/src/gate.rs` は、門に掛けるかどうかの判定では `<dir>` の下で `preview/` と retired の外を全部見るので、足した字が実態と合う。印の鮮度の要約値は観点が読む文書だけを連結するので、そちらにはまだ入らない＝後続の便 102 が運ぶ） | B | 要件書の版上げ（語彙を同梱） | 1 |
| 整合 | F-3 | 直す | `adr` `ADR-2.options.a.reason` | **注で断る。案の理由の本文は触らない。** 括弧「P-10.1（機構の欄（live）が M0・未発効）」は 2026-09-12 時点の写しで、いまの憲法の条 P-10 の機構の欄は `live: now` である。括弧の外の主張（この判断は P-10.1 を満たすことを射程に入れていない・凍結と検査が同じ関数を通るので検査側からの独立は満たさない）はいまも成り立つので、**発効した判断の案の理由は当時の記録として残す**。同じ字が判断の記録の欄の決まりの `limits_note` にも在るが、そちらは生成区間 | B（注）＋ **C** | 判断の記録の注（承認欄と決定と帰結は触らない）／2 か所目は便 | 2 |
| 整合 | F-4 | 直す | `srs` `constraints.CON9.text` | 末尾「folio が出すのは機械が読む id の索引（FR14）まで」に、第 1.28 版で足した要件 FR21（全体像の短い出力と席が始まるときの 1 行）を追随させる。「文書の中身そのものは folio からは出さない」という制約の芯は動かない | B | 要件書の版上げ | 1 |
| 整合 | F-5 | 直す | `vocabulary` `terms.attention-list.def` | 「束そのものは絞らない」を「束そのものは**影響集合で**絞らない」に直し、「束は観点が読むと宣言した節まで絞ったうえで渡す（要件 FR17）」の 1 文を足す。判断の記録 ADR-13 決定 (5) の「切らない」は影響集合で切らないという意味で、要件 FR17 の第 1.28 版が定める節までの絞りとは別の話である | B | 要件書の版上げ（語彙を同梱） | 1 |
| 整合 | F-6 | 直す | `srs` `requirements.FR20.shall` | **規範文は触らず、注で断る。** 判断の記録 ADR-13 決定 (9)（門の「まだ分からない」の側だけを便の書き換える file の単位にする）は**まだ実装されていない**（実装 `crates/folio/src/gate.rs` は置き場の全体で要約値を突き合わせる）。決定 (9) は判断の記録 ADR-13 決定 (16) の便の列の ⑩ が運ぶ後続で、いまの規範文は狭める前の形として正しい。注にその 1 文を足すと、要件と判断の記録が同じ入力に別の値を返すように読める箇所が消える | B | 要件書の版上げ | 1 |
| 忠実さ | F-1 | 直す | `srs` `requirements.FR14.plain` | 平易文が規範文の第 2 文（索引は節点と辺を持ち、欄はこれこれで、種類と型は閉じた一覧とする）を丸ごと落としていた。同じ file の要件 FR9 の平易文は同種の縛りを「決まった種類から選び」と写しているので、落としは様式ではなく漏れである。**義務は増えない**（規範文に在る義務を写すだけ） | B | 要件書の版上げ | 1 |
| 忠実さ | F-2 | 直す | `srs` `requirements.FR21.plain` | 平易文が規範文の条件「設計文書が在るときは」を落としていた。この条件は要件 FR3（文書がまだ無いあいだの 1 行）との区別そのもので、FR21 の注も書き分けている。平易文に条件を戻す | B | 要件書の版上げ | 1 |
| 忠実さ | F-3 | 直す | `adr` `ADR-13.plain` | 平易文が決定 (7)（前の周の判定を持ち越さない）・(9) と (14)（門の「まだ分からない」の側を便の file の単位へ狭め、判断の記録 ADR-8 の決定 (4) を狭める）・(13)（実装の状態を要件書の注から導出物へ移す）を落としていた。ほかの判断の記録（ADR-4・ADR-8・ADR-11）の平易文は決定を全部たどっているので、書き方の差ではない。**決定と帰結の本文と承認欄は 1 字も触らない** | B | 判断の記録の平易文 | 2 |
| 忠実さ | F-4 | 参考 | `design-note` `example.sections.3.rows.index.output` | **実態 F-2 と同じ 1 行なので同時に直した。** 参考の重さだが、同じ行の `note` を直して `output` を古いまま残すと行として食い違うので分けなかった。「直すかどうかは凍結 anchor の扱いと合わせて決める」の断りには、**anchor は `tests/fixtures/floor_base/design-intent/design-note/example.yaml` の別の凍結の写しで、生きた見本を直しても anchor は動かない**と実測で答えた（歯 760 本が全部緑） | 承認不要 | 見本（拘束力なし・承認欄を持たない） | — |
| 読みやすさ | F-1 | 直す | `index` `shelf.documents.design-note` | **便候補。** 棚の設計ノートの card の名札が英字の id（`example`・`figures`）になるのは、面の生成器 `crates/folio/src/face_index.rs` の `note_rows` がリンクの字に `q.id` を使うためで、正本は `meta.title` に日本語の題を持っている。**正本は 1 字も動かない**ので、直す先は実装。`docs/design/face-deliveries-plan.md` §11 に置いた（同じ card の判断の記録 14 本をどう扱うかも便の設計で決める） | **C** | 面の生成器の便 | — |
| 読みやすさ | F-2 | 直す | `index` `lanes.first.stops` | **語彙の側で直す。** 名札「道具が 1 回で通す段の図」の「段」は要件書の図 2 の 7 段（手順の工程）の意味だが、語彙の「段」は条の段（いつも守る／確認してから変える／絶対にやらない）で閉じている。語彙の「段」の項に、図 2 の 7 段と判断の記録の「反証の 2 段目」は別の語だと断る 1 文を足すと引ける。**入口の正本は触らない**＝名札を替えても判断の記録の「反証の 2 段目」という第 3 の意味は残るので、語彙の側の断りは結局要る（浮くのは入口の面の版上げ v0.4 だけである） | B | 要件書の版上げ（語彙を同梱） | 1 |
| 読みやすさ | F-3 | 直す | `srs` `scope.not_build` | 「M0 では作らない」の一覧の 2 行が末尾に「着地済み」と書き、面でも 対象外 の札の下に 着地済み が並ぶ。**いつの版の話かを行の頭に置く**（所見の 2 案のうち 1 つ目）＝「M0 では作らない（その後 M1 で着地済み）— 設計ノートの生成。…」の形にする。範囲そのものは 1 つも動かない | B | 要件書の版上げ | 1 |
| 読みやすさ | F-4 | 直す | `srs` `requirements.FR4.title` | 見出し「3 枚を 1 つの生成器で出す」と平易文の「3 枚」が、入口の棚の「19 面」と受入基準 AC2 の平易文の「5 つ」と並んで読める。**見出しと平易文から枚数を外し、注で 3 と 5 と 19 の関係を書く**（この要件が名指すのは M0 の 3 面／設計ノートの面は FR9・判断の記録の面は FR16／いま読める面の数は入口の棚が正本から数える・条 P-6.3）。**規範文は触らない**（M0 の義務は 3 面のままで正しい） | B | 要件書の版上げ | 1 |
| 読みやすさ | F-5 | 参考 | `rules` `discipline.D-9.what` | **直さない。** 「PreToolUse」に日本語の言い換えも語彙の項も無い。直すには規則の表の行 D-9 の `what` を書き換えることになり、**行の変更は裁定 id と時刻が要る**（条 P-17.1）。参考 1 件のために持ち主の裁定を 1 つ起こすのは割に合わない。次に行 D-9 を触る便か一括に同梱する | 参考 | — | — |
| 読みやすさ | F-6 | 参考 | `constitution` `sources` | **直さない。** 出所 2 と 出所 3 が同じ「記号の読み方」の一文を 2 度持つ。憲法の本文の変更であり、条 A-2 の改訂の手順（判断の記録・撤退条件・持ち主の承認・anchor の凍結し直し）が要る。一括 11 の 21 周目 読みやすさ F-1 と一括 12 の 24 周目 読みやすさ F-1 と同じ扱いで、**憲法の字の直しをためて 1 本の判断の記録で運ぶ**のが安い | 参考 | — | — |
| 実態 | F-1 | 直す | `design-note` `schema.index.index_note` | 便 103（上の節） | 便 103 | 実装の定数の便 | — |
| 実態 | F-2 | 直す | `design-note` `example.sections.3.rows.index.note` | 見本の口の表の索引の行。着地した口（`folio graph --print`・2026-09-22）に合わせて、状態を「着地済み（2026-09-22・要件書 FR14 の口）」に、出力の欄を「索引の節点（id・種類・所属 file・欄の要約値・1 行の題）と辺（両端の節点の id と型）」に直した（忠実さ F-4 と同じ行） | 承認不要 | 見本（拘束力なし・承認欄を持たない） | — |
| 実態 | F-3 | 直す | `design-note` `schema.guards.post` | 便 103（上の節） | 便 103 | 実装の定数の便 | — |
| 実態 | F-4 | 直す | `adr` `ADR-13.consequences` | **帰結の本文は触らず、注で断る。** 「節点ごとの要約値の表を持つ印はおよそ 16 キロバイトになる（今は 976 byte）」は決定の時点の見積で、便 99 の着地後の実測は表 6,635 byte・印の全体 7,689 byte（契約 `docs/design/delivery-99.md`）。差の理由は注の (i) と同じ＝節点を id を持つ行に閉じたこと。同じ見積を持っていた下調べの設計ノートの側は一括 12 が実測に直してあり、判断の記録の側だけが残っていた | B | 判断の記録の注（承認欄と決定と帰結は触らない） | 2 |

**まとめた後の問いの数 = 2。**（止める 1 件と 直す 2 件は便 103・直す 1 件と参考 2 件は便候補か据え置き・直す 1 件と参考 1 件は承認欄を持たない見本。残る 12 件を 2 つの承認にまとめた。）

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.29 版の起草。** 要件 FR4 の見出しと平易文から枚数を外し注を新設（読 F-4）／FR14 の平易文に規範文の第 2 文（忠実さ F-1）／FR20 の注に判断の記録 ADR-13 決定 (9) が後続であること（整合 F-6）／FR21 の平易文に条件「設計文書が在るときは」（忠実さ F-2）／制約 CON9 に要件 FR21 の追随（整合 F-4）／範囲の章の「M0 では作らない」の 2 行の書き出し（読 F-3）／`meta.version` を v1.29 に・`status_note` と `approval` に起草の行。**`effective_version` は v1.28 のまま**（承認の行は席が承認後に書く） |
| `design-intent/vocabulary.yaml` | 3 項。設計文書の項の中身の列挙に索引の欄の決まりの正本と、その正本が天井の読む文書の一覧にまだ載っていない（足すのは便 102）・入口の棚には載せない（判断の記録 ADR-14 決定 (2)）という状態の断り（整合 F-2）／注意の先の項に「影響集合で」と、節までの絞りの 1 文（整合 F-5）／段の項に、図 2 の 7 段と反証の 2 段目は別の語だという断り（読 F-2）。**要件書と同じ承認で運ぶ**（判断の記録 ADR-13 §8.1 と一括 11・12 と同じ形） |
| `design-intent/adr/ADR-13.yaml` | `plain` に決定 (7)・(9) と (14)・(13) の 3 つ（忠実さ F-3）と、`note` に【天井の 25 周目の所見への応答】(j)（印の見積と実測・実態 F-4）と (k)（平易文に足した 3 つの記録）。**決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-2.yaml` | `note` に条 P-10.1 の機構の欄の訂正 1 文（整合 F-3）。**案の理由・決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/design-note/example.yaml` | §3 の口の表の索引の行の `output` と `note`（実態 F-2・忠実さ F-4）。見本は拘束力なし（`status: example`）で承認欄を持たない |
| `docs/design/face-deliveries-plan.md` | §11 に一括 13 からの便候補 2 本（入口の棚の設計ノートの名札＝読 F-1／判断の記録の欄の決まりの `limits_note`＝整合 F-3 の 2 か所目） |
| `docs/design/batch13-triage.md` | **新設**（この file） |

**実装（`crates/folio/src`）・凍結 anchor（`tests/fixtures/`）・歯（`crates/folio/tests`）・生成区間は 1 byte も触っていない。**

## 持ち主へ問う項

### 問 1. 要件書 第 1.29 版の発効（語彙 3 項を同梱）

**やりたいこと。** 要件書を第 1.29 版に上げ、語彙の 3 項を同じ承認で発効する。中身は天井の 25 周目が出した 9 件の「直す」で、**要件の規範文と受入基準の判定の式は 1 つも動かない**。動くのは見出し 1 本・平易文 3 本・注 2 本（うち 1 本は新設）・制約の文 1 本・範囲の章の 2 行・語彙の定義 3 本である。

**理由。** 9 件はどれも「文書どうし、または文書と実装が、同じことについて別のことを言っている」形である。

1. 索引の欄の決まりの正本が、設計文書の中身の列挙にも数えない例外にも無い（門がこの file を数えるかが語彙から引けない）。
2. 制約 CON9 が folio の出力の上限を索引までと書いたまま、第 1.28 版で足した要件 FR21 に追随していない。
3. 語彙の「注意の先」が「束は絞らない」と読め、要件 FR17 の「観点が読むと宣言した節まで絞る」と逆に読める。
4. 要件 FR20 の規範文と判断の記録 ADR-13 決定 (9) が、同じ門について別の単位を書いている（決定 (9) が後続であることを注が言っていない）。
5. 要件 FR14 の平易文が規範文の 4 文のうち 1 文を丸ごと落としている。
6. 要件 FR21 の平易文が規範文の条件を落とし、要件 FR3 の 1 行と区別できない。
7. 入口の読む順番の名札の「段」を語彙で引くと、条の段という別の意味に当たる。
8. 範囲の章で「作らない」の下に「着地済み」が並び、非エンジニアがそこで止まる。
9. 要件 FR4 の見出しと平易文が「3 枚」と数え、入口の棚の「19 面」と受入基準 AC2 の「5 つ」のどれが今の数か決められない。

放っておくと次の周の 整合 と 忠実さ と 読みやすさ が同じ所で所見を立て直す。5・6・9 は非エンジニアが読む面に出る字なので、**持ち主自身が読む面の正しさの問題**でもある。

**代替案。**
- **(A) 版を分ける。** 忠実さの 2 件（平易文の落ち）だけ先に発効し、残りを次の一括へ回す。承認が 2 回になり、周も 2 回になる。
- **(B) 9 件のうち要件 FR4（読 F-4）だけ落とす。** 見出しを変えるのは「面が増えたら見た目の案を問う」という判断の記録 ADR-5 の撤退条件 ② とは別の話（見た目でなく数え方）だが、見出しは持ち主が walk で見た字なので分けて問うこともできる。分けると承認が 2 回になる。
- **(C) 読 F-2 を入口の正本の側で直す。** 名札を「folio が 1 回で通す 7 段の図」に替える。入口の面の版上げ（v0.4）が要る（**面の walk は要らない**＝入口の正本の第 0.3 版は一括 11 の直しをふつうの一括の承認で発効しており、受入基準 AC5 の walk は 1 回きりの記録の受入で、入口の字を替えるたびに課すものではない）。ただし名札の替えでは、判断の記録の「反証の 2 段目」という第 3 の意味が残るので、語彙の側の断りは結局要る。

**コスト。** 承認 1 回。動く欄は 見出し 1・平易文 3・注 2・制約 1・範囲の 2 行・語彙 3 で、**要件の番号は 1 つも動かず（条 P-7.1）、受入基準の判定の式も床の歯も 1 つも動かない**（歯 760 本は枝の HEAD で全部緑）。周は 1 回増える（この枝が着地した後の 26 周目）。

**推奨 = 9 件をまとめて第 1.29 版として発効する（代替案は採らない）。** 9 件とも義務を増やさず減らさず、字を実態に合わせるだけなので、分けて問う利得が無い。読 F-2 は語彙の側で直す案を採る（名札の替えでは判断の記録の「反証の 2 段目」という第 3 の意味を覆えないため。代替案 (C) との差は入口の面の版上げ 1 つで、手番の重さで選んだのではない）。

### 問 2. 判断の記録 ADR-13 の平易文と、ADR-13 / ADR-2 の注の訂正

**やりたいこと。** 判断の記録 ADR-13 の平易文に、落ちていた決定 3 つ（(7) 前の周の判定を持ち越さない／(9) と (14) 門の「まだ分からない」の側を便の file の単位へ狭め、判断の記録 ADR-8 の決定 (4) を狭める／(13) 実装の状態を要件書の注から導出物へ移す）を足す。あわせて ADR-13 の注に 2 点（印の大きさの見積と実測の差・平易文に足した記録）、ADR-2 の注に 1 点（条 P-10.1 の機構の欄はいま `live: now` である）を書く。

**決定・帰結・案の理由・承認欄は 1 字も触らない。**

**理由。** 3 つとも「発効した判断の記録が、非エンジニアの読む面で決定を落としている」形である。ADR-13 の平易文は 16 の決定のうち 3 つを写しておらず、落ちた 3 つは**門の止め方が変わること**という、持ち主が最も知りたい類の決定を含む。ほかの判断の記録（ADR-4・ADR-8・ADR-11）の平易文は決定を全部たどっているので、書き方の差ではなく漏れである。

注の 2 点は「決定の時点の見積が、着地後の実測と違う」形で、一括 12 が下調べの設計ノートの側だけを実測に直し、判断の記録の側を取り残していた。ADR-2 の 1 点は憲法の条 P-10 の機構の欄が `live: now` になった後も、案の理由の括弧が「M0・未発効」のまま残っていたもの。

**代替案。**
- **(A) 平易文でなく注に書く。** 決定 3 つを注の訂正として書き、平易文は触らない。平易文は非エンジニアが読む面の本体なので、注へ逃がすと読み手には届かない。
- **(B) 決定と帰結の本文を直す。** 実態 F-4 の「およそ 16 キロバイト」を実測へ書き換える。発効した判断の帰結の本文を後から書き換えることになり、同じ数が判断の記録と生成物の 2 か所に在る形になる（条 P-6.3）。
- **(C) ADR-2 の案の理由の括弧を直す。** 「M0・未発効」を今の状態へ書き換える。案の理由は**決定の時点で何が真だったか**の記録なので、書き換えると当時の判断の前提が読めなくなる。

**コスト。** 承認 1 回。動くのは平易文 1 本（段落 1 つ追加）と注 3 か所。**義務は 1 つも増えない**（規範文と決定に在るものを写すだけ）。床の歯も面の生成器も動かない。

**推奨 = 平易文に決定 3 つを足し、残りは注で断る（代替案は採らない）。** 発効した判断の決定・帰結・案の理由・承認欄は触らないという一括 11・12 と同じ向きで、平易文だけは「決定を全部たどる」という ADR-13 以外の判断の記録の形に揃える。

## 本一括で動かさなかったもの

| 何 | なぜ |
|---|---|
| 整合 F-1（止める）・実態 F-1・実態 F-3 | 直す先が生成区間で、正本は実装の型付きの定数。**便 103 が運ぶ**（上の節） |
| 整合 F-3 の 2 か所目（判断の記録の欄の決まりの `limits_note`） | 同上。便候補として `docs/design/face-deliveries-plan.md` §11 に置いた |
| 読みやすさ F-1（入口の棚の設計ノートの名札） | 直す先は面の生成器 `crates/folio/src/face_index.rs`。正本は 1 字も動かない。便候補として §11 に置いた |
| 読みやすさ F-5（参考・行 D-9 の「PreToolUse」） | 規則の表の行の変更には裁定 id と時刻が要る（条 P-17.1）。参考 1 件のために裁定を 1 つ起こさない |
| 読みやすさ F-6（参考・憲法の出所の重複） | 憲法の本文の変更で、条 A-2 の改訂の手順（判断の記録・撤退条件・承認・anchor の凍結し直し）が要る。一括 11 と一括 12 が同じ理由で残した憲法の字の直しと束ねて 1 本の判断の記録で運ぶ |
| 要件書の `effective_version` と承認欄の承認の行 | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 天井の 25 周目の結果と、印

- 周 = `~/.local/share/folio2/ceiling/2026-09-22-round25/`（base main 278ba59・観点 4・審査役 opus・各観点の `findings.yaml` に 20 件）。
- 観点の判定 = 忠実さ **合格**・読みやすさ **合格**・実態との整合 **合格**・文書どうしの整合 **不合格**（止める 1）。
- **止める 1 件（整合 F-1）の反証は本書の起草の時点で実施中**で、`refute-notes/` は空である。周全体の結論はまだ出ていない。
- **印は打たない。** この枝の PR は門が「まだ分からない」の側で返すので、席が受付の手順で運ぶ（規則の表の開発規律行 D-12）。

## 床の結果（この枝の HEAD で実測・2026-09-22）

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | rc 0（9 file 一致） |
| `folio inject --check --dir design-intent` | 68 行 / 7,912 byte 一致 |
| `cargo nextest run -p folio` | **760 / 760 PASS** |

**歯は 1 本も直していない。** 一括 12 が 10 か所直したのと違い、本一括が動かした欄（平易文・注・制約の文・範囲の行・語彙の定義・見本の 1 行）はどれも歯が数や字面で pin していない。一括 12 の申し送り（実の `design-intent` の実測を数や字面で pin する歯を、数でなく性質で当てる形へ移す便）は**まだ起票されていない**ので、次の一括の起草役も同じ申し送りを引き継ぐ。
