# 一括 14 の仕分け — 天井の 26 周目の所見 14 件と便 102 の 🔴 1・2

**状態: 起草・承認待ち（2026-09-23 起草・改訂 b 2026-09-23 23:3x JST〔独立の検証 ~/.local/share/folio2/handoff-2026-09-23/batch14-verify.md の直す 8 点・問う 3 点・参考 4 点を反映〕・改訂 c 2026-09-23 23:5x JST〔再検証の直す 3 点・参考 3 点を席が反映〕）。** 下の 問 1〜6 が持ち主への承認要求である。承認の記帳（要件書・天井の正本・入口の正本・索引の欄の決まりの正本の承認欄・規則の表の行 R-17 の裁定と ruled_at・判断の記録 ADR-13 の注の断り）は、持ち主が答えた後に席が同じ枝の別の commit で正本の側へ書く。**この文書にも正本にも、持ち主の逐語はまだ 1 字も入っていない。**

起草 2026-09-23・orchestrator 席（起草は opus 5.5 の起草役に委ねた。正本の作成の行は席を責任者として「orchestrator 席（AI・fable 5.1）」と書く）・作業場所 `.worktrees/planner-batch14`（branch `docs/batch14`・`origin/main` b81ae12 から切った）。

材料は 2 つである。

1. **天井の 26 周目の所見 14 件**（周 = `~/.local/share/folio2/ceiling/2026-09-22-round26/`・観点 4・審査役 opus）。判定は 4 観点とも **合格**（止める 0）で、印 `design-intent/preview/ceiling-stamp.yaml` は この周のもの（round 2026-09-22-round26・判定 合格）。内訳は 整合 直す 7・読みやすさ 直す 4・実態 直す 3・忠実さ 0。**参考の所見は 0 件**（findings-all.txt の 14 件はすべて「直す」）。
2. **便 102 の自己申告の 🔴 1**（`docs/design/delivery-102.md` §1 (i)・台帳 f2-648 notes 2026-09-22 21:4x JST と 22:37 JST）。門と印の要約値が集めるのは「観点の reads が指す文書」だけで、読む文書の一覧（documents）ではない。便 102 は `graph.yaml` を読む文書の一覧にだけ足したので、`graph.yaml` を書き換えても印は古くならない。**26 周目の 整合 F-2 は同じ穴を周の側から拾っている**（読む文書の一覧に在って、どの観点も読まない文書が 1 つ在る）。
   改訂 b で **便 102 の 🔴 2**（`graph.yaml` は `status: draft`・第 0.1 版のまま）も足した。問 3 で整合の観点が読むので、発効を同じ一括で問う（独立の検証の問う 1・問 6）。

**生成区間に在る所見は 0 件である。** 14 件の place はどれも人が書く欄にある。整合 F-2 の根拠の逐語は天井の正本の生成区間（`schema.documents_note`）から引いているが、直す先は人が書く `viewpoints` の節なので、この一括で直せる。

**印はもう古い。** main（b81ae12）の時点で `folio ceiling --gate --write-set design-intent/srs.yaml` は「まだ分からない（印が古い）」を返す（26 周目の後に便 105〜115 と ADR-15・ADR-16 が設計文書を動かしたため）。この枝の PR も門は「まだ分からない」の側で返すので、席が受付の手順で運ぶ（規則の表の開発規律行 D-12）。

## 仕分けの表（14 件 + 🔴 1・2）

仕分けの記号は一括 13 と同じ向き。**B** = 本一括で正本を直した（持ち主の承認が要る）／**承認不要** = 本一括で直したが承認欄を持たない／**C** = 便候補（直す先が実装）／**参考** = 直さない。本一括に **承認不要** と **参考** は 0 件。

| 観点 | id | 重さ | place（実在を確認） | 是正の内容 | 仕分け | 裁定の種類 | 問 |
|---|---|---|---|---|---|---|---|
| 整合 | F-1 | 直す | `vocabulary` `terms.design-intent.def` | 「索引の欄の決まりの正本は、天井の読む文書の一覧にはまだ載っていない＝足すのは便 102」の 1 文を**落とした**（一括 13 が足した断り・`docs/design/face-deliveries-plan.md` §11 の 3 本目の予告どおり）。版の番号やどの観点が読むかは語彙に写さない（正本は天井の正本の `viewpoints`）。「入口の棚には載せない」の 1 文は残した | B | 要件書の版上げ（語彙を同梱） | 1 |
| 整合 | F-2 | 直す | `ceiling` `viewpoints`（coherence の reads） | 整合の観点の読む文書に `{doc: graph, fields: [schema.node_kinds, schema.edge_types, schema.edge_fields]}` を足し、読み手の想定と問いの文の文書の列を 10 種にした。**便 102 の 🔴 1 と同じ 1 行** | B | 天井の正本の版上げ（第 0.13 版） | 3 |
| 整合 | F-3 | 直す | `index` `shelf.explain` | 「道具自身の正本 = 相談窓口の正本と天井の正本は載せない」に**索引の欄の決まりの正本**を足した（判断の記録 ADR-14 決定 (2)）。棚の中身は 1 つも動かない | B | 入口の正本の版上げ（第 0.4 版） | 1 |
| 整合 | F-4 | 直す | `rules` `thresholds.R-17.population` | 母集団に**数えない正本 2 つとその理由**の 1 文を足した（索引の欄の決まりの正本は meta と生成区間しか持たず id を持つ行が 0・支度表の正本は語彙が設計文書の正本に数えない）。実装（`crates/folio/src/mentions.rs` の `TARGETS`）は初めからこの 9 つで数えており、値〔0 件〕も数える対象も動かない | B | 規則の表の行の変更（条 P-17.1・裁定 id が要る） | 4 |
| 整合 | F-5 | 直す | `srs` `requirements.FR17.shall` | **規範文は触らず、注 2 か所で断る。** 規範文と実装（便 98・`crates/folio/src/ceiling_src.rs` の `cut_sections`）と語彙は「節」で揃っており、判断の記録 ADR-13 の決定 (5)(10) の「読む欄」とは単位が違う＝**決定 (5) の目標の単位（欄）より粗い節で実装しており、切る上限（読まないと宣言した欄だけを切る）は守っている**。束に残る中身は決定の字どおりより多い。ADR-13 の注 (m) に「欄まで絞ることはしない（節で止める）」を決めた字で書き、FR17 の注にも同じ断りを書いた。注で済ませるか判断の記録の改訂にするかは問 5 で問う | B | 要件書の版上げ ＋ 判断の記録の注 | 1・5 |
| 整合 | F-6 | 直す | `constitution` `articles.P-2.mechanism.note` | 「数える面は入口・憲法・要件書・判断の記録・設計ノートの 5 つ」を「数える面の一覧の正本は要件書の受入基準 AC2 で、ここには写さない・P-6.3」に。**機構の注は条 A-2.2 の改訂の範囲の外**（一括 10・11 と同じ扱い）で、憲法の版は上がらない | B | 憲法の機構の注（改訂の範囲の外） | 5 |
| 整合 | F-7 | 直す | `srs` `requirements.FR20.shall` | **規範文の 1 句を直す。** 「その一覧に設計文書の正本が 1 つも無ければ 0」を「その一覧に設計文書の置き場の file（生成物の棚 preview/ と退役の置き場の file を除く）が 1 つも無ければ 0」に。実装（`crates/folio/src/gate.rs` の `is_design_source`）が初めから数える範囲の字で、振る舞いは変わらない。語彙の設計文書の項の門の 1 文と、FR20 の平易文・FR20 の確かめ方と受入基準 AC18 の平易文と固定の材料の場合の名札（「設計文書の置き場の file を触らない便は通す」）も同じ字に揃えた（判定の式は変えない） | B | 要件書の版上げ（規範文） | 2 |
| 読みやすさ | F-1 | 直す | `srs` `meta.status_note` | **便候補。** 面の生成器（`crates/folio/src/face_srs.rs` の `approval`）が承認欄の頭に「版ごとの来歴」の折りたたみを出し、すぐ下の承認の行と同じ来歴を 2 度読ませる。正本の側で `status_note` を要旨だけに縮める案は採らない＝来歴は承認の行の stamp より細かい（例: v1.12 の直した要件の id の並びは承認の行に無い）ので、縮めると正本から細目が消える。§12 に置いた | **C** | 面の生成器の便 | — |
| 読みやすさ | F-2 | 直す | `adr` `ADR-13.plain` | 平易文の「この repo の設計文書」を「この置き場の設計文書」に（要件 FR21 の平易文と同じ字）。記録を ADR-13 の注 (n) に。あわせて語彙の用語集に「repo（版管理の置き場）」の項を立てた＝判断の記録 ADR-16 の平易文の 9 か所（26 周目の後に発効したので周はまだ読んでいない）も同じ形で、一度に閉じる（問 1） | B | 判断の記録の平易文 ＋ 語彙（要件書と同梱） | 1・5 |
| 読みやすさ | F-3 | 直す | `adr` `ADR-8.note`（ADR-11・ADR-6・ADR-4 も同形） | **便候補。** 注はもう折りたたみ（`<details>`）の中に在り、名札が「注」の 1 字だけ。名札を「注（書き手あての改訂の記録）」のように読み手あての断りにするのは面の生成器（`crates/folio/src/face_adr.rs` 734〜738 行）の 1 か所で、判断の記録 16 本に一度に効く。正本の注の頭に 1 文ずつ足す案は 4 本を手で揃えることになり、次の判断の記録で漏れるので採らない。§12 に置いた | **C** | 面の生成器の便 | — |
| 読みやすさ | F-4 | 直す | `rules` `thresholds.R-1.population` | **語彙の側で閉じる。** 語彙に「口座」の項を新設し、定義の中で「口座共通（home 配下）」を開いた。口座は行 R-1 のほかに行 D-8 と判断の記録 ADR-16 にも出るので、行の字を替えるより広く閉じる。**行 R-1 の字は触らない**＝行を変えると裁定 id が要り（条 P-17.1）、さらに歯 `crates/folio/tests/face_constitution.rs` の `f79_rules_ruling_folds_the_previous_ones` が行 R-1 の前の裁定の数（4）を実の記録で pin しているので落ちる（1 度目の起草で行を直して実測した・下の床の節） | B | 要件書の版上げ（語彙を同梱） | 1 |
| 実態 | F-1 | 直す | `srs` `requirements.FR17.note` | 「絞る口は後続の便で入り」を着地の字に（便 98・台帳 f2-648.140・2026-09-22・`ceiling_src.rs` の `cut_sections`／便 97・台帳 f2-648.135・2026-09-22）。所見が「bundle.rs の keep」と書いた場所は、実測では `ceiling_src.rs` の `cut_sections` の中の `keep` だった | B | 要件書の版上げ | 1 |
| 実態 | F-2 | 直す | `srs` `requirements.FR20.note` | 「面の天井の名札は印を読む形に変える（別の便）」を「便 83（台帳 f2-648.121・2026-09-22 着地・`crates/folio/src/face.rs` の `ceiling_stamp`）で印を読む形に変わった」に | B | 要件書の版上げ | 1 |
| 実態 | F-3 | 直す | `srs` `nonfunctional.NFR3.note` | 母集団の外の正本の並びに**索引の欄の決まりの正本**（便 95・2026-09-22 着地）を足し、それが床 `folio check` の読む file（`crates/folio/src/check.rs` の `FILES` 7 本）にも入らないことを断った | B | 要件書の版上げ | 1 |
| 便 102 | 🔴 1 | — | `ceiling` `viewpoints` | 整合 F-2 と同じ 1 行（上の行） | B | 天井の正本の版上げ | 3 |

| 便 102 | 🔴 2 | — | `graph` `meta.status`（draft・v0.1） | 発効の承認の行を足すだけ（中身は変えない）。問 3 で整合の観点が読むので、起草のまま読むと次の周が拾いうる（独立の検証の問う 1）。正本の頭の状態の行に「一括 14 の問 6 で問う」を書いた | B | 索引の欄の決まりの正本の発効 | 6 |

**まとめた後の問いの数 = 6。**（直す 14 件のうち 12 件と 🔴 1・🔴 2 を 6 つの問いにまとめ、2 件は便候補に回した。）

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 読みやすさ F-1（要件書の面の承認欄の来歴の二重）・F-3（判断の記録の面の注の名札） | 直す先は面の生成器（実装）。正本は 1 字も動かない。便候補として `docs/design/face-deliveries-plan.md` §12 に置いた |
| 生成区間の直し | **本周には無い**（14 件とも人が書く欄）。生成区間は手で直さない（条 P-6.2・判断の記録 ADR-9 / ADR-11）ので、在れば便候補に回すところだった |
| 便 102 の 🔴 3（`documents` の行の幅の崖） | 本一括は読む文書の一覧（生成区間）に文書を足さないので、崖に触れない |
| 要件書の `effective_version`・天井 / 入口 / 索引の欄の決まりの承認の行・索引の欄の決まり `graph.yaml` の `meta.status` と頭の注釈 2 行（起草 → 発効）・行 R-17 の裁定の逐語と `ruled_at`（承認の日付に書き換える）・ADR-13 の注の承認の断り | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.32 版の起草。** FR20 の規範文の 1 句（整合 F-7）／FR17 の注（整合 F-5・実態 F-1）／FR20 の注（実態 F-2・規範文の直しの記録）／NFR3 の注（実態 F-3）／FR20 の平易文と確かめ方・受入基準 AC18 の平易文と固定の材料の場合の名札（整合 F-7）／`meta.version` を v1.32 に・`status_note` の要旨に「第 1.32 版は起草・承認待ち」と来歴の末尾に v1.32 の起草・`approval` に 作成 の行。**`effective_version` は v1.31 のまま** |
| `design-intent/vocabulary.yaml` | 3 項。設計文書の項（整合 F-1 = 一括 13 の断りの 1 文を落とす・整合 F-7 = 門は置き場で数える）／**口座の項を新設**（読 F-4）／**repo（版管理の置き場）の項を新設**（読 F-2 と同じ形・判断の記録 ADR-16 の平易文 9 か所）。**要件書と同じ承認で運ぶ**（一括 11〜13 と同じ形） |
| `design-intent/ceiling.yaml` | **第 0.13 版の起草。** 整合の観点の読む文書に graph の閉じた一覧 3 つ・読み手の想定と問いの文を 10 種に・頭の状態の行と `approval` に 作成 の行（便 102 🔴 1・整合 F-2） |
| `design-intent/index.yaml` | **第 0.4 版の起草。** 棚の説明の道具自身の正本の並びに索引の欄の決まりの正本（整合 F-3）・頭の状態の行と `approval` に 作成 の行 |
| `design-intent/rules.yaml` | **行 R-17 の 1 行。** 母集団に数えない正本 2 つとその理由（整合 F-4）・`ruling` を「一括 14 の承認待ち」の形に（承認後に `ruled_at` も書き換える旨を欄に書いた）・`ruled_at` を 2026-09-23 に。値・種別・段・数える対象は不変 |
| `design-intent/adr/ADR-13.yaml` | `plain` の「repo」を「置き場」に（読 F-2）・`note` に【天井の 26 周目の所見への応答】(m)（欄まで絞ることはしない＝節で止める・決定 (5) の単位より粗い・整合 F-5）と (n)（平易文の直しの記録）。**決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/constitution.yaml` | 条 P-2 の機構の注の面の並びを受入基準 AC2 を指す字に（整合 F-6）。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触っていない**＝条 A-2.2 の改訂に当たらない |
| `design-intent/graph.yaml` | 頭の状態の行に「発効は一括 14 の問 6 で問う」の 1 文だけ（meta と生成区間は不変・承認の行は席が承認後に足す） |
| `docs/design/face-deliveries-plan.md` | §11 の 3 本目の頭に「済み」の断り／§12 に一括 14 からの便候補 3 本（読 F-1・読 F-3・床の歯「documents の id はどれかの観点の reads に在る」） |
| `docs/design/batch14-triage.md` | **新設**（この file） |

**実装（`crates/folio/src`）・凍結 anchor（`tests/fixtures/`）・歯（`crates/folio/tests`）・生成区間は 1 byte も触っていない。**

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.32 版の注 3 本と、語彙 3 項・入口の正本 第 0.4 版の発効

**やりたいこと。** 要件書の注 3 本（FR17・FR20・NFR3）を実態に合わせ、語彙の 3 項（設計文書の項の直し・口座の項の新設・repo の項の新設）と入口の正本 第 0.4 版（棚の説明の 1 句）を同じ承認で発効する。**この問いの分では、要件の規範文も平易文も受入基準の判定の式も 1 字も動かない。**（FR20 の規範文の 1 句は 問 2 で別に問う。この問いが問 2 に依るのは 1 か所だけ＝語彙の設計文書の項の門の 1 文〔「門は…置き場で数える」〕で、問 2 を退けたらその 1 文を元の字に戻す。第 1.32 版の発効は 問 1 と 問 2 の両方の答えで決まる。）

**理由。** 8 件のうち 6 件は「着地したのに文書が古い」か「同じ集合を 2 文書が違う数で書いている」形で、残り 2 件（口座・repo）は「用語集に無い語」の形である。

1. 語彙の設計文書の項が、便 102 の着地後も「索引の欄の決まりは天井の読む文書の一覧にまだ載っていない」と言い、天井の正本 第 0.12 版と逆のことを書いている（整合 F-1）。一括 13 が足したこの断りの 1 文を落とす。どの観点が何を読むかの正本は天井の正本なので、語彙には写さない。
2. 入口の棚の説明が「道具自身の正本 = 相談窓口の正本と天井の正本」と等号で結び、判断の記録 ADR-14 決定 (2) が棚に載せないと決めた 3 つ目を落としている（整合 F-3）。
3. 要件 FR17 の注が、2026-09-22 に着地した絞る口と独立の anchor を「後続の便で入る」と書いている（実態 F-1）。同じ注に、判断の記録 ADR-13 の決定 (5) の「読む欄」より粗い「節」で止めていること（切る上限は守っている）を足す（整合 F-5）。
4. 要件 FR20 の注が、2026-09-22 に着地した面の天井の名札を「別の便」と書いている（実態 F-2）。
5. 非機能要件 NFR3 の注が、参照 id を数えない正本の並びから索引の欄の決まりを落としている（実態 F-3）。
6. 規則の表の行 R-1 の母集団の「口座」「home 配下」が用語集に無く、「口座」は日常語と別の意味で使われている（読 F-4）。口座は行 D-8 と判断の記録 ADR-16 にも出るので、語彙に項を立てて 3 か所を一度に閉じる。
7. 判断の記録 ADR-16 の平易文に、用語集に無い「repo」が 9 か所ある（「scribe2 の repo」「新しい repo では」など）。26 周目の読 F-2 が ADR-13 で拾ったのと同じ形で、ADR-16 は周の後（2026-09-23 17:39 JST）に発効したので周はまだ読んでいない。語彙に「repo（版管理の置き場）」の項を立てて一度に閉じる（英字の識別子の一覧 identifiers には同じ綴りが在るが、用語集〔terms〕には無かった）。

**代替案。**
- **(A) 読 F-4 を行 R-1 の字で直す。** 母集団の「口座共通（home 配下）」を日本語に開く。行の変更なので裁定 id が要り（条 P-17.1）、歯 `f79_rules_ruling_folds_the_previous_ones` が行 R-1 の前の裁定の数を 4 で pin しているので、歯を直す席の PR が同時に要る（1 度目の起草で実測した）。行 D-8 と判断の記録 ADR-16 の「口座」は閉じないまま残る。
- **(B) 入口の正本を触らず、語彙の側で断る。** 棚の説明の字は持ち主が第 0.3 版で承認した字なので、語彙の設計文書の項に「棚に載せない」の 1 文だけ残す形にもできる（今もその 1 文は在る）。ただし棚の説明の等号が 2 つのままなので、次の周の整合が同じ所を拾い直す。

**コスト。** 承認 1 回。動くのは要件書の注 3 本・語彙の定義 1 本と新しい項 2 つ・入口の棚の説明の 1 句。**要件の番号は 1 つも動かず（条 P-7.1）、床の歯も 1 本も直さない**（枝の HEAD で 779 本すべて通る）。

**推奨 = 8 件をまとめて発効する（代替案は採らない）。** 義務を増やさず減らさず、字を実態に合わせるだけである。読 F-4 を語彙で閉じるのは、行の字を替えるより閉じる範囲が広く、裁定 id も歯の直しも要らないためである。

### 🔴 問 2. 要件 FR20 の規範文の 1 句を、門が実際に数える範囲の字にする

**やりたいこと。** 要件 FR20 の規範文の

- 前 — 「その一覧に**設計文書の正本**が 1 つも無ければ 0（通す）。」
- 後 — 「その一覧に**設計文書の置き場の file（生成物の棚 preview/ と退役の置き場の file を除く）**が 1 つも無ければ 0（通す）。」

に直す。同じ字を 4 か所に揃える。

- 語彙の設計文書の項の門の 1 文 —「門は設計文書の正本かどうかでなく置き場で数える」。
- FR20 の平易文 — 前「（設計文書を触らない便は通す）」→ 後「（設計文書の置き場の file を触らない便は通す）」。
- 受入基準 AC18 の平易文 — 前「設計文書を触らない便は通す」→ 後「設計文書の置き場の file を触らない便は通す」。
- 受入基準 AC18 の固定の材料の文と FR20 の確かめ方の場合の名札 — 前「門の 6 場合（設計文書を触らない・…）」→ 後「門の 6 場合（設計文書の置き場の file を触らない・…）」。

**受入基準 AC18 の判定の式（6 場合と 3 値の終了コード）は動かない。** 場合の名札は字で、判定の式ではない。

**理由。** 語彙の設計文書の項は「支度表の正本は設計文書の正本に数えない」と書きながら、同じ項で「支度表の正本は門が数える側に在る」と書く。規範文は門の鍵を「設計文書の正本」と書くので、**支度表の正本だけを書き換える便を、要件の字は「通す」と読み、語彙と実装は「門に掛ける」と読む**（整合 F-7）。実装（`crates/folio/src/gate.rs` の `is_design_source`）は初めから「設計文書の置き場の下で、生成物の棚 `preview/` と `retired` を含む path を除いた file」を数えているので、字を実装へ寄せれば 3 つが揃い、振る舞いは 1 つも変わらない。この範囲には凍結 anchor の置き場（`design-intent/anchors/`）も入る（今も入っている）。

**代替案。**
- **(A) 語彙の側を直す。** 支度表の正本も「設計文書の正本」に数える。すると行 R-17 の母集団・天井の読む文書の一覧・入口の棚の数え方が同じ語を使っているので、3 か所が一斉に「支度表を入れるか」を問われる。支度表は相談窓口の生成物に承認欄を足したもので、天井の読む文書にも入らない。
- **(B) 規範文は触らず、FR20 の注で断る。** 注で「ここで言う設計文書の正本は置き場の file の全部」と読み替えを定めると、規範文の語と語彙の語が違う意味になり、読み替えの在り処が注に隠れる。次の周の整合がもう一度拾う見込みが高い。

**コスト。** 承認 1 回（問 1 と同じ版で運ぶ）。規範文の 1 句・平易文の 2 句・場合の名札の 2 句・注の 1 段落。**実装・歯・凍結 anchor は動かない。** 平易文を揃えないと、支度表だけを直す便を平易文は「通す」と読めるので、同じ食い違いが規範文から平易文の層へ移るだけになる（次の周の忠実さが拾う）。

**推奨 = 規範文の 1 句を置き場の字に直す（代替案は採らない）。** 振る舞いを変えずに、要件・語彙・実装の 3 つを同じ範囲の字に揃えられる唯一の案である。

### 🔴 問 3. 天井の正本 第 0.13 版 — 文書どうしの整合の観点が索引の欄の決まりを読む（便 102 の 🔴 1）

**やりたいこと。** 天井の正本の整合の観点の読む文書に 1 行を足す。

```yaml
      - {doc: graph, fields: [schema.node_kinds, schema.edge_types, schema.edge_fields]}
```

あわせて、整合の観点の読み手の想定を「9 種の文書」から「10 種の文書」に、問いの文の文書の列に「索引の欄の決まり」を足す。観点の数・重さ・読む文書の一覧（生成区間）・ほかの 3 観点は動かない。

**理由。** 天井の印の要約値と門が測り直す要約値は、**観点の reads が指す文書だけ**を連結する（便 102 の起草役が `crates/folio/src/gate.rs` と `crates/folio/src/stamp.rs` で測った事実）。第 0.12 版は `graph.yaml` を読む文書の一覧に足したが、どの観点の reads にも入れなかったので、**`graph.yaml` を書き換えても印は古くならない**＝一括 12 の問 4 の 2 が塞ぎたかった穴が半分開いたままである。26 周目の整合 F-2 も同じ所を「読む文書の一覧に在って、どの観点も読まない文書が 1 つ在る」と拾った。整合の観点にするのは、索引の欄の決まり（節点の種類・辺の型・辺の欄）が判断の記録 ADR-13 の決定 (1)(2)(8) と突き合わせる先だからである（便 102 の起草役の推奨と同じ）。

欄の名は `schema.` を頭に付けた。`graph.yaml` の最上位の節は `meta` と `schema` の 2 つだけで、束を絞る単位は最上位の節なので、`node_kinds` と裸で書くと束が「宣言に在るが正本に無い節 3」を出す。実測で `folio ceiling --write` は `正本に無い節 0`・整合の束の `sources/` に `graph.yaml` が入った。

**代替案。**
- **(A) 4 観点すべての reads に足す。** 忠実さと読みやすさは平易文や人の文を読む観点で、`graph.yaml` には平易文が無い（meta と生成区間だけ）。束が 3 つ増えるだけで、見る目は増えない。
- **(B) 実態の観点に足す。** `graph.yaml` の閉じた一覧は実装の定数（`crates/folio/src/graph.rs`）の写しで、`folio schema --check` が byte 一致を数える＝実態との食い違いは床の領分であり、観点の問いの文は床の領分を挙げないと定めている。
- **(C) 読む文書の一覧から `graph` を外す。** 一覧は生成区間（正本は `crates/folio/src/ceiling.rs`）なので実装の便が要り、一括 12 の問 4 の承認を覆すことになる。`graph.yaml` を書き換える便が印に引っかからない形がそのまま残る。
- **(D) 印と門の要約値を、読む文書の一覧（documents）の全部に広げる実装の便（根の直し）。** 「一覧に在るがどの観点も読まない文書」の穴を、次に文書を足したときにも開かない形で塞ぐ。`crates/folio/src/gate.rs` と `crates/folio/src/stamp.rs` の便で、凍結の印の写しが動く。床の歯「documents の id はどれかの観点の reads に在る」（条 P-3.1）と並べて、`docs/design/face-deliveries-plan.md` §12 の便候補に置いた。本問の推奨と両立する（先に reads で閉じ、根は便で塞ぐ）。

**コスト。** 承認 1 回。整合の束が 1 file（`graph.yaml`・4,648 byte）増える。天井の正本を書き換えるので印は古くなる（印は main の時点ですでに古い）。**便 102 の契約表は「足すと束の凍結 anchor と所見 fixture の要約値が動く」と見積もっていたが、実測では歯 779 本がすべて通った**＝凍結 anchor は実の正本でなく最小の写し（`tests/fixtures/ceiling/`）から組むので、実の天井の正本の reads を足しても動かない。

**推奨 = 整合の観点だけに足す（代替案 A〜C は採らない・D は便候補として別に運ぶ）。** 読まれる `graph.yaml` が起草のままである点（便 102 の 🔴 2）は 問 6 で問う。

### 🔴 問 4. 規則の表の行 R-17 の母集団に、数えない正本 2 つを明記する

**この問いは「規則の表の行を変えてよいか」の裁定である。** 行の変更には裁定の id と時刻を行に付けることが条 P-17.1 で要る。承認をもらったら、席が行 R-17 の `ruling` の欄に逐語と時刻を書く。

**やりたいこと。** 行 R-17 の母集団の末尾に 1 文を足す。

- 後 — 「設計文書の置き場に在る正本のうち 2 つはこの母集団に入れない＝索引の欄の決まりの正本（graph.yaml）は meta と生成区間しか持たず id を持つ行が 1 つも無いため、支度表の正本は相談窓口の生成物に承認欄を足したもので設計文書の正本に数えないため（語彙の「設計文書」の項）」

**値〔0 件〕・種別・段・数える対象は 1 つも動かない。**

**理由。** 語彙の設計文書の項は索引の欄の決まりの正本を設計文書に数え、天井の正本も読む文書の一覧に持つのに、行 R-17 の母集団の並び（9 つ）だけがその 1 file を落としている（整合 F-4）。実装（`crates/folio/src/mentions.rs` の `TARGETS`）はこの 9 つで数え、code の注も「支度表は入れない」と書いている。**落としているのは理由があってのこと**なので、並びに足すより、落とす理由を行に書くほうが実装と合う。

**代替案。**
- **(A) 母集団の並びに索引の欄の決まりを足す。** 行の字が実装の `TARGETS` と食い違うので、`mentions.rs` に 1 行足す便が要る。`graph.yaml` に id を持つ行は 0 なので、数えても 0 のままで、得るものが無い。
- **(B) 行は触らず、語彙の設計文書の項に「行 R-17 は数えない」と書く。** 行の母集団の正本は行の欄（語彙の母集団の項のとおり・P-6.3）なので、数えない理由を行の外に置くと、同じ集合の定義が 2 か所に割れる。

**コスト。** 行 1 つの字。裁定 id と時刻が要る。歯は動かない（行 R-17 の前の裁定の数を pin する歯は無い・枝の HEAD で 779 本すべて通る）。

**推奨 = 数えない 2 つと理由を行に書く（代替案は採らない）。**

### 🔴 問 5. 判断の記録 ADR-13 の平易文と注、憲法 P-2 の機構の注

**やりたいこと。** 3 か所を直す。**判断の記録の決定・帰結・承認欄と、憲法の条文（id・見出し・規範文・段・縛る相手）は 1 字も触らない。**

1. 判断の記録 ADR-13 の平易文の「この repo の設計文書」を「この置き場の設計文書」に（読 F-2）。
2. 判断の記録 ADR-13 の注に【天井の 26 周目の所見への応答】(m)(n) を足す。(m) = 着地した口（便 98）と要件 FR17 の規範文は**欄まで絞ることはしない（節で止める）**＝決定 (5)(10) の「観点が宣言する読む欄」より粗い単位で、束に残る中身は決定の字どおりより多い／決定 (5) の「切るのは観点が自分で「読まない」と宣言した欄だけ」が定める切る上限は守られている／欄まで絞る口は置かず、節で止める形を今の実装として受け入れる（整合 F-5）。(n) = 1 の記録。
3. 憲法の条 P-2 の機構の注の「数える面は入口・憲法・要件書・判断の記録・設計ノートの 5 つ」を「数える面の一覧の正本は要件書の受入基準 AC2 で、ここには写さない・P-6.3」に（整合 F-6）。

**理由。** 1 は非エンジニアあての平易文に、用語集に無い「repo」が言い換えなしで出ている（同じ設計文書のほかの平易文は「置き場」か「版管理（公開 repo）」の形で開いている）。2 は、判断の記録の字だけが「欄」、要件と実装と語彙が「節」と書いていて、**単位が違い、束に残る中身も違う**（例: 忠実さが憲法について宣言するのは `articles` の中の `plain` と `statements.text` だけだが、節で絞ると `articles` の全欄が残る）。決定 (5) の目標の単位（欄）より粗い節で実装しており、切る上限（読まないと宣言した欄だけを切る）は守っている。粗い側は束が多く残る側＝精度の側に倒れる違いで、撤退条件にも触れない。3 は、判断の記録 ADR-6 の注が「今の数の正本は受入基準 AC2」と定めているのに、条の機構の注が同じ一覧の 3 つ目の写しを持っている（条 P-6.3 から外れる）。

**2 の扱いは次の対から選ぶ。**
- **(2-a) 注で済ませる（推奨）。** 注 (m) に「欄まで絞ることはしない（節で止める）」を**決めた字として**書く（今の字）。条 P-13.3 の「設計文書の変更は判断の記録か版付きの文書として残す」は、版付きの判断の記録の注と、この一括の承認の逐語で満たす。
- **(2-b) 判断の記録の改訂にする。** 一括 11〜13 の「決定は触らず注で断る」の先例は、同じ意味を違う字で書いていた場合に当てたもので、今回は意味（粒度）が違う。決定 (5)(10) の単位を「節」に改める改訂（判断の記録どうしの改訂の欄・撤退条件の見直し・敵対的検証）を起こす形で、承認と手順が 1 つずつ増える。

**ほかの代替案。**
- **(A) 判断の記録 ADR-13 の決定 (5)(10) の「欄」を「節」に、改訂の手順を経ずに直す。** 発効した決定の本文を後から書き換えることになり、条 P-13.3 の残し方にも当たらない。
- **(B) 憲法の機構の注を触らず、ためて 1 本の判断の記録で運ぶ。** 一括 11〜13 が憲法の出所の重複を「ためる」扱いにしたのは、出所（`sources`）の変更が条 A-2 の改訂の手順を要するためだった。**機構の注は条 A-2.2 の改訂の範囲の外**（一括 10・11 で機構の注を同じ承認で直した先例がある）なので、ためる理由が無い。

**コスト。** 承認 1 回。動くのは平易文の 1 語・注の 2 段落・機構の注の 1 句。**義務は 1 つも増えない。** 憲法の版は上がらない（条 A-2.2）。`folio inject --check` の生成区間（前文と規範文）は動かない（枝の HEAD で 68 行 / 7,912 byte 一致）。

**推奨 = 3 か所とも直し、2 は注で済ませる（2-a）。** 違いは精度の側に倒れるもので撤退条件に触れず、欄まで絞る口を作る予定も無いので、決定の本文を改める手順に見合う利得が無い。

### 🔴 問 6. 索引の欄の決まりの正本（graph.yaml）第 0.1 版の発効

**やりたいこと。** `design-intent/graph.yaml` の承認欄に持ち主の承認の行を 1 つ足し、`meta.status` を `effective` にし、頭の注釈 2 行（「v0.1 起草」「発効は一括 14 の問 6 で問う」）を発効の字に直す。**中身（meta のほかの欄と生成区間）は 1 字も変えない。** 足す行の形は他の正本と同じで、席が承認後に書く。

```yaml
    - {role: 承認, who: 持ち主（shuu5）, when: <承認の日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 14 の問 6）", verbatim: <逐語>, version: v0.1}
```

**理由。** 問 3 を承認すると、天井の整合の観点が `graph.yaml` を読む。いまの `graph.yaml` は `status: draft`・第 0.1 版で、発効していない（便 102 の 🔴 2・`docs/design/delivery-102.md` §1 (i) は「発効を次の一括に載せるか、起草のままでよいかを決める」と書いた）。起草のまま読ませると、次の周の実態か整合が「発効していない文書を天井が読むと宣言している」と拾う見込みがあり、所見 → 一括 → 承認 → 周の 1 往復が余分にかかる。`graph.yaml` は meta を除いて全部が生成区間（実装の定数 `crates/folio/src/graph.rs` の写し）で、閉じた一覧の中身は判断の記録 ADR-13 の決定 (1)(2)(8) が発効の時点で決めている。発効の判断材料は小さい。

**代替案。**
- **(A) 起草のまま読む。** 次の周が拾わないように、天井の正本の読む文書の一覧の `graph` の行の注に「起草のまま読む」と断る。ただしその注は生成区間（正本は `crates/folio/src/ceiling.rs`）なので、実装の便が要る。

**コスト。** 承認 1 回（ほかの問と同じ回で答えられる）。承認欄の行 1 つと `meta.status` の 1 語。床の歯は動かない（承認後の形を検証役が枝の写しで実測して緑・2026-09-23・まだ分からない点の 1 を見よ）。承認後に席が行を足したら床 4 本を撃ち直す。

**推奨 = 発効する（代替案は採らない）。**

## 床の結果（この枝の HEAD で実測・2026-09-23・main root の binary = b81ae12 の build）

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | rc 0（9 file 一致） |
| `folio inject --check --dir design-intent` | 68 行 / 7,912 byte 一致 |
| `cargo nextest run --workspace` | **779 / 779 PASS** |
| `folio ceiling --write`（整合の束の確認） | 観点 4・正本に無い節 0・整合の `sources/` に `graph.yaml` が入る |
| `folio ceiling --gate --write-set design-intent/srs.yaml` | まだ分からない（印が古い）＝main でも同じ |

**歯は 1 本も直していない。** ただし **1 度目の起草で行 R-1 の母集団を直したとき、歯 `crates/folio/tests/face_constitution.rs` の `f79_rules_ruling_folds_the_previous_ones`（240 行 `assert_eq!(source_previous("R-1"), 4)`）が 5 ≠ 4 で落ちた。** 実の規則の表の「前の裁定」の数を定数で pin している歯で、行 R-1 をどう変えても裁定の鎖が 1 つ伸びるので必ず落ちる。本一括は読 F-4 を語彙の側で閉じてこの歯を避けたが、**次に行 R-1・R-2・R-14・D-1・D-3 のどれかを変える一括は、この歯（と同じ file の `f79_rules_ruling_without_previous_is_unchanged`〔行 D-1 の前の裁定 0 を pin〕）を先に「数でなく性質で当てる形」へ直す席の PR が要る**（一括 12 と 13 の申し送り〔実の `design-intent` の実測を数や字面で pin する歯を性質で当てる形へ移す便〕と同じ系統で、その便はまだ起票されていない）。

## まだ分からない点

1. **問 6 の承認後の床。** `graph.yaml` の承認の行を足し `meta.status` を `effective` にした形は、検証役が枝の写しで実測して緑だった（folio check 合格・schema --check 一致・build 23 file・ceiling --write 正本に無い節 0・nextest 779/779・2026-09-23）。承認の行は席が承認後に書き、そのとき床を撃ち直す。
2. **C 2 本の便の大きさは測った**（`docs/design/face-deliveries-plan.md` §3 の式・2026-09-23）。余地は `crates/folio/src/face_srs.rs` 476・`crates/folio/src/face_adr.rs` 644・`crates/folio/tests/face_adr.rs` 398・`crates/folio/tests/face_srs.rs` 1103 で、どちらの便も S でも M でも受付の余地がある。歯 `tests/face_srs.rs` の `f81_approval_history_is_folded` と `tests/face_adr.rs` の `NOTE_FOLD` が今の折りたたみの字を pin しているので、便の write-set に歯が入る。「まだ分からない」なのは、便の設計で折りたたみをどう変えるか（外すか名札を替えるか）だけである。
