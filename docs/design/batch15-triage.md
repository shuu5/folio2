# 一括 15 の仕分け — 天井の 27 周目の所見（直す 22 件と止める 1 件）と席の実測 1 件

**状態: 起草・承認待ち（2026-09-24 00:1x JST 起草・00:2x JST 改訂 b = 整合の補いの審査の直す 4 件〔F-6〜F-9〕と忠実さ F-1 の反証の確定を取り込んだ・00:3x JST 改訂 c = 独立の検証 ~/.local/share/folio2/handoff-2026-09-24/batch15-verify.md の直す 15 点・問う 3 点・参考 9 点を席の裁定どおりに当てた・07:1x JST 改訂 f = 席の実測〔器の口 s2-07l.473 は未着地〕で要件 FR11 と FR13 の注の誤りを直し、判断の記録 ADR-16 の注 (f) と問 7 を足した・07:1x JST 改訂 g = 器の側の返事〔s2-07l.473 の再開〕を FR11 の注と問 7 に足した・改訂 h = 判定点 ③ の測定値を ADR-16 決定 (1)(5) の字〔器の受付が導出物を読んで通した記録〕に揃え、.473 の着地を前提と書き分けた）。** 下の 問 1〜7 が持ち主への承認要求である。承認の記帳（要件書の承認欄と `effective_version`・規則の表の行 R-16 の裁定と `ruled_at`・判断の記録 ADR-16・ADR-13・ADR-3・ADR-6・ADR-9 の注の断り・状態の 1 枚と台帳）は、持ち主が答えた後に席が同じ枝の別の commit で正本の側へ書く（書く欄の全部は §「承認の後に席が書く欄」）。**この文書にも正本にも、持ち主の逐語はまだ 1 字も入っていない。**

起草 2026-09-24・orchestrator 席（起草は opus 5.5 の起草役に委ねた。正本の作成の行は席を責任者として「orchestrator 席（AI・fable 5.1）」と書く）・作業場所 `.worktrees/planner-batch15`（branch `docs/batch15`・`origin/main` 2c3a631 から切った）。

材料は天井の 27 周目の所見である（周 = `~/.local/share/folio2/ceiling/2026-09-23-round27/`・観点 4・審査役 opus・束は main 2c3a631 で組んだ・正は各観点の `findings.yaml`）。判定は 忠実さ **不合格**（止める 1）・整合 合格・読みやすさ 合格・実態 合格。整合は補いの審査（判断の記録 ADR-1〜7・9・10・12 を読む）で直す 4 件（F-6〜F-9）が同じ `findings.yaml` に足された（判定は合格のまま・止める 0）。`findings-all.txt` は補いの後に作り直した一覧で、27 行とも各観点の `findings.yaml` と id・重さ・場所が一致する（正は `findings.yaml`）。所見は 27 件で、内訳は次のとおり（`findings.yaml` から数え直した）。

| 観点 | 止める | 直す | 参考 |
|---|---|---|---|
| 整合 | 0 | 7 | 2 |
| 忠実さ | 1 | 4 | 1 |
| 読みやすさ | 0 | 6 | 1 |
| 実態 | 0 | 5 | 0 |
| 計 | 1 | 22 | 4 |

**止める 1 件（忠実さ F-1）の反証は「支持」**（反証の記録 `~/.local/share/folio2/ceiling/2026-09-23-round27/refute-notes/fidelity-F-1.md`・反証役 opus）。席も「支持・止める のまま」と確定した（2026-09-24 00:2x JST の知らせ）。

**同じ箇所を 2 つの観点が指す組が 2 つ在る。** 整合 F-1 と実態 F-2（要件 FR13 と非機能要件 NFR1 の注）、整合 F-3 と実態 F-4（受入基準 AC5 の固定の材料の退役の置き場）で、それぞれ 1 つの直しにまとめた。

**生成区間に在る所見は 0 件である。** 23 件の place はどれも人が書く欄にある。

**印はもう古い。** main（2c3a631）でもこの枝でも `folio ceiling --gate --write-set design-intent/srs.yaml` は「まだ分からない（印が古い）」を返す（印は 26 周目のもの）。この枝の PR は席が受付の手順で運ぶ（規則の表の開発規律行 D-12）。

## 仕分けの表（直す 22 件 + 止める 1 件）

仕分けの記号は一括 14 と同じ向き。**B** = 本一括で正本を直した（持ち主の承認が要る）／**承認不要** = 本一括で直したが承認欄を持たない／**C** = 便候補（直す先が実装）／**A-2** = 条文の改訂が要るので本一括では直さない／**参考** = 直さない。本一括に **承認不要** は 0 件。

| 観点 | id | 重さ | place（実在を確認） | 是正の内容 | 仕分け | 裁定の種類 | 問 |
|---|---|---|---|---|---|---|---|
| 整合 / 実態 | F-1 / F-2 | 直す | `srs` `requirements.FR13.note`（同じ形で `nonfunctional.NFR1.note`） | 前「状態は 保留（器の口 s2-07l.473 の着地後・M3 の入口で再判定）」→ 後「状態は 保留＝判断の記録 ADR-16 決定 (5) の M3 の入口の再判定で、M3 に入れず保留のまま残した」。器の口の着地（2026-09-22）を条件の字から事実の字へ。FR13 の口が要件 FR11 の導出物の便の後であることを書いた。**次の再判定の時機（M3 の出口）は問 2 で問う。** 台帳の字は「この欄に従い、食い違えばこの欄を正」に改めた（実態 F-1 と同じ直し）。FR13 の「未実装（2026-09-20 時点）」を 2026-09-24 の実測の字に | B | 要件書の版上げ（注） | 1・2 |
| 整合 | F-2 | 直す | `rules` `thresholds.R-16.note` | 前「scribe2 の暫定床（…宣言順まで同じ）と同じ式」→ 後「同じ所（印・直後の文字・単位・母集団から外すもの・文の区切り）と、式の 3 つの違い（参照 id として数える形・数と単位の読み方・解決の有無＝判断の記録 ADR-16 文脈 (7)）」。**同じ注の撤去の条件**（「M1 の残りが揃った後に器へ要求する」）も ADR-3 決定 (7) の「引き取ったら」を ADR-16 決定 (6) が定め直した字にした（所見の外・同じ形）。揃える向きは器の側で folio2 の読みと今の値は動かないことも書いた。同じ「同じ式」を持つ要件 FR12 の注・語彙の散文の門の項・判断の記録 ADR-3 の注（決定 (7) の断り）も揃えた。値・種別・段・母集団は不変。**ADR-16 決定 (6) が参照 id の形を読む便に割り当てた直しの時機を前倒しする** | B | 規則の表の行の変更（条 P-17.1・裁定 id が要る）＋ 要件書 ＋ 判断の記録 ADR-16 の注 (d)・ADR-3 の注 | 3 |
| 整合 / 実態 | F-3 / F-4 | 直す | `srs` `acceptance.AC5.red_test.fixture` | 前「見本は design-intent/preview/retired/ へ退役済み」→ 後「見本は 2026-09-18 に design-intent/preview/retired/ へ退役し、その棚ごと 2026-09-23 に repo から消した（見本は git の履歴にだけ在る・総点検の 🔴 4・A-1 の承認）」。退役は PR #69（fc70f24）、消したのは PR #267（c7379ee）で実測。`git ls-files` に retired は 0 件 | B | 要件書の版上げ（受入の固定の材料の文・判定の式は不変） | 1 |
| 整合 | F-6 | 直す | `srs` `constraints.CON8.text`（と `adr` `ADR-16.context` (1)） | CON8 の末尾に「契約表を持つ設計ノートを利用者（scribe2）の repo の folio の置き場へ置く段は M3 で、他の設計文書の移行を問い直す時機は M3 の後である（判断の記録 ADR-16 決定 (1)・第 1.33 版で追随）」を足した（制約は注の欄を持たないので本文に 1 文）。前の括弧は消さずに「（他の設計文書の移行は M1 の後に改めて問う＝第 1.1 版の裁定の字・時機は末尾の 1 文が定める）」とし、時機を定める字を末尾の 1 文だけにした。ADR-16 の注 (e) に、文脈 (1) が ADR-3 決定 (8) に無い範囲の裁定を ADR-3 のものとして書いていることを断った。ADR-16 の文脈と決定の字は変えない | B | 要件書の版上げ（制約）＋ 判断の記録の注 | 4 |
| 整合 | F-7 | 直す | `adr` `ADR-3.decision`（決定 (9) の括弧） | 決定の字は変えず、ADR-3 の注の末尾に断りを 1 段（索引の中身の今の正本は要件 FR14 と索引の欄の決まりの正本・文書と契約は節点にしない・境界は同じ） | B | 判断の記録の注 | 4 |
| 整合 | F-8 | 直す | `adr` `ADR-6.consequences` | 帰結の字は変えず、ADR-6 の注の末尾に断りを 1 段（「配信の外」は「認証なしの公開ネットの外」の書き損じ・支度表の面は決定 (3) のとおり入口の面の節として配信先へ出る・文脈 (4) の字が正しい） | B | 判断の記録の注 | 4 |
| 整合 | F-9 | 直す | `adr` `ADR-9.context`（と案 a の理由） | 文脈と案の字は変えず、ADR-9 の注の末尾に断りを 1 段（「ADR-1 の帰結 4 つ目」は「5 つ目」の書き損じ・2026-09-20 の直し〔裁定 2026-09-21 00:00 JST〕は帰結の欄だけを正した） | B | 判断の記録の注 | 4 |
| 忠実さ | F-1 | **止める**（反証 支持） | `adr` `ADR-16.plain` | 前「…ページの見た目を直すこと、AI による読みやすさの検査は、この段に入れません。」→ 後 決定 (1) の並びに揃える（AI による意味の検査〔天井・4 つの観点のどれも〕・AI の手元へ憲法を写すこと・便が入ったかを印から読むこと・AI へ常時渡す説明文の量を数えること・scribe2 の側の仮の検査を外すこと）。除外の並びは 2 文に割った。反証の記録の指摘に沿って、範囲を閉じる文の肯定側「道具の検査に掛けられる」を決定 (1) と同じ広さの「道具の形の検査（床）に掛けて合格させ、scribe2 が読む形の写しをそこから作れる」にした（床だけに狭めると導出物が M3 の外と読めるので、導出物も入れた）。**決定・帰結・承認欄・見出しは 1 字も触っていない** | B | 判断の記録の平易文 ＋ 注 (a) | 4 |
| 忠実さ | F-2 | 直す | `srs` `requirements.FR7.plain` | 括弧の前「1 枚でも欠けたら 1 byte も書かないという書き出しの約束は、要件 FR5 の注が持ちます」→ 後「1 枚でも作れなければ 1 byte も書きません」＝取り違えた帰属（FR5 の注）だけを外した。面の導出の全部か無しかは、FR7 の注に事実として書いた（「面の導出は全部か無しかである（1 枚でも導出できなければ 1 byte も書かない・実装は crates/folio/src/site.rs・M3 の範囲を決めた判断の記録の決定 (5) が「配信の組み立ての全部か無しかは変えない」と決めている）」）。判断の記録を id（ADR-16）で名指さない理由は床の節に書いた。手順図と順序図の「全部か無しか」の名札はこの注を根拠に残した。規範文は不変 | B | 要件書の版上げ（平易文・注） | 1 |
| 忠実さ | F-3 | 直す | `srs` `figures.fig-plain`（refs と中身） | 手順図の矢印 e2 の名札「合格」→「合格・まだ分からない」、e3「不合格・まだ分からない」→「不合格」。図の caption と subtitle の「確かめられなければ止めて知らせる」を「決まりに反していれば止めて知らせ、確かめられなければ「まだ分からない」と知らせる」に＝FR5 の注（まだ分からない なら書いたうえでその終了コード）の字。節点「ページを作る」の小字を「全部か無しか」→「全部か無しか・分からなければ知らせる」にし、字が収まるよう幅を 140 → 160 にした（図の道具の読める字の大きさの検査が 140 では落とした）。帯・節点の並びは不変。要件書の面を 1440 の幅で開いて、名札が線にも節点にも重ならないことを目で確かめた | B | 要件書の版上げ（図） | 1 |
| 忠実さ | F-4 | 直す | `vocabulary` `field_terms.tier.def` | 前「確認してから変える（消す・外へ出す・外の道具や鍵を使う操作は実行前に持ち主へ聞く）」→ 後「確認してから変える（実行の前に持ち主の確認か承認を得る。消す・外へ出す・外の道具や鍵を使う操作のほか、条文の改訂と外部の依存の増減もこの段に入り、folio が folio 自身を変える運用は取り消せない操作のときだけ聞く。どの条がこの段かは憲法の各条の段の欄が持つ）」。条の id は写さない（条 P-6.3） | B | 要件書の版上げ（語彙を同梱） | 1 |
| 忠実さ | F-5 | 直す | `constitution` `articles.A-1.plain` | 前「…の 3 つは、道具が勝手にやらず、」→ 後「…の 3 つは、道具も、道具を作る側（AI の席や作業する人）も勝手にやらず、」＝縛る相手 both。**条文（id・見出し・規範文・段・縛る相手）は触らない**＝条 A-2.2 の改訂に当たらない（一括 10・11 で条の平易文を同じ承認で直した先例） | B | 憲法の平易文（改訂の範囲の外） | 5 |
| 読みやすさ | F-1 | 直す | `index` `shelf.documents.adr` | **便候補。** 入口の面の判断の記録のカードは記録を「ADR-n」の番号だけで並べる（`crates/folio/src/face_index.rs` の `adr_rows`・`Record` は題を読まない）。直す先は面の生成器で、入口の正本の字は動かさない（題の正本は判断の記録の側・P-6.3）。`docs/design/face-deliveries-plan.md` §13 に置いた | **C** | 面の生成器の便 | — |
| 読みやすさ | F-2 | 直す | `srs` `meta.promise` | 前「…入口・憲法・要件書の 3 枚が同じ見た目で生成され、機械検査の結果が必ず表示される。」→ 後「…設計文書の人が読むページがすべて 1 つの生成器から同じ見た目で生成され、機械検査（床）の結果と AI の審査（天井）の判定が必ず表示される。」（面の名札が「この文書が約束すること（1 文）」なので 1 文に畳んだ）。面の数は書かない（一括 13 の FR4 と同じ向き）。天井の名札は 5 面に出て、印が無くても「まだ分からない」を出すことを `crates/folio/src/face.rs` の `ceiling_stamp` で確かめた | B | 要件書の版上げ（表紙） | 1 |
| 読みやすさ | F-3 | 直す | `srs` `requirements.FR17.figures`（同じ形で `FR18.figures`） | FR17 の `[図1]` を `[]` に、FR18 の `[図1, 図3]` を `[図3]` に。図 1（誰が使い、何が出るか）の出る側は支度表・3 面・検査の結果だけで、天井を描いていない。図 1 に出力を足す案は問 1 の代替案 A | B | 要件書の版上げ（図の指し先） | 1 |
| 読みやすさ | F-4 | 直す | `constitution` `articles.N-6.title` | **直さない。** 見出しは条文の 5 欄の 1 つで、変えると条 A-2 の改訂（判断の記録・撤退条件・敵対的検証・持ち主の承認・凍結 anchor の版）になる。一括 12 が同じ理由で残した前文の順位の「walk」と束ねて 1 本の判断の記録で運ぶかを問 6 で問う。**要件書の図 fig-tech の caption と subtitle に同じ字が在ったので、そちらは本一括で直した**（問 1・条文ではない） | **A-2** | 条文の改訂 | 6 |
| 読みやすさ | F-5 | 直す | `adr` `ADR-16.title` | **見出しは変えない**（承認された決定の要約）。平易文の (1) を「憲法の写しの列の「いちばん最初の 1 枚」（見出しの「列の根」）の番号は」と「これを見出しの「憲法の名ごとの表」（利用者の repo ごとに、その repo の憲法が名乗る名前で 1 行を持つ表）に広げ」にし、見出しの 2 語にそれぞれ結び付けた。語彙に「列の根」の項を新設した（根は最初の 1 枚そのもの・道具はその要約値を持つ＝判断の記録 ADR-2 と ADR-16 の使い方） | B | 判断の記録の平易文 ＋ 語彙（要件書と同梱） | 4 |
| 読みやすさ | F-6 | 直す | `adr` `ADR-16.plain` | 「撃つ」3 か所を開いた。「撃った道具の版」→「動かした道具の版」／「「相談窓口の命令を撃て」と言うのに」→「相談窓口の命令を使うよう勧めるのに」／「自動の検査で道具を撃つようになってから」→「自動の検査が道具を動かすようになってから」。文脈・決定・反対側からの確認の本文の「撃つ」（面で 19 回）は発効した本文なので触らず、語彙に「撃つ（道具を）」の項を足して閉じた | B | 判断の記録の平易文 ＋ 語彙（要件書と同梱） | 4 |
| 実態 | F-1 | 直す | `srs` `requirements.FR11.note` | 前「台帳 f2-648.143 と判断の記録と設計ノートは同じ状態を指すだけである」→ 後「…の字はこの欄に従い、食い違えばこの欄を正とする」。台帳 f2-648.143 の題と本文は `bd --readonly show` で第 1.30 版の「保留・M3 で再判定」のまま（Updated 2026-09-22）と実測した。**台帳の側を直すのは席の手番**（承認の後に書く欄の 9） | B | 要件書の版上げ（注） | 1 |
| 実態 | F-3 | 直す | `adr` `ADR-13.note`（(h) の末尾の 1 文） | (h) の字は来歴として残し、末尾に「〔この 1 文は 2026-09-22 の時点の記述で、今の状態は (o)〕」を足した。新しい注 (o) に今の状態を書いた（FR13 は第 1.30 版で保留・M3 にも入れない・便の列の残り f2-648.145〜.148 に FR13 の便は無い・FR11 の便は本判断の列の外・(h) の 3 つのうち本判断の便の列が運んだのは FR14 で着地済み）。**決定・帰結・承認欄は触らない** | B | 判断の記録の注 | 4 |
| 実態 | F-5 | 直す | `constitution` `articles.P-13.mechanism`（note） | 前「書き込み先は呼び手が渡す出力先の指定だけだが」→ 後 書き込み先を 3 つに分けて書く（呼び手が渡す指定・そこから決まる固定の path・指定が無いときの既定〔folio hello の 1 回きりの印は利用者ごとの状態の置き場・folio inject は呼んだ場所の CLAUDE.md・図の道具と部品目録の導出の作業は一時の置き場〕）。v1 の path を既定に持つ口が無いことは、2026-09-24 に `crates/folio/src` の書き込みの口（`fs::write` などを持つ 13 file）を読んで確かめた。**歯で固定していない**の断りは残した | B | 憲法の機構の注（改訂の範囲の外） | 5 |
| 席の実測 | — | 直す相当 | `srs` `requirements.FR11.note`（同じ字で `FR13.note`・`adr` `ADR-16.context` (6)・`decision` (5)） | **天井の所見ではなく、席の実測（2026-09-24）。** 第 1.31 版の FR11 の注と ADR-16 の文脈 (6)・決定 (5) は「器の口 s2-07l.473 は 2026-09-22 に着地」と書くが、s2-07l.473（導出物の欄 goal の受け口）は 2026-09-22 に器の台帳で memo として閉じられ未着地（閉じた理由 = folio2 席の返答「保留・閉じてよい」）。器の今の読み手（scribe2 main 596989b の約束の行の読み）は欄 goal を未知の key として表ごと断る。FR11 の注の前「保留の条件だった器の口 s2-07l.473 は 2026-09-22 に着地し、M3 の入口の再判定で解いた。」→ 後「解除の前提とした器の口 s2-07l.473（導出物の欄 goal の受け口）は着地していない＝s2-07l.473 は 2026-09-22 に memo として閉じられ未着地（folio2 席の返答「保留・閉じてよい」）。器の読み手は導出物の欄 goal を未知の key として断る（scribe2 main 596989b・2026-09-24 実測）ので、判定点 ③（器が導出物を読む・ADR-16 決定 (1)）は器の受け口の着地が要る。s2-07l.473 は 2026-09-24 に器の台帳で再開された（契約 1 本・S 見込み）。s2-07l.473 の着地は判定点 ③ の前提（受け口）である。判定点 ③ の測定値は、器の受付が folio2 の導出物を読んで通した記録（利用者の台帳）で、在れば着地・無ければ まだ分からない（ADR-16 決定 (5)）。folio2 の側の口（独立の命令・便 119）はそれに依らず起こす（…ADR-16 の注 (f)）。」。一括 15 が起草した FR13 の注の「着地したが」も「着地していない（2026-09-22 に memo として閉じられた・要件 FR11 の注）」に。ADR-16 は決定・文脈・平易文の字を変えず注 (f) で誤りを記録。要件書の来歴と承認欄の v1.31 の行の同じ字は、その版の記録なので書き換えない | B | 要件書の版上げ（注）＋ 判断の記録の注 ＋ 保留の解除の扱い | 1・4・7 |

**参考の 4 件は直さない**（整合 F-4〔ADR-14 の帰結の「入口の棚の正本は 1 字も動かない」と入口 第 0.4 版〕・整合 F-5〔ADR-15 の注の「歯を含むか」の読み〕・忠実さ F-6〔FR18 の平易文の「本当に」〕・読みやすさ F-7〔範囲の章の「M0 では作らない」の入れ子〕）。

**所見の外で、同じ直しに含めたもの**（同じ形の残りを残さないため）。

- 行 R-16 の注の撤去の条件（「M1 の残りが揃った後に器へ要求する」→ ADR-3 決定 (7) の条件を判断の記録 ADR-16 決定 (6) が定め直した字）と、器の側の門の残りの日付（2026-09-20 → 2026-09-23・ADR-16 文脈 (7)）。要件 FR12 の注も同じ字にした。
- 判断の記録 ADR-3 の注に、決定 (7) の「器が同じ式を暫定で置き」の断り（整合 F-2 と同じ形・問 3）。
- 要件書の順序図 fig-tech の caption と subtitle の「同じ端末の中と tailnet の外への bind の拒否」（読みやすさ F-4 と同じ字・問 1）。
- 要件 FR7 の注に面の導出の全部か無しかを事実として書いた（忠実さ F-2 の直しで図の名札の根拠が要件書から消えないようにする・問 1）。
- 憲法の出所（sources）の 3 が 2 と同じ「記号の読み方」を 2 度持つ（25 周目 読みやすさ F-6・一括 13 が残した）。出所は改訂の範囲（`amendment_scope` = schema・precedence・articles）の外なので本一括で直した（問 5）。
- 語彙に「撃つ（道具を）」の項（判断の記録 ADR-16 の本文の「撃つ」を一度に閉じる・問 4）。
- 非機能要件 NFR1 の注（FR13 と同じ「M3 の入口で再判定」の形と「同じ状態を指すだけ」の形）。
- 判断の記録 ADR-16 の平易文の範囲を閉じる文（反証の記録の指摘）。
- 語彙の末尾の注釈の「後に足した語」の並びに、一括 14 が足した「口座・repo」が抜けていた。一括 15 の「列の根・撃つ」と一緒に足した（注釈は規則ではない）。

**まとめた後の問いの数 = 7。**（止める 1 件と直す 22 件を 6 つの問いにまとめ、改訂 f で席の実測 1 件〔直す相当・合わせて直す 23 相当〕の扱いを問 7 に足した。1 件は便候補、1 件は条文の改訂の問いで、どちらも本一括では正本を直さない。）

## 問どうしの依存

**退けた問の分は、要件書の来歴（`meta.status_note` の v1.33）と作成の行の stamp からも外す。** 来歴と作成の行は一括全部の要旨を持つので、下の一覧の「元に戻す」はこの 2 か所も含む。

- **要件書 第 1.33 版の中身は 問 1〜4 の答えで決まる。** 語彙は要件書と同じ承認で運ぶ（一括 11〜14 と同じ形）。問 3 の分（要件 FR12 の注・語彙の散文の門の項）と問 4 の分（語彙の列の根と撃つの項・語彙の末尾の注釈の「列の根・撃つ」・制約 CON8 の括弧と 1 文）も第 1.33 版に入っている。**退けた問の分は元の字に戻して発効する**（席が承認の後の commit で戻す）。
- **問 1 と 問 2 は要件 FR13 と非機能要件 NFR1 の同じ注の中の別の文である。** 問 1 は「再判定は済んだ・保留のまま」「台帳の字はこの欄に従う」の文、問 2 は「次に再判定するのは M3 の出口」の 1 文だけを問う。問 2 を退けたらその 1 文だけを外す（または持ち主が選んだ時機の字にする）。来歴と作成の行の「FR13 と NFR1 の次の再判定は M3 の出口」も同じく外す。
- **問 3 を退けたら、行 R-16 の注・行 R-16 の `ruling` の頭と `ruled_at`（前の裁定 2026-09-22 11:42 JST と `ruled_at: 2026-09-22` に）・要件 FR12 の注・語彙の散文の門の項・判断の記録 ADR-16 の注 (d)・判断の記録 ADR-3 の注の決定 (7) の断りの 1 文を元に戻す。** ほかの問は問 3 に依らない。
- **問 4 を退けたら、判断の記録 ADR-16 の平易文と注 (a)〜(c)・(e)・判断の記録 ADR-13 の (h) の断りと注 (o)・ADR-3 / ADR-6 / ADR-9 の注の断り・制約 CON8 の 1 文・語彙の列の根の項を元に戻す。** あわせて語彙の撃つの項と末尾の注釈の「列の根・撃つ」も戻す。問 4 の中の直しは一部だけ退けてもよいが、互いに独立ではない。item 5（ADR-16 の注 (a)〜(c)）は item 1〜3 の記録なので、1〜3 のどれかを退ければ対応する注も落とす。CON8 の括弧と 1 文と ADR-16 の注 (e) は同じ所見の 3 か所なので一緒に扱う。ADR-16 の注 (d) は同じ段落に在るが問 3 の分である。忠実さ F-1 は止めるの所見なので、item 1 を退けると次の周も不合格のままになる。
- **問 7 は問 1 と問 4 の中の同じ事実の直し（FR11 と FR13 の注・ADR-16 の注 (f)）に依る。** 事実の誤り（「着地」）の直しは問 7 の答えに依らず問 1 と問 4 で運ぶ。問 7 で案 A 以外を選んだら、FR11 の注の「folio2 の側の口（独立の命令・便 119）はそれに依らず起こす」の 1 句を答えの字に書き直す（案 B = 状態を保留に戻す・案 C = ADR-3 の改訂の記録を指す）。案 B は FR11 の状態の変更なので、要件書の版を第 1.34 版として別に上げる。
- **問 5 と 問 6 はほかの問に依らない。** 問 6 は正本を 1 字も動かさない（承認されても、動くのは次の判断の記録の起草から）。

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 読みやすさ F-1（入口の面の判断の記録のカードの題） | 直す先は面の生成器（実装）。正本は 1 字も動かない。便候補として `docs/design/face-deliveries-plan.md` §13 に置いた |
| 読みやすさ F-4（憲法 N-6 の見出し） | 見出しは条文の 5 欄の 1 つ＝条 A-2 の改訂。本一括では直さず、問 6 で改訂を起こすかを問う（要件書の図に在った同じ字は本一括で直した） |
| 参考の 4 件 | 参考は直さない（一括 14 と同じ扱い） |
| 生成区間の直し | **本周には無い**（23 件とも人が書く欄）。生成区間は手で直さない（条 P-6.2・判断の記録 ADR-9 / ADR-11）ので、在れば便候補に回すところだった |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.33 版の起草。** FR7 の平易文と注（忠実さ F-2）／制約 CON8 の括弧と末尾の 1 文（整合 F-6）／順序図 fig-tech の caption と subtitle（読みやすさ F-4 と同じ字）／FR11 の注（実態 F-1）／FR12 の注（整合 F-2）／FR13 の注と NFR1 の注（整合 F-1・実態 F-2）／FR17 と FR18 の図の指し先と注（読みやすさ F-3）／受入基準 AC5 の固定の材料と注（整合 F-3・実態 F-4）／手順図 fig-plain の caption・subtitle・矢印 2 本の名札・節点「ページを作る」の小字と幅（忠実さ F-3）／`meta.promise`（読みやすさ F-2）／`meta.version` を v1.33 に・`status_note` の要旨に「第 1.33 版は起草・承認待ち」と来歴の末尾に v1.33 の起草・`approval` に 作成 の行（who = orchestrator 席（AI・fable 5.1））。**`effective_version` は v1.32 のまま** |
| `design-intent/vocabulary.yaml` | 4 項。段（忠実さ F-4）／散文の門（整合 F-2）／**列の根の項を新設**（読みやすさ F-5）／**撃つ（道具を）の項を新設**（読みやすさ F-6 の本文の側）。末尾の注釈に一括 14 と一括 15 の足した語。**要件書と同じ承認で運ぶ** |
| `design-intent/rules.yaml` | **行 R-16 の 1 行。** 注の「同じ式」と揃える向きと撤去の条件（整合 F-2）・`ruling` を「一括 15 の承認待ち」の形に（承認後に `ruled_at` も書き換える旨を欄に書いた）・`ruled_at` を 2026-09-24 に。値・種別・段・母集団は不変 |
| `design-intent/adr/ADR-16.yaml` | `plain` の 6 か所（範囲を閉じる文・除外の並びを 2 文に・「撃った」・(1) の 2 行・「撃て」・「撃つ」＝忠実さ F-1・読みやすさ F-5 / F-6）・`note` に【天井の 27 周目の所見への応答】(a)〜(e)。**見出し・決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-13.yaml` | `note` の (h) の末尾に断り・【天井の 27 周目の所見への応答】(o)（実態 F-3）。**決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-3.yaml` | `note` の末尾に【天井の 27 周目の所見への応答】1 段（整合 F-7 と、決定 (7) の「同じ式」の断り＝問 3）。**決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-6.yaml` | `note` の末尾に【天井の 27 周目の所見への応答】1 段（整合 F-8）。**決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-9.yaml` | `note` の末尾に【天井の 27 周目の所見への応答】1 段（整合 F-9）。**文脈・案・決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/constitution.yaml` | 条 A-1 の平易文（忠実さ F-5）・条 P-13 の機構の注（実態 F-5）・出所 3 の「記号の読み方」の重複（25 周目 読みやすさ F-6）。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触っていない**＝条 A-2.2 の改訂に当たらない（出所は改訂の範囲 `amendment_scope` の外で、凍結 anchor の写しにも入らない） |
| `docs/design/face-deliveries-plan.md` | §13 に一括 15 からの便候補 1 本（読みやすさ F-1） |
| `docs/design/batch15-triage.md` | **新設**（この file） |

**実装（`crates/folio/src`）・凍結 anchor（`tests/fixtures/`）・歯（`crates/folio/tests`）・生成区間は 1 byte も触っていない。** 天井の正本・入口の正本・索引の欄の決まりの正本も触っていない。

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.33 版の注・平易文・図・受入の材料・表紙と、語彙の段の項

**やりたいこと。** 要件書の次の直しと、語彙の「段」の項の直しを、同じ承認で発効する。**要件の規範文と受入基準の判定の式は 1 字も動かない。**

1. 要件 FR13 と非機能要件 NFR1 の注の状態の字を「M3 の入口の再判定は判断の記録 ADR-16 決定 (5) で済み、保留のまま残した」にする。FR11・FR13・NFR1 の注の「台帳と判断の記録と設計ノートは同じ状態を指すだけ」を「字はこの欄に従い、食い違えばこの欄を正とする」にする（整合 F-1・実態 F-1 / F-2）。
2. 受入基準 AC5 の固定の材料の見本の置き場を「退役し、その棚ごと 2026-09-23 に repo から消した」にする（整合 F-3・実態 F-4）。
3. 要件 FR7 の注に「面の導出は全部か無しかである（1 枚でも導出できなければ 1 byte も書かない・実装は crates/folio/src/site.rs・M3 の範囲を決めた判断の記録の決定 (5) が「配信の組み立ての全部か無しかは変えない」と決めている）」を事実として書く。平易文の括弧は、前「1 枚でも欠けたら 1 byte も書かないという書き出しの約束は、要件 FR5 の注が持ちます」→ 後「1 枚でも作れなければ 1 byte も書きません」＝取り違えた帰属だけを外す。手順図と順序図の「全部か無しか」の名札はこの注を根拠に残す（忠実さ F-2）。
4. 手順図 fig-plain で「まだ分からない」をページを作る側へ付け替え、caption と subtitle を同じ字にし、節点「ページを作る」の小字を「全部か無しか・分からなければ知らせる」にする（忠実さ F-3）。
5. 語彙の段の項の「確認してから変える」の言い換えを、4 本の条（消す・出す・使う／条文の改訂／外部の依存／自分自身の運用）を覆う字にする（忠実さ F-4）。
6. 表紙の約束の 1 文を、M0 の 3 枚に閉じない字にする（読みやすさ F-2）。
7. 要件 FR17 と FR18 の図の指し先から図 1 を外す（読みやすさ F-3）。
8. 順序図 fig-tech の caption と subtitle の「同じ端末の中と tailnet の外への bind の拒否」を「同じ端末の中と tailnet の中より外への bind の拒否」にする（読みやすさ F-4 と同じ字の残り・図の字は条文ではない）。

**理由。** 所見 10 件（8 つの直しにまとめ、うち 1 つは同じ字の残り）とも「着地や再判定が済んだのに字が古い」か「平易文・図・言い換えが根拠の字と合わない」形である。1 は、判断の記録 ADR-16 が 2026-09-23 に済ませた再判定を、要件書の注がまだ先の出来事として書いている。台帳 f2-648.143 と .144 は題と本文が第 1.30 版のままで、注の「同じ状態を指すだけ」が実態と合わない。状態の正本がこの欄 1 か所であることは第 1.30 版で決めてあるので、台帳の側が遅れたときの読み方を字にした。3 と 4 は、FR5 の注が「まだ分からない なら書いたうえでその終了コード」と書くのに、平易文と図が「止める」側へ入れていた。3 で面の全部か無しかを平易文から消さずに FR7 の注で持つのは、性質そのものは実装に在り（判断の記録 ADR-16 決定 (5) が「配信の組み立ての全部か無しか（条 P-4.1 の実装）は変えない」と書く）、足りなかったのは要件書の側の持ち主の字だけだからである＝平易文・図・注が同じ事実を指す。8 は、問 6 で問う憲法 N-6 の見出しと同じ読み違い（同じ端末の中への bind も拒むと読める）が要件書の図に在ったもので、図の字は条文ではないので本一括で直せる。

**代替案。**
- **(A) 読みやすさ F-3 を、図 1 に天井の出力を足して直す。** 要件書の `outputs` に「天井の所見と判定の印（FR17）」の行を足すと図 1 の出る側に出る（帯の上限 4 に収まる）。ただし M1 と M2 の出力（設計ノートの面・判断の記録の面・導出物）も図 1 に無く、天井だけを足すと図 1 が半分だけ新しくなる。FR9〜FR16 と FR19〜FR21 は図の指し先を持たない（`[]`）ので、外す方が同じ形に揃う。
- **(C) 忠実さ F-2 を、平易文と図 2 枚から「全部か無しか」を消して直す（起草の元の案）。** FR5 の注にも規範文にも無い性質を要件書から外す向き。ただし性質は実装に在り判断の記録 ADR-16 決定 (5) が「変えない」と書くので、要件書の側の字が欠ける。推奨は注で事実として持つ向き（①）。
- **(B) 実態 F-1 を、注は触らず台帳の題と本文を直すことで閉じる。** 台帳の側が追随すれば「同じ状態を指すだけ」は今は真になる。ただし次に状態が動いたとき台帳が遅れると同じ所見がまた出る。本一括は注の字を改め、台帳の追随も席の手番として行う（両方）。

**コスト。** 承認 1 回（問 2〜4 と同じ版で運ぶ）。動く欄は 注 7（FR7・FR11・FR13・NFR1・FR17・FR18・AC5。FR13 と NFR1 の注には問 2 の 1 文も入る）・平易文 1・手順図の矢印の名札 2 と caption・subtitle 2 と節点の小字と幅 1・順序図の caption・subtitle 2・受入の固定の材料の文 1・表紙 1・図の指し先 2・語彙の定義 1。**要件の番号は 1 つも動かず（条 P-7.1）、床の歯も 1 本も直さない**（枝の HEAD で 779 本すべて通る）。

**推奨 = 8 つの直しをまとめて発効する（代替案は採らない）。** 義務を増やさず減らさず、字を根拠と実態に合わせるだけである。

### 🔴 問 2. 要件 FR13 と非機能要件 NFR1 の保留を、次に再判定する時機

**やりたいこと。** 2 つの注に次の 1 文を足す。

- 後 —「次に再判定するのは M3 の出口（ADR-16 決定 (1) の判定点 ①〜③ が揃ったとき）である。」（NFR1 は括弧なしの同じ文）

**理由。** 判断の記録 ADR-16 決定 (5) は 2 本を「M3 に入れず、保留のまま残す」と決めたが、次にいつ問い直すかは書いていない。第 1.30 版の注は「M3 の入口で再判定」と書いていたので、その時機が過ぎた今、保留を解く条件がどの文書からも読めない（整合 F-1 が指した所）。時機を書かないと、保留が「いつまでも保留」と区別できなくなる。M3 の出口にするのは、FR13 の口が要件 FR11 の導出物の便（ADR-16 決定 (7) の ⑤・M3 の中）の後でしか起こせず、その便が着地するのが M3 の出口の手前だからである。NFR1 は量を数える口が無いことが保留の理由で FR11 に依らないが、M3 の出口が席から問い直す最初の区切りなので同じ時機にそろえる。

**代替案。**
- **(A) 時機でなく条件で書く。** FR13 は「FR11 の導出物の便が着地したとき」、NFR1 は「量を数える口の便を持ち主が起こすと決めたとき」。FR13 は正確だが、NFR1 は持ち主の裁定待ちという字になり、席から問い直す契機が無い。
- **(B) 時機を書かず「持ち主が問うまで保留」とする。** 書くことは最も少ないが、保留を解く条件が読めないという所見はそのまま残り、次の周の整合が同じ所を拾う見込みが高い。

**コスト。** 承認 1 回（問 1 と同じ版）。注の 1 文ずつ。M3 の出口で席が 2 本を持ち主に問う手番が 1 つ増える。

**推奨 = M3 の出口で再判定する（代替案は採らない）。**

### 🔴 問 3. 規則の表の行 R-16 の注の「器の暫定の門と同じ式」を、実測の字に直す

**この問いは「規則の表の行を変えてよいか」の裁定である。** 行の変更には裁定の id と時刻を行に付けることが条 P-17.1 で要る。承認をもらったら、席が行 R-16 の `ruling` の欄に逐語と時刻を書く。

**やりたいこと。** 判断の記録 ADR-16 決定 (6) が参照 id の形を読む便に割り当てた直し（行 R-16 の注の「同じ式」）を先に運ぶ＝決定の時機を前倒しする。行 R-16 の注の 2 か所を直し、同じ字を持つ 3 か所を揃える。**値（印・禁止・単位）・種別・段・母集団は 1 つも動かない。**

1. 行 R-16 の注 — 前「scribe2 の暫定床（xtask の散文の門・印 5・直後の文字 5・単位 13 の宣言順まで同じ）と同じ式（ADR-3 決定 (7)）。」→ 後「scribe2 の暫定床（xtask の散文の門・ADR-3 決定 (7)）とは、印 5・「禁止」の直後の文字 5・単位 13（宣言の順まで）と、母集団から外すものと、文の区切りが同じで、式には 3 つの違いが在る＝参照 id として数える形・数と単位の読み方（英字の単位の直後の複数形の字と全角の空白）・参照 id の解決の有無（判断の記録 ADR-16 文脈 (7) の実測・2026-09-23）。揃える向き（揃えるのは器の側・folio2 の読みと今の値は動かない）と、参照 id の形をこの行の値に持たせる直しは ADR-16 決定 (6) の便が運ぶ。」
2. 同じ注の撤去の条件 — 前「folio2 の M1 が引き取ったら scribe2 側を撤去する（引き取りは便 24 で済んだ〔2026-09-18〕が器の側の式は 2026-09-20 時点でも残っており、撤去は M1 の残りが揃った後に器へ要求する）。」→ 後「撤去の条件は、ADR-3 決定 (7) の「folio2 が引き取ったら」を ADR-16 決定 (6) が定め直した（契約表を持つ利用者の設計文書が全部移り、利用者の自動の検査が folio の床を動かすようになったとき）。引き取りは便 24 で済んだ〔2026-09-18〕が、器の側の門は 2026-09-23 時点でも残っている。」
3. 要件 FR12 の注 — 前「M1 まで器（scribe2）が同じ式を暫定で持ち、M1 が引き取ったら撤去する。folio2 の引き取りは便 24 で済んだ（2026-09-18）が、器の側の暫定の式は 2026-09-20 時点でも残っている。」→ 後「器（scribe2）が暫定の門を持つ。撤去の条件は、ADR-3 決定 (7) の「folio2 が引き取ったら」を判断の記録 ADR-16 決定 (6) が定め直した。folio2 の引き取りは便 24 で済んだ（2026-09-18）が、器の側の暫定の門は 2026-09-23 時点でも残っている。器の暫定の門とこの要件の床は、印と単位の一覧は同じだが同じ式ではない（違いが 3 つ在る）。違いの中身と、」（以下は前の字のまま「撤去をいつ器へ要求するかの正本は規則の表の行 R-16 の注で…」に続く）。
4. 語彙の散文の門の項 — 前「folio2 の M1 で実在し、それまで器（scribe2）が同じ式を暫定で持つ（ADR-3）。」→ 後「folio2 の M1 で実在する。器（scribe2）も印と単位の一覧が同じ暫定の門を持つが、式は同じではない（違いの正本は規則の表の行 R-16 の注・ADR-3）。」
5. 判断の記録 ADR-3 の注 — 決定 (7) の「器（scribe2）が同じ式…を自分の検査に暫定で置き」は、ADR-16 文脈 (7) の実測では同じ式ではなく 3 つの違いが在ること・違いの正本は行 R-16 の注・撤去の条件は ADR-16 決定 (6) が定め直したことを 1 文で断る（決定の字は変えない）。

判断の記録 ADR-16 の注 (d) に、決定 (6) が「参照 id の形を読む便で直す」とした注の字を一括 15 が先に直したこと（時機の前倒し）と、その便に残る分（行の値に参照 id の形の欄を足す直し・要件 FR12 の規範文の直し・揃える向きと 12 例の fixture を契約に書くことなど）を書いた。

**理由。** 判断の記録 ADR-16 は 2026-09-23 に「器の暫定の門と行 R-16 は 3 つ違う」と実測し、決定 (6) で「行 R-16 の注の「同じ式」は合わない」と認めた（整合 F-2）。ただし直しは参照 id の形を読む便へ送った。その便は M3 の数に入らず、器の暫定の門の撤去の前提として後に置かれているので、それまで規則の表の行が判断の記録の実測と逆の事実を持ち続け、周のたびに同じ所見が出る見込みが高い。撤去の条件も同じ注が古い字のまま持っていた（所見の外・同じ形）。

**代替案。**
- **(A) 決定 (6) のとおり、参照 id の形を読む便まで待つ。** 判断の記録の字どおりで、行の変更は 1 回で済む。その間、行の注と語彙と FR12 の注の 3 か所が実測と逆の字を持つ。
- **(B) 行は触らず、要件 FR12 の注と語彙だけを直す。** 裁定 id は要らないが、FR12 の注が撤去の時機の正本と名指す行 R-16 の注が古いままで、同じ集合の記述が割れる。

**コスト。** 行 1 つの字（裁定 id と時刻が要る）・要件の注 1・語彙の定義 1・判断の記録の注 1 段落（ADR-16 の (d)）と 1 文（ADR-3）。歯は動かない（行 R-16 の前の裁定の数を pin する歯は無い・`crates/folio/tests/face_constitution.rs` が pin するのは行 R-1・R-2・D-1・D-3 だけ）。参照 id の形を読む便は、決定 (6) が割り当てた分のうち注の字の直しを除いたものを運ぶことになる。

**推奨 = 字だけを先に直す（代替案は採らない）。** 振る舞いは変えず、事実と逆の字を残す期間を無くす。

### 🔴 問 4. 判断の記録 5 本の平易文と注（ADR-16・ADR-13・ADR-3・ADR-6・ADR-9）、制約 CON8、語彙の列の根の項

**やりたいこと。** 次を直す。**判断の記録の見出し・文脈・決定・案・帰結・承認欄は 1 字も触らない**（発効済みの判断の記録の食い違いは注で断る＝一括 13・14 の ADR-13 と同じ扱い）。

1. ADR-16 の平易文の「この段に入れません」の並びを決定 (1) に揃える（忠実さ F-1・**止める**・反証 支持）。
   - 前 —「scribe2 の憲法・判断の記録・調べものの文書を移すこと、人が読むページを作ること、ページの見た目を直すこと、AI による読みやすさの検査は、この段に入れません。」
   - 後の並びは 2 文に割る —「scribe2 の憲法・判断の記録・調べものの文書を移すこと、人が読むページを作って見せること、ページの見た目を直すことは、この段に入れません。」と「AI による意味の検査（天井・4 つの観点のどれも）、AI の手元へ憲法を写すこと、便が入ったかを印から読むこと、AI へ常時渡す説明文の量を数えること、scribe2 の側の仮の検査を外すことも、この段に入れません。」
   - あわせて範囲を閉じる文 —「道具の検査に掛けられるようにする。これだけです。」→「道具の形の検査（床）に掛けて合格させ、scribe2 が読む形の写しをそこから作れるようにする。これだけです。」（決定 (1) の「床に掛けて合格まで届き、器が読む導出物をそこから作れる」と同じ広さ）
2. ADR-16 の平易文の「撃つ」3 か所を「動かす」「勧める」の字にし、本文の側の「撃つ」は語彙に「撃つ（道具を）」の項を足して閉じる（読みやすさ F-6）。
3. ADR-16 の平易文の (1) を見出しの「列の根」「憲法の名ごとの表」にそれぞれ結び付け（「「いちばん最初の 1 枚」（見出しの「列の根」）の番号」・「見出しの「憲法の名ごとの表」（利用者の repo ごとに、その repo の憲法が名乗る名前で 1 行を持つ表）」）、語彙に「列の根」の項を足す（読みやすさ F-5）。
4. ADR-13 の注 (h) の末尾に「〔この 1 文は 2026-09-22 の時点の記述で、今の状態は (o)〕」を足し、注 (o) に今の状態を書く（実態 F-3）。
5. ADR-16 の注 (a)〜(c) に 1〜3 の記録を書く（1〜3 のどれかを退けたら対応する注も落とす）。
6. 制約 CON8 の括弧を「（他の設計文書の移行は M1 の後に改めて問う＝第 1.1 版の裁定の字・時機は末尾の 1 文が定める）」に改め、末尾に 1 文を足し、ADR-16 の注 (e) で文脈 (1) の出所の取り違えを断る（整合 F-6）。
   - 足す文 —「契約表を持つ設計ノートを利用者（scribe2）の repo の folio の置き場へ置く段は M3 で、他の設計文書の移行を問い直す時機は M3 の後である（判断の記録 ADR-16 決定 (1)・第 1.33 版で追随）。」
   - 注 (e) — 文脈 (1) の「ADR-3 決定 (8) と要件書 CON8 が…と裁定済み」のうち、範囲を裁定したのは CON8 だけ（ADR-3 決定 (8) は順序を決め、範囲は別の論点として先送りした）。
7. ADR-3・ADR-6・ADR-9 の注の末尾に【天井の 27 周目の所見への応答】を 1 段ずつ足す。
   - ADR-3（整合 F-7）— 決定 (9) の括弧の索引の中身（文書・要件・契約の id と 1 行の題）は 2026-09-16 の時点の字で、今の中身は要件 FR14（第 1.28 版）と索引の欄の決まりの正本の節点の種類が持つ（文書と契約は節点にしない）。境界（id の索引を出すまで）は同じ。
   - ADR-6（整合 F-8）— 帰結の「支度表は…配信の外（N-6.2）」の「配信の外」は「認証なしの公開ネットの外」の書き損じとして読む。支度表の面は決定 (3) のとおり入口の面の節として配信先へ出る。
   - ADR-9（整合 F-9）— 文脈と案 a の理由の「ADR-1 の帰結 4 つ目」は「5 つ目」の書き損じとして読む（2026-09-20 の直し〔裁定 2026-09-21 00:00 JST〕は帰結の欄の番号だけを正した）。

**理由。** 1 は、持ち主が承認の根拠に読む平易文が、M3 の範囲（決定 (1)）を変えて読める。決定 (1) は天井を丸ごと M3 の外に置くのに、平易文は「読みやすさの検査」の 1 観点だけを名指すので、残る 3 観点は M3 に入ると読める。肯定側が「床」より広い「道具の検査」なので、「これだけです」も反対の読みを塞がない（反証の記録）。ただし肯定側を床だけに狭めると、決定 (1) の「器が読む導出物をそこから作れる」が M3 の外と読めるので、導出物も同じ文に入れた。2 と 3 は、非エンジニアあての平易文と見出しに、用語集に無い語が言い換えなしで出ている。4 は、注 (h) が今は保留の FR13 を本判断の便の列が運ぶと書いている。6 は、ADR-16 決定 (1) が「CON8 のとおり M3 の後に改めて問う」と CON8 を名指すのに、CON8 の字は「M1 の後」で、ADR-16 の文脈 (1) は ADR-3 決定 (8) に無い裁定を ADR-3 のものとして書いている（両立はするが指す中身が違う）。7 の 3 つは、発効済みの判断の記録の字が、後の要件・同じ記録の決定・相手の判断の記録の番号と食い違っている。

**代替案。**
- **(A) ADR-16 の見出しを平易文の言い方に直す。** 「列の根」を「憲法の写しの列の最初の 1 枚の番号」に開く。見出しは持ち主が 2026-09-23 に承認した決定の要約で、字を変えると承認した物と違う物になる。語彙の項は ADR-2 と ADR-16 の本文の「列の根」にも効く。
- **(C) 整合 F-6 を、CON8 を触らずに ADR-16 の注だけで断る。** 要件書が動かないので承認の範囲は小さいが、CON8 を読む人は「M1 の後」しか見えず、次の周が同じ所を拾う見込みが残る。CON8 は要件書の制約で、ADR-16 の決定はもう承認されているので、CON8 を決定に追随させる方が正本の向きに合う。
- **(D) 整合 F-7 を、ADR-13 の改訂の欄（revises）に ADR-3 決定 (9) を足して記帳する。** 発効済みの ADR-13 の型付きの欄を後から足すことになり、改訂の欄の意味（その判断が発効で狭めた・広げた相手）を後付けで作る。注で断る方が一括 13・14 の扱いと揃う。
- **(B) ADR-13 の注 (h) を今の状態の字に書き換える。** 読み手が迷わないのは同じだが、2026-09-22 の時点で何と書いたかの来歴が消える。本一括は (h) を残して断りを添えた。

**コスト。** 承認 1 回。平易文の 6 か所（範囲を閉じる文・除外の並び・「撃った」・(1) の 2 行・「撃て」・「撃つ」）・注 5 段落（ADR-16・ADR-13・ADR-3・ADR-6・ADR-9 に 1 段落ずつ・ADR-16 の (e) は同じ段落の中）と ADR-13 の (h) の末尾の断りの 1 句・制約の括弧と 1 文・語彙の項 2。**義務は 1 つも増えない。** 判断の記録の状態（accepted）は変わらない。

**推奨 = 7 つとも直す（代替案は採らない）。** 止める 1 件は平易文の直しで閉じる。決定の本文を改める要は無い。

### 🔴 問 5. 憲法の条 A-1 の平易文と、条 P-13 の機構の注と、出所の重複

**やりたいこと。** 3 か所を直す。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触らない＝条 A-2.2 の改訂に当たらず、憲法の版は上がらない。**

1. 条 A-1 の平易文 — 前「…の 3 つは、道具が勝手にやらず、決まった対話の場所であなたに確認してからにします。」→ 後「…の 3 つは、道具も、道具を作る側（AI の席や作業する人）も勝手にやらず、決まった対話の場所であなたに確認してからにします。」（忠実さ F-5）
2. 条 P-13 の機構の注 — 前「書き込み先は呼び手が渡す出力先の指定だけだが、そのことを歯で固定してはいない（2026-09-20 時点）」→ 後「書き込み先は、呼び手が渡す指定（出力先・置き場・印の置き場・CLAUDE.md の path）と、そこから決まる固定の path（例 = 置き場の下の天井の印 preview/ceiling-stamp.yaml・凍結 anchor の置き場）と、指定が無いときの既定（folio hello の 1 回きりの印は利用者ごとの状態の置き場・folio inject は呼んだ場所の CLAUDE.md・図の道具と部品目録の導出の作業は一時の置き場）で決まり、v1 の path を既定に持つ口は無い（2026-09-24 に crates/folio/src の書き込みの口を読んで確かめた・天井の 27 周目 実態 F-5）。ただしそのことを歯で固定してはいない」（実態 F-5）
3. 出所 3 の「記号の読み方」— 前「…／ C14（P-5.5・G2=C の二層の写し）。記号の読み方: C-n / N-n = 前の版（scribe2）の憲法の条の番号・G-n / P-n = 2026-09-12 の裁定の番号（判断の記録 ADR-1 の出所）・「G-n=X」の「=X」はその裁定で選ばれた案の記号」→ 後「…／ C14（P-5.5・G2=C の二層の写し）。記号の読み方は出所 2 のとおり」（25 周目 読みやすさ F-6・一括 13 が残した）

**理由。** 1 は、条 A-1 の縛る相手は both（道具と作る側の作法）で、規範文 A-1.1 は操作する者を問わない。平易文が主語を「道具」に絞ると、AI の席が同じ確認を要ることが読めない。2 は、`folio hello` が `--state` を渡されないと既定の置き場へ書き、`folio ceiling --stamp` が置き場の下の固定の path へ書くので、「呼び手が渡す出力先の指定だけ」は実装より狭い。3 は、出所 2 と 3 が同じ一文を 2 度持つ。一括 13 は出所の直しに条 A-2 の手順が要るとして残したが、出所は改訂の範囲（`amendment_scope` = schema・precedence・articles）の外で、凍結 anchor の写し（`crates/folio/src/anchor.rs` の `project`）にも入らない＝一括 13 の前提は誤りで、条 A-1 の平易文と同じく改訂の範囲の外として直せる。

**代替案。**
- **(A) 条 P-13 の機構の注の直しを、書き込み先を数える歯の便と一緒に運ぶ。** 歯が入れば「まだ分からない」の断りも外せる。ただしその便は起票されておらず、それまで注が実装より狭い字を持つ。

**コスト。** 承認 1 回。平易文の 1 句・機構の注の 1 文・出所の 1 文。`folio inject --check` の生成区間（前文と規範文）は動かない（枝の HEAD で 68 行 / 7,912 byte 一致）。

**推奨 = 3 か所とも直す（代替案は採らない）。**

### 🔴 問 6. 憲法 N-6 の見出しを直す改訂を起こすか（本一括では直さない）

**やりたいこと。** 本一括では何も直さない。持ち主に問うのは「憲法の字の直しを 1 本の判断の記録で運ぶ改訂を、一括 15 の後に起こしてよいか」である。束ねるのは次の 2 つ。

1. 条 N-6 の見出し（27 周目 読みやすさ F-4）— 前「見せる先を同じ端末の中と tailnet の外へ広げない」→ 例「見せる先を、同じ端末の中と tailnet の中より外へ広げない」。
2. 前文の順位（`precedence`）の根拠の行の「walk」（24 周目 読みやすさ F-1・一括 12 が残した）。

**理由。** 見出しの「同じ端末の中と tailnet の外」は、「端末の中」と「tailnet の外」を並べた句にも読め、そう読むと「同じ端末の中へも広げない」になって、loopback を許す平易文と逆になる。平易文を読めば解けるが、見出しは面の目次と棚にも出る。見出しは条文の 5 欄の 1 つなので、直すには条 A-2 の手順（判断の記録・撤退条件・敵対的検証〔条 A-2.3〕・持ち主の承認・凍結 anchor の版）が要る。一括 12 が同じ理由で前文の順位の「walk」を残しており（`precedence` は改訂の範囲の中）、1 本ずつ起こすと手順が 2 回になる。一括 13 が一緒に残した出所の重複は改訂の範囲の外だったので、問 5 で本一括で直した。要件書の図 fig-tech に在った同じ字は一括 15 で揃えた（問 1）。発効済みの判断の記録 ADR-12 の決定の本文にも同じ字（「同じ端末の中と tailnet の外」）が在り、決定の本文は変えないので、注で断るかどうかをこの束の判断の記録で一緒に決める。

**代替案。**
- **(A) 起こさずに、ためておく。** 平易文が意味を解くので実害は小さい。次の周の読みやすさが同じ所をまた拾う見込みがある。
- **(B) N-6 の見出しだけを 1 本の判断の記録で直す。** 束ねるより早いが、手順が「walk」の分だけ後でまた要る。

**コスト。** 承認 1 回（起こしてよいかの裁定）。起こした後に、判断の記録の起草・敵対的検証・撤退条件・憲法 第 1.3 版の凍結・持ち主の承認がもう 1 回要る。本一括の正本は 1 字も動かない。

**推奨 = 一括 15 の後に、2 つを束ねた 1 本の判断の記録を起こす。**

### 🔴 問 7. 要件 FR11 の保留の解除（第 1.31 版）の前提が偽だった。どう扱うか

**やりたいこと。** 判断の記録 ADR-16 の版 A（要件書 第 1.31 版・2026-09-23 17:39 JST の承認）は、要件 FR11 の保留を「器の口 s2-07l.473 の着地（2026-09-22）で条件が満たされた」として解いた。その前提は偽だった（席の実測 2026-09-24）。s2-07l.473（導出物の欄 goal の受け口）は 2026-09-22 に器の台帳で memo として閉じられ未着地で、閉じた理由は folio2 席の返答「保留・閉じてよい」（導出物は M3 の入口で再判定・導出 file の形は未定）である。器の今の読み手（scribe2 main 596989b の約束の行の読み）は欄 goal を未知の key として表ごと断る。誤りの字の直し（FR11 と FR13 の注・ADR-16 の注 (f)）は問 1 と問 4 で運ぶ。この問いは、偽の前提で解いた保留をどう扱うかを問う。

**理由。** FR11 は 2 つの口でできている。folio2 の側の口（契約表から導出物を組み、版管理の導出物との差分 0 を数える独立の命令・便 119 = 案 folio derive --check）と、器の側の受け口（導出物を読む）である。前者は器の受け口に依らず作れて、差分 0 は folio2 の中で数えられる。器が導出物を読めるかは ADR-16 決定 (1) の判定点 ③ で、決定 (5) は判定点 ③ を「利用者の台帳の記録が在れば着地、無ければ「まだ分からない」の測定値」と定めている。したがって前提が偽でも、FR11 の folio2 の側の義務と判定点 ③ の測り方は崩れない。崩れるのは「器が今すぐ読める」という見込みだけで、M3 の出口（判定点 ③ が通る）の律速が器の受け口の着地に移る。

**器の側の返事（scribe2 orchestrator・2026-09-24 07:1x JST・session scribe2-df）。** s2-07l.473 は memo として再開された。契約 1 本（S 見込み）で、器の読み手（read_rows）が pointer の .toml の行の欄 goal を受け、審査の節の本文と契約 file の goal をそこから取る直しと歯を運ぶ。順は器の待ち行列 5 本と器の判断の記録 ADR-0059〜0062 の後で、着地の見込みは本日の窓の終わりから明日である。受け口の形は folio2 の推奨のまま。導出物（toml）への求めは 3 つ＝① 欄 goal の名は変えない、② 空の一覧の欄名の省略は受け口の側で任意の欄として受ける（folio2 の側の変更は要らない）、③ 先頭の版の宣言の key の名と値の形は器の drift の歯が pin するので、変えるときは先に知らせる。3 つとも判断の記録 ADR-3 決定 (4) の形のままで満たせる。案 A でも s2-07l.473 の着地は判定点 ③ の前提（受け口）である。判定点 ③ の測定値は、器の受付が folio2 の導出物を読んで通した記録（利用者の台帳）で、在れば着地・無ければ まだ分からない（ADR-16 決定 (5)）。

**案。**
- **(A) 保留の解除は維持し、folio2 の側の口（便 119）は進める（推奨）。** s2-07l.473 の着地は判定点 ③ の前提（受け口）である。判定点 ③ の測定値は、器の受付が folio2 の導出物を読んで通した記録（利用者の台帳）で、在れば着地・無ければ まだ分からない（ADR-16 決定 (5)）。器の受け口が着地するまでは、判定点 ③ は「まだ分からない」のままである。s2-07l.473 の再開は器の側が 2026-09-24 に済ませたので、席は再開の事実と器の台帳の id を f2-648 notes に記帳する。誤りは ADR-16 の注 (f) に記録する（問 4）。
- **(B) FR11 を保留に戻す。** 版 A の保留の解除を撤回する要件書 第 1.34 版を別に上げ、便 119 は park する。前提どおりの順（器の受け口 → folio2 の口）に戻せるが、folio2 の中で閉じて作れる口まで止まり、M3 の判定点 ② の便（ADR-16 決定 (7) の ⑤）が器の都合を待つ。保留の解除と撤回が 2 版続き、要件書の来歴が往復する。
- **(C) ADR-3 決定 (4) の導出物の形を、器の今の読み手に合わせて改める（欄 goal を落とす）。** 器の受け口を待たずに判定点 ③ が通る見込みが立つ。ただし ADR-3 決定 (4) は各行に節の本文の逐語（goal）を運ぶと決めた判断で、落とすと器の審査が契約の目的を導出物から読めなくなる。発効した決定の改訂なので、判断の記録の改訂（撤退条件・敵対的検証・持ち主の承認）が要り、器の側の合意も要る。

**コスト。**
- 案 A — 承認 1 回（ほかの問と同じ回）。席の手番 1 つ（scribe2 の planner への要望と記帳）。要件書の版は第 1.33 版の注の直しだけ。M3 の出口が器の受け口の着地を待つ見込みを、状態の 1 枚の未決に置く。
- 案 B — 承認 1 回に加え、第 1.34 版の起草と承認がもう 1 回。便 119 の契約（起こしていれば）の park。
- 案 C — 判断の記録の改訂 1 本（起草・敵対的検証・承認）と器の側の合意。導出物の形の凍結 anchor（ADR-3 決定 (4) の形の fixture）が動く見込み。

**推奨 = 案 A。** folio2 の中で閉じて作れる口は、器の受け口に依らず作れて、判定点 ③ は決定 (5) の測り方のまま「まだ分からない」で表に出せる（条 P-4.2）。偽の前提は注で記録し、器の受け口は要望として器の台帳へ渡す。

## 承認の後に席が書く欄

持ち主が答えた後に、席が同じ枝の別の commit で書く。退けた問の分は「問どうしの依存」のとおり元の字に戻す。

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.32 → v1.33 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の先頭の文 | 「いま発効しているのは 第 1.33 版（<承認の日付>）です。」 |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.33 | 「v1.33 = 2026-09-24 起草（…・承認待ち。…）」を「v1.33 = <日付> 発効（逐語「…」・一括 15 = …）」に |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 15〔問 1〜7〕の承認要求への回答）", verbatim: <逐語>, version: v1.33}`） |
| 5 | `design-intent/rules.yaml` | 行 R-16 の `ruling` の先頭と `ruled_at` | 「一括 15 の承認待ち（…）」を「f2-648 notes <時刻> JST（逐語「…」・一括 15 の問 3 = …）」に・`ruled_at` を承認の日付に |
| 6 | `design-intent/adr/ADR-16.yaml` | `note` の【天井の 27 周目の所見への応答】の見出し | 「2026-09-24 起草・持ち主の承認待ち」を「持ち主の承認 <日付 時刻> JST「<逐語>」」に |
| 7 | `design-intent/adr/ADR-13.yaml` | `note` の【天井の 27 周目の所見への応答】の見出し | 6 と同じ |
| 7-2 | `design-intent/adr/ADR-3.yaml`・`ADR-6.yaml`・`ADR-9.yaml` | `note` の末尾の【天井の 27 周目の所見への応答】の見出し（3 本とも） | 6 と同じ |
| 8 | `docs/design/batch15-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に |
| 9 | 台帳（`bd`） | f2-648.143 と f2-648.144 の題と本文・f2-648.93 の本文 | .143 = 第 1.31 版で保留の解除・便は ADR-16 決定 (7) の ⑤（便 119）・口の名は `folio build --check` でなく独立の命令・器の口 s2-07l.473 は 2026-09-22 に memo として閉じられ未着地で、2026-09-24 に再開された（契約 1 本・S 見込み）。s2-07l.473 の着地は判定点 ③ の前提（受け口）である。判定点 ③ の測定値は、器の受付が folio2 の導出物を読んで通した記録（利用者の台帳）で、在れば着地・無ければ まだ分からない（ADR-16 決定 (5)）。（問 7 の答えの字。案 B なら保留に戻す）。.144 = M3 に入れず保留・次の再判定は M3 の出口（問 2 の答え）。.93 = 撤去の条件は ADR-16 決定 (6)。f2-648 notes に承認の逐語 |
| 10 | `docs/design/state.md` | 要件書の版の行（16 行）・いま走っている PR の行（次の一手の 1）・次の一手の 6・未決の表の 1〜3 | 要件書 = 第 1.33 版（発効の日付と逐語・一括 15）／走っている PR = 一括 15 の後の手番／次の一手の 6 = 一括 15 → 天井の 28 周目／未決の 1 = 一括 15 を外す・2 と 3 = FR13 と NFR1 の次の再判定を問 2 の答えの字に（NFR1 の「席（M3 の入口で再判定）」を含む） |
| 11 | 台帳（`bd`） | 問 6 を承認したとき | 憲法の字の直しの束の判断の記録の起票 |
| 12 | 台帳（`bd`） | 歯 `f90_the_real_srs_carries_the_adrs_field`（欄 adrs の行の数 14 を pin）を性質で当てる形へ直し、FR7 の欄 adrs に ADR-16 を足して注を id の字に戻す席の PR | 台帳 f2-648.162（歯の負債）に追記済み。行 R-17 を言い換えで避けた 4 か所（FR7 の注・AC5 の固定の材料・ADR-6 の注・ADR-3 の注）の一覧も台帳に置き、順に id へ戻す |
| 13 | 器（scribe2）の planner と台帳 f2-648 notes | 問 7 を案 A で承認したとき | scribe2 の planner へ s2-07l.473 の再開（導出物の欄 goal の受け口）は要望済み（2026-09-24 07:1x JST 返信 = 再開・契約 S 見込み・f2-648 notes に記帳済み）。.473 の着地（判定点 ③ の前提）は f2-648 notes に記帳する。判定点 ③ の測定値は、器の受付が folio2 の導出物を読んで通した記録（利用者の台帳）で、在れば着地・無ければ まだ分からない（ADR-16 決定 (5)）。案 B なら第 1.34 版（FR11 を保留に戻す）の起草と便 119 の park、案 C なら ADR-3 決定 (4) の改訂の判断の記録の起票 |

語彙と憲法には承認欄の行が無い（語彙は要件書の承認で運び、憲法の平易文と機構の注の直しは一括 10・11・14 と同じく承認欄を持たない）。

## 床の結果（この枝の HEAD で実測・2026-09-24・main root の binary = 2c3a631 の build）

**改訂 f（2026-09-24 07:1x JST）の撃ち直し。** 枝の source から build した binary（worktree の `target/debug/folio`・src は 2c3a631 から不変）で、`folio check` 合格（違反 0・まだ分からない 0）・`folio schema --check` rc 0（9 file 一致）・`folio inject --check` 68 行 / 7,912 byte 一致。**main root の binary は main 0b0aafd の build に替わっており、それで撃つと `folio schema --check` が「srs.yaml: 生成区間 1174 byte ≠ 導出 1189 byte」で落ちる。** main の便 117（a34c6b2・要件書の最上位の節の閉じた一覧に scope_m3）が要件書の生成区間を 1 行動かしたためで、改訂 f の前の HEAD ab04c4f でも同じに落ちる（改訂 f の直しとは関係ない）。枝に main を取り込めば main の生成区間の 1 行が入る。取り込みは席の手番（PR の前）。`folio check` と `folio inject --check` は main root の binary でも通った。

**改訂 g（2026-09-24 07:1x JST）の撃ち直し。** 席が main を枝へ取り込んだ（4978a21）後の HEAD で、枝の source から build し直した binary と main root の binary（0b0aafd）の両方で、`folio check` 合格（違反 0・まだ分からない 0）・`folio schema --check` rc 0・`folio inject --check` 68 行 / 7,912 byte 一致。改訂 f の節の schema --check の落ちは取り込みで解けた。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | rc 0（9 file 一致） |
| `folio inject --check --dir design-intent` | 68 行 / 7,912 byte 一致 |
| `cargo nextest run --workspace --no-tests=fail --no-fail-fast` | **779 / 779 PASS** |
| `folio build --out <scratch> --write` | 床 合格・23 file 書けた。要件書の面（表紙の約束の 1 文・図 4 の手順図・図 5 の順序図の caption）と ADR-16 の面（平易文の範囲を閉じる文・2 文に割った除外・(1) の結び付け）を 1440 の幅で開いて目で確かめた（名札は線にも節点にも重ならない） |
| `folio ceiling --gate --write-set design-intent/srs.yaml` | まだ分からない（印が古い）＝main でも同じ |

**歯は 1 本も直していない。** 改訂 c で手順図の節点の小字を長くしたところ、図の道具の検査が 1 度目は「字が節点の幅に収まらない」、2 度目は「1440 の幅で読める字の大きさ（6px）を割る」で落ちた（どちらも build が「まだ分からない」を返した）。節点の幅を 160 にして通した。**改訂 c で FR7 の欄 adrs に ADR-16 を足したところ、歯 `crates/folio/tests/check.rs` の `f90_the_real_srs_carries_the_adrs_field`（887 行 `assert_eq!(rows.len(), 14, …)`＝実の要件書の欄 adrs の行の数を 14 で pin）が 15 ≠ 14 で落ちた。** 欄 adrs を足さず、FR7 の注では判断の記録を id で名指さない字（「M3 の範囲を決めた判断の記録の決定 (5)」）にして通した（id で名指すと行 R-17 が「FR7 と ADR-16 の型付きの欄に対が無い」で落とす）。**席の PR で歯を直す候補**＝この歯を「数でなく性質で当てる形」へ移せば、FR7 の注を id で名指す字に戻せる（一括 14 の床の節が挙げた `f79_*` の 2 本と同じ系統）。1 度目の床で、受入基準 AC5 の固定の材料に「要件 NFR2 の注」と書いたところ、床の行 R-17 が「AC5 の散文が NFR2 を指すのに、どちらの型付きの欄にも無い」で落ちた。AC5 の検証の対象を増やすことになるので欄には足さず、字を「総点検の 🔴 4・A-1 の承認」に替えて通した。

## まだ分からない点

**0 件。** 起草のときの 3 点（忠実さ F-1 の反証・整合の補いの所見・手順図の見た目）は、席の知らせ 2 通と、独立の検証の SVG の実測と、改訂 c の後に面を開いた目視で閉じた。持ち主の walk はこの一括の承認の範囲の外である。
