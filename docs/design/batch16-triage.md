# 一括 16 の仕分け — 天井の 28 周目の所見（直す 25 件・参考 5 件）

**状態: 承認済み・発効（持ち主の承認 2026-09-24 11:51 JST・対話面 R-8・逐語「全部推奨で」・裁定 id = f2-648 notes 2026-09-24 11:51 JST・一括 16〔問 1〜5〕を含む 8 問の承認要求への回答・問 1〜5 とも推奨どおり・要件書 第 1.35 版が発効・承認の記帳は席が同じ枝の別の commit で正本の側へ書いた〔承認の後の欄の 1〜8〕）。来歴（2026-09-24 10:3x JST 起草〔最初の 3 commit・印の PR #293 の着地の後の main a70477e の上〕・10:49 JST に main 8066e4c へ載せ替え・10:5x JST に改訂 b〔独立の検証 `~/.local/share/folio2/handoff-2026-09-24/batch16-verify.md` の blocking 2・文面 11・低い重さ 2 と、席の裁定 3 点〈着地の順・C を f2-648.171 に束ねる・便 125 の引用は発効の後に便 125 の改訂で直す〉を当て、床を撃ち直した〕。時刻は commit の時刻〔`TZ=Asia/Tokyo git log --format=%cd`〕で取った・承認の後に main 628c547〔便 ②・③・123 の着地の後〕へ載せ替えた）。** 下の 問 1〜5 が持ち主への承認要求だった。承認の記帳（要件書の承認欄と `effective_version` と `status_note`・判断の記録 ADR-16 と ADR-2 の注の見出し・状態の 1 枚と台帳）は、持ち主が答えた後に席が同じ枝の別の commit で正本の側へ書く（書く欄の全部は §「承認の後に席が書く欄」）。持ち主の逐語は、承認の後の記帳の commit で初めて正本とこの文書に入れた。

起草 2026-09-24・orchestrator 席（起草は opus 5.5 の起草役に委ねた。正本の作成の行は席を責任者として「orchestrator 席（AI・fable 5.1）」と書く＝要件書 第 1.33 版・第 1.34 版の作成の行と同じ）・作業場所 `.worktrees/planner-batch16`（branch `docs/batch16`・`origin/main` 2d90e8d から切り、印の PR #293〔a70477e〕の着地の後と、便 121 の契約の PR #296〔8066e4c〕の着地の後に載せ替えた）。

材料は天井の 28 周目の所見である（周 = `~/.local/share/folio2/ceiling/2026-09-24-round28/`・観点 4・束は main 2d90e8d で組んだ・正は各観点の `2026-09-24-round28/<観点>/findings.yaml`）。判定は 4 観点とも **合格**（止める 0）。`findings-all.txt` の 30 行は各観点の `findings.yaml` と id・重さ・場所が一致する。所見の数は `findings.yaml` から数え直した。

| 観点 | 止める | 直す | 参考 |
|---|---|---|---|
| 整合 | 0 | 7 | 1 |
| 忠実さ | 0 | 5 | 1 |
| 読みやすさ | 0 | 7 | 3 |
| 実態 | 0 | 6 | 0 |
| 計 | 0 | 25 | 5 |

**同じ箇所を 2 つ以上の観点が指す組が 3 つ在る。** それぞれ 1 つの直しにまとめた。

- 整合 F-1・実態 F-1・実態 F-2 — 要件 FR11 の注の後半と受入基準 AC9 の固定の材料（便 119 の着地の前の字）。
- 読みやすさ F-1・忠実さ F-6（参考）— 受入基準 AC19・AC20・AC23 の平易文の「撃つ」。
- 整合 F-8・実態 F-4 — 判断の記録の欄の決まり（`adr/schema.yaml`）の生成区間の `limits_note`。

**生成区間に在る所見は 4 件である**（整合 F-6・整合 F-8・実態 F-3・実態 F-4）。生成区間は手で直さない（条 P-6.2・判断の記録 ADR-9 / ADR-11）ので、4 件とも便候補（C）に回した。

**印は main（a70477e）で 28 周目のものに付け直された。** 本一括は設計文書の正本を書き換えるので、本一括の後は印が古くなる（この枝の HEAD で `folio ceiling --gate --write-set design-intent/srs.yaml` は「まだ分からない（印が古い）」）。この枝の PR は席が受付の手順で運ぶ（規則の表の開発規律行 D-12）。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る）／**B** = 判断の記録が要る（条文の改訂か、発効した判断の決定の改訂）／**C** = 便候補（直す先が実装か生成区間。生成区間は手で直せない＝条 P-6.2）／**D** = 直さない（理由を書く）。

| 観点 | id | 重さ | place（実在を確認） | 是正の内容 | 仕分け | 問 |
|---|---|---|---|---|---|---|
| 整合 / 実態 | F-1 / F-1 | 直す | `srs` `requirements.FR11.note` | 注の後半の 3 文（「口は未実装である（2026-09-23 時点）」「便は決定 (7) の ⑤ で起こし」「いまは folio build --check と書いてある生成区間はその便で直す」）を、着地の後の字 1 段に置き換えた＝口は便 119（台帳 f2-648.167）で着地し、命令の名 folio derive と、設計ノートの欄の決まりの床の定数が名指す命令（生成区間の字 folio derive --check）も同じ便で直した。実測＝`design-intent/design-note/schema.yaml` の `derived.check` は `{command: folio derive --check, …}`・実装は `crates/folio/src/derive.rs` | A | 1 |
| 実態（整合 F-1 も指す） | F-2 | 直す | `srs` `acceptance.AC9.red_test.fixture` | 前「tests/fixtures/contracts/（凍結 anchor = 契約表と期待導出物の対 + 1 字ずらした導出物）。固定の材料はまだ置いていない＝要件 FR11 の口が未実装のため（2026-09-20 時点）・path は便で置くときの予定の名」→ 後「tests/fixtures/design-note/derive-anchor.yaml と derive-anchor.toml（凍結 anchor = 手書きの契約表と、それから期待される導出物の対・便 119 が 2026-09-24 に置いた）。1 字ずらした導出物は file に置かず、歯 crates/folio/tests/derive.rs が期待の導出物の写しの 1 byte を変えて作る」。実測＝2 file は在り、`tests/fixtures/contracts/` は無い。歯 `f119_one_byte_drift_and_missing_are_nonzero_and_sourceless_files_are_not_counted` が写しの 1 byte を変えて `--check` の 1 を見る。便 119 の契約の申し送り（`docs/design/delivery-119.md`「AC9 の固定の材料の path の字は後の要件書の版で直す」）を閉じる。判定の文（sentence）は不変 | A | 1 |
| 整合 | F-2 | 直す | `adr` `ADR-16.note` | 注 (d) と (f) の末尾の「一括 15 の問 3 / 問 7 で持ち主に問う」を、その場で裁定の結果の字に改めた（問 3 = 推奨どおり承認・行 R-16 の裁定の欄／問 7 = 案 A が承認＝保留の解除は保ち、folio2 の側の口は進め、判定点 ③ は記録が在るまで「まだ分からない」・決定 (5) の「満たされた」は誤りの記録として残し、解除そのものは有効）。前の字は消した（残すと注の中で未決と決着が並ぶ）。新しい段 (g) に直したことを書いた。**決定・文脈・帰結・承認欄・見出しは 1 字も触っていない** | A | 4 |
| 整合 | F-3 | 直す | `vocabulary` `terms.ceiling-stamp.def` | 前 = 印の中身を括弧で数え上げる（節点ごとの要約値の表と残差の要約値が欠けていた）→ 後 = 「中身（周の id・観点ごとの判定・読んだ正本と面と材料の束の要約値など）の数え上げの正本は要件 FR20 の規範文で、ここには写さない（P-6.3）」。数え上げを 2 か所に持たない形にした | A | 2 |
| 整合 | F-4 | 直す | `srs` `requirements.FR22.note`（と `FR22.shall`・`acceptance.AC19.red_test.sentence`・`adr` `ADR-16.decision` (3)） | FR22 の規範文と AC19 の判定の文の書く file の数え上げに「索引の欄の決まりの file（graph.yaml）」を足した。FR22 の注の「骨格は正本 7 file と 2 つの置き場に加えて graph.yaml を持つ」の 1 文は、規範文へ移した記録の字に置き換えた（前の字は消した）。ADR-16 決定 (3) の字は変えず、注 (h) で「書くもの の数え上げが、同じ決定の『folio schema --write と同じ導出』が graph.yaml も書くことを落としていた・数え上げの正本は FR22 の規範文」と断った。**要件の規範文と受入の判定の式が動く**ので、ほかの直しと分けて 問 3 で問う | A | 3 |
| 整合 | F-5 | 参考 | `index` `intake.lead` | 直さない | D | — |
| 整合 | F-6 | 直す | `graph` `schema.edge_types_note` | 生成区間（正本は `crates/folio/src/graph.rs` の FLOOR の `edge_types_note`）。後の字の案は §「便候補（C）の中身」の C-1。**人が書く頭の注釈**（`# 位置: 判断の記録 ADR-13 決定 (1)(2)。`）は本一括で「（節点の範囲は判断の記録 ADR-14 決定 (1) が id を持つ行に狭めた＝文書そのものと節は節点にしない）」を足した（meta と生成区間は不変・版は上がらない） | C | 4（頭の注釈だけ） |
| 整合 | F-7 | 直す | `vocabulary` `terms.frozen-anchor.def` | 定義から「独立」を外し、条 P-10.1 の修飾に任せた。後「検査の比較元として置く、動かない基準（固定の見本データ）。作る側からも検査する側からも独立したものを 1 本以上持つことは条 P-10.1 が求める（…）。凍結 anchor の置き場（design-intent/anchors/）の憲法の写しのように、凍結と検査が同じ写しの関数を通るものも凍結 anchor と呼ぶが、それは差分検査の比較元であり、P-10.1 の言う独立した凍結 anchor ではない（判断の記録 ADR-2）」。呼び名を分ける案は 問 2 の代替案 | A | 2 |
| 整合 / 実態 | F-8 / F-4 | 直す | `adr` `schema.limits_note`（`schema.anchor.limits_note`） | 生成区間（正本は `crates/folio/src/floor_adr.rs` の FLOOR の `limits_note`）。古い字は 3 項（P-10.1 を「live は M0・未発効」・P-17.2 / P-17.4 の機構を「便 0 の検査（CI）で置く＝day-1」・条の廃止を「M0 で決める」）。後の字の案は C-2。**便 121（`docs/design/delivery-121.md`・枝 docs/d121）が同じ `limits_note` の末項と凍結 anchor `tests/fixtures/schema/adr-region.txt` を書き換える**ので、C の 4 件は台帳 f2-648.171（便 ③ の後続・enums_note の直し・同じ file と anchor）に束ね、便 ③ の着地の後・次の天井の周の前に 1 便で運ぶ（席の裁定・§「便候補（C）の中身」）。ADR-2 の注が「直すのは便」と書いた `limits_note` を運ぶ項は 28 周目の時点で台帳に無かった（実態 F-4 の実測）が、f2-648.171 に束ねることで閉じる | C | — |
| 忠実さ | F-1 | 直す | `srs` `requirements.FR11.plain` | 前「版管理（公開 repo）に置いてある file」→ 後「版管理（repo）に置いてある file」。比べる相手は利用者の repo にも在り（受入基準 AC24）、公開とは限らない。所見の note が参考として挙げた憲法 N-6 の平易文の「版管理（公開 repo）」は直さない（§「この一括に入れなかったもの」） | A | 1 |
| 忠実さ | F-2 | 直す | `srs` `nonfunctional.NFR2.plain` | 前「（消さずに退役の棚へ移しました）」→ 後「（まず退役の棚へ移し、その棚は 2026-09-23 に承認を得て消しました。版管理の履歴には残っています）」＝同じ項の注の字（3 つの退役の棚は 2026-09-23 に repo から消した・総点検 🔴 4・A-1 承認）に合わせた | A | 1 |
| 忠実さ | F-3 | 直す | `srs` `requirements.FR24.plain` | 末尾に「今ある 2 つの凍結の命令（写しの列だけ・番号の一覧だけを作る命令）が何を前提に動くかも変えません。」を足した＝規範文の最後の 1 文の写し | A | 1 |
| 忠実さ | F-4 | 直す | `srs` `requirements.FR25.plain` | 前「…収まっていれば、いつもどおり検査を続けます。」→ 後「…収まっていれば、いつもどおり検査を続け、置き場の一覧に無い値を使った条を違反として出します。」＝規範文の「置き場の値域に無い値を違反にする」の写し（受入基準 AC22 の平易文と同じ向き） | A | 1 |
| 忠実さ | F-5 | 直す | `adr` `ADR-16.plain` | 散文の門の段落を決定 (6) の字に揃えた。違いを 3 つ（番号として数える形・数と単位の読み方〔複数形の字と全角の空白〕・行き先を調べるかどうか）と数え、形だけ認める種類に scribe2 の憲法の条の番号と英字を含む規則の番号も M3 では入れること、最初の割り振りは scribe2 の持ち主が決めること、数と単位の読み方は repo ごとに変えず scribe2 が folio の読みに揃えることを足した。注 (i) に記録。**決定・見出し・承認欄は触っていない** | A | 4 |
| 忠実さ | F-6 | 参考 | `srs` `acceptance.AC19.plain` | 読みやすさ F-1 と同じ直しで閉じた | A | 1 |
| 読みやすさ | F-1 | 直す | `srs` `acceptance.AC19.plain`（同じ形で `AC20.plain`・`AC23.plain`） | 平易文の「撃つ」「撃った」を「実行する」「実行した」に（語彙の「撃つ」の項の言い換えのとおり）。同じ語を持つ題 2 つ（AC19・AC23）も同じ字にした（入口の「決める人」の道が最初に当たる字なので）。AC23 の平易文は、あわせて床に「（形の検査）」を添え、「撃った道具の組み立ての印」を「実行した道具の版を特定する印（組み立ての要約値）」に開いた（判断の記録 ADR-16 の平易文の「動かした道具の版を特定する番号」と同じ向き）。判定の文（sentence）と固定の材料の「撃つ」は作る人あての字として残した（語彙の項が閉じる）。平易文に「撃」の字を持つのは要件書では 3 つだけと数えた（ほかの平易文と入口・憲法・相談窓口・天井・判断の記録の平易文は 0） | A | 1 |
| 読みやすさ | F-2 | 直す | `srs` `requirements.FR23.plain`（同じ字で `AC20.plain`） | 平易文の頭に照らし合わせの目的（「憲法の写しの列が、決まった最初の 1 枚（列の根）から始まっているかを確かめます」）を足し、「表に足すための印を全部の桁で出します」を「最初の 1 枚の印を省かずに全部の桁で出します（この印は、表に行を足すときの材料になります）」にした。AC20 の平易文にも、頭に同じ目的の 1 文（「憲法の写しの列が決まった最初の 1 枚から始まっているかを道具が確かめることを見せる」）を足し、印の中身（最初の 1 枚の印・要約値・表に行を足すときの材料・全部の桁）を同じ向きの字にした（受入の見せ方の文なので、FR23 と同じ字そのものではない）。**見出しは変えていない**（見出しの 2 語は語彙の「列の根」の項が閉じる）。義務は足していない（行を足す手順は注と規則の表の行 D-11 が持つ） | A | 1 |
| 読みやすさ | F-3 | 直す | `srs` `scope_m3.build`（4 行目と 5 行目） | 作業場の言い方を開いた＝撃った・撃つ→動かした・記帳→記録・s2- の項→scribe2 の台帳の項・台帳 f2-648→folio2 の台帳の親の項（f2-648）・席→作業する AI（席）・組み立て→組み立て（道具の実行 file）・「どの版の」→「どの組み立て（版）の」。「build」の字は同じ節の作らない の 2 行目が命令 folio build の意味で使うので、4 行目には使わなかった。中身（前提・記録の持ち主・記録の中身）は変えていない。写しの元の判断の記録 ADR-16 決定 (1) の字は変えない＝節の注に「正本は決定 (1)・食い違えば決定 (1) を正」と「4 行目と 5 行目は開いた字で写した」を書いた | A | 1 |
| 読みやすさ | F-4 | 直す | `srs` `scope_m3.note` | 写しの編集の記録（置き替えた字の列・外から足した字の列・便 117 / 118 の着地の文）を外し、読み手に要る事実だけにした＝正本の断り・4 / 5 行目を開いた字で写したこと・6 行目と受入基準の id は写しの側で足したこと・依存の裁定（scribe2 の A3）が当たる段・参照 id の形の便の位置・便の列の正本の在り処。外した編集の記録の字は版管理の履歴（main a70477e までの `scope_m3.note`）に在り、写しの形の決め方は要件書 第 1.34 版の起草の記録 `docs/design/srs-vB.md` に在る | A | 1 |
| 読みやすさ | F-5 | 直す | `srs` `scope.not_build`（4 行目） | 前「…読みやすさの AI 検査（…）は M2 の「作る」の側に移り、要件 FR17 / FR18 と判断の記録 ADR-8 が持つ」→ 後「…は M2 で作った。M2 はこの章に範囲の節を持たず、M2 で作ったもの（天井）の範囲は要件 FR17 / FR18 と判断の記録 ADR-8 が持つ」。案内先の無い「M2 の「作る」の側」を外した。範囲の節 `scope_m2` を足す案は 問 1 の代替案 | A | 1 |
| 読みやすさ | F-6 | 直す | `constitution` `sources`（出所 2 の記号の読み方） | 読み方を実際の記号に合わせて書き直した＝出所 2 と 3 の本文のハイフンの無い番号（C11.2・N2・A4・C13 など）は scribe2 の憲法の条で、この憲法の条とは別の番号空間（この憲法の「前の版」は folio v1 を指し、scribe2 ではない）・括弧の中のハイフン付きの番号はそれを写した先のこの憲法の条（R-6 は規則の表の行）・G の番号と出所 4 の P1〜P5 は 2026-09-12 の持ち主の裁定（範囲を出所 2 と 3 の本文に限ったので、出所 4 のハイフンの無い P1〜P5 を scribe2 の条と読ませない）。前の字は「C-n / N-n = 前の版（scribe2）の条」「G-n / P-n = 裁定の番号」で、ハイフン付きと書きながら本文はハイフン無し、P-n は本文ではこの憲法の条（P-18 など）を指していた。**条文と schema 節・前文は 1 字も触っていない**（出所は改訂の範囲 `amendment_scope` の外で、凍結 anchor の写しにも入らない＝一括 15 の問 5 と同じ扱い） | A | 5 |
| 読みやすさ | F-7 | 直す | `rules` `thresholds.R-2.ruling` | **正本の側** = 語彙に「一括（直しの一括）」の項を足した（「一括 n」が面から引ける）。**面の側（C の部分）** = 憲法の面の数値の表の裁定の枡は字「）・前の裁定 = 」で最新の 1 件と過去を分けるが、規則の表の 11 行はこの区切りの無いつなぎ目を持ち、枡に 2〜3 件が出る。直す先は面の生成器（`crates/folio/src/face_constitution.rs` の `ruling`）で、`docs/design/face-deliveries-plan.md` §14 に置いた。規則の表の行は 1 行も変えていない（区切りを正本で揃えると 11 行に裁定 id と時刻が要り〔条 P-17.1〕、裁定の欄に「区切りを揃えただけ」の裁定がもう 1 件ずつ足されて来歴がかえって増える） | A（面の側は C） | 2 |
| 読みやすさ | F-8 | 参考 | `index` `shelf.documents.adr` | 直さない | D | — |
| 読みやすさ | F-9 | 参考 | `index` `shelf.documents.srs` | 直さない | D | — |
| 読みやすさ | F-10 | 参考 | `srs` `requirements.FR25.title` | 直さない | D | — |
| 実態 | F-3 | 直す | `design-note` `schema.index.index_note` | 生成区間（正本は `crates/folio/src/floor_note.rs` の FLOOR の `index_note`）。「閉じた一覧も、索引の欄の決まり design-intent/graph.yaml が正本として持つ」は、`crates/folio/src/graph.rs` の先頭の注・`graph.yaml` の `node_kinds_note` と `edge_types_note`・要件 FR14 の注（「正本は実装の型付きの定数で、その写しは索引の欄の決まりの正本の生成区間に在る」）と向きが逆。後の字の案は C-3 | C | — |
| 実態 | F-5 | 直す | `adr` `ADR-2.note` | 注の末尾の「正本は実装の型付きの定数（crates/folio/src/adr.rs）」を「（2026-09-22 の時点では crates/folio/src/adr.rs・便 113〔台帳 f2-648.158〕で crates/folio/src/floor_adr.rs へ移った）」にその場で直し、直しの記録を 1 文足した。実測＝`limits_note` は `crates/folio/src/floor_adr.rs` 423 行に在り、`crates/folio/src/adr.rs` に無い。89c668d（f2-648.158）が床の定数を移した commit。**文脈・決定・案・帰結・承認欄は触っていない** | A | 4 |
| 実態 | F-6 | 直す | `srs` `meta.status_note` | 来歴の v1.33 の「一括 15 = 天井の 27 周目の直し・承認待ち。」から「・承認待ち」を外した＝一括 15 の承認の後の記帳で書き換え漏れた字（一括 15 の欄の表の 3 は前の字「v1.33 = 2026-09-24 起草（…・承認待ち。…）」と後の字「v1.33 = <日付> 発効（…）」を示していたが、記帳は「起草」を「発効」に替えただけで「・承認待ち」の 1 句を残した）。本一括の承認の後の欄の表の 3 は、頭の「起草」と末尾の「・承認待ち」の 2 か所を別々に名指し、表の頭に書いた後の数え方を足した | A | 1 |

**数。** A = 22（整合 F-1・F-2・F-3・F-4・F-7／忠実さ F-1〜F-6／読みやすさ F-1〜F-7／実態 F-1・F-2・F-5・F-6）・B = 0・C = 4（整合 F-6・整合 F-8・実態 F-3・実態 F-4）・D = 4（整合 F-5・読みやすさ F-8・F-9・F-10）。計 30。A のうち忠実さ F-6 は参考だが、読みやすさ F-1 と同じ直しで閉じた。整合 F-6 は C に数えたが、人が書く頭の注釈の 1 句だけは本一括で直した。読みやすさ F-7 は A に数えたが、面の側の直しは C として §14 に置いた。

**B が 0 である理由。** 28 周目の所見に条文（id・見出し・規範文・段・縛る相手）や前文・schema 節を直すものは無く、発効した判断の記録の決定の本文を改めるものも無い（整合 F-4 の ADR-16 決定 (3) は注で断り、数え上げの正本を要件の規範文に置いた）。前の周から残る憲法 N-6 の見出しと前文の順位の「walk」は、判断の記録 ADR-17（起草中・枝 docs/adr17）が運ぶので本一括に入れない。

## D（直さない）の理由

| id | なぜ直さないか |
|---|---|
| 整合 F-5（入口の相談窓口の節の最初の一歩） | folio2 自身の入口としては今も正しい（folio2 には憲法の正本が在り、folio intake は動く）。新しい repo の読み手向けの案内は、骨格の命令と案内の 1 行の便（便 125・`docs/design/delivery-125.md`）が着地したときに節の note へ足すのがよい＝口の無い命令を今は案内できない。便 125 の着地の後の一括で拾う。拾い先は、承認の後の欄 11 で便 125 の契約の §5「本便の着地の後に席が見ること」に入口の相談窓口の節の note を足す |
| 読みやすさ F-8（入口の棚の判断の記録のカードに題が無い） | 27 周目 読みやすさ F-1 と同じ所で、一括 15 が便候補として `docs/design/face-deliveries-plan.md` §13 に置き済み。正本の字は動かさない（題の正本は判断の記録の側・条 P-6.3） |
| 読みやすさ F-9（棚と表紙の「更新 2026-09-12」が作成日） | 参考。面の名札の字（「更新」）と正本の `meta.generated` の意味のずれで、直す先は面の生成器。便候補にするかは §14 の便と一緒に面の便の設計で問う（本一括では起票しない）。拾えるよう `docs/design/face-deliveries-plan.md` §14 に参考の 1 行で置いた |
| 読みやすさ F-10（FR25 の見出しの「部分集合」） | 参考。平易文の「道具の一覧の中に収まっていれば」が言い換えになっており、見出しは作る人も読む技術の字。見出しを動かすと面の目次と導出物の題が動く |

## 便候補（C）の中身

**3 本とも生成区間（床の定数の説明の注＝`*_note` の欄）で、床は読まない**（欄集合・語彙・参照 id の検査の外）。直すと生成区間と凍結 anchor（`tests/fixtures/schema/` の region の写し）が動く。**便 121（`adr/schema.yaml` の生成区間と `adr-region.txt`）と便 123（5 file の `top_level_note`・`graph-region.txt` を含む）が同じ file と anchor を書き換え、台帳 f2-648.171（便 ③ の後続・`floor_adr.rs` の `enums_note` と `adr/schema.yaml` と `adr-region.txt` を小の便 1 本で直す項）が C-2 と同じ file と anchor を持つ。** 席の裁定（2026-09-24）で、C の 4 件は f2-648.171 に束ね、便 ③ の着地の後・次の天井の周の前に 1 便で運ぶ（新しい台帳の項は起こさない・順序の問いは持ち主に出さない）。余地（`face-deliveries-plan.md` §3 の式・2026-09-24・main 2d90e8d）は `crates/folio/src/graph.rs` 762・`crates/folio/src/floor_adr.rs` 999・`crates/folio/src/floor_note.rs` 1011 で、S で受付の余地がある（便 121 の後の `floor_adr.rs` は契約の見積で余地 約 990）。

- **C-1（整合 F-6）`crates/folio/src/graph.rs` の `edge_types_note`。** 前「…figures は伝播に使わない（決定 (2)）。観点が読む文書の欄（reads）と入口の棚の関係は、文書を節点にする便で足す」→ 後の案「…figures は伝播に使わない（決定 (2)）。観点が読む文書と入口の棚の関係は辺にしない＝文書そのものを節点にしないので（判断の記録 ADR-14 決定 (1)）、その関係は天井の正本の読む欄の表と入口の棚から引く」。
- **C-2（整合 F-8・実態 F-4）`crates/folio/src/floor_adr.rs` の `limits_note` の 3 項。** ① 4 項目「…P-10.1（live は M0）は未発効で、day-1 の代替として tests/floor_cases.yaml を置くが、床の入力ではない（fixture の不在は検出されない）＝M0 で床の入力に取り込む。」→ 案「…P-10.1 の機構の状態は憲法の条 P-10 の機構の欄が持つ（ここには写さない）。tests/floor_cases.yaml は歯の入力で、床の入力ではない（fixture の不在は床では検出されない・判断の記録 ADR-2 の注）。」② 6 項目「…機構は憲法のとおり便 0 の検査（CI）で置く＝day-1 の床では効いていない。…（便 0 の検査の領分）」→ 案「…機構は憲法の条 P-17 の機構の欄が持つ（便 0 は着地したがその検査は置かれず、床では効いていない）。…（その検査の領分）」③ 12 項目「…P-7.2「廃止は状態で」を条に適用する形は M0 で決める。」→ 案「…形は M0 では決めておらず、台帳 f2-648.100 に起票した（判断の記録 ADR-2 の帰結）。」
- **C-3（実態 F-3）`crates/folio/src/floor_note.rs` の `index_note`。** 前「…節点と辺の欄も、節点の種類と辺の型の閉じた一覧も、索引の欄の決まり design-intent/graph.yaml が正本として持つ＝この節はその置き場を指すだけで写しを持たない（P-6.3）。」→ 案「…節点と辺の欄と、節点の種類と辺の型の閉じた一覧の正本は実装の型付きの定数（crates/folio/src/graph.rs）で、その写しは索引の欄の決まり design-intent/graph.yaml の生成区間に在る＝この節はその置き場を指すだけで写しを持たない（P-6.3）。」

**後の字は案である。** 字は便の起草役が実装と凍結 anchor の実測で確かめて決める（便の契約の §1 に書く）。

## 着地の順（席の裁定・2026-09-24）

便 ②（便 121・受付済みで走っている）→ 便 ③（便 122）→ 便 123 → **一括 16（持ち主の承認の後）** → 便 125 の順に着地させる。C の 4 件（f2-648.171 に束ねる）は便 ③ の着地の後・次の天井の周の前に 1 便で運ぶ。

- 一括 16 が着地すると設計文書の正本が変わり、天井の印（28 周目・main a70477e）は古くなる。**一括 16 の着地の後に受け付ける便のうち、天井の門の対象の便（設計文書の置き場の file を書き換える便）は、門が「まだ分からない（印が古い）」を返すので、規則の表の開発規律行 D-12 の前例（印が古いときの席の受付の手順）で受ける。** 便 125 はこれに当たる。
- 便 ②・③・123 は一括 16 より先に着地させるので、その受付は今の印（28 周目）のまま門を通る見込みである（便 123 の契約の門の実測は main a70477e で 0）。
- 一括 16 は 3 便の着地の後に main へ載せ替え、床 4 本と nextest を撃ち直してから着地させる（便 123 が同じ srs.yaml・vocabulary.yaml・graph.yaml の生成区間を書き換えるが、本枝の hunk とは行が離れている）。

## 問どうしの依存

- **要件書 第 1.35 版の中身は 問 1・問 2・問 3 の答えで決まる。** 語彙は要件書と同じ承認で運ぶ（一括 11〜15 と同じ形）。退けた問の分は元の字に戻して発効する（席が承認の後の commit で戻す）。**退けた問の分は、要件書の来歴（`meta.status_note` の v1.35）と作成の行の stamp からも外す**（2 か所とも一括全部の要旨を持つ）。問 1〜3 を全部退けたら版を上げない＝`meta.version` を v1.34 に戻し、来歴の v1.35 の項と作成の行と、`status_note` の頭の「第 1.35 版（一括 16）は起草・承認待ちです。」を外す（来歴の v1.33 の「承認待ち」の消し忘れの直しは問 1 の分なので、問 1 を退けたときも一緒に戻る）。
- **問 3 は 問 4 の注 (h) と対である。** 問 3 を退けたら、FR22 の規範文・AC19 の判定の文・FR22 の注の 1 段を元の字に戻し、判断の記録 ADR-16 の注 (h) を落とす。問 3 を承認して問 4 を退けたときは、(h) だけを残して (g)(i) と平易文と ADR-2 の注と `graph.yaml` の頭の注釈を戻す＝ADR-16 の注の 28 周目の段は (h) 1 つになる。
- **問 3 の答えは便 125 の契約（`docs/design/delivery-125.md`・枝 docs/d123・HEAD 4692889 の時点の字）の引用に効く。** 契約は §1 (b) で書く file を 11 本（正本 7・graph.yaml・判断の記録の欄の決まりと最初の判断の記録・設計ノートの欄の決まり）と決め、graph.yaml の根拠を「要件 FR22 の注〔第 1.34 版〕の字」に置いている（§1 (b) の書く file の行・§1 (d) の 1 は graph.yaml を書かない形を「採らない」）。承認なら 11 本が規範文と一致し、契約の 2 か所の引用は一括 16 の発効の後に便 125 の改訂で「要件 FR22 の規範文（第 1.35 版）」の字に直す（席の裁定・便 125 の受付は一括 16 の後なので間に合う）。退けても FR22 の注が graph.yaml を持ち続けるので、契約は今の字のままで通る。
- **問 1 の中の直しは互いに独立である。** ただし FR11 の注（item 1）と AC9 の固定の材料（item 2）は同じ事実（便 119 の着地）の 2 か所なので一緒に扱う。AC20 の平易文は item 6（撃つ）と item 7（列の根の目的）の両方が同じ 1 文を直しているので、片方だけ退けるときは席が残る方の字に書き直す。
- **問 2 の 3 項は互いに独立である。** 一括の項を退けても、読みやすさ F-7 の面の側の便候補（§14）はそのまま残る。ただし §14 の項の頭の括弧の後半「語彙の「一括」の項は一括 16 で足した」は外す（席が承認の後の commit で戻す）。
- **問 4 の (g) と (i) は互いに独立で、ADR-2 の注と `graph.yaml` の頭の注釈もほかに依らない。** 注 (g) は注 (d)(f) の末尾の書き換えそのものの記録なので、(g) を退けるなら (d)(f) の末尾も元の字に戻す。
- **問 5 はほかの問に依らない。** 判断の記録 ADR-17（起草中）は同じ `constitution.yaml` の条 N-6 の見出しと前文の順位と版の欄を直すが、本一括が触る出所（`sources`）とは重ならない。どちらが先に着地しても、後の枝の載せ替えで済む。

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 生成区間 4 件（整合 F-6・F-8・実態 F-3・F-4） | 生成区間は手で直さない（条 P-6.2・判断の記録 ADR-9 / ADR-11）。§「便候補（C）の中身」に後の字の案を置いた |
| 読みやすさ F-7 の面の側（裁定の枡の区切り） | 直す先は面の生成器。`docs/design/face-deliveries-plan.md` §14 に置いた |
| 憲法 N-6 の平易文の「版管理（公開 repo）」（忠実さ F-1 の note が参考として挙げた） | N-6 の平易文は folio2 自身の承認済みの文書を folio2 の repo へ置く話で、比べる相手を利用者の repo に広げる要件 FR11 と事情が違う。同じ条は判断の記録 ADR-17 が見出しを改訂中で、同じ条の字を 2 つの枝で動かさない |
| 憲法 N-6 の見出しと前文の順位の「walk」 | 判断の記録 ADR-17（起草中・枝 docs/adr17）が運ぶ（一括 15 の問 6 の承認） |
| 生成区間の注から条 id N-3 を外す直し | 便 123（`docs/design/delivery-123.md`・枝 docs/d123）が運ぶ。28 周目の所見には無い |
| `adr/schema.yaml` の生成区間の列の根の直し | 便 121（`docs/design/delivery-121.md`）が運ぶ。C-2 は台帳 f2-648.171 に束ね、便 ③ の着地の後に起こす |
| 判断の記録 ADR-9 の文脈と帰結の「adr.rs と note.rs の FLOOR」 | 2026-09-19 の発効の時点の字で、今の置き場を名指す字ではない（ADR-2 の注の「正本は…adr.rs である」とは違い、現在形の断定でない）。所見も無い |
| 判断の記録 ADR-16 の平易文の「scribe2 の側の読む口が 2026-09-22 に出来たので、保留を解く条件は満たされました」 | 誤りは注 (f) が記録し、一括 15 は「平易文の字は変えない」と承認した。本一括の注 (f) の末尾で、解除そのものは有効と書いた |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.35 版の起草。** FR11 の注の後半と平易文（整合 F-1・実態 F-1・忠実さ F-1）／AC9 の固定の材料（実態 F-2）／NFR2・FR24・FR25 の平易文（忠実さ F-2〜F-4）／FR23・AC20 の平易文（読みやすさ F-2）／AC19・AC20・AC23 の平易文と AC19・AC23 の題の「撃つ」（読みやすさ F-1・忠実さ F-6）／FR22 の規範文と注・AC19 の判定の文（整合 F-4・問 3）／範囲の章の「M0 では作らない」の 4 行目（読みやすさ F-5）／M3 の範囲の節の作る の 4・5 行目と注（読みやすさ F-3・F-4）／`meta.version` を v1.35 に・`status_note` の頭に「第 1.35 版（一括 16）は起草・承認待ちです。」・来歴の v1.33 の「承認待ち」を外し（実態 F-6）・末尾に v1.35 の起草・`approval` に 作成 の行（who = orchestrator 席（AI・fable 5.1））。**`effective_version` は v1.34 のまま・承認の行は書いていない** |
| `design-intent/vocabulary.yaml` | 3 項。天井の判定の印（整合 F-3）／凍結 anchor（整合 F-7）／**一括の項を新設**（読みやすさ F-7・id `batch`・append only）。末尾の注釈の「後に足した語」に一括 16 の 1 語を足した。**要件書と同じ承認で運ぶ** |
| `design-intent/adr/ADR-16.yaml` | `plain` の散文の門の段落（忠実さ F-5）・`note` の (d)(f) の末尾（整合 F-2）・【天井の 28 周目の所見への応答】(g)(h)(i)。**見出し・文脈・決定・帰結・承認欄は 1 字も触っていない** |
| `design-intent/adr/ADR-2.yaml` | `note` の末尾の置き場の字と直しの記録の 1 文（実態 F-5）。**文脈・決定・案・帰結・承認欄は 1 字も触っていない** |
| `design-intent/constitution.yaml` | 出所 2 の記号の読み方（読みやすさ F-6）。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触っていない**＝条 A-2.2 の改訂に当たらず、憲法の版は上がらない |
| `design-intent/graph.yaml` | 人が書く頭の注釈の 1 句（整合 F-6 の人が書く側）。meta と生成区間は不変 |
| `docs/design/face-deliveries-plan.md` | §14 に一括 16 からの便候補 1 本（読みやすさ F-7 の面の側） |
| `docs/design/batch16-triage.md` | **新設**（この file） |

**実装（`crates/folio/src`）・凍結 anchor（`tests/fixtures/`）・歯（`crates/folio/tests`）・生成区間・規則の表（`rules.yaml`）・印（`preview/ceiling-stamp.yaml`）は 1 byte も触っていない。** 天井の正本・入口の正本・相談窓口の正本も触っていない。

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.35 版の注・平易文・題・受入の固定の材料・範囲の章・来歴

**やりたいこと。** 要件書の次の直しを同じ承認で発効する。**要件の規範文と受入基準の判定の式は 1 字も動かない**（規範文と判定の式が動く FR22 と AC19 は問 3 で別に問う）。

1. 要件 FR11 の注の後半の「口は未実装・便はこれから・生成区間はまだ folio build --check」の 3 文を、便 119 で口が着地し命令の名と生成区間の字も直したという字 1 段にする（整合 F-1・実態 F-1）。
2. 受入基準 AC9 の固定の材料を、予定の名（tests/fixtures/contracts/）と「まだ置いていない」の字から、便 119 が置いた凍結 anchor の path（tests/fixtures/design-note/derive-anchor.yaml と .toml）と 1 字ずらしの作り方にする（実態 F-2）。
3. FR11 の平易文の「版管理（公開 repo）」を「版管理（repo）」にする（忠実さ F-1）。
4. 非機能要件 NFR2 の平易文の「消さずに退役の棚へ移しました」を、棚を 2026-09-23 に承認を得て消した字にする（忠実さ F-2）。
5. FR24 の平易文に「今ある 2 つの凍結の命令が何を前提に動くかも変えません」、FR25 の平易文に「置き場の一覧に無い値を使った条を違反として出します」を足す（忠実さ F-3・F-4）。
6. 受入基準 AC19・AC20・AC23 の平易文と、AC19・AC23 の題の「撃つ」を「実行する」にする。AC23 の平易文は、あわせて床に「（形の検査）」を添え、「撃った道具の組み立ての印」を「実行した道具の版を特定する印（組み立ての要約値）」に開く（読みやすさ F-1）。
7. FR23 と AC20 の平易文に、照らし合わせの目的（列が決まった最初の 1 枚から始まっているか）と、出す印の中身（最初の 1 枚の印を全部の桁で・表に行を足すときの材料）を書く。AC20 は受入の見せ方の文なので、同じ中身を「…を道具が確かめることを見せる」の形で書く（読みやすさ F-2）。
8. M3 の範囲の節の作る の 4・5 行目の作業場の言い方を開き、節の注を正本の断りと要る事実だけにする（読みやすさ F-3・F-4）。
9. 範囲の章の「M0 では作らない」の 4 行目の「M2 の「作る」の側に移り」を、M2 はこの章に範囲の節を持たず範囲は FR17 / FR18 と判断の記録 ADR-8 が持つ、の字にする（読みやすさ F-5）。
10. 来歴の v1.33 の項に残った「承認待ち」を外す（実態 F-6）。

**理由。** 所見 14 件（10 の直しにまとめた・うち 1 件は参考の忠実さ F-6）とも「着地したのに字が古い」「平易文が規範文や注と合わない」「非エンジニアの読み手を想定していない字」の形である。1 と 2 は、便 119 の着地を同じ注の前半が書きながら後半が着地前の字のままで、同じ欄の中で口の状態が逆に読める。2 は便 119 の契約が「後の要件書の版で直す」と申し送った字である。3〜5 は平易文が規範文の範囲を狭めるか落としている。6〜9 は、入口の「決める人」の道が最初に当たる平易文・題・範囲の章に、語彙が言い換えを定めた作業場の言い方と、案内先の無い案内が残っていた。10 は一括 15 の承認の記帳で書き換え漏れた字で、同じ欄の頭（発効）と逆の字が 1 か所残っていた。

**代替案。**
- **(A) 読みやすさ F-5 を、範囲の節 `scope_m2`（M2 の作る／作らない）を足して直す。** 案内先が在る形になり、M0・M1・M3 と揃う。ただし要件書の最上位の節の閉じた一覧は床の定数（`crates/folio/src/check.rs`）で、便 117 と同じ形の便（閉じた一覧の改訂・生成区間・凍結 anchor・面の生成器）が版上げより先に要る。M2 の範囲は要件 FR17 / FR18 と判断の記録 ADR-8 が既に持つので、写しを 1 つ増やすことにもなる。
- **(B) 読みやすさ F-3 を、M3 の範囲の節は写しのまま触らず、判断の記録 ADR-16 の平易文への案内だけを節の注に足して直す。** 写しと正本の字が揃ったままになる。ただし面の上で範囲の節の字そのものは作業場の言い方のままで、所見が指した「読み手を想定していない形」は残る。

**コスト。** 承認 1 回（問 2・問 3 と同じ版で運ぶ）。動く欄は 注 2（FR11・範囲の節 M3）・平易文 8（FR11・NFR2・FR23・FR24・FR25・AC19・AC20・AC23）・題 2（AC19・AC23）・受入の固定の材料 1（AC9）・範囲の章の行 3（「M0 では作らない」の 4 行目・M3 の作る の 4 行目と 5 行目）・来歴の 1 句。**要件の番号は 1 つも動かず（条 P-7.1）、床の歯も 1 本も直さない**（枝の HEAD で 796 本すべて通る）。

**推奨 = 10 の直しをまとめて発効する（代替案は採らない）。** 義務を増やさず減らさず、字を根拠と実態と読み手に合わせるだけである。

### 🔴 問 2. 語彙の 3 項（天井の判定の印・凍結 anchor・一括）

**やりたいこと。** 語彙の 2 項を直し、1 項を足す。語彙は要件書と同じ承認で運ぶ。

1. 「天井の判定の印」の定義の中身の数え上げを外し、数え上げの正本は要件 FR20 の規範文と書く（整合 F-3）。
2. 「凍結 anchor」の定義から「作る側からも検査する側からも独立した」を外し、独立は条 P-10.1 の修飾に任せる。あわせて、design-intent/anchors/ の憲法の写しは凍結 anchor と呼ぶが差分検査の比較元で、P-10.1 の言う独立した凍結 anchor ではない（判断の記録 ADR-2）と書く（整合 F-7）。
3. 「一括（直しの一括）」の項を足す＝天井の 1 周か数周の所見（止める・直す・参考）を仕分け、同じ回に持ち主へ出す問い（便の問いを一緒に運ぶ回もある）とあわせて、持ち主の 1 回の承認でまとめて当てる回・番号は回の順・規則の表の裁定の欄の「一括 n」はその字を変えた承認の回（読みやすさ F-7）。

**理由。** 1 は、同じ数え上げを要件 FR20 と語彙の 2 か所に持ち、FR20 が第 1.28 版で足した中身（節点ごとの要約値の表と残差の要約値）が語彙に無かった（条 P-6.3）。2 は、語彙の定義に従うと、判断の記録 ADR-2・ADR-11・ADR-12・ADR-15 と欄の決まりが凍結 anchor と呼ぶ anchors/ の写しが凍結 anchor でなくなる＝同じ語が 2 つの意味を持っていた。憲法 P-10.1 は「独立した凍結 anchor」と修飾で独立を足しており、語の定義に独立を含めなくても P-10.1 の義務は変わらない。3 は、入口の「決める人」の道が案内する憲法の面の数値の表の裁定の欄に「一括 n」が多く出るのに、語彙にも面にも意味が無かった。

**代替案。**
- **(A) 整合 F-7 を、anchors/ の写しの呼び名を分けて直す（例「凍結の写し」）。** 語彙の定義は今のまま（独立を含む）。ただし発効した判断の記録 ADR-2・ADR-11・ADR-12・ADR-15 の決定と帰結の本文、欄の決まりの生成区間（`anchor_note` ほか・実装の定数）、歯と便の契約の多くが anchors/ の写しを凍結 anchor と呼んでおり、判断の記録の本文は変えないので注の断りが 4 本と実装の便が要る。
- **(B) 読みやすさ F-7 を、語彙に足さず面の側（§14 の便）だけで直す。** 枡に出る字は減るが、残る 1 件の「一括 n」の意味はやはり引けない。

**コスト。** 承認 1 回（問 1 と同じ版）。定義 2・新しい項 1・末尾の注釈の 1 語。**義務は 1 つも増えない。** 語彙の id は append only（`batch` を足すだけ・条 P-7.1）。

**推奨 = 3 項とも直す（代替案は採らない）。**

### 🔴 問 3. 要件 FR22（骨格の命令）の規範文と受入基準 AC19 の判定の文に、索引の欄の決まりの file（graph.yaml）を足す

**やりたいこと。** 骨格の命令が書く file の数え上げに graph.yaml を足す。**要件の規範文と受入の判定の式が動く**。

- FR22 の規範文 — 前「folio は利用者が選んだ置き場（--dir）に、正本 7 file と、判断の記録の置き場とその欄の決まりの file と、設計ノートの置き場とその欄の決まりの file を書く。」→ 後「folio は利用者が選んだ置き場（--dir）に、正本 7 file と、索引の欄の決まりの file（graph.yaml）と、判断の記録の置き場とその欄の決まりの file と、設計ノートの置き場とその欄の決まりの file を書く。」（規範文のほかの文は不変）
- AC19 の判定の文 — 「…置き場に撃つと正本 7 file と判断の記録と設計ノートの置き場と欄の決まりが書かれて…」→「…置き場に撃つと正本 7 file と索引の欄の決まり（graph.yaml）と判断の記録と設計ノートの置き場と欄の決まりが書かれて…」
- FR22 の注の「骨格は正本 7 file と 2 つの置き場に加えて graph.yaml を持つ」の 1 文を、規範文へ移した記録の字に置き換える。
- 判断の記録 ADR-16 決定 (3) の「書くもの」の字は変えず、注 (h) で「同じ決定の『folio schema --write と同じ導出』が graph.yaml も書くことを数え上げが落としていた・骨格が書く file の数え上げの正本は FR22 の規範文」と断る。

**理由。** 書く file の範囲は要件の中身なのに、規範文・受入の判定の文・判断の記録の決定は graph.yaml を持たず、要件の注だけが持っていた（整合 F-4）。骨格が graph.yaml を書かないと `folio schema --check` が「まだ分からない」（2）を返し、骨格の生成区間が床の定数と一致することを利用者が確かめる口が行き止まりになる（便 125 の起草の試作の実測 2026-09-24・`docs/design/delivery-125.md` §1 (d) の 1）。便 125 の契約は graph.yaml を書く形を採り（§1 (b) の 11 本・§1 (d) の 1 は書かない形を「採らない」）、その根拠を規範文でなく要件 FR22 の注〔第 1.34 版〕に置いている。規範文を直さないままだと、便は規範文の列挙より 1 本多く書き、書く file の範囲という要件の中身が注にしか無いままになる。決定 (3) の本文を改めない理由は、同じ決定の次の文（生成区間は `folio schema --write` と同じ導出で書く）が graph.yaml を書くことを既に含み、数え上げの落ちは決定の中身の変更でなく写しの落ちだからである（一括 13〜15 と同じく、発効した判断の記録の食い違いは注で断る）。

**代替案。**
- **(A) 規範文は触らず、注で持ったままにする（今の形）。** 承認の範囲は小さいが、規範文と受入の判定の文の数え上げが実装（便 125）と 1 本ずれ、次の周の整合が同じ所を拾う見込みが高い。
- **(B) 数え上げをやめ、性質の字にする。** 例「置き場に、folio schema --write が生成区間を書く file のすべて（正本と欄の決まり）と、判断の記録と設計ノートの置き場を書く」。file が増えても規範文を直さずに済む。ただし何を書くかが規範文から直に読めなくなり、受入の判定の文も数を持てなくなる。
- **(C) 判断の記録 ADR-16 の決定 (3) を改める判断の記録を起こす。** 決定と要件の字が揃う。ただし中身の判断は変わらない（graph.yaml は同じ決定の導出の文に含まれる）ので、判断の記録 1 本の起草・敵対的検証・承認は字の揃えに対して重い。

**コスト。** 承認 1 回（問 1 と同じ版）。規範文の 1 句・受入の判定の文の 1 句・注の 1 段・判断の記録の注 1 段。便 125（起草済み・未受付）の契約の書く file 11 本（正本 7・graph.yaml・判断の記録の欄の決まりと最初の判断の記録・設計ノートの欄の決まり）は規範文と一致するので、書く中身は直さずに済む。直すのは契約の 2 か所の引用の字（「要件 FR22 の注〔第 1.34 版〕」→「要件 FR22 の規範文〔第 1.35 版〕」）だけで、一括 16 の発効の後に便 125 の改訂で直す。歯は動かない（FR22 は未実装・歯は便 125 が足す）。

**推奨 = 規範文と受入の判定の文に graph.yaml を足す（代替案は採らない）。**

### 🔴 問 4. 判断の記録 ADR-16 の平易文と注、ADR-2 の注、索引の欄の決まりの頭の注釈

**やりたいこと。** 次を直す。**判断の記録の見出し・文脈・決定・案・帰結・承認欄は 1 字も触らない**（発効済みの判断の記録の食い違いは注で断る＝一括 13〜15 と同じ扱い）。判断の記録の状態（accepted）は変わらない。

1. ADR-16 の平易文の散文の門の段落を決定 (6) に揃える（忠実さ F-5）。
   - 前 —「違うのは「番号」として数える形で、これは repo ごとに規則の表の行に持たせます。…番号の行き先を調べられない形（節の記号・台帳の番号・4 桁の判断の番号）は「形だけ認める」種類に分けます。」
   - 後 —「違いは 3 つあります。「番号」として数える形と、数と単位の読み方（英字の単位の後の複数形の字と全角の空白）と、番号の行き先を調べるかどうかです。番号として数える形は、repo ごとに規則の表の行に持たせます。…番号の行き先を調べられない形（節の記号・台帳の番号・4 桁の判断の番号）は「形だけ認める」種類に分けます。scribe2 の憲法の条の番号と英字を含む規則の番号も、行き先が folio の置き場の外なので、M3 では同じ種類に置きます。どの形をどちらの種類に置くかの最初の割り振りは、scribe2 の持ち主が決めます。数と単位の読み方は repo ごとに変えず、scribe2 の側が folio の読みに揃えます。」
2. ADR-16 の注 (d) と (f) の末尾の「一括 15 の問 3 / 問 7 で持ち主に問う」を、裁定の結果の字にする（整合 F-2）。(f) には、問 7 の案 A の中身（保留の解除は保ち、folio2 の側の口は進め、判定点 ③ は記録が在るまで「まだ分からない」）と、決定 (5) の「満たされた」は誤りの記録として残し解除そのものは有効であることを書く。
3. ADR-16 の注に【天井の 28 周目の所見への応答】(g)(i) を足す（1 と 2 の記録）。(h) は問 3 の分である。
4. ADR-2 の注の「正本は実装の型付きの定数（crates/folio/src/adr.rs）」を、便 113 で crates/folio/src/floor_adr.rs へ移ったことが読める字にする（実態 F-5）。
5. 索引の欄の決まり（`graph.yaml`）の人が書く頭の注釈の「位置: 判断の記録 ADR-13 決定 (1)(2)」に、節点の範囲を ADR-14 決定 (1) が狭めたことの 1 句を足す（整合 F-6 の人が書く側・meta と生成区間は不変・版は上がらない）。

**理由。** 1 は、持ち主が承認の根拠に読む平易文が、決定 (6) の 3 つの違いのうち 1 つだけを挙げ、揃える向き・形だけ認める種類の中身・初版を決める人を落としていた（義務の向きは同じ）。2 は、一括 15 は 7 問とも承認されて要件書と規則の表には記帳されたのに、ADR-16 の注だけが未決の字のままで、ADR-16 だけを読むと決定 (5) の解除の扱いが未決と読めた。前の字を残して段を足すと、同じ注の中に「問う」と「承認された」が並ぶので、その場で書き換えた。4 は、直す便を探す読み手が今は床の定数を持たない file を開く。5 は、頭の注釈が節点の範囲を狭めた側の判断の記録を名指さない（生成区間の注の直しは便候補 C-1）。

**代替案。**
- **(A) 注 (d)(f) を書き換えず、28 周目の段にだけ裁定の結果を書く。** 承認済みの注の字が 1 字も動かない。ただし (d)(f) の末尾の「持ち主に問う」が同じ注に残り、次の周が同じ所を拾う見込みがある（一括 14・15 の検証で繰り返し出た「前の形を消さずに足す」の形）。

**コスト。** 承認 1 回。平易文の 1 段落（5 文）・注の末尾 2 か所・注の新しい段 1 つ（(g)(i)・(h) は問 3）・ADR-2 の注の括弧 1 つと 1 文・頭の注釈の 1 句。**義務は 1 つも増えない。**

**推奨 = 5 つとも直す（代替案は採らない）。**

### 🔴 問 5. 憲法の出所の記号の読み方

**やりたいこと。** 出所 2 の「記号の読み方」を実際の記号に合わせて書き直す（読みやすさ F-6）。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触らない＝条 A-2.2 の改訂に当たらず、憲法の版は上がらない。**

- 前 —「記号の読み方: C-n / N-n = 前の版（scribe2）の憲法の条の番号・G-n / P-n = 2026-09-12 の裁定の番号（判断の記録 ADR-1 の出所）・「G-n=X」の「=X」はその裁定で選ばれた案の記号」
- 後 —「記号の読み方: 出所 2 と 3 の本文のハイフンの無い番号（C11.2・N2・A4・C13 など）は scribe2 の憲法の条の番号で、この憲法の条とは別の番号空間である（この憲法の「前の版」は folio v1 を指し、scribe2 ではない）。括弧の中のハイフン付きの番号（P-18・N-2・A-3 など）は、それを写した先のこの憲法の条で、R-6 は規則の表の行である。G8=A のような G の番号と出所 4 の P1〜P5 は 2026-09-12 の持ち主の裁定の番号（判断の記録 ADR-1 の出所・出所 4）で、「=A」はその裁定で選ばれた案の記号」

**理由。** 憲法・要件書・語彙のほかの箇所では「前の版」は folio v1 を指す（条 P-13 の平易文ほか）のに、出所だけが同じ語で scribe2 を指していた。さらに読み方は「C-n / N-n」「G-n / P-n」とハイフン付きで書くが、出所の本文の scribe2 の条はハイフン無し（C11.2・N2）で、ハイフン付きの P-18 や N-2 はこの憲法の条である＝読み方どおりに読むと取り違える。出所 3 は「記号の読み方は出所 2 のとおり」なので、直すのは 1 か所で済む。

**代替案。**
- **(A) scribe2 の条の番号に「scribe2 の」を 1 つずつ添える。** 読み方の説明が要らなくなるが、出所 2 と 3 の本文の番号 20 か所ほどが長くなり、来歴の字（裁定の逐語の写しを含む）を書き換えることになる。

**コスト。** 承認 1 回。出所の 1 文。`folio inject --check` の生成区間（前文と規範文）は動かない（枝の HEAD で 68 行 / 7,912 byte 一致）。判断の記録 ADR-17（起草中）とは触る節が重ならない。

**推奨 = 書き直す（代替案は採らない）。**

## 承認の後に席が書く欄

持ち主が答えた後に、席が同じ枝の別の commit で書く。退けた問の分は「問どうしの依存」のとおり元の字に戻す。**「承認待ち」「起草」の字を持つ欄は全部ここに挙げた**（一括 15 の記帳で `status_note` の来歴の 1 句が漏れた＝実態 F-6。書いた後に、表の 2・3・5・6 の 4 欄を 1 つずつ開いて、一括 16 に触れる字に「承認待ち」と「起草・」が残っていないことを確かめる。**ただし、かぎ括弧の中の「承認待ち」（実態 F-6 の説明として来歴の v1.35 の項と作成の行の stamp が引く「来歴の v1.33 の「承認待ち」の消し忘れ」）は正しい字で、消さない。** `status_note` は 1 行に全版の来歴を持ち、ほかの版の「承認待ち」〔v1.17 の起草の項など〕はその版の記録なので、grep の 1 本では数えられない）。

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.34 → v1.35 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の頭の 2 文 | 「いま発効しているのは 第 1.34 版（2026-09-24）です。第 1.35 版（一括 16）は起草・承認待ちです。」を「いま発効しているのは 第 1.35 版（<承認の日付>）です。」の 1 文に |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.35 | 「v1.35 = 2026-09-24 起草（一括 16 = …・承認待ち）」を「v1.35 = <日付> 発効（逐語「…」・f2-648 notes <時刻> JST・一括 16 = …）」に。**頭の「起草」と末尾の「・承認待ち」の 2 か所とも書き換える** |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 16〔問 1〜5〕の承認要求への回答）", verbatim: <逐語>, version: v1.35}`）。作成の行は起草のまま（書き換えない） |
| 5 | `design-intent/adr/ADR-16.yaml` | `note` の【天井の 28 周目の所見への応答（一括 16・2026-09-24 起草・持ち主の承認待ち）】の見出し | 「2026-09-24 起草・持ち主の承認待ち」を「2026-09-24 起草・持ち主の承認 <日付 時刻> JST「<逐語>」」に |
| 6 | `design-intent/adr/ADR-2.yaml` | `note` の末尾の「（2026-09-24・天井の 28 周目 実態 F-5・一括 16・持ち主の承認待ち）」 | 「持ち主の承認待ち」を「持ち主の承認 <日付 時刻> JST「<逐語>」」に |
| 7 | `docs/design/batch16-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に |
| 8 | `docs/design/state.md` | 要件書の版の行・いま走っている PR の行・次の一手・未決の表 | 要件書 = 第 1.35 版（発効の日付と逐語・一括 16）／一括 16 の後の手番（便候補 C の起票・天井の 29 周目）。state.md の今の字は書き換える前に読んで、一括 16 に触れる行を全部直す |
| 9 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）と、一括 16 の仕分けの在り処 |
| 10 | 台帳（`bd`） | f2-648.171 の notes（新しい項は起こさない） | C-1〜C-3（C の 4 件）を f2-648.171 に束ねる旨と、後の字の案の在り処（この文書の §「便候補（C）の中身」）を追記する。運ぶ時機は便 ③ の着地の後・次の天井の周の前（席の裁定）。§14 の面の便（裁定の枡）は `docs/design/face-deliveries-plan.md` の運びどおり |
| 11 | `docs/design/delivery-125.md`（枝 docs/d123・便 125 の改訂） | §1 (b) の書く file の 11 本の行の括弧「要件 FR22 の注〔第 1.34 版〕の字」と、§1 (d) の 1 の「要件 FR22 の注（第 1.34 版・改訂 g）が…（規範文の列挙には無く、注で持つ）」／§5 の「本便の着地の後に席が見ること」 | 問 3 を承認したら、2 か所の引用を「要件 FR22 の規範文（第 1.35 版）」の字に直す（退けたら直さない）。§5 には、入口の相談窓口の節の note に骨格の命令を添えること（28 周目 整合 F-5・一括 16 は D）を足す（問 3 の答えに依らない） |

語彙と憲法と索引の欄の決まりの頭の注釈には承認欄の行が無い（語彙は要件書の承認で運び、憲法の出所と頭の注釈の直しは一括 10・11・14・15 と同じく承認欄を持たない）。規則の表は本一括で 1 行も変えていないので、裁定 id と時刻を付ける行は無い（条 P-17.1）。

## 床の結果（この枝の HEAD で実測・2026-09-24 10:5x JST・改訂 b の後）

main 8066e4c の上に載せ替えた枝の、改訂 b を当てた作業ツリーで撃った。binary は main root の `target/debug/folio`（`crates/` は main の afabdf8 から不変＝枝の source と同じ）。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | rc 0（9 file 一致） |
| `folio inject --check --dir design-intent` | rc 0（68 行 / 7,912 byte 一致） |
| `folio derive --dir design-intent --out ../contracts --check` | rc 0（一致 1・差分 0） |
| `cargo nextest run --workspace --no-tests=fail --no-fail-fast` | **796 / 796 PASS**（枝の worktree・一時の target dir） |
| `folio build --dir design-intent --out <scratch> --write` | 床 合格・23 file 書けた。要件書の面（範囲の章の M2 の行・M3 の範囲の節）・憲法の面（出所）・ADR-16 の面（平易文の散文の門の段落）に直した字が出ることを字で確かめた。1440 の幅で開いて目で見る確認はしていない |
| `folio ceiling --dir design-intent --gate --write-set design-intent/srs.yaml` | まだ分からない（印が古い）＝本一括が正本を書き換えたため。印は 28 周目（main a70477e） |

**歯は 1 本も直していない。** 起草の 1 度目の床で、語彙の一括の項の定義に置き場の path（docs/design/）を書いたところ、行 R-9 が「語彙に無い英字の語 docs」で落とした。path を外し「回ごとの仕分けの設計ノート（例 batch16-triage.md）」の字にして通した。要件の欄 adrs と受入の欄 verifies は 1 行も足していない（行 R-17 と歯 `f90_the_real_srs_carries_the_adrs_field` に触れない）。

## まだ分からない点

- **器の口 s2-07l.473 の今の状態は測っていない。** 要件 FR11 の注の「2026-09-24 に器の台帳で再開された」の字は一括 15 のままで、本一括は動かしていない。着地していれば FR11 の注と判定点 ③ の字が次の周で古くなる。
- **面を 1440 の幅で目で見る確認はしていない。** 一括 16 は図の名札と節点の字を動かしていないので、図の見た目は変わらない見込み。平易文と範囲の章の字が長くなった箇所の折り返しは、持ち主の walk の範囲の外として席が PR の前に開いて見る。
- **便候補 C-1〜C-3 の後の字は案である。** 生成区間の凍結 anchor（`tests/fixtures/schema/` の region の写し）との byte 一致と、便 121・便 123 の着地の後の注の字は、便の起草役が実測で決める。
