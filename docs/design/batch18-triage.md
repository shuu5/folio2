# 一括 18 の仕分け — 天井の 31 周目の所見（止める 1 件・直す 15 件・参考 4 件）

**状態: 起草・承認待ち（2026-09-24・改訂 b〔独立の検証 `~/.local/share/folio2/handoff-2026-09-24/batch18-verify.md` の blocking 2・文面 8 と、判断の記録 ADR-20 の推奨の変更への追随〕・枝 `docs/adr20` の改訂 b・c〔7ba4c8d〕の上へ載せ替え・改訂 c〔席の裁定＝取り込みは 1 本の枝で 1 回〕・規則の表の開発規律行 D-14 の止める場合 (イ) で往復を止めて出す）。** 下の 問 1〜4 が持ち主への承認要求である。問の案はどれも席の推奨の案で書いた（判断の記録 ADR-19 決定 (1) ①）。承認の記帳（要件書の承認欄と `effective_version` と `status_note`・規則の表の行 R-17 の `ruling` と `ruled_at`・判断の記録 2 本の注の見出し・状態の 1 枚と台帳）は、持ち主が答えた後に席が同じ枝の別の commit で正本の側へ書く（書く欄の全部は §「承認の後に席が書く欄」）。持ち主の逐語はこの文書にも正本にも書いていない（条 P-12.3）。

**この一括は、4 観点とも合格した枝ではない（規則の表の開発規律行 D-14 の断り）。** 31 周目は、整合が 不合格（止める 1 件・反証で 支持）、忠実さ・読みやすさ・実態の 3 観点が 合格だった。止める 1 件（整合 F-1）は 30 周目の整合 F-1 と同じ種類で、直しは判断の記録 ADR-18 の決定の字に掛かり、判断の記録 ADR-20（提案中・枝 `docs/adr20` に在る・承認要求 `docs/design/adr-20.md`）の発効が要る。これは行 D-14 の止める場合 (イ)（判断の記録の発効が要るとき）に当たる。だから枝の上の周を回さずに往復をここで止め、31 周目の所見と直しを持ち主へ出す（行 D-14・判断の記録 ADR-19 決定 (3)）。**この枝の新しい字は、どの周もまだ読んでいない。**

**承認の後の手順。** ① 持ち主が ADR-20 の承認要求の問 1・問 2 と、この一括の問 1〜3 に答えたら、席がこの枝（`docs/batch18`）の上で ADR-20 の発効の記帳（`docs/design/adr-20.md` §3）を書く。② 同じ枝の別の commit で一括 18 の記帳（§「承認の後に席が書く欄」）を書く。③ **1 本の枝で 1 回だけ本流へ取り込む**＝この枝の取り込みの要求（PR #308）の base を main に変えて取り込み、ADR-20 の枝の取り込みの要求（PR #307）は #308 に含まれるので取り込まずに閉じる（席の裁定・改訂 c）。④ 取り込みの時刻で窓を閉じる（行 D-14）。⑤ 本流で天井の 32 周目を回す（ADR-20 の発効〔判断の記録の状態と改訂の欄〕と、この一括の引き金の欄の変更のどちらも周の引き金なので、門は まだ分からない を返す）。ADR-20 の発効の後の便（`docs/design/adr-20.md` §5・前文と schema 節を引き金の定数に足す案 d の便と生成区間の注の字・門が まだ分からない のまま出す扱いは ADR-20 の 🔴 問 2）と、この一括の C の分（整合 F-5）は、32 周目の前に着地させる。

起草 2026-09-24・orchestrator 席（起草は opus 5.5 の起草役に委ねた。正本の作成の行は席を責任者として「orchestrator 席（AI・fable 5.1）」と書く＝要件書 第 1.35 版〜第 1.38 版の作成の行と同じ）・作業場所 `.worktrees/planner-batch18`（branch `docs/batch18`・判断の記録 ADR-20 の枝 `docs/adr20` 6928f70〔本流 ca60873 + ADR-20 proposed〕から切り、改訂 b の後に `docs/adr20` の改訂 b・c の先頭 7ba4c8d の上へ載せ替えた〔`git rebase --onto`・衝突なし・本枝は ADR-20 の file を触っていない〕）。参考値の base は ca60873 + ADR-20 改訂 c（7ba4c8d）である。

材料は天井の 31 周目の所見である（周 = `~/.local/share/folio2/ceiling/2026-09-24-round31/`・観点 4・束は main ca60873 で組んだ・正は各観点の `2026-09-24-round31/<観点>/findings.yaml`・止める 1 件の反証の理由は `refute-notes/coherence-F-1.md`）。`findings-all.txt` の 20 行は各観点の `findings.yaml` と id・重さ・場所が一致する。所見の数は `findings.yaml` から数え直した。

| 観点 | 判定 | 止める | 直す | 参考 |
|---|---|---|---|---|
| 整合 | 不合格 | 1 | 6 | 1 |
| 忠実さ | 合格 | 0 | 2 | 1 |
| 読みやすさ | 合格 | 0 | 5 | 0 |
| 実態 | 合格 | 0 | 2 | 2 |
| 計 | — | 1 | 15 | 4 |

**止める 1 件の反証は 支持（反証役の結果 `2026-09-24-round31/coherence/refute/F-1/result.yaml`・2026-09-24T06:58:47Z・理由 `refute-notes/coherence-F-1.md`）。**

**同じ箇所か同じ落としを 2 つ以上の所見が指す組が 2 つ在る。** それぞれ 1 つの直しにまとめた。

- 整合 F-1（止める）・整合 F-2 — 判断の記録 ADR-18 の決定 (1) と (2) の食い違いと、決定 (1) ① が前文と schema 節を含まないこと。30 周目の整合 F-1・F-4 と同じ種類で、判断の記録 ADR-20 が閉じる（問 4）。
- 整合 F-6・忠実さ F-2 — 設計ノートの見本の口の表の索引の行の注「図の元になる索引」。問 3。

**生成区間に在る所見は 1 件である**（整合 F-5・判断の記録の欄の決まりの `limits_note`）。手で直せないので C（台帳 f2-648.171 の便に束ねる・条 P-6.2）。

**印は main（a70477e）の 28 周目のまま。** この枝の HEAD で `folio ceiling --gate --write-set design-intent/srs.yaml` は「まだ分からない（印が古い（引き金の要約値の欄が無い））」を返す（main ca60873 でも同じ）。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る）／**B** = 持ち主の答えを先に取る（判断の記録の発効か、要件の規範文の欄の変更）。判断の記録の分は枝に当てず、要件の規範文の欄の分は推奨の字を枝に当てて独立の項として問う／**C** = 便候補（直す先が実装か生成区間。生成区間は手で直せない＝条 P-6.2）／**D** = この一括では直さない（理由を書く）。

| 観点 | id | 重さ | place（実在を確認） | 是正の内容 | 仕分け | 問 |
|---|---|---|---|---|---|---|
| 整合 | F-1 | 止める（反証 支持） | `adr` `ADR-18.decision`（決定 (2) の末尾の文） | 判断の記録 ADR-20（提案中）の決定 (1) が、決定 (2) の「辺を足すだけの変更を引き金に数えない」を規範の欄に入らない辺の欄に限り、受入基準の確かめる要件・要件の確かめ方の中の受入基準・規則の表の行の条を引き金に数える。ADR-18 の決定の本文は書き換えず、ADR-20 の改訂の欄（狭める向き）と ADR-18 の注で断る（ADR-20 の承認要求の推奨＝案 d）。**この一括では ADR-18・ADR-20 の決定の本文を 1 字も変えない。** ADR-18 の注の新しい段（下の整合 F-7 の行）に、この所見は ADR-20 の発効で閉じることを書いた | B | 4 |
| 整合 | F-2 | 直す | `adr` `ADR-18.decision`（決定 (1) ①） | ADR-20 が、決定 (1) ① の範囲に条 A-2 の改訂の範囲に在る前文と憲法の schema 節を足し、引き金の型付きの定数にも入れる（ADR-20 の承認要求の推奨＝案 d・定数の便は発効の後）。30 周目 整合 F-4 と同じ所見。整合 F-1 と同じ扱い | B | 4 |
| 整合 | F-3 | 直す | `srs` `requirements.FR11.milestone` | FR11 の段の欄を M1 → M3。確かめる受入基準 AC9 の段の欄も M1 → M3（受入基準の段の欄は周の引き金の外）。M1 の範囲の節の作るの 2 行目を、前「契約表の節（器 scribe2 の型から導出した欄の決まりを読む）と導出物の生成・差分 0 の検査」→ 後「契約表の節（器 scribe2 の型から導出した欄の決まりを読む）」にし、作らないの側に 1 項「導出物の生成・差分 0 の検査（M1 で作ると決めた後に保留にし、M3 の口へ移した・要件 FR11・判断の記録 ADR-16 決定 (5)(7)）」を足した（改訂 b・検証の文面 d＝面の「M1 で作る」の札の下に作らないものの断りが出ないように）。FR11 の注の末尾に直しの記録の 1 文。**規範文・確かめ方・受入の判定の式は不変**。FR11 の段の欄と M1 の範囲の節は周の引き金の欄（ADR-18 決定 (1) ③） | A | 1 |
| 整合 | F-4 | 直す | `srs` `requirements.FR7.when` | 起きるときの欄を、前「folio build が実行されたとき」→ 後「folio build が実行されたとき（規範文の前半）と、folio serve が起動されたとき（後半の配信の接続先）」。shall の字と確かめ方は変えない。FR7 の注の末尾に直しの記録の 1 文（根拠の条 N-6.1 は serve の起動ごとに掛かり、確かめ方の欄が見るのも serve の起動）。**起きるときの欄は規範文の欄なので B とし、問 1 の独立の項にした**（推奨の字は枝に当てた。退けたら元の字に戻す）。folio serve は配信先を生成しない（`crates/folio/src/serve.rs` は配信だけ）ので、起きるときを「build か serve」と並べると前半の生成が serve でも起きると読めるため、前半と後半に分けて書いた | B | 1 |
| 整合 | F-5 | 直す | `adr` `schema.limits_note` | 生成区間（正本は `crates/folio/src/floor_adr.rs` の定数・凍結の写し `tests/fixtures/schema/adr-region.txt`）。判断の記録 ADR-2 の注と一括 16 の C が既に台帳 f2-648.171 の便に回している字（「P-10.1（live は M0）は未発効」・「M0 で床の入力に取り込む」・「条の廃止…M0 で決める」）。f2-648.171 の notes に 31 周目 整合 F-5 を足す（承認の後の欄の 11） | C | — |
| 整合 | F-6 | 直す | `design-note` `example.sections.3.rows.index.note` | 前「…図の生成の 3 つの口（検査・描画・埋め込み）とは別の口で、図の元になる索引を返すので同じ表に並べる」→ 後「…図の生成の 3 つの口（検査・描画・埋め込み）とは別の口で、図の入力ではない（図の元は節 2 の型付き記述）。口の表の見本として同じ表に並べる」。見本の先頭の注（YAML の注釈）に「口の表の索引の行だけは要件書 FR14 の口を見本として並べたもので、図の生成の口ではない」を足した。契約表の行 a の題（口 3 つ）は変えない。`folio derive --check` は一致のまま（導出物に口の表は入らない） | A | 3 |
| 整合 | F-7 | 直す | `adr` `ADR-18.decision`（決定 (4) の括弧の条 P-15.2） | **決定の字は変えず、注で断った。** ADR-18 の注の末尾に【天井の 31 周目の所見への応答（一括 18・…・持ち主の承認待ち）】の段＝決定 (4) の条 P-15.2 は同じ関数で測る形の引き合いで、印と門を編集の時点で止める仕掛けと読ませるものではない。印（--stamp）と門（--gate）はどちらも編集時の仕掛けではなく、憲法の条 P-3 の機構の注のとおり床の性質を持つ命令なので、規則の表の行の注が書く folio2 側の編集時の仕掛けの数（0 本）は変わらない（改訂 b・検証の文面 f）。同じ関数で測る根拠は条 P-6.3 と条 P-3.1 の側に在る。同じ段に、整合 F-1・F-2 は ADR-20 の発効で閉じることを書いた。**見出し・文脈・決定・案・帰結・撤退条件・承認欄は 1 字も触っていない**（発効済みの判断の記録の食い違いは注で断る＝一括 13〜17 と同じ扱い） | A | 3 |
| 整合 | F-8 | 参考 | `srs` `requirements.FR18.shall` | 直さない（§D の理由） | D | — |
| 忠実さ | F-1 | 直す | `adr` `ADR-16.plain` | 前「2 つがどちらも無いときにだけ使える命令で、同時に作れるようにします。」→ 後「2 つがどちらも無く、ほかの検査にも引っかかりが無い（違反も「まだ分からない」も無い）ときにだけ使える命令で、同時に作れるようにします。」。注の末尾に【天井の 31 周目の所見への応答】の段。**見出し・文脈・決定・案・帰結・撤退条件・承認欄は 1 字も触っていない** | A | 3 |
| 忠実さ | F-2 | 直す | `design-note` `example.sections.3.rows.index.note` | 整合 F-6 と同じ直しで閉じた | A | 3 |
| 忠実さ | F-3 | 参考 | `vocabulary` `terms.batch.def` | 前「規範の欄を動かさない注だけの是正は、承認を求めず記帳して取り込む。」→ 後「持ち主に問う項を持たず規範の欄を動かさない一括（注だけの是正）は、枝の上の周が合格した後に、承認を求めず記帳して取り込む。」＝判断の記録 ADR-19 決定 (1) ⑧ と行 D-14 の 2 つの条件を添えた。参考だが、字を 1 句足すだけで読み違いが消え、同じ定義が次の周でまた拾われる見込みがあるので A にした | A | 3 |
| 読みやすさ | F-1 | 直す | `srs` `scope_m3.build` | 要件書と憲法の面の字の「ADR-n」を判断の記録の面（`adr-N.html`）への導線にし、上の帯にも判断の記録への入口を置く直しは、面の生成器（`crates/folio/src/face_srs.rs`・`face.rs` の憲法の面）の便である。正本の字は正しい。台帳の便候補の項 f2-648.182 を起こした。今は入口の棚から辿れるので、読み手は行き止まりにはならない | C | — |
| 読みやすさ | F-2 | 直す | `srs` `scope.not_build` | M2 の枠を立てるには、要件書の最上位の節の閉じた一覧（生成区間・正本は `crates/folio/src/check.rs`）に任意の節 `scope_m2` を足し、面の生成器の段の範囲の節の並び（`crates/folio/src/face_srs.rs` の `scope_chapter`・今は M0・M1・M3）と周の引き金の定数（`crates/folio/src/ceiling.rs` の `TRIGGER_SRS_WHOLE`）にも足す便が要る。章の頭の断りも、今の面は範囲の章の頭に正本の字を出す口を持たない（見出しは固定の字）。どちらも正本の字だけでは直せないので C。中身は指す先（要件 FR17 / FR18・判断の記録 ADR-8）だけでよい。台帳の便候補の項 f2-648.183 を起こした。M0 の作らないの 4 つ目の項の末尾の断りは今のまま残る | C | — |
| 読みやすさ | F-3 | 直す | `srs` `requirements.FR13.plain` | 前「scribe2 が commit の末尾に書く印だけを見て答えます。」→ 後「scribe2 が版管理に変更を記録するとき（その 1 回分の記録を commit と呼びます）に、その記録の末尾に書く印だけを見て答えます。」。語彙には足さない（commit は英字の識別子の側の版管理の操作名で、平易文の中の言い換えで閉じる）。FR13 の注の末尾に直しの記録の 1 文。規範文は変えない | A | 1 |
| 読みやすさ | F-4 | 直す | `srs` `requirements.FR20.plain` | 並びを改めた＝① 便を出す前に印を見て 通す・止める・まだ分からない の 3 通りに答える、② 3 通りの条件（今の字のまま）と対象外、③ 印の説明、④ 規範の欄の中身の順。**規範の欄の中身の文（「規範の欄とは、…在ります。」）は第 1.38 版の字を逐語のまま最後へ移した**（改訂 b・検証の blocking 2＝一括 17 の問 2 で FR20・ADR-18 の平易文と語彙の 3 か所を揃えた言い換えを 1 か所だけ縮めると割れるため）。変えた字は、先頭の答えの文・条件の文の「規範の欄（この段落の最後の文に書く 5 つ）」の括弧・印の説明の文の 3 つだけ。**一括 17 の問 2 で採らなかった「一覧を指すだけにする」形（代替案 (A)）には戻していない**。FR20 の注の末尾に直しの記録の 1 文。規範文・確かめ方・受入の判定の式は不変 | A | 1 |
| 読みやすさ | F-5 | 直す | `rules` `thresholds.R-17.what` | what の頭に平易な 1 文を足し、対が何と何の組かを名指した＝後「散文（注や平易文など）で別の行を指したのに、指した行と指された行のどちらの表の欄（型付きの欄）にもその指しが書かれていないものの数。詳しくは、設計文書の中で id を持つ行の散文の欄に現れた、その行以外の id のうち、どちらの行の型付きの欄にも無いものの数。対（id を持つ行と、その散文に出た別の id の組）は向きを問わない（片方の行の欄に相手が在れば満たす）」。後半は前の字のまま。**規則の表の行の what の変更なので、裁定の id と時刻が要る（条 P-17.1）**＝`ruling` の頭を「一括 18 の承認待ち（…）」にした（一括 17 の行 D-12・R-12 と同じ形）。値〔0 件〕・数える対象・母集団・種別・段・状態・refs は不変。値を緩めないので条 P-17.4 の検証は掛からない。what は周の引き金の欄（ADR-18 決定 (1) ④） | A | 3 |
| 実態 | F-1 | 直す | `constitution` `articles.P-7.mechanism` | 機構の注の末尾に＝「id の一覧の側の baseline は置き場の ids- の anchor の和で、最後の凍結より後に足した要件・受入基準の id と判断の記録の id は baseline に無いので、その消失と付け替えはまだ数えない（床は違反も まだ分からない も出さない・凍結し直すまでは人が見る）。いまの baseline は要件書 第 1.24 版の時点に凍結した ids-v1.24 の 1 本である。凍結し直す時機を決める口は持たない（後続・台帳の控え）。」（改訂 b・検証の文面 b＝一般の形を先に、今の実例を後に）。台帳の控え＝f2-648.186。実測＝`crates/folio/src/ids.rs` の頭の注（baseline は `anchors/` の `ids-*.yaml` の和）・`design-intent/anchors/` の ids- は `ids-v1.24.yaml` の 1 本。**機構の欄は条文の 5 欄でも schema 節でも前文でもない**ので、条 A-2.2 により改訂に当たらず、憲法の版は上がらない（一括 14〜17 の憲法の機構の注の先例と同じ）。kind・live・段・極性は不変。凍結し直すこと自体（`folio check --freeze-ids`）は本一括に入れない（§この一括に入れなかったもの） | A | 2 |
| 実態 | F-2 | 直す | `constitution` `articles.P-12.mechanism` | 機構の注に、数える承認欄の範囲を書いた＝「数えるのは、判断の記録と設計ノートの承認欄（欄 who・date・ruling・verbatim・surface の欄充足）と、憲法の meta.approval（凍結 anchor の写しとの一致）である。要件書・入口・天井の正本・相談窓口・索引の欄の決まりの正本の meta.approval の行（欄 role・who・when・stamp・verbatim・version）は裁定 id と対話面の欄を持たず、床はその欄充足をまだ数えない（後続・台帳の控え）。支度表は設計文書の正本に数えない（語彙の設計文書の項）。その行の逐語と、stamp の字に書く裁定 id と対話面は人が確かめる。」（改訂 b・検証の blocking 1＝索引の欄の決まりの正本 graph.yaml の承認欄の行も同じ形で床が数えない。支度表 intake-sheet.yaml も承認欄を持つが、設計文書の正本に数えないので列挙に入れず 1 句で断った）。台帳の控え＝f2-648.187実測＝`crates/folio/src/floor_note.rs` の `APPROVAL_REQUIRED`・`crates/folio/src/adr.rs` の `check_approval`・`crates/folio/src/anchor.rs` の meta_approval の突き合わせ。条文の 5 欄・schema 節・前文の外（条 A-2.2）。kind・live・段・極性は不変 | A | 2 |
| 実態 | F-3 | 参考 | `rules` `thresholds.R-17.note` | 直さない（§D の理由）。台帳の控え f2-648.184 を起こした | D | — |
| 実態 | F-4 | 参考 | `srs` `requirements.FR16.shall` | 直さない（§D の理由）。台帳の控え f2-648.185 を起こした | D | — |

**数。** A = 11（整合 F-3・F-6・F-7／忠実さ F-1〜F-3／読みやすさ F-3〜F-5／実態 F-1・F-2）・B = 3（整合 F-1・F-2 は判断の記録 ADR-20／整合 F-4 は要件の起きるときの欄）・C = 3（整合 F-5・読みやすさ F-1・F-2）・D = 3（整合 F-8・実態 F-3・F-4）。計 20。整合 F-6 と忠実さ F-2 は同じ直しだが、所見ごとに A と数えた。A のうち参考は 1 件（忠実さ F-3）。

**B が判断の記録の分と要件の規範文の欄の分の 2 種を持つ理由。** 整合 F-1・F-2 は判断の記録 ADR-20 の発効が要り、この一括の枝には字を当てない（問 4）。整合 F-4 は要件の規範文の欄（起きるときの欄）を動かす直しなので、A と分けて問 1 の独立の項として問い、推奨の字は枝に当てた（退けたら元の字に戻す）。

## D（直さない）の理由

| id | なぜ直さないか |
|---|---|
| 整合 F-8（要件 FR18 に面の天井の名札の添え書き〔印の後に変わった所はまだ読まれていない〕の口が無い） | 参考で、所見自身も「要件に足すかを問うこと」を提案している。添え書きの義務の正本は判断の記録 ADR-18 決定 (6) で、便イ（台帳 f2-648.179）で着地している。要件 FR18 の規範文に足すと、要件が増え（義務の文が 1 つ増える）、確かめる受入基準に場合を足すかも問うことになり、この一括の範囲（31 周目の直し）を越える。要件書の側が決定 (6) を写さないことは、今は食い違いでなく書き落としで、規範の向きは決定 (6) が持つ。次に FR18 を開く版で、要件の補い（第 1.24 版で面の名札の口を足した先例と同じ形）として問う |
| 実態 F-3（行 R-17 の注の「写しを設計文書の置き場へ導出することは後続（P-5.6）」の後続が台帳に無い） | 正本の字は正しい（後続と書いている）。欠けているのは台帳の控えで、正本を直す所見ではない。行 D-11 の範囲（判定に使う閉じた一覧）の定数なので、席が台帳に控え f2-648.184 を起こした（承認に依らない） |
| 実態 F-4（`folio face --help` と `folio check --help` の説明の字が古い） | 文書の側は正しく、直す先は `crates/folio/src/main.rs` の説明の字（実装）。所見どおり、配信の命令の説明（tailnet の内側だけ）は判断の記録 ADR-12 の帰結が次に配信の実装を触る便へ回しているので、同じ便に束ねるのがよい。席が台帳に控え f2-648.185 を起こした（承認に依らない） |

## 便候補（C）の中身

- **整合 F-5 ＝ 判断の記録の欄の決まりの生成区間の `limits_note`。** 台帳 f2-648.171（便 ③ の後続・生成区間の注の便）に束ねる。直す先は `crates/folio/src/floor_adr.rs` の床の定数の字と、`design-intent/adr/schema.yaml` の生成区間（`folio schema --write`）と、凍結の写し `tests/fixtures/schema/adr-region.txt`。30 周目 実態 F-4・F-5（同じ `limits_note`）も同じ項に在る。判断の記録 ADR-20 の発効の後の便（`docs/design/adr-20.md` §5・引き金の定数と `graph.rs`・`ceiling.rs` の注の文字列）とは書き換える file が重ならないが、同じ「生成区間の注の字」を持つ便なので、1 本に束ねるかは契約で決める。32 周目の前に着地させる（行 D-14・枝に書けない所見は窓を閉じた後、次の周の前に便で運ぶ）。
- **読みやすさ F-1 ＝ 要件書と憲法の面の ADR-n を判断の記録の面への導線に。** 面の生成器の便。字の「ADR-n」を `adr-N.html` へのリンクにする場合、発効していない判断の記録（面を生成しない番号）の扱いと、上の帯に判断の記録の入口を置くかを契約で決める。面の見た目の直しの便の数え（判断の記録 ADR-5 の撤退条件 ②）に当たるかも契約で確かめる。
- **読みやすさ F-2 ＝ 要件書の範囲の章に M2 の枠。** 要件書の最上位の節の閉じた一覧（`check.rs`・生成区間）に任意の節 `scope_m2`、面の段の範囲の節の並び（`face_srs.rs`）、周の引き金の定数 `TRIGGER_SRS_WHOLE`（`ceiling.rs`・天井の正本の生成区間の写しと凍結の写し）に足す便。引き金の定数を足すのは判断の記録 ADR-18 決定 (1) ③ の「範囲の節」の中なので、決定は変えない。便の着地の後に、要件書の版で `scope_m2`（中身は要件 FR17 / FR18 と判断の記録 ADR-8 を指すだけ）を足し、M0 の作らないの 4 つ目の項の断りを外す。

## 運び方（規則の表の開発規律行 D-14 の窓）

- **取り込みは 1 本の枝で 1 回である（席の裁定・改訂 c）。** ADR-20 の分と一括 18 の分の承認の記帳はどちらもこの枝に書き、PR #308（base を main に変える）で 1 回だけ本流へ取り込む。PR #307（ADR-20 の枝）は #308 に含まれるので閉じる。窓はこの 1 回の取り込みの時刻で閉じる。
- 31 周目は不合格なので、本一括は行 D-14 の窓の下で運ぶ。行 D-14 の原則は、承認を求める前に枝の上で周を回し直し、4 観点とも合格した枝だけを 1 回の承認要求で出すことである。**本一括はその例外で、止める 1 件の直しが判断の記録 ADR-20 の発効を要するので、行 D-14 の止める場合 (イ) で、枝の上の周を回さずに往復を止めて出す（頭の断り）。** 窓の間は、本流の設計文書を書き換える便を器へ出さない。
- **本一括が周の引き金の欄を動かすのは 4 か所である**（要件 FR11 の段の欄・要件 FR7 の起きるときの欄・M1 の範囲の節〔節を丸ごと数える〕・規則の表の行 R-17 の what）。受入基準 AC9 の段の欄、行 R-17 の裁定の欄、平易文・注・語彙・機構の注・設計ノートの見本は引き金の外である（ADR-18 決定 (1)・天井の正本の生成区間 `trigger`）。ADR-20 の発効（判断の記録の状態と改訂の欄）と、発効の後の案 d の便（前文と schema 節を引き金の定数に足す）も引き金の要約値を動かす。だから承認の後に本流で 32 周目を回す（頭の手順 ⑤）。
- 行 D-14 の止める場合のうち、31 周目の分の直しに条文の改訂・消す操作・外へ出す操作は無い。判断の記録の発効が要るのは整合 F-1・F-2 の分だけで、それは ADR-20 の承認要求が問う（問 4）。**行 R-7 の数え（止める場合 (ア)）には当たらない**＝同じ観点と場所の 止める（整合 × ADR-18.yaml）が 30・31 周目で行 R-7 の回数（2 回）に達した（超えてはいない・窓の中では 1 回）ので (ア) には当たらず、(イ) で止めて出す。一括 17 の窓は取り込み（ca60873）で閉じたので、行 D-14 のとおり一括 18 の窓の数えは 31 周目の 1 から始まる（改訂 b・検証の文面 g・改訂 c で字を揃えた）。
- 32 周目で整合 F-1・F-2 と同じ所見が再び立ったら、ADR-20 の発効（改訂の欄と ADR-18 の注の断り）と案 d の便が本流に在るかを先に確かめる。在れば新しい所見として扱い、無ければ ADR-20 の運びの遅れとして扱う。どちらでも 32 周目は新しい窓（取り込みで一括 18 の窓を閉じた後）なので、行 R-7 の数えは 1 から始める。

## 問どうしの依存

| 問 | 依存と、退けたときに席が戻すもの |
|---|---|
| 問 1 | **要件書 第 1.39 版の中身は問 1 の答えで決まる。** 中の項は互いに独立である。退けた項は元の字に戻して発効し（席が承認の後の commit で戻す）、要件書の来歴（`meta.status_note` の v1.39）と作成の行の stamp からも外す（2 か所とも一括全部の要旨を持つ）。問 1 を全部退け、ADR-20 も発効しないなら版を上げない＝`meta.version` を v1.38 に戻し、来歴の v1.39 の項と作成の行と、`status_note` の頭の「第 1.39 版（一括 18）は起草・承認待ちです。」を外す。FR7 の起きるときの欄（項 2）だけを退けたら、起きるときの欄を「folio build が実行されたとき」に戻し、FR7 の注の末尾の 1 文を外す |
| 問 1 と問 4（ADR-20） | **要件書の版は ADR-20 と同じ版にまとめる。** ADR-20 の承認要求 §3 の 8 は、発効の後に要件 FR20 の注に 1 文を足し、欄 adrs に ADR-20 を足して要件書の版を 1 つ上げる。本一括の枝は ADR-20 の枝を含み、ADR-20 の記帳もこの枝に書いて 1 回（PR #308）で本流へ取り込み、PR #307 は閉じる（席の裁定・改訂 c）。だから第 1.39 版を「一括 18 と判断の記録 ADR-20 の発効」の版にする（来歴と作成の行と承認の行の stamp に ADR-20 を足す）。ADR-20 だけを先に承認して一括 18 の答えが別の回になったときも、取り込みは答えが揃ってからの 1 回で、版は第 1.39 版に同居させる（一括 18 の問 1 を全部退けたら、第 1.39 版は ADR-20 の分だけの版になる）。FR20 の平易文（本一括）と FR20 の注の 1 文（ADR-20）は別の欄で、互いに依らない。**ただし FR20 の平易文は、一括 18 の並べ替えの後の字に、ADR-20 の発効で「憲法の条文」の後に「と前文と schema 節」が足される**（ADR-20 の承認要求 §3 の 12・ADR-18 の平易文も同じ向きで §3 の 3）。両方を承認したら、席は一括 18 の字の上に ADR-20 の句を当てる（ADR-20 の承認要求 §3 の 8 と §5 は、ADR-20 の改訂 d で同じ 1 回の取り込みの字に直る） |
| 問 2 | **ほかの問に依らない。** 退けた注は前の字に戻す。条 P-7 の注を退けても、凍結し直す時機の控え（f2-648.186）は台帳に残す |
| 問 3 | **中の項は互いに独立である。** 退けた項は元の字に戻す。行 R-17 の what（項 5）を退けたら、what と `ruling` の頭を前の字に戻す（`ruled_at` は元も 2026-09-23）。ADR-18 の注の段（項 3）を退けたら、段ごと外す。そのとき整合 F-1・F-2 が ADR-20 で閉じることの記録は、ADR-20 の発効の記帳が ADR-18 の注に書く【判断の記録 ADR-20 による改訂】の段（ADR-20 の承認要求 §3 の 4）だけになる |
| 問 3 と問 4 | **ADR-18 の注の新しい段は「起草中の判断の記録 ADR-20 の発効で閉じる」と書いた。** ADR-20 を発効したら、この句を「判断の記録 ADR-20（<日付> 発効）で閉じた」に書き換える（承認の後の欄の 6）。ADR-20 を退けたら、この句を「判断の記録 ADR-20 は退けられ、整合 F-1・F-2 は未解決（次の判断の記録で扱う）」に書き換える |
| 問 4 | **この一括では問わない。** 答えは ADR-20 の承認要求の問 1・問 2 の答えを使う。ADR-20 をどの案で発効しても、本一括の A と C の字は変わらない（ADR-18 の注の段の句だけが上の行のとおり変わる） |

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 判断の記録 ADR-18・ADR-20 の決定の本文 | 席の裁定で変えない。ADR-20 の推奨（案 d）も ADR-18 と ADR-13 の決定の本文を書き換えず、ADR-20 の改訂の欄と両者の注で断る |
| 判断の記録の欄の決まりの `limits_note`（整合 F-5） | 生成区間。台帳 f2-648.171 の便に足す（§C の中身） |
| 面の導線と M2 の枠（読みやすさ F-1・F-2） | 面の生成器と最上位の節の一覧の便（§C の中身） |
| id の一覧の凍結し直し（`folio check --freeze-ids`）と、凍結し直す時機を行にすること | 条 P-7 の機構の注は数えの外を正直に書く直しに留めた。凍結し直すと `design-intent/anchors/` に新しい凍結 anchor が増え（消さない・上書きしない）、その版は発効した版でなければならない。時機を決めると開発の規律の行が 1 つ増える（条 N-2.1＝型付きの行に置く）。どちらも本一括の範囲（31 周目の直し）を越えるので、台帳の控え f2-648.186 で別に問う |
| 要件書・入口・天井の正本・相談窓口・索引の欄の決まりの正本の承認欄の行に裁定 id と対話面の欄を足し、床で欄充足を数えること | 5 つの正本の欄の決まりと床の定数と凍結の写しが動く便で、本一括の範囲を越える。条 P-12 の機構の注は今の数える範囲を書く直しに留めた（台帳の控え f2-648.187） |
| 要件 FR18 に面の名札の添え書きを足すこと（整合 F-8） | D（§D の理由） |
| `crates/folio/src/main.rs` の命令の説明の字（実態 F-4） | D。実装の字。配信の実装を触る便へ束ねる（台帳の控え f2-648.185） |
| `docs/design/adr-20.md` §7 の最後の項（31 周目の所見に止めるは 0 件） | ADR-20 の承認要求の字で、本一括の枝では直していない。ADR-20 の起草役が枝 `docs/adr20` の改訂 b で直し（頭の起点の項に 31 周目の整合 F-1・F-2 を書いた）、この枝はその先頭 7ba4c8d の上へ載せ替えた |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.39 版の起草。** FR11 の段の欄と注・受入基準 AC9 の段の欄・M1 の範囲の節の作るの 2 行目と作らないの 1 項（整合 F-3）／FR7 の起きるときの欄と注（整合 F-4）／FR13 の平易文と注（読みやすさ F-3）／FR20 の平易文と注（読みやすさ F-4）／`meta.version` を v1.39 に・`status_note` の頭に「第 1.39 版（一括 18）は起草・承認待ちです。」・末尾に v1.39 の起草・`approval` に 作成 の行。**`effective_version` は v1.38 のまま・承認の行は書いていない** |
| `design-intent/constitution.yaml` | 条 P-7 と条 P-12 の機構の注（実態 F-1・F-2）。**条文と schema 節・前文と、機構の欄の kind・live・段・極性は 1 字も触っていない** |
| `design-intent/rules.yaml` | 行 R-17 の what と `ruling` の頭（読みやすさ F-5）。値・数える対象・母集団・種別・段・状態・refs・注・`ruled_at` は不変 |
| `design-intent/vocabulary.yaml` | 一括の項の定義の 1 文（忠実さ F-3）。語彙の項は増やしていない |
| `design-intent/adr/ADR-16.yaml` | `plain` の (3) の命令の条件（忠実さ F-1）と `note` の末尾の段 |
| `design-intent/adr/ADR-18.yaml` | `note` の末尾の段（整合 F-7 と、整合 F-1・F-2 の在り処） |
| `design-intent/design-note/example.yaml` | 口の表の索引の行の注と、先頭の注釈の 1 文（整合 F-6・忠実さ F-2） |
| `docs/design/batch18-triage.md` | **新設**（この file） |

判断の記録 2 本（ADR-16・ADR-18）の**見出し・文脈・決定・案・帰結・撤退条件・承認欄・状態は 1 字も触っていない**（ADR-16 は平易文と注、ADR-18 は注だけ）。判断の記録 ADR-20 と承認要求 `docs/design/adr-20.md`・`docs/design/adr-20-grill.md` は 1 byte も触っていない。**実装（`crates/`）・凍結 anchor（`tests/fixtures/`・`design-intent/anchors/`）・生成区間・印（`preview/ceiling-stamp.yaml`）・歯は 1 byte も触っていない。** 入口・天井の正本・相談窓口の正本・索引の欄の決まりの正本・支度表の正本も触っていない。

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.39 版（FR11 の段・FR7 の起きるとき・FR13 と FR20 の平易文）

**やりたいこと。** 要件書を第 1.39 版に上げ、次の 4 つを直す。**shall（規範文の本文）と受入の判定の式は 1 字も変えない。** 動く規範文の欄は項 2 の FR7 の起きるときの欄だけである。

1. 要件 FR11（導出物を生成し、差分 0 を数える）の段の欄を M1 から M3 にする。確かめる受入基準 AC9 の段も M3 にし、M1 の範囲の節の作るの 2 行目から導出物の生成・差分 0 の検査を外して、作らないの側に「M1 で作ると決めた後に保留にし、M3 の口へ移した」の 1 項を足す（整合 F-3）。
2. **（規範文の欄が動く項）** 要件 FR7（組み立てて、見せる）の起きるときの欄を「folio build が実行されたとき（規範文の前半）と、folio serve が起動されたとき（後半の配信の接続先）」にする（整合 F-4）。
3. 要件 FR13 の平易文の英字の commit に「版管理に変更を記録した 1 回分」の言い換えを添える（読みやすさ F-3）。
4. 要件 FR20（天井の門）の平易文を、答えの 3 通りと門の条件を先に、印の説明を次に、規範の欄の中身を最後に置く順にする。規範の欄の中身の文は第 1.38 版の字のまま最後へ移し、字は変えない（読みやすさ F-4）。

**理由。** 1 は、同じ要件が M1（段の欄）と M3（M3 の範囲の節・判断の記録 ADR-16 決定 (5)(7)）の 2 つの段に読めていた。語彙の M の項は各要件がどの段のものかを欄で持つと定め、その欄は周の引き金の欄でもある。2 は、起きるときが build の実行だけなので、serve の接続先の縛りが build の実行を条件にしているように読めた。根拠の条 N-6.1 は serve を起動するたびに掛かり、確かめ方の欄が見ているのも serve の起動である。3 は、非エンジニアあての平易文に言い換えの無い英字が出ていた。4 は、平易文の 1 段落に門の目的・印の中身・規範の欄の長い一覧・3 通りの答え・対象外が詰まり、要旨（便の前に印を見て 3 通りに答える）が埋もれていた。

**代替案。**
- **(A) 2 を起きるときの欄でなく注の断りで直す**（「規範文の後半は serve の起動ごとに掛かる」を注に書き、起きるときの欄は変えない）。規範文の欄は 1 つも動かない。ただし起きるときの欄と後半の縛りの食い違いは字のまま残り、次の周の整合が同じ所を拾う見込みがある。
- **(B) 4 を、規範の欄の一覧を ADR-18 決定 (1) を指す 1 文だけにする**（読みやすさ F-4 の勧めの字どおり）。一番短い。ただし一括 17 の問 2 で採らなかった形（非エンジニアが平易文だけで何が変わると周が回るかを読めない）に戻り、ADR-18 の平易文と語彙の言い換えとも割れる。

**コスト。** 承認 1 回。段の欄 2（FR11・AC9）・範囲の節の行 2（作るの 1 行の短縮と作らないの 1 項）・起きるときの欄 1（FR7）・平易文 2（FR13・FR20）・注 4（FR7・FR11・FR13・FR20）。**要件の番号は 1 つも動かず（条 P-7.1）、義務は 1 つも増えない。** 周の引き金の欄が動くのは FR11 の段の欄・M1 の範囲の節・FR7 の起きるときの欄の 3 か所（どれも ADR-20 の発効と同じ 32 周目で読む）。床の歯は 1 本も直さない（枝の HEAD で 855 本すべて通る）。

**推奨 = 1〜4 を当てる（代替案は採らない）。**

### 🔴 問 2. 憲法の条 P-7 と条 P-12 の機構の注（数える範囲を実態に合わせる）

**やりたいこと。** 憲法の機構の注 2 か所を、床が実際に数える範囲の字にする。**条文（id・見出し・規範文・段・縛る相手）と schema 節・前文は 1 字も触らない＝条 A-2.2 の改訂に当たらず、憲法の版は上がらない。** 機構の欄の kind・live・段・極性も変えない。

1. 条 P-7（番号を再利用しない）の注に、id の一覧の側の基準（凍結 anchor）は最後の凍結までの id しか持たず、その後に足した要件・受入基準・判断の記録の id の消失と付け替えはまだ数えない（床は違反も「まだ分からない」も出さない・凍結し直すまで人が見る）ことを書き、今の基準は要件書 第 1.24 版の時点の 1 本だと添える（実態 F-1）。
2. 条 P-12（承認は 1 つの面から、言葉どおりに残す）の注に、床が承認欄の欄充足を数えるのは判断の記録と設計ノートの承認欄と憲法の承認欄だけで、要件書・入口・天井の正本・相談窓口・索引の欄の決まりの正本の承認欄の行は裁定 id と対話面の欄を持たず床がまだ数えないこと、その行は人が確かめることを書く。支度表は設計文書の正本に数えないと 1 句で断る（実態 F-2）。

**理由。** 今の 2 つの注は対象を限らずに「今効いている検査」と言い、条の充足が全部の id・全部の承認欄に掛かって読めた。実態の観点は、本流の写しで第 1.25 版より後の要件の番号を付け替えても、要件書の最初の版の承認の行から逐語を消しても、床が「合格」を返すことを確かめた（今の行はどれも逐語を持ち、番号の付け替えも起きていないので実害は無い）。黙る範囲を注に書けば、約束の字と実態が揃う。**ただし注は黙る範囲を字で見せるだけで、床の出力は 合格 のまま変わらない。** 判定できないものを「まだ分からない」として出す条 P-4.2 を床が充たすのは、台帳の控え f2-648.186 と f2-648.187 の便の後であり、この承認は条 P-4.2 の充足を約束しない。

**代替案。**
- **(A) 注は今のまま残し、黙る範囲を数える便を先に運ぶ**（id の一覧の凍結し直しを発効ごとの手順にする・5 つの正本の承認欄の行に欄を足して床で数える）。約束の字は広いまま保てる。ただし手順は開発の規律の新しい行（条 N-2.1）と、5 つの正本の欄の決まり・床の定数・凍結の写しの便が要り、32 周目までに揃わない見込みで、その間の周は同じ注を拾い直す。

**コスト。** 承認 1 回。機構の注 2 つ。`folio inject --check` の生成区間は動かない（枝の HEAD で 68 行 / 7,912 byte 一致）。周の引き金の欄は動かない（機構の欄は ADR-18 決定 (1) ① の条文の 5 欄の外）。**義務は減らない**＝床が数えるものは変わらず、注の字を正直にするだけ。黙る範囲を数える後続は台帳の控え f2-648.186・f2-648.187 に起こした（承認に依らない）。

**推奨 = 1・2 を当てる（代替案は採らない）。**

### 🔴 問 3. 語彙と判断の記録と規則の表の言い換え・断り（一括の定義・ADR-16 の平易文・ADR-18 の注・行 R-17 の何を数えるか・設計ノートの見本）

**やりたいこと。** 次の 5 つを直す。**判断の記録の見出し・文脈・決定・案・帰結・撤退条件・承認欄・状態は 1 字も触らない**（発効済みの判断の記録の食い違いは平易文か注で断る＝一括 13〜17 と同じ扱い）。

1. 語彙の「一括」の定義の「規範の欄を動かさない注だけの是正は、承認を求めず記帳して取り込む」を、「持ち主に問う項を持たず規範の欄を動かさない一括（注だけの是正）は、枝の上の周が合格した後に、承認を求めず記帳して取り込む」にする（忠実さ F-3）。
2. 判断の記録 ADR-16 の平易文の「2 つがどちらも無いときにだけ使える命令」を、「2 つがどちらも無く、ほかの検査にも引っかかりが無い（違反も「まだ分からない」も無い）ときにだけ使える命令」にし、注の末尾に段を足す（忠実さ F-1）。
3. 判断の記録 ADR-18 の注の末尾に段を足す。決定 (4) の括弧の条 P-15.2（編集の時点で止める仕掛けの条）は同じ関数で測る形の引き合いで、印と門は編集時の仕掛けではない（床の性質の命令・規則の表の行の注が書く編集時の仕掛けの数 0 本は変わらない）、根拠は条 P-6.3 と条 P-3.1 の側に在ると断る。同じ段に、整合 F-1・F-2 は判断の記録 ADR-20 の発効で閉じると書く（整合 F-7）。
4. 規則の表の行 R-17 の「何の数値か」の頭に、「散文（注や平易文など）で別の行を指したのに、指した行と指された行のどちらの表の欄にもその指しが書かれていないものの数」の 1 文を足し、対が「id を持つ行と、その散文に出た別の id の組」だと名指す（読みやすさ F-5）。**規則の表の行の変更なので、裁定の id と時刻が要る（条 P-17.1）。** 値〔0 件〕・数える対象・母集団・種別・段・状態は変えない。
5. 設計ノートの見本の口の表の索引の行の注の「図の元になる索引を返すので同じ表に並べる」を、「図の入力ではない（図の元は節 2 の型付き記述）。口の表の見本として同じ表に並べる」にする（整合 F-6・忠実さ F-2）。

**理由。** 1 は、同じ定義が正本を行 D-14 と名指すので義務は動かないが、定義だけを読むと「問う項を持たない」「枝の上の周の合格の後」の 2 つの条件が落ちて読めた。2 は、平易文だけを読むと、始まりの凍結の命令が断る場合（ほかの検査に違反か「まだ分からない」が在る）が 1 つ見えなかった（要件 FR24 の平易文は第 1.35 版でこの前提を持っている）。3 は、印と門の根拠に条 P-15.2 を引くと門を編集時の仕掛けと読ませ、規則の表が数える編集時の仕掛けの数（0 本）と食い違って読めた。決定の字は発効済みなので注で断る。4 は、入口の「決める人」の読む順番が案内する憲法 §5 数値の表で、非エンジニアが何を数える行か取れなかった。5 は、正本のどこにも「索引が図の元」とする文が無く、図の元は文書の図の節の型付き記述である（判断の記録 ADR-4 決定 (1)・要件 FR15）。見本の先頭の注の「FR15 の言い換えで新しい決定を含まない」とも合わなかった。

**代替案。**
- **(A) 3 を、ADR-18 の決定 (4) の括弧の字を直す形で閉じる**（判断の記録の改訂の欄を持つ新しい判断の記録か、ADR-20 に足す）。字は決定の中で揃う。ただし判断の記録の発効が要り（行 D-14 の止める場合 (イ)）、ADR-20 の本文を変えることになる（席の裁定で変えない）。
- **(B) 4 を、行の注に平易な説明を足す形にする**（何の数値かの欄は触らない）。裁定の id が要らない（注は引き金の外）。ただし憲法 §5 の数値の表が出すのは何の数値かの欄で、読み手が困った所は直らない。

**コスト。** 承認 1 回。語彙の定義の 1 句・判断の記録の平易文の 1 文と注の段 2・規則の表の行 1 つの何の数値かの欄と裁定の欄の頭・見本の注 1 と注釈 1。**義務は 1 つも増えない。** 周の引き金の欄が動くのは 4 の行 R-17 の何の数値かの欄だけ（ADR-18 決定 (1) ④）。承認の後に行 R-17 の裁定の id と時刻と逐語を席が書く。

**推奨 = 1〜5 を当てる（代替案は採らない）。**

### 🔴 問 4. 判断の記録 ADR-20 の発効との依存（整合 F-1〔止める〕・F-2）

**やりたいこと。** この問では新しく問わない。31 周目の整合 F-1（止める・反証 支持）と整合 F-2 は、30 周目の整合 F-1・F-4 と同じ所見で、判断の記録 ADR-20 の発効で閉じる。ADR-20 の承認要求の推奨は案 d で、ADR-18 と ADR-13 の決定の本文は書き換えず（ADR-20 の改訂の欄と両者の注で断る）、前文と schema 節は引き金の型付きの定数に足す。発効の後の便（M）を、門が まだ分からない を返すまま器へ出すかは ADR-20 の 🔴 問 2 が別に問う。**答えは ADR-20 の承認要求 `docs/design/adr-20.md` の 🔴 問 1・問 2 の答えをそのまま使う。**

**理由。** 同じ論点を 2 つの承認要求で重ねて問うと、答えが割れたときに正本の字が 2 通りに決まる。ADR-20 の承認要求は、案と撤退条件と敵対の検証と独立の検証を添えた全文の承認要求で、この一括は同じ論点を持たない。この一括の A の字は ADR-20 の答えに依らない（ADR-18 の注の新しい段の 1 句だけが答えで変わる・§問どうしの依存）。

**代替案。**
- **(A) ADR-20 を待たず、この一括だけを先に取り込む。** 本流の 32 周目で整合 F-1・F-2 がまた立つ見込みが高く、周が 1 本むだになる。

**コスト。** この問の承認は要らない（ADR-20 の問 1・問 2 の承認に含まれる）。ADR-20 の発効の後に、引き金の定数（案 d）と生成区間の注の字の便 1 本（M・ADR-20 の承認要求 §5）が要る。32 周目はその便の着地の後に回す。

**推奨 = ADR-20 の承認要求の問 1・問 2 の推奨（案 d・便を出す裁定）に従い、同じ回の承認で、ADR-20 と一括 18 をこの枝の 1 回の取り込み（PR #308）で本流へ入れる（PR #307 は閉じる・要件書は第 1.39 版に同居・代替案は採らない）。**

## 承認の後に席が書く欄

持ち主が答えた後に、席がこの枝（`docs/batch18`）の別の commit で書く（同じ枝の ADR-20 の発効の記帳の commit の後）。ADR-20 の分の欄も枝 `docs/adr20` ではなくこの枝に書き、PR #308 の 1 回で本流へ取り込む（PR #307 は閉じる・席の裁定・改訂 c）。退けた問の分は「問どうしの依存」のとおり元の字に戻す。**「承認待ち」「起草」の字を持つ欄は全部ここに挙げた。** 書いた後に、表の 2・3・5〜8 の欄を 1 つずつ開いて、一括 18 に触れる字に「承認待ち」と「起草・」が残っていないことを確かめる（`status_note` は 1 行に全版の来歴を持ち、ほかの版の「承認待ち」はその版の記録なので、grep の 1 本では数えられない）。

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.38 → v1.39 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の頭の 2 文 | 「いま発効しているのは 第 1.38 版（2026-09-24）です。第 1.39 版（一括 18）は起草・承認待ちです。」を「いま発効しているのは 第 1.39 版（<承認の日付>）です。」の 1 文に |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.39 | 「v1.39 = 2026-09-24 起草（一括 18 = …・承認待ち）」を「v1.39 = <日付> 発効（逐語「…」・f2-648 notes <時刻> JST・一括 18 = …）」に。**頭の「起草」と末尾の「・承認待ち」の 2 か所とも書き換える**。ADR-20 と同じ版にしたら（§問どうしの依存）、要旨に「判断の記録 ADR-20 の発効＝FR20 の注の 1 文と欄 adrs」を足す |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 18〔問 1〜3〕の承認要求への回答〔と判断の記録 ADR-20 の問 1〕）", verbatim: <逐語>, version: v1.39}`）。作成の行は起草のまま（ADR-20 と同じ版にしたら作成の行の stamp の要旨に ADR-20 を足す） |
| 5 | `design-intent/rules.yaml` | 行 R-17 の `ruling` の先頭と `ruled_at` | 「一括 18 の承認待ち（席が承認後に…）」を「f2-648 notes <時刻> JST（逐語「…」・一括 18 の問 3 の 4 = what の頭に平易な 1 文と対の名指し・値・数える対象・母集団・種別・段・状態は不変）」に・`ruled_at` を承認の日付に |
| 6 | `design-intent/adr/ADR-18.yaml` | `note` の【天井の 31 周目の所見への応答（一括 18・2026-09-24 起草・持ち主の承認待ち）】の見出しと段の中の句 | 見出しの「持ち主の承認待ち」を「持ち主の承認 <日付 時刻> JST「<逐語>」」に。段の中の「起草中の判断の記録 ADR-20 の発効で閉じる」を、ADR-20 を発効したら「判断の記録 ADR-20（<日付> 発効）で閉じた」に（退けたら §問どうしの依存の字に） |
| 7 | `design-intent/adr/ADR-16.yaml` | 同じ見出し | 「持ち主の承認待ち」を「持ち主の承認 <日付 時刻> JST「<逐語>」」に |
| 8 | `docs/design/batch18-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に |
| 9 | `docs/design/state.md` | 要件書の版の行・判断の記録の行・次の一手・未決の表 | 要件書 = 第 1.39 版（発効の日付と逐語・一括 18〔と ADR-20〕）／一括 18 の後の手番（C の便 2 本の控えと f2-648.171 の便・ADR-20 の生成区間の字の便・本流の 32 周目）。state.md の今の字は書き換える前に読んで、一括 18 に触れる行を全部直す |
| 10 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）と、一括 18 の仕分けの在り処と、枝の上の周を回さず行 D-14 (イ) で止めて出したことと、次の手番（取り込みの後の本流の 32 周目） |
| 11 | 台帳（`bd`） | f2-648.171 の notes | 31 周目 整合 F-5（`limits_note` の 3 か所）を足す。32 周目の前に着地させる |
| 12 | 台帳（`bd`） | 控えの項（memo）6 つ・**起票済み**（承認に依らず取り込みの前に起こした・正本の注が「台帳の控え」と書くため） | f2-648.182 = 要件書と憲法の面の ADR-n を判断の記録の面への導線に（読みやすさ F-1）／f2-648.183 = 要件書の範囲の章に M2 の枠（読みやすさ F-2）／f2-648.184 = 行 R-17 の閉じた一覧 5 つの写しを生成区間へ導出（実態 F-3）／f2-648.185 = folio face・check・serve の説明の字（実態 F-4）／f2-648.186 = id の一覧を凍結し直す時機（条 P-7 の注・実態 F-1）／f2-648.187 = 要件書・入口・天井の正本・相談窓口・索引の欄の決まりの正本の承認欄の行の欄と床の数え（条 P-12 の注・実態 F-2・支度表は設計文書の正本に数えない）。承認の後に書くものは無い（f2-648.187 の題は席が改訂 b に合わせて直す） |

語彙と憲法と設計ノートの見本には承認欄の行が無い（見本は status example で承認欄を持たない。語彙は要件書の承認で運び、憲法の機構の注の直しは一括 14〜17 の憲法の注の直しと同じく承認欄を持たない）。

## 床の結果（この枝の HEAD で実測・2026-09-24・参考値）

base は ca60873 + ADR-20（6928f70）。binary は枝で `cargo build --all-targets` した `target/debug/folio`（`crates/` は main と同じ）。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio inject --check --dir design-intent` | rc 0（68 行 / 7,912 byte 一致） |
| `folio schema --dir design-intent --check` | rc 0（9 file 一致） |
| `folio derive --dir design-intent --out ../contracts --check` | rc 0（一致 1・差分 0） |
| `cargo nextest run --workspace --no-tests=fail --no-fail-fast` | **855 / 855 PASS** |
| `folio build --dir design-intent --out <scratch> --write` | 床 合格・27 file 書けた。要件書の面（FR13・FR20 の平易文）と憲法の面（行 R-17 の何の数値か）に直した字が出ることを字で確かめた。1440 の幅で開いて目で見る確認はしていない |
| `folio ceiling --dir design-intent --gate --write-set design-intent/srs.yaml` | まだ分からない（印が古い（引き金の要約値の欄が無い））・rc 2＝印は 28 周目（main a70477e）で、便アの前に書いたので引き金の要約値の欄を持たない。main ca60873 でも同じ |

**歯は 1 本も直していない。** 起草の 1 度目の床で落ちたものは無かった（行 R-17 の散文の言及の検査も 0 件）。

## まだ分からない点

- **この枝の新しい字は、どの周もまだ読んでいない。** 行 D-14 (イ) で枝の上の周を回さずに出すので、FR20 の平易文の新しい並び・FR7 の起きるときの欄・行 R-17 の何の数値かの頭の 1 文・条 P-7 と P-12 の注が新しい所見を生むかは、取り込みの後の本流の 32 周目で初めて分かる。
- **FR7 の起きるときの欄の新しい字（前半と後半を括弧で分ける）が、起きるときの欄の型（〜したとき）として読みやすいかは分からない。** 代替案 (A)（注の断り）なら規範文の欄は動かない。
- **要件書の版を ADR-20 と同じ版にするかは、持ち主の答えの形（同じ回か別の回か）で決まる。** 別の回なら本一括は第 1.40 版へ番号を送る（§問どうしの依存）。
- **面を 1440 の幅で目で見る確認はしていない。** 席が PR の前に要件書の面の FR20 と憲法の面の §5 の行 R-17 を開いて見る。
- **ADR-20 の承認要求の字との突き合わせは、載せ替えの後（7ba4c8d）に行った。** 問 4・依存の表・頭の手順は ADR-20 改訂 c の推奨（案 d・🔴 2 問・第 1.39 版に同居・FR20 の平易文に前文と schema 節を足す §3 の 12）に合わせた。取り込みの形は、その後の席の裁定（一括 18 改訂 c）で「この枝の 1 回・PR #307 は閉じる」にした。ADR-20 の承認要求の §5 と §3 の 8 がこの字に揃うのは ADR-20 の改訂 d の後で、その後にこの枝を載せ替える（未実施）。ADR-20 の問 2 を退けたら、32 周目を便の前に回すので、頭の手順 ⑤ の「便は 32 周目の前に着地させる」は ADR-20 の分だけ外れる。
