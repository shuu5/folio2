# 一括 19 の仕分け — 天井の 32 周目の所見（止める 3 件・直す 20 件・参考 3 件）

**状態: 起草・持ち主の承認待ち（2026-09-24 起草・orchestrator 席〔起草は opus 5.5 の起草役に委ねた〕・作業場所 `.worktrees/planner-batch19`・枝 `docs/batch19`・土台 = 本流 68196cf・改訂 b〔席の裁定＝整合 F-1〜F-3 は判断の記録 ADR-22・実態 F-1 の便の形〕・改訂 b の 2〔席の裁定＝運び方は行 D-14 の止める場合 (イ) で今出す・実態 F-1 と前文の平易文は判断の記録 ADR-23〕・改訂 c〔問 4 の ADR-23 の向きと帰結の便の字〕・改訂 d〔独立の検証 `~/.local/share/folio2/handoff-2026-09-24/batch19-verify.md`〔411c17a を読んだ〕の blocking 3・文面 5・漏れ 4・低い重さ 1〕）。** 持ち主への承認要求は **🔴 問 1・問 2**（この枝に当てた字）である。**問 3 は ADR-22、問 4 は ADR-23 を指すだけで新しく問わず**、それぞれ判断の記録 ADR-22（枝 `docs/adr22` で起草中）と ADR-23（枝 `docs/adr23` で起草中）の承認要求の問 1 を指すだけである（一括 18 の問 4 と同じ形）。3 つの承認要求は同じ回に出す。問の案はどれも席の推奨の案で書いた（判断の記録 ADR-19 決定 (1) ①）。承認の記帳（§「承認の後に席が書く欄」）は持ち主が答えた後に席が同じ枝の別の commit で書く。持ち主の逐語はこの文書にも正本にも書いていない（条 P-12.3）。

**この一括は、4 観点とも合格した枝ではない（規則の表の開発規律行 D-14 の断り）。** 32 周目は読みやすさが 合格、忠実さ・実態・整合の 3 観点が 不合格（止める 各 1 件・3 件とも反証で 支持）だった。止める 3 件の扱いは次のとおりである。

- **忠実さ F-1** は判断の記録 ADR-16 の平易文の直し（A）で、この枝に書いた。
- **整合 F-1** は判断の記録 ADR-3 決定 (8) を狭める改訂の欄が要り、新しい判断の記録 ADR-22 で閉じる（B）。
- **実態 F-1** は憲法の schema 節の live の規則の改訂が要り、新しい判断の記録 ADR-23 と憲法 第 1.4 版で閉じる（B・条 A-2 の手順）。

整合 F-1 と実態 F-1 は、判断の記録の発効と条文の改訂が要るので枝では閉じない。これは行 D-14 の止める場合 (イ) に当たる。**だから枝の上の 33 周目を回さずに往復をここで止め、32 周目の所見と直しを持ち主へ出す**（一括 18 と同じ形・行 D-14・判断の記録 ADR-19 決定 (3)）。33 周目は、ADR-22・ADR-23 の発効と、発効の後に要る便の着地の後に、本流で回す。**この枝の新しい字は、どの周もまだ読んでいない。**

材料は天井の 32 周目の所見である（周 = `~/.local/share/folio2/ceiling/2026-09-24-round32/`・観点 4・束は本流 68196cf で組んだ・正は各観点の `2026-09-24-round32/<観点>/findings.yaml`）。止める 3 件の反証は 3 件とも着いてから仕分けた。

| 止める | 反証 | 結果の file（時刻） | 理由 |
|---|---|---|---|
| 忠実さ F-1 | **支持** | `fidelity/refute/F-1/result.yaml`（2026-09-24T13:57:00Z） | `refute-notes/fidelity-F-1.md` |
| 実態 F-1 | **支持** | `reality/refute/F-1/result.yaml`（2026-09-24T13:57:38Z） | `refute-notes/reality-F-1.md` |
| 整合 F-1 | **支持** | `coherence/refute/F-1/result.yaml`（2026-09-24T13:59:40Z） | `refute-notes/coherence-F-1.md` |

| 観点 | 判定 | 止める | 直す | 参考 |
|---|---|---|---|---|
| 読みやすさ | 合格 | 0 | 5 | 1 |
| 忠実さ | 不合格 | 1 | 4 | 1 |
| 実態 | 不合格 | 1 | 4 | 0 |
| 整合 | 不合格 | 1 | 7 | 1 |
| 計 | — | 3 | 20 | 3 |

**同じ箇所か同じ落としを 2 つ以上の所見が指す組が 4 つ在る。** それぞれ 1 つの直しにまとめた。

- 実態 F-4・整合 F-8 — 要件 FR20 の注の「受入基準 AC18 の 6 場合」。
- 実態 F-2・整合 F-9 — 判断の記録 ADR-20 の注の「決定 (5) ⑤ の便は後続」（便 129 は着地済み）。
- 忠実さ F-2・F-3・読みやすさ F-5 — 判断の記録 ADR-21 の平易文（判定点 ③ の名指し・決定 (2)・発効の後の答え）。
- 整合 F-1・F-2・F-3 — 判断の記録 ADR-3 決定 (8) と、その帰結の制約 CON8 と M3 の範囲の節（ADR-22 が閉じる）。
- 実態 F-1・読みやすさ F-1 — 憲法の schema 節と前文（どちらも条 A-2 の改訂の範囲・ADR-23 が閉じる）。

**生成区間に在る所見は無い。** 実装に掛かる所見は 2 件（実態 F-1 は ADR-23 の発効の後の帰結の便・読みやすさ F-2 は面の生成器の便）である。

**印は本流の 28 周目のまま。** この枝の HEAD で `folio ceiling --gate --write-set design-intent/srs.yaml` は「まだ分からない（印が古い（引き金の要約値の欄が無い））」を返す（本流 68196cf でも同じ）。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る・問 1 か問 2）／**B** = 新しい判断の記録が閉じる（この枝には字を当てない。ADR-22 は問 3・ADR-23 は問 4 が指す）／**C** = 便候補（直す先が実装・台帳の控え）／**D** = この一括では直さない（理由を書く）。

| 観点 | id | 重さ | place | 是正の内容 | 仕分け | 問 |
|---|---|---|---|---|---|---|
| 忠実さ | F-1 | 止める（反証 支持） | `adr` `ADR-16.plain` | 平易文の末尾の読み方の段の「この判断の「scribe2」は、判断の記録 ADR-21 で、器の次の世代（scribe v3）の設計の置き場に読み替えています。」を、読み替えるのは道具を試す相手（外の利用者）だけで、置き場（design-intent/folio2）と「床の受付の宣言」の形は同じ repo に本来の憲法を別に持つ利用者に限り、v3 の置き場の場所は v3 が選び、骨格の憲法は v3 の憲法そのもので v3 の持ち主が書き換えてから承認する、の字にした（ADR-21 決定 (2) と改訂の欄〔ADR-16 決定 (3) を狭める〕と同じ向き）。判定点 ③ の記帳に読んだ器の組み立てを名指すことも添えた（改訂 d で「その字」の指し先を「骨格の「本来の憲法を指す」字」にし、第 1.0 版の承認と始まりの凍結の順を足した・検証の文面 a）。注の末尾に【天井の 32 周目の所見への応答】の段。**判断の記録の決定の改訂は要らない**（ADR-21 の決定と改訂の欄は正しく、平易文の言い換えの誤り）。見出し・文脈・決定・案・帰結・撤退条件・承認欄は 1 字も触っていない | A | 2 |
| 忠実さ | F-2 | 直す | `adr` `ADR-21.plain` | 「そのまま v3 の置き場で確かめます」を「中身を変えずに…確かめます」にし、3 つ目の確かめの記録に器の repo と commit を名指すことを足し、そのぶん合格の条件が狭くなると書いた。注に段 | A | 2 |
| 忠実さ | F-3 | 直す | `adr` `ADR-21.plain` | 決定 (2)（置き場と骨格の形を本来の憲法を別に持つ利用者に限る・v3 の置き場は v3 が選ぶ・v3 の持ち主が骨格の憲法を v3 の条へ書き換えてから第 1.0 版を承認し、その後に始まりの凍結）の 3 文を平易文に足した | A | 2 |
| 忠実さ | F-4 | 直す | `srs` `acceptance.AC25.title` | 題を「…通した記帳が、読んだ器の組み立てを名指して利用者の台帳に在る」にした（判定の文と平易文は第 1.40 版で狭めた字）。**受入基準の題は周の引き金の欄**なので、問 1 の中で名指して問う。判定の文は変えない | A | 1 |
| 忠実さ | F-5 | 直す | `srs` `requirements.FR20.plain` | 「注や平易文や図だけが変わっているとき」に括弧「（憲法の前文と schema 節の中の注と平易文は除きます。前文と schema 節は丸ごと規範の欄です）」を足した（ADR-20 決定 (2)）。所見が名指す同じ字の 2 か所（判断の記録 ADR-18 の平易文・語彙の「周の引き金」の定義）にも同じ向きの括弧を足した。FR20 と ADR-18 の注に記録の文 | A | 1・2 |
| 忠実さ | F-6 | 参考 | `srs` `requirements.FR11.plain` | FR11 と受入基準 AC9 の平易文の「scribe2 が読む形」を規範文と同じ「器（便を運ぶ道具）が読む形」にした。字を 1 句替えるだけで版の中の呼び方が揃うので A にした | A | 1 |
| 実態 | F-1 | 止める（反証 支持） | `constitution` `schema.mechanism_live_rule` | 憲法の schema 節は live が now でない条（本流で P-11・P-13・P-15・P-17・P-18・A-3 の 6 条・どれも live M1）の検査結果を「まだ分からない」として出すと約束し、`folio check` は「合格（違反 0・まだ分からない 0）」を返す。day-1 の Python の床（814366c の check_draft.py）は出していたが、Rust の床へ写したときに落ちた（反証の記録）。**新しい判断の記録 ADR-23（起草中・枝 `docs/adr23`）で schema 節の live の規則を「床の検査の対象外として数え、条ごとに表に出す」向きに改め、憲法を第 1.4 版に上げる**（条 A-2）。対象外の数を要約に出す便は ADR-23 の発効の後の帰結として起こす。起草への申し送りは §「判断の記録 ADR-23 の起草への申し送り」。**行 D-14 の止める場合 (イ) の例外**。この枝には字を当てない | B | 4 |
| 実態 | F-2 | 直す | `adr` `ADR-20.note` | 注の末尾に【決定 (5) ⑤ の便の着地】の段＝便 129（台帳 f2-648.197・本流 101acf5・2026-09-24 22:43 JST）で着地し、引き金の憲法の写しは改訂の範囲・索引と天井の正本の注の生成区間も本判断の字に替わった。問 2 のもう 1 本（f2-648.171）は便 130（51168af）。【発効】の段と帰結の字は発効の時点の字として残す。ADR-18 の注の「ADR-20 の便で改訂の範囲へ広げる」にも着地の 1 文を足した | A | 2 |
| 実態 | F-3 | 直す | `adr` `ADR-16.note` | 注 (f) の「器の今の読み手は欄 goal を断る」「判定点 ③ は受け口の着地が要る」は着地の前の字と断り、受け口は s2-07l.586 として scribe2 の 0337cf1（2026-09-24 07:40 JST）に着地し前提は満たされた（測定値は今も利用者の台帳の記帳）と書いた。忠実さ F-1 と同じ段 | A | 2 |
| 実態 | F-4 | 直す | `srs` `requirements.FR20.note` | 「受入基準 AC18 の 6 場合は混在の場合を持たない」を「受入基準 AC18 の場合（第 1.36 版から 7 場合）は混在の場合を持たない」にした（混在の場合が無いことは今も正しい） | A | 1 |
| 実態 | F-5 | 直す | `srs` `requirements.FR22.note` | 「始まりの凍結（FR24）で閉じる。」の後に、始まりの凍結は憲法の承認欄に利用者の持ち主の承認と裁定 id を書いてから撃つ（ADR-21 決定 (2) の順）こと、骨格は裁定を「未記入」で書き、凍結は書く前に確かめないので、骨格のまま凍結すると凍結の後の床が違反 1 を返すこと（台帳 f2-648.193・未解決）を足した。順の出所は一般の順の ADR-16 決定 (7) の ⑦ と、v3 の置き場の ADR-21 決定 (2)（改訂 d・検証の文面 b）。実測 = `crates/folio/src/init.rs` の骨格の承認欄・`crates/folio/src/anchor.rs` の「approvals[n].ruling に台帳 id が無い」 | A | 1 |
| 整合 | F-1 | 止める（反証 支持） | `adr` `ADR-3.decision`（決定 (8)） | 決定 (8)「scribe2 の設計文書の中の契約表を folio2 の M1 で設計ノートの YAML 正本と導出物へ移す」を狭める改訂の欄がどの判断の記録にも無く、ADR-21 決定 (6) は範囲の改訂を改訂の欄に限る（反証 支持）。**新しい判断の記録 ADR-22**（起草中・枝 `docs/adr22`）の改訂の欄で ADR-3 決定 (8) を狭める＝契約表の移し先は M3 の口・scribe2 の設計文書は移らない（ADR-21）・v3 の設計は folio2 の形で起こす。**行 D-14 の止める場合 (イ) の例外**。この枝には字を当てない | B | 3 |
| 整合 | F-2 | 直す | `srs` `constraints.CON8.text` | 制約 CON8 の頭の文（M1 で移す）は ADR-3 決定 (8) の帰結なので、ADR-22 の承認の後に席が書く欄で直す（ADR-22 の起草役がその欄に載せる）。この枝では触らない | B | 3 |
| 整合 | F-3 | 直す | `srs` `scope_m3.not_build` | M3 の範囲の節の「要件書 CON8 のとおり M3 の後に改めて問う」と M1 の範囲の節の注の「CON8 の別の論点」は CON8 と同じ帰結なので、ADR-22 の承認の後に席が書く欄で直す。この枝では触らない | B | 3 |
| 整合 | F-4 | 直す | `srs` `acceptance.AC25.note` | 「器の受け口（台帳 s2-07l.473）の着地はこの判定点の前提であって、測定値ではない」を、.473 は着地せずに閉じられ受け口は .586 に引き継がれて 2026-09-24 に着地した（FR11 の注）、前提は満たされた、の字にした。記録の文 | A | 1 |
| 整合 | F-5 | 直す | `intake` `questions.q3.why` | 相談窓口の問い q3 の理由と対象の行（adr）の注の「最初は 0 本の棚」を「最初は判断の記録が無いか、骨格の命令（folio init）が書いた 1 本（欄の決まりを採る判断・提案中）だけの棚」にした（要件 FR22・ADR-16 決定 (2)(カ)）。問い q3 は骨格の雛形へ写るので、骨格が 1 本を書いた置き場にも正しい字になる。判断の記録 ADR-6 の注の末尾に段（決定 (2) ③ の「最初は 0 本」は発効の時点の字）。**ADR-16 が ADR-6 決定 (2) ③ の範囲を変えたことを改訂の欄で持つかは、ADR-22 の範囲の外で、台帳の控え f2-648.198 で別に扱う**（ADR-22 の承認要求 §6）。相談窓口の正本の版は上げていない（一括 5 が問い q4 の字を直したときも第 0.1 版のまま・承認は一括の承認で運んだ先例） | A | 2 |
| 整合 | F-6 | 直す | `rules` `discipline.D-14.what` | 行 D-14 の注の末尾に 1 文＝止める場合の「観点と場所の組」は、同じ種類の失敗の定めの正本である行 R-7 の same_failure の 3 つ組を天井の所見に当てた読みで、検査の id は観点・判定は反証で支持された 止める・対象の path は所見の置き場の file に当たる。この行が足すのは読みと窓の中の数え方だけで、同じ種類の定めを別に持たない。**what は変えていない**（what の変更は引き金の欄）。規則の表の行の変更なので `ruling` の頭を「一括 19 の承認待ち（…）」にした（条 P-17.1・一括 18 の行 R-17 と同じ形）。kind・status・段・refs は不変 | A | 2 |
| 整合 | F-7 | 直す | `adr` `ADR-8.note` | 門の形の裁定（2026-09-21）の「鮮度は厳格（設計文書が 1 byte でも変われば古い）」の直後に「〔この鮮度の定めは 2026-09-24 に置き換わった・この注の末尾の段〕」、注の末尾に段＝ADR-18 決定 (4)(5) と FR20 の規範文（第 1.36 版・便アの着地）で置き換わり、今の門は引き金の要約値で古さを測る。印の置き場・運び方・器への結線・門の撤退条件は今も同じ。この注は門の形の裁定と撤退条件を読む 1 か所のまま（FR20 の注の指しは変えない）。持ち主の逐語は変えていない。記録の段の中に置き換えの指しの括弧を 1 つ差し込んだ | A | 2 |
| 整合 | F-8 | 直す | `srs` `requirements.FR20.note` | 実態 F-4 と同じ直しで閉じた | A | 1 |
| 整合 | F-9 | 参考 | `adr` `ADR-20.note` | 実態 F-2 と同じ直しで閉じた | A | 2 |
| 読みやすさ | F-1 | 直す | `constitution` `precedence.plain` | 前文の平易文の「全部そろえる」と「そろっている」の言い換え。前文は平易文を含めて条 A-2 の改訂の範囲（判断の記録 ADR-20 決定 (2)）なので、ADR-23 の憲法 第 1.4 版に載せる（席の裁定）。この枝には字を当てない | B | 4 |
| 読みやすさ | F-2 | 直す | `srs` `scope_m3.build` | 範囲の節の番号（ADR-16・ADR-21・AC23〜AC25・FR22〜FR25・FR11）を押せる行き先にするのは面の生成器（`crates/folio/src/face_srs.rs`）の便で、正本の字は写しを外した理由（条 P-6.3・30 周目 整合 F-6）のとおり正しい。一括 18 が起こした台帳の控え f2-648.182（要件書と憲法の面の ADR-n を判断の記録の面への導線に）に、範囲の節の要件・受入基準の番号の導線を足す（席が notes に書く・承認に依らない） | C | — |
| 読みやすさ | F-3 | 直す | `srs` `acceptance.AC23.plain` | 平易文の頭に「M3 の出口を決める 3 つの確かめ（判定点）の 1 つ目です。」を足し、「利用者（v3 の設計の置き場）」を「利用者（器の次の世代〔scribe v3・以下 v3〕の設計の置き場）」にした（AC23 が入口の「決める人」の行き先で v3 と判定点の初出）。注に記録の文。平易文は引き金の外 | A | 1 |
| 読みやすさ | F-4 | 直す | `rules` `discipline.D-1.what` | 語彙に 1 項「AI のモデルの名と念入りさ（opus・sonnet・xhigh）」（id `ai-model-effort`）を足した＝opus と sonnet はモデルの型の名で sonnet は opus より軽く速い型、行 D-1 は並列実行・便・作業者の内側の上限を opus とし件数で増える側は sonnet でよいとする（席そのもののモデルは要件書 CON5）、xhigh は念入りさの段で high の 1 つ上（その上にも段が在る）、値の正本は要件書 CON5 と行 D-1（ここには写さない）。改訂 d で、道具の念入りさの段が 5 つ（low・medium・high・xhigh・max）で xhigh の上に max が在ることと、席が fable で動くことに合わせて字を直した（独立の検証の blocking 3）。行 D-1 の what は変えていない（what は引き金の欄で、凍結の行の裁定が要る）。同じ所見が添えた行 D-9 の「PreToolUse の hook」は、hook が語彙に既に在る（自動の仕掛け（hook）・止める仕掛け（guard）の項）ので本一括では触らない | A | 1 |
| 読みやすさ | F-5 | 直す | `adr` `ADR-21.plain` | 平易文の末尾に「（発効の後の読み方）」の段＝2026-09-24 に発効・答えは v3 は新しい repo（scribe3）で作り folio の実装は後から取り込む・だから保留は起きなかった・上の段落は承認をお願いしたときの字のまま（ADR-16 の平易文の末尾の段の形に倣った。改訂 d で、持ち主の逐語は「承認する」なので鉤括弧の「あなたの答え」をやめ、「推奨どおり承認し…と決まりました」にした・検証の文面 c）。「別の控え 8 件」に台帳の項 f2-648.189〜f2-648.196 を添えた | A | 2 |
| 読みやすさ | F-6 | 参考 | `constitution` `sources` | 直さない（§D の理由） | D | — |

**数。** A = 19（忠実さ F-1〜F-6／実態 F-2〜F-5／整合 F-4〜F-9／読みやすさ F-3〜F-5）・B = 5（整合 F-1〜F-3 は ADR-22／実態 F-1・読みやすさ F-1 は ADR-23）・C = 1（読みやすさ F-2）・D = 1（読みやすさ F-6）。計 26。同じ直しで閉じた組（実態 F-4 と整合 F-8・実態 F-2 と整合 F-9）も所見ごとに A と数えた。A のうち 止める は 1 件（忠実さ F-1）、参考は 3 件のうち 2 件（忠実さ F-6・整合 F-9）。

**A で動く周の引き金の欄は 1 か所である**（受入基準 AC25 の題）。平易文・注・語彙・相談窓口の正本・行 D-14 の注と裁定の欄・判断の記録の平易文と注は引き金の外である（天井の正本の生成区間 `trigger`）。**規範文（shall）・受入の判定の文・制約・範囲の節・条文・schema 節・前文・判断の記録の決定と改訂の欄・規則の表の what と値は 1 字も動かしていない。**

## D（直さない）の理由

| id | なぜ直さないか |
|---|---|
| 読みやすさ F-6（憲法 §8 出所の記号） | 参考。所見が「説明しない」と言う G の番号と「=A」の読み方は、出所 2 の ref の末尾に既に在る（「G8=A のような G の番号と出所 4 の P1〜P5 は 2026-09-12 の持ち主の裁定の番号…「=A」はその裁定で選ばれた案の記号」）。章の頭に 1 文を足す口は面に無く（出所の章の見出しは固定の字）、字の重複のために触らない |

## 便候補（C）の中身

### 読みやすさ F-2 ＝ 範囲の節の番号の導線

台帳 f2-648.182 に束ねる（面の生成器の便・面の見た目の直しの便の数え〔判断の記録 ADR-5 の撤退条件 ②〕に当たるかは契約で確かめる）。席が f2-648.182 の notes に 32 周目 読みやすさ F-2 を足す（承認に依らない・取り込みの前）。

## 運び方（行 D-14 の窓）

- **窓は枝 `docs/batch19` を切った時点で開いた。** 窓の間は、設計文書を書き換える便の台帳の件を器の列で保留にする（`pipe dispatch hold`・行 D-14・席の手番）。窓は本流への取り込みで閉じる。
- **止める場合 (イ) で、枝の上の 33 周目を回さずに今出す（席の裁定）。** 行 D-14 は、周が不合格なら支持の 止める のそれぞれに直しを当ててから回し直すとし、条文の改訂か判断の記録の発効が要るときは往復を止めてその場で持ち主へ出すとする。整合 F-1（ADR-22 の発効）と実態 F-1（ADR-23 の発効と憲法の改訂）は枝に直しを当てられないので、直しを当てた忠実さ F-1 と A の字だけで 33 周目を回しても合格しない。だから 32 周目の所見と A の直しを、ADR-22・ADR-23 の承認要求と同じ回に持ち主へ出す。
- **取り込みと 33 周目。** 持ち主が一括 19 と ADR-22・ADR-23 に答えたら、席が承認の記帳を書いて本流へ取り込み（取り込みの形は席が 3 本の枝の載せ替えで決める）、取り込みの時刻で窓を閉じる。ADR-23 の発効の後に、その帰結の便（対象外の数を要約に出す）を起こし、その着地の後に本流で 33 周目を回す（ADR-22・ADR-23 の発効〔判断の記録の状態と改訂の欄〕・憲法 第 1.4 版・AC25 の題をまとめて読む）。
- **周の引き金の欄の変更は受入基準 AC25 の題の 1 か所だけである。** 整合 F-3 を B へ移して範囲の節の字を本流に戻したので（改訂 b・ee9a7f4＝独立の検証の blocking 1 は済み）、この枝の A で動く引き金の欄は AC25 の題だけになった。ADR-22・ADR-23 の発効（判断の記録の状態と改訂の欄・憲法の schema 節と前文・制約 CON8・範囲の節）も引き金を動かすので、33 周目はそれをまとめて読む。
- **行 R-7 の数え（止める場合 (ア)）には当たらない。** 窓は一括 18 と ADR-21 の取り込み（本流 68196cf）の後に切った新しい窓で、数えは 32 周目の 1 から始まる（行 D-14「次の窓では数え直す」）。32 周目の止める 3 件の組（観点 × 所見の置き場の file）は、忠実さ × `adr/ADR-16.yaml`・実態 × `constitution.yaml`・整合 × `adr/ADR-3.yaml` で、どれも窓の中で 1 回である。30・31 周目の止めるは整合 × `adr/ADR-18.yaml` の 1 組だけで、32 周目の 3 組と重ならない。29 周目の実態 F-1（`constitution.yaml`・条 N-3 の機構の欄）は同じ組だが、一括 17 の窓（取り込み ca60873 で閉じた）の数えで、今の窓には数えない。取り込みで窓を閉じるので、33 周目は新しい窓の数えになる。**窓をまたぐと繰り返しは在る**（独立の検証の数え）＝実態 × `constitution.yaml` の支持の 止める は 12・17・29・32 周目の 4 回で、29 周目（条 N-3 の機構の欄）と 32 周目（schema 節の live の規則）はどちらも憲法の機構の欄と実装の食い違いである。所見の場所（欄）は毎回別である（12 周目 = 条 P-1 と P-3 の機構の欄・17 周目 = 条 N-6.1 の規範文・29 周目 = 条 N-3 の機構の欄・32 周目 = schema 節の live の規則）。行 D-14 の数えは窓ごとに 1 から始まる。忠実さ × `adr/ADR-16.yaml` は 27・32 周目の 2 回である。窓の数えでは止める場合 (ア) に当たらないが、条 P-11 の趣旨で持ち主が繰り返しを見られるように添える。

## 問どうしの依存

| 問 | 依存と、退けたときに席が戻すもの |
|---|---|
| 問 1 | **要件書 第 1.41 版の中身は問 1 の答えで決まる。** 中の項は互いに独立である。退けた項は元の字に戻し、要件書の来歴（`meta.status_note` の v1.41）と作成の行の stamp からも外す。語彙の 2 項（周の引き金の括弧・AI のモデルの名と念入りさの項）は要件書の承認で運ぶので問 1 に入れた |
| 問 1 と問 3・問 4 | **要件書の版は ADR-22 と同じ版にまとめる見込み。** ADR-22 の承認の後に席が書く欄（制約 CON8・M3 と M1 の範囲の節）は要件書の字なので、ADR-22 と一括 19 を同じ回に承認したら第 1.41 版に同居させる（一括 18 と ADR-20 の第 1.39 版の同居と同じ形）。取り込みの形（1 本の枝で 1 回か）は席が ADR-22 の枝との載せ替えで決める。問 1 を全部退け ADR-22 も発効しないなら版を上げない＝`meta.version` を v1.40 に戻し、来歴の v1.41 の項と作成の行と、`status_note` の頭の「第 1.41 版（一括 19）は起草・承認待ちです。」を外す |
| 問 1 と問 2 | **忠実さ F-5 の括弧は 3 か所（FR20 の平易文〔問 1〕・語彙の周の引き金〔問 1〕・ADR-18 の平易文〔問 2〕）に同じ向きで当てた。** 片方だけを退けると同じ定義が割れるので、退けるなら 3 か所とも戻す |
| 問 2 と問 3 | **依らない。** 判断の記録 ADR-6 の注の末尾の段は、ADR-6 決定 (2) ③ を ADR-22 の範囲の外として台帳の控え f2-648.198 で扱う字にした（ADR-22 の承認要求 §6 の判定と同じ） |
| 問 3 | **この一括では問わない。** 答えは ADR-22 の承認要求の問 1 の答えを使う。ADR-22 をどの案で発効しても、本一括の A と C の字は変わらない |
| 版の同居と取り込みの順 | **要件書は、ADR-22 の承認の後の欄（制約 CON8・M1 と M3 の範囲の節）も同じ回に承認されたら第 1.41 版に同居させる**（一括 18 と ADR-20 の第 1.39 版の先例）。ADR-22 が別の回になったら第 1.42 版へ送る。**憲法は ADR-23 で第 1.4 版に上がり、凍結 anchor に v1.4 の写しを足す**（`folio check --freeze-anchor`・上書きしない）。凍結は全検査が違反 0 のときだけ作れるので、取り込みの枝の上で一括 19 と ADR-22 の記帳を先に書き、床が合格してから ADR-23 の憲法の版と anchor の凍結を書く順にする（席が 3 本の枝の載せ替えで決める） |
| 問 4 | **この一括では問わない。** 答えは ADR-23 の承認要求の問 1 の答えを使う。ADR-23 は憲法の版（第 1.4 版）で、要件書の版とは別に上がる。ADR-23 をどの案で発効しても、本一括の A と C の字は変わらない。前文の平易文（読みやすさ F-1）は ADR-23 に載る |

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 判断の記録 ADR-3 決定 (8) の範囲の改訂と、制約 CON8 の頭の文・M3 と M1 の範囲の節の CON8 の指し | 判断の記録 ADR-22 が閉じる（B・問 3）。この枝では触らない（席の裁定） |
| 判断の記録 ADR-6 決定 (2) ③ の範囲の改訂 | 同じ種類だが ADR-22 の範囲の外（ADR-22 の承認要求 §6）。整合 F-5 は 直す で行 D-14 の止める場合に当たらない。台帳の控え f2-648.198 で小さい判断の記録の候補として別に扱う |
| 憲法の schema 節の live の規則と前文の平易文（実態 F-1・読みやすさ F-1） | 条 A-2 の改訂の範囲。判断の記録 ADR-23 と憲法 第 1.4 版が閉じる（B・問 4）。この枝では憲法を触らない |
| 規則の表の行 D-1 と D-9 の what | 語彙の項で閉じた（行 D-1 は凍結の行で、what は引き金の欄） |
| 面の番号の導線（読みやすさ F-2） | 面の生成器の便（C・f2-648.182） |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.41 版の起草。** 受入基準 AC25 の題と注・FR20 の平易文と注・FR11 の平易文と注・受入基準 AC9 の平易文・受入基準 AC23 の平易文と注・FR22 の注／`meta.version` を v1.41 に・`status_note` の頭に「第 1.41 版（一括 19）は起草・承認待ちです。」・末尾に v1.41 の起草・`approval` に 作成 の行。**`effective_version` は v1.40 のまま・承認の行は書いていない。制約 CON8 と範囲の節は触っていない** |
| `design-intent/vocabulary.yaml` | 周の引き金の定義の括弧・新しい項 `ai-model-effort`（id は append only）・足した語の注釈の行 |
| `design-intent/intake.yaml` | 問い q3 の理由と対象の行 adr の注（版は第 0.1 版のまま） |
| `design-intent/rules.yaml` | 行 D-14 の注の末尾の 1 文と `ruling` の頭（what・値・kind・status・refs・`ruled_at` は不変） |
| `design-intent/adr/ADR-16.yaml` | 平易文の末尾の読み方の段と注の末尾の段 |
| `design-intent/adr/ADR-21.yaml` | 平易文の 4 か所と注の末尾の段 |
| `design-intent/adr/ADR-20.yaml` | 注の末尾の段（着地の記録） |
| `design-intent/adr/ADR-18.yaml` | 平易文の括弧と注の末尾の段 |
| `design-intent/adr/ADR-8.yaml` | 注の中の 1 句（置き換わったことの指し）と注の末尾の段 |
| `design-intent/adr/ADR-6.yaml` | 注の末尾の段 |
| `docs/design/batch19-triage.md` | **新設**（この file） |

判断の記録 6 本の**見出し・文脈・決定・案・帰結・撤退条件・改訂の欄・承認欄・状態は 1 字も触っていない**（平易文と注だけ）。**憲法・天井の正本・入口・索引の欄の決まりの正本・支度表・実装（`crates/`）・凍結 anchor（`tests/fixtures/`・`design-intent/anchors/`）・生成区間・印（`design-intent/preview/ceiling-stamp.yaml`）・歯は 1 byte も触っていない。**

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.41 版と語彙（AC25 の題・平易文と注の直し）

**やりたいこと。** 要件書を第 1.41 版に上げ、次を直す。**shall（規範文）と受入の判定の文は 1 字も変えない。** 周の引き金の欄が動くのは項 1 の受入基準 AC25 の題だけである。

1. **（引き金の欄が動く項）** 受入基準 AC25（M3 の出口の 3 つ目の確かめ）の題に「読んだ器の組み立てを名指して」を足す。判定の文と平易文は第 1.40 版で既にそう狭めてあり、題だけが古かった（忠実さ F-4）。
2. 要件 FR20（天井の門）の平易文の「注や平易文や図だけ」に、憲法の前文と schema 節の中の注と平易文は除く括弧を足す。語彙の「周の引き金」の定義にも同じ括弧を足す（忠実さ F-5）。
3. 平易文の呼び方と言い換え＝FR11 と受入基準 AC9 の「scribe2 が読む形」を「器が読む形」に（忠実さ F-6）・受入基準 AC23 の初出の v3 と判定点に言い換え（読みやすさ F-3）。
4. 注の古い字＝FR20 の注の「AC18 の 6 場合」（実態 F-4・整合 F-8）・受入基準 AC25 の注の器の受け口（整合 F-4）・FR22 の注に始まりの凍結の前提と台帳 f2-648.193（実態 F-5）。
5. 語彙に 1 項「AI のモデルの名と念入りさ（opus・sonnet・xhigh）」を足す（読みやすさ F-4）。

**理由。** 1 は、題だけを読むと記帳が在れば着地と読め、判定の文と割れていた。2 は、判断の記録 ADR-20 で前文と schema 節は平易文や注まで丸ごと周の引き金に入ったので、前文の平易文だけを直した便を門が「通す」と読めた（実際は「まだ分からない」）。3〜5 は、平易文の呼び方の割れ・言い換えの無い語・着地や数の古い字である。

**代替案。**
- **(A) 項 1 を注の断りで直す**（題は変えず、注に「判定の文が名指しを求める」を書く）。引き金の欄は動かない。ただし題と判定の文の割れは字のまま残り、次の周の忠実さが同じ所を拾う見込みがある。
- **(B) 項 5 を行 D-1 の what に言い換えを書く形にする**（所見の勧めの前半）。表の行で読める。ただし行 D-1 は凍結の行で、what は周の引き金の欄なので、規則の表の行の裁定が要り、モデルの名が変わるたびに行を直すことになる。

**コスト。** 承認 1 回。題 1・平易文 4（FR20・FR11・AC9・AC23）・注 5（FR20・FR11・FR22・AC23・AC25）・語彙の定義の括弧 1 と項 1。**要件の番号は 1 つも動かず（条 P-7.1）、義務は 1 つも増えない。** 床の歯は 1 本も直さない。

**推奨 = 1〜5 を当てる（代替案は採らない）。**

### 🔴 問 2. 判断の記録の平易文と注・相談窓口の問いの理由・行 D-14 の注

**やりたいこと。** 次を直す。**判断の記録の見出し・文脈・決定・案・帰結・撤退条件・改訂の欄・承認欄・状態は 1 字も触らない**（発効済みの判断の記録の食い違いは平易文か注で断る＝一括 13〜18 と同じ扱い）。

1. **（止める の直し）** 判断の記録 ADR-16 の平易文の末尾の読み方の段を、読み替えるのは道具を試す相手だけで、置き場と「床の受付の宣言」の形は本来の憲法を別に持つ利用者に限る字にする（忠実さ F-1・反証 支持）。注に、器の受け口の着地（s2-07l.586）で注 (f) の前提が満たされたことも書く（実態 F-3）。
2. 判断の記録 ADR-21 の平易文に、判定点 ③ の名指し・決定 (2)（置き場と骨格の形）・発効の後の答えの段・控え 8 件の id を足す（忠実さ F-2・F-3・読みやすさ F-5）。
3. 判断の記録 ADR-20 と ADR-18 の注に、便 129・130 の着地を書く（実態 F-2・整合 F-9）。ADR-18 の平易文に問 1 の 2 と同じ括弧を足す（忠実さ F-5）。
4. 判断の記録 ADR-8 の注の「鮮度は厳格」に、ADR-18 決定 (5) で置き換わったことの指しと段を足す（整合 F-7）。
5. 相談窓口の問い q3 の理由と対象の行の注の「最初は 0 本の棚」を、骨格の命令が 1 本を書く場合を含む字にし、判断の記録 ADR-6 の注に断りの段を足す（整合 F-5）。
6. 規則の表の行 D-14 の注に、「観点と場所の組」は行 R-7 の same_failure の 3 つ組を天井の所見に当てた読みだと 1 文を足す（整合 F-6）。**規則の表の行の変更なので、裁定の id と時刻が要る（条 P-17.1）。** what と値は変えない。

**理由。** 1 は、平易文だけを読むと、ADR-21 が狭めた置き場と宣言の形まで v3 に当たると読め、決定と逆になっていた（止める）。2 は、決定 (1) の狭めと決定 (2) が平易文に無く、発効の後の答えが注にしか無かった。3 と 4 は、着地や置き換えの後も「後続」「厳格」の字が残り、案内された 1 か所を読むと今と逆の定めが読めた。5 は、骨格が判断の記録を 1 本書くと決めた後も「最初は 0 本」の字が残り、その字が骨格の雛形として利用者の置き場へ写る。6 は、同じ種類の失敗の定めが 2 行に在るように読めた。

**代替案。**
- **(A) 1 を、ADR-21 の改訂の欄の側だけで足りるとして直さない。** 平易文は持ち主が読む層なので、決定と逆に読める字は残せない。反証の記録も、注が正しく限っていても平易文の読み手の層ではないと結んだ。
- **(B) 6 を、行 R-7 の same_failure の欄の側に天井の読みを書く形にする。** 同じ種類の定めの正本の側に読みが寄る。ただし行 R-7 は値（2 回）を持つ閾値の行で、欄の変更は引き金の欄の変更になり、行 D-14 の注の 1 文より重い。

**コスト。** 承認 1 回。判断の記録の平易文 3（ADR-16・ADR-21・ADR-18）と注 6（ADR-16・ADR-21・ADR-20・ADR-18・ADR-8・ADR-6）・相談窓口の正本の 2 か所・行 D-14 の注と裁定の欄。**義務は 1 つも増えない。** 周の引き金の欄は動かない。承認の後に行 D-14 の裁定の id と時刻と逐語を席が書く。

**推奨 = 1〜6 を当てる（代替案は採らない）。**

### 🔴 問 3. 判断の記録 ADR-22 の発効との依存（整合 F-1〔止める〕・F-2・F-3）

**やりたいこと。** この問では新しく問わない。32 周目の整合 F-1（止める・反証 支持）は、判断の記録 ADR-3 決定 (8)「scribe2 の設計文書の中の契約表を folio2 の M1 で設計ノートの YAML 正本と導出物へ移す」を狭める改訂の欄がどの判断の記録にも無いという所見である。新しい判断の記録 ADR-22（別の起草役が枝 `docs/adr22` で起草中）の改訂の欄の 2 行で、ADR-3 決定 (8) と、その時機を定め直した ADR-16 決定 (1) の末尾（「契約表を持たない設計文書の移行は、要件書 CON8 のとおり M3 の後に改めて問う」）を狭める（契約表の移し先は M3 の口・scribe2 の設計文書は移らない〔ADR-21〕・v3 の設計は folio2 の形で起こす）。決定の本文は書き換えない。**判断の記録 ADR-6 決定 (2) ③（「最初は 0 本の棚」・整合 F-5）は ADR-22 の範囲の外である**（ADR-22 の起草役の判定・承認要求 `docs/design/adr-22.md` §6＝根と重さが違う）。この一括は相談窓口の問い q3 の字と ADR-6 の注を直すだけで、改訂の欄で結ぶかは台帳の控え f2-648.198（小さい判断の記録の候補）で別に扱う。整合 F-2（制約 CON8）と F-3（M3 と M1 の範囲の節の CON8 の指し）は ADR-3 決定 (8) の帰結なので、ADR-22 の承認の後に席が書く欄で直す。**答えは ADR-22 の承認要求の問 1 の答えをそのまま使う。** これは行 D-14 の止める場合 (イ)（判断の記録の発効が要る）の例外である。

**理由。** 同じ論点を 2 つの承認要求で重ねて問うと、答えが割れたときに正本の字が 2 通りに決まる。ADR-22 の承認要求は、案と撤退条件と敵対の検証を添えた全文の承認要求で、この一括は同じ論点を持たない。

**代替案。**
- **(A) ADR-22 を待たず、この一括だけを先に取り込む。** 次の周で整合 F-1 がまた立つ見込みが高く、周が 1 本むだになる。

**コスト。** この問の承認は要らない（ADR-22 の承認に含まれる）。

**推奨 = ADR-22 の承認要求の問 1 の推奨に従い、同じ回の承認で ADR-22 と一括 19 を本流へ入れる（代替案は採らない）。**

### 🔴 問 4. 判断の記録 ADR-23 の発効との依存（実態 F-1〔止める〕・読みやすさ F-1）

**やりたいこと。** この問では新しく問わない。32 周目の実態 F-1（止める・反証 支持）は、憲法の schema 節が「live が now でない条の検査結果を「まだ分からない」として出す」と約束するのに、`folio check` が 6 条ぶん 0 件を出すという所見である。新しい判断の記録 ADR-23（別の起草役が枝 `docs/adr23` で起草中）で、schema 節の live の規則を「live が now でない条は床の検査の対象外として数え、条ごとに表に出す」向きに改め、同じ改訂に前文の平易文の言い換え（読みやすさ F-1）を載せて、憲法を第 1.4 版に上げる（条 A-2）。対象外の数を要約に出す便は、ADR-23 の発効の後にその帰結として起こす。**答えは ADR-23 の承認要求の問 1 の答えをそのまま使う。** これは行 D-14 の止める場合 (イ)（条文の改訂と判断の記録の発効が要る）の例外である。

**理由。** 条文の改訂は、判断の記録と撤退条件を先に書き、敵対の検証を添え、持ち主の承認を得てから版を上げる（条 A-2.1〜A-2.3）。この一括の枝で schema 節の字を直すと、その手順を飛ばす。

**コスト。** この問の承認は要らない（ADR-23 の承認に含まれる）。憲法の schema 節と前文は周の引き金に丸ごと入る（`crates/folio/src/ceiling.rs` の憲法の範囲の定数）ので、ADR-23 の発効の後に本流で周を回す（検証の文面 d）。

**推奨 = ADR-23 の承認要求の問 1 の推奨に従い、同じ回の承認で ADR-23 と一括 19 を本流へ入れる。**

### 判断の記録 ADR-23 の起草への申し送り（問 4 の前の字・材料）

改訂 b の時点で問 4 に書いた案と見積を、ADR-23 の起草の材料として残す。**持ち主への問いではない。**

- **案。** (a) 約束どおり 6 条を「まだ分からない」に数え、`folio check` の判定を「まだ分からない」（rc 2）にする＝一番正直だが、床は 6 条の機構が動くまで合格せず、器の受付と便の検証の前提「床 4 本が rc 0」と、check の合格を前提にする歯と、凍結 anchor（floor_cases の rc 0 を期待する 21 case）が動く／(b) 6 条を条ごとに「機構未稼働（まだ分からない）」として出し、要約の行に別枠で数える（例「合格（違反 0・まだ分からない 0・機構未稼働 6）」）＝判定の値と rc は今のまま／(c) schema 節の約束を「対象外」に改める＝実装は変えないが、条 P-4.1 の向きに逆らう。起草役の推奨は (b)（条 P-4.2 の「表に出す」を条ごとの行で満たし、条 P-4.1 は要約の別枠で守り、運び方は変えない）。
- **(b) が schema 節の改訂を要る理由（起草役の読み）。** schema 節は「その検査結果を「まだ分からない」として出す」と書き、要件書の 3 値（verdicts の「まだ分からない — 検査できなかった」・要件 FR5「実行できなかった検査を合格と表示しない」）と並べると、条ごとに「まだ分からない」を出しながら要約で「合格」と言う形が、同じ 3 値の中で割れると読める。schema 節の字に 1 句（機構未稼働の条は判定の値に数えず、条ごとの行と要約の別枠に出す）を足すなら、要件 FR5 の注か規範文にも同じ断りが要るかを確かめる。
- **(b) の便の見積（M）。** `crates/folio/src/check.rs`（機構の値域の検査で `mechanism.live` を読む所の隣に、条の列を 1 周して数える口）・結果の型（`crates/folio/src/verdict.rs` の周り＝違反と「まだ分からない」の外に別枠の数）・要約の行（`crates/folio/src/main.rs` の check の出力。`folio build` の要約の行〔`crates/folio/src/site.rs`〕にも出すかは契約で決める）・要約の行の字を逐語で見る歯（`crates/folio/tests/` の 10 file に約 21 か所＝check・constitution_range・findings・freeze_root・schema・unknown_fields・adr・note・badge・site）・新しい歯（6 条ぶんの行と要約の数・live を now にすると行が消えること）。
- **凍結 anchor への響き。** `tests/floor_cases.yaml`（146 case・凍結の土台は憲法 第 1.0 版の写しで、live が now でない条が 14 本在る）は `expect_rc` と `expect_msg`（部分一致）で見る。(b) は rc を変えず、`expect_msg` に要約の行の「まだ分からない 0）」の逐語は 0 件なので、期待値は動かない見込み。(a) なら rc 0 を期待する 21 case がすべて rc 2 になる。
- **窓との関係。** 便は設計文書の置き場の file を書き換えない見込みなので門の対象外だが、ADR-23 の発効の前に起こすと、発効していない決定を実装することになる。便は ADR-23 の発効の後に起こす。
- **day-1 の先例。** Python の床（814366c の scripts/check_draft.py）は条 P-18.4 を live に応じて「まだ分からない（live delivery-0…）」と出していた（反証の記録）。

## 承認の後に席が書く欄

持ち主が答えた後に、席がこの枝（`docs/batch19`・ADR-22 の枝へ載せ替えた後）の別の commit で書く。退けた問の分は §「問どうしの依存」のとおり元の字に戻す。**「承認待ち」「起草」の字を持つ欄は全部ここに挙げた。** 書いた後に表の 2・3・5〜11 の欄を 1 つずつ開いて、一括 19 に触れる字に「承認待ち」と「起草・」が残っていないことを確かめる（`status_note` は 1 行に全版の来歴を持つので、grep の 1 本では数えられない）。ADR-22 の分の欄（制約 CON8・範囲の節・ADR-3 と ADR-22 の記帳）は ADR-22 の承認要求の、ADR-23 の分の欄（憲法 第 1.4 版・schema 節と前文・凍結 anchor の写し・ADR-23 の記帳）は ADR-23 の承認要求の §「承認の後に席が書く欄」が持つ。

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.40 → v1.41 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の頭の 2 文 | 「いま発効しているのは 第 1.40 版（2026-09-24）です。第 1.41 版（一括 19）は起草・承認待ちです。」を「いま発効しているのは 第 1.41 版（<承認の日付>）です。」の 1 文に |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.41 | 「v1.41 = 2026-09-24 起草（一括 19 = …・承認待ち）」を「v1.41 = <日付> 発効（逐語「…」・f2-648 notes <時刻> JST・一括 19 = …）」に。**頭の「起草」と末尾の「・承認待ち」の 2 か所とも書き換える**。ADR-22 と同じ版にしたら、要旨に ADR-22 の分を足す |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 19〔問 1・2〕〔と判断の記録 ADR-22・ADR-23〕の承認要求への同じ回の回答）", verbatim: <逐語>, version: v1.41}`）。作成の行は起草のまま（ADR-22 と同じ版にしたら作成の行の stamp の要旨に ADR-22 を足す） |
| 5 | `design-intent/rules.yaml` | 行 D-14 の `ruling` の先頭と `ruled_at` | 「一括 19 の承認待ち（席が承認後に…）」を「f2-648 notes <時刻> JST（逐語「…」・一括 19 の問 2 の 6 = 注に行 R-7 の same_failure の読みの 1 文・what・種別・段・状態・refs は不変）」に・`ruled_at` を承認の日付に |
| 6 | `design-intent/adr/ADR-16.yaml` | 注の【天井の 32 周目の所見への応答（一括 19・2026-09-24 起草・持ち主の承認待ち）】 | 「持ち主の承認待ち」を「持ち主の承認 <日付 時刻> JST「<逐語>」」に |
| 7 | `design-intent/adr/ADR-21.yaml` | 同じ見出し | 同じ |
| 8 | `design-intent/adr/ADR-18.yaml` | 同じ見出し | 同じ |
| 9 | `design-intent/adr/ADR-8.yaml` | 同じ見出し | 同じ |
| 10 | `design-intent/adr/ADR-6.yaml` | 同じ見出し | 同じ（段の末尾の台帳の控え f2-648.198 の句は承認に依らないので変えない） |
| 11 | `design-intent/adr/ADR-20.yaml` | 注の【決定 (5) ⑤ の便の着地（一括 19・2026-09-24 起草・持ち主の承認待ち）】 | 同じ |
| 12 | `docs/design/batch19-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に |
| 13 | `docs/design/state.md` | 要件書の版の行・憲法の版の行・判断の記録の行・次の一手・未決の表 | 要件書 = 第 1.41 版（発効の日付と逐語・一括 19〔と ADR-22〕）・憲法 = 第 1.4 版（ADR-23）／ADR-23 が決めた便・C の控え・本流の 33 周目。state.md の今の字は書き換える前に読んで、一括 19 に触れる行を全部直す |
| 14 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）・一括 19 の仕分けの在り処・行 D-14 の止める場合の例外（B・C）・枝の上の 33 周目を回さず行 D-14 (イ) で止めて出したこと・次の手番（取り込みの後の便と本流の 33 周目） |
| 15 | 台帳（`bd`） | f2-648.182 の notes | 32 周目 読みやすさ F-2（範囲の節の番号の導線）を足す（**承認に依らない・取り込みの前**） |
| 16 | 台帳（`bd`） | 実態 F-1 の便の項 | ADR-23 の帰結の便（対象外の数を要約に出す）を、ADR-23 の発効の後に起こす |
| 17 | 器の列 | 窓の保留の解放 | 取り込みで窓を閉じたら、窓を開いたときに保留にした設計文書を書き換える便の件を解放する（行 D-14「窓を閉じたら解放する」・起草の時点で列は空） |
| 18 | 本流 | 33 周目 | 枝の上に合格の印が無いので（判断の記録 ADR-19 決定 (2) の「枝の印がそのまま本流の印になる」は当たらない）、取り込みと ADR-23 の帰結の便の着地の後に、本流で 33 周目を回す |
| 19 | `design-intent/srs.yaml` | 要件書の版の扱い | ADR-22・ADR-23 を一括 19 と同じ回に出して同じ回に承認されたら、ADR-22 の承認の後の欄（制約 CON8・M1 と M3 の範囲の節）を第 1.41 版に同居させる。別の回になったら ADR-22 の分は第 1.42 版へ番号を送る（§問どうしの依存の「版の同居と取り込みの順」） |
| 20 | `design-intent/srs.yaml` | 整合 F-3 を B へ移した後の直し | M3 の範囲の節の作らないの行と M1 の範囲の節の注は、この一括では書かない。ADR-22 の承認の後の欄が書く（改訂 b で本流の字に戻した・二重に書かない） |

相談窓口の正本と語彙と行 D-14 の注以外の規則の表には承認欄の行を書かない（相談窓口の正本は一括 5 の先例どおり第 0.1 版のまま、語彙は要件書の承認で運ぶ）。

## 床の結果（この枝の HEAD で実測・2026-09-24・参考値）

binary は本流の `target/debug/folio`（68196cf で組んだもの）と、この枝で組んだ `target/debug/folio`（この枝の `crates/` と `tests/` は 68196cf と byte 一致）。改訂 b（整合 F-3 の字を戻した）の後も同じ 4 本を撃ち直した。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0）。起草の 1 度目は行 R-17 の違反 1（ADR-6 の注に ADR-3 の名を書いた散文の言及）を返したので、ADR-6 の注から ADR-3 の名を外した |
| `folio inject --check --dir design-intent` | rc 0（68 行 / 7,912 byte 一致） |
| `folio schema --dir design-intent --check` | rc 0（9 file 一致） |
| `folio derive --dir design-intent --out contracts --check` | rc 0（一致 1・差分 0） |
| `cargo nextest run --workspace --no-tests=fail --no-fail-fast` | **861 / 861 PASS**（この枝で組んだ binary・起草の字・改訂 b の字・改訂 d の字でそれぞれ 1 度） |
| `folio build --dir design-intent --out <scratch> --write` | 床 合格・28 file 書けた。受入基準 AC25 の新しい題が要件書の面に、判断の記録 ADR-21 の平易文の新しい段が ADR-21 の面に、語彙の新しい項が憲法の面の用語集に出ることを字で確かめた。1440 の幅で開いて目で見る確認はしていない |
| `folio ceiling --dir design-intent --gate --write-set design-intent/srs.yaml` | まだ分からない（印が古い（引き金の要約値の欄が無い））・rc 2＝印は 28 周目。本流 68196cf でも同じ |

**歯は 1 本も直していない。**

## まだ分からない点

- **この枝の新しい字は、どの周もまだ読んでいない。** 行 D-14 (イ) で枝の上の周を回さずに出すので、ADR-16 と ADR-21 の平易文の新しい段・AC25 の題・語彙の新しい項が新しい所見を生むかは、取り込みの後の本流の 33 周目で初めて分かる。
- **ADR-23 の帰結の便の形（要約の行の字・条ごとの行・歯）は ADR-23 の起草と便の契約で決まる**（§ADR-23 の起草への申し送り）。
- **ADR-6 決定 (2) ③ と ADR-16 決定 (2)(カ) を改訂の欄で結ぶかは決まっていない。** ADR-22 の範囲の外で、台帳の控え f2-648.198 が持つ。次の周の整合が「整合 × adr/ADR-6.yaml」で拾い直す見込みはある（独立の検証の blocking 2 の読み）。
- **ADR-16 の平易文の「scribe2 の側の読む口が 2026-09-22 に出来たので」は誤った事実のまま残る**（発効したときの字として残す段落の中・末尾の段が断る。注 (f) は直した）。次の周の実態か忠実さが拾う見込みはある（独立の検証の低い重さ 2）。
- **相談窓口の正本の版を上げなかった**（一括 5 の先例）。版を上げる運びにするなら、承認の後に第 0.2 版と承認の行を席が書く。
- **面を 1440 の幅で目で見る確認はしていない。** 席が承認要求の前に、要件書の面の AC23・AC25 と、ADR-16・ADR-21 の面の平易文と、憲法の面の用語集の新しい項を開いて見る。
