# 一括 22 の仕分け — 天井の 35 周目で次の一括へ回した 1 件（整合 F-1・制約 CON8 本文の括弧の出所）と、便 142 の着地の後続（要件 FR20 の注の「既知の穴」の字）

**状態: 起草・持ち主の承認待ち（承認の前に、席がこの枝の上で 36 周目を回す）。** 来歴（2026-09-25 起草・orchestrator 席〔起草は opus 5.5 の起草役に委ねた〕・作業場所 `.worktrees/planner-batch22`・枝 `docs/batch22`・土台 = 本流 `2fbc1af`〔一括 21 の取り込み `f85d346` の後に、便 142 の契約 `84a9a0e` と着地 `2fbc1af` が載った版〕）。持ち主への承認要求は **🔴 問 1** の 1 つである。問の案は席の推奨の案で書いた（判断の記録 ADR-19 決定 (1) ①）。

**この一括は周の引き金の欄を 1 か所動かす**（要件書の制約 CON8 の本文）。行 D-14 の頭の文により、**承認を求める前に、この枝の上で 36 周目を回す**（§運び方・§承認の前に席が行う段取り）。周は席が回す。起草役は回していない。

材料は 2 件である。どちらも台帳 f2-648.215 に在る。

| # | 出所 | 観点 | id | 重さ | place | 台帳 |
|---|---|---|---|---|---|---|
| 1 | 天井の 35 周目（一括 21 の枝 `3634009` の上・`~/.local/share/folio2/ceiling/2026-09-25-round35/`・4 観点とも合格） | 整合 | F-1 | 直す | `srs` `constraints.CON8.text` | f2-648.215（description） |
| 2 | 便 142（台帳 f2-648.214・契約 `docs/design/delivery-142.md` §1 (a) の 5・6 と §5・着地 main `2fbc1af`）の後続 | — | — | — | `srs` `requirements.FR20.note` | f2-648.215 notes 2026-09-25 20:21・22:22 JST |

**35 周目の印は本流の印である。** 35 周目は一括 21 の枝の上で回した周で、その印（`dcd8544`）は一括 21 の取り込み（`f85d346`）で本流の印になった（判断の記録 ADR-19 決定 (2)）。材料 1 は、その周が「引き金の欄なので一括 21 では直さない」として次の一括へ回した 1 件である（一括 21 の仕分け §この一括に入れなかったもの・承認の後の欄 #13）。材料 2 は周の所見ではなく、便 142 の契約が「着地の後の次の一括で注を直す」と控えた字である。行 D-14 の「1 回の承認要求は本流の 1 つの周の所見から切った 1 つの一括とし、周をまたいで束ねない」には当たらない（周の所見は 35 周目の 1 周だけで、材料 2 は所見でない）。

**生成区間に在る材料は無い。面の生成器の直しも無い。** 2 件とも要件書の正本の字の直しで、便では運べない（要件書の正本は設計文書の置き場に在り、門の対象になる）。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る・問 1）。**B** = 便へ回す。**C** = 控え（この一括では直さず、理由を残す）。

| # | 観点 | id | 是正の内容 | 引き金 | 仕分け | 問 |
|---|---|---|---|---|---|---|
| 1 | 整合 | F-1 | 制約 CON8 の本文の前の文の括弧「（判断の記録 ADR-16 決定 (5)(7)・ADR-21 決定 (1)）」を「（判断の記録 ADR-16 決定 (1)(5)・ADR-21 決定 (1)）」に、後の文の括弧「（要件 FR11・便 119・2026-09-24 着地）」を「（判断の記録 ADR-16 決定 (5)(7)・要件 FR11・便 119・2026-09-24 着地）」にした（下の判定 1）。見出しと来歴の 2 文と `source` は変えない | **動く**（`TRIGGER_SRS_CONSTRAINT` = id・text） | A | 1 |
| 2 | — | 便 142 の後続 | 要件 FR20 の注の末尾の段「門には既知の穴が在る（…未解決）。…直るまでは、門を作業ツリーの一番上の置き場から撃つ（第 1.43 版で足した・…）。」を、穴の来歴と、便 142 で閉じた後の門の答え（照らせないときは まだ分からない と撃ち直し方）と、規範文の「1 つも無ければ 0」の前提（同じ根で照らせること）の字にした（下の判定 2） | 動かない（要件の `note` は `TRIGGER_SRS_REQUIREMENT` の外） | A | 1 |
| 3 | 実態 | 35 周目 F-2 の 2 つ目の直す先 | 要件 FR20 の規範文に write-set の path の基点（`--dir` と同じ根）を書く案。この一括では規範文を変えない（下の §この一括に入れなかったもの） | — | C | — |

**数。** A = 2（35 周目 整合 F-1・便 142 の後続）。B = 0。C = 1。計 3。

**A で動く周の引き金の欄は 1 か所である**（制約 CON8 の本文）。引き金の欄の正は実装の型付きの定数（`crates/folio/src/ceiling.rs` の `TRIGGER_*`）で、要件の行の引き金は id・shall・strength・pattern・when・verify・milestone の 7 欄、制約の行は id・text の 2 欄である。要件 FR20 の注・要件書の来歴と承認欄は引き金の外である。**規範文（shall）・番号・段の欄・受入の判定の文・条文・判断の記録・規則の表・天井の正本・語彙は 1 字も動かしていない。** 引き金の欄が 1 か所だけであることは、写しの上で制約 CON8 の本文を本流の字に戻すと `folio ceiling --gate` が「通す」を返し、FR20 の注だけを戻すと「まだ分からない」を返すことで確かめた（§床の結果）。

### 判定 1 ＝ 制約 CON8 の前の文は ADR-16 決定 (1)(5)、後の文は ADR-16 決定 (5)(7) を出所にする

- **所見の指摘は正しい。** 前の文は「契約表を設計ノートの YAML 正本の節に置き、器が読む導出物をそこから作る形は、M3 の出口の利用者…が自分の repo で最初からその形で書く契約表に当てる」と言う。当てる相手を決めるのは ADR-16 決定 (1)（M3 の範囲＝契約表を持つ設計ノートを外部の利用者が置き場に置き、器が読む導出物をそこから作れる）で、ADR-21 決定 (1) がその利用者を v3 の設計の置き場と読み替えた。ADR-16 決定 (7) は便の列と順序で、当てる相手を決めていない。
- **前の文の括弧は ADR-22 決定 (2) と同じ (1)(5) にした。** ADR-22 決定 (2) の逐語は「その向きを M3 の出口の利用者（v3 の設計の置き場）が自分の repo で最初から設計ノートの形で書く契約表に当てるのは、ADR-16 決定 (1)(5) と ADR-21 決定 (1) である」。ADR-16 決定 (5) は導出物の形と、器の受付がその導出物を読んで通すこと（判定点 ③）を定め、前の文の「器が読む導出物をそこから作る形」に当たる。
- **後の文の括弧に ADR-16 決定 (5)(7) を足した。** ADR-22 決定 (1) の逐語は「導出物の口の段は、ADR-16 決定 (5)(7) と要件 FR11 の段の欄（M3）が持つ」。後の文「導出物を作る口は M3 の口として作った」の「M3 の口として」の出所がこれに当たる。一括 21 で 1 文を 2 文に割ったとき、(5)(7) は後の文の出所なのに前の文に残った（所見の読み）。
- **台帳の控えの字「決定 (1) だけ」とは違う形である。** 台帳 f2-648.215 の description は「括弧を判断の記録 ADR-16 決定 (1)・ADR-21 決定 (1) に直す」と書き、根拠に「ADR-22 決定 (2) もそう書く」を挙げる。ADR-22 決定 (2) の実物は「(1)(5)」で、所見 F-1 の勧めも一括 21 の仕分けの承認の後の欄 #13 も「前の文は (1)(5)・後の文は (5)(7)」だった。「(1)」だけにすると、ADR-22 決定 (2) と制約の本文が別の出所を言う形になり、次の周の整合が同じ所を拾う見込みがある。そこで所見と #13 の形を推奨にし、台帳の字の形を代替案 A に置いた（問 1）。どちらも同じ引き金の欄の中の直しで、周の数は変わらない。

### 判定 2 ＝ 要件 FR20 の注は、穴を閉じた後の字にし、規範文の前提を 1 句で書く

- **便 142 の着地で注の字が実態と食い違った。** 第 1.43 版の注は「門には既知の穴が在る（…未解決）」「直るまでは、門を作業ツリーの一番上の置き場から撃つ」と書く。便 142（台帳 f2-648.214・main `2fbc1af`）は `crates/folio/src/gate.rs` の run の最初に根の突き合わせを置き、照らせないときに まだ分からない を返す形にした。
- **新しい字は契約と実装に合わせた。** 契約 `docs/design/delivery-142.md` §1 (a) の 5 は「規範文の『1 つも無ければ 0』は、write-set と置き場を同じ根で照らせることを前提にしている。だから今の dir の外の `--dir` では、実装の file だけの write-set でも照らせず 2 になる」「次の一括で FR20 の注を直すときも、この前提を 1 句で書く」と書き、§1 (a) の 6 と §5 が字の向き（穴は便 142 で閉じた・一番上以外から撃つと まだ分からない と撃ち直し方を返す）を書く。理由の字は `gate.rs` の型付きの定数（`UNKNOWN_DIR_OUTSIDE` = --dir が今の dir の下に無い・write-set の根と照らせない・作業ツリーの一番上から撃つ／`UNKNOWN_OTHER_ROOT` = --dir と write-set の根が違う／`FROM_THE_TOP` = 作業ツリーの一番上から撃つ）で、注はそれを写さず 2 つの場合の名と撃ち直し方だけを書く。
- **「一番上以外から撃つと必ず まだ分からない」とは書かなかった。** 便 142 の §1 (b) の 5 のとおり、本流の一番上から作業ツリーの置き場を今の dir の下の `--dir` で渡し、write-set が実装の file だけなら、門は今までどおり 0（設計文書の正本を書き換えない便）を返す。起草役が本流の binary（`2fbc1af` の後の組み立て）で撃って確かめた。

| 撃つ所（今の dir） | --dir | write-set | 終了コード | 理由の行 |
| --- | --- | --- | ---: | --- |
| 作業ツリーの一番上 | design-intent | design-intent/srs.yaml | 0 | 通す（印が 4 観点とも合格・引き金の要約値が同じ…）※字を直す前 |
| 本流の一番上 | 作業ツリーの置き場（相対） | design-intent/srs.yaml | 2 | --dir と write-set の根が違う＝design-intent/srs.yaml・作業ツリーの一番上から撃つ |
| 本流の一番上 | 作業ツリーの置き場（絶対） | design-intent/srs.yaml | 2 | 同上 |
| 本流の一番上 | 作業ツリーの置き場（相対） | crates/folio/src/gate.rs | 0 | 通す（設計文書の正本を書き換えない便） |
| 無関係の dir | 作業ツリーの置き場（絶対） | crates/folio/src/gate.rs | 2 | --dir が今の dir の下に無い・write-set の根と照らせない・作業ツリーの一番上から撃つ |
| 作業ツリーの crates/ | ../design-intent | design-intent/srs.yaml | 2 | 同上 |
| 作業ツリーの一番上 | design-intent | 置き場の要件書の絶対 path | 2 | --dir と write-set の根が違う＝<絶対 path>・作業ツリーの一番上から撃つ |
| 本流の一番上 | design-intent | design-intent/srs.yaml | 0 | 通す（本流の置き場・対照） |

- **来歴の字は残した。** 注は版ごとの来歴を持つ欄なので、第 1.43 版で穴を書いたこと（天井の 35 周目 実態 F-2）と、穴の中身（黙って 0 を返していた・条 P-4.1 に反する）を過去の字で残し、末尾に「第 1.44 版で着地の後の字にした」を置いた。第 1.43 版の字のうち「本流の一番上から…絶対 path でも相対 path でも当たる」の括弧は、新しい字の場合の名（今の dir の下に無い・根が違う）と重なるので外した。
- **2 は判定が実行できないときの族に置く。** 受入基準 AC18 の 7 場合は根が合っている前提の場合で、便 142 は 7 場合を増やさず、門が既に持つ まだ分からない の族（印が読めない・引き金の要約値が測れない など）に置いた（契約 §1 (a) の 5）。注にもそう書いた。

### 判定 3 ＝ 版は第 1.44 版にし、効く版と来歴の頭の文は変えない

- 一括 21 の判定 4 と同じ形にした。`meta.version` だけを v1.44 にし、`meta.effective_version` は v1.43 のまま、`status_note` の頭の文「いま発効しているのは 第 1.43 版（2026-09-25）です。」は変えない。便 138 の後は、要件書の面の承認欄のリードが版の欄と効く版から「v1.44 は起草・承認待ち」を出す（この枝の組み立てで 3 か所に出ることを確かめた）。
- `status_note` の来歴の末尾に「v1.44 = 2026-09-25 起草（…・裁定 id〔承認の後に席が書く〕・承認待ち）」を、`approval` に作成の行を足した。承認の行は書いていない。
- 語彙は触っていない。

## 便の候補（B）の中身

無い。

## 運び方（行 D-14 の窓）

- **窓は枝 `docs/batch22` を切った時点で開いた。** 窓の間は、本流の設計文書の正本を書き換える便の台帳の件を、器の列で保留にする（`pipe dispatch hold`・行 D-14・席の手番）。
- **承認を求める前に、この枝の上で 36 周目を回す。** この一括は引き金の欄を 1 か所動かす（制約 CON8 の本文）。36 周目が 4 観点とも合格したら、36 周目の所見と結果を添えて問 1 を 1 回の承認要求で出す。不合格なら、支持の止めるに直しを当てて回し直す。
- **引き金を動かさない形は選べない。** 材料 1 は引き金の欄そのものが直す先で、引き金の外で閉じる字が無い。材料 2 だけなら引き金は動かず、行 D-14 の「注だけの是正」に当たるが、同じ要件書の版で運ぶので問 1 に入れた。
- **行 R-7 の数え（行 D-14 の止める場合）には当たらない。** 数えるのは窓の中の反証で支持された止めるで、この窓は枝を切ったところで 0 から始まる。材料 1 は 直す である。
- **直す と 参考 の繰り返し（窓で区切らない数え・R-7 と D-14 の数えとは別）。** 1〜35 周目の束の `findings.yaml` 141 本を読み、場所（doc と at）が同じ所見を数えた（観点は問わない・script と出力は控え `batch22-scripts/repeat.py`・`repeat.txt`）。

| 場所 | 前の周（周・観点・id・重さ） | 中身が同じ指摘か |
|---|---|---|
| `srs` `constraints.CON8.text`（材料 1） | 27 周目 整合 F-6（直す）・32 周目 整合 F-2（直す）・33 周目 読みやすさ F-4（直す）・34 周目 実態 F-2（参考） | 別の指摘（27 周目 = 移行を問う時機・32 周目 = M1 で移す順序・33 周目 = 本文の並び・34 周目 = 「M3 で作る」の時制）。括弧の出所の指摘は 35 周目が初めてで、34 周目の直し（一括 21 の 2 文への割り）が生んだずれである |
| `srs` `requirements.FR20.note`（材料 2） | 13・14・16・18・26・30・32 周目に計 11 件 | 所見ではないので数えの対象外。参考に、注の「着地の前の字が着地の後も残る」形の指摘は 14 周目 実態 F-1・26 周目 実態 F-2・30 周目 実態 F-3・32 周目 実態 F-4 に在った。今回は着地と同じ日に字を直す |

  制約 CON8 の本文は 5 周続けて所見が立った場所である。どれも中身は別だが、直しが次のずれを生む形が 34 → 35 周目で 1 回起きた。36 周目で同じ場所に所見が立ったら、席は本文の書き方（出所の括弧を文ごとに持つか、`source` の欄へ寄せるか）を問い直す材料にする。

## 問どうしの依存

| 問 | 依存と、退けたときに席が戻すもの |
|---|---|
| 問 1 | **要件書 第 1.44 版の中身は問 1 の答えで決まる。** 項 1 と項 2 は互いに独立である。項 1 を退けたら制約 CON8 の本文を第 1.43 版の字に戻し、来歴（`meta.status_note` の v1.44）と作成の行の stamp から項 1 の句を外す＝引き金の欄は動かず、36 周目の結果は要らなくなる（戻した字は本流の印と同じ引き金の要約値になる）。代替案 A を採るなら、項 1 の字を「（判断の記録 ADR-16 決定 (1)・ADR-21 決定 (1)）。導出物を作る口は M3 の口として作った（要件 FR11・便 119・2026-09-24 着地）。」にし、来歴の句を直す（引き金の欄の直しなので、周は推奨の字で回した後なら回し直す）。項 2 を退けたら FR20 の注を第 1.43 版の字に戻す（注は古い字のままになる）。問 1 を全部退けたら版を上げない＝`meta.version` を v1.43 に戻し、来歴の v1.44 の項と作成の行を外す |
| 36 周目 | 36 周目の所見で直しが増えたら、問 1 の項に足してから出す（一括 22 の枝の上の直しとして同じ承認要求に載せる・行 D-14「枝の上で回したすべての周の所見と直しと結果を添えて」）。足した直しが引き金の欄を動かしたら、37 周目を回す |

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 要件 FR20 の規範文に write-set の path の基点（`--dir` と同じ根）を書く案（35 周目 実態 F-2 が挙げた 2 つ目の直す先・仕分け C） | 便 142 の契約 §1 (a) の 5 は、根が合わないときの まだ分からない を「判定が実行できないとき」の族に置き、要件書の改訂を要らないとした。同じ族の 印が読めない・引き金の要約値が測れない も規範文に書いていない。規範文を変えると引き金の欄がもう 1 つ動き、受入基準 AC18 の場合と確かめ方の字も問うことになる。今回は注に前提を 1 句で書き、規範文の字と実装の答えの関係を読めるようにした。36 周目が規範文と実装の食い違い（今の dir の外の `--dir` で実装だけの write-set が 2）を所見に挙げたら、席は規範文の改訂を次の一括の材料にする |
| 制約 CON8 の `source` の欄 | 引き金の外で、今の字（ADR-3 決定 (8)・ADR-22・scribe2 planner の裁定・持ち主の裁定）は本文の括弧と別の役（制約の出所の全体）を持つ。所見は `source` を挙げていない |
| 判断の記録 ADR-16・ADR-21・ADR-22 の注 | 発効した判断の記録の字は、制約の括弧の出所の直しに依らない。改訂にも当たらない |
| `docs/design/state.md` | 一括 20・21 の起草も触らず、承認の後の記帳で書き換えた先例に従う（承認の後の欄 #6） |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.44 版の起草。** 制約 CON8 の本文の 2 つの括弧（引き金）／要件 FR20 の注の末尾の段（引き金の外）／`meta.version` を v1.44 に・`status_note` の末尾に v1.44 の起草（裁定 id の枠は「承認の後に席が書く」）・`approval` に 作成 の行。**`effective_version` は v1.43 のまま・来歴の頭の文は足さない（判定 3）・承認の行は書いていない** |
| `docs/design/batch22-triage.md` | **新設**（この file） |

**憲法・規則の表・判断の記録・語彙・天井の正本・入口・索引の欄の決まりの正本・相談窓口の正本・支度表・実装（`crates/`）・凍結 anchor（`tests/fixtures/`・`design-intent/anchors/`）・生成区間・印（`design-intent/preview/ceiling-stamp.yaml`）・歯は 1 byte も触っていない。**

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.44 版（制約 CON8 の本文の括弧の出所・要件 FR20 の注の門の穴を閉じた後の字）

**やりたいこと。** 要件書を第 1.44 版に上げ、次の 2 つを直す。**shall（規範文）・番号・段の欄・受入の判定の文は 1 字も変えない。** 周の引き金の欄が動くのは項 1 だけである。

1. **（引き金の欄が動く項）** 制約 CON8 の本文の前の文「…契約表に当てる」の出所の括弧を「判断の記録 ADR-16 決定 (1)(5)・ADR-21 決定 (1)」にし、後の文「導出物を作る口は M3 の口として作った」の括弧に「判断の記録 ADR-16 決定 (5)(7)」を足す。判断の記録 ADR-22 の決定 (2) と (1) が同じ 2 文の出所をこの形で書いている（35 周目 整合 F-1）。
2. 要件 FR20 の注の「門には既知の穴が在る…未解決…直るまでは、門を作業ツリーの一番上の置き場から撃つ」を、便 142（2026-09-25 着地）の後の字にする。穴は閉じた。いまは作業ツリーの一番上以外から撃って照らせないとき、門は「まだ分からない」と撃ち直し方（作業ツリーの一番上から撃つ）を返す。「1 つも無ければ通す」の規範文は、書き換える file の一覧と置き場を同じ根で照らせることが前提だ、という 1 句も書く。

**理由。** 1 は、一括 21 で本文を 2 文に割ったとき、後の文の出所だった決定 (7)（便の順序）が前の文（当てる相手）の括弧に残った。2 は、便 142 で門の穴が閉じたのに、注が「未解決・直るまではこう撃つ」と言い続ける。

**代替案。**
- **(A) 項 1 を、前の文の括弧を「判断の記録 ADR-16 決定 (1)・ADR-21 決定 (1)」にするだけにする**（台帳 f2-648.215 の控えの字）。後の文の括弧は変えない。ただし ADR-22 決定 (2) は同じ文の出所を「(1)(5)」と書き、ADR-22 決定 (1) は後の文の出所を「(5)(7)」と書くので、制約の本文と判断の記録が別の出所を言う形が残る。
- **(B) 項 2 の代わりに、要件 FR20 の規範文に write-set の path の基点を書く**（35 周目 実態 F-2 が挙げたもう 1 つの形）。引き金の欄がもう 1 つ動き、受入基準 AC18 の場合と確かめ方も問うことになる。

**コスト。** 承認 1 回と、承認の前の 36 周目 1 回（項 1 が引き金を動かすため）。制約の本文の括弧 2 つ・要件の注 1 段。**要件の番号は 1 つも動かず（条 P-7.1）、義務は 1 つも増えない。** 床の歯は 1 本も直さない。

**推奨 = 1・2 を当てる（代替案は採らない）。**

## 承認の前に席が行う段取り（枝の上の 36 周目）

行 D-14 と判断の記録 ADR-19 の順である。どれも持ち主の承認に依らない。

1. **本流を枝へ取り込む**（行 D-14「周の前ごとに本流を枝へ取り込む」）。起草の時点の土台は本流 `2fbc1af`。36 周目の束を組む前に本流が動いていたら取り込む。
2. **枝の HEAD で 36 周目を回す**（束を組み、4 観点を回し、止めるがあれば反証を当てる）。門（`folio ceiling --gate`）は作業ツリーの一番上から撃つ（便 142 の後は、一番上以外から撃つと まだ分からない を返す）。本流の `target/debug/folio` は `2fbc1af` の後の組み立て（2026-09-25 22:21）である。
3. **不合格なら**、支持の止めるのそれぞれに直しを当てて回し直す。直しを当てずに回し直してよいのは実行できなかった回だけで、窓の中で 1 回まで（行 D-14）。行 D-14 の what の末尾の止める場合に当たったら、往復を止めて持ち主へ出す。
4. **4 観点とも合格したら**、枝の上で印（`design-intent/preview/ceiling-stamp.yaml`・`folio ceiling --stamp`）を書いて commit する。この印が取り込みで本流の印になる（ADR-19 決定 (2)）。
5. **36 周目の所見の直しを問 1 に足す**（直す・参考。一括 22 の枝の上の直しとして同じ承認要求に載せる）。足した直しが引き金の欄を動かしたら、2 に戻る。
6. **1 回の承認要求で出す**（35 周目の 1 件と便 142 の後続、36 周目の所見・直し・結果を添える）。

## 承認の後に席が書く欄

持ち主が答えた後に、席がこの枝（`docs/batch22`）の別の commit で書く。退けた項の分は §「問どうしの依存」のとおり元の字に戻す。**「承認待ち」「起草」の字を持つ欄は全部ここに挙げた。** 書いた後に #1〜#5 の欄を 1 つずつ開いて、一括 22 に触れる字に「承認待ち」と「起草・」が残っていないことを確かめる（`status_note` は 1 行に全版の来歴を持つので、grep の 1 本では数えられない）。

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.43 → v1.44 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の頭の 1 文 | 「いま発効しているのは 第 1.43 版（2026-09-25）です。」を「いま発効しているのは 第 1.44 版（<承認の日付>）です。」に |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.44 | 「v1.44 = 2026-09-25 起草（一括 22 = …・裁定 id〔承認の後に席が書く〕・承認待ち）」を「v1.44 = <日付> 発効（逐語「…」・f2-648 notes <時刻> JST・一括 22 = …・問 1 の 1〜2 を…）」に。**頭の「起草」と末尾の「裁定 id〔承認の後に席が書く〕・承認待ち」の 2 か所とも書き換える**。36 周目の直しを足したら要旨にも足す |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 22〔問 1〕の承認要求への回答）", verbatim: <逐語>, version: v1.44}`） |
| 5 | `docs/design/batch22-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に。36 周目の結果（周の置き場・判定・所見の数・印の commit）を足す |
| 6 | `docs/design/state.md` | 書いた時点・基準・統合先・最後に着地した便・天井の印・要件書の行・次の一手・未決 | 要件書 = 第 1.44 版（発効の日付と逐語・一括 22）／天井の印 = 36 周目／便 142 の着地。書き換える前に読んで、一括 21 の後に動いた行を全部直す |
| 7 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）・一括 22 の仕分けの在り処・枝の上の 36 周目の結果・次の手番 |
| 8 | 台帳（`bd`） | f2-648.215 | 2 件を当てたことを notes に記帳して close（代替案 A を採らなかったなら、description の「決定 (1)」の字と違う形で当てたことも書く） |
| 9 | 器の列 | 窓の保留の解放 | 取り込みで窓を閉じたら、窓を開いたときに保留にした設計文書を書き換える便の件を解放する（行 D-14） |

## 床の結果（この枝の起草の字で実測・2026-09-25・参考値）

binary は本流の `target/debug/folio`（`2fbc1af` の後の組み立て・2026-09-25 22:21・この枝の `crates/` と `tests/` は `2fbc1af` と同じ）。作業ツリーの一番上で撃った。控え `~/.local/share/folio2/handoff-2026-09-25/batch22-draft.md` に command と字を全部写した。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0）・rc 0。標準エラーに「機構がまだ無い条」の 1 行（P-11・P-13・P-15・P-17・P-18・A-3） |
| `folio inject --check --dir design-intent` | rc 0（68 行 / 7,912 byte 一致）。憲法を触らないので本流と同じ |
| `folio schema --dir design-intent --check` | rc 0（9 file 一致） |
| `folio derive --dir design-intent --out ../contracts --check` | rc 0（一致 1・差分 0） |
| `folio build --dir design-intent --out <一時 dir> --write` | 床 合格・rc 0・30 file 書けた（1,935,474 byte）。要件書の面に制約 CON8 の新しい括弧 2 つ、FR20 の注の「穴は便 142」「第 1.44 版で着地の後の字にした」が出て、「門には既知の穴が在る」「直るまでは」は 0 か所、承認欄のリードに「v1.44 は起草・承認待ち」が 3 か所、`<title>` は「folio2 — 要件書（v1.44）」であることを字で確かめた。1440 の幅で開いて目で見る確認はしていない |
| `folio ceiling --gate --dir design-intent --write-set design-intent/srs.yaml` | **まだ分からない（印が古い（引き金の要約値が違う））・rc 2**。写しの上で制約 CON8 の本文を本流の字に戻すと「通す（…印の後に引き金の外の変更が在る（節点 3 個…））」・rc 0（FR20 の注と meta は残したまま）、FR20 の注だけを戻すと rc 2、両方戻すと rc 0＝引き金を動かすのは制約 CON8 の本文の 1 か所だけ。代替案 A の字にしても rc 2（同じ欄が動く）。本流 `2fbc1af` の一番上の同じ命令は「通す（…節点 3 個…）」・rc 0 |
| nextest | 撃っていない（この一括は正本の字の直しだけで、作業ツリーに組み立ての置き場が無い・`crates/` と `tests/` は本流と同じ） |

**歯は 1 本も直していない。**

## まだ分からない点

- **この枝の新しい字は、どの周もまだ読んでいない。** 36 周目（枝の上）で初めて分かる。特に、制約 CON8 の本文の括弧が 2 つとも判断の記録を名指す形が長くないか（読みやすさ）、FR20 の注の新しい段が規範文との食い違いを読ませないか（整合・実態＝今の dir の外の `--dir` で実装だけの write-set が 2 になる所）は、拾われる見込みがある。
- **台帳の控えの字（決定 (1) だけ）と推奨の字（(1)(5) と (5)(7)）の違い。** 席が控えの字を意図して書いたなら、代替案 A に戻す。どちらでも周の数は変わらない。
- **面を 1440 の幅で目で見る確認はしていない。** 席が承認要求の前に、要件書の面の制約 CON8 と FR20 の注を開いて見る。
