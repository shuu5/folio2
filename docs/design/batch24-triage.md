# 一括 24 の仕分け — 天井の 40 周目の直すと参考・tsuzuri の知らせ・一括 23 の控え 1 件（受入基準 AC18 と要件 FR20 の 8 つ目の場合の名札・ADR-24 の「同じ根 = 同じ起点」の橋と注の出所・憲法 N-4 の平易文・規則の表 D-12 の出所）

**状態: 起草・持ち主の承認待ち・枝の上の 42 周目の前（改訂 d まで）。** 枝の上の 41 周目（改訂 c の `b865e1d` の束・`~/.local/share/folio2/ceiling/2026-09-26-round41/`）は **実態 不合格**（止める F-1・反証 支持）・整合／忠実さ／読みやすさは合格・所見は計 8 件。改訂 d で 8 件を全部、周の引き金の外で当てた（§枝の上の 41 周目の所見・席の裁定＝引き金は動かさない）。行 D-14 により、不合格の後は直しを当てて枝の上で 42 周目を回す（席の手番）。**以下は改訂 c の時点の状態の字（記録として残す）:** 起草・持ち主の承認待ち・枝の上の 41 周目の前（改訂 c まで）。 改訂 b（2026-09-26・席の裁定）で、起草の問い 1 の答え「書く」に従い、門を撃つ場所の 1 文を行 D-12 の注の末尾に書き、起草の 🔴 問 3 を 🔴 問 2 に束ねた（§改訂 b）。起草の問い 2（制約 CON8 の見出しは C）と問い 3（問 1 の項の組み方はそのまま）も席が裁定した。改訂 c（2026-09-26・独立の検証 `~/.local/share/folio2/handoff-2026-09-26/batch24-verify.md`〔支持・blocking 0・非 blocking 5〕）で非 blocking の 5 件を全部当てた（§改訂 c・どれも引き金の外）。 来歴（2026-09-26 起草・orchestrator 席〔起草は opus 5.5 の起草役に委ねた〕・作業場所 `.worktrees/planner-batch24`・枝 `docs/batch24`・push していない・土台 = 本流 `0f92ae6`〔一括 23 の取り込み `c711110` の後に、40 周目の印 `c7cfa3a` と便 148 の着地が載った版〕）。起草の記録（読んだ材料・script・実測）は `~/.local/share/folio2/handoff-2026-09-26/batch24-draft.md`。

**この一括は周の引き金の欄を 2 か所動かす**（要件 FR20 の確かめ方・受入基準 AC18 の判定の文。どちらも 8 つ目の場合の名札の字だけ）。行 D-14 の頭の文により、**承認を求める前に、この枝の上で 41 周目を回す**（§承認の前に席が行う段取り）。周は席が回す。起草役は回していない。ほかの直し（平易文・注・憲法の平易文・規則の表の注と refs と裁定）は引き金の外である（写しで実測・§床の結果）。

持ち主への承認要求は **🔴 問 1**（要件書 第 1.46 版と、同じ回で直す判断の記録・憲法の平易文の字）・**🔴 問 2**（規則の表 D-12 の注と refs と、注の末尾の門を撃つ場所の 1 文）の 2 つで、1 回の承認要求に束ねる（起草の 3 問を改訂 b で 2 問にした）。問の案は席の推奨の案で書いた（判断の記録 ADR-19 決定 (1) ①）。

## 材料

材料は 9 件である。台帳 f2-648.221 に在る。7 件は天井の 40 周目（本流 `c711110` の上・`~/.local/share/folio2/ceiling/2026-09-26-round40/`・4 観点とも合格・止める 0・直す 6・参考 2）の所見である。残りは tsuzuri の設計席の知らせ 1 件と、一括 23 で控えた 1 件と、39 周目の参考 1 件（40 周目 整合 F-2 と同じ中身）。

**40 周目の印は本流の印である。** 40 周目は一括 23 の取り込みの後に本流で回した周で、印は席の PR #334（本流 `c7cfa3a`）で本流に入った。行 D-14 の「1 回の承認要求は本流の 1 つの周の所見から切った 1 つの一括とし、周をまたいで束ねない」に沿う。39 周目 整合 F-3 は、40 周目 整合 F-2 と同じ場所・同じ中身の所見なので、40 周目の所見として扱った。tsuzuri の知らせと一括 23 の控えは周の所見ではない。

| # | 出所 | 観点 | id | 重さ | place | 台帳 |
|---|---|---|---|---|---|---|
| 1 | 天井の 40 周目 | 読みやすさ | F-1 | 直す | `srs` `acceptance.AC18.red_test.sentence`（と `requirements.FR20.verify.how`） | f2-648.221 の 1 |
| 2 | 天井の 40 周目 | 読みやすさ | F-2 | 直す | `adr` `ADR-24.decision` | f2-648.221 の 2 |
| 3 | 天井の 40 周目 | 読みやすさ | F-4 | 直す | `adr` `ADR-24.plain` | f2-648.221 の 3 |
| 4 | 天井の 40 周目 | 忠実さ | F-1 | 直す | `constitution` `articles.N-4.plain` | f2-648.221 の 4 |
| 5 | 天井の 40 周目 | 実態 | F-1 | 直す | `adr` `ADR-24.note` | f2-648.221 の 5 |
| 6 | 天井の 40 周目 | 整合 | F-1 | 参考 | `rules` `discipline.D-12.note` | f2-648.221 の 6 |
| 7 | tsuzuri の設計席（tsuzuri-2c）の知らせ（持ち主の指示 2026-09-26T00:22Z・t3-hub.1） | — | — | — | `adr` `ADR-21.note` | f2-648.221 の 7・f2-648 notes 2026-09-26 09:23 JST |
| 8 | 一括 23 の仕分けの C（36 周目 読みやすさ F-4 の後半） | 読みやすさ | — | — | `srs` `constraints.CON8.title` | f2-648.221 の 8 |
| 9 | 天井の 39 周目 整合 F-3・40 周目 整合 F-2 | 整合 | F-3・F-2 | 参考 | `adr` `ADR-24.consequences`（直す先の候補 = `rules` `discipline.D-12.note`） | f2-648.221 の 9 |
| — | 天井の 40 周目 | 読みやすさ | F-3 | 直す | `adr` `ADR-18.note`（判断の記録の面の逆向きの導線） | f2-648.222（便 148 で着地済み・本流 `0f92ae6`） |

**生成区間に在る材料は無い。実装と歯は直さない。** 40 周目 読みやすさ F-3 は面の生成器の直しで、便 148（台帳 f2-648.222・本流 `0f92ae6`）が判断の記録の面に「ほかの判断の記録による改訂」の行を足して着地した。本一括では扱わない。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る）。**B** = 便へ回す。**C** = 控え（この一括では直さず、理由を残す）。

| # | 観点 | id | 是正の内容 | 引き金 | 仕分け | 問 |
|---|---|---|---|---|---|---|
| 1 | 読みやすさ | F-1 | 要件 FR20 の確かめ方と受入基準 AC18 の判定の文の 8 つ目の場合の名札「write-set と --dir を同じ根で照らせない」を「書き換える file の一覧と設計文書の置き場を同じ起点で照らし合わせられない」にした（要件 FR20 の平易文と同じ字）。FR20 の注に第 1.46 版の段（下の判定 1） | **動く**（`TRIGGER_SRS_REQUIREMENT` の verify／`TRIGGER_SRS_ACCEPTANCE` の red_test.sentence） | A | 1（項 1・2） |
| 2 | 読みやすさ | F-2 | 判断の記録 ADR-24 の平易文に「同じ起点 = 門を実行する場所・決定と文脈と改訂の欄は同じものを同じ根と書く・列の根とは別」の 1 文、注の言い換えの行に同じ写しを足した。**決定・文脈・改訂の欄の字は変えない**（下の判定 2） | 動かない（plain・note は `TRIGGER_ADR_FIELDS` の外） | A | 1（項 3） |
| 3 | 読みやすさ | F-4 | ADR-24 の平易文の「門を作業ツリー（…）の一番上以外の場所で門を実行すると」の前の「門を」を外した | 動かない | A | 1（項 3） |
| 4 | 忠実さ | F-1 | 憲法 N-4 の平易文の「この 3 つが揃っていない書き換えは」を「この 3 つが揃っていない憲法の条文の書き換え（改訂）は」にした。**条文・前文・schema 節は 1 字も変えない**＝条 A-2.2 の改訂に当たらない（下の判定 3） | 動かない（憲法の引き金は条の id・title・tier・binds・statements と前文と schema 節） | A | 1（項 5） |
| 5 | 実態 | F-1 | ADR-24 の注の境目 2 つの出所「契約 delivery-142.md §1 (b) の 3」を、実装 `other_root` と単体の歯 `f142_the_dir_and_the_write_set_share_one_root`（1 つ目の境目）と独立の検証の実測（2 つ目の境目）に直した。契約 §1 (b) の 2 の 3 の定義の字が境目で実装より粗いことと、定義の正本が決定 (1) と実装であることを 1 句添えた（下の判定 4） | 動かない | A | 1（項 3） |
| 6 | 整合 | F-1（参考） | 規則の表 D-12 の注の頭の出所の列に「ADR-18 決定 (5) の『無ければ 通す』は ADR-24 の改訂の欄が照らせるときに限った」と ADR-24 決定 (1) を足し、refs に ADR-24 を足した。裁定の欄の頭に承認待ちの字（一括 20 の形）。what・種別・段・状態は変えない（下の判定 5） | 動かない（`TRIGGER_RULES_FIELDS` の外＝注・裁定・refs） | A | 2 |
| 7 | — | tsuzuri | ADR-21 の注の末尾に「folio init --dir は v3 では tz init と読む（v3 は人が打つ binary を tz の 1 つにし folio の命令を subcommand に吸収・folio2 の repo は code を持ち込むまで今の命令のまま）」の 1 段 | 動かない | A | 1（項 4） |
| 8 | 読みやすさ | 36 周目 F-4 の後半 | 制約 CON8 の見出しの「段階」。**変えない**（下の判定 6） | 動かない（見出しは `TRIGGER_SRS_CONSTRAINT` の外） | C | — |
| 9 | 整合 | 39 周目 F-3・40 周目 F-2 | 門を作業ツリーの一番上から撃つ作法が、判断の記録 ADR-24 の帰結にだけ在り規則の表に無い。**改訂 b で**、D-12 の注の末尾に「撃つ場所は作業ツリーの一番上・規則ではなく席の手順」の 1 文を書いた（下の判定 7・§改訂 b） | 動かない（注） | A（改訂 b） | 2 |
| 10 | 読みやすさ | 40 周目 F-3 | 判断の記録の面の逆向きの導線 | — | B（便 148 で着地済み） | — |

**数（改訂 b の後）。** A = 8（材料 1〜7 と、改訂 b の材料 9）。B = 1（40 周目 読みやすさ F-3・着地済み）。C = 1（材料 8）。🔴 = 2（問 1・問 2）。起草の時点は A 7・C 2・🔴 3 だった。

**A で動く周の引き金の欄は 2 か所である。** 引き金の欄の正は実装の型付きの定数（`crates/folio/src/ceiling.rs` の `TRIGGER_*`）で、要件の行は id・shall・strength・pattern・when・verify・milestone、受入基準の行は id・title・verifies・red_test.sentence である。写しの上で 2 か所を 1 つずつ本流の字に戻しても門は まだ分からない のまま、2 か所を両方戻すと 通す になることを確かめた（§床の結果）。**規範文（shall）・番号・段の欄・強さ・場合の数と中身・条文・規則の表の what・判断の記録の決定と文脈と改訂の欄・天井の正本・語彙は 1 字も動かしていない。**

### 判定 1 ＝ 8 つ目の場合の名札を平易文の字にする（規範文の「同じ根」は変えない）

- **所見の指摘は正しい。** 名札「write-set と --dir を同じ根で照らせない」は、要件書の面の語彙に無い英字の `--dir` を持つ。語彙で「根」を引くと、意味の違う「列の根」（凍結 anchor の憲法の写しの列の最初の 1 枚）に着く。同じ受入基準 AC18 の平易文と要件 FR20 の平易文は、既に「同じ起点で照らし合わせられない」と書いている。
- **名札の字は要件 FR20 の平易文の字に揃えた。** 「書き換える file の一覧と設計文書の置き場を同じ起点で照らし合わせられない」である。AC18 の平易文は「置き場」とだけ書くが、名札の並びの 1 つ目が「設計文書の置き場の file を触らない」なので、同じ並びの中で置き場の字を揃えた（迷った点 1）。
- **場合の数と中身は変えない。** 8 場合・照らせないとき 2（まだ分からない）のままである。歯は名札の字を読まない。`crates/` と `tests/` のどこにも名札の字は無い（`grep` で 0）。歯 `crates/folio/tests/gate.rs` の `f142_` の 2 本が確かめるのは門の理由の行の字（「--dir と write-set の根が違う」「--dir が今の dir の下に無い」）で、実装の定数の字である。要件書の名札を変えても歯と食い違わない。
- **規範文の「今の dir を同じ根として照らす」は変えない。** 規範文はその文の中で write-set（書き換える file の一覧）と --dir と根の読み方を定めている。規範文は周の引き金で、所見も規範文を挙げていない。FR20 の注の第 1.46 版の段に、規範文の「同じ根」と平易文と名札の「同じ起点」が同じものだと書いた。
- **FR20 の注の第 1.45 版の段の「8 つ目 = write-set と --dir を同じ根で照らせない → 2」は変えない。** 第 1.45 版の直しの記録の字である。

### 判定 2 ＝ ADR-24 の「同じ根」と「同じ起点」は平易文と注でつなぎ、決定は触らない

- **決定の字は変えない。** ADR-24 は 2026-09-26 に発効した判断の記録で、決定の欄は周の引き金である。発効した決定の本文は書き換えない作法がある（ADR-24 決定 (4)・ADR-21 決定 (6)・判断の記録の欄の決まりの revises_note）。「同じ根」を「同じ起点」に替えるのは字句の直しでも決定の本文の書き換えで、自分自身の決定を改訂の欄で変える口は無い。
- **つなぎは平易文と注に置いた。** 平易文は判断の記録の面の頭に出て、持ち主が最初に読む。「直した後の門は…添えます。」の次に「『同じ起点』は門を実行する場所のことで、一覧の path も置き場の path もそこから読みます。この判断の決定・文脈・改訂の欄は、同じものを『同じ根』と書きます（凍結した憲法の写しの列の『列の根』とは別のものです）。」を足した。注の言い換えの行（置き場の path = --dir など）にも同じ写しを足した。
- **決定を触る形は採らなかった。** 触るなら、ADR-24 を改訂の欄で変える新しい判断の記録が要る。字句のつなぎのために判断の記録を 1 本起こすのは費用が重く、平易文の 1 文で読み手の取り違えは閉じる（迷った点 2）。

### 判定 3 ＝ 憲法 N-4 の平易文は改訂に当たらないので本一括に入れる

- **条 A-2.2 に照らした。** 条 A-2.2 は「条文（id・見出し・規範文・段・縛る相手）と schema 節・前文が 1 文字も変わらない変更を改訂として扱わない」。平易文はこの一覧に無い。N-4 の機構の注も、数える母集団を「schema 節・前文・各条の id / title / statements / tier / binds」とする。平易文だけの直しは改訂に当たらず、憲法の版は上がらない。周の引き金（`TRIGGER_CONSTITUTION_ARTICLES` = id・title・tier・binds・statements）の外でもある。
- **先例は一括 15 である。** 一括 15（本流 `73a6bf7`・`docs/design/batch15-triage.md` の忠実さ F-5 と 🔴 問 5）が条 A-1 の平易文を同じ扱いで直した。その仕分けは「一括 10・11 で条の平易文を同じ承認で直した先例」と書き、憲法の平易文の直しは承認欄を持たないとした。**依頼の書いた先例「一括 20 / 22」には憲法の平易文の直しは無かった**（`git show` で憲法の差分を確かめた・迷った点 3）。
- **字は規範文の範囲に合わせた。** 規範文 N-4.1 は対象を「条文の改訂」に限る。平易文を「この 3 つが揃っていない憲法の条文の書き換え（改訂）は、機械が止めます。」にした。「憲法の」で文書を、「（改訂）」で規範文の語を示した。前文と schema 節の変更も機構の母集団に入るが、規範文が「条文の改訂」とだけ言うので、平易文は規範文の範囲に留めた。

### 判定 4 ＝ ADR-24 の注の境目の出所を実装と歯と実測に直す

- **所見の指摘は正しい。** 契約 `docs/design/delivery-142.md` の §1 (b) の 3 は関数 `run` の最初の 2 つの確かめの段で、境目を書いていない。3 つ目の形の定義は §1 (b) の 2 の 3 に在り、字は「path の要素の先頭が --dir の要素の列の途中から後ろの部分と一致し、--dir の要素の列の全部とは一致しない」である。字どおりに当てると境目の 2 つとも注の例と逆になる。
- **本流の binary で境目を撃ち直した。** 作業ツリーの一番上から撃った（`batch24-draft.md` §境目の実測）。`--dir .worktrees/x/design-intent` で一覧 `design-intent` は 通す rc 0。`--dir a/a` で一覧 `a/a` は まだ分からない rc 2。どちらも注の例のとおりである。
- **出所は実装 `crates/folio/src/gate.rs` の `other_root` にした。** 「下に在る」は、その並びで始まり、並びより長いことを言う。1 つ目の境目は同じ file の単体の歯 `f142_the_dir_and_the_write_set_share_one_root` が確かめる。2 つ目の境目を確かめる歯は無いので、独立の検証の実測と一括 23 の改訂 d の実測を出所に残した。契約の字が境目で粗いことと、定義の正本が本判断の決定 (1) と実装であることを 1 句添えた。

### 判定 5 ＝ 規則の表 D-12 の注と refs に ADR-24 を足す（what は変えない）

- **所見の指摘は正しい。** D-12 の注の頭は門の式の出所に ADR-18 決定 (5) を挙げるが、その「無ければ 通す」を照らせるときに限った ADR-24 を挙げない。refs も同じである。行 D-12 から出所を辿る読み手は、狭める前の字に着く。
- **注の頭の出所の列に 1 句と ADR-24 決定 (1) を足した。** 「ADR-18 決定 (1)(3)(5)（決定 (5) の『無ければ 通す』は、判断の記録 ADR-24 の改訂の欄が、書き換える file の一覧と設計文書の置き場を同じ起点で照らせるときに限った）・ADR-24 決定 (1)・要件書 FR20 の口」。refs の末尾に ADR-24 を足した。
- **規則の表の行の変更は裁定 id と時刻が要る**（条 P-17.1）。裁定の欄の頭に「一括 24 の承認待ち（席が承認の後に裁定 id と時刻と逐語をここへ書く・…・天井の 40 周目 整合 F-1）・前の裁定 = …」を置いた。一括 20 の起草（`7468416`）が行 D-14 と R-13 の裁定の欄に置いた形に倣った。`ruled_at` は承認の後に席が書く（§承認の後に席が書く欄 #5）。
- **what・種別・段・状態は変えない。** 注と裁定と refs は周の引き金の外である（`TRIGGER_RULES_FIELDS` は注・裁定・来歴を除く）。

### 判定 6 ＝ 制約 CON8 の見出しの「段階」は変えない（C）

- **「段階」は語彙の語である。** 語彙の項 milestone の見出しは「M（段階）・M の出口」で、定義は「完成までの段階の呼び名」である。見出しの「導出物を作る口の段階」は、導出物を作る口を置く M（M3）を指す。語彙で引ける。
- **37〜40 周目の 4 周は、この見出しに所見を立てていない。** 36 周目の読みやすさ F-4 の後半（「読めるまで時間がかかる」）が最後で、一括 23 は本文を 3 文に絞った。
- **判断の記録 ADR-3 の注の段が見出しを逐語で引く。** 見出しを変えると ADR-3 の注にも 1 句が要る。見出しは周の引き金の外なので、持ち主が望めば後の一括でいつでも直せる（周を増やさない）。

### 判定 7 ＝ 門を撃つ場所の 1 文は、行 D-12 の注に席の手順として置く（改訂 b で枝に書いた・問 2）

- **この所見は 3 周続いた。** 38 周目 整合 F-3（直す）・39 周目 整合 F-3（参考）・40 周目 整合 F-2（参考）である（§運び方の繰り返しの数え）。中身は同じで、席の受付の作法（門を作業ツリーの一番上から撃つ）が判断の記録 ADR-24 の帰結の字にだけ在り、席の受付の作法の正本である行 D-12 に無い。
- **規則の行は足さない。** 規則にするなら行の追加と裁定が要る。撃つ場所の誤りで照らせない所では門が まだ分からない を返して止まる（40 周目 整合 F-2 も「条に反する越境ではない」と書く）。**改訂 d の断り:** 起草と改訂 b はここで「作法が欠けても黙って通す穴は無い」と書き、D-12 の注にも同じ約束を書いたが、41 周目の実態 F-1（止める・反証 支持）が、置き場の渡し間違い（実在しない置き場の名・置き場でない場所）では門が確かめずに 通す を返す穴を実測した（本流 `0f92ae6` でも再現・台帳 f2-648.224）。改訂 d で約束を外し、穴を穴として書いた。
- **D-12 の注に「規則ではなく席の手順」として 1 文を置く形を推奨にした。** 先例は行 D-13 の注の「契約の取り込みの前に席が作業ツリーで受付を先に撃つことは規則ではなく席の手順」である。字は下の 🔴 問 2 の 3 に在り、当てた script は `batch24-scripts/apply_q3.py` である。**起草では枝に書かず問 3 で問うたが、改訂 b で席の裁定（起草の問い 1 = 書く）により枝に書いた。** 注は引き金の外なので周は増えない。41 周目の前に書いたので、41 周目がその字を読み、3 周続いた所見が閉じたかを確かめられる。持ち主が退けたら 1 文を外すだけである。

## 改訂 b（席の裁定・41 周目の前）

席は起草の問い 1〜3 を次のとおり裁定した（2026-09-26）。

| 問い | 席の裁定 | 当てたこと |
|---|---|---|
| 1. 門を撃つ場所の 1 文を 41 周目の前に枝へ書くか | 書く | 行 D-12 の注の末尾に 1 文、裁定の欄の承認待ちの字に同じ 1 文の句（`apply_q3.py` を作業ツリーの一番上で撃った）。起草の 🔴 問 3 を 🔴 問 2 に束ね、🔴 を 2 問にした |
| 2. 制約 CON8 の見出しは C でよいか | C でよい | 何もしない（判定 6） |
| 3. ADR-24・ADR-21・憲法 N-4 の直しを問 1 の項に入れた形 | そのまま | 何もしない |

**引き金は 2 欄のままである。** 足した 1 文は規則の表の注で、周の引き金の外（`TRIGGER_RULES_FIELDS` は注・裁定・refs を除く）。改訂 b の後の門は まだ分からない rc 2 のまま、写しの上で要件書の 2 欄を戻すと 通す rc 0（`gate-revert-b.txt`）。本流の写しに `apply.py` と `apply_q3.py` を順に当てると枝と同じ字になる（diff -r で同一）。

## 改訂 c（独立の検証の非 blocking 5 件・41 周目の前）

独立の検証（`~/.local/share/folio2/handoff-2026-09-26/batch24-verify.md`・別の文脈の AI）は **支持・10 項目一致・blocking 0・非 blocking 5** だった。席の依頼で 5 件を全部当てた。どれも周の引き金の外で、引き金は FR20.verify と AC18.red_test.sentence の 2 欄のままである（`gate-revert-c.txt`）。

| # | 欄 | 当てた字 |
|---|---|---|
| 1 | 要件書の `meta.status_note` の v1.46 の句・`meta.approval` の作成の行の stamp | 改訂 b で D-12 の注の末尾に足した撃つ場所の 1 文を挙げた（来歴は「…と、注の末尾に門を撃つ場所の 1 文〔席の手順・整合 F-2・39 周目 整合 F-3〕」・作成の行は「…と、注の末尾に門を撃つ場所の 1 文」）。来歴の「門をの重なり」を「「門を」の重なり」に |
| 2 | 判断の記録 ADR-24 の注 | 契約 §1 (b) の 2 の 3 の括弧を「定義（要旨＝…〔2 番目の要素から後ろ・3 番目から後ろ…〕…--dir の要素の列の全部とは一致しない）」にした。逐語ではなく要旨と断り、契約の実物の括弧の中身を戻した |
| 3 | 判断の記録 ADR-21 の注 | 知らせの字に 6 つの crate を戻し、「知らせの言う 6 つの crate（tsuzuri の側の判断の記録の 4 番が持つ構成で、folio2 の 4 番の判断の記録とは別）」と書いた。検証役の勧めの字「ADR-4 の 6 crate」は、床の行 R-17（散文が指す判断の記録の id は型付きの欄にも要る）が folio2 の ADR-4 への参照と読んで落とした（違反 1・rc 1）。知らせの ADR-4 は tsuzuri の repo の判断の記録（`~/projects/local-projects/tsuzuri/design-intent/adr/ADR-4.yaml`）で、folio2 の ADR-4（図の道具）ではないので、id の形を使わずに書いた。2026-09-26T00:22Z を持ち主の指示の時刻として書いた（台帳 f2-648 notes 09:23 JST の書き方）。この仕分けの材料の表と問 1 の項 4 の字も同じにした |
| 4 | この仕分けの 🔴 問 2 の項 2 の引用 | 末尾の「。」を外し、規則の表の注は末尾に句点を置かない書き方だと断った |
| 5 | 🔴 の逐語（起草の記録） | 版の 3 欄（`meta.version`・`meta.status_note`・`meta.approval`）の対を足した（`verbatim.py`） |

**本流の写しに `apply.py`・`apply_q3.py`・`apply_c.py` を順に当てると枝と同じ字になる**（diff -r で同一）。

## 枝の上の 41 周目の所見（改訂 d）

41 周目は改訂 c の `b865e1d` の束で回した（`~/.local/share/folio2/ceiling/2026-09-26-round41/2026-09-26-round41/<観点>/findings.yaml`・反証の記録 `refute-notes/reality-F-1.md`）。**実態 不合格**（止める F-1・反証 支持）・整合 合格（直す 2）・忠実さ 合格（直す 1）・読みやすさ 合格（直す 3・参考 1）。計 8 件を、席の裁定（引き金は動かさない＝要件 FR20 の規範文と ADR-24 の決定は触らない）のとおり全部、周の引き金の外で当てた。

| 観点 | id | 重さ | 場所 | 所見の要旨 | 当てた字（改訂 d） |
|---|---|---|---|---|---|
| 実態 | F-1 | **止める**（反証 支持） | `rules` `discipline.D-12.note` | 改訂 b の 1 文の「手順が欠けても黙って通す穴は無い」が実装と食い違う。門は `--dir` が実在する置き場かを確かめず、`--dir design-intent/adr` や打ち間違いの `--dir design-intnet` で要件書を書き換える一覧に 通す rc 0 を返す（条 P-4.1） | 約束を外し、撃つ場所の誤り（照らせない）は まだ分からない で拾うが、置き場の渡し間違いは門が確かめず 通す を返す穴が在る、と書いた（本流 `0f92ae6` の実測・台帳 f2-648.224 に床の穴として起票済み）。段が条 P-4.1 を指すので refs に P-4（床の行 R-17 が求めた）。裁定の欄の句も揃えた |
| 整合 | F-1 | 直す | `rules` `discipline.D-12.note` | 撃つ場所の在り処の名指しが 4 か所（D-12 の注・語彙の作業ツリーの項・ADR-24 の帰結・FR20 の注）で食い違う | 正本を要件 FR20 の規範文と注に決めた。D-12 の注は「正本は要件 FR20…この注はそれを指すだけ」。FR20 の注に同じ 1 文と、既存の「その手順の正本は D-12」が門を撃つ時機を含む受付の手順の全体を言うことの断り。ADR-24 の注に在り処の段（帰結の字は発効のまま）。語彙の作業ツリーの項は既に「FR20 の規範文と注が持ち、ここには写さない」なので変えない |
| 整合 | F-2 | 直す | `adr` `ADR-18.note` | 「例と実装の在り処は両方の注が持つ」は、例が ADR-24 の注にだけ在るのと合わない | 「実装の在り処は両方の注が持ち、例（境目の 2 つを含む）は ADR-24 の注だけが持つ（要件 FR20 の注はそれを指す）」に |
| 忠実さ | F-1 | 直す | `constitution` `articles.N-4.plain` | 「決定の番号（裁定 id）」は、判断の記録の決定の番号（例 (4)）と読める | 語彙の裁定 id の短い言い換え「持ち主が決めた記録の番号（裁定 id）」に（条文・前文・schema 節は不変＝条 A-2.2 の改訂に当たらない） |
| 読みやすさ | F-1 | 直す | `srs` `requirements.FR20.plain` | 要件書の面で「同じ起点」が何か書かれていない（AC18 の平易文と名札も同じ字） | FR20 と AC18 の平易文に「同じ起点（門を実行する場所）から」、FR20 の注に「同じ起点とは、門を実行する場所を基準に、書き換える file の一覧と設計文書の置き場を同じ物差しで照らし合わせること」。名札（判定の文と確かめ方）は引き金なので変えない。語彙に項は足さない |
| 読みやすさ | F-2 | 直す | `adr` `ADR-24.plain` | 橋の 1 文が語彙に無い英字の path を使う | 「一覧に書いた file の場所の書き方も、置き場の場所の書き方も、そこから数えて読みます」に。語彙に path の項は無く、path は英字の識別子の免除一覧（R-9）に在るだけ。決定と文脈の path は引き金か発効した本文なので触らない |
| 読みやすさ | F-3 | 直す | `rules` `discipline.D-12.note` | 「今の dir」「相対で渡す」が説明なしで憲法の面の規則の表に出る | 「門を撃つ場所（命令を実行する場所）は作業ツリーの一番上」にし、「今の dir」「相対で渡す」を外した |
| 読みやすさ | F-4 | 参考 | `adr` `ADR-21.note` | tz の段が binary・subcommand・crate・code を並べる | 段の頭に「要旨＝v3 では命令の名が tz に変わるが、この判断の中身（判定点）は変わらない。」 |

**往復の数え（行 D-14 の止める場合・行 R-7）。** 窓は枝 `docs/batch24` を切った時点で 0 から始まった。反証で支持された止めるは 41 周目の実態 F-1 の 1 件で、組は（実態 × `design-intent/rules.yaml`）である。行 R-7 の 2 回には達していない。行 D-14 の what の末尾の止める場合（判断の記録の発効が要る・行 R-7 の回数に達した など）には当たらない＝直しを当てて枝の上で 42 周目を回す（席の裁定）。

**台帳 f2-648.224 の控え。** 置き場の渡し間違いで門が 通す を返す穴は、床の穴（実装 `crates/folio/src/gate.rs` の `run`・条 P-4.1）として席が台帳 f2-648.224 に起票した（memo・P2）。直しは便で運ぶ（門が `--dir` を置き場と確かめられない〔天井の正本や印の置き場が無い〕ときに まだ分からない を返す形が候補）。本一括では実装を直さず、D-12 の注に穴を穴として書いた。便が着地したら、D-12 の注の穴の文を着地の後の字にする（次の一括か便の後の記帳）。

**床の行 R-17 の実測。** 改訂 d の最初の字は D-12 の注が条 P-4.1 を名指すのに refs に P-4 が無く、`folio check` が「[R-17] rules.yaml: D-12 の散文が P-4.1 を指すのに、D-12 の型付きの欄にも P-4 の型付きの欄にも無い（型付きの欄へ書き写す）」で落ちた（rc 1）。refs に P-4 を足して合格した。

## 運び方（行 D-14 の窓）

- **窓は枝 `docs/batch24` を切った時点で開いた。** 窓の間は、本流の設計文書の正本を書き換える便の台帳の件を、器の列で保留にする（`pipe dispatch hold`・行 D-14・席の手番）。便 149（台帳 f2-648.223 の観測 1）は `crates/` だけの便と聞いている。
- **承認を求める前に、この枝の上で 41 周目を回す。** この一括は引き金の欄を 2 か所動かす。41 周目が 4 観点とも合格したら、41 周目の所見と結果を添えて問 1・問 2 を 1 回の承認要求で出す。不合格なら、支持の止めるに直しを当てて回し直す。
- **引き金を動かさない形は選べない。** 材料 1 の直す先は受入基準の判定の文と確かめ方の名札そのもので、平易文は既に正しい字を持つ。
- **行 R-7 の数え（行 D-14 の止める場合）。** この窓は枝を切ったところで 0 から始まった。
- **直す と 参考 の繰り返し（窓で区切らない数え・R-7 と D-14 の数えとは別）。** 1〜40 周目の束の `findings.yaml` 160 本（凍結 anchor の置き場を除く）を読み、場所（doc と at）が本一括の場所に当たる所見を数えた（script と出力は控え `batch24-scripts/repeat.py`・`repeat.txt`）。

| 場所 | 前の周（周・観点・id・重さ） | 中身が同じ指摘か |
|---|---|---|
| `adr` `ADR-24.consequences`（材料 9） | 38 周目 整合 F-3（直す）・39 周目 整合 F-3（参考）・40 周目 整合 F-2（参考） | **同じ指摘が 3 周続いた**（撃つ場所の作法が D-12 に無い）。改訂 b で D-12 の注に席の手順として書いた（判定 7・問 2） |
| `adr` `ADR-24.plain`（材料 2・3） | 39 周目 忠実さ F-2（参考）・読みやすさ F-2（直す） | 別の指摘（ADR-18 の注の断りの字・撃ち直し方の言い換え）。一括 23 の改訂 e で当てた |
| `adr` `ADR-24.decision`（材料 2） | 38 周目 読みやすさ F-3（参考） | 別の指摘（3 つ目の形の定義の文の長さ） |
| `srs` `acceptance.AC18`（材料 1） | 14 周目 実態 F-2（直す・fixture）・37 周目 忠実さ F-2（参考）・読みやすさ F-2（直す・平易文の「同じ根」） | 37 周目の読みやすさ F-2 は平易文の「同じ根」を「同じ起点」にした直しで、今回はその直しが判定の文の名札に届いていなかった所を拾った。同じ語の 2 か所目である |
| `rules` `discipline.D-12`（材料 6） | 22 周目 整合 F-3・29 周目 実態 F-2・30 周目 整合 F-2・読みやすさ F-3 | 別の指摘（門の撃ち方・引き金の一覧の在り処・裁定の優先・周の回し時） |
| `constitution` `articles.N-4`（材料 4） | 無し | — |
| `srs` `constraints.CON8.title`（材料 8） | 34 周目 読みやすさ F-4（参考）・36 周目 読みやすさ F-4 の後半（本文の所見に含まれる） | 34 周目は前の見出しの字への指摘で、一括 20 で見出しを改めた。今の見出しへの指摘は 36 周目の 1 件だけ |

## 問どうしの依存

| 問 | 依存と、退けたときに席が戻すもの |
|---|---|
| 問 1 | 項 1（確かめ方と AC18 の名札）と項 2（FR20 の注の第 1.46 版の段）は対で、項 1 を退けたら項 2 の段も外す。項 1 を退けると引き金の欄は動かず、41 周目は要らない（ほかの項は注と平易文だけ）。項 3（ADR-24 の平易文と注）・項 4（ADR-21 の注）・項 5（憲法 N-4 の平易文）は互いにもほかの項とも独立に退けられる（退けたらその欄を本流の字に戻す）。来歴（`meta.status_note` の v1.46）と作成の行の stamp から退けた項の句を外す。問 1 を全部退けたら版を上げない＝`meta.version` を v1.45 に戻し、来歴の v1.46 の項と作成の行を外す |
| 問 2 | ほかの問と独立である。項 1（注の頭の出所の列と refs）と項 2（注の末尾の撃つ場所の 1 文）は互いに独立に退けられる。退けた項の字を本流の字に戻し、裁定の欄の承認待ちの字から退けた項の句を外す。両方退けたら D-12 の注・refs・裁定の欄を本流の字に戻す（`ruled_at` は元から変えていない） |
| 41 周目 | 41 周目の所見で直しが増えたら、問 1 の項に足してから出す（行 D-14「枝の上で回したすべての周の所見と直しと結果を添えて」）。足した直しが引き金の欄を動かしたら、42 周目を回す |

## この一括に入れなかったもの

| 何 | なぜ |
|---|---|
| 判断の記録 ADR-24 の決定・文脈・改訂の欄の「同じ根」 | 発効した決定の本文は書き換えない（ADR-24 決定 (4)・ADR-21 決定 (6)）。つなぎは平易文と注に置いた（判定 2） |
| 要件 FR20 の規範文の「同じ根」 | 規範文の中で読み方を定めている。所見も規範文を挙げていない（判定 1） |
| 判断の記録 ADR-24 の帰結の「席の受付は行 D-12 の門を作業ツリーの一番上から撃つ」 | 40 周目 整合 F-2 が勧めた 2 つの形のうち、帰結を事実の字に留める形は、発効した判断の記録の本文の書き換えになる。D-12 の注に置く形を採った（判定 7・問 2） |
| 制約 CON8 の見出し | 判定 6 |
| `docs/design/state.md` | 一括 20〜23 の起草も触らず、承認の後の記帳で書き換えた先例に従う（承認の後の欄 #8） |
| 承認の後に席が書く欄（下の節） | **持ち主が答えるまで書かない。** 承認の逐語は捏造しない（条 P-12.3） |

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/srs.yaml` | **第 1.46 版の起草。** 要件 FR20 の確かめ方（引き金）・注／受入基準 AC18 の判定の文（引き金）／`meta.version` を v1.46 に・`status_note` の末尾に v1.46 の起草（裁定 id の枠は「承認の後に席が書く」）・`approval` に作成の行。**`effective_version` は v1.45 のまま・来歴の頭の文は変えない・承認の行は書いていない** |
| `design-intent/adr/ADR-24.yaml` | 平易文（「門を」の重なり・同じ起点と同じ根の橋の 1 文）・注（言い換えの行に同じ根・境目の出所）。見出し・状態・文脈・決定・案・改訂の欄・帰結・撤退条件・承認欄は変えない |
| `design-intent/adr/ADR-21.yaml` | 注の末尾に【v3 の命令の名】の 1 段。見出し・文脈・決定・案・改訂の欄・帰結・撤退条件・承認欄は変えない |
| `design-intent/constitution.yaml` | 条 N-4 の平易文の 1 句。条文・前文・schema 節・機構・出所は変えない |
| `design-intent/rules.yaml` | 行 D-12 の注の頭の出所の列・refs・裁定の欄の頭（承認待ち）。**改訂 b** で注の末尾に門を撃つ場所の 1 文と、裁定の欄の承認待ちの字に同じ 1 文の句。what・種別・段・状態・`ruled_at` は変えない |
| `docs/design/batch24-triage.md` | **新設**（この file） |

**天井の正本・語彙・入口・索引の欄の決まりの正本・相談窓口の正本・支度表・判断の記録（ADR-24・ADR-21 の上の欄を除く）・実装（`crates/`）・凍結 anchor（`tests/fixtures/`・`design-intent/anchors/`）・印（`design-intent/preview/`）・生成区間・歯は 1 byte も触っていない。**

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.46 版と、同じ回で直す判断の記録・憲法の平易文の字

**平易な説明。** 40 周目の AI の審査が、読み手がつまずく字を 6 か所挙げました。いちばん大きいのは、受入基準の 8 つ目の場合の名前が、語彙に無い英字（--dir）と、別の意味で使っている「根」の字で書かれていたことです。同じ受入基準の平易な説明は既に「同じ起点で照らし合わせられない」と書けているので、名前もその字に揃えます。ほかは、判断の記録 ADR-24 の中で「同じ根」と「同じ起点」が同じものだと分かる 1 文を足すこと、同じ判断の記録の説明の日本語の崩れと、注の出所の書き違いを直すこと、憲法の条 N-4 の説明が規範の文より広く読める所を「憲法の条文の書き換え」に絞ることです。あわせて、tsuzuri の設計席からの知らせ（次の世代では folio init を tz init と打つ）を判断の記録 ADR-21 の注に記録します。決まりの中身（場合の数・門の答え方・条文）は 1 つも変わりません。

**やりたいこと。** 要件書を第 1.46 版に上げ、次の 5 項を直す。**要件の番号・段の欄・強さ・規範文は 1 字も変えない。実装と歯も変えない。** 周の引き金の欄が動くのは項 1 の 2 か所である。改訂前と改訂後の逐語は起草の記録（`~/.local/share/folio2/handoff-2026-09-26/batch24-draft.md` の §問 1・問 2 の逐語）に在る。

1. **（引き金）要件 FR20 の確かめ方と受入基準 AC18 の判定の文。** 8 つ目の場合の名札を「write-set と --dir を同じ根で照らせない」から「書き換える file の一覧と設計文書の置き場を同じ起点で照らし合わせられない」に（40 周目 読みやすさ F-1）。
2. 要件 FR20 の注の末尾に第 1.46 版の段（名札を替えた理由・場合の数と中身と歯は不変・規範文の「同じ根」と名札の「同じ起点」は同じもの）。
3. 判断の記録 ADR-24 の平易文と注。平易文の「門を作業ツリー（…）の一番上以外の場所で門を実行すると」の前の「門を」を外す（読みやすさ F-4）。平易文に「『同じ起点』は門を実行する場所のことで…この判断の決定・文脈・改訂の欄は、同じものを『同じ根』と書きます（…『列の根』とは別のものです）。」を足し、注の言い換えの行にも同じ写しを足す（読みやすさ F-2）。注の境目の出所「契約 docs/design/delivery-142.md §1 (b) の 3」を、実装 `other_root`・単体の歯 `f142_the_dir_and_the_write_set_share_one_root`・独立の検証の実測に直し、契約の定義の字が境目で粗いことを 1 句添える（実態 F-1）。**決定の字は変えない。**
4. 判断の記録 ADR-21 の注の末尾に、v3 では「folio init --dir」を tz init と読む 1 段（tsuzuri の設計席の知らせ・持ち主の指示 2026-09-26T00:22Z・t3-hub.1・台帳 f2-648 notes 2026-09-26 09:23 JST）。**決定の字は変えない。**
5. 憲法 N-4 の平易文を「判断の記録・持ち主が決めた記録の番号（裁定 id）・あなたの OK の言葉、この 3 つが揃っていない憲法の条文の書き換え（改訂）は、機械が止めます。」に（40 周目 忠実さ F-1・「持ち主が決めた記録の番号」は改訂 d で語彙の裁定 id の短い言い換えに揃えた・41 周目 忠実さ F-1）。**条文・前文・schema 節は 1 字も変えない＝条 A-2.2 の改訂に当たらず、憲法の版は上がらない**（先例 = 一括 15 の条 A-1 の平易文）。
6. **（改訂 d・引き金の外）** 41 周目の直し。要件 FR20 と受入基準 AC18 の平易文の「同じ起点」に「（門を実行する場所）」を添え、FR20 の注に同じ起点の言い換えと、門を撃つ場所と撃ち直し方の正本はこの要件の規範文と注で、行 D-12 の注・語彙の作業ツリーの項・ADR-24 の帰結の 2 つ目と注はそれを指すことを書く（読みやすさ F-1・整合 F-1）。判断の記録 ADR-24 の平易文の橋の 1 文の「一覧の path も置き場の path も」を「一覧に書いた file の場所の書き方も、置き場の場所の書き方も」にし（読みやすさ F-2）、注に撃つ場所の在り処の段（帰結の字は発効のまま・整合 F-1）。判断の記録 ADR-18 の注の「例と実装の在り処は両方の注が持つ」を「実装の在り処は両方の注・例は ADR-24 の注だけ」に（整合 F-2）。判断の記録 ADR-21 の注の段の頭に要旨の 1 文（読みやすさ F-4・参考）。**決定の字はどれも変えない。**

**理由。** 1〜3 は、40 周目の読みやすさと実態の所見がそのまま当たる。名札と平易文が同じものを別の字で呼び、名札の字は語彙で引くと別の意味に着く。4 は、次の世代の置き場が命令の名を変えたので、判断の記録を読む人が v3 で打つ命令を取り違えないためである。判定点は命令の名に依らない。5 は、平易文が規範文より広い禁止に読めた（見出しが範囲を補うので読み手の義務は変わらない）。

**代替案。**
- **(A) 当てない。** 40 周目の直す 5 件と知らせを控えのまま残す。次の周が同じ場所を拾う見込みが高い（AC18 は 37 周目にも同じ語で所見が立った）。
- **(B) 項 1 を当てず、引き金の外（項 2〜5）だけを当てる。** 周が要らず、注だけの是正として記帳で取り込める（行 D-14）。ただし 8 つ目の名札の字は残り、受入基準を開く読み手は語彙に無い語で止まる。
- **(C) 項 3 の橋を、ADR-24 の決定の字を「同じ起点」に替える形にする。** 決定の本文の書き換えになり、発効した判断の記録の作法（決定 (4)）に反する。改訂の欄で変えるなら新しい判断の記録が要る。

**コスト。** 承認 1 回と、承認の前の 41 周目 1 回（項 1 が引き金を動かすため）。名札 2 か所・注 3 か所・平易文 3 か所（ADR-24 に 2・憲法に 1）。**要件の番号は 1 つも動かず（条 P-7.1）、実装と歯は 1 本も直さない。**

**推奨 = 1〜5 を当てる（代替案は採らない）。**

### 🔴 問 2. 規則の表 D-12 の注と refs に判断の記録 ADR-24 を足し、注に門を撃つ場所の 1 文を置く

**平易な説明。** 規則の表の行 D-12（設計文書を書き換える便は、出す前に天井の門を通す、という作法）を 2 か所直します。1 つ目。行 D-12 は門の答え方の出所に判断の記録 ADR-18 だけを挙げています。その答え方の一部（「設計文書が無ければ通す」）は、今朝承認いただいた ADR-24 で「照らし合わせられるときだけ」に狭まりました。行 D-12 から出所を辿る人が狭める前の字に着かないよう、出所の列と参照の一覧に ADR-24 を足します。2 つ目。門は作業ツリー（枝ごとに取り出した作業用の置き場）の一番上から実行しないと「まだ分からない」と答えて止まります。この「一番上から実行する」という席の手順は ADR-24 の帰結の字にだけ書いてあり、席の作法の置き場である行 D-12 に無いと、AI の審査が 38・39・40 周目と 3 回続けて挙げました。規則を増やすのではなく、注に「これは規則ではなく席の手順」と断って、撃つ場所の正本（要件 FR20）を指す段を置きます。撃つ場所を誤ると門は「まだ分からない」と答えて止まります。ただし、設計文書の置き場の名を打ち間違えたときは、門が確かめずに「通す」と答えてしまう穴が見つかりました（41 周目）。この穴は隠さずに注へ書き、道具の直しは別の件（台帳 f2-648.224）にします。行の決まりそのもの（何を守るか・種別・段）は変えません。規則の表の行を変えるときは裁定の番号と時刻を行に付ける決まり（条 P-17.1）なので、この承認をその裁定にします。

**やりたいこと。** 字は枝に書いてある（項 2 は改訂 b）。
1. 行 D-12 の注の頭「判断の記録 ADR-8 決定 (4)(6)・ADR-18 決定 (1)(3)(5)・要件書 FR20 の口（folio ceiling --stamp / --gate）。」を「判断の記録 ADR-8 決定 (4)(6)・ADR-18 決定 (1)(3)(5)（決定 (5) の「無ければ 通す」は、判断の記録 ADR-24 の改訂の欄が、書き換える file の一覧と設計文書の置き場を同じ起点で照らせるときに限った）・ADR-24 決定 (1)・要件書 FR20 の口（folio ceiling --stamp / --gate）。」に。refs を `[ADR-8, ADR-13, ADR-18, D-14, ADR-19]` から `[ADR-8, ADR-13, ADR-18, D-14, ADR-19, ADR-24, P-4]` に（40 周目 整合 F-1・P-4 は項 2 の段のため）。
2. 行 D-12 の注の末尾（「…M の出口を表す型付きの欄が無いので門ではなくこの行が持つ」の後）に次の段を足す（39 周目 整合 F-3・40 周目 整合 F-2・改訂 d で 41 周目 実態 F-1〔止める〕・整合 F-1・読みやすさ F-3 を当てた字）。refs に条 P-4 も足す（段が条 P-4.1 を指すので・床の行 R-17）。

   > 門を撃つ場所（命令を実行する場所）は作業ツリーの一番上で、これは規則ではなく席の手順である。撃つ場所と撃ち直し方の正本は要件 FR20 の規範文と注で、この注はそれを指すだけである（判断の記録 ADR-24 決定 (1)・天井の 38〜41 周目 整合）。撃つ場所の誤りで書き換える file の一覧と設計文書の置き場を同じ起点で照らせないときは、門は まだ分からない を返し、撃ち直し方を理由の行に出す。ただし設計文書の置き場の渡し間違い（実在しない置き場の名や、設計文書の置き場でない場所を渡す）は門が確かめず、設計文書を書き換える便でも 通す を返す穴が在る（本流 0f92ae6 の実測・台帳 f2-648.224 に床の穴として起票・条 P-4.1・天井の 41 周目 実態 F-1）

   （句点は置かない。規則の表の注は末尾に句点を置かない書き方で、行 D-14 の注も同じ。）

3. 裁定の欄の頭に承認の裁定 id と時刻と逐語を置き、`ruled_at` を承認の日付にする（承認の後に席が書く）。

**理由。** 1 は、ADR-18 の注と要件 FR20 の判断の記録の欄は ADR-24 を指しているが、行 D-12 だけが指していない。3 面の辿りを揃える。2 は、38・39・40 周目の整合が同じ中身を 3 周続けて挙げた（判定 7）。行 D-13 の注が「契約の取り込みの前に席が作業ツリーで受付を先に撃つことは規則ではなく席の手順」と置いた先例と同じ形で、規則を増やさない（条 N-2 の散文の規則にもならない）。**what・種別・段・状態は不変**で、注と refs は周の引き金の外である。

**代替案。**
- **(A) 当てない。** 1 は両立はしている（40 周目も参考とした）が、行 D-12 から辿る読み手は狭める前の字に着く。2 は、撃つ場所の誤りは門が止めるが、次の周も同じ所を拾う見込みが高い（置き場の渡し間違いの穴は 2 を当てても当てなくても在り、直しは台帳 f2-648.224）。
- **(B) 2 を規則の行（what）に足す。** 規則として縛る形。門が既に止めるので縛る利益は小さく、what が変わると周の引き金が動く。

**コスト。** 承認 1 回（問 1 と同じ回）。注の 1 句と 1 文、refs の 1 項。周は増えない（どれも引き金の外）。

**推奨 = 1〜3 を当てる（代替案は採らない）。**

## 承認の前に席が行う段取り（枝の上の 41 周目）

行 D-14 と判断の記録 ADR-19 の順である。どれも持ち主の承認に依らない。

1. **本流を枝へ取り込む**（行 D-14「周の前ごとに本流を枝へ取り込む」）。起草の時点の土台は本流 `0f92ae6`。41 周目の束を組む前に本流が動いていたら取り込む。
2. **済み（改訂 b）。** 門を撃つ場所の 1 文を枝に書いた（`apply_q3.py`・席の裁定）。
3. **枝の HEAD で 41 周目を回す**（束を組み、4 観点を回し、止めるがあれば反証を当てる）。門（`folio ceiling --gate`）は作業ツリーの一番上から撃つ。本流の `target/debug/folio` は 2026-09-26 11:02 の組み立て（便 148 の着地の直前の組み立てで、`gate.rs` と `ceiling.rs` は本流 `0f92ae6` と同じ）である。
4. **不合格なら**、支持の止めるのそれぞれに直しを当てて回し直す。直しを当てずに回し直してよいのは実行できなかった回だけで、窓の中で 1 回まで（行 D-14）。
5. **4 観点とも合格したら**、枝の上で印（`design-intent/preview/ceiling-stamp.yaml`・`folio ceiling --stamp`）を書いて commit する。この印が取り込みで本流の印になる（ADR-19 決定 (2)）。
6. **41 周目の所見の直しを問 1 に足す**（直す・参考）。足した直しが引き金の欄を動かしたら、3 に戻る。
7. **1 回の承認要求で出す**（問 1・問 2・40 周目の材料・41 周目の所見と結果を添える）。

## 承認の後に席が書く欄

持ち主が答えた後に、席がこの枝（`docs/batch24`）の別の commit で書く。退けた項の分は §「問どうしの依存」のとおり元の字に戻す。**「承認待ち」「起草」「承認の後に席が書く」の字を持つ欄は全部ここに挙げた。** 書いた後に #1〜#6 の欄を 1 つずつ開いて、一括 24 に触れる字に「承認待ち」「起草・」「承認の後に席が書く」が残っていないことを確かめる。

| # | file | 欄 | 何を書くか | 状態 |
|---|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.45 → v1.46 | 未 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の頭の 1 文 | 「いま発効しているのは 第 1.45 版（2026-09-26）です。」を「いま発効しているのは 第 1.46 版（<承認の日付>）です。」に | 未 |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.46 | 「v1.46 = 2026-09-26 起草（一括 24 = …・裁定 id〔承認の後に席が書く〕・承認待ち）」を「v1.46 = <日付> 発効（逐語「…」・f2-648 notes <時刻> JST・対話面 R-8・問 1・問 2 の…・一括 24 = …）」に。**頭の「起草」と末尾の「裁定 id〔承認の後に席が書く〕・承認待ち」の 2 か所とも書き換える**。41 周目の直しを足したら要旨にも足す | 未 |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 24〔問 1・問 2〕の承認要求への回答）", verbatim: <逐語>, version: v1.46}`） | 未 |
| 5 | `design-intent/rules.yaml` | 行 D-12 の `ruling`・`ruled_at` | 裁定の欄の頭「一括 24 の承認待ち（席が承認の後に裁定 id と時刻と逐語をここへ書く・…）」を「f2-648 notes <日付> <時刻> JST（逐語「<逐語>」・一括 24 の問 2 = …）」に。`ruled_at` を <日付> に。問 2 を退けたら注・refs・裁定の欄を本流の字に戻す | 未 |
| 6 | `design-intent/rules.yaml` | 行 D-12 の注の末尾（問 2 の項 2・改訂 b で枝に書き、改訂 d で直した） | 問 2 の項 2 を退けたら段を外し、refs の P-4 と、裁定の欄の承認待ちの字から同じ段の句を外す。当てたら何もしない | 未 |
| 7 | `docs/design/batch24-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に。41 周目の結果（周の置き場・判定・所見の数・印の commit）を足す | 未 |
| 8 | `docs/design/state.md` | 書いた時点・基準・統合先・天井の印・要件書の行・次の一手・未決 | 要件書 = 第 1.46 版（発効の日付と逐語・一括 24）／天井の印 = 41 周目。書き換える前に読んで、一括 23 の後に動いた行を全部直す | 未 |
| 9 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）・一括 24 の仕分けの在り処・枝の上の 41 周目の結果・次の手番 | 未 |
| 10 | 台帳（`bd`） | f2-648.221 | 8 件を当てたことを notes に記帳して close。材料 8（CON8 の見出し）を控えた（C）ことを 1 句残す | 未 |
| 11 | 器の列 | 窓の保留の解放 | 取り込みで窓を閉じたら、窓を開いたときに保留にした設計文書を書き換える便の件を解放する（行 D-14） | 未 |

## 床の結果（この枝の起草の字で実測・2026-09-26・参考値）

binary は本流の `target/debug/folio`（2026-09-26 11:02 の組み立て・この枝の `crates/` と `tests/` は本流 `0f92ae6` と同じ）。作業ツリーの一番上で、相対の `--dir` で撃った。控え `~/.local/share/folio2/handoff-2026-09-26/batch24-draft.md` に command と字を全部写した（`batch24-scripts/floor.sh`・`floor-head.txt`）。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0）・rc 0。標準エラーに「機構がまだ無い条」の 1 行（P-11・P-13・P-15・P-17・P-18・A-3） |
| `folio inject --check --dir design-intent` | rc 0（68 行 / 7,912 byte 一致）。憲法の平易文は生成区間の外なので本流と同じ |
| `folio schema --dir design-intent --check` | rc 0（9 file 一致） |
| `folio derive --dir design-intent --out ../contracts --check` | rc 0（一致 1・差分 0） |
| `folio build --dir design-intent --out <一時 dir> --write` | 床 合格・rc 0・31 file。要件書の面に新しい名札、憲法の面に N-4 の新しい平易文、ADR-24 の面に橋の 1 文、ADR-21 の面に tz init が出る（字で確かめた） |
| `folio ceiling --gate --dir design-intent --write-set design-intent/srs.yaml design-intent/adr/ADR-24.yaml design-intent/adr/ADR-21.yaml design-intent/constitution.yaml design-intent/rules.yaml` | **まだ分からない（印が古い（引き金の要約値が違う））・rc 2**＝行 D-14 により承認の前に 41 周目が要る。改訂 b の後も同じ（`floor-head-b.txt`・床 4 本 rc 0・build 31 file）。改訂 c の後も同じ（`floor-head-c.txt`）。改訂 d の後も同じ（`floor-head-d.txt`・write-set に ADR-18 を足した） |
| 写しの上で引き金の欄を戻した門（`batch24-scripts/gate_revert.py`・`gate-revert.txt`・改訂 b の後は `gate-revert-b.txt`・改訂 c の後は `gate-revert-c.txt`・改訂 d の後は `gate-revert-d.txt` で同じ結果＝引き金は 2 欄のまま） | 確かめ方だけ戻す・判定の文だけ戻す＝どちらも rc 2。2 か所を両方戻す＝通す（節点 5 個と節点の外の字）rc 0。要件書の直しを全部戻す＝通す（節点 4 個）rc 0。ほかの 4 file も本流の字に戻す＝通す（正本の要約値が同じ）rc 0＝**引き金を動かすのは項 1 の 2 か所だけ** |
| nextest | 撃っていない（この一括は正本の字の直しだけで、`crates/` と `tests/` は本流と同じ） |

**歯は 1 本も直していない。**

## まだ分からない点

- **41 周目はまだ回っていない。** 特に、ADR-24 の平易文に足した橋の 1 文で読みやすさ F-2 が閉じるか（決定の節を読む人は平易文の後に決定を読むので、決定の字の「同じ根」が残る）は、41 周目が判定する。閉じなければ、次の手は決定を改訂の欄で変える新しい判断の記録になり、費用が重い。
- **名札の字が長くなった。** 8 つ目の名札は前より長い。確かめ方と判定の文の括弧の中の並びが読みにくくなる見込みがある。
- **面を 1440 の幅で目で見る確認はしていない。** 席が承認要求の前に、要件書の面の FR20 と AC18、憲法の面の N-4、ADR-24 と ADR-21 の面を開いて見る。
