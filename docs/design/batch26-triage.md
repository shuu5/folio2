# 一括 26 の仕分け — 台帳 f2-648.75 と f2-648.186 の控え（規則の表に閾値の行 2 本 R-18・R-19 と開発規律の行 1 本 D-15・要件書 第 1.48 版で FR1 の規範文の値を行 R-18 に寄せる・入口 第 0.6 版）

**状態: 起草・承認待ち（2026-09-26・枝 `docs/batch26`・本流 `57364e1` に載せ替え済み）。** 来歴は次のとおり。

| 改訂 | commit | 中身 |
|---|---|---|
| 起草 | `86433d3` | 行 R-18〜R-20・D-15 と、憲法の条 4 つの関係の欄・条 P-7 の機構の注の 1 文 |
| b | `a80fceb` | 要件書 第 1.48 版の起草＝FR1 の規範文の「5 問以下」を行 R-18 を指す字に（席の裁定） |
| b' | `fc7baea` | 席が写しの歯 f91 の期待を直した |
| c | `8157f03` | 独立の検証（`~/.local/share/folio2/handoff-2026-09-26/batch26-verify.md`・blocking 1 + 非 blocking 8）の直し |
| c' | `21d2823` | 席が f91 の D-15 の refs に P-7.1 |
| d | `cedaef5` | 枝の上の 46 周目（4 観点とも合格・直す 8・参考 1）の直しと、**行 R-20 の保留**（席の裁定） |
| d' | `7815e19` | 席が f91 の期待から R-20 を外した |
| 47 周目の印 | `c1c1142` | 枝の上の 47 周目（4 観点とも合格・直す 4・参考 4） |
| e | この commit | 47 周目の直す 4・参考 4 を当てた（**引き金の欄は 0 か所動かす**＝印は 47 周目のまま・門は 通す） |

行を足す向きは持ち主が 2026-09-26 15:22 JST に裁定済み（対話面 R-8・f2-648 notes・逐語「すり合わせ：／１．推奨で／２．推奨で／３．推奨で／４．推奨で／５．推奨で／／また３問についてもすべて承認する」の 1 と 3）。**行の文面と値は、この仕分けの 🔴 問 1 で持ち主に見せてから発効する**（台帳 f2-648.75 の起票の字「行の値と文面は便の前に 🔴 で持ち主へ問う」・条 P-17.1）。

**この一括は周の引き金の欄を 4 か所動かす**＝規則の表の行 3 本（行の id・what・value・kind・status・stage・population の欄が `crates/folio/src/ceiling.rs` の `TRIGGER_RULES_FIELDS` に入る）と、要件 FR1 の規範文 1 か所（`TRIGGER_SRS_REQUIREMENT` の shall）。改訂 c までは行 4 本で計 5 だったが、改訂 d で行 R-20 を外した。1 つずつ当てた写しで、どれも門を「印が古い」rc 2 にした（§床の結果）。憲法の直し（条の関係の欄と機構の注）・要件書の規範文以外の直し・入口の直しは引き金の外である。改訂 d は 46 周目の後に引き金の欄（行 R-18 の種別・行 R-20 を外す）を動かしたので、行 D-14 により 47 周目を回した（4 観点とも合格・印 `c1c1142`）。**改訂 e は引き金の外だけを直した**（行の refs と注・要件の平易文と注・憲法の注・判断の記録の注）。門は「通す（印の後に引き金の外の変更が在る）」rc 0 で、次の周は要らない（§床の結果）。

持ち主への承認要求は **🔴 問 1**（行 3 本の文面と値・項 1・2・4。項 3 = R-20 は保留）と **🔴 問 2**（要件書 第 1.48 版 = FR1 の規範文の 1 句と、同じ値の言い換えを外す入口 第 0.6 版・問 1 の項 1 と対）の 2 つで、1 回の承認要求に束ねる。

## 材料

材料は 2 件である。どちらも周の所見ではなく、台帳の控え（memo）を持ち主が棚卸しで「推奨で」と答えたものである。加えて、枝の上の 46 周目の所見 9 件を当てた（§枝の上の 46 周目の所見）。

| # | 出所 | 中身 | 台帳 |
|---|---|---|---|
| 1 | 判断の記録 ADR-11 決定 (3)(エ)・(4)⑦・行 D-11・下調べ `docs/design/adr-11-survey.md` §2-b | 実装にだけ在る閾値 3 つ（相談窓口の質問の数の上限・設計ノートの面の章の数の上限・版管理の照合の待ち時間）を規則の表の閾値の行に足す。値は実装の今の値 | f2-648.75（棚卸しの問 1） |
| 2 | 天井の 31 周目 実態 F-1・一括 18 の仕分け・条 P-7 の機構の注 | id の一覧の凍結 anchor は `ids-v1.24.yaml` の 1 本だけで、第 1.24 版より後に足した id は改番の検査の外。凍結し直す時機を規則の表の開発規律の行にする（案 (b)） | f2-648.186（棚卸しの問 3） |

**実測（土台 `79998ca`）。** 3 つの定数は今も実装に在り、規則の表には無い。規則の表の最大の id は閾値 R-17・開発規律 D-14 である。

| 定数 | 場所（土台の行） | 値 | 使われ方 | 超えたときの結果 |
|---|---|---|---|---|
| `MAX_QUESTIONS` | `crates/folio/src/intake.rs:40` | 5 | 床 `folio check` が相談窓口の正本の questions の行を数える（同 file:89） | 違反（不合格） |
| `MAX_CHAPTERS` | `crates/folio/src/face_note.rs:79` | 12 | 面の生成器が章（節の行 + 図の節が在れば 1）を数える（同 file:246〜247）。値は章の帯の表 `BANDS`（12 組）の長さ | 面を生成しない＝`folio face` も `folio build` も まだ分からない（歯 `face_note_unknown_when_there_are_too_many_chapters`・`site.rs` は組み立て前に止まり書かない） |
| `TIMEOUT` | `crates/folio/src/gitcheck.rs:22` | 20 秒 | `git` の命令 1 本ごとの待ち上限（同 file:73）。凍結 anchor の列の照合と版管理の根の問い合わせ | 照合は まだ分からない。根の問い合わせは置き場の親に倒す（**改訂 d で保留**） |

**質問の数の上限は「実装にだけ在った」のではない。** 判断の記録 ADR-11 の注 (イ) は、決定 (4)⑦ がこの上限を実装にだけ在ると数えたのを誤りと訂正している（要件 FR1 の規範文・ADR-6 決定 (2)・相談窓口の正本の質問 5 行が既に持っていた）。行 R-18 は「要件の規範文と実装の定数の 2 か所に在った値を行に寄せる」行である（46 周目 整合 F-2）。

id の一覧の凍結の口は `crates/folio/src/ids.rs`（関数 `freeze`）。file の名は要件書の `meta.version`、全検査が合格のときだけ書き、同じ名の file が在れば断る（N-1.1）。写しで撃つと `ids-v1.48.yaml`（id 80 本）ができ、commit するまで床は未追跡の anchor で違反 1、commit の後は合格である。

## 仕分けの表

仕分けの記号。**A** = この一括で正本を直した（持ち主の承認が要る）。**B** = 便へ回す。**C** = 控え（この一括では直さず、理由を残す）。**保留** = 行を外し、前提の便の後に別の一括で問う。

| # | 材料 | 是正の内容 | 引き金 | 仕分け | 問 |
|---|---|---|---|---|---|
| 1 | 1 | 行 R-18（相談窓口の質問の数の上限・5 問以下・種別 deny〔改訂 d〕）を足し、条 P-1 の関係の欄に `rules: [R-18]` | **動く**（行） | A | 1（項 1） |
| 2 | 1 | 行 R-19（設計ノートの面の章の数の上限・12 章以下・deny）を足し、条 P-2 の関係の欄の rules に R-19 | **動く**（行） | A | 1（項 2） |
| 3 | 1 | 行 R-20（版管理へ撃つ命令 1 本の待ち時間の上限・20 秒）。**改訂 d で行と条 P-4 の関係の欄の R-20 を外した**（判定 7） | 動かない（外した） | **保留** | 1（項 3 の段） |
| 4 | 2 | 行 D-15（要件書の版の発効ごとに id の一覧を凍結し直す）を足し、条 P-7 の関係の欄に `rules: [D-15]`、条 P-7 の機構の注の 3 文を直す（逐語は 🔴 問 1） | **動く**（行）。憲法は動かない | A | 1（項 4） |
| 5 | 2 | 承認の後に、この枝で 1 回だけ `ids-v1.48.yaml` を凍結する（行 D-15 の最初の当てはめ＝第 1.25〜1.48 版の間に足した id を改番の検査に入れる遅れの取り戻しを兼ねる） | 動かない（anchor は引き金の外） | A | 1（項 4 の付け足し） |
| 6 | 1 | 実装が行から値を読む形（ADR-11 決定 (3)(エ) の後半・散文の門 R-16 と同じ型）。実装の定数 `MAX_QUESTIONS` の注と違反の字を行 R-18 を出所にする字に直すのも同じ便（46 周目 実態 F-2） | — | B | — |
| 7 | 1 | 要件 FR1 の規範文の「5 問以下」と行 R-18 の値の二重。規範文を「規則の表の行 R-18 が定める数以下の」にし、FR1 の関係の欄 rules に R-18、注に数の正本の 1 文（改訂 b・判定 6）。入口の相談窓口の節の本文と歩みも数に依らない字に（改訂 d・46 周目 整合 F-4） | **動く**（FR1 の規範文）。入口は動かない | A | 2 |
| 8 | 1 | 要件 FR9・FR10 の注に「この値は行 R-nn が持つ」の 1 文を写す | — | C | — |
| 9 | 46 周目 | 憲法 N-3 の機構の注を床の実態（relations の鍵と amended_by の必須の欄は数えている）に（実態 F-1） | 動かない（注） | A | 1（同じ回の憲法の注） |

**数（改訂 d）。** A = 6（#1・#2・#4・#5・#7・#9・46 周目の直しは各行に含めた）。B = 1（#6）。C = 1（#8）。保留 = 1（#3）。🔴 = 2（問 1・項 3 つ〔項 3 は保留の段〕／問 2）。

### 行ごとの表

| id | 条（article） | what の要旨 | value | kind | status | stage | refs | 出所 |
|---|---|---|---|---|---|---|---|---|
| R-18 | P-1 | 相談窓口の正本に並べる質問の数の上限 | 5 問以下 | **deny**（改訂 d） | 仮 | post | FR1・ADR-11・D-11・R-16・P-6.3 | `intake.rs` `MAX_QUESTIONS`・要件 FR1 の規範文（第 1.47 版まで） |
| R-19 | P-2 | 設計ノートの面の章の数の上限（節の行 + 図の節・承認欄は数えない） | 12 章以下 | deny | 仮 | post | P-4.1・ADR-16・ADR-11・D-11・R-16 | `face_note.rs` `MAX_CHAPTERS` |
| D-15 | P-7 | 要件書の版の発効を記帳した枝で `folio check --freeze-ids` を撃ち、凍結 anchor を 1 本足す（消さない・上書きしない） | — | human-review | 仮 | — | P-7.1・N-1.1 | 台帳 f2-648.186 案 (b)・`ids.rs` |

各行の `population`・`note` の全文と `ruling` の承認待ちの字は `design-intent/rules.yaml` の行そのものに在る（ここには写さない）。

### 判定 1 ＝ 縛る条（article）の選び方

- **R-18 → P-1。** 値の出所は要件 FR1 で、FR1 の根拠の欄（basis）は P-1 だけである。憲法の側でも条 P-1 の関係の欄が FR1 を持ち、条 P-1 の機構の注は相談窓口の支度表（FR2・AC1）を名指す。
- **R-19 → P-2。** 章の上限は面の生成器が章ごとに帯（class `band-1`〜`band-6` を 2 回）を当てる表の長さで決まる。提示層と部品目録を縛る条 P-2 に当たり、同じ条の行 R-3・R-5 と並ぶ。超えたときに まだ分からない に落とすことは条 P-4.1 に依るので refs に置いた。
- **D-15 → P-7。** 番号を増やすだけ（P-7.1）の検査の母集団を広げる時機である。上書きしないこと（N-1.1）は凍結の口が守る。自分の条の規範文 P-7.1 も refs に置いた（改訂 c・検証 N6・先例 = 行 R-5・R-14・R-15・D-11）。
- **条 P-17 の関係の欄には足さない。** 条 P-17 の関係の欄は R-17 も D-9〜D-14 も持たない。行 R-4 の 2 つ目の数えは各行の article の条の関係の欄で満たす（先例 = 行 D-13 の追加〔PR #268〕が条 P-6 にだけ足した形）。

### 判定 2 ＝ 種別（kind）と状態（status）——**R-18 は deny にした（改訂 d・46 周目 整合 F-1）**

- **判定の材料。** 規則の表の生成区間の kind_meaning は、deny を「機械が測って、値域の外なら落とす（上限の超過・下限の不足・固定の値との違い。憲法の reject / build-check に対応）」、build-check を「生成時の検査で数え、違反なら落とす（憲法の build-check に対応）」と定める。kind_map_to_constitution は `deny: [reject, build-check]`・`build-check: [build-check]`。
- **R-18 は deny に当たる。** 床 `folio check` が質問の行を数え、上限を超えたら落とす＝deny の「上限の超過」である。床の違反は憲法の機構 build-check に写り（条 P-1 の機構は build-check）、kind_map の deny の右辺に build-check が在るので対応は崩れない。build-check の定義にも当たるが、deny を排除する字は定義に無い。先例は行 R-2（8,000 byte 以下・deny・`folio inject --check` が落とす）と行 R-13（1 本以上・deny）で、どちらも機械が数えて上限や下限の外を落とす。
- **deny にした理由。** 条 P-17.4（deny 種別の値を緩める前に A-2.3 の検証）が掛かるのは deny の行だけである。判断の記録 ADR-11 決定 (3)(エ) が閾値を行に置く理由に挙げた利益（裁定 id と時刻・値を緩める前の検証）と揃う。要件 FR1 の規範文は folio の振る舞い（質問を提示する数）を縛っており、同じ形の上限の行 R-19 も deny である。
- **R-19 は deny。** 違反を数える検査ではなく、値の外で生成を止めて まだ分からない に落とす。先例は行 R-14（図の道具の検査を通らない図は生成しない・deny・条 P-4）。
- **状態は 3 本とも「仮」。** 値は実装の今の値の写しで、実測して凍結した値ではない（先例 = 新設の行 R-16・R-17・D-12〜D-14）。
- **段（stage）は 2 本とも post。** 編集の時点で止める仕掛けではない。

### 判定 3 ＝ 裁定の欄と ruled_at

- `ruling` は「一括 26 の承認待ち（席が承認の後に裁定 id と時刻と逐語をここへ書く・一括 26 の問 1 の項 n = …）・行を足す向きの裁定 = f2-648 notes 2026-09-26 15:22 JST（逐語「…」の 1 または 3・…）」の形にした（先例 = 一括 25 の起草 `8fcab36` の行 D-12）。
- `ruled_at` は起草の日（2026-09-26）。承認の後に席が承認の日付で書き直す。
- 項の番号は起草のまま（R-18 = 項 1・R-19 = 項 2・D-15 = 項 4）。項 3（R-20）は保留の段として 🔴 問 1 に残す。

### 判定 4 ＝ what と note の字

- **what は数える対象と単位だけにした。** 語彙に無い英字の語は行 R-9 が数える（最初の字「intake.yaml」は R-9 が落としたので population に移した）。
- **note には、数える口・超えたときの結果・値の出所・行 D-11 の範囲のどこに当たるか・実装がまだ行から読まないこと を書いた。** R-18 は違反、R-19 は生成を拒む判定に使う値である。
- **R-18 の注（改訂 d・46 周目 整合 F-2・実態 F-2）。** 「実装にだけ在った閾値」を「要件 FR1 の規範文と実装の定数の 2 か所に在った値を規則の表の行に寄せた」にし、ADR-11 の注 (イ) の訂正を 1 句で指した。実装の定数の注と違反の字が第 1.48 版の後も FR1 を出所に挙げていることと、実装が行から読む後続の便で直すことを書いた。種別 deny と、値を緩める前の検証が掛かることも書いた。
- **D-15 の注（改訂 d・46 周目 読みやすさ F-3）。** 頭に「何のためか」を置き、比較元（凍結してある番号の一覧）が無いと床が番号の消失と付け替え（条 P-7.1）を見つけられず、発効した版ごとに 1 本足すと足した番号が改番の検査の母集団に入る、と書いた。英字の baseline は注から外した（語彙の母集団・凍結 anchor の語と「比較元」で言う）。第 1.24 版は「天井の 31 周目の時点では」と時点を添えた字にした（凍結の後に字が事実と食い違わない・検証 B1 と同じ考え）。what は変えない。
- **refs は note と population で名指した id を全部持つ**（行 R-17 が 0 件のまま）。改訂 e で行 R-18 の refs に P-17 と A-2.3 を足した（注が条 P-17.4 と A-2.3 を名指すため・47 周目 読みやすさ F-1。依頼は P-17 だけだったが、A-2.3 を名指すと行 R-17 が 1 件で落としたので A-2.3 も足した）。
- **改訂 e の注の直し（47 周目）。** R-18 の注の「値を緩める前の検証」を「条 P-17.4 が定める事前の検証（条 A-2.3 の敵対的な検証）」に（読みやすさ F-1）。R-19 の注の値の理由を「設計ノートの面は章ごとに色と絵記号の違う見出しの帯を 1 つずつ付け、その組が 12 通りしか無い」の読み手の字に（読みやすさ F-2）。R-18・R-19 の注の「R-16 と同じ型」の後に「骨格の命令 folio init が利用者の置き場に足す行には入らない＝利用者の置き場に写す行ではない。後続の便で実装が値をどこから取るかはその便の設計ノートで決める」を置いた（整合 F-3・要件 FR22 の id は書かない＝行 R-17 と f91 の数を動かさない）。行 D-11 の注に、決定 (4)⑦ の待ち時間を保留した 1 文（整合 F-1・実態 F-2）。
- **D-15 の時機は「発効を記帳した一括の枝の上で、記帳の後に」にした。** 枝の上なら承認の記帳の直後に撃って同じ PR で運べる。anchor は周の引き金の外である。

### 判定 5 ＝ 憲法の直しは条文の改訂に当たらない

- 条の関係の欄（relations）と機構の注（mechanism.note）は、条文の範囲（`amendment_scope` = schema・precedence・articles の 5 欄 id / title / tier / binds / statements）の外である（条 A-2.2）。憲法の版は上げない（先例 = PR #268 の条 P-6 の関係の欄、`249a7a2` の条 P-7 の機構の注）。
- 写しで、憲法だけ枝の字にした門は「通す」rc 0＝周の引き金の外である。
- **改訂 d で直した憲法の注は 2 つ。** 条 P-7（46 周目 整合 F-3・検証 n1 = 「id の一覧の側の」を戻し「和」の二重を 1 文にまとめた）と、条 N-3（46 周目 実態 F-1 = 床が relations の鍵の閉じた一覧〔`refs.rs`〕と amended_by の必須の欄の空〔`link.rs`〕を既に数えている実態に合わせた・条文の欄は触らない。改訂 e で amended_by の行の欄の欠けと未知の欄〔違反の字「未知の欄（N-3）」・`link.rs` が `floor_adr.rs` の `AMENDED_BY_ENTRY` の閉じた一覧で数える〕も足した・47 周目 実態 F-1）。

### 判定 6 ＝ FR1 の規範文の値を行 R-18 に寄せる（改訂 b・席の裁定）と、入口の字（改訂 d）

- **二重の中身。** 質問の数の上限 5 が要件 FR1 の規範文と行 R-18 の値の 2 か所に在り、どちらも人が書く（条 P-6.3・P-6.4）。
- **形は非機能要件 NFR3 の第 1.25 版の先例。** 規範文の括弧の値を外して値の正本を行 R-4 に寄せた形（天井の 22 周目 整合 F-4）。FR1 の規範文は「folio は規則の表の行 R-18 が定める数以下の易しい質問を推奨回答つきで提示し、…」にした。
- **平易文も行 R-18 を指す字にした（改訂 e・47 周目 整合 F-2）。** 改訂 b〜d では平易文の「質問は 5 つまで」を値の言い換えとして残したが、同じ一括の入口 第 0.6 版が同じ数を書かない字にしたのと扱いが逆になっていた。平易文を「質問の数には上限があり（規則の表の行 R-18）、どれにも「おすすめ」がついています。」にし、注の 1 文を「平易文も数を書かずにこの行を指す」に合わせた。要件書は骨格の命令が外へ写さないので、行の id を書いてよい。要件書の来歴と作成の行の「平易文は変えない」の字も直した。
- **FR1 の関係の欄 rules に R-18 を足した**（行 R-17）。注で先例を名指すときは id を使わない字にした。
- **入口の相談窓口の節（改訂 d・46 周目 整合 F-4）。** 入口の正本 `index.yaml` の本文「易しい質問が 5 つまで出る」を「易しい質問が、決まった上限の数まで出る」に、歩みの「質問 5 つ（おすすめ付き）」を「易しい質問（おすすめ付き）」にした。入口の正本は頭注で「数はここに書かない（P-6.3）」と決めている。**行の id を書かなかった理由**：最初の字「規則の表の行 R-18 が定める数まで出る」は、骨格の命令 `folio init` が入口の本文を外の置き場へ写すので、写しの歯 `crates/folio/tests/init.rs` の `f125_no_folio2_ids_in_the_skeleton` が「骨格に folio2 の id（R-18）が在る」で落とした（写しの nextest 951 / 952）。外の置き場の規則の表に R-18 は無いので、値にも id にも依らない字にした。入口は第 0.6 版の起草にした（頭注・版・作成の行）。入口の正本は周の引き金の外である。
- **確かめ方の「決まった回答 5 つ」は変えない。** 相談窓口の正本の質問の数に合わせた固定の材料の数で、上限の写しではない。

### 判定 7 ＝ 行 R-20 を本一括から外す（保留・改訂 d・席の裁定）

- **理由。** 規則の表の欄の決まりの生成区間の `excluded`（`rules.yaml` の schema 節・正本は `crates/folio/src/rules.rs` の定数）は、「時間（「60 分以内」）・費用（「300k token 以下」）」を「測る仕組みが別で凍結できない」として表の外と定める。行 R-20 の「20 秒」は、字のうえでこの「時間」に当たる（46 周目の忠実さの審査役の観点の外の気づき・所見の file の外で席に伝わった）。除外の字の趣旨は人の作業の時間と費用で、命令の待ち上限は機械が測れる値だが、字を読み替えて行を足すと生成区間と行が食い違う。
- **前提の便。** 除外の字を「人の作業の時間・費用」に狭める便（実装の定数・S）を先に運び、その後に別の一括で行を問う。
- **保留の記帳（改訂 e・47 周目 整合 F-1・実態 F-2）。** 判断の記録 ADR-11 の注の末尾に「決定 (4)⑦ の行き先」の段（3 つのうち 2 つは行に置いた・待ち時間は欄の決まりの除外〔時間〕に当たるので保留・除外の字を狭める便の後に別の一括で問う・決定の中身は変えない・裁定は一括 26 の承認の後に席が書く）を足し、行 D-11 の注に同じ 1 文を置いた（どちらも行の id は書かない＝行 R-17 の数えを動かさない）。台帳は f2-648.227（席）。
- **外したもの。** `rules.yaml` の行 R-20、条 P-4 の関係の欄の R-20、要件書の来歴と作成の行の「R-18〜R-20」「条 4 つ」の字、写しの歯 f91 の R-20 の行（席が当てる）。46 周目の読みやすさ F-1（what の「その照合」の先行語）と F-2（population の英字「process」）は R-20 の字への所見なので、次の一括で行を問うときに当てる（控え）。
- **id R-20 の扱い。** 次の一括で行を足すときは **R-20 をそのまま使う**。行 R-20 は本流に一度も入らず、発効もしていない仮の起草の行の取り下げで、番号を空けて残す廃止（条 P-7.2）とは別の事柄である。条 P-7.1（再利用・改番の禁止）が名指すのは要件・条文・判断・受入基準の id で、規則の表の行は条 P-17 の手続き（行の追加に裁定 id と時刻）で縛られる。本流の規則の表の最大は R-19 のままになる。持ち主が「次の一括では R-21 から」を選ぶなら、それに従う（🔴 問 1 の付記）。

## 運び方（行 D-14 の窓）

- **窓は枝 `docs/batch26` を切った時点で開いた。** 窓の間は、本流の設計文書の正本を書き換える便の台帳の件を器の列で保留にする（行 D-14・席の手番）。
- **46 周目**（枝 `21d2823`・本流 `57364e1` の上・45 周目の印の後）は 4 観点とも合格（止める 0・直す 8・参考 1）。
- **47 周目を回す。** 改訂 d は引き金の欄（行 R-18 の種別・行 R-20 を外す）を動かしたので、46 周目の印は書かず、改訂 d の HEAD で 47 周目を回す。4 観点とも合格したら印を書いて commit し、46・47 周目の所見と結果を添えて 1 回の承認要求で出す。
- **行 R-7 の数え（行 D-14 の止める場合）。** 46 周目は止める 0 で、窓の中の数えは 0 のまま。

## 枝の上の 46 周目の所見（改訂 d）

46 周目は `21d2823` の束で回り、**4 観点とも合格**（止める 0・直す 8・参考 1）＝`~/.local/share/folio2/ceiling/2026-09-26-round46/2026-09-26-round46/<観点>/findings.yaml`。

| 観点 | id | 重さ | place | 中身（要旨） | 当てた字 |
|---|---|---|---|---|---|
| 整合 | F-1 | 直す | `rules` `thresholds.R-18.kind` | ADR-11 決定 (3)(エ) は行に置く利益に条 P-17.4 を挙げるが、R-18 は build-check で P-17.4 が掛からない | 種別を deny に（判定 2）。注に deny と、値を緩める前の検証が掛かることを書いた |
| 整合 | F-2 | 直す | `rules` `thresholds.R-18.note` | 「実装にだけ在った閾値」が ADR-11 の注 (イ) の訂正と同じ注の前の文に合わない | 「要件 FR1 の規範文と実装の定数の 2 か所に在った値を行に寄せた」に・注 (イ) の訂正を 1 句で指した |
| 整合 | F-3 | 直す | `constitution` `articles.P-7.mechanism.note` | 差し替えた文が「id の一覧の側の」を落とし、前の文と「和」を二重に書く | 前の文に「（design-intent/anchors/）」「凍結し直すたびに 1 本増える」を寄せ、差し替えた文を消した（🔴 問 1 の逐語） |
| 整合 | F-4 | 直す | `index` `intake.text` | 入口の本文と歩みの「5 つ」が行 R-18 を指さず、言い換えとも断られていない | 数に依らない字にした（判定 6・入口 第 0.6 版） |
| 読みやすさ | F-1 | 直す | `rules` `thresholds.R-20.what` | 「その照合」の先行語が無く、根の問い合わせの場合を言わない | R-20 を保留したので当てない（次の一括の控え・判定 7） |
| 読みやすさ | F-2 | 直す | `rules` `thresholds.R-20.population` | 英字の「子 process」 | 同上 |
| 読みやすさ | F-3 | 直す | `rules` `discipline.D-15.note` | 要の語 baseline が語彙に無く、何のために凍結し直すかが取れない | 注の頭に「何のためか」・baseline を「比較元（凍結してある番号の一覧）」に（判定 4） |
| 実態 | F-1 | 直す | `constitution` `articles.N-3.mechanism` | relations と amended_by を「まだ数えない」に並べるが、床は既に数えている | 床の実態の字に（判定 5） |
| 実態 | F-2 | 参考 | `rules` `thresholds.R-18.note` | 実装の定数の注と違反の字が第 1.48 版の後も FR1 を出所に挙げる | 行 R-18 の注に 1 句（後続の便で直す）・#6 の便の中身に足した |

忠実さの観点は所見 0 件。観点の外の気づき（行 R-20 と生成区間の `excluded`）は判定 7。

## 枝の上の 47 周目の所見（改訂 e）

47 周目は改訂 d' `7815e19` の束で回り、**4 観点とも合格**（止める 0・直す 4・参考 4・印 `c1c1142`）＝`~/.local/share/folio2/ceiling/2026-09-26-round47/2026-09-26-round47/<観点>/findings.yaml`。**改訂 e は全部 引き金の外で、動いた引き金の欄は 0**（門は 通す rc 0）。

| 観点 | id | 重さ | place | 中身（要旨） | 当てた字 |
|---|---|---|---|---|---|
| 整合 | F-1 | 直す | `adr` `ADR-11.decision` | 決定 (4)⑦ と行 D-11 は待ち時間を行に置くと言い、欄の決まりの除外は時間を外す。保留とその理由が要件書の来歴にしか無い | ADR-11 の注の末尾に「決定 (4)⑦ の行き先」の段・行 D-11 の注に 1 文（判定 7）。決定の字は変えない |
| 整合 | F-2 | 直す | `srs` `requirements.FR1.plain` | 平易文が数を手書きで持ち、入口 第 0.6 版の扱いと逆 | 平易文を行 R-18 を指す字に・注の 1 文を合わせた（判定 6） |
| 整合 | F-3 | 参考 | `rules` `thresholds.R-18.note` | 「R-16 と同じ型」で読むと、骨格の命令が利用者の置き場に足す行（FR22）に R-18 は無い | R-18・R-19 の注に「利用者の置き場に写す行ではない・値の取り方は後続の便の設計ノートで決める」 |
| 読みやすさ | F-1 | 直す | `rules` `thresholds.R-18.note` | 「値を緩める前の検証」が何を指すか名指さない | 条 P-17.4 と A-2.3 を名指し、refs に P-17・A-2.3 |
| 読みやすさ | F-2 | 参考 | `rules` `thresholds.R-19.note` | 値の理由が実装の表の言葉だけ | 面で見える物の字（色と絵記号の違う見出しの帯・12 通り）に |
| 読みやすさ | F-3 | 参考 | `index` `meta.status` | 入口 v0.6 は起草なのに status が effective で、面が「v0.6（発効）」と出る | 当てない。前の版の起草（v0.3 = `d20d70a`・v0.4 = `2c3a631`）も status は effective のままだった＝先例と同じ形。§まだ分からない点へ |
| 実態 | F-1 | 直す | `constitution` `articles.N-3.mechanism` | amended_by の行の欄の欠けと未知の欄も床が落とすのが注から落ちている | 注に足した（判定 5） |
| 実態 | F-2 | 参考 | `adr` `ADR-11.decision` | 保留の記帳が要件書の来歴にしか無い | 整合 F-1 と同じ直し |

忠実さの観点は所見 0 件。

## 問どうしの依存

| 項 | 依存と、退けたときに席が戻すもの |
|---|---|
| 問 2 | **問 1 の項 1（行 R-18）と対である。** 問 1 の項 1 を退けたら問 2 も退ける＝規範文が指す行が無くなる。退けたら要件書を第 1.47 版の字に戻す（`meta.version`・FR1 の規範文・関係の欄 rules の R-18・注の末尾の 2 文・`status_note` の v1.48 の来歴・承認欄の v1.48 の作成の行）。入口の第 0.6 版は値に依らない字なので、問 2 を退けても残してよい（退けるなら頭注・版・作成の行・本文・歩みを第 0.5 版に戻す）。問 2 だけ退けて問 1 の項 1 を当てるなら、行 R-18 の注の出所の字を FR1 の規範文が値を持つ字に戻す |
| 項 1・2 | 互いに独立である。退けた項の行を `rules.yaml` から外し、その行の条の関係の欄（P-1 の `rules: [R-18]` を丸ごと・P-2 の R-19）を本流の字に戻し、写しの歯 f91 の期待から同じ行を外す（R-18 = refs 5・R-19 = refs 5）。両方退けたら台帳 f2-648.75 は開いたまま |
| 項 4 | 項 1・2 と独立である。退けたら行 D-15・条 P-7 の関係の欄の `rules: [D-15]`・条 P-7 の機構の注の 3 文を本流の字に戻し、写しの歯 f91 の期待から `("D-15", &["P-7.1", "N-1.1"])` を外す（表の行 18 → 17・refs の行の数 18 → 17・refs の id の合計 59 → 57）。#5 の凍結は行 D-15 に依らずに当てられるが、推奨は項 4 と対。凍結する版は問 2 に従う＝**問 2 を承認 → `ids-v1.48.yaml`・問 2 を退ける → `ids-v1.47.yaml`** |
| 項 3（R-20） | 保留（判定 7）。承認の対象ではない |
| 憲法 N-3 の注 | ほかの項と独立（床の実態の字・引き金の外）。退けたら本流の字に戻す |

## この一括に入れなかったもの

- **実装がこの行から値を読む便（B・#6）。** 判断の記録 ADR-11 決定 (3)(エ) の後半。行の値と定数の一致を機械は数えない（取り込みの審査で人が見る・行 D-11）。承認の後に席が台帳へ起こす（`prose.rs` の `ROW_ID` = R-16 と同じ読み方で行 R-18・R-19 を読む形が候補）。同じ便で、`intake.rs` の定数の注「要件書 FR1 の規範文の数」と違反の字「FR1 の上限」を行 R-18 を出所にする字に直す（46 周目 実態 F-2）。
- **行 R-20（保留・#3）。** 生成区間の `excluded` を「人の作業の時間・費用」に狭める便（実装の定数・S）の後に、別の一括で R-20 として問う（判定 7）。46 周目の読みやすさ F-1・F-2 はそのときに当てる。
- **要件 FR9・FR10 の注への写す先の 1 文（C・#8）。** 規範文に値を持たず、行の側の refs で向きは型付きの欄に在る。
- **値の言い換えが残る散文（全数・検証 N7）。** 要件書の `meta.promise`「易しい質問 5 つに答える」、FR1 の平易文「質問は 5 つまで」（注で言い換えと断った）、判断の記録 ADR-6 の文脈と平易文の「5 問以下」「質問は 5 つだけ」、ADR-11 の注「FR1 の規範文（5 問以下）」、ADR-16 の文脈 (8) と案と ADR-21 の文脈と決定の「章の上限 12」、実装 `crates/folio/src/gitcheck.rs` の頭注「各命令の待ち上限 20 秒」。**どれも残してよい**（値の言い換えか記録の時点の字で、値の正本は行 R-18・R-19。ADR-6・ADR-21 の本文は発効した判断の記録なので変えない。gitcheck.rs は R-20 の保留とともに次の一括で扱う）。入口の本文と歩みは改訂 d で数に依らない字にした。

## 本一括でこの枝に書いたもの

| file | 何を書いたか |
|---|---|
| `design-intent/rules.yaml` | 閾値の節の末尾に行 R-18・R-19、開発規律の節の末尾に行 D-15（生成区間と頭の注釈は触らない・規則の表に版の欄は無い）。R-20 は起草で足し改訂 d で外した |
| `design-intent/constitution.yaml` | 条 P-1・P-2・P-7 の関係の欄の rules（3 か所・P-4 の R-20 は改訂 d で外した）・条 P-7 の機構の注（3 文）・条 N-3 の機構の注（1 文を 2 文に）。版は上げない |
| `design-intent/srs.yaml` | 第 1.48 版の起草：`meta.version` v1.48（`effective_version` は v1.47 のまま・`counts` は不変）・`status_note` の来歴の v1.48（起草・承認待ち）・承認欄の v1.48 の作成の行・FR1 の規範文の 1 句・FR1 の関係の欄 rules に R-18・FR1 の注の末尾の 2 文 |
| `design-intent/index.yaml` | 第 0.6 版の起草：頭注の 1 行目と「# 状態:」・`meta.version` v0.6・承認欄の v0.6 の作成の行・相談窓口の節の本文と歩みの 1 つ（改訂 d） |
| `crates/folio/tests/check.rs` | **席の手**（改訂 b' `fc7baea`・c' `21d2823`）。改訂 d の分（R-20 の行を外す）は席が `f91-refs.patch` で当てる。起草役は `crates/` と `tests/` を書いていない |
| `docs/design/batch26-triage.md` | この仕分け |

## 持ち主へ問う項

### 🔴 問 1. 規則の表に行を 3 本足す（閾値 2 本 R-18・R-19・開発規律 1 本 D-15）の文面と値

**平易な説明。** folio の数の決まり（「質問は 5 つまで」など）は、規則の表という 1 か所にまとめて、変えるときは持ち主の承認と日付を行に残す約束です。ところが、質問の数と設計ノートの章の数の上限は、プログラムの中（と要件書の文）にしか無く、表に載っていませんでした。これを表に載せます。数はプログラムの今の数のままで、変えません。もう 1 つは作法の行です。folio は「一度振った要件の番号を消したり付け替えたりしていないか」を検査していますが、比べる元の一覧が第 1.24 版で止まっていて、その後に足した番号は検査の外でした。要件書の新しい版が決まるたびに、比べる元の一覧を 1 本足す、という作法を表に書きます。

**やりたいこと。** 次の 3 行を規則の表に足す（**3 本とも周の引き金の欄**）。行の全文は `design-intent/rules.yaml` の行そのもの。

1. **R-18（条 P-1・種別 deny）** 相談窓口（folio intake）の正本に並べる質問の数の上限 ＝ **5 問以下**。床 `folio check` が数え、超えたら不合格。deny なので、上限を広げる前に反対側からの検証が要る（条 P-17.4）。
2. **R-19（条 P-2・種別 deny）** 設計ノートの面の章の数の上限 ＝ **12 章以下**（節の数に、図の節が在れば 1 を足す）。超えた設計ノートは面を作らず「まだ分からない」。
3. **（保留）R-20 版管理（git）へ撃つ命令 1 本の待ち時間の上限 20 秒。** 起草では足したが、この一括から外した。規則の表の決まり（表に載せないものの一覧）が「時間」を表の外としていて、20 秒が字のうえでそれに当たるためである。その決まりを「人の作業の時間・費用」に狭める便の後に、別の一括で問う。**持ち主の推奨（閾値 3 本）から 1 本減る。**
4. **D-15（条 P-7）** 要件書の新しい版の発効を記帳したら、席はその枝の上で `folio check --freeze-ids` を撃ち、番号の一覧の凍結 anchor を 1 本足す（消さない・上書きしない）。問 2 で要件書が第 1.48 版になるので、承認の後にこの枝で第 1.48 版の一覧（`ids-v1.48.yaml`・写しで番号 80 本）を凍結する。これが行 D-15 の最初の当てはめで、第 1.25〜1.48 版の間に足した番号も検査に入る。

**付記（番号 R-20 の扱い）。** 外した行の番号 R-20 は、次の一括で同じ中身の行を足すときにそのまま使う。本流に一度も入らず発効もしていない起草の行の取り下げで、番号を空けて残す廃止とは別の事柄だからである（判定 7）。「次の一括では R-21 から」とするなら、そう答えてほしい。

同じ回で、憲法の条 P-1・P-2・P-7 の関係の欄に行の id を足し、条 P-7 と条 N-3 の機構の注を次の逐語で直す（条文の改訂ではない・判定 5）。条 P-7 の注は版を名指さない字にしたので、承認の後の凍結でも憲法の字は変えずに済む。
あわせて、行 D-11 の注の末尾と判断の記録 ADR-11 の注の末尾の段に、決定 (4)⑦ の 3 つ目（版管理の照合の待ち時間）を本一括で行にせず保留した旨とその理由（規則の表の欄の決まりの除外〔時間〕・除外の字を狭める便の後に別の一括で問う・台帳 f2-648.227）を足す（どちらも引き金の外・判断の記録の決定の字は変えない）。行 D-11 は注の 1 文の追記なので、条 P-17.1 により裁定の欄と日付を本承認で更新する（承認の後の欄 #1・#2）。ADR-11 の注の段の「裁定〔承認の後に席が書く〕」も本承認の裁定 id で埋める（#15）。

- **条 P-7 の注**（本流 `57364e1` の字 → 改訂 d の字）
  - 前: 「id の一覧の側の baseline は置き場の ids- の anchor の和で、最後の凍結より後に足した」 後: 「id の一覧の側の baseline は置き場（design-intent/anchors/）の ids- の anchor の和で、凍結し直すたびに 1 本増える。最後の凍結より後に足した」
  - 前: 「いまの baseline は要件書 第 1.24 版の時点に凍結した ids-v1.24 の 1 本である。」 後: （消す＝前の文に寄せた・46 周目 整合 F-3・検証 B1 と n1）
  - 前: 「凍結し直す時機を決める口は持たない（後続・台帳の控え）。」 後: 「凍結し直す時機は規則の表の開発規律の行 D-15 が持つ（人が守る作法で、時機を機械が数える口は持たない）。」
- **条 N-3 の注**（46 周目 実態 F-1）
  - 前: 「条の中の行（rationale・retreat・relations・amended_by）の欄と、この一覧に入らない最上位の節の中の欄と、mechanism の鍵そのものの不在とほかの部位の必須の欄はまだ数えない（後続の便・台帳の控え）。」
  - 後: 「条の中の行のうち、relations の鍵は閉じた一覧（reqs・rules・articles・sections）に無いものを違反にし（crates/folio/src/refs.rs）、amended_by の各行の欄の欠けと未知の欄（違反の字「未知の欄（N-3）」）と必須の欄の空を違反にする（crates/folio/src/link.rs が欄の閉じた一覧で数える・必須の欄は条 A-2 と N-4 の機構の注と同じ中身）。rationale と retreat の行の未知の欄と、この一覧に入らない最上位の節の中の欄と、mechanism の鍵そのものの不在とほかの部位の必須の欄はまだ数えない（後続の便・台帳の控え）。」

**理由。** 判断の記録 ADR-11 決定 (3)(エ) は、検査の結果を分ける数を規則の表の行に置くと決めた。裁定 id と時刻（条 P-17.1）と、値を緩める前の検証（条 P-17.4）は規則の表の行にしか掛からないためである。番号の一覧は、凍結し直す時機が決まっていないために第 1.24 版で止まっている（天井の 31 周目 実態 F-1）。

**代替案。**
- **(A) 値を変えて載せる。** 今回の向きの裁定は「値は実装の今の値」なので採らない。
- **(B) R-18 を build-check のままにする。** 床の違反で落とす形は同じだが、上限を広げる前の検証（条 P-17.4）が掛からない（判定 2）。
- **(C) D-15 の時機を「本流へ取り込んだ後」にする。** 凍結 anchor 1 本のために別の PR が要る（判定 4）。
- **(D) D-15 を機械の口にする。** 実装の便が要り、台帳 f2-648.186 の案 (b)（作法の行）と違う。
- **(E) R-20 を今の字で足す。** 規則の表の決まり（時間は表の外）と行が食い違う（判定 7）。

**コスト。** 承認 1 回と、承認の前の枝の上の周（46 周目は合格・改訂 d の後に 47 周目）。**値は 1 つも変わらず、実装は直さない。** 写しの歯 1 本（f91）の期待を席が直す（§落ちる歯）。

**推奨 = 項 1・2・4 を承認（項 3 は保留・代替案は採らない）。**

### 🔴 問 2. 要件書 第 1.48 版（FR1 の規範文の「5 問以下」を、規則の表の行 R-18 を指す字にする）と入口 第 0.6 版

**平易な説明。** 問 1 で「質問は 5 つまで」の数を規則の表の行 R-18 に載せると、同じ 5 が要件書の FR1 の決まりの文にも残り、2 か所に同じ数を人が書くことになります。要件書の決まりの文からは数を消して「規則の表の行 R-18 が定める数以下」と書き、数は行 R-18 の 1 か所だけに置きます。入口のページの相談窓口の説明（「5 つまで出る」）も、数を書かない字にします。やさしく言い換えた文も「質問の数には上限があり（規則の表の行 R-18）」と、数を書かずに行を指す字にします。

**やりたいこと。** 要件書を第 1.48 版に上げ、要件 FR1 の規範文（**周の引き金の欄**）を次のようにする。

- 前: 「folio は 5 問以下の易しい質問を推奨回答つきで提示し、回答から文書の集合を写像して支度表 1 枚を生成する。」
- 後: 「folio は規則の表の行 R-18 が定める数以下の易しい質問を推奨回答つきで提示し、回答から文書の集合を写像して支度表 1 枚を生成する。」

同じ回で、FR1 の関係の欄 rules に R-18 を足し、注の末尾に数の正本の 2 文を置き、行 R-18 の注の出所の字を「値の正本はこの行・規範文はこの行を指す」にする。入口を第 0.6 版に上げ、相談窓口の節の本文を「易しい質問が 5 つまで出る。」から「易しい質問が、決まった上限の数まで出る。」に、歩みの「質問 5 つ（おすすめ付き）」を「易しい質問（おすすめ付き）」にする（どれも引き金の外）。FR1 の平易文も「質問の数には上限があり（規則の表の行 R-18）、どれにも「おすすめ」がついています。」にする（改訂 e・引き金の外）。確かめ方は変えない。

**理由。** 条 P-6.3（同じ内容を 2 つの面が持つとき一方を正本とする）と P-6.4（2 つの面を人が書く設計を採らない）。先例は非機能要件 NFR3 の第 1.25 版。入口の正本は頭注で数を書かない（P-6.3）と決めている（46 周目 整合 F-4）。入口の字に行の id を書かないのは、骨格の命令 `folio init` がこの本文を外の置き場へ写すためである（判定 6）。

**代替案。**
- **(A) 当てない。** 2 か所に同じ値が残る。
- **(B) 規範文の値を残し、行 R-18 の側を「FR1 の規範文の数」と書く。** ADR-11 決定 (3)(エ) と逆向きで、条 P-17.1・P-17.4 が要件書の値に掛からない。

**コスト。** 問 1 と同じ承認 1 回・同じ枝の上の周に含まれる（引き金の欄が 1 か所）。要件の番号・段・確かめ方・受入基準は変わらない。実装と歯は直さない。

**推奨 = 承認。問 1 の項 1 と対（問 1 の項 1 を退けたら問 2 も退ける）。**

## 承認の前に席が行う段取り

1. **写しの歯 f91 の期待を直す**（席の手）。改訂 b'・c' で当てた字から、改訂 d で R-20 の行を外す分を当てる（差分は `~/.local/share/folio2/handoff-2026-09-26/batch26-scripts/f91-refs.patch`〔`21d2823` からの差分〕・当てた写しで 952 / 952）。前の差分は `f91-refs-draft.patch`（起草）と `f91-refs-c.patch`（改訂 c）に残した。
2. **改訂 d の HEAD で 47 周目を回す**（束・4 観点・止めるがあれば反証）。本流が動いていたら先に枝へ取り込む。門は作業ツリーの一番上から `--dir design-intent` で撃つ。
3. **不合格なら**、支持の 止める に直しを当てて回し直す。**4 観点とも合格したら**、枝の上で印を書いて commit する。
4. **周の所見の直しを問 1・問 2 に足し**、1 回の承認要求で出す（46・47 周目の所見と結果を添える）。

## 承認の後に席が書く欄

持ち主が答えた後に、席がこの枝（`docs/batch26`）の別の commit で書く。退けた項の分は §問どうしの依存のとおり元の字に戻す。**「承認待ち」「起草」の字を持つ欄は全部ここに挙げた。**

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/rules.yaml` | 行 R-18・R-19・D-15 の `ruling`（改訂 f: 行 D-11 の `ruling` も = 注の 1 文の追記〔R-20 の保留〕・条 P-17.1・頭の「一括 26 の承認待ち（…）・前の裁定 = …」を「f2-648 notes <日付> <時刻> JST（逐語「<逐語>」・…）・前の裁定 = …」に） | 頭の「一括 26 の承認待ち（席が承認の後に…・値と文面は一括 26 の問 1 で持ち主に問う）」を「f2-648 notes <日付> <時刻> JST（逐語「<逐語>」・一括 26 の問 1 の項 n = …の初版の行）」に。後ろの「行を足す向きの裁定 = …15:22 JST…」は残す |
| 2 | `design-intent/rules.yaml` | 同じ 3 行と行 D-11 の `ruled_at` | 承認の日付（D-11 は改訂 f で 2026-09-26 = 起草の日・承認の日付に上げる） |
| 3 | `design-intent/anchors/ids-v1.48.yaml`（問 2 を承認）／`ids-v1.47.yaml`（問 2 を退ける） | 新しい file | 承認の記帳（#10〜#13 を含む）の commit の後、床が合格の枝で `folio check --dir design-intent --freeze-ids` を撃ち、できた file を commit する（行 D-15 の最初の当てはめ・写しで id 80 本・commit の後の床は合格） |
| 4 | `docs/design/batch26-triage.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に。枝の上の周の結果を足す |
| 5 | `docs/design/state.md` | 規則の表の行・次の一手・未決 | 行 R-18・R-19・D-15 の発効と、id の一覧の凍結 anchor が 2 本になったこと、R-20 の保留 |
| 6 | 台帳（`bd`） | f2-648 notes | 承認の逐語と時刻（条 P-12.2）・この仕分けの在り処・枝の上の周の結果 |
| 7 | 台帳（`bd`） | f2-648.75 | 当てたこと（R-18・R-19）と **R-20 の保留**（理由 = 生成区間の `excluded` の「時間」・前提の便・次の一括で R-20 として問う）を notes に記帳する。close するか、R-20 の分を新しい件に分けるかは席の判断。実装が行から読む便（#6・B）を新しい件に起こす |
| 8 | 台帳（`bd`） | f2-648.186 | 行 D-15 と凍結した file を notes に記帳して close |
| 9 | 台帳（`bd`） | 新しい件 | 生成区間の `excluded` の「時間・費用」を「人の作業の時間・費用」に狭める便（`crates/folio/src/rules.rs` の定数・S・R-20 の前提） |
| 10 | 器の列 | 窓の保留の解放 | 取り込みで窓を閉じたら、保留にした便の件を解放する（行 D-14） |
| 11 | `design-intent/srs.yaml` | `meta.effective_version`・`meta.status_note` の頭の 1 文 | v1.47 → v1.48。「いま発効しているのは 第 1.47 版（2026-09-26）です。」を「第 1.48 版（<承認の日付>）」に（問 2） |
| 12 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.48 | 頭の「起草」と末尾の「裁定 id〔承認の後に席が書く〕・承認待ち」の 2 か所を、発効の日付・逐語・裁定 id に（先例 = 一括 25 の改訂 f の v1.47） |
| 13 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・一括 26〔問 1・問 2〕の承認要求への回答）", verbatim: <逐語>, version: v1.48}`） |
| 14 | `design-intent/index.yaml` | 頭注の 1 行目・「# 状態:」・`meta.approval` | 1 行目を「（v0.6・orchestrator 席）」に。「# 状態:」の v0.6 の「起草・承認待ち（…・裁定 id〔承認の後に席が書く〕）」を発効の字に。承認の行 1 つ（`version: v0.6`・一括 26 の問 2） |
| 15 | `design-intent/adr/ADR-11.yaml` | `note` の末尾の段「決定 (4)⑦ の行き先」 | 「持ち主の裁定〔一括 26 の承認の後に席が書く〕」を「持ち主の裁定 f2-648 notes <日付> <時刻> JST（逐語「<逐語>」・一括 26）」に |

## 床の結果（改訂 d の字で実測・2026-09-26・参考値）

binary は本流の `target/debug/folio`。作業ツリーの一番上で、相対の `--dir` で撃った。script と出力は `~/.local/share/folio2/handoff-2026-09-26/batch26-scripts/`（`floor.sh`・`floor-d.txt`・`revise_d.py`・`f91-refs.patch`。前の改訂の分は `floor-head.txt`・`floor-b.txt`・`floor-c.txt`・`gate_probe.sh`・`gate-probe.txt`・`gate-probe-b.txt`）。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0）・rc 0。途中の字は参照 id の検査が 2 件で落とした（要件書の来歴と作成の行の「R-18〜R-20」が消えた R-20 を指す）＝来歴の字を直した。行 R-17 は新しい字でも 0 件 |
| `folio inject --check` | rc 0（68 行 / 7,912 byte 一致＝規範文と前文は動かない） |
| `folio schema --check` | rc 0（9 file 一致） |
| `folio derive --dir design-intent --out ../contracts --check` | rc 0（一致 1・差分 0） |
| `folio build --dir design-intent --out <一時 dir> --write` | 床 合格・rc 0・32 file |
| 門（枝の字） | rc 2（印が古い＝46 周目の後に引き金が動いた・想定どおり） |
| nextest（写し・`21d2823` + 改訂 d の design-intent + `f91-refs.patch`・`cargo nextest run -p folio --no-fail-fast`） | **952 / 952**。入口の最初の字（行の id を書いた本文）では `f125_no_folio2_ids_in_the_skeleton` が落ちて 951 / 952 だった（判定 6） |

**引き金の欄の数（写しの門の実測）。** 起草のとき 4 行それぞれが rc 2（`gate-probe.txt`）、改訂 b のとき FR1 の規範文だけが rc 2・要件書の規範文以外は rc 0（`gate-probe-b.txt`）、憲法だけは rc 0。改訂 d は行 R-20 を外したので、残る引き金は行 3 本と FR1 の規範文で **計 4**。入口の正本は引き金の一覧（`ceiling.rs` の `TRIGGER_*`）に無い。

### 改訂 e の床の結果（2026-09-26・参考値）

script と出力は同じ置き場の `revise_e.py`・`floor-e.txt`・`gate-e.txt`。

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0）・rc 0。途中の字は行 R-17 が 1 件で落とした（R-18 の注の A-2.3）＝refs に A-2.3 を足した |
| `folio inject --check` / `folio schema --check` / `folio derive --check` | rc 0 / rc 0 / rc 0 |
| `folio build --dir design-intent --out <一時 dir> --write` | 床 合格・rc 0・32 file |
| `folio ceiling --gate --dir design-intent --write-set design-intent/rules.yaml design-intent/constitution.yaml design-intent/srs.yaml design-intent/adr/ADR-11.yaml` | **通す（印が 4 観点とも合格・引き金の要約値が同じ・印の後に引き金の外の変更が在る（節点 6 個と節点の外の字・次の引き金の周が読む））・rc 0**＝**改訂 e で動いた引き金の欄は 0** |
| nextest（写し・`c1c1142` + 改訂 e の design-intent + `f91-refs.patch`） | **952 / 952** |

### 落ちる歯と期待の差分（席が直す）

`crates/folio/tests/check.rs` の `f91_the_real_rules_carry_the_refs_field`（写しの規則の表の refs の行を全部読み、行ごとの refs と合計を固定の表と比べる）。

| 所 | 本流の期待 | 枝の今（`21d2823`） | 改訂 d の後 |
|---|---|---|---|
| 表 `F91_ROWS` の長さ | 15 | 19 | **18** |
| 表の行（足す分） | — | R-18・R-19・R-20・D-15 | `("R-18", ["FR1", "ADR-11", "D-11", "R-16", "P-6.3", "P-17", "A-2.3"])`（改訂 e で P-17・A-2.3）・`("R-19", ["P-4.1", "ADR-16", "ADR-11", "D-11", "R-16"])`・`("D-15", ["P-7.1", "N-1.1"])`（R-20 の行を外す） |
| refs の行の数 | 15 | 19 | **18** |
| refs の id の合計 | 47 | 66 | **59**（66 − R-20 の refs 7）。**改訂 e で 61**（R-18 の refs に P-17 と A-2.3・`c1c1142` の 59 からの差分が `f91-refs.patch`・前の差分は `f91-refs-d.patch`） |

落ちる歯は f91 の 1 本だけである（`f91-refs.patch` を当てずに撃つと 951 / 952 になる見込み・当てて 952 / 952 を実測）。行の数を数えるほかの歯（`tests/face.rs`・`tests/face_index.rs`）は固定の数を持たないので落ちない。入口の字は骨格の歯が見るので、行の id を書かない字にした（判定 6）。

## まだ分からない点

- **行の値と実装の定数の一致を機械は数えない**（#6 の便が着地するまで）。その間は取り込みの審査で人が見る（行 D-11）。`intake.rs` の定数の注と違反の字は FR1 を出所に挙げたまま（46 周目 実態 F-2）で、#6 の便で直す。
- **行 R-20 の保留**（判定 7）。前提の便（生成区間の `excluded` を狭める）が着地するまで、版管理の待ち時間は実装にだけ在る。次の一括で R-20 として問う（持ち主が別を選べば従う）。待ち時間の「緩める」の向き（延ばすと まだ分からない が減る）は、そのときに判断の記録で読む。
- **FR1 の確かめ方の「決まった回答 5 つ」。** 相談窓口の正本の今の質問の数に合わせた材料の数と読んだ（上限の写しではない）。
- **行 R-19 の値は帯の表の長さと同じ数である。** 上限を広げるには面の生成器の帯の表も伸ばす便が要る。R-19 は deny なので、広げる前に条 P-17.4 の検証も要る。
- **D-15 は人が守る作法で、機械は時機を数えない。** 要件書の版が上がったのに凍結が無い状態を床は落とさない（代替案 (D) を採らないため）。
- **47 周目は合格した**（印 `c1c1142`）。改訂 e の字（引き金の外）は、門のとおり次の引き金の周が読む。
- **入口の版の起草のときの `meta.status`（47 周目 読みやすさ F-3）。** 入口の正本は要件書と違って発効している版の欄（`effective_version`）を持たず、起草の間も status は effective のまま（前の版の起草の先例と同じ）。面は起草中の v0.6 を「発効」と出す。要件書と同じく発効している版を別に持たせるかは、次の一括の材料（入口の正本の欄と面の生成器の両方が動く）。
- **面を 1440 の幅で目で見る確認はしていない。** 規則の表の面は行が 3 本増え、注は長い。
