# 設計: 便 109 — 入口の棚の閉じた一覧と設計ノートの状態の閉じた一覧・文書 id の形を `face_labels.rs`・`face_note.rs` から新しい `shelf.rs` へ責務で降ろし、層が上がる辺を 7 本から 6 本へ減らす（参考値・base main 1632cb7）（振る舞い不変・FR4 / FR19 / FR9）

- 要件: FR4（入口・憲法・要件書の面を 1 つの生成器と 1 組の design token で出す・第 1.30 版）/ FR19（文書の決まりの部分を機械の中の決まりから各 file の生成区間へ写す・第 1.30 版）/ FR9（設計ノートを 1 つの型で生成する・第 1.30 版）。入口の棚の閉じた一覧は入口の面の生成器が棚の行を組むのに読み、入口の正本の検査が棚の閉じた id の列（入口の正本の生成区間の床の木の葉）を組み立て時に取り出すのに読む。設計ノートの状態の閉じた一覧と文書 id の形は、設計ノートの面と入口の面の設計ノートの行と図の命令（FR15）が読む。図の命令は id の形の 1 行しか変わらないので req には挙げない。
- 条: P-2.4（部品・図の型・密度 profile は閉じた一覧で持つ）/ P-6.3（同じ内容を 2 つの面が持つとき、一方を正本とし他方は導出する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (2)(3)(4)(5) と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §4-a・§4-b の **便 109 の行**と §4-d。本便は ADR-15 が定めた 11 本の列の 4 本目である。便 108 の後に残る 層が上がる辺 7 本のうち、入口の正本の検査の file が面の file を名指す 1 本が本便で消える。ADR-15 の決定 (2) が 閉じた一覧が読む層に在るという定義が実測と合うのは 3 本目と 4 本目の便の後 と書くうちの、4 本目にあたる。**本便は ADR-15 の決定 (5) が 既存の歯は前の割りを字面で固定している 1 本だけを 4 本目で直す と定めた唯一の例外を持つ便である**（§1 (e)）。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `dh` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 8 本と `crates/folio/tests/` の 2 本だけで、verify が名指す歯の file 2 本〔入口の面の歯と図の命令の歯〕は本文を変えずに write-set に入る）。**起草役が write-set 12 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 108 の着地（main 1632cb7）を受けて、写しを実物の main に取り直して全部を測り直し、§0 の前の便・表題の base・§5 の依存（書き込み範囲の重なりの解消）・§1 (h) の 7 を実物の形に直した。区間・write-set・verify・done・size・上向きの辺・歯の本数と名・見え方・余地の表・src 全体の行数・出力の byte・diff の byte・構造の歯の直しは、初稿（便 108 の起草役の実測 patch の木で測った）と 1 つも違わなかった。変異は 5 本（棚の id・層 2・再輸出の形・構造の歯の見張りの輪を空にする・構造の歯を直さない）を実物で撃ち直し、結果は (d) の表と同じだった。
- 前の便: **便 108 着地済み（main 1632cb7・部品目録の閉じた一覧を `catalog.rs` へ・行 `dg`・台帳 f2-648.153）**。便 107 も着地済み（main 368a11a・行 `df`・台帳 f2-648.152）。起草役は初めに main 368a11a に便 108 の起草役の実測 patch を当てた木で測り始めたが、着地を受けて写しを 1632cb7 の実物に取り直し、区間・write-set・余地・上向きの辺・歯の本数と名・見え方の拡大・出力の byte・構造の歯の直しをすべて測り直した。便 108 の実物と patch の差は 2 file（`catalog.rs` の頭の注 3 行と `parts.rs` の頭の注 1 行）で、本便の区間と書き換える行には 1 つも掛からない（区間を当てる台本は無修正で当たり、足す行と消す行の字は patch の木と 1 字も違わない）。この契約の数はすべて 1632cb7 の実測（参考値）である。**base は main 1632cb7**。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り）

2 つの file が、面の生成と閉じた一覧を同居させている。

1. `crates/folio/src/face_labels.rs`（層 4 面に出す）は、面の共有の名札（憲法の値域の型への網羅の場合分けと、値域に依らない名札の表）と、**入口の棚の閉じた一覧**を持つ。棚の一覧は、棚の文書の型 `Shelf` と、文書の id から棚への表 `SHELF_DOCS`・関係の id から置き場の class への表 `SHELF_RELATIONS`・付録の id から章の番号と数の単位への表 `ANNEXES`・凡例の id から sw の class への表 `SHELF_LEGEND` の 5 つである（71 行）。どれも入口の正本の棚の行に許す id の集合を閉じる。
2. `crates/folio/src/face_note.rs`（層 4）は、設計ノートの面の生成と、**設計ノートの状態の閉じた一覧** `STATUS`（状態の値から名札への表）と、**文書 id の形** `is_doc_id`（欄の決まりの id_pattern を手書きで判じる関数）を持つ（13 行）。

1 の一覧は `face.rs` が `face_labels.rs` を丸ごと再輸出するので、呼ぶ側は `face` の名で名指している。**入口の正本の検査 `entrance.rs`（層 2 検査する）が、棚の閉じた id の列を組み立て時に取り出すために `face` を名指す**。これが便 108 の後に残る 層が上がる辺 7 本のうちの 1 本（`entrance` → `face`）で、実際の行き先は再輸出の元の `face_labels.rs` である（設計ノート §5-a の 4 の先例そのもの）。入口の面の生成器 `face_index.rs` と面の入口 `face.rs` も同じ一覧を読む。

2 の一覧は、入口の面の生成器（設計ノートの行の状態の名札と id の形）と図の命令 `figure.rs`（--doc の id の形）が `face_note.rs` を名指して読む。どれも同じ層 4 の辺で決まりには反していないが、閉じた一覧が面の層に在る。ADR-15 の決定 (2) が 閉じた一覧が読む層に在るという定義が実測と合うのは本便の後 と書いている部分である。

区間の外に定義がある名の列挙（ADR-15 決定 (5)）: 棚の一覧が使う名は同じ区間の `Shelf` だけで、状態の表と id の形は区切りの名を 1 つも使わない。区間を切り出して組み立て（`cargo build --all-targets`）を 0 警告で通して確かめた。

便 108 の後の 層が上がる辺は **7 本**（参考値）。境界の歯の file の凍結した一覧と一致している。

### (b) 直す先 — 2 つの閉じた一覧を新しい `shelf.rs`（層 1 読む）へ、字を変えずに移す

**1. 棚の閉じた一覧**（`face_labels.rs` の 71 行・型 1 と表 4）と、**設計ノートの状態の閉じた一覧と文書 id の形**（`face_note.rs` の 13 行）を、新しい file `crates/folio/src/shelf.rs` へ字を変えずに移す。新しい file の頭の注 3 行は、何をどこから降ろしたかと誰が読むかを書く。`face_labels.rs` と `face_note.rs` は頭の注を 1 行ずつ足す。`face_labels.rs` に残る名札の表（文書の状態・確かめ方・図の色・入口の状態の 4 つ）と、`face_note.rs` の面の生成の本体は 1 字も動かない。

**見え方の拡大は 0 か所**（ADR-15 決定 (3) が契約に書けと言う数）。棚の一覧はいま公開の印を持ち、`face.rs` の丸ごとの再輸出を通して道具の中のどこからでも届く。新しい file は入口の私の区切りなので、公開の印のままで届く範囲は同じである。状態の表と id の形は道具の中で公開の印のまま移る。起草役は src 全体で、道具の中で公開の印が 123 → 123、公開の印が 367 → 367、再輸出の行が 3 → 3 であることを数えた（参考値）。**再輸出の行は 1 行も置かない**（便 106〜108 と同じ）。`face.rs` の `face_labels.rs` の丸ごとの再輸出は残るが、そこから棚の一覧が抜けるだけで行は動かない。

**名指しの行だけを書き換える file**。棚の一覧を `face` 経由で、状態の表と id の形を `face_note` 経由で名指していた file は、どれも新しい file を直接名指すように取り込みの行を書き換える。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `entrance.rs` | 2 | 取り込み 1 行（面の区切りから新しい区切りへ・字の順の位置へ）と、棚の表を名指す 8 行の区切り名（棚の閉じた id の列の定義 4 行と単体の歯の 4 行） |
| `face.rs` | 4 | 付録の表の取り込み 1 行（字の順の位置へ） |
| `face_index.rs` | 4 | 取り込み 2 文（面の区切りの取り込みから棚の 5 つの名を外して 1 行に収め、`face_note` の取り込みを新しい区切りの取り込みに替える）と、状態の表と id の形を名指す 3 行と注 2 行の区切り名 |
| `face_note.rs` | 4 | 状態の表と id の形の取り込み 1 行（移した 2 つを本体が呼ぶため） |
| `figure.rs` | 4 | 取り込み 1 行（`face_note` から新しい区切りへ・同じ位置）と、id の形を呼ぶ 1 行の区切り名 |

**この 5 file では、表の行のほかは 1 字も動かない。** 入口 `main.rs` は区切りの宣言を 1 行足す（並びは字の順）。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の 4 本目で、塞がっている file を開ける便 113〜116 より先に運ぶ。入口の面の読み手を割る便 115 は、棚の一覧が本便で降りた後の形で測ってある（設計ノートの反対側からの確認の表で、便 115 の区間が名指してよい名は 棚の表と付録は便 109 で降りるので可 とされている）。

### (c) 採らなかった形

| 形 | 退けた理由 |
| --- | --- |
| 棚の一覧だけを降ろし、状態の表と id の形は `face_note.rs` に置いたままにする | 層が上がる辺の数は同じ 6 になるが、閉じた一覧が面の層に 1 つ残り、ADR-15 の決定 (2) が本便の後に実測と合うと言う定義が合わない。設計ノート §4-b の便 109 の行も 2 つを 1 便で運ぶ。起草役の走査では、2 つを降ろすと同じ層の相互の名指し（入口の面と設計ノートの面）が 1 対解ける（18 → 17・参考値） |
| `face_labels.rs` に新しい file の再輸出の 1 行を置き、`face.rs` 経由の名指しを残す | 走査は字面しか見ないので、入口の正本の検査は `face` への辺に見えたまま残り、層が上がる辺が減らない。境界の歯の一覧から対を消せば歯 2 が落ちる（起草役の変異の実測・(d)）。再輸出の穴も 1 つ増える（設計ノート §5-a の 4） |
| 閉じた一覧を便 108 の部品目録の閉じた一覧の file に足す | その file は組み立ての script が部品目録から導出した写しだけを取り込む file で、人の手の定義を持たない（P-6.4 の形）。手で書いた棚の一覧を同居させると、その file の責務が 導出した写し と言えなくなる |
| 閉じた一覧を入口の正本の検査 `entrance.rs`（層 2）へ置く | 層が上がる辺は消えるが、面の 4 file が検査の file を名指すことになり、閉じた一覧が検査の層に在る。ADR-15 の決定 (2)・(3) は閉じた一覧を層 1 に置く |
| 既存の構造の歯 `f87_face_is_split_and_under_the_cap` を直さない | 棚の一覧の 3 つの頭（`SHELF_DOCS`・`ANNEXES`・`Shelf`）が `face_labels.rs` に無くなるので、その歯が落ちる（起草役の実測・(d)） |
| その歯の見る頭の表から 3 つを消すだけにする | 歯は通るが、3 つの定義がどこに在るかを誰も見なくなる。起草役の変異では、3 つの見張りを外しても verify の行も workspace の 779 本も全部緑だった＝**弱めたことを歯は捉えない**（(h) の言えないことの 4）。本便は 3 つを新しい置き場で見張り続ける形を採る |
| その歯を消して新しい名の歯を足す・名を変える | 歯の名の一覧が変わる。ADR-15 の決定 (5) の 直すのは 1 本だけで、ほかは動かさない を、名と本数を変えないことと読む（便 106〜108 と同じ読み） |
| 閉じた一覧を層 0（土台）へ置く | 入口の正本の棚の id と、設計ノートの正本の状態と id の形を知っている＝文書の形を知っている。層 0 の定義（文書を何も知らない）に当てはまらず、層 1 の定義（正本の byte を型に直し、閉じた一覧を持つ）が当たる |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。

- `crates/folio/tests/face_index.rs`（20 本）: 入口の面の命令の歯。凍結 fixture（`tests/fixtures/face/` の入口の面の写し）との byte 一致、棚の表の外の値（関係・付録・凡例）が まだ分からない になること、生成した面が部品の検査を通ることを、命令を起動して測る。棚の一覧の中身（置き場の class・付録の章の番号・凡例の並び）が変われば落ちる。
- `crates/folio/tests/figure.rs`（23 本）: 図の命令の歯。文書 id の形に合わない --doc が まだ分からない になることを含む。
- 単体の歯（bin の中）の `f76_index_floor_derives_the_frozen_anchor_byte_for_byte`（1 本）: 入口の正本の生成区間の床の木が、凍結 anchor `tests/fixtures/schema/index-region.txt`（設計判断の席が独立に組んだ・P-10.1）と byte で一致する。床の木の葉は棚の閉じた id の列なので、棚の表の id が 1 つでも変われば落ちる。
- 構造の歯 `f87_face_is_split_and_under_the_cap`（1 本・本便で直す・(e) の 2）と境界の歯 2 本（(e) の 1）。
- 面・束・支度表・索引・部品の検査の出力: 起草役は base と本便の後の 2 つの binary で同じ正本から出力を作り、byte で突き合わせた（(f) の 1）。

起草役の変異の実測（写しの repo・verify の 6 行と workspace の nextest を撃って戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 構造の歯・3 入口の面の歯・4 図の歯・5 凍結 anchor の単体の歯・6 clippy）である。

| 変異 | verify で落ちたもの | workspace |
| --- | --- | --- |
| 棚の文書の置き場の class を 1 つ変える | 3（生成した面が部品の検査を通る歯・4 本） | 16 本落ちた |
| 棚の文書の id を 1 つ変える | 3（10 本）と 5（凍結 anchor） | 85 本落ちた |
| 付録の章の番号を変える | 3（凍結 fixture との byte 一致・2 本） | 15 本落ちた |
| 文書 id の形を緩める（先頭の大文字を許す） | 4（1 本） | 2 本落ちた（図の歯と設計ノートの面の歯） |
| 設計ノートの状態の名札の字を 1 つ変える | **落ちない** | **全部緑**（base から在る穴・(h) の 6） |
| 層の割り当ての表から新しい区切りの行を消す | 1（層の表の歯） | 同じ 1 本 |
| 消した対（入口の正本の検査から面）を一覧に戻す | 1（辺の歯・src に無い対） | 同じ 1 本 |
| 新しい区切りを層 2 に置く | **落ちない** | **全部緑**（(h) の 5） |
| 新しい区切りを層 4 に置く | 1（辺の歯・入口の正本の検査から新しい区切りが上向きになる） | 同じ 1 本 |
| 入口の正本の検査を面の区切りの名指しに戻し、`face_labels.rs` に新しい file の再輸出を置く（(c) の 2 行目） | 1（辺の歯・一覧に無い対 入口の正本の検査と面） | 同じ 1 本 |
| 棚の一覧の定義を `face_labels.rs` にも残す（写しを 2 つ持つ） | 2（構造の歯）と 6（使われない定義） | 1 本落ちた |
| 構造の歯を直さない（base の字のまま・(c) の 5 行目） | 2（3 本の頭が `face_labels.rs` に無い） | 同じ 1 本 |
| 構造の歯の、降りた 3 本の見張りの輪を空にする（(c) の 6 行目の形） | **落ちない** | **全部緑**（(h) の 4） |
| 構造の歯の `face.rs` の上限を 1,250 から 1,500 へ緩める | **落ちない** | **全部緑**（(h) の 4） |
| 構造の歯を消す（歯の印を外す） | 2（名に当たる歯が 0 本で nextest が断る） | 全部緑（1 本減る） |

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない・構造の歯 1 本を直す）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り shelf を層 1 で 1 行足す（47 行 → 48 行） |
| まだ降ろしていない層が上がる辺の一覧 | 入口の正本の検査の file から面の file への 1 対を消す（7 対 → 6 対）。**行を足さない** |

残る 6 対は、凍結の様態の型を名指す 3 対（anchor・check・ids から freeze）、束を名指す 2 対（所見と門から bundle）、印の名を名指す 1 対（門から stamp）で、設計ノートの反対側からの確認（改訂 c の最終確認）が組み立てで再現した内訳から、部品の検査から face（便 108）と入口の正本の検査から face（本便）を除いたものと一致する。

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 48 区切りで一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 6 対で一致 |

**2. 構造の歯 1 本を直す（ADR-15 決定 (5) の唯一の例外）。** `crates/folio/tests/face_labels.rs` の `f87_face_is_split_and_under_the_cap` は、便 87 が `face.rs` から名札と名札の表を `face_labels.rs` へ割ったときの file の配り方を字面で固定する歯である。設計ノート §4-d はこう書く: **既存の歯も 1 本だけ直す。便 109 は、前の割り（便 87）を字面で固定している歯 f87_face_is_split_and_under_the_cap を直す。入口の棚の閉じた一覧がその歯の見る file から降りるので、直さないと落ちる。** ADR-15 の決定 (5) も 既存の歯は、前の割りを字面で固定している 1 本だけを 4 本目で直し、ほかは 1 本も動かさない と定める。

- **直す理由**: この歯は出力（生成される HTML の byte）を測らない。便 87 の割りで移した 8 本の定義の頭が `face_labels.rs` に在って `face.rs` に無いことを、file の字面で数える。本便は、そのうち棚の一覧の 3 本（`SHELF_DOCS` の表・`ANNEXES` の表・`Shelf` の型）を `face_labels.rs` から新しい file へ降ろすので、歯の前提の配り方そのものが変わる。配り方を変える便がその配り方を固定する歯を直すのは同義反復であって、振る舞い不変の約束を緩めない（設計ノートの反対側からの確認の §4-d への所見）。
- **直す範囲（前の割りの file の配り方を固定する部分だけ）**: 歯が数える 4 つのうち、変えるのは 2 つ目（8 本の頭が `face_labels.rs` に在る）の 3 本分だけである。8 本の頭の表を、`face_labels.rs` に残る 5 本の表と、本便で新しい file へ降りた 3 本の表の 2 つに割り、3 本については **新しい file に在る・`face_labels.rs` に無い・`face.rs` に無い** の 3 つを数える 1 つの輪を足す。残る 5 本の 在る と無い、`face.rs` の器の式の上限 1,250、`face.rs` の丸ごとの再輸出の行がちょうど 1 本、の 3 つは 1 字も変えない。3 本の頭の字面も変えない（表の行を移すだけ）。歯の名・本数・置き場（`tests/face_labels.rs`）も変えない。頭の注に便 109 で直したことを 1 行足す。
- **弱めない**: 直す前の歯は 3 本について `face_labels.rs` に在る と `face.rs` に無い を数えていた。直した後は 新しい file に在る と `face.rs` に無い を数え、`face_labels.rs` に無い を足す。見張りの数は 6 から 9 に増え、1 つも減らない。
- **出力の不変は別の歯が受け持つ**（設計ノート §4-c・ADR-15 決定 (5)）: 構造の歯は出力の不変を測らないので、直しても出力の不変の主張は緩まない。出力の不変を受け持つのは次の 3 つで、本便はどれも 1 つも動かさない。(1) **歯の総数**: workspace の nextest が base と同じ 779 本すべて緑（名の一覧も 1 つも違わない）。(2) **床**: `folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）。(3) **生成区間の一致**: `folio schema --dir design-intent --check` が 9 file とも一致。これに加えて、入口の面の凍結 fixture の歯と、入口の正本の生成区間の凍結 anchor の歯（(d)）が棚の一覧の中身を独立に測る。
- **構造の歯は 便が触れない基準 ではない**（設計ノート §4-d・§5-a の 1 と同旨）: 境界の歯 2 本と同じく、固定している割りを変える便が自分で書き直せる。本便の直しはその形が実際に起きた 1 例目で、書き直しが弱めていないことを確かめるのは取り込みの審査である（(h) の 4・(i) の 3）。

**歯の本数は base の 779 本のまま**（参考値・本便は歯を足しも消しもしない）。歯の名の一覧（`cargo nextest list --workspace`）は base と 1 つも違わない（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。配信先の組み立て（`folio build --write`・22 file・1,231,263 byte）は `diff -r` で差 0。天井の材料の束（`folio ceiling --write`・観点 4・191 file・6,714,361 byte）は差 0。相談窓口の質問（`folio intake --print`）と索引（`folio graph --print`・37,394 byte）と導出した一覧の JSON の 1 行（`folio parts --print`・1,400 byte）は byte 一致。部品の検査（`folio parts --check`・組み立ての出力の 5 面に当てた）はどちらも 合格 で出力が byte 一致。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致（68 行・7,912 byte）。どれも終了コード 0。入口の正本の生成区間は棚の閉じた id の列を葉に持つので、本便の移動が id の列を 1 つでも動かせば `schema --check` と凍結 anchor の歯（(d)）が落ちる。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **779 本すべて緑**、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。本便では、入口の正本の検査の単体の歯が棚の表を区切りの名で名指す 4 行は、この組み立てでしか見えない。

### (g) 大きさ

size は **S**（見積 100 行）。本便が新しく書く行は、新しい file の頭の注 3 行と空行 3 行・`face_labels.rs` と `face_note.rs` の頭の注 1 行ずつ・取り込みの行と区切り名の行（`entrance.rs` 9・`face.rs` 1・`face_index.rs` 7・`face_note.rs` 1・`figure.rs` 2）・区切りの宣言 1 行・構造の歯の直しの 19 行と、境界の歯の file の 1 行である。**ほかはすべて字を変えない移動**（棚の一覧の 71 行・状態の表の 7 行・id の形の 6 行と、構造の歯の頭の表の 3 行ほか）である。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | S の見積 100 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/face_labels.rs` | 326 | 1,174 | +1,074 | 255（減る・余地 1,245） |
| `crates/folio/src/face_note.rs` | 1,024 | 476 | +376 | 1,011（減る・余地 489） |
| `crates/folio/src/shelf.rs` | 新設（base に無い） | 1,500 | +1,400 | 90（余地 1,410） |
| `crates/folio/src/main.rs` | 577 | 923 | +823 | 578 |
| `crates/folio/src/entrance.rs` | 378 | 1,122 | +1,022 | 378 |
| `crates/folio/src/face.rs` | 986 | 514 | +414 | 987（余地 513） |
| `crates/folio/src/face_index.rs` | 1,379 | **121** | +21 | 1,376（減る・余地 124） |
| `crates/folio/src/figure.rs` | 700 | 800 | +700 | 700 |

**いちばん狭いのは `face_index.rs` の 121 で、S の見積 100 を上回る。** M（見積 300）では `face_index.rs` の 1 本で受付が断る。この file は ADR-15 が 塞がっている 4 file と呼ぶものの 1 つで、本便は取り込みの 2 文と区切り名しか触らない（正規化行は 3 行減る）。**`face_labels.rs` と `face_note.rs` の余地は 1,174 と 476 で、どちらも S の見積を大きく上回る**（どちらも本便で減る）。

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 318 | 1,182 | 318（1 行足して 1 行消す） |
| `crates/folio/tests/face_labels.rs` | 62 | 1,438 | 83（余地 1,417） |
| `crates/folio/tests/face_index.rs` | 810 | 690 | 不変 |
| `crates/folio/tests/figure.rs` | 821 | 679 | 不変 |

src 全体は 47 file・26,201 行から 48 file・26,206 行になる（参考値）。

**設計ノート §4-b との差**（参考値）: 移すもの・新しい file・名指しの file・直す構造の歯は §4-b の便 109 の行と揃う。数の違いは 3 点で、どれも理由がある。(1) `face_labels.rs` の 326 → 255 は §4-b と同じ。`face_note.rs` は §4-b の 1,023 → 1,012 に対し 1,024 → 1,011 で、base の 1 行は便 108 の取り込みの行の差、後の 1 行の差は本便が頭の注を 1 行足し、§4-b の測り（ADR-15 の起草役の写しの台本）が置いた取り込み 1 行を字の順の位置で同じに置いた差である。(2) 新しい file は §4-b の 87 行に対し 90 行で、頭の注を 3 行にした分である。(3) §4-b の測りは取り込みの行を元の位置で置き換え、区切り名を道つきで書いた。本便は取り込みの行を字の順の位置へ置き、`face_index.rs` の面の区切りの取り込みを 1 行に収めた（正規化行は 3 行減る）。§4-b の測りは構造の歯の 3 本について `face.rs` に無い を数えなかったが、本便は数え続ける（(e) の 2・弱めない）。

**便の diff の大きさ**は、写しの repo で base からの差を測って **18,699 byte**（参考値・10 file・足す 137 行・消す 111 行）で、器の門が測る上限 150,000 byte の下に収まる。1 本の割りなので 1 行で運べる。

### (h) 本便が運ばないもの・言えないこと・撤退条件

**運ばないもの。**

- 残る 6 対の 層が上がる辺（天井の読み手と印の名・凍結の様態の型・`folio check` の口の後始末）。便 110〜112 が運ぶ。
- `face_labels.rs` に残る名札の表（文書の状態・確かめ方・図の色・入口の状態）と、読む順番の行き先の判じ（`stop_anchor`）と確かめ方の名札の関数。どれも面の file だけが読む名札で、層が上がる辺に効かない。これらを閉じた一覧として層 1 へ降ろすかは本便では決めない（設計ノート §4-b の便 109 の行に無い）。
- `face.rs` の `face_labels.rs` の丸ごとの再輸出の行。棚の一覧が抜けるだけで行は動かない。
- 入口の正本の検査が持つ棚の閉じた id の列（取り出しの定数 4 本）と、入口の正本の生成区間の床の木。区切り名の行のほかは動かない。
- 塞がっている 4 file（判断の記録の床・設計ノートの床・入口の面・憲法の面）を開ける割り。便 113〜116 が運ぶ。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指し。本判断の外である。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる。
2. 走査は**字面だけを見る**。再輸出を挟めば辺を一覧に書かずに同じ依存を作れる（今の repo に先例が在り、本便が消す辺の実際の行き先もそうだった）。本便は再輸出を 1 行も足さない。
3. 走査は**区間の外から接頭辞なしで取り込んだ名を見ない**。本便の区間では、`face_note.rs` の本体が状態の表と id の形を接頭辞なしで呼んでおり、組み立て（`cargo build --all-targets`）で確かめて取り込み 1 行で届かせた。
4. **構造の歯の直しが弱めても、どの歯も落ちない。**起草役の変異では、構造の歯の降りた 3 本の見張りの輪を空にしても、`face.rs` の上限を 1,250 から 1,500 へ緩めても、verify の 6 行も workspace の 779 本（参考値）も全部緑だった（(d) の表）。歯を丸ごと消す変化は verify 2（名に当たる歯が 0 本）が捉えるが、本文を弱める変化は捉えない。**着地の時にこれを確かめられるのは取り込みの審査だけである。審査は (i) の 3 の手順で区間の純度を確かめ、構造の歯の差分が (e) の 2 の範囲（3 本の頭の表を割り、新しい file に在る・`face_labels.rs` に無い・`face.rs` に無い の輪を足す）に収まっていることを読む。**
5. **新しい区切りの層の割り当ての誤りを、境界の歯は一部しか捉えない。**起草役の変異で、新しい区切りを層 1 でなく層 2 に置いても全部緑だった（(d) の表）。本便の後に新しい区切りを名指すのは層 2（入口の正本の検査）と層 4 の file だけなので、層 2 に置いても上向きの辺が生えないためである。層 4 に置けば辺の歯が落ちる。層 1 が正しいことは (c) の 層 0 の行と ADR-15 決定 (2) の定義で示し、審査が境界の歯の file の差分で確かめる。
6. **設計ノートの状態の名札の字が変わっても、どの歯も落ちない**（(d) の表・base から在る穴）。状態の名札は設計ノートの面と入口の面に出るが、設計ノートの凍結 fixture は状態 draft の 1 本、実の正本は状態 example の 2 本で、変異で変えた名札（発効）の設計ノートが 1 本も無いためである（起草役が正本を数えて確かめた。ほかの状態の名札が変われば捉えるかは測っていない）。本便はその表を字を変えずに移すだけで、穴を広げも狭めもしない（着地の時の確かめは (i) の 3 の区間の純度）。穴を塞ぐ歯を足すのは本便の外である（ADR-15 決定 (5) の 歯は増えない）。
7. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、便 110 以降と並行に運べない（直列になる）。

**撤退条件**。ADR-15 の撤退条件 (2) を本便に当てると、着地後の 層が上がる辺が直前（便 108 の着地後の 7 本）より**増えていたら**、決定 (5) の順を組み直す。増えたかを捉えるのは、境界の歯の file の差分（凍結した一覧の行の増減）を読む取り込みの審査であり、記帳は台帳 f2-648 に行う。歯は一覧との一致しか見ない。本便の計画値は 6 本（参考値）で、6 本と違ったら（7 本以下でも）計画か実測のどちらかが誤っているので、次の便を出す前に審査が列を見直す。これは ADR より厳しい本便の自主の条件である。どちらの場合も区間の切り出しは戻さない。戻すと 1 本の層が上がる辺が復活し、列の残りの前提が崩れるためである。

構造の歯の直しについての撤退条件は ADR-15 に無い。本便は自主の条件として、取り込みの審査が構造の歯の差分を (e) の 2 の範囲の外（3 本の頭のほかの見張りの変更・名の変更・見張りの数の減り）と判じたら、差分を差し戻して契約を改訂する。ADR-15 の撤退条件 (3)（境界が責務で引けない場所）には当たらない。本便の 2 つの一覧はどちらも層 1 の定義に当たる。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7 と同じで、次の 6 つである。起草役の台本（区間を当てる 1 本・走査・行数・変異・出力の比較）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d109-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 47 file・233 辺（下 154・同じ層 72・上 7）、本便の後が 48 file・235 辺（下 159・同じ層 70・上 6）だった（参考値）。消える辺は 3（入口の正本の検査から面・入口の面から設計ノートの面・図から設計ノートの面）、足す辺は 5（入口の正本の検査・面の入口・入口の面・設計ノートの面・図から新しい file・どれも下向き）。
3. **区間の純度と見え方と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（移動は 88 行。移動でない行は (g) の新しく書く行と、取り込みと区切り名の旧い行だけ）。構造の歯の file の差分で移動でない行は、足す側が頭の注 1 行・2 つの表の注 2 行・降りた 3 本の表の開き 1 行・3 本を見張る輪の 15 行の 19 行、消す側が元の表の注 1 行だけである（3 本の頭の字面と表の閉じと空行は多重集合の上で移動と数える）。見え方は src 全体の 道具の中で公開 の印と 公開 の印と再輸出の行の数を base と比べる。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違いは 0）。
4. **出力の byte**: base と本便の後の binary で `folio build --write`・`folio ceiling --write`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check`（組み立ての出力の 5 面）を撃ち、`diff -r` と `cmp` で比べる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。

## 2. 範囲

- 入れる: 新しい src の file 1 本（入口の棚の閉じた一覧と設計ノートの状態の閉じた一覧・文書 id の形）・`face_labels.rs` と `face_note.rs` からの字を変えない移動・その 2 file の頭の注 1 行ずつ・`face_note.rs` の取り込み 1 行・入口の区切りの宣言 1 行・名指しの 4 file（`entrance.rs`・`face.rs`・`face_index.rs`・`figure.rs`）の取り込みの行と区切り名・境界の歯の file の層の表 1 行と辺の一覧 1 対の削除・構造の歯 `f87_face_is_split_and_under_the_cap` の、前の割りの file の配り方を固定する部分（3 本の頭の置き場）の直し。
- 入れない: 残り 6 本の層が上がる辺・`face_labels.rs` に残る名札の表と関数・`face.rs` の丸ごとの再輸出・入口の正本の検査の棚の id の列と床の木・構造の歯のほかの見張り（器の式の上限・残る 5 本の頭・再輸出の行）と名と本数・ほかの既に在る歯の名や本数や置き場の変更・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯・再輸出の行。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| shelf | 新しい src の file | `crates/folio/src/shelf.rs`（責務の層 1 読む）。入口の棚の閉じた一覧（棚の文書の型と表 4 つ）を `face_labels.rs` から、設計ノートの状態の閉じた一覧と文書 id の形を `face_note.rs` から、字を変えずに受ける |
| trim | 取り除き | `crates/folio/src/face_labels.rs` から棚の一覧を、`crates/folio/src/face_note.rs` から状態の表と id の形を外す。どちらも頭の注を 1 行足し、`face_note.rs` は移した 2 つを取り込む 1 行を足す |
| rewire | 名指しの行 | 棚の一覧を `face` 経由で、状態の表と id の形を `face_note` 経由で名指していた 4 file（入口の正本の検査・面の入口・入口の面・図）の取り込みの行と区切り名を新しい区切りへ向け直す |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に shelf を層 1 で 1 行足し、層が上がる辺の凍結した一覧から 1 対を消す（7 対 → 6 対）。歯の本文は変えない |
| f87 | 構造の歯の直し | `crates/folio/tests/face_labels.rs` の `f87_face_is_split_and_under_the_cap` の頭の表を、残る 5 本と降りた 3 本に割り、3 本を新しい file に在る・`face_labels.rs` に無い・`face.rs` に無い で見張る（ADR-15 決定 (5) の唯一の例外・§1 (e) の 2） |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、置き場も移さない。境界の歯 2 本は型付きデータだけが変わり、構造の歯 1 本は前の割りの file の配り方を固定する部分だけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) の 4〜6 に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h) 4・5）ので、取り込みの審査は区間の純度と構造の歯の差分を §1 (i) の 3 の手順で確かめる。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる着地済みの便: 便 108（部品目録の閉じた一覧・main 1632cb7・台帳 f2-648.153）・便 107（正本の cursor・main 368a11a・台帳 f2-648.152）・便 106（床の機械・main b8fb27f・台帳 f2-648.151）。
- **書き換える file が重なる並行の便は無い**（起草役が base main 1632cb7 で確かめた）。便 108 の live な run との書き込み範囲の重なり（初稿の受付の先撃ちで出た 1 件）は、便 108 の着地で**解消**した。
- ADR-15 の列の便 110 以降は、どれも境界の歯の file を書き換えるので**本便の着地を待つ**（設計ノート §5-a の 6・直列になる）。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "dh"
title = "入口の棚の閉じた一覧（棚の文書の型 Shelf と表 SHELF_DOCS・SHELF_RELATIONS・ANNEXES・SHELF_LEGEND）を crates/folio/src/face_labels.rs から、設計ノートの状態の閉じた一覧 STATUS と文書 id の形 is_doc_id を crates/folio/src/face_note.rs から、新しい file crates/folio/src/shelf.rs へ字を変えずに責務で降ろす。これは判断の記録 ADR-15 の決定 (2)(3)(5) が定めた 11 本の列の 4 本目で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 109 の行と §4-d がその中身を持つ。降ろす先は責務の層 1 読む である。入口の正本の検査 entrance.rs が face.rs の丸ごとの再輸出を通して棚の表を名指していたのが、検査の file から面の file への層が上がる辺の出どころなので、entrance.rs は新しい file を直接名指し、層が上がる辺は 7 本から 6 本へ減る。見え方の拡大は 0 か所で、再輸出の行は 1 行も置かない。棚の表・状態の表・id の形を名指していた entrance・face・face_index・face_note・figure は取り込みの行と区切り名だけを書き換え、入口 main.rs は区切りの宣言 1 行だけを足し、ほかの行は 1 字も動かない。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の型付きデータを書き換え、層の割り当ての表に shelf を層 1 で 1 行足し、まだ降ろしていない層が上がる辺の凍結した一覧から入口の正本の検査の file から面の file への 1 対を消して 6 対にする（行は足さない・歯の本文は変えない）。ADR-15 の決定 (5) の唯一の例外として、前の割り（便 87）の file の配り方を字面で固定する構造の歯 f87_face_is_split_and_under_the_cap（crates/folio/tests/face_labels.rs）の、降りた 3 本の定義の頭の置き場を見る部分だけを直し、3 本が新しい file に在り face_labels.rs にも face.rs にも無いことを見張る（名・本数・ほかの見張りは変えず、見張りの数は減らない）。出力の不変はこの歯でなく、歯の総数（workspace の 779 本すべて緑で名も同じ）と床（folio check 合格）と生成区間の一致（folio schema --check の 9 file）が受け持つ。歯は足さず消さず、面・束・支度表・索引・部品の検査の出力の byte も生成区間の byte も床の判定も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR4", "FR19", "FR9"]
section = "1"
write-set = ["+crates/folio/src/shelf.rs", "-crates/folio/src/face_labels.rs", "-crates/folio/src/face_note.rs", "-crates/folio/src/face_index.rs", "crates/folio/src/main.rs", "crates/folio/src/entrance.rs", "crates/folio/src/face.rs", "crates/folio/src/figure.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/face_labels.rs", "crates/folio/tests/face_index.rs", "crates/folio/tests/figure.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test face_labels", "cargo nextest run -p folio --test face_index", "cargo nextest run -p folio --test figure", "cargo nextest run -p folio --bin folio f76_index_floor_derives_the_frozen_anchor_byte_for_byte", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "S"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した 6 対とちょうど一致する）、構造の歯 f87_face_is_split_and_under_the_cap の 1 本が緑（face.rs が器の式で 1,250 行以内・face_labels.rs に残る 5 本の頭が在って face.rs に無い・降りた 3 本の頭が新しい file に在って face_labels.rs にも face.rs にも無い・face.rs の丸ごとの再輸出がちょうど 1 本・名は base と同じ）、入口の面の命令の歯 20 本が緑（凍結 fixture との byte 一致と、棚の表の外の値が まだ分からない になることを含む・base と同じ本数で名も 1 つ違わない）、図の命令の歯 23 本が緑（文書 id の形に合わない --doc が まだ分からない になることを含む・base と同じ本数で名も 1 つ違わない）、bin の単体の歯 f76_index_floor_derives_the_frozen_anchor_byte_for_byte の 1 本が緑（入口の正本の生成区間の床の木が凍結 anchor と byte で一致する＝棚の閉じた id の列が動かない）、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い）で、workspace の nextest が 779 本すべて緑（base と同じ本数で名も 1 つ違わない・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
