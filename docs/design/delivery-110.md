# 設計: 便 110 — 天井の正本の読み手と材料の束の組み直し・所見と束の閉じた一覧 8 本・印の file 名を `bundle.rs`・`ceiling.rs`・`stamp.rs` から新しい `ceiling_src.rs` へ責務で降ろし、層が上がる辺を 6 本から 3 本へ減らす（参考値・base main 0f00d59）（振る舞い不変・FR17 / FR18 / FR20 / FR19）

- 要件: FR17（天井の材料の束を観点ごとに組む・第 1.30 版）/ FR18（所見の形と天井の 3 値を床で数える・第 1.30 版）/ FR20（天井の門・印と門・第 1.30 版）/ FR19（文書の決まりの部分を機械の中の決まりから各 file の生成区間へ写す・第 1.30 版）。天井の正本の読み手と束の組み直しは、束を組む命令（FR17）と、所見を数えるときに束を組み直して要約値を突き合わせる検査（FR18）と、印と門（FR20）が読む。所見と束の閉じた一覧 8 本は天井の正本の生成区間の床の木の葉（FR19）で、束の読み手も同じ一覧を読む。
- 条: P-5.1（規則・閾値・型の一覧は型付きデータに置く）/ P-6.3（同じ内容を 2 つの面が持つとき、一方を正本とし他方は導出する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (2)(3)(4)(5) と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §4-a・§4-b の **便 110 の行**と §4-e の末尾の段。本便は ADR-15 が定めた 11 本の列の 5 本目である。便 109 の後に残る 層が上がる辺 6 本のうち、所見の検査と門が束の file を名指す 2 本と、門が印の file を名指す 1 本の、計 3 本が本便で消える。区間に所見と束の閉じた一覧 8 本を入れるのは、設計ノートの反対側からの確認の直し b-1-2（入れないと 3 でなく 4 になる）による。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `di` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 9 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 4 本〔束・所見・印・門の命令の歯〕は本文を変えずに write-set に入る）。**起草役が write-set 14 本をそのまま渡して（`--write-set=<字>` の形で。空白区切りで `-` 始まりの字を渡すと命令の読み手が旗と読んで断る）実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 改訂: 改訂 b（2026-09-23・検証役 d110-verify.md の直し 1〜4）= size M の理由を器の事実（L も読むが余地で断る）に・撤退条件 (3) の言い切りを審査に預ける形に・(h) 8 見え方の数と歯の本数をどの歯も見ないこと・--refute の 0 件の断りと門の渡し方。patch・write-set・verify・done・size は直していない。
- 前の便: **便 109 着地済み（main 0f00d59・入口の棚の閉じた一覧と設計ノートの状態の閉じた一覧を `shelf.rs` へ・行 `dh`・台帳 f2-648.154）**。便 108 も着地済み（main 1632cb7・行 `dg`・台帳 f2-648.153）。起草役は初めに main 368a11a に便 108 と便 109 の起草役の実測 patch を当てた木で測り始め、便 108 の着地で 1632cb7 に便 109 の patch を当てた木へ、便 109 の着地で 0f00d59 の実物へ、写しを 2 度取り直した。**区間を当てた差分は 3 つの木で 1 字も違わず**（実測 patch の sha256 が同じ）、区間・write-set・余地・上向きの辺・歯の本数と名・見え方の拡大・出力の byte・diff の byte もすべて同じだった。便 109 の実物と patch の差（`shelf.rs` の頭の注・`tests/face_labels.rs` の 2 行）は本便の区間に掛からない。変異の束は 0f00d59 の実物の木で撃ち直した。**この契約の数はすべて 0f00d59 の実測（参考値）である。base は main 0f00d59**。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り）

天井（AI による意味の検査）の file 3 本が、層の違う責務を同居させている。

1. `crates/folio/src/bundle.rs`（層 3 導出する・正規化 730 行）は、束を組む命令 `folio ceiling --write` の口 `run` と置き場へ書く `write_all` のほかに、**天井の正本を読む手**（型 `Ceiling`・`Viewpoint`・`Rules` と `load` とその補助）、**束を memory の上で組み直す手**（`Built`・`build_all`・`build_one`・`build_counted`・最上位の節で切る `cut_sections` とその補助・正本と面の写し `copy_sources`・`copy_faces`・`read_dir_names`・`question_text`・`reads_text`）、**束の要約値** `digest_text` と `DIGEST_FILE`、**面の名の形の表** `FACE_NAMES` と型 `FaceName`、**1 回の実行の結果の型** `Outcome`、天井の正本の file 名を持つ（base の 32〜125 行の 94 行と 144〜638 行の 495 行）。
2. 所見の検査 `findings.rs`（層 2 検査する）は、所見を数えるときに束を組み直して要約値を突き合わせるため、2 の読む手・組み直す手・要約値と結果の型を `bundle` の名で名指す。**これが層が上がる辺 `findings` → `bundle`** である。
3. 天井の門 `gate.rs`（層 2）は、正本の要約値を測るため読む手と `read_dir_names` を `bundle` の名で名指し（**`gate` → `bundle`**）、印の file 名 `STAMP_FILE` を印の file `stamp.rs`（層 3）から取る（**`gate` → `stamp`**）。
4. 天井の床 `ceiling.rs`（層 2）は、**所見と束の閉じた一覧 8 本**（観点の 3 値 `VERDICT_VALUES`・所見の必須の欄 `FINDING_REQUIRED`・任意の欄 `FINDING_OPTIONAL`・場所の欄 `PLACE_REQUIRED`・反証の値域 `REFUTE_VALUES`・起動の記録の欄 `RECORD_REQUIRED`・写しの骨格 `BUNDLE_SKELETON`・要約値の規則の名 `BUNDLE_DIGEST`）を持つ。どれも天井の正本の生成区間の床の木の葉で、束の読む手（所見の欄の決まりを埋める・骨格を残す・要約値の頭）も接頭辞なしで取り込んで読む。
5. 印 `stamp.rs` と索引 `graph.rs`（どちらも層 3）も読む手を `bundle` の名で名指す（同じ層の辺で決まりには反していない）。

区間の外に定義がある名の列挙（ADR-15 決定 (5)・設計ノート §5-a の 5）: 1 の区間が使う名は、要約値の計算 `sha256`（層 0）・判定の 3 値の型（層 0）・正本の構文の木 `yaml`（層 1）・取り出しの結果の型（層 1 の `cursor`）と、4 の閉じた一覧 8 本だけである。8 本は区間が接頭辞なしで取り込んでいるので字面の辺に現れず、区間だけを層 1 へ降ろすと層 1 から層 2 への上向きの辺が 1 本生える（反対側からの確認の b-1-2）。印の file 名は何も名指さない。区間と 8 本と印の file 名を切り出して、組み立て（`cargo build --all-targets`）を 0 警告で通して確かめた。

便 109 の後の 層が上がる辺は **6 本**（参考値）。境界の歯の file の凍結した一覧と一致している。

### (b) 直す先 — 3 つを新しい `ceiling_src.rs`（層 1 読む）へ、字を変えずに移す

**1. 新しい file** `crates/folio/src/ceiling_src.rs` へ、次を字を変えずに移す。所見と束の閉じた一覧 8 本（`ceiling.rs` の注 1 行と定義 1 行ずつ・16 行）、印の file 名（`stamp.rs` の注と定義の 2 行）、束の側の区間（`bundle.rs` の 94 行と 495 行）。新しい file の頭の注 4 行は、何をどこから降ろしたか・移した注の中の file 名は移す前の置き場を指すこと・誰が読むかを書く。取り込みは 7 行（`sha256`・判定の型・`yaml`・取り出しの結果の型と標準の 3 行）。新しい file は 660 行（参考値）。

`bundle.rs` に残るのは、頭の注（1 行を足す）と、命令の口 `run` と置き場へ書く `write_all` と、単体の歯 1 本である（109 行）。`ceiling.rs` は 8 本の定義と間の空行が抜け、頭の注 1 行と 8 本の取り込み 1 文（4 行）を足す。`ceiling.rs` の床の木・形の検査・ほかの閉じた一覧（観点の id の列・文書の id の集合・束の中身・反証の束の中身・結果の欄・反証の規則の文・最上位の節）は 1 字も動かない。`stamp.rs` は印の file 名の 2 行と空行が抜け、頭の注 1 行を足す。

**2. 単体の歯 3 本**（`bundle.rs` の単体の歯の区画）。ADR-15 の決定 (5) の ほかは 1 本も動かさない を、便 106〜109 と同じく 名と本文を変えない と読み、置き場は測る名の在る所へ合わせる。

| 歯 | 測る名 | 置き場 |
| --- | --- | --- |
| `bundle_face_name_forms_follow_the_site_outputs` | 面の名の形の表と、型 `FaceName` の私の関数 | 新しい file の単体の歯の区画へ本文と名を変えずに移す。**残すと組み立たない**（その関数は私なので別の区切りから呼べない・起草役が写しで `cargo check --all-targets` を撃ち、私の関数を呼ぶ誤り 6 件を実測） |
| `bundle_digest_concatenates_in_path_byte_order_without_itself` | 要約値の 1 行・要約値の file 名・要約値の規則の名 | 新しい file の区画へ本文と名を変えずに移す（測る名は全部新しい file に在る） |
| `f102_the_face_name_table_covers_every_document_id` | 面の名の形の表と、天井の床 `ceiling.rs` の文書の id の集合 | `bundle.rs` に残す。新しい file へ移すと、単体の歯の区画から層 1 → 層 2 の上向きの辺が生える（境界の歯は単体の歯の区画も走査する・(d) の変異）。区画の取り込み 1 行だけを、親の丸ごとの取り込みから新しい file の面の名の形の表の取り込みに替える |

新しい file の区画は、単体の歯の印の区画 1 つ（区画名 ceiling_src_tests・親の丸ごとの取り込み 1 行）である。移した 2 本は歯の名の一覧で道だけが変わる（束の区切りの区画から新しい区切りの区画へ・fn の名は同じ）。

**3. 見え方の拡大は 5 か所**（ADR-15 決定 (3) が契約に書けと言う数）。`bundle.rs` に残る命令の口と置き場へ書く関数が、降ろした 観点ごとの束の型 `Built`（型 1 と欄 3）と 4 観点の組み立て `build_all`（1）を読むので、この 5 つを私から道具の中で公開へ広げる。ほかの移した名は、元の file での見え方（公開か私）のまま移る。新しい file は入口の私の区切りなので、公開の印のままで届く範囲は元の file と同じである。起草役は src 全体で、道具の中で公開の印が 123 → 128、公開の印が 367 → 367、再輸出の行が 3 → 3 であることを数えた（参考値）。**再輸出の行も別名の取り込み（名を変えて取り込む形）も 1 行も置かない**（便 106〜109 と同じ）。

**4. 名指しの行だけを書き換える file**。束の側の名を `bundle` 経由で、印の file 名を `stamp` 経由で、閉じた一覧を区間の中で接頭辞なしで名指していた file は、どれも新しい file を直接名指すように取り込みの行と区切り名を書き換える。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `findings.rs` | 2 | 取り込み 1 行（束の区切りから新しい区切りへ・字の順で天井の床の取り込みの次へ）と、読む手・組み直し・要約値・問いの文・結果の型を名指す 10 行と注 2 行の区切り名 |
| `gate.rs` | 2 | 取り込み 2 行を 1 行へ（束の区切りと印の file 名を新しい区切りから）と、読む手と型を名指す 3 行の区切り名 |
| `ceiling.rs` | 2 | 閉じた一覧 8 本の取り込み 1 文（4 行・字の順の位置へ）と頭の注 1 行 |
| `stamp.rs` | 3 | 取り込み 1 行（束の区切りから新しい区切りへ・印の file 名を足す）と、読む手を呼ぶ 1 行の区切り名と頭の注 1 行 |
| `graph.rs` | 3 | 取り込み 1 行（天井の正本の型） |
| `bundle.rs` | 3 | 取り込み（区間だけが使っていた標準の 1 行と区切りの 4 文〔天井の床の閉じた一覧・取り出しの結果の型・sha256・yaml〕を外し、新しい区切りの 4 つの名の 1 行を足す）と頭の注 1 行と単体の歯の区画の取り込み 1 行 |
| `face.rs` | 4 | 取り込み 1 行（新しい区切り・字の順の位置へ）と、天井の名札の注意の文に印の file 名を出す 1 行の区切り名 |

**この 7 file では、表の行のほかは 1 字も動かない。** 入口 `main.rs` は区切りの宣言を 1 行足し（並びは字の順）、配信先が無いときの まだ分からない を組む 1 行の区切り名を書き換える。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の 5 本目で、塞がっている file を開ける便 113〜116 より先に運ぶ。

### (c) 採らなかった形

| 形 | 退けた理由 |
| --- | --- |
| 設計ノート §4-b の測り（ADR-15 の起草役の写しの台本）の形: `bundle.rs` に結果の型の再輸出、`stamp.rs` に印の file 名の再輸出を置き、所見の検査・門・印は新しい区切りを束の名の別名で取り込む | 走査は字面しか見ないので辺の数は同じ 3 に着くが、再輸出の穴が 2 つ増え（設計ノート §5-a の 4）、別名は本当の行き先を読み手から隠す。さらにその形は単体の歯 3 本を `bundle.rs` に残すので、`cargo build --all-targets` で単体の歯の区画が組み立たない（面の名の形の歯が私の関数を呼ぶ・起草役の実測）。§4-b の測りは `cargo build` しか撃たず、単体の歯の区画を見ていなかった |
| 閉じた一覧 8 本を `ceiling.rs` に置いたまま、束の側の区間だけを降ろす | 新しい file（層 1）が天井の床（層 2）を名指し、上向きの辺が 1 本生えて 3 でなく 4 になる（反対側からの確認の b-1-2） |
| 閉じた一覧 8 本の定義を `ceiling.rs` にも残す（写しを 2 つ持つ） | P-6.3 に反する（同じ一覧を人が 2 か所に書く）。値が同じ写しなら、verify も workspace の 779 本も全部緑で、**歯はこれを捉えない**（(d) の変異・(h) の 4） |
| 印の file 名を `stamp.rs` に置いたままにする | 門から印への上向きの辺が残る |
| 命令の口と置き場へ書く関数も新しい file へ降ろす（`bundle.rs` を空にする） | 置き場へ書くのは導出の層の責務で、層 1 の定義（正本の byte を型に直す）に当たらない。設計ノート §4-b の便 110 の行も口と書く関数を `bundle.rs` に残す。見え方の拡大 5 か所は、この割りの代価である |
| 文書の id の集合を測る単体の歯も新しい file へ移す | 単体の歯の区画から新しい file（層 1）が天井の床（層 2）を名指し、上向きの辺が生える（(d) の変異で境界の歯が落ちる） |
| 新しい file を層 2（検査する）に置く | 閉じた一覧と正本を型に直す手は層 1 の定義に当たる（ADR-15 決定 (2)(3)）。層 2 に置いても上向きの辺は生えないが（(d) の変異）、定義に合わない |
| 行を 2 本に割って運ぶ | 便の diff は 66,879 byte（参考値）で器の上限 150,000 byte の下に収まるので、1 本の割りを 1 行で運べる |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。

- `crates/folio/tests/bundle.rs`（19 本）: 束を組む命令の歯。凍結 anchor（`tests/fixtures/ceiling/bundle/` と、要約値を folio の code に依らず `sha256sum` で測り直す手組みの実測・P-10.1 / P-10.2）との一致、写しの byte、決定性と全部か無しか、まだ分からない 6 種、読む手が一覧を床の定数から取ること、最上位の節での絞りを、命令を起動して測る。
- `crates/folio/tests/findings.rs`（39 本）: 所見の検査と反証の束の命令の歯。凍結 fixture の束と所見 file（`tests/fixtures/ceiling/findings/`）から、束を組み直した要約値の突き合わせ・欄の決まり・値域を測る。
- `crates/folio/tests/stamp.rs`（11 本）と `crates/folio/tests/gate.rs`（7 本）: 印と門の命令の歯。印の置き場と、門が印と正本の要約値から 3 値を返すことを測る。
- 単体の歯（bin の中）の `ceiling_floor_derives_the_frozen_anchor_byte_for_byte`（1 本）: 天井の正本の生成区間の床の木が、凍結 anchor `tests/fixtures/schema/ceiling-region.txt`（設計判断の席が独立に組んだ・P-10.1）と byte で一致する。閉じた一覧 8 本は床の木の葉なので、1 つでも字が変われば落ちる。
- 境界の歯 2 本（(e) の 1）。
- 面・束・反証の束・印・門・支度表・索引・部品の検査の出力: 起草役は base と本便の後の 2 つの binary で同じ正本から出力を作り、byte で突き合わせた（(f) の 1）。

起草役の変異の実測（写しの repo・verify の 8 行と、比較のため verify の外の天井の床の命令の歯 `tests/ceiling.rs` と、workspace の nextest を撃って戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 束・3 所見・4 印・5 門・6 凍結 anchor の単体の歯・7 移した要約値の単体の歯・8 clippy）である。変異はどれも base main 0f00d59 の実物に本便を当てた木で撃った。

| 変異 | verify で落ちたもの | 天井の床の命令の歯（verify の外） | workspace |
| --- | --- | --- | --- |
| 反証の結果の値域の字を 1 つ変える | 2（9 本）・3（34 本）・6（凍結 anchor） | 20 本 | 240 本落ちた |
| 束の要約値の規則の名を変える | 2（8 本）・3（34 本）・6 | 20 本 | 240 本落ちた |
| 写しの骨格の語を 1 つ変える | 2（11 本）・3（34 本）・6 | 20 本 | 243 本落ちた |
| 印の file 名を変える | 4（10 本）・5（4 本） | 緑 | 20 本落ちた |
| 面の名の形を 1 つ変える | 2（13 本）・3（39 本）・4（11 本） | 緑 | 64 本落ちた |
| **要約値に要約値の file 自身を混ぜる** | **7 だけ**（1 本） | 緑 | 1 本落ちた（同じ単体の歯） |
| 最上位の節の切り出しで列 0 の `-` を節に数える | 2（1 本） | 緑 | 1 本落ちた |
| 1 回の結果の型の まだ分からない の文言を変える | 2（6 本）・3（2 本） | 緑 | 8 本落ちた |
| 天井の正本が無いときの文言を変える | 2（1 本） | 緑 | 1 本落ちた |
| 層の割り当ての表から新しい区切りの行を消す | 1（層の表の歯） | 緑 | 同じ 1 本 |
| 消した対（門から印）を一覧に戻す | 1（辺の歯・src に無い対） | 緑 | 同じ 1 本 |
| 新しい区切りを層 0 に置く | 1（新しい区切りから層 1 の 2 file への上向き） | 緑 | 同じ 1 本 |
| 新しい区切りを層 2 に置く | **落ちない** | 緑 | **全部緑**（(h) の 5） |
| 新しい区切りを層 3 に置く | 1（層 2 の 3 file から新しい区切りへの上向き） | 緑 | 同じ 1 本 |
| 閉じた一覧 8 本の定義を `ceiling.rs` にも残す（写しを 2 つ持つ・(c) の 3 行目） | **落ちない** | 緑 | **全部緑**（(h) の 4） |
| 文書の id の集合を測る単体の歯を新しい file へ移す（(c) の 6 行目） | 1（単体の歯の区画から層 1 → 層 2 の上向き） | 緑 | 同じ 1 本 |
| 門を印の file の再輸出経由の名指しに戻す（(c) の 1 行目の一部） | 1（一覧に無い対 門と印） | 緑 | 同じ 1 本 |
| 移した要約値の単体の歯を歯でなくする（歯の印を外す） | 7（名に当たる歯が 0 本で nextest が断る） | 緑 | 全部緑（1 本減って 778 本） |

**verify に移した要約値の単体の歯の行（7）を置く理由**: 要約値に要約値の file 自身を混ぜる変化は、束・所見・印・門の命令の歯も凍結 anchor の歯も天井の床の命令の歯も全部緑で、workspace で落ちるのは移した単体の歯 `bundle_digest_concatenates_in_path_byte_order_without_itself` の 1 本だけだった。束を組む命令は要約値を測るときにまだ要約値の file を持たず、所見の検査は測り直す側で同じ関数を呼ぶので、命令の歯には差が出ない。この変化を verify の中で捉えられるのはこの 1 本だけなので、verify に名で置く。本便はこの歯の置き場を移すので、移した先で名に当たる歯が 0 本になれば（歯の印が外れれば）7 が断る（表の最後の行）。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り ceiling_src を層 1 で 1 行足す（48 行 → 49 行） |
| まだ降ろしていない層が上がる辺の一覧 | 所見の検査と門から束への 2 対と、門から印への 1 対を消す（6 対 → 3 対）。**行を足さない** |

残る 3 対は、凍結の様態の型を名指す 3 対（anchor・check・ids から freeze）で、便 111・112 が消す。設計ノートの反対側からの確認（改訂 c の最終確認）が組み立てで再現した列（7 → 6 → 3 → 1 → 0）の 3 と一致する。

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 49 区切りで一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 3 対で一致 |

**2. 単体の歯 2 本の置き場**（(b) の 2）。本文と名は 1 字も変えない。

**歯の本数は base の 779 本のまま**（参考値・本便は歯を足しも消しもしない）。歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）の違いは、移した単体の歯 2 本の道（束の区切りの区画から新しい区切りの区画へ）だけである（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。配信先の組み立て（`folio build --write`・22 file・1,231,263 byte）は `diff -r` で差 0。天井の材料の束（`folio ceiling --write`・観点 4・191 file・6,714,361 byte）は差 0。その束に `--check` を当てた出力（536 byte）は byte 一致（どちらも終了 2 まだ分からない・所見 file が無いため）。持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check`（716 byte）・`--refute`（出力一致・写しの 211 file・7,572,583 byte が `diff -r` で差 0）を当て、設計文書の置き場の写しに同じ周から `--stamp` を当てた（出力一致・書いた印 7,825 byte を含む写しが `diff -r` で差 0）。門 `--gate` は 3 通り（正本を含まない書き込み範囲 2 つと、写しの正本を含み印と正本の要約値を読む 1 つ = 通す・印が 4 観点とも合格で正本の要約値が同じ）で出力が byte 一致。相談窓口の質問（`folio intake --print`・105 byte）と索引（`folio graph --print`・37,394 byte）と導出した一覧の JSON の 1 行（`folio parts --print`・1,400 byte）は byte 一致。部品の検査（`folio parts --check`・組み立ての出力の 5 面に当てた）はどちらも 合格 で出力が byte 一致。 26 周目の写しには 止める の所見が無いので、`--refute` は反証の束を 1 本も組まない（出力は 所見 0）。反証の束を組む道は、所見の検査の歯（`tests/findings.rs`・凍結 fixture）が測る。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致（68 行・7,912 byte）。どれも終了コード 0。天井の正本の生成区間は閉じた一覧 8 本を葉に持つので、本便の移動が一覧の字を 1 つでも動かせば `schema --check` と凍結 anchor の歯（(d)）が落ちる。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **779 本すべて緑**、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。本便では、単体の歯 3 本の置き場の選び（(b) の 2）がこの組み立てでしか決まらない。

### (g) 大きさ

size は **M**（見積 300 行）。本便の新しい file は 660 行（参考値）で、S の見積 100 も M の見積 300 も超える。契約の型（`docs/design/delivery-template.md` の §4）は大きさに S か M を挙げる。器は L（見積 800）も読むが、L では `findings.rs`（余地 404）・`graph.rs`（762）・`face.rs`（513）の 3 本で受付が断る（検証役の実測・参考値）。そこで、S と M のうち**実際の増分に近い大きい方の M** を選ぶ。新しい file は 1,500 行の余地で測られ、本便の後も 840 行の余地が残る。既に在る file の増分はどれも 1 行以下である。本便が新しく書く行（移動でない行）は、新しい file の頭の注 4 行・取り込み 3 行・空行 3 行・単体の歯の区画の開き 2 行と閉じ 1 行、見え方を広げた 5 行、`bundle.rs`・`ceiling.rs`・`stamp.rs` の頭の注 1 行ずつ、取り込みの行と区切り名の行（`findings.rs` 13・`gate.rs` 4・`ceiling.rs` 4・`stamp.rs` 2・`graph.rs` 1・`bundle.rs` 2・`face.rs` 2・`main.rs` 1）、区切りの宣言 1 行と、境界の歯の file の 1 行である。**ほかはすべて字を変えない移動**（閉じた一覧 8 本の 16 行・印の file 名の 2 行・束の側の 589 行・単体の歯 2 本）である。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | M の見積 300 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/bundle.rs` | 730 | 770 | +470 | 109（減る・余地 1,391） |
| `crates/folio/src/ceiling.rs` | 576 | 924 | +624 | 557（減る・余地 943） |
| `crates/folio/src/stamp.rs` | 343 | 1,157 | +857 | 341（減る・余地 1,159） |
| `crates/folio/src/gate.rs` | 252 | 1,248 | +948 | 251（減る・余地 1,249） |
| `crates/folio/src/ceiling_src.rs` | 新設（base に無い） | 1,500 | +1,200 | 660（余地 840） |
| `crates/folio/src/findings.rs` | 1,096 | **404** | +104 | 1,096 |
| `crates/folio/src/face.rs` | 987 | 513 | +213 | 988（余地 512） |
| `crates/folio/src/graph.rs` | 738 | 762 | +462 | 738 |
| `crates/folio/src/main.rs` | 578 | 922 | +622 | 579 |

**いちばん狭いのは `findings.rs` の 404 で、M の見積 300 を上回る。** 本便は `findings.rs` の取り込み 1 行と区切り名しか触らない（正規化行は変わらない）。**`bundle.rs`・`ceiling.rs`・`stamp.rs` の余地は 770・924・1,157 で、どれも M の見積を大きく上回り、どれも本便で減る。**

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 318 | 1,182 | 316（1 行足して 3 行消す） |
| `crates/folio/tests/bundle.rs` | 996 | 504 | 不変 |
| `crates/folio/tests/findings.rs` | 1,296 | 204 | 不変 |
| `crates/folio/tests/stamp.rs` | 696 | 804 | 不変 |
| `crates/folio/tests/gate.rs` | 268 | 1,232 | 不変 |

src 全体は 48 file・26,206 行から 49 file・26,225 行になる（参考値）。

**設計ノート §4-b との差**（参考値）: 移すもの・新しい file・名指しの file は §4-b の便 110 の行と揃う。数と書き込み範囲の違いは 3 点で、どれも理由がある。(1) `bundle.rs` は §4-b の 730 → 135 に対し 730 → 109 で、新しい file は §4-b の 621 行に対し 660 行である。差の大半は単体の歯 2 本とその区画（28 行）を新しい file へ移したことと、頭の注を 4 行にしたこと、§4-b の測りが置いた再輸出の行を置かないことによる。(2) 書き込み範囲に `face.rs` が入る。§4-b の測りは `stamp.rs` に印の file 名の再輸出を置いて `face.rs` の 1 行を書き換えずに済ませたが、本便は再輸出を置かないので、面の入口が印の file 名を新しい区切りから直接名指す。(3) 書き込み範囲に verify の歯の file 3 本（所見・印・門の命令の歯）が入る。どれも本文を変えない。§4-b の測りは `cargo build` しか撃たず、単体の歯の区画を組み立てていなかった（(c) の 1 行目）。

**便の diff の大きさ**は、写しの repo で base からの差を測って **66,879 byte**（参考値・10 file・足す 694 行・消す 677 行）で、器の門が測る上限 150,000 byte の下に収まる。file を割る便は消す側と足す側の両方を持つので diff が倍に育つが、1 本の割りなので 1 行で運べる。

### (h) 本便が運ばないもの・言えないこと・撤退条件

**運ばないもの。**

- 残る 3 対の 層が上がる辺（凍結の様態の型・`folio check` の口の後始末）。便 111・112 が運ぶ。
- `ceiling.rs` に残る閉じた一覧（観点の id の列・文書の id の集合・束の中身・反証の束の中身・結果の欄・反証の規則の文・最上位の節）と床の木。束の読む手が使わない一覧で、層が上がる辺に効かない。これらを層 1 へ降ろすかは本便では決めない（設計ノート §4-e の末尾の段のとおり、本便が降ろす 8 本は 束の組み直しが使うから という理由で選んだ）。
- `findings.rs` の所見の読み・観点の数え・反証の束の組み立てと、`stamp.rs` と `gate.rs` の本体。区切り名の行のほかは動かない。
- 移した注の中の file 名の書き直し。移した注は字を変えずに運び、新しい file の頭の注がその file 名は移す前の置き場を指すと断る。
- 塞がっている 4 file（判断の記録の床・設計ノートの床・入口の面・憲法の面）を開ける割り。便 113〜116 が運ぶ。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指し。本判断の外である。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる。
2. 走査は**字面だけを見る**。再輸出や別名を挟めば辺を一覧に書かずに同じ依存を作れる（今の repo に先例が在る）。本便は再輸出も別名の取り込みも 1 行も足さない。着地の時の確かめは (i) の 3 の見え方の数（再輸出の行 3 → 3）である。
3. 走査は**区間の外から接頭辞なしで取り込んだ名を見ない**。本便の区間では、束の読む手が閉じた一覧 8 本を接頭辞なしで呼んでおり、組み立て（`cargo build --all-targets`）で確かめて 8 本を区間に入れた。
4. **閉じた一覧 8 本の写しを `ceiling.rs` にも残す変化を、どの歯も捉えない。**起草役の変異では、8 本の定義を `ceiling.rs` に戻して床の木がそちらを読むようにしても（値は同じ）、verify の 8 行も workspace の 779 本も全部緑だった（(d) の表）。値が同じなので出力も凍結 anchor も動かない。**着地の時にこれを確かめられるのは取り込みの審査だけである。**審査は (i) の 3 の手順で区間の純度を確かめ、`ceiling.rs` の差分で 8 本の定義が消える側にだけ在り、足す側は取り込み 1 文と頭の注 1 行だけであることを読む。
5. **新しい区切りの層の割り当ての誤りを、境界の歯は一部しか捉えない。**起草役の変異で、新しい区切りを層 1 でなく層 2 に置いても全部緑だった。本便の後に新しい区切りを名指すのは層 2 以上の file だけで、新しい区切り自身が名指すのは層 0 と層 1 だけなので、層 2 に置いても上向きの辺が生えないためである。層 0 に置けば新しい区切りから層 1 への辺が、層 3 に置けば層 2 の file からの辺が上向きになって歯 2 が落ちる。層 1 が正しいことは (c) の 層 2 の行と ADR-15 決定 (2) の定義で示し、審査が境界の歯の file の差分で確かめる。
6. **命令の歯が測らない振る舞いがある。**要約値に要約値の file 自身を混ぜる変化は、移した単体の歯 1 本しか捉えない（(d) の表）。本便はその歯を verify に名で置いた。単体の歯の置き場が変わっても本文と名が変わっていないことは、審査が (i) の 3 の歯の名の一覧（違いは 2 本の道だけ）と区間の純度で確かめる。
7. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、便 111 以降と並行に運べない（直列になる）。
8. **見え方の数と歯の本数を、どの歯も見ない。** 検証役の変異では、公開の型だけを持つ私の関数をもう 1 つ道具の中で公開に広げても（6 か所目）、移した面の名の形の単体の歯を丸ごと消しても（778 本）、移した要約値の単体の歯の本文から要約値の file を混ぜる行を外して要約値に要約値の file 自身を混ぜても、verify の 8 行も workspace も全部緑だった。着地の時の確かめは (i) の 3 の見え方の数（道具の中で公開の印 128）と歯の名の一覧（779 本）と区間の純度である。

**撤退条件**。ADR-15 の撤退条件 (2) を本便に当てると、着地後の 層が上がる辺が直前（便 109 の着地後の 6 本）より**増えていたら**、決定 (5) の順を組み直す。増えたかを捉えるのは、境界の歯の file の差分（凍結した一覧の行の増減）を読む取り込みの審査であり、記帳は台帳 f2-648 に行う。歯は一覧との一致しか見ない。本便の計画値は 3 本（参考値）で、3 本と違ったら（6 本以下でも）計画か実測のどちらかが誤っているので、次の便を出す前に審査が列を見直す。これは ADR より厳しい本便の自主の条件である。どちらの場合も区間の切り出しは戻さない。戻すと 3 本の層が上がる辺が復活し、列の残りの前提が崩れるためである。閉じた一覧 8 本・天井の正本の型と読み手・印の file 名は、層 1 の定義（正本の byte を型に直し、閉じた一覧を持つ）に当たる。束の組み直し（面の写し・問いの文・reads.yaml の文・所見の欄の決まりの文を memory の上で組む手）は、ADR-15 決定 (2) の 導出する（正本から別の型付きの出力を作る）の形を持つ。1 回の実行の結果の型も命令の口の型である。どちらも層 2 の所見の検査が使うので層 3 以上には置けず、設計ノート §4-b の便 110 の行（ADR-15 が承認した列）も層 1 に置く。ただしどちらの層にも属する部分に当たりうるので、ADR-15 の撤退条件 (3) の数えに入れるかは取り込みの審査が判じる（撤退条件 (3) の判定者・裁定は台帳 f2-648 notes）。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7 と同じで、次の 6 つである。起草役の台本（区間を当てる 1 本・走査・行数・変異・出力の比較）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d110-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 48 file・235 辺（下 159・同じ層 70・上 6）、本便の後が 49 file・239 辺（下 166・同じ層 70・上 3）だった（参考値）。消える辺は 8（所見の検査から束・門から束・門から印の上向き 3、印から束・索引から束の同じ層 2、束から取り出しの結果の型・sha256・yaml の下向き 3）、足す辺は 12（束・天井の床・面の入口・所見の検査・門・索引・入口・印から新しい file の下向き 8、新しい file から sha256 と判定の型の下向き 2、新しい file から取り出しの結果の型と yaml の同じ層 2）。
3. **区間の純度と見え方と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（一致は 643 行。移動でない行は (g) の新しく書く行と、取り込みと区切り名の旧い行と、見え方を広げた 5 行の旧い字だけ）。一致には、新しい file の取り込み 4 行（`bundle.rs` から消える同じ字の取り込みと対になる）と、単体の歯の区画の親の丸ごとの取り込み 1 行が含まれる。見え方は src 全体の 道具の中で公開 の印と 公開 の印と再輸出の行の数を base と比べる（123 → 128・367 → 367・3 → 3）。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違いは移した単体の歯 2 本の道だけ）。
4. **出力の byte**: base と本便の後の binary で `folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check`（組み立ての出力の 5 面）を撃ち、`diff -r` と `cmp` で比べる。`--refute` と `--stamp` は書くので、周の置き場と設計文書の置き場の写しに当てる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。

## 2. 範囲

- 入れる: 新しい src の file 1 本（天井の正本の読み手と束の組み直し・所見と束の閉じた一覧 8 本・印の file 名）・`bundle.rs`・`ceiling.rs`・`stamp.rs` からの字を変えない移動・その 3 file の頭の注 1 行ずつ・見え方を広げる 5 か所・単体の歯 2 本の置き場の移動（本文と名は不変）と残す 1 本の区画の取り込み 1 行・入口の区切りの宣言 1 行と区切り名 1 行・名指しの 6 file（`findings.rs`・`gate.rs`・`ceiling.rs`・`stamp.rs`・`graph.rs`・`face.rs`）の取り込みの行と区切り名・境界の歯の file の層の表 1 行と辺の一覧 3 対の削除。
- 入れない: 残り 3 本の層が上がる辺・`ceiling.rs` に残る閉じた一覧と床の木・`findings.rs`・`stamp.rs`・`gate.rs` の本体・命令の口と置き場へ書く関数の置き場・移した注の中の file 名の書き直し・ほかの既に在る歯の名や本数や本文の変更・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯・再輸出の行と別名の取り込み。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| ceiling_src | 新しい src の file | `crates/folio/src/ceiling_src.rs`（責務の層 1 読む）。所見と束の閉じた一覧 8 本を `ceiling.rs` から、印の file 名を `stamp.rs` から、天井の正本の読み手・束の組み直し・要約値・面の名の形・1 回の結果の型を `bundle.rs` から、字を変えずに受ける。単体の歯 2 本を本文と名を変えずに受ける |
| trim | 取り除き | `crates/folio/src/bundle.rs`・`crates/folio/src/ceiling.rs`・`crates/folio/src/stamp.rs` から移すものを外す。3 file とも頭の注を 1 行足し、`bundle.rs` と `ceiling.rs` は新しい区切りの取り込みを足す |
| widen | 見え方 | 観点ごとの束の型（型 1 と欄 3）と 4 観点の組み立て（1）の 5 か所を、私から道具の中で公開へ広げる（`bundle.rs` に残る口と書く関数が読む） |
| rewire | 名指しの行 | 束の側の名を `bundle` 経由で、印の file 名を `stamp` 経由で名指していた file（所見の検査・門・印・索引・面の入口・入口）の取り込みの行と区切り名を新しい区切りへ向け直す |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に ceiling_src を層 1 で 1 行足し、層が上がる辺の凍結した一覧から 3 対を消す（6 対 → 3 対）。歯の本文は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わり、単体の歯 2 本は置き場だけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h) の 4・5）ので、取り込みの審査は区間の純度と見え方の数と歯の名を §1 (i) の 3 の手順で確かめる。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる着地済みの便: 便 109（入口の棚の閉じた一覧・main 0f00d59・台帳 f2-648.154）・便 108（部品目録の閉じた一覧・main 1632cb7・台帳 f2-648.153）・便 107（正本の cursor・main 368a11a・台帳 f2-648.152）・便 106（床の機械・main b8fb27f・台帳 f2-648.151）。
- **書き換える file が重なる並行の便は無い**（起草役が base main 0f00d59 で確かめた）。便 109 との書き込み範囲の重なり（境界の歯の file・`main.rs`・`face.rs`）は、便 109 の着地で**解消**した。
- ADR-15 の列の便 111 以降は、どれも境界の歯の file を書き換えるので**本便の着地を待つ**（設計ノート §5-a の 6・直列になる）。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "di"
title = "天井の正本の読み手と材料の束の組み直し（天井の正本の型 Ceiling・Viewpoint・Rules と読み手 load・観点ごとの束の型 Built と組み手 build_all・build_one・最上位の節の切り出し・正本と面の写し・要約値 digest_text と DIGEST_FILE・面の名の形 FACE_NAMES と FaceName・1 回の結果の型 Outcome）を crates/folio/src/bundle.rs から、所見と束の閉じた一覧 8 本（VERDICT_VALUES・FINDING_REQUIRED・FINDING_OPTIONAL・PLACE_REQUIRED・REFUTE_VALUES・RECORD_REQUIRED・BUNDLE_SKELETON・BUNDLE_DIGEST）を crates/folio/src/ceiling.rs から、印の file 名 STAMP_FILE を crates/folio/src/stamp.rs から、新しい file crates/folio/src/ceiling_src.rs へ字を変えずに責務で降ろす。これは判断の記録 ADR-15 の決定 (2)(3)(5) が定めた 11 本の列の 5 本目で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 110 の行がその中身を持つ。降ろす先は責務の層 1 読む である。所見の検査 findings.rs と門 gate.rs（層 2）が束の file bundle.rs（層 3）を名指し、門が印の file stamp.rs（層 3）を名指していたのが層が上がる辺 3 本の出どころなので、どちらも新しい file を直接名指し、層が上がる辺は 6 本から 3 本へ減る。閉じた一覧 8 本は束の読み手が接頭辞なしで読むので区間に入れる（入れないと新しい file が天井の床を名指して 4 本になる）。見え方の拡大は 5 か所（bundle.rs に残る命令の口と置き場へ書く関数が読む Built の型と欄 3 つと build_all）で、再輸出の行も別名の取り込みも 1 行も置かない。bundle.rs の単体の歯 3 本のうち、降りた名だけを測る 2 本（bundle_face_name_forms_follow_the_site_outputs・bundle_digest_concatenates_in_path_byte_order_without_itself）は本文と名を変えずに新しい file の単体の歯の区画へ移し、天井の床の文書 id の集合を測る 1 本は bundle.rs に残す。findings・gate・stamp・graph・face は取り込みの行と区切り名だけを、ceiling.rs は取り込み 1 文と頭の注 1 行を、bundle.rs と stamp.rs は取り込みと頭の注 1 行を書き換え、入口 main.rs は区切りの宣言 1 行と区切り名 1 行を書き換え、ほかの行は 1 字も動かない。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の型付きデータを書き換え、層の割り当ての表に ceiling_src を層 1 で 1 行足し、まだ降ろしていない層が上がる辺の凍結した一覧から所見の検査と門から束への 2 対と門から印への 1 対を消して 3 対にする（行は足さない・歯の本文は変えない）。歯は足さず消さず本文も変えず、面・束・反証の束・印・門・支度表・索引・部品の検査の出力の byte も生成区間の byte も床の判定も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR17", "FR18", "FR20", "FR19"]
section = "1"
write-set = ["+crates/folio/src/ceiling_src.rs", "-crates/folio/src/bundle.rs", "-crates/folio/src/ceiling.rs", "-crates/folio/src/stamp.rs", "-crates/folio/src/gate.rs", "crates/folio/src/main.rs", "crates/folio/src/findings.rs", "crates/folio/src/graph.rs", "crates/folio/src/face.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/bundle.rs", "crates/folio/tests/findings.rs", "crates/folio/tests/stamp.rs", "crates/folio/tests/gate.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test bundle", "cargo nextest run -p folio --test findings", "cargo nextest run -p folio --test stamp", "cargo nextest run -p folio --test gate", "cargo nextest run -p folio --bin folio ceiling_floor_derives_the_frozen_anchor_byte_for_byte", "cargo nextest run -p folio --bin folio bundle_digest_concatenates_in_path_byte_order_without_itself", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した 3 対とちょうど一致する）、束を組む命令の歯 19 本が緑（凍結 anchor の束の file の一覧と要約値との一致と、まだ分からない 6 種と、最上位の節での絞りを含む・base と同じ本数で名も 1 つ違わない）、所見の検査と反証の束の命令の歯 39 本が緑（束を組み直した要約値の突き合わせと、所見の欄の決まりと値域を含む・base と同じ本数で名も 1 つ違わない）、印の命令の歯 11 本が緑（印の置き場と中身を含む・base と同じ本数で名も 1 つ違わない）、門の命令の歯 7 本が緑（印と正本の要約値からの 3 値を含む・base と同じ本数で名も 1 つ違わない）、bin の単体の歯 ceiling_floor_derives_the_frozen_anchor_byte_for_byte の 1 本が緑（天井の正本の生成区間の床の木が凍結 anchor と byte で一致する＝閉じた一覧 8 本の字が動かない）、bin の単体の歯 bundle_digest_concatenates_in_path_byte_order_without_itself の 1 本が緑（新しい file の区画へ本文と名を変えずに移した後も、要約値の 1 行が束の file を相対 path の byte 順に連結し要約値の file 自身を数えない）、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い）で、workspace の nextest が 779 本すべて緑（base と同じ本数・名の一覧の違いは移した単体の歯 2 本の道だけ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
