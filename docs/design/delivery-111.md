# 設計: 便 111 — 凍結の実行の様態の型（旗・列の結果・旗の後始末）と凍結しないときの 1 行を `freeze.rs` から新しい `phase.rs` へ責務で降ろし、層が上がる辺を 3 本から 1 本へ減らす（参考値・base main 81f5f60）（振る舞い不変・FR5 / NFR3）

- 要件: FR5（検査結果を必ず返す・第 1.31 版）/ NFR3（参照は必ずつながる・第 1.31 版）。凍結の実行の様態の型は `folio check` の旗（`--emit-amends`・`--freeze-anchor`・`--freeze-ids`）と、凍結 anchor の列の検査が残す列の結果と、旗の後始末（標準出力・標準エラーへ書くもの・終了コード）を表す。凍結しないときの 1 行は、違反とまだ分からないの数を出して凍結を断る（FR5 の 実行できなかった検査を合格にしない）。凍結 anchor の列と id の一覧の凍結は、条と要件の id の消失と改番を数える土台である（NFR3 の出所の P-7）。
- 条: P-4.1（検査・生成が実行できなかった結果を異常なしとして扱わない）/ P-6.3（同じ内容を 2 つの面が持つとき、一方を正本とし他方は導出する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (2)(3)(4)(5) と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §3-b の 6 番・§4-a・§4-b の **便 111 の行**。本便は ADR-15 が定めた 11 本の列の 6 本目である。便 110 の後に残る 層が上がる辺 3 本（どれも凍結の書き手 `freeze.rs` を名指す）のうち、凍結 anchor の列の検査 `anchor.rs` と id の消失と改番の検査 `ids.rs` からの 2 本が本便で消える。残る 1 本（`folio check` の口 `check.rs` が後始末の本体を呼ぶ）は便 112 が消す。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `dj` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 7 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 3 本〔凍結の旗の命令の歯・id の凍結の命令の歯・床の case 134 本の歯〕は本文を変えずに write-set に入る）。**起草役が write-set 11 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 110 の着地（main 81f5f60・台帳 f2-648.155）を受けて、写しを実物の main に取り直して全部を測り直し、§0 の前の便・表題の base・§1 (f) の出力の byte の 4 つの値・§1 (h) の 8・§5 の依存を実物の形に直した。main には便 110 のほかに判断の記録 ADR-16 と要件書 第 1.31 版（PR #277）と歯 2 本の是正（PR #278・`tests/adr.rs` と `tests/check.rs`）が入っていたので、要件の版を 第 1.31 版 に直し、正本に依る出力の byte（配信先・天井の束・印・索引）を取り直した。区間を当てた差分は初稿の実測 patch と 1 byte も違わず、区間・write-set・verify・done・size・上向きの辺（3 → 1 と内訳）・辺の計・歯の本数と名（779・差 0）・見え方（拡大 0）・余地の表・src 全体の行数・diff の byte・凍結の旗の 16 通りの一致・門は 1 つも違わなかった。変異は、どの歯も捉えない形と層の置き誤りの 6 本（M2・M4・M7・M8・M11・M13）を実物で撃ち直し、初稿と同じ結果だった。
- 前の便: **便 110 着地済み（main 81f5f60・天井の正本の読み手と束の組み直しを新しい file へ・行 `di`・台帳 f2-648.155）**。便 109 も着地済み（main 0f00d59・行 `dh`・台帳 f2-648.154）。起草役は初めに main 0f00d59 に便 110 の起草役の実測 patch を当てた木で測り、便 110 の着地を受けて写しを 81f5f60 の実物に取り直した。本便の区間を当てた差分は 2 つの木で 1 byte も違わなかった（本便の実測 patch が同じ）。**この契約の数はすべて 81f5f60 の実測（参考値）である。base は main 81f5f60**。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り）

凍結の書き手 `crates/folio/src/freeze.rs`（層 3 導出する・正規化 357 行）が、層の違う 2 つの責務を同居させている。

1. `freeze.rs` は、`folio check --emit-amends` と `--freeze-anchor` の本体（後始末の本体 `after`・差分の行 `emit_lines`・凍結 `freeze`・`--freeze-anchor` 専用の言い換え `not_frozen`・版の並べ方 `ver_key` ほか）のほかに、**凍結の実行の様態の型 3 つ**（`folio check` の旗 `Flag`・凍結 anchor の列の検査が残した列の結果 `State`・旗の後始末 `After`）と、**凍結しないときの 1 行** `not_frozen_by`（違反とまだ分からないの数と旗の綴りから断りの 1 行を組む）を持つ（base の 17〜62 行の 46 行と 152〜159 行の 8 行）。
2. 凍結 anchor の列の検査 `anchor.rs`（層 2 検査する）は、旗が `--freeze-anchor` のとき現行との一致の検査を替え、列の結果を返すため、`Flag` と `State` を `freeze` の名で名指す。**これが層が上がる辺 `anchor` → `freeze`** である。
3. id の消失と改番の検査 `ids.rs`（層 2）は、旗を受け、`--freeze-ids` の後始末を返し、凍結しないときの 1 行を組むため、`Flag`・`After`・`not_frozen_by` を `freeze` の名で名指す（**`ids` → `freeze`**）。
4. `folio check` の口 `check.rs`（層 2）は、`Flag`・`After` のほかに後始末の本体 `freeze::after` を呼ぶ（**`check` → `freeze`**）。後始末の本体は凍結の書き手の本体なので本便では動かさず、この辺は残る（便 112 が口の署名を変えて消す）。
5. 入口 `main.rs` と配信 `site.rs`（どちらも層 5）も `Flag`（入口は `After` も）を `freeze` の名で名指す（下向きの辺で決まりには反していない）。

区間の外に定義がある名の列挙（ADR-15 決定 (5)・設計ノート §5-a の 5）: 型 3 つが使う名は、正本の構文の木の値 `yaml::Value`（層 1）と標準の道 `PathBuf` だけである。凍結しないときの 1 行が使う名は判定の `Report`（層 0 の `verdict.rs`）だけである。どれも区間に接頭辞なしで入るのは型の欄と引数の型だけで、上の層を名指さない。区間を切り出して、組み立て（`cargo build --all-targets`）を 0 警告で通して確かめた。

便 110 の後の 層が上がる辺は **3 本**（参考値）。境界の歯の file の凍結した一覧と一致している。

### (b) 直す先 — 型 3 つと 1 行を新しい `phase.rs`（層 1 読む）へ、字を変えずに移す

**1. 新しい file** `crates/folio/src/phase.rs` へ、`freeze.rs` の 2 つの区間を字を変えずに移す。旗・列の結果・後始末の型 3 つ（説明の注と derive の行を含む 46 行）と、凍結しないときの 1 行（説明の注 1 行と本体 7 行）である。新しい file の頭の注 4 行は、何をどこから降ろしたか・移した注の中の file 名は移す前の置き場を指すこと・誰が読むかを書く。取り込みは 3 行（標準の道・判定の `Report`・`yaml` の `Value`）。新しい file は 65 行（参考値）。

`freeze.rs` に残るのは、`--emit-amends` と `--freeze-anchor` の本体と補助である（303 行）。頭の注に 1 行（型と 1 行を降ろした先）を足し、標準の取り込みを `Path` だけにし、新しい区切りの 4 つの名の取り込み 1 行を足す。`--freeze-anchor` 専用の言い換え `not_frozen`（私）は `freeze.rs` だけが使うので `freeze.rs` に残す。

**2. 単体の歯**。移す区間にも `freeze.rs` にも単体の歯の区画は無い。単体の歯は 1 本も動かない。

**3. 見え方の拡大は 0**（ADR-15 決定 (3) が契約に書けと言う数）。移す型 3 つとその欄は既に公開の印を持ち、凍結しないときの 1 行は既に道具の中で公開である。`freeze.rs` に残る `not_frozen` は私のまま動かさない。起草役は src 全体で、道具の中で公開の印を含む行が 128 → 128、行頭の公開の印が 365 → 365、再輸出の行が 3 → 3 であることを数えた（参考値・数え方は (i) の 3）。**再輸出の行も別名の取り込み（名を変えて取り込む形）も 1 行も置かない**（便 106〜110 と同じ）。

**4. 名指しの行だけを書き換える file**。型 3 つと 1 行を `freeze` 経由で名指していた file は、どれも新しい file を直接名指すように取り込みの行と区切り名を書き換える。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `anchor.rs` | 2 | 取り込み 1 行（旗と列の結果を新しい区切りから・字の順の位置へ） |
| `ids.rs` | 2 | 取り込み 1 行（旗と後始末と新しい区切りそのものを・字の順の位置へ）と、凍結しないときの 1 行を名指す 4 行の区切り名（`freeze` → 新しい区切り） |
| `check.rs` | 2 | 取り込み 1 行を 2 行へ（後始末の本体のため `freeze` の区切りそのものの取り込みを残し、旗と後始末の型は新しい区切りから・字の順の位置へ） |
| `freeze.rs` | 3 | 頭の注 1 行・標準の取り込み 1 行・新しい区切りの取り込み 1 行 |
| `site.rs` | 5 | 取り込み 1 行（旗を新しい区切りから） |
| `main.rs` | 5 | 取り込み 1 行（旗と後始末の型を新しい区切りから）と区切りの宣言 1 行（並びは字の順） |

**この 6 file では、表の行のほかは 1 字も動かない。** 入口 `main.rs` の旗を組む行と後始末を読む行は、型の名を接頭辞なしで使っているので書き換えない。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の 6 本目で、塞がっている file を開ける便 113〜116 より先に運ぶ。残る `check` → `freeze` は、口が後始末を呼ばず材料を返して入口が呼ぶ形へ署名を変える便 112 が消す（呼ぶ順が変わるので、ADR-15 は便 112 だけに 4 つ目の受入を置く）。本便は呼ぶ順を 1 つも変えない。

**層 1 に置く理由と、その限界**。旗は `folio check` の閉じた一覧（4 つの値）で、列の結果は凍結 anchor の列と憲法を型に直した値（現行の写し・索引・最新の anchor・版管理に在ったか）なので、層 1 の定義（正本の byte を型に直し、閉じた一覧を持つ）に当たる。後始末の型と凍結しないときの 1 行は、検査の結果を人と終了コードへ返す形で、正本を型に直す手ではない。層 0（土台）に置くと、列の結果が正本の構文の木の値を欄に持つので土台が層 1 を名指す上向きの辺が生える（(d) の変異で境界の歯が落ちる）。層 2 に置くと上向きの辺は生えないが、層 2 は数える層で、この 4 つは何も数えない。設計ノート §4-a の便 111 の行も 4 つを 1 つの新しい file（層 1）に置く。4 つは同じ呼び手の組（入口・配信・列の検査・id の凍結・口・凍結の書き手）が一緒に読むので、本便は 4 つを割らずに層 1 へ置く。後始末の型と 1 行が層 1 の定義に当たるかを ADR-15 の撤退条件 (3) の数えに入れるかは、取り込みの審査が判じる（(h) の撤退条件）。

### (c) 採らなかった形

| 形 | 退けた理由 |
| --- | --- |
| 設計ノート §4-b の測り（ADR-15 の起草役の写しの台本）の形: `--freeze-anchor` 専用の言い換え `not_frozen` も新しい file へ移し、道具の中で公開へ広げる | 辺の数は同じ 1 に着くが、見え方が 1 か所広がり（設計ノート §4-d が便 111 に数えた 凍結しなかった理由）、`--freeze-anchor` の本体だけが使う言い換えが層 1 に入る。ADR-15 決定 (3) の 元の file には命令の口の本体だけを残す の 本体 は `freeze.rs` 側である。(d) の変異で、この形はどの歯も捉えないことを確かめた |
| `ids.rs` が凍結しないときの 1 行を接頭辞なしで取り込む（台本の形） | 書き換える行の数は同じだが、base の区切り名つきの呼び方（`freeze::not_frozen_by`）から行き先の字だけを替える方が、審査が行の対を読みやすい。どちらも辺の数は同じ |
| 後始末の本体 `after` も新しい file へ降ろす | 後始末の本体は `--emit-amends` と `--freeze-anchor` の本体（差分の行・凍結）と id の凍結 `ids.rs` の本体を呼ぶので、層 1 から層 2・層 3 への上向きの辺が生える。口が後始末を呼ぶ形を変えるのは便 112 の仕事である |
| 型 3 つと 1 行を判定の `verdict.rs`（層 0）へ入れる | 層 0 は文書を何も知らない共通の型と計算（ADR-15 決定 (2)）だが、列の結果は憲法と凍結 anchor の列を型に直した値を欄に持つ。層 0 から `yaml` への上向きの辺も生える |
| 新しい file を層 2（検査する）に置く | 上向きの辺は生えないが（(d) の変異で境界の歯は落ちない）、4 つは何も数えないので層 2 の定義に当たらない |
| `freeze.rs` に再輸出の行を置き、名指しの file を書き換えない | 走査は字面を見るので、`anchor.rs` と `ids.rs` が `freeze` を名指したまま辺が残り、数は 3 のままである。再輸出の穴も増える（設計ノート §5-a の 4）。(d) の変異で、`anchor.rs` だけを再輸出経由に戻すと境界の歯が落ちることを確かめた |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。

- `crates/folio/tests/freeze.rs`（5 本）: `--emit-amends` と `--freeze-anchor` の命令の歯。凍結した土台（`tests/fixtures/floor_base/design-intent/`・憲法 v1.0）の写し全部を一時 dir に作って git init と 1 commit を行い、版を上げた差分の行・変異なしの見出しだけ・同じ版の断り（後始末の断り）・合成した改訂の凍結（後始末の結果の 1 行と索引の追記）・amends を 1 字違えた凍結しない 1 行の全文（違反 1 件・まだ分からない 0 件・旗の綴り）を、命令を起動して測る。
- `crates/folio/tests/ids.rs`（6 本）: id の消失と改番の命令の歯。同じ土台の写しで、消えた id・改番・足した id・anchor が無いときのまだ分からない・`--freeze-ids` が 1 度だけ書く（凍結した木が土台の anchor と byte で一致・2 度目は上書きしないで断る）・実の id の一覧の凍結 anchor の要約値を測る。
- `crates/folio/tests/floor_cases.rs`（1 本）: 凍結 fixture `tests/floor_cases.yaml` の床の case 134 本を、case ごとに凍結した土台（`tests/fixtures/floor_base/design-intent/`）の写しへ当てて folio に撃ち、全部の期待の終了コードと出力を満たすことを数える。case は `--freeze-anchor` と `--emit-amends` を撃つ場面（凍結の断り・凍結しない 1 行・差分の行を別の写しへ貼って凍結し直す）を持つ。
- 境界の歯 2 本（(e) の 1）。
- 凍結の旗の出力: 起草役は base と本便の後の 2 つの binary で、同じ写しに旗を撃って byte で突き合わせた（(f) の 1）。

起草役の変異の実測（写しの repo・verify の 5 行と、比較のため verify の外の `folio check` の命令の歯 `tests/check.rs`（51 本）と凍結 anchor の検査の歯 `tests/anchor.rs`（3 本）と、workspace の nextest を撃って戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 凍結の旗の命令の歯・3 id の凍結の命令の歯・4 床の case・5 clippy）である。

| 変異 | 1 | 2 | 3 | 4 | 5 | 比較 `check` | 比較 `anchor` | workspace |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M1 凍結しない 1 行の文言を変える（凍結しない → 凍結せず） | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 緑 | 落ち 2（凍結の旗の歯 1・床の case 1） |
| M2 凍結しない 1 行で 保留を数えない（まだ分からないの数から保留を外す） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（(h) の 4） |
| M3 凍結しない 1 行の違反とまだ分からないの数を入れ替える | 緑 | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M4 id の凍結の凍結しない 1 行の旗の綴りを変える（名指しを書き換えた 4 行とも） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（(h) の 4） |
| M5 層の割り当ての表から新しい区切りの行を消す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M6 消した対（列の検査から凍結の書き手）を一覧に戻す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M7 新しい区切りを層 2 に置く | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（(h) の 3） |
| M8 新しい区切りを層 0 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M9 新しい区切りを層 3 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M10 列の結果の型の定義を `freeze.rs` にも残す（写しを 2 つ・`freeze.rs` の写しは誰も使わない） | 緑 | 緑 | 緑 | 緑 | **落ち**（使われない定義） | 緑 | 緑 | 緑（779） |
| M11 凍結しない 1 行の定義を `ids.rs` に写し、`ids.rs` が自分の写しを呼ぶ（新しい file の定義は凍結の書き手が使い続ける） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（(h) の 5） |
| M12 列の検査を凍結の書き手の再輸出経由の名指しに戻す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 |
| M13 `--freeze-anchor` 専用の言い換えも新しい file へ移して道具の中で公開へ広げる（§4-b の台本の形） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（(h) の 6） |

verify の 3（id の凍結の命令の歯）は、どの変異でも落ちなかった。それでも verify に置くのは、名指しを書き換える `ids.rs` の `--freeze-ids` の道（旗を受け、凍結の結果の 1 行と上書きしない断りを後始末で返す）を命令で通す歯がこの file だけだからである。M2 と M4 は base から在る穴で、本便の移動では生まれない（本便は 1 行の本文も 4 行の綴りも字を変えない）。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り phase を層 1 で 1 行足す（49 行 → 50 行・並びは層 1 の中の字の順） |
| まだ降ろしていない層が上がる辺の一覧 | 凍結 anchor の列の検査と id の凍結から凍結の書き手への 2 対を消す（3 対 → 1 対）。**行を足さない**。残る 1 対だけの一覧は 1 行の形に書く（rustfmt が 1 行の形を求める。3 行の縦並びのままだと書式の差分が 1 つ増える） |

残る 1 対は、`folio check` の口が後始末の本体を名指す対（`check` から `freeze`）で、便 112 が消す。設計ノートの反対側からの確認（改訂 c の最終確認）が組み立てで再現した列（6 → 3 → 1 → 0）の 1 と一致する。

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 50 区切りで一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 1 対で一致 |

**2. 単体の歯は動かない**（(b) の 2）。

**歯の本数は base の 779 本のまま**（参考値・本便は歯を足しも消しもしない）。歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）は base と 1 字も違わない（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。
   - 凍結の旗: 写しの根（`design-intent/` と器の導出 file `contracts/schema.toml`）を作って git init と 1 commit を行い、4 つの場面（素の写し・憲法の版と P-1 の題を上げた写し・id の一覧の凍結 anchor を消した写し・版を上げずに P-1 の題だけを変えた写し）それぞれに、旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つを撃った。16 通りの標準出力と標準エラーの 32 本が byte で一致し、終了コードも一致し、撃った後の写しの中身（`--freeze-ids` が書いた id の一覧の凍結 anchor を含む）は `diff -r` で差 0 だった。この 16 通りは、後始末の型の 4 つの形（旗なし・差分の行・版の断り・凍結の結果の 1 行）と、凍結しないときの 1 行の 2 つの旗の綴り（`--freeze-anchor` と `--freeze-ids`）を全部通る。
   - ほかの出力: 配信先の組み立て（`folio build --write`・23 file・1,316,521 byte）は `diff -r` で差 0。天井の材料の束（`folio ceiling --write`・観点 4・199 file・7,250,322 byte）は差 0。その束と持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check` を当てた出力（536 byte と 716 byte）・`--refute`（写しの 211 file・7,572,583 byte が差 0）・`--stamp`（印 7,860 byte を含む写しが差 0）・門 `--gate` 3 通りは byte 一致。相談窓口の質問（105 byte）・索引（38,207 byte）・導出した一覧の JSON の 1 行（1,400 byte）・部品の検査（組み立ての出力の 5 面・どちらも 合格）は byte 一致。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致（68 行・7,912 byte）。どれも終了コード 0。このうち床と生成区間の一致は、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃つのは取り込みの審査である。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **779 本すべて緑**、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。`cargo fmt --check` の差分の数は、触る file のどれでも base より増えない（境界の歯の file は 1 つ減る・ほかは同じ）。

### (g) 大きさ

size は **S**（見積 100 行）。**受付が余地を測るのは `+` と印の無い file だけで、行が減る `-` の file（`freeze.rs` と境界の歯の file）は測らない**。測られる src でいちばん狭いのは `anchor.rs` の 496 で、S の見積 100 も M の見積 300 も上回る（参考値）。本便が移動のほかに書く行は 25 行（新しい file の頭の注 4 行・取り込み 3 行・空行 2 行、`freeze.rs` の頭の注 1 行と取り込み 2 行、名指しの行 11 行〔`anchor.rs` 1・`check.rs` 2・`ids.rs` 5・`main.rs` 2・`site.rs` 1〕、境界の歯の file の 2 行）で、新しい file も 65 行なので S とする。**ほかはすべて字を変えない移動**（型 3 つの 46 行・凍結しないときの 1 行の 8 行と、空行 2 行の対）である。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | S の見積 100 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/freeze.rs` | 357 | 1,143 | 受付は測らない（`-`） | 303（減る・余地 1,197） |
| `crates/folio/src/phase.rs` | 新設（base に無い） | 1,500 | +1,400 | 65（余地 1,435） |
| `crates/folio/src/anchor.rs` | 1,004 | **496** | +396 | 1,004 |
| `crates/folio/src/check.rs` | 879 | 621 | +521 | 880（余地 620） |
| `crates/folio/src/ids.rs` | 316 | 1,184 | +1,084 | 316 |
| `crates/folio/src/main.rs` | 579 | 921 | +821 | 580（余地 920） |
| `crates/folio/src/site.rs` | 261 | 1,239 | +1,139 | 261 |

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 316 | 1,184 | 313（1 行足して 5 行を 1 行に・受付は測らない `-`） |
| `crates/folio/tests/freeze.rs` | 309 | 1,191 | 不変 |
| `crates/folio/tests/ids.rs` | 260 | 1,240 | 不変 |
| `crates/folio/tests/floor_cases.rs` | 760 | 740 | 不変 |

src 全体は 49 file・26,225 行から 50 file・26,238 行になる（参考値）。

**設計ノート §4-b との差**（参考値）: 移すもの・新しい file・名指しの file・新しい file の 65 行は §4-b の便 111 の行と揃う。違いは 3 点で、どれも理由がある。(1) `freeze.rs` は §4-b の 357 → 300 に対し 357 → 303 である。§4-b の測りは `--freeze-anchor` 専用の言い換え `not_frozen`（注なし 3 行と空行 1）も移していた。本便はそれを残し（(c) の 1 行目）、頭の注を 1 行足す。(2) §4-d は便 111 で見え方を広げた箇所を 凍結しなかった理由 と数えたが、本便は 0 である（同じく `not_frozen` を残すため）。(3) 書き込み範囲に verify の歯の file 2 本（id の凍結の命令の歯と床の case）が入る。どれも本文を変えない。§4-b の測りは `cargo build` しか撃たず、歯の file を verify に名指す前の測りだった。

**便の diff の大きさ**は、写しの repo で base からの差を測って **11,199 byte**（参考値・8 file・足す 81 行・消す 71 行）で、器の門が測る上限 150,000 byte の下に収まる。

### (h) 本便が運ばないもの・言えないこと・撤退条件

**運ばないもの。**

- 残る 1 対の 層が上がる辺（`folio check` の口が後始末の本体を呼ぶ `check` → `freeze`）。便 112 が口の署名を変えて運ぶ。本便は `check.rs` の取り込みの行のほかは 1 字も動かさず、呼ぶ順も変えない。
- `freeze.rs` の本体（後始末の本体・差分の行・凍結・`--freeze-anchor` 専用の言い換え・版の並べ方ほか）と、`anchor.rs` の列の検査の本体と、`ids.rs` の id の凍結の本体。区切り名の行のほかは動かない。
- 移した注の中の file 名の書き直し。移した注（`FreezeIds` は便 88 の `ids.rs`・列の結果は列の検査が返す、など）は字を変えずに運び、新しい file の頭の注がその file 名は移す前の置き場を指すと断る。
- 塞がっている 4 file（判断の記録の床・設計ノートの床・入口の面・憲法の面）を開ける割り。便 113〜116 が運ぶ。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指し。本判断の外である。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる。
2. 走査は**字面だけを見る**。再輸出や別名を挟めば辺を一覧に書かずに同じ依存を作れる（今の repo に先例が在る）。本便は再輸出も別名の取り込みも 1 行も足さない。凍結の書き手の再輸出経由に戻す形は、名指しが `freeze` のままなので境界の歯が捉える（(d) の変異）。同じ層か下の層の file に再輸出を置く形は捉えない。着地の時の確かめは (i) の 3 の見え方の数（再輸出の行 3 → 3）である。
3. **層の置き誤りのうち、層 2 に置く誤りは歯が捉えない。** 起草役の変異で、新しい区切りを層 2 に置いても全部緑だった（(d) の表）。新しい区切りを名指すのは層 2・層 3・層 5 の file で、新しい区切り自身が名指すのは層 0 と層 1 なので、層 2 では上向きの辺が生えない。層 0 に置く誤りは、新しい区切りが `yaml`（層 1）を名指すので境界の歯が捉える。層 3 以上に置く誤りは、層 2 の 3 file からの上向きの辺として境界の歯が捉える。便 108・便 109 と違い、層 0 の誤りは本便では捉えられる。
4. **凍結しないときの 1 行の中身の一部は、どの歯も測っていない。** 起草役の変異で、まだ分からないの数から保留を外す変化（M2）と、id の凍結の 4 行が渡す旗の綴りを変える変化（M4）は、verify の 5 行も workspace の 779 本も全部緑だった（base から在る穴・保留を起こす場面と `--freeze-ids` が凍結しない場面を歯が持たない）。文言と、違反とまだ分からないの数の並びは、凍結の旗の命令の歯が全文で捉える（M1・M3）。本便は区間を字を変えずに移し、4 行は区切り名の字だけを替えるので振る舞いは変わらないが、その主張を支えるのは歯ではなく、(i) の 3 の区間の純度と (f) の 1 の旗の 16 通りの byte 比較（`--freeze-ids` が凍結しない場面を含む）である。**着地の時にこれを確かめられるのは取り込みの審査だけである。** 穴を塞ぐ歯を足すのは本便の外（後続の候補）。
5. **同じ定義を 2 か所に持つ変化の一部は、どの歯も捉えない。** 凍結しないときの 1 行を `ids.rs` に写して自分の写しを呼ぶ変化（M11）は全部緑だった（条 P-6.3 に反する形）。写しが誰にも使われない場合（M10）は、clippy が使われない定義として捉える。着地の時の確かめは (i) の 3 の区間の純度（足した側に区間の定義が 2 度現れない）である。
6. **見え方の拡大を、どの歯も数えない。** `--freeze-anchor` 専用の言い換えも新しい file へ移して道具の中で公開へ広げる形（M13・§4-b の台本の形）は全部緑だった。本便の見え方の拡大 0 を確かめるのは (i) の 3 の見え方の数である。
7. **凍結の旗の出力の不変は、歯では一部しか測れない。** 凍結の旗の命令の歯と床の case は凍結した土台（`tests/fixtures/` の下）の写しを使い、実の正本の旗の出力は測らない。(f) の 1 の 16 通りは起草役が撃った比較で、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃ち直すのは取り込みの審査である。
8. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、便 112 以降と並行に運べない（直列になる）。

**撤退条件**。ADR-15 の撤退条件 (2) を本便に当てると、着地後の 層が上がる辺が直前（便 110 の着地後の 3 本）より**増えていたら**、決定 (5) の順を組み直す。増えたかを捉えるのは、境界の歯の file の差分（凍結した一覧の行の増減）を読む取り込みの審査であり、記帳は台帳 f2-648 に行う。歯は一覧との一致しか見ない。本便の計画値は 1 本（参考値）で、1 本と違ったら（3 本以下でも）計画か実測のどちらかが誤っているので、次の便を出す前に審査が列を見直す。これは ADR より厳しい本便の自主の条件である。どちらの場合も区間の切り出しは戻さない。戻すと 2 本の層が上がる辺が復活し、列の残りの前提が崩れるためである。ADR-15 の撤退条件 (3)（境界が責務で引けない場所）について: 旗と列の結果は層 1 の定義に当たるが、後始末の型と凍結しないときの 1 行は検査の結果を返す形で、正本を型に直す手ではない（(b) の 層 1 に置く理由と、その限界）。どちらの層にも属する部分に当たりうるので、撤退条件 (3) の数えに入れるかは取り込みの審査が判じる（撤退条件 (3) の判定者）。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7 と同じで、次の 6 つである。起草役の台本（区間を当てる 1 本・走査・行数・変異・出力の比較 2 本）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d111-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 49 file・239 辺（下 166・同じ層 70・上 3）、本便の後が 50 file・243 辺（下 171・同じ層 71・上 1）だった（参考値）。消える辺は 4（列の検査と id の凍結から凍結の書き手への上向き 2、入口と配信から凍結の書き手への下向き 2〔入口は区切りの宣言だけになり、接頭辞つきの名指しが消える〕）、足す辺は 8（列の検査・口・凍結の書き手・id の凍結・入口・配信から新しい file の下向き 6、新しい file から判定の型への下向き 1、新しい file から `yaml` への同じ層 1）。
3. **区間の純度と見え方と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（一致は 56 行＝型 3 つの 46 行と凍結しないときの 1 行の 8 行と、新しい file の空行 2 行が `freeze.rs` から消える空行 2 行と対になったもの）。移動でない行は、足した側が (g) の 25 行、消した側が旧い取り込みと区切り名の 10 行（`anchor.rs` 1・`check.rs` 1・`freeze.rs` 1・`ids.rs` 5・`main.rs` 1・`site.rs` 1）と境界の歯の file の一覧の 5 行である。見え方は src 全体で、道具の中で公開の印（`pub(crate) `）を含む行と、行頭の公開の印（空白の後の `pub `）の行と、再輸出（`pub use` と `pub(crate) use`）の行を base と比べる（128 → 128・365 → 365・3 → 3）。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違い 0）。
4. **出力の byte**: base と本便の後の binary で、(f) の 1 の凍結の旗 16 通りと、`folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check` を撃ち、`diff -r` と `cmp` で比べる。凍結の旗と `--refute` と `--stamp` は書くので、写しに当てる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。

## 2. 範囲

- 入れる: 新しい src の file 1 本（凍結の実行の様態の型 3 つと凍結しないときの 1 行）・`freeze.rs` からの字を変えない移動・`freeze.rs` の頭の注 1 行と取り込み 2 行・名指しの 5 file（`anchor.rs`・`ids.rs`・`check.rs`・`site.rs`・`main.rs`）の取り込みの行と区切り名・入口の区切りの宣言 1 行・境界の歯の file の層の表 1 行と辺の一覧 2 対の削除。
- 入れない: 残り 1 本の層が上がる辺と `folio check` の口の署名・後始末の本体・`freeze.rs` と `anchor.rs` と `ids.rs` の本体・`--freeze-anchor` 専用の言い換えの置き場・見え方の拡大・移した注の中の file 名の書き直し・既に在る歯の名や本数や本文の変更・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯・再輸出の行と別名の取り込み。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| phase | 新しい src の file | `crates/folio/src/phase.rs`（責務の層 1 読む）。`folio check` の旗・列の結果・旗の後始末の型 3 つと、凍結しないときの 1 行を `freeze.rs` から字を変えずに受ける |
| trim | 取り除き | `crates/folio/src/freeze.rs` から移すものを外し、頭の注 1 行を足し、標準の取り込みを 1 つ減らし、新しい区切りの取り込みを足す |
| rewire | 名指しの行 | 型 3 つと 1 行を `freeze` 経由で名指していた file（列の検査・id の凍結・口・配信・入口）の取り込みの行と区切り名を新しい区切りへ向け直す。口は後始末の本体のため `freeze` の取り込みを残す |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に phase を層 1 で 1 行足し、層が上がる辺の凍結した一覧から 2 対を消す（3 対 → 1 対・1 行の形）。歯の本文は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h)）ので、取り込みの審査は区間の純度と見え方の数と歯の名を §1 (i) の 3 の手順で確かめ、床と生成区間の一致を §1 (f) の 2 の命令で撃つ。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる着地済みの便: 便 110（天井の正本の読み手と束の組み直し・main 81f5f60・台帳 f2-648.155）・便 109（入口の棚の閉じた一覧・main 0f00d59・台帳 f2-648.154）・便 108（部品目録の閉じた一覧・main 1632cb7・台帳 f2-648.153）・便 107（正本の cursor・main 368a11a・台帳 f2-648.152）・便 106（床の機械・main b8fb27f・台帳 f2-648.151）。本便と便 110 はどちらも境界の歯の file と `main.rs` を書き換えるが、便 110 は着地済みなので書き込み範囲の重なりは無い。
- ADR-15 の列の便 112 以降は、どれも境界の歯の file を書き換えるので**本便の着地を待つ**（設計ノート §5-a の 6・直列になる）。便 112 は本便が残す `check.rs` の取り込み（後始末の本体のための `freeze` の区切り）と、凍結した一覧の最後の 1 対を消す。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "dj"
title = "凍結の実行の様態の型 3 つ（folio check の旗 Flag・凍結 anchor の列の検査が残した列の結果 State・旗の後始末 After）と凍結しないときの 1 行 not_frozen_by を crates/folio/src/freeze.rs から新しい file crates/folio/src/phase.rs へ字を変えずに責務で降ろす。これは判断の記録 ADR-15 の決定 (2)(3)(5) が定めた 11 本の列の 6 本目で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 111 の行がその中身を持つ。降ろす先は責務の層 1 読む である。凍結 anchor の列の検査 anchor.rs と id の消失と改番の検査 ids.rs（層 2）が型と 1 行のために凍結の書き手 freeze.rs（層 3）を名指していたのが層が上がる辺 2 本の出どころなので、どちらも新しい file を直接名指し、層が上がる辺は 3 本から 1 本へ減る（残る 1 本は folio check の口 check.rs が後始末の本体 after を呼ぶ辺で、便 112 が消す）。--freeze-anchor 専用の言い換え not_frozen は freeze.rs だけが使うので freeze.rs に残し、見え方の拡大は 0 で、再輸出の行も別名の取り込みも 1 行も置かない。anchor・ids・check・site・main は取り込みの行と区切り名だけを、freeze.rs は頭の注 1 行と取り込み 2 行を書き換え、入口 main.rs は区切りの宣言 1 行を足し、ほかの行は 1 字も動かず、後始末を呼ぶ順も変えない。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の型付きデータを書き換え、層の割り当ての表に phase を層 1 で 1 行足し、まだ降ろしていない層が上がる辺の凍結した一覧から列の検査と id の凍結から凍結の書き手への 2 対を消して 1 対にする（行は足さない・歯の本文は変えない）。歯は足さず消さず本文も変えず、単体の歯も動かず、folio check の旗 3 つの標準出力と標準エラーと終了コードと書く file も、面・束・印・門・索引・部品の検査の出力の byte も、生成区間の byte も床の判定も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR5", "NFR3"]
section = "1"
write-set = ["+crates/folio/src/phase.rs", "-crates/folio/src/freeze.rs", "crates/folio/src/anchor.rs", "crates/folio/src/check.rs", "crates/folio/src/ids.rs", "crates/folio/src/main.rs", "crates/folio/src/site.rs", "-crates/folio/tests/modules.rs", "crates/folio/tests/freeze.rs", "crates/folio/tests/ids.rs", "crates/folio/tests/floor_cases.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test freeze", "cargo nextest run -p folio --test ids", "cargo nextest run -p folio --test floor_cases", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "S"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した 1 対とちょうど一致する）、凍結の旗の命令の歯 5 本が緑（--emit-amends の差分の行と見出しだけ・--freeze-anchor の同じ版の断りと合成した改訂の凍結と、amends を 1 字違えたときの凍結しない 1 行の全文を含む・base と同じ本数で名も 1 つ違わない）、id の消失と改番の命令の歯 6 本が緑（--freeze-ids が 1 度だけ書き凍結した木が土台の anchor と byte で一致し 2 度目は上書きしないで断ることを含む・base と同じ本数で名も 1 つ違わない）、床の case の歯 1 本が緑（床の case 134 本の期待の終了コードと出力を全部満たす・凍結の断りと凍結しない 1 行と差分の行を貼って凍結し直す場面を含む）、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い）で、workspace の nextest が 779 本すべて緑（base と同じ本数・名の一覧も base と同じ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
