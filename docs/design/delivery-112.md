# 設計: 便 112 — `folio check` の口が凍結の後始末を呼ばず材料を返し、命令の入口が口を出た直後に 1 度だけ呼ぶ形へ口の署名を変え、層が上がる辺を 1 本から 0 本へ減らす（参考値・base main ab39057）（出力不変・FR5 / NFR3）

- 要件: FR5（検査結果を必ず返す・第 1.31 版）/ NFR3（参照は必ずつながる・第 1.31 版）。`folio check` の口 `check_dir` は正本 7 file の床の判定（違反・まだ分からない・保留）を返す口で、凍結の後始末は旗（`--emit-amends`・`--freeze-anchor`・`--freeze-ids`）の結果（貼れる差分の行・凍結の結果の 1 行・凍結しない 1 行・同じ版の断り）を判定に足して返す（FR5 の 実行できなかった検査を合格にしない）。凍結 anchor の列と id の一覧の凍結は、条と要件の id の消失と改番を数える土台である（NFR3 の出所の P-7）。
- 条: P-4.1（検査・生成が実行できなかった結果を異常なしとして扱わない）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ P-3.3（床の合格を完成として扱わない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (2)(3)(4)(5) と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §3-a の口から凍結の書き手への行・§3-b の 7 番・§4-a・§4-b の **便 112 の行**・§4-c の 4 つ目の受入。本便は ADR-15 が定めた 11 本の列の 7 本目で、層をまたいで使われるものを降ろす 7 本（便 106〜112）の最後である。便 111 の後に残る 層が上がる辺 1 本（`folio check` の口 `check.rs` が凍結の書き手 `freeze.rs` の後始末の本体を呼ぶ）を本便が消し、辺の数が 0 に着く。ADR-15 決定 (5) の末尾は、口の呼ぶ順が変わる 7 本目にだけ 4 つ目の受入（凍結の後始末が同じ条件で同じ回数だけ走る）を置く（設計ノート §8 の b-4 補）。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `dk` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（本便は新しい file を作らず、行が減る file も無いので `+` も `-` も付けない）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 3 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 5 本〔`folio check` の命令の歯・凍結の旗の命令の歯・id の凍結の命令の歯・床の case の歯・配信先の組み立ての歯〕は本文を変えずに write-set に入る）。**起草役が write-set 9 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 111 の着地（main ab39057・台帳 f2-648.156）を受けて、写しを実物の main に取り直して全部を測り直し、表題の base・§0 の前の便・§1 (h) の言えないことの 7・§5 の依存を実物の形に直した。初稿は main 81f5f60 に便 111 の起草役の実測 patch を当てた木で測っていた。実物の main と その木の違いは、`freeze.rs` の頭の注 1 行と phase.rs の頭の注 4 行だけで、本便の区間の file（口・入口・配信・境界の歯）には掛からない。区間の台本は無修正で当たり、区間の差分は初稿の実測と 1 byte も違わない（4,768 byte・sha256 が同じ）。区間・write-set・verify・done・size・上向きの辺（1 → 0 と内訳）・辺の計・歯の本数と名（779・差 0）・見え方・余地の表・src 全体の行数・diff の byte・出力の byte・凍結の旗の 16 通りの一致・凍結の後始末の回数と条件（計器の記録）・門は 1 つも違わなかった。変異は区間に依る 6 本（M1・M2・M8・M10・M13・M16）を実物で撃ち直した（結果は (d) の表と同じ）。
- 前の便: **便 111 着地済み（main ab39057・凍結の実行の様態の型を phase.rs へ・行 `dj`・台帳 f2-648.156）**。便 110 も着地済み（main 81f5f60・行 `di`・台帳 f2-648.155）。起草役は初めに main 81f5f60 に便 111 の起草役の実測 patch を当てた木で測り、便 111 の着地を受けて写しを ab39057 の実物に取り直した。本便の区間を当てた差分は 2 つの木で 1 byte も違わなかった。**この契約の数はすべて ab39057 の実測（参考値）である。base は main ab39057**（規則の表の行 D-13）。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り・ADR-15 決定 (5)）

`folio check` の口 `crates/folio/src/check.rs`（層 2 検査する・正規化 880 行）の `check_dir` は、正本 7 file の床を数えたあと、最後の文で凍結の書き手 `crates/folio/src/freeze.rs`（層 3 導出する）の後始末の本体 `freeze::after` を自分で呼び、判定と後始末の結果の組を返す。設計ノート §3-b の 7 番の形（同居しているのは口の側で、口の本体が下の層に居座っている）である。

1. 口が後始末へ渡すのは 6 つである。検査する dir・旗・凍結 anchor の列の検査が残した列の結果（便 111 で phase.rs へ降りた型）・読めた判断の記録・id の消失と改番の検査が読んだ現行の id・判定の束（後始末が違反を足すので可変で渡す）。列の結果と判断の記録と現行の id は、口の中の局所の変数で、判断の記録が読めなかったときは無い（ADR-15 決定 (3)）。
2. **これが層が上がる辺 `check` → `freeze`（層 2 → 層 3）の唯一の出どころである。** 口が `freeze` を名指すのは後始末の本体を呼ぶ 1 か所と、その取り込みの 1 行だけである。
3. 口を呼ぶのは 2 か所だけである（src と歯の全 file を名指しの字で数えた）。入口 `crates/folio/src/main.rs`（層 5）の `folio check` は 3 つの旗から旗を組んで口を呼び、後始末の結果が同じ版の断りなら断りの 1 行を出して終了コード 1 で終わり、そうでなければ違反の行・まだ分からないの行・判定の 1 行を出してから、貼れる差分の行か凍結の 1 行を出す。判定（3 値と終了コード）は後始末が足した違反を含む。配信 `crates/folio/src/site.rs`（層 5）の `folio build --write` は旗なし（`Flag::None`）で口を呼んで床の判定だけを使い、後始末の結果は捨てる。
4. 後始末の本体は旗で分かれる。旗なしの腕は dir も材料も判定の束も読まずに何もしない形（`After::Nothing`）を返す。`--emit-amends` は差分の行を組み、`--freeze-anchor` は凍結するか凍結しない 1 行を返し（凍結の検査で違反を足すことがある）、`--freeze-ids` は id の凍結を書くか凍結しない 1 行を返す。**旗なし以外の腕は判定の束を変えうるので、判定を出す前に 1 度だけ呼ぶ順が振る舞いの一部である**（(f) の 2）。

区間の外に定義がある名の列挙（ADR-15 決定 (5)・設計ノート §5-a の 5）: 入口へ移す呼び出しが使う名は、後始末の本体（層 3）と、口の引数の dir と旗（入口では局所の変数）と、口の中の 3 つの局所の変数と判定の束である。3 つの局所の変数は新しい材料の型に載せて返すので、その型の欄が名指すのは列の結果の型（層 1 の phase.rs）・判断の記録の型（層 2 の `adr.rs`）・現行の id の型（層 2 の `ids.rs`）で、口はどれも既に名指している（下向きか同じ層）。入口が新しく名指すのは後始末の本体の区切り `freeze`（層 5 → 層 3 の下向き）だけで、入口は区切りの宣言で既に持っている。上の層を名指す名は 1 つも無い。写しの repo に区間を当て、組み立て（`cargo build --all-targets`）を 0 警告で通して確かめた。

便 111 の後の 層が上がる辺は **1 本**（参考値）。境界の歯の file の凍結した一覧と一致している。

### (b) 直す先 — 口は後始末を呼ばず材料を返し、入口が口を出た直後に 1 度だけ呼ぶ（ADR-15 決定 (3)(5)・設計ノート §4-b の便 112 の行）

**1. 口**（`check.rs`）。後始末の材料の型 `Materials` を足す（公開の型 1 つと公開の欄 3 つ・列の結果・判断の記録・現行の id をどれも有るか無いかで持つ・説明の注 2 行と欄の注 3 行）。口の署名の返り値を、判定と後始末の結果の組から、判定と材料の組へ変える。口の最後の文は後始末を呼ばず、3 つの局所の変数を材料に載せて返す。取り込みは `freeze` の区切りの 1 行を消し、phase.rs からの取り込みを旗と後始末の型から旗と列の結果の型へ替える。口の説明の注 1 行（列の結果の渡し先）を書き直す。**口の中の検査の文は 1 字も動かない。**

**2. 入口**（`main.rs`）。口を呼ぶ行を、判定の束を可変で受けて材料を受ける形に替え、**その直後に**後始末の本体を 1 度だけ呼ぶ（dir・旗・材料の 3 つ・判定の束を、口が渡していたのと同じ順と同じ値で渡す）。注の 1 行を添える。後始末の結果を読む行（同じ版の断り・違反と判定の印字・差分の行と凍結の 1 行）と終了コードは 1 字も動かない。入口は後始末の本体を区切り名つきで呼ぶ（区切りの宣言で持っている名なので取り込みの行は足さない）。

**3. 配信**（`site.rs`）。口の結果の組の 2 つ目を受ける束縛を、捨てる形（`_`）に替える（1 行）。**配信は後始末を呼ばない。** 配信は旗なしの定数で口を呼び、後始末の旗なしの腕は何も読まず何も書かないので、呼ぶ行を足しても振る舞いは変わらず、呼ばない行を足す理由も無い（(c) の 2 行目・(f) の 2）。

**4. 境界の歯の型付きデータ**（`tests/modules.rs`）。層の割り当ての表は変えない（本便は新しい区切りを作らない）。まだ降ろしていない層が上がる辺の凍結した一覧から最後の 1 対（口から凍結の書き手）を消し、空の一覧にする（1 行・ADR-15 決定 (4)）。歯の本文は 1 字も変えない。

**5. 見え方**（ADR-15 決定 (3) が契約に書けと言う数）。既に在る項目の見え方の拡大は **0**（私から公開へ変えた項目は無い）。新しく公開になるのは材料の型 1 つとその欄 3 つで、どれも口の中の局所の変数だったものを運ぶ器である。起草役は src 全体で、道具の中で公開の印を含む行が 128 → 128、行頭の公開の印が 365 → 369（足した型と欄の 4 行）、再輸出の行が 3 → 3 であることを数えた（参考値・数え方は (i) の 3）。**再輸出の行も別名の取り込みも 1 行も置かない**（便 106〜111 と同じ）。

**この 4 file では、上の箇条の行のほかは 1 字も動かない。**

**材料の型を口の file（層 2）に置く理由**。欄が名指すのは層 1 と層 2 の型なので、層 2 より下に置くと上向きの辺が生える（phase.rs〔層 1〕に置くと `phase` → `adr`・`phase` → `ids` の 2 本・(d) の変異で境界の歯が落ちる）。凍結の書き手（層 3）に置くと口がそれを名指して `check` → `freeze` が残る（(d) の変異で境界の歯が落ちる）。材料は口の検査が残したものなので、口の file に置くのが責務の線に合う（ADR-15 決定 (1)(3)）。

**呼ぶ順が変わらない理由**（4 つ目の受入・ADR-15 決定 (5)）。base では後始末の呼び出しは口の最後の文で、口を出た直後に入口が結果を読む。本便の後は、口の最後の文が材料を返し、入口の次の文が後始末を呼ぶ。2 つの間に判定の束を読む文も変える文も無く、渡す値は同じ変数を移したものである。したがって `folio check` の 1 回の起動につき、後始末は同じ旗と同じ材料と同じ判定の束で 1 度だけ、判定の印字より前に走る。これを歯と計器で確かめた結果は (d) と (f) の 2 にある。

**順の理由**（ADR-15 決定 (5)）。本便は層をまたいで使われるものを降ろす 7 本の最後で、塞がっている file を開ける便 113〜116 より先に運ぶ。本便の後、上向きの辺は 0 になり、以後は字面の上で上向きの辺を 1 本でも足す変更が境界の歯で落ちる（ADR-15 の帰結）。

### (c) 採らなかった形（ADR-15 決定 (3)(5)）

| 形 | 退けた理由 |
| --- | --- |
| ADR-15 の起草役の写しの台本の形（材料の型の名を 保留 に当たる英語の語にし、口の最後に dir を使ったことにする空の 1 行を置く） | 判定の束が既に同じ語の欄（保留 の一覧）を持つので、同じ file の読み手が 2 つを取り違える。dir は口の中の検査が使い続けるので空の 1 行は要らない。辺の数と振る舞いは同じ |
| 配信も後始末を旗なしで呼ぶ（base の関数の呼び出しの回数を字どおりに保つ） | 旗なしの腕は何も読まず何も書かずに何もしない形を返すだけなので、呼んでも捨てるだけの行になる。配信から凍結の書き手への辺（下向き）も 1 本増える。(d) の変異で、この形はどの歯も base との違いを捉えないことを確かめた（呼んでも呼ばなくても出力は同じ）。4 つ目の受入の読み方は (f) の 2 に書いた |
| 口が後始末を関数の引数として受け取り、口の中の同じ位置で呼ぶ（呼ぶ側を差し替える形） | 呼ぶ位置は base と同じになるが、口の署名が後始末の型の組（列の結果・判断の記録・現行の id・判定の束）を引数の型として持ち、読み手に 2 段の間接が増える。設計ノート §4-b の便 112 の行は 材料を返して入口が呼ぶ 形である |
| 後始末の本体を層 2 へ降ろす | 後始末の本体は差分の行・凍結（層 3 の本体）と id の凍結を呼ぶので、層 2 から層 3 への上向きの辺が本体の側に移るだけで数は減らない。便 111 の (c) と同じ理由 |
| 材料の型を phase.rs（層 1）へ置く | 欄が判断の記録と現行の id（層 2）を名指すので、層 1 から層 2 への上向きの辺が 2 本生える。(d) の変異で境界の歯が落ちる |
| 材料の型を `freeze.rs`（層 3）へ置く | 口がその型を名指すので `check` → `freeze` が残る。(d) の変異で境界の歯が落ちる |
| 材料を型にせず 4 つ組で返す | 欄に名が無く、入口と配信が位置で読む。口の返り値の読みやすさが下がる。辺と振る舞いは同じ |
| 境界の歯の一覧に最後の 1 対を許可として残す・無効化の旗や 今回だけ の口を置く | 条 N-3.1 と ADR-15 決定 (4)（一覧は許可ではない）が拒む。歯 2 は集合の一致を見るので、辺が消えた後に一覧に残すと落ちる（(d) の変異） |

### (d) 歯の土台と変異（P-10.1・P-10.2）

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。

- `crates/folio/tests/check.rs`（51 本）: `folio check` の命令の歯。凍結した土台（`tests/fixtures/floor_base/design-intent/` ほか）の写しと実の正本に命令を撃ち、違反・まだ分からない・判定の 1 行・終了コードを測る。口の判定の束を入口が印字する道を通る。
- `crates/folio/tests/freeze.rs`（5 本）: `--emit-amends` と `--freeze-anchor` の命令の歯。版を上げた差分の行・変異なしの見出しだけ・同じ版の断り・合成した改訂の凍結（凍結の結果の 1 行と索引の追記）・amends を 1 字違えた凍結しない 1 行の全文を測る。後始末の 4 つの形のうち旗なし以外の 3 つを通る。
- `crates/folio/tests/ids.rs`（6 本）: id の消失と改番の命令の歯。`--freeze-ids` が 1 度だけ書く（凍結した木が土台の anchor と byte で一致・2 度目は上書きしないで断る）ことを含む。
- `crates/folio/tests/floor_cases.rs`（`--test` で撃つと 11 本・床の case の歯 1 本と、同じ file に取り込まれた `yaml.rs` と `sha256.rs` の単体の歯 10 本）: 凍結 fixture `tests/floor_cases.yaml` の床の case 134 本を凍結した土台の写しへ当てて folio に撃つ。`--freeze-anchor` と `--emit-amends` を撃つ場面を持つ。
- `crates/folio/tests/site.rs`（12 本）: 配信先の組み立ての歯。`--write` が最初に旗なしで口を呼び、床の 3 値を標準出力の 1 行目に出し、不合格なら 1 file も書かないことを測る。口のもう 1 つの呼び手の道を通る。
- 境界の歯 2 本（(e) の 1）。

起草役の変異の実測（写しの repo・verify の 7 行と workspace の nextest を撃ち、あわせて変異の binary で (f) の 1 の凍結の旗 16 通りを base の binary と byte で比べて戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 `folio check` の命令の歯・3 凍結の旗の命令の歯・4 id の凍結の命令の歯・5 床の case・6 配信先の組み立て・7 clippy）である。

| 変異 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | workspace | 旗 16 通り |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M1 入口が後始末を呼ばない（何もしない形に置き換える） | 緑 | 緑 | **落ち 5** | **落ち 1** | **落ち 1** | 緑 | **落ち**（使われない定義） | 落ち 7 | 差 16 本 |
| M2 入口が後始末を 2 度呼ぶ | 緑 | 緑 | **落ち 2** | **落ち 1** | **落ち 1** | 緑 | 緑 | 落ち 4 | 差 6 本 |
| M3 入口が違反 0 のときだけ後始末を呼ぶ（条件を変える） | 緑 | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 落ち 2 | 差 4 本 |
| M4 入口が後始末へ旗なしを渡す | 緑 | 緑 | **落ち 5** | **落ち 1** | **落ち 1** | 緑 | 緑 | 落ち 7 | 差 16 本 |
| M5 入口が列の結果を渡さない | 緑 | 緑 | **落ち 5** | 緑 | **落ち 1** | 緑 | **落ち**（読まれない欄） | 落ち 6 | 差 12 本 |
| M6 入口が判断の記録を渡さない | 緑 | 緑 | **落ち 3** | 緑 | **落ち 1** | 緑 | **落ち**（読まれない欄） | 落ち 4 | 差 8 本 |
| M7 入口が現行の id を渡さない | 緑 | 緑 | 緑 | **落ち 1** | 緑 | 緑 | **落ち**（読まれない欄） | 落ち 1 | 差 2 本 |
| M8 判定を後始末より先に決める（呼ぶ順の入れ替え・後始末が足す違反が判定に入らない） | 緑 | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 落ち 2 | 差 1 本 |
| M9 違反と判定の行を印字してから後始末を呼ぶ（呼ぶ順の入れ替え） | 緑 | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 落ち 2 | 差 5 本 |
| M10 口も後始末を呼び続ける（入口と合わせて 2 度・口が `freeze` を名指す） | **落ち 1** | 緑 | **落ち 2** | **落ち 1** | **落ち 1** | 緑 | 緑 | 落ち 5 | 差 6 本 |
| M11 消した対（口から凍結の書き手）を一覧に残す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 差 0 |
| M12 口に使わない `freeze` の取り込みを残す（区間の当て損ない） | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち**（使われない取り込み） | 落ち 1 | 差 0 |
| M13 配信も後始末を旗なしで呼ぶ（base の関数の呼び出しの回数へ戻す形） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779・(h) の 1） | 差 0 |
| M14 材料の型を phase.rs（層 1）へ置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 差 0 |
| M15 材料の型を `freeze.rs`（層 3）へ置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 差 0 |
| M16 入口が口の判定の束を捨て、空の判定の束で後始末と印字をする（口の組の受け損ない） | 緑 | **落ち 34** | **落ち 1** | **落ち 3** | **落ち 1** | **落ち 1** | 緑 | 落ち 142 | 差 11 本 |
| M17 配信が口の判定の束を捨て、空の判定の束で床を出す（口の組の受け損ない） | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち 6** | 緑 | 落ち 10 | 差 0 |

旗 16 通りの列は、変異の binary と base の binary で (f) の 1 の凍結の旗の 16 通りを撃ち、標準出力と標準エラーの 32 本のうち byte が違った本数である（参考値）。変異 17 本のうち、verify の 7 行のどれかが落ちなかったのは M13 だけである。呼ぶ順の入れ替え（M8・M9）・回数（M1・M2・M10）・条件（M3・M4）・材料の渡し損ない（M5〜M7）は、凍結の旗の命令の歯と床の case と id の凍結の命令の歯が出力の違いとして捉え、渡し損ないと呼ばない形は clippy も捉える。層の置き誤り（M14・M15）と一覧の残し（M11）と取り込みの残し（M12）は境界の歯が捉える。verify の 2（`folio check` の命令の歯）と 6（配信先の組み立ての歯）は旗を撃たないので後始末の変異は捉えない。2 は口の判定の束を入口が印字する道を実の正本と土台の写しで通す歯で、口の組の受け損ない（M16）でいちばん多く落ちる。6 は口のもう 1 つの呼び手（配信）の道を通る唯一の歯で、M17 は verify の中で 6 だけが落ちる。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない・ADR-15 決定 (4)(5)）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 変えない（50 区切りのまま・本便は区切りを足しも消しもしない） |
| まだ降ろしていない層が上がる辺の一覧 | 口から凍結の書き手への最後の 1 対を消して空にする（1 対 → 0 対）。**行を足さない** |

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 50 区切りで一致（不変） |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 空の集合で一致 |

本便の後、凍結した一覧は空なので、字面の上で層が上がる辺を 1 本でも足す変更はこの歯で落ちる（ADR-15 の帰結）。設計ノート §4-a の列（20 → 13 → 8 → 7 → 6 → 3 → 1 → 0）の 7 本目の 0 と一致する。

**2. 単体の歯は動かない**（口・入口・配信の区間に単体の歯の区画は無い）。

**歯の本数は base の 779 本のまま**（参考値・本便は歯を足しも消しもしない）。歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）は base と 1 字も違わない（起草役の実測）。

### (f) 凍結 anchor・出力が動かないことと、凍結の後始末が同じ条件で同じ回数だけ走ること（4 つ目の受入・ADR-15 決定 (5)・設計ノート §4-c）

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。
   - 凍結の旗: 写しの根（`design-intent/` と器の導出 file `contracts/schema.toml`）を作って git init と 1 commit を行い、4 つの場面（素の写し・憲法の版と P-1 の題を上げた写し・id の一覧の凍結 anchor を消した写し・版を上げずに P-1 の題だけを変えた写し）それぞれに、旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つを撃った。16 通りの標準出力と標準エラーの 32 本が byte で一致し、終了コードも一致し、撃った後の写しの中身（`--freeze-ids` が書いた id の一覧の凍結 anchor を含む）は `diff -r` で差 0 だった。この 16 通りは、後始末の 4 つの形（何もしない・差分の行・版の断り・凍結の結果の 1 行）と、凍結しない 1 行の 2 つの旗の綴りを全部通る。
   - ほかの出力: 配信先の組み立て（`folio build --write`・23 file・1,316,521 byte・口を旗なしで呼ぶ道）は `diff -r` で差 0。天井の材料の束（`folio ceiling --write`・観点 4・199 file・7,250,322 byte）は差 0。その束と持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check` を当てた出力（536 byte と 716 byte）・`--refute`（写しの 211 file・7,572,583 byte が差 0）・`--stamp`（印 7,860 byte を含む写しが差 0）・門 `--gate` 3 通りは byte 一致。相談窓口の質問（105 byte）・索引（38,207 byte）・導出した一覧の JSON の 1 行（1,400 byte）・部品の検査（組み立ての出力の 5 面・どちらも 合格）は byte 一致。写しの外で撃った凍結の旗 3 つと版を上げた写しの旗 4 つも byte 一致・終了コード一致。
2. **凍結の後始末が同じ条件で同じ回数だけ走る**（4 つ目の受入）。起草役は計器を写しにだけ当てて数えた（台本 `d112-draft-probe.py`・repo には入れない）。計器は後始末の本体を同じ署名の包みで囲み、環境変数が在るときだけ、1 回の呼び出しにつき 1 行（副命令・旗・材料 3 つの有無・呼ぶ前と後の判定の束の 3 つの数〔違反・まだ分からない・保留〕・返した形）を file に足す。口の頭にも 1 行（副命令・旗）を足す。計器の記録の関数は判定の `verdict.rs`（層 0）に置き、計器が依存の辺を足さない（計器つきの 2 つの木でも workspace の 779 本が全部緑）。同じ計器を base と本便の後の木に当て、workspace の nextest と (f) の 1 の 2 つの比較を撃って記録を突き合わせた（参考値）。
   - **`folio check`（旗を渡す唯一の副命令）**: workspace で起動 374 回・(f) の 1 の比較で 24 回のどれも、base と本便の後で 口 → 後始末 がこの順に 1 度ずつ走った（1 回の起動の中の並びが両方で同じ）。記録の行の多重集合（旗・材料の有無・呼ぶ前と後の数・返した形）は **1 行も違わない**。旗の内訳は旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つ全部で、後始末が違反を足した回（呼ぶ前と後の数が違う回）も同じ数だけ在る。
   - **`folio build`（配信・旗なしの定数）**: base では口が中で後始末の旗なしの腕に入り（workspace で 29 回・比較で 1 回）、本便の後は入らない（配信は材料を捨てる）。base の旗なしの腕の回はどれも、判定の束の 3 つの数が呼ぶ前と後で同じで、何もしない形を返した。配信の出力は (f) の 1 で byte 一致である。
   - **読み方**: 4 つ目の受入の 走る を、後始末が何かを読むか書くか判定の束を変えうる腕（旗なし以外の腕）に入ることと読む。その腕に入る回数と条件（旗・材料・判定の束）は、`folio check` で base と 1 回も違わず、`folio build` では base も本便の後も 0 回である。字どおりの関数の呼び出しの回数は `folio build` で base の 1 回から 0 回に減る。減るのは何も読まず何も書かない腕だけで、出力の違いは 0 である。**この読み方で 4 つ目の受入を満たすとするかは取り込みの審査が判じる**（(h) の言えないことの 1）。字どおりの回数を保つ形は (c) の 2 行目で退けた。
3. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致（68 行・7,912 byte）。どれも終了コード 0（ADR-15 決定 (5) の受入の 2 つ目と 3 つ目）。このうち床と生成区間の一致と (f) の 2 の計器は、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃つのは取り込みの審査である。
4. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **779 本すべて緑**、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。`cargo fmt --check` の差分の数は、触る file のどれでも base と同じである。

### (g) 大きさ（器の行数の上限 R-C4 と同じ式・ADR-15 決定 (1)）

size は **S**（見積 100 行）。受付は write-set の印の無い file の余地を測る。測られる src でいちばん狭いのは `check.rs` の 620 で、S の見積 100 も M の見積 300 も上回る（参考値）。歯の側でいちばん狭いのは `tests/check.rs` の 323 で、どちらの見積も上回る。本便が書く行は、口に足す 20 行（取り込み 1 行・材料の型の 10 行・空行 1 行・口の説明の注と署名の 2 行・材料を組んで返す 6 行）と消す 13 行（取り込み 2 行・口の説明の注と署名の 2 行・後始末の呼び出しと返り値の 9 行）、入口に足す 10 行と消す 1 行、配信の 1 行、境界の歯の file の 1 行で、差分は足す 32 行・消す 16 行（参考値）なので S とする。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | S の見積 100 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/check.rs` | 880 | **620** | +520 | 887（余地 613） |
| `crates/folio/src/main.rs` | 580 | 920 | +820 | 589（余地 911） |
| `crates/folio/src/site.rs` | 261 | 1,239 | +1,139 | 261 |

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 313 | 1,187 | 313（1 行を書き換えるだけ） |
| `crates/folio/tests/check.rs` | 1,177 | 323 | 不変 |
| `crates/folio/tests/freeze.rs` | 309 | 1,191 | 不変 |
| `crates/folio/tests/ids.rs` | 260 | 1,240 | 不変 |
| `crates/folio/tests/floor_cases.rs` | 760 | 740 | 不変 |
| `crates/folio/tests/site.rs` | 727 | 773 | 不変 |

src 全体は 50 file・26,238 行から 50 file・26,254 行になる（参考値）。

**設計ノート §4-b との差**（参考値）: 口の file の 879 → 887 は、base が便 111 の後なので 880 → 887 になる（便 111 が取り込みの行を 1 つ足した）。書き込み範囲は §4-b の 5 file（口・入口・配信・境界の歯・`folio check` の命令の歯）に、verify の歯の file 4 本（凍結の旗の命令の歯・id の凍結の命令の歯・床の case・配信先の組み立ての歯）が本文を変えずに加わる。§4-b の測りは `cargo build` しか撃たず、歯の file を verify に名指す前の測りだった。§6 の表の 便 112 の後 の下向き 170・同じ層 71 に対し、起草役の走査は下向き 172・同じ層 71 である（下向きの 2 本の違いは、設計ノートの測りの台本と実際に着地した便 106〜111 の区間の違いに由来する。どの便のどの辺かは本便では測っていない。本便の区間は下向きを 1 本足して上向きを 1 本消すだけである）。

**便の diff の大きさ**は、写しの repo で base からの差を測って **4,768 byte**（参考値・4 file・足す 32 行・消す 16 行）で、器の門が測る上限 150,000 byte の下に収まる。

### (h) 本便が運ばないもの・言えないこと・撤退条件（ADR-15 撤退条件 (1)〜(4)）

**運ばないもの。**

- 後始末の本体（`freeze.rs`）・口の中の検査の文・`anchor.rs` と `ids.rs` の本体・phase.rs の型。どれも 1 字も動かない。
- 入口の後始末の結果を読む行と終了コードの決め方。後始末を呼ぶ文を口の中から入口の口を出た直後へ移すだけである。
- 塞がっている 4 file（判断の記録の床・設計ノートの床・入口の面・憲法の面）を開ける割り。便 113〜116 が運ぶ。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指し。本判断の外である（設計ノート §9 の 4 と 6）。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. **4 つ目の受入を、どの歯も直には数えない。** 後始末が走る回数と条件を数えるのは (f) の 2 の計器で、器の verify では撃てない。歯が捉えるのは、後始末を呼ばない・2 度呼ぶ・条件や順を変える・旗や材料を渡し損なう変化のうち、歯の土台の場面で出力が変わるものである（(d) の変異の表では M1〜M10 のどれも verify のどれかが落ちた）。歯の土台に無い場面で出力が変わる変化は歯が捉えない場合がある。旗なしの腕だけの回数の違い（配信が呼ぶか呼ばないか・M13）はどの歯も捉えず、出力にも現れない。**着地の時にこれを確かめられるのは取り込みの審査だけである**（計器の台本と撃ち方は (i) の 4）。4 つ目の受入を歯にしないのは、歯は便 106 で境界の歯 2 本を足すだけで以後は増やさない ADR-15 決定 (5) の形に従うためで、後始末の回数を数える口を src に置くと計器が製品の表面になるためでもある。
2. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる（ADR-15 決定 (4)）。
3. 走査は**字面だけを見る**。再輸出や別名を挟めば辺を一覧に書かずに同じ依存を作れる（今の repo に先例が在る）。本便は再輸出も別名の取り込みも 1 行も足さない。着地の時の確かめは (i) の 3 の見え方の数（再輸出の行 3 → 3）である。
4. **層の置き誤りのうち、同じ層か下の層への誤りは境界の歯が捉えない場合がある。** 本便は新しい区切りを作らないので層の表の置き誤りは起きない。材料の型の置き場の誤り（層 1 の phase.rs・層 3 の `freeze.rs`）は、どちらも層が上がる辺を生むので境界の歯が捉える（(d) の変異）。層 0 か層 2 の別の file へ置く誤りは歯が捉えないことがあり（層 0 は欄が層 1 と層 2 を名指すので捉える・層 2 の別の file は捉えない）、席の審査で見る。
5. **見え方の拡大を、どの歯も数えない。** 本便の見え方の拡大 0（新しく公開になるのは材料の型とその欄だけ）を確かめるのは (i) の 3 の見え方の数である。
6. **輪ごと外す弱め方は clippy が捉える。** 入口が後始末を呼ばなくなると後始末の本体が誰にも使われない定義になり、clippy が落ちる（(d) の変異）。
7. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、便 113 以降は本便の着地を待って直列に運ぶ（設計ノート §5-a の 6）。便 111 は着地済みなので、本便の前には待つ便が無い。

**撤退条件**（ADR-15 の撤退条件の逐語を引く。閾値は ADR-15 の値、比べる相手は直前の便〔便 111〕の着地後の値、判定は取り込みの審査が行い、記帳は台帳 f2-648 に行う）。

- (1) 逐語: 責務で割っても受付の断りが減らない。数えるのは取り込みの審査で、台帳 f2-648 へ記帳する。11 本目の便が着地した後の 10 便で、実装の側の file の余地が足りずに受付で断られた回数を数える。3 回に達したら、決定 (2) の層の数と決定 (3) の降ろし方を捨てて測り直す判断の記録を起こす。歯の側の file が原因の断りはここに数えない（本判断は歯の側の境界を決めていない）。— 本便は 7 本目なので、数えの窓はまだ開いていない。
- (2) 逐語: 上向きの辺が増える。境界の歯が出す上向きの辺の集合を、取り込みの審査が直前の便の一覧と比べる（歯そのものは凍結した一覧との一致しか見ないので、便が辺を増やして一覧も同時に足せば歯は通る。増えたことを捉えるのは、審査が歯の file の差分を読む側である）。11 本の便のどれかの着地後に、上向きの辺が直前の便より増えていたら、決定 (5) の順を捨てて順を組み直す。— 本便の直前の値は便 111 の着地後の 1 本、本便の計画値は 0 本（参考値）。審査は境界の歯の file の差分（凍結した一覧の行の増減）を読み、1 本以上なら（直前と同じ 1 本でも）計画か実測のどちらかが誤っているので、便 113 を出す前に列を見直す。これは ADR より厳しい本便の自主の条件である。
- (3) 逐語: 境界が責務で引けない場所が出る。判定するのは取り込みの審査（届かないときは持ち主）で、便を運ぶ席ではない。1 つの file を責務で割ろうとして、どちらの層にも属する部分が残り、割り方を決められないと審査が判じた便が 2 本に達したら、決定 (2) の層の定義を捨てて測り直す。— 本便は file を割らず、材料の型は口の検査が残したものを運ぶ層 2 の型で、後始末を呼ぶのは層 5 の命令の入口である（(b) の 材料の型を口の file に置く理由）。本便を数えに入れるかは審査が判じ、直前の便までの数えは台帳 f2-648 の記帳が持つ。
- (4) 逐語: 数えられない回が 2 回を超えたら、本判断の撤退条件の判定を「まだ分からない」に落として持ち主へ渡す。「同じ」とみなすのは（撤退条件の番号・数えられなかった理由・対象の便）の 3 つ組が一致する回である。この 2 回は本判断が自分で置く数であり、規則の表の行 R-7 の繰り返し上限とは別である（行 R-7 が言う 3 つ組は検査の id と判定と対象の道で、本判断が数えるものとは母集団が違う）。— 本便で数えられない回になりうるのは、(f) の 2 の計器を審査が撃てなかったときの 4 つ目の受入である。その回は撤退条件 (2) の数え（境界の歯と走査）とは別に記帳する。

どの場合も区間は戻さない。戻すと層が上がる辺が 1 本復活し、列の残り（便 113〜116）の前提が崩れるためである。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7 と同じで、次の 6 つである。起草役の台本（区間を当てる 1 本・走査・行数・計器 3 本・変異・出力の比較 2 本・受付の再現）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d112-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 50 file・243 辺（下 171・同じ層 71・上 1）、本便の後が 50 file・243 辺（下 172・同じ層 71・上 0）だった（参考値）。消える辺は 1（口から凍結の書き手への上向き）、足す辺は 1（入口から凍結の書き手への下向き）。
3. **見え方と歯の名**: src 全体で、道具の中で公開の印（`pub(crate) `）を含む行と、行頭の公開の印（空白の後の `pub `）の行と、再輸出（`pub use` と `pub(crate) use`）の行を base と比べる（128 → 128・365 → 369・3 → 3）。行頭の公開の印の増分が、足した材料の型とその欄の行だけであることを diff で確かめる。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違い 0）。
4. **4 つ目の受入（計器）**: base と本便の後の写しそれぞれに計器の台本を当て（後始末の本体を包み、口の頭に 1 行を足す）、計器の記録の file を環境変数で渡して workspace の nextest と (f) の 1 の 2 つの比較を撃つ。記録を比べる台本は、起動ごとの並び（口と後始末の順と回数）を副命令ごとに数え、起動の番号を外した行の多重集合を副命令ごとに比べる。期待は、`folio check` の並びと多重集合が base と同じで、`folio build` の違いが旗なしの腕の回だけ（判定の束の数が呼ぶ前と後で同じ・何もしない形）であること。
5. **出力の byte**: base と本便の後の binary で、(f) の 1 の凍結の旗 16 通りと、`folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check` を撃ち、`diff -r` と `cmp` で比べる。凍結の旗と `--refute` と `--stamp` は書くので、写しに当てる。生成区間は `folio schema --dir design-intent --check`。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。

## 2. 範囲

- 入れる: 口の file に後始末の材料の型 1 つ（公開の欄 3 つ）・口の署名の返り値の変更（判定と材料の組）・口の最後の文の後始末の呼び出しを材料の組み立てへ替えること・口の取り込み 2 行と説明の注 1 行・入口の口を呼ぶ行の書き換えと、その直後の後始末の呼び出し 1 つと注 1 行・配信の束縛 1 行・境界の歯の file の辺の一覧の最後の 1 対の削除。
- 入れない: 後始末の本体と口の中の検査の文・入口の後始末の結果を読む行と終了コード・`freeze.rs`・phase.rs・`anchor.rs`・`ids.rs` の本体・層の割り当ての表・新しい src の file・見え方の拡大・既に在る歯の名や本数や本文の変更・計器（写しの中だけ）・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯・再輸出の行と別名の取り込み。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| materials | 後始末の材料の型 | `crates/folio/src/check.rs` に足す公開の型 `Materials`。列の結果・判断の記録・現行の id を、有るか無いかで持つ（口の中の局所の変数だったもの） |
| mouth | 口の署名 | `crates/folio/src/check.rs` の `check_dir` の返り値を判定と材料の組にし、後始末を呼ばない。`freeze` の取り込みを消す |
| entry | 入口の呼び出し | `crates/folio/src/main.rs` の `folio check` の道で、口を出た直後に後始末の本体を 1 度だけ、同じ旗と材料と判定の束で呼ぶ |
| site | 配信の束縛 | `crates/folio/src/site.rs` の床の道で、材料を捨てる（後始末は呼ばない） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層が上がる辺の凍結した一覧を空にする。歯の本文と層の表は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h)）ので、取り込みの審査は見え方の数と歯の名を §1 (i) の 3 の手順で確かめ、4 つ目の受入を §1 (i) の 4 の計器で確かめ、床と生成区間の一致を §1 (f) の 3 の命令で撃つ。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる着地済みの便: **便 111（main ab39057・行 `dj`・台帳 f2-648.156・凍結の実行の様態の型を phase.rs へ）**・便 110（main 81f5f60・台帳 f2-648.155）・便 109（main 0f00d59・台帳 f2-648.154）・便 108（main 1632cb7・台帳 f2-648.153）・便 107（main 368a11a・台帳 f2-648.152）・便 106（main b8fb27f・台帳 f2-648.151）。本便の区間は便 111 の後の木（口の取り込みが phase.rs から旗と後始末の型を取る形・境界の歯の一覧が 1 対）に当たる。本便と便 111 はどちらも `check.rs`・`main.rs`・`site.rs`・境界の歯の file を書き換えるが、便 111 は着地済みなので書き込み範囲の重なりは無い。
- ADR-15 の列の便 113 以降は、どれも境界の歯の file を書き換えるので**本便の着地を待つ**。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "dk"
title = "folio check の口 crates/folio/src/check.rs の check_dir が凍結の後始末の本体 freeze::after を自分で呼ぶのをやめ、後始末の材料（凍結 anchor の列の検査が残した列の結果・読めた判断の記録・id の消失と改番の検査が読んだ現行の id）を口の file に足す公開の型 Materials に載せて、判定と材料の組を返すように口の署名を変える。後始末は命令の入口 crates/folio/src/main.rs が口を出た直後に 1 度だけ、口が渡していたのと同じ dir と旗と材料と判定の束で呼ぶ。配信 crates/folio/src/site.rs は旗なしで口を呼んで材料を捨て、後始末を呼ばない（後始末の旗なしの腕は何も読まず何も書かない）。これは判断の記録 ADR-15 の決定 (2)(3)(5) が定めた 11 本の列の 7 本目で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 112 の行がその中身を持つ。口（層 2 検査する）が凍結の書き手 freeze.rs（層 3）を名指していた最後の層が上がる辺が消え、入口（層 5）から凍結の書き手への下向きの辺が 1 本足され、層が上がる辺は 1 本から 0 本になる。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の、まだ降ろしていない層が上がる辺の凍結した一覧から最後の 1 対（口から凍結の書き手）を消して空にする（行は足さない・層の割り当ての表と歯の本文は変えない）。ADR-15 の決定 (5) の 4 つ目の受入として、folio check の 1 回の起動につき後始末は同じ旗・同じ材料・同じ判定の束で判定の印字より前に 1 度だけ走り（base と同じ回数と条件）、folio build では base も本便の後も後始末の旗なし以外の腕に入らない。口の中の検査の文・後始末の本体・入口の後始末の結果を読む行と終了コードの決め方は 1 字も動かず、既に在る項目の見え方の拡大は 0（新しく公開になるのは材料の型とその欄 3 つだけ）で、再輸出の行も別名の取り込みも置かない。歯は足さず消さず本文も変えず、単体の歯も動かず、folio check の旗 3 つの標準出力と標準エラーと終了コードと書く file も、配信先・束・印・門・索引・部品の検査の出力の byte も、生成区間の byte も床の判定も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい file も dir も作らず file も消さない"
req = ["FR5", "NFR3"]
section = "1"
write-set = ["crates/folio/src/check.rs", "crates/folio/src/main.rs", "crates/folio/src/site.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/check.rs", "crates/folio/tests/freeze.rs", "crates/folio/tests/ids.rs", "crates/folio/tests/floor_cases.rs", "crates/folio/tests/site.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test check", "cargo nextest run -p folio --test freeze", "cargo nextest run -p folio --test ids", "cargo nextest run -p folio --test floor_cases", "cargo nextest run -p folio --test site", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "S"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が空の凍結した一覧とちょうど一致する）、folio check の命令の歯 51 本が緑（違反・まだ分からない・判定の 1 行・終了コードを実の正本と凍結した土台の写しで測る・base と同じ本数で名も 1 つ違わない）、凍結の旗の命令の歯 5 本が緑（--emit-amends の差分の行と見出しだけ・--freeze-anchor の同じ版の断りと合成した改訂の凍結と、amends を 1 字違えたときの凍結しない 1 行の全文を含む・base と同じ本数で名も 1 つ違わない）、id の消失と改番の命令の歯 6 本が緑（--freeze-ids が 1 度だけ書き凍結した木が土台の anchor と byte で一致し 2 度目は上書きしないで断ることを含む・base と同じ本数で名も 1 つ違わない）、床の case の file の歯 11 本が緑（床の case 134 本の期待の終了コードと出力を全部満たす 1 本と、同じ file に取り込まれた読み手と要約値の単体の歯 10 本・凍結の断りと凍結しない 1 行と差分の行を貼って凍結し直す場面を含む）、配信先の組み立ての歯 12 本が緑（--write が最初に旗なしで口を呼び床の 3 値を 1 行目に出し不合格なら 1 file も書かないことを含む・base と同じ本数で名も 1 つ違わない）、clippy が 0 警告（後始末の本体が入口から使われ、口に使われない取り込みが無い）で、workspace の nextest が 779 本すべて緑（base と同じ本数・名の一覧も base と同じ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
