# 設計: 便 113 — 判断の記録の床の定数（欄の集合の型・欄の決まりの定数・床の木）を `adr.rs` から新しい floor_adr.rs（層 1 読む）へ字を変えずに責務で降ろし、塞がっている `adr.rs` の余地を 300 以上へ開ける（参考値・base main f9e48c6）（出力不変・FR19 / FR5）

- 要件: FR19（文書の決まりの部分を、機械の中の決まりから各 file の生成区間へ写す・第 1.31 版）/ FR5（検査結果を必ず返す・第 1.31 版）。判断の記録の床の定数は、`folio check` が判断の記録（`design-intent/adr/`）の欄を照らす閾値・値域・欄の集合であり（FR5 の 3 値の判定の材料）、同時に `folio schema` が欄の決まりの file `adr/schema.yaml` の生成区間を導出する正本である（FR19）。
- 条: P-5.1（規則・閾値・型の一覧は型付きデータに置く）/ P-5.6（実装の型付きの定数を規則の正本とするあいだ、その写しを設計文書の置き場へ決定的に導出する）/ P-6.3（同じ内容を 2 つの面が持つとき、一方を正本とし他方は導出する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (1)(2)(3)(4)(5) と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §2 の `adr.rs` の行（余地 110）・§4-a と §4-b の **便 113 の行**・§4-d（便 113 の見え方の拡大）・§4-e（床の定数を 2 本しか降ろさない理由）・§5-a の 4（再輸出を挟んだ名指しは見えない・便 113 自身も再輸出を使う）と 5（区間の外に定義がある名の列挙）。本便は ADR-15 が定めた 11 本の列の 8 本目で、塞がっている 4 file を開ける 4 本（便 113〜116）の 1 本目である。層が上がる辺は便 112 の後に 0 本で、本便は 0 本のまま運ぶ。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `dl` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 5 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 4 本〔判断の記録の命令の歯・欄の決まりの生成区間の命令の歯・床の case の歯・判断の記録の突き合わせの命令の歯〕は本文を変えずに write-set に入る）。**起草役が write-set 10 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 設計ノートからの逸脱: 設計ノート §4-b の便 113 の行は、`adr.rs` に新しい区切りの再輸出を置き、書き込み範囲に `link.rs` を入れない形で測っていた（§5-a の 4 も 便 113 自身も再輸出を使う と書く）。本便は再輸出を置かず、`link.rs` と `schema.rs` が新しい file を直に名指す。理由は、再輸出を挟んだ名指しは走査に見えない（§5-a の 4 が挙げる穴）ので、その穴を避ける向きだからである。§4-b の行は列の見積の材料で、判断の記録 ADR-15 が縛るのは層・上向きの辺の一覧（0 のまま）・便の本数と順・既存の歯を動かさないことである。`link.rs` が書き込み範囲に入るのは名指しの行だけで、設計ノート §4 の原則（ほかの file は名指しの行だけ）の内である。見え方の拡大は §4-d の 25 + 2 でなく 24 + 2 で、拡大が少ない向きである（日付の形 1 本は床の木だけが使うので私のまま）。この逸脱は席の裁定で受けた（取り込みの審査・台帳 f2-648 notes 2026-09-23）。
- 改訂: 改訂 c（2026-09-23・席）= 検証役（~/.local/share/folio2/handoff-2026-09-23/d113-verify.md・判定「直してから」・blocking 0・文面だけ）の所見 6 点を反映 = 撤退条件 (1)(3)(4) の当てはめの二重の文を 1 つに・受付が余地を測る範囲（src の下だけ・歯の側は測らない）・区間の外の名の列挙に撤退条件の種類の型の注の鍵の名を足す・撤退条件 (2) の当てはめの参照先と ADR-15 の逐語の末尾の 1 文・移した注の file 名の断りを実態に。区間・数・write-set・verify・size は変えない。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 112 の着地（main f9e48c6・台帳 f2-648.157）を受けて、写しを実物の main に取り直して全部を測り直し、表題の base・§0 の前の便・§1 (h) の言えないことの 7・§5 の依存を実物の形に直した。あわせて席の裁定 2 つ（台帳 f2-648 notes 2026-09-23）を文面に入れた（§0 の 設計ノートからの逸脱の段・§1 (h) の撤退条件 (3) の見立てと (1)(4) の 1 文・§1 (i) の 3 の 24 + 2 と §4-d の差の理由）。done は件数を参考値とし、本数と名の正本を base の cargo nextest list、case の件数の正本を fixture の expected_cases にした。初稿の木（ab39057 + 便 112 の起草役の実測 patch）と実物の f9e48c6 の違いは、`check.rs` の注 6 行（材料の型とその欄の説明・口の説明）と `main.rs` の注 1 行（後始末を呼ぶ行の説明）だけで、本便の区間の file（判断の記録の床・突き合わせ・生成区間の導出・入口の区切りの宣言・境界の歯の層の表）には掛からない。`main.rs` の注の行は字数が変わったが正規化行は同じ（589）である。区間の台本は無修正で当たり、区間の差分は初稿の実測と本体が 1 byte も違わない（71,203 byte・違いは `main.rs` の index の行の blob の要約値だけ）。区間・write-set・verify・size・上向きの辺（0 のまま）・辺の計（243 → 246 と内訳）・歯の本数と名（779・差 0）・見え方（128 → 154・369・3・0）・余地の表・src 全体の行数・純度（移動 411）・diff の byte・出力の byte・凍結の旗の 16 通りの一致・門は 1 つも違わなかった。変異は区間に依る 7 本（M1・M4・M7・M10・M11・M17・M18）を実物で撃ち直した（結果は (d) の表と同じ）。
- 前の便: **便 112 着地済み（main f9e48c6・`folio check` の口が凍結の後始末を入口へ返し、層が上がる辺を 1 → 0 にした・行 `dk`・台帳 f2-648.157）**。便 111 も着地済み（main ab39057・行 `dj`・台帳 f2-648.156）。起草役は初めに main ab39057 に便 112 の起草役の実測 patch を当てた木で測り、便 112 の着地を受けて写しを f9e48c6 の実物に取り直した。本便の区間を当てた差分は 2 つの木で本体が 1 byte も違わない（改訂 b の行）。便 112 の区間と本便の区間は、同じ file では入口 `main.rs` と境界の歯の file の 2 本で重なるが、行は重ならない（重なり無し）。**この契約の数はすべて f9e48c6 の実測（参考値）である。base は main f9e48c6**（規則の表の行 D-13）。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り・ADR-15 決定 (1)(5)）

判断の記録の床 `crates/folio/src/adr.rs`（層 2 検査する）は、層の違う 2 つの責務を同居させている。

1. `adr.rs` は、判断の記録の欄の決まりの検査（欄の決まりの file の読み・記録ごとの欄の検査・改訂の欄・図の節・承認欄・記録どうしの検査・床の突き合わせ）と、その検査と床の読み口（`floor_strs`・`floor_val`・`floor_num`）のほかに、**判断の記録の床の定数**を持つ。床の定数は、欄の集合の型 `Keys`（必須と任意の 2 欄）・欄の決まりの定数 26 本（id の形・日付の形・裁定 id の形・持ち主・帰結の欄の名・改訂の欄の名・欄の集合 9 本・空にしない欄の一覧・値域 7 本・案の下限と採る数・図の型の一覧の置き場）・床の木 `FLOOR`（欄の決まりの file の schema 節の全部を、説明の注〔`_note` で終わる欄〕を含めて file の順と字面のまま持つ）である（base の 22〜456 行の 435 行）。
2. 正規化で 1,390 行・余地 **110**（参考値）で、器の受付が size M（見積 300）の便を断る 4 file の 1 つである。435 行のうち床の木が説明の注 2 行を含めて 349 行を占める。
3. 床の木は 2 つの読み手が読む。判断の記録の検査（層 2）と、欄の決まりの生成区間を導出する `schema.rs`（層 3・`crate::adr::FLOOR` を名指す）である。判断の記録の突き合わせ `link.rs`（層 2）は改訂の来歴の欄の集合を `adr::AMENDED_BY_ENTRY` の名で名指す。ほかの file（凍結 anchor の列の検査・版管理との照合・id の凍結・凍結の書き手・区間の消し込み）は床の読み口を通して床の木を読み、定数を直には名指さない。

区間の外に定義がある名の列挙（ADR-15 決定 (5) の太字・設計ノート §5-a の 5）: 区間が使う名のうち区間の外に定義があるのは、床の木の型 `Floor`（その 4 つの形 `Map`・`Val`・`Strs`・`Num`）と欄の集合を床の木へ写す keys_floor の書き方（どちらも層 1 の `floor.rs`）と、撤退条件の種類の型（名の列 `RetreatKind::NAMES` と、注の鍵に使う 3 つの形の名・層 1 の `constitution_enums.rs`・組み立て時に憲法から導出した型）の 3 つだけである。どれも層 1 で、上の層を名指さない。区間を写しに切り出し、組み立て（`cargo build --all-targets`）を 0 警告で通して確かめた。逆向き（区間の中に定義があり区間の外が使う名）は、`adr.rs` の検査の本体と単体の歯が 26 名（定数 24 本・`FLOOR`・`Keys`）、`link.rs` が 1 名（`AMENDED_BY_ENTRY`）、`schema.rs` が 1 名（`FLOOR`）で、`crates/folio/tests/` の歯は 1 名も名指さない（歯は命令を撃つ）。

便 112 の後の層が上がる辺は **0 本**（参考値）で、境界の歯の file の凍結した一覧は空である。

### (b) 直す先 — 床の定数を新しい floor_adr.rs（層 1 読む）へ字を変えずに移す（ADR-15 決定 (3)・設計ノート §4-b の便 113 の行）

**1. 新しい file** `crates/folio/src/floor_adr.rs` へ、`adr.rs` の区間（欄の集合の型の説明の注から床の木の閉じまでの 435 行）を字を変えずに移す。新しい file の頭の注 4 行は、何をどこから降ろしたか・検査の本体と読み口は `adr.rs` に残ること・見え方だけを広げたこと・誰が読むかを書く。取り込みは 2 行（撤退条件の種類の型と、床の木の型と keys_floor の書き方）。新しい file は 443 行・正規化 501 行（参考値）。

`adr.rs` に残るのは、判断の記録の欄の決まりの検査の本体と、床の読み口 3 本（とその下の道の辿り）と、欄の決まりの file の名と、単体の歯の区画である。頭の注に 1 行（床の定数を降ろした先・読み口はここに残すこと）を足し、撤退条件の種類の取り込み 1 行を消し、床の木の型の取り込みから keys_floor を外し、新しい区切りの名 26 個の取り込み（rustfmt の形で 6 行）を足す。**再輸出の行は置かない**（下の 3）。

**2. 単体の歯は動かない。** `adr.rs` の単体の歯 5 本（id の形・床の読み口・床の注が突き合わせの外であること・床の木の導出が凍結 anchor と byte 一致・承認者の値域）は `adr.rs` に残り、名も本文も変わらない（歯の名は `adr::tests::` のまま）。歯は親の取り込みを `use super::*` で受けるので、新しい区切りの定数を `adr.rs` の取り込み経由で読む。移す区間に単体の歯の区画は無い。

**3. 見え方の拡大は 26 か所**（ADR-15 決定 (3) が契約に書けと言う数・参考値）。

| 広げたもの | 数 | 理由 |
| --- | ---: | --- |
| 欄の決まりの定数（私 → 道具の中で公開） | 24 | `adr.rs` の検査の本体が読む。日付の形 1 本は床の木だけが使うので私のまま |
| 欄の集合の型の 2 欄（必須と任意・私 → 道具の中で公開） | 2 | `adr.rs` の欄の検査（`check_keys`）が 2 欄を読む |
| 既に道具の中で公開だったもの（欄の集合の型・改訂の来歴の欄の集合・床の木） | 0 | 印を変えない |

行頭の公開の印（`pub `）は 1 つも足さない。**再輸出の行（`pub use` と `pub(crate) use`）も、別名の取り込みも、super:: の名指しも 1 行も足さない。** 起草役は src 全体で、道具の中で公開の印を含む行が 128 → 154（差 26 がちょうど上の表の 26）、行頭の公開の印の行が 369 → 369、再輸出の行が 3 → 3、`use super::*;` のほかの super:: の名指しが 0 → 0 であることを数えた（参考値・数え方は (i) の 3）。設計ノート §4-d は便 113 を 25 + 2 と数えたが、本便は日付の形を私のまま残すので 24 + 2 である。

**4. 名指しの行だけを書き換える file**。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `adr.rs` | 2 | 頭の注 1 行・取り込み 3 か所（撤退条件の種類の 1 行を消す・床の木の型の 1 行から keys_floor を外す・新しい区切りの 26 名の取り込み 6 行を足す） |
| `link.rs` | 2 | 取り込み 1 行（新しい区切りそのものを・字の順の位置へ）と、改訂の来歴の欄の集合を名指す 1 行の区切り名（`adr` → 新しい区切り） |
| `schema.rs` | 3 | 判断の記録の床の木を名指す 1 行の区切り名（`crate::adr::FLOOR` の `adr` → 新しい区切り） |
| `main.rs` | 5 | 区切りの宣言 1 行（並びは字の順・`floor` の次） |

**この 4 file では、表の行のほかは 1 字も動かない。** `link.rs` と `schema.rs` が名指す先を新しい file へ直に向けるのは、再輸出を置かないためである（(c) の 1 行目と 2 行目）。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の後に運ぶ、塞がっている file を開ける 4 本の 1 本目である。先に便 106 が床の木の型を層 1 の `floor.rs` へ降ろしたので、床の定数を層 1 へ降ろしても降ろした先が上の層を名指さない（区間の外の名は (a) の 3 つとも層 1）。

**層 1 に置く理由と、その限界**。床の定数は判断の記録の正本の欄の閉じた一覧（欄の集合）と値域と床の木の値そのもので、ADR-15 決定 (2) の層 1 の定義（正本の byte を型に直し、閉じた一覧と値域と床の木の型を持つ）に当たる。決定 (3) の 使う側の一番下の層 は上限を与える（使う側の一番下は層 2 の `adr.rs` と `link.rs`）。層 2 に置いても上向きの辺は生えない（(d) の変異で境界の歯は落ちない）が、層 2 は数える層で、床の定数は何も数えない。層 0 に置くと、床の木の型と撤退条件の種類（層 1）を名指すので上向きの辺が生える（(d) の変異で境界の歯が落ちる）。設計ノート §4-a の便 113 の行も層 1 に置く。床の読み口 3 本は床の木を道で引いて検査に渡す口で、使う側（層 2 の 6 file と層 3 の凍結の書き手）の一番下は層 2 なので、決定 (3) のとおり `adr.rs`（層 2）に残す（(c) の 3 行目）。本便の置き場は層 1 の定義と決定 (3) の上限で決められるので、起草役は ADR-15 の撤退条件 (3)（割り方を決められない）に当たらないと見立てる。判定は取り込みの審査が行う（(h) の撤退条件）。

### (c) 採らなかった形（ADR-15 決定 (3)(4)）

| 形 | 退けた理由 |
| --- | --- |
| 設計ノート §4-b の測り（ADR-15 の起草役の台本）の形: `adr.rs` に新しい区切りを丸ごと再輸出する 1 行（glob）を置き、`link.rs` と `schema.rs` は `adr` 経由の名指しのまま書き換えない | 辺の数は同じ 0 に着き、書き込み範囲から `link.rs` が外れるが、再輸出の行が 1 つ増え（3 → 4）、新しい区切りの全部の名が `adr` の名でも道具の全体から名指せるようになる。走査は字面を見るので `link.rs` と `schema.rs` の真の行き先（新しい file）が辺に現れない（設計ノート §5-a の 4）。(d) の変異で、この形はどの歯も捉えないことを確かめた。本便は再輸出を置かず、名指す 2 file を新しい file へ直に向ける |
| 改訂の来歴の欄の集合 1 名だけを `adr.rs` から再輸出し、`link.rs` を書き換えない | 同上（再輸出の行が 1 つ増え、`link.rs` から新しい file への辺が字面から消える）。`link.rs` の書き換えは 2 行で、余地 843 に対して小さい |
| 床の読み口 3 本と道の辿りも新しい file へ移す | 読み口は判断の記録の検査と、凍結 anchor の列の検査・版管理との照合・id の凍結・突き合わせ・区間の消し込み（どれも層 2）・凍結の書き手（層 3）が使う。使う側の一番下は層 2 なので、決定 (3) は `adr.rs` に残すことと矛盾しない。移すと名指す 6 file の書き換えか再輸出の行が要る。(d) の変異で、移して `adr.rs` から再輸出する形はどの歯も捉えないことを確かめた（再輸出の行が 3 → 4） |
| 日付の形も道具の中で公開へ広げる（台本の形・区間の私の定数を全部広げる） | 日付の形は床の木だけが使い、`adr.rs` の検査の本体は読まない。広げる理由が無い。(d) の変異で、広げてもどの歯も捉えないことを確かめた（見え方の数だけが 1 増える） |
| 取り込みを新しい区切りの glob（再輸出でない私の取り込み）1 行にする | 行は 5 行減るが、`adr.rs` が新しい区切りのどの名を読むかが取り込みの行から読めなくなる。見え方の数は同じ |
| 床の定数を判定の `verdict.rs`（層 0）へ入れる | 層 0 は文書を何も知らない共通の型と計算（ADR-15 決定 (2)）だが、床の定数は判断の記録の正本の欄の名と値域を持つ。層 0 から層 1 への上向きの辺も生える |
| 新しい file を層 2（検査する）に置く | 上向きの辺は生えないが（(d) の変異で境界の歯は落ちない）、床の定数は何も数えないので層 2 の定義に当たらない |
| 床の定数の残り 5 本（天井・入口・相談窓口・規則の表・正本 7 file）も同じ便で降ろす | 責務の線は床の木 9 本すべてに一様に掛かるが、本判断が運ぶのは余地が 300 未満の 2 本（判断の記録・設計ノート）だけで、残りは後続に置く（設計ノート §4-e・§9 の 5）。どれを先に運ぶかを余地で決めることと、境界を余地で決めることは別である（ADR-15 決定 (1)） |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台と変異（P-10.1・P-10.2）

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。

- `crates/folio/tests/adr.rs`（20 本）: 判断の記録の命令の歯。凍結した土台（`tests/fixtures/adr/` の 3 組と design-intent の写し）に `folio check` を撃ち、案の数・承認欄・図の節・帰結の欄・改訂の欄・欄の決まりの写しのずれの違反を、種類と場所と文言まで測る。床の定数の値（案の下限・値域・欄の集合）が変わると落ちる。
- `crates/folio/tests/schema.rs`（18 本）: 欄の決まりの生成区間の命令の歯。design-intent の写しに `folio schema --check` と `--write` を撃ち、判断の記録の側の実の生成区間が凍結 anchor `tests/fixtures/schema/adr-region.txt` と byte 一致することを測る。床の木の説明の注の 1 字の違いも落ちる。
- `crates/folio/tests/floor_cases.rs`（`--test` で撃つと 11 本・床の case の歯 1 本と、同じ file に取り込まれた `yaml.rs` と `sha256.rs` の単体の歯 10 本）: 凍結 fixture `tests/floor_cases.yaml` の床の case の全部（件数の正本は fixture の expected_cases）を、case ごとに凍結した土台の写しへ当てて folio に撃ち、全部の期待の終了コードと出力を満たすことを数える。判断の記録の欄の決まりを崩す case を持つ。
- `crates/folio/tests/link.rs`（3 本）: 判断の記録の突き合わせの命令の歯。改訂の来歴の欄の集合を読む道（`amended_by` の孤児）と撤退条件の種類の値域の突き合わせを、手書きの最小の土台で測る。名指しを書き換える `link.rs` の道を命令で通す。
- 単体の歯 2 本（`adr.rs` の区画・`--bin folio` で名指す）: 床の木の導出が凍結 anchor `tests/fixtures/schema/adr-region.txt` と byte 一致する歯と、床の読み口が床の木の値と数を道で引ける歯。前者は床の木の中身の 1 字の違いを、後者は読み口と床の木のつながりを捉える。
- 境界の歯 2 本（(e) の 1）。

起草役の変異の実測（写しの repo・verify の 8 行と、比較のため verify の外の `folio check` の命令の歯 `tests/check.rs` と、workspace の nextest を撃ち、変異の binary で実の design-intent に `folio schema --check` と `folio check` を撃って base の binary と byte で比べて戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 判断の記録の命令の歯・3 生成区間の命令の歯・4 床の case・5 突き合わせの命令の歯・6 床の木の導出の単体の歯・7 床の読み口の単体の歯・8 clippy）である。

| 変異 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 比較 `check` | workspace | 出力（生成区間・床） | 見え方（公開の印の行・行頭 pub・再輸出・super::） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M1 床の定数の値を 1 つ変える（案の下限 2 → 3） | 緑 | **落ち 20** | **落ち 9** | **落ち 1** | **落ち 3** | **落ち 1** | **落ち 1** | 緑 | **落ち 40** | 落ち 213 | 差・差 | 154・369・3・0 |
| M2 床の説明の注の 1 字を変える | 緑 | 緑 | **落ち 7** | 緑 | 緑 | **落ち 1** | 緑 | 緑 | 緑 | 落ち 29 | 差・一致 | 154・369・3・0 |
| M3 床の木から 1 欄（id_pattern）を落とす | 緑 | **落ち 20** | **落ち 9** | **落ち 1** | **落ち 3** | **落ち 1** | 緑 | 緑 | **落ち 40** | 落ち 212 | 差・差 | 154・369・3・0 |
| M4 同じ定義を 2 か所（adr.rs に STATUS の写しを置いて自分の写しを読む） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 154・369・3・0 |
| M5 同じ定義を 2 か所（adr.rs に DATE_FORMAT の写し・誰も使わない） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち**（使われない定義） | 緑 | **全部緑**（779） | 一致・一致 | 154・369・3・0 |
| M6 新しい区切りを層 0 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 154・369・3・0 |
| M7 新しい区切りを層 2 に置く | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 154・369・3・0 |
| M8 新しい区切りを層 3 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 154・369・3・0 |
| M9 層の表から新しい区切りの行を消す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 154・369・3・0 |
| M10 層 3 に置き、名指す側 2 file を super:: の名指しに替える | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 154・369・3・2 |
| M11 glob の再輸出 1 行（ADR-15 の起草役の台本の形・link と schema は adr 経由のまま） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 155・369・4・0 |
| M12 1 名の再輸出（AMENDED_BY_ENTRY）・link は adr 経由のまま | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 155・369・4・0 |
| M13 FLOOR だけが使う DATE_FORMAT も道具の中で公開へ | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 155・369・3・0 |
| M14 FLOOR を pub へ（道具の外へ向けた印） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 153・370・3・0 |
| M15 新しい区切りが上の層の名を再輸出する（adr の check_keys） | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち**（使われない取り込み） | 緑 | 落ち 1 | 一致・一致 | 155・369・4・0 |
| M16 凍結した一覧に在りもしない対を足す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 154・369・3・0 |
| M17 図の節の検査を輪ごと外す（check_figures の呼び出しを消す） | 緑 | **落ち 4** | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち**（使われない定義） | 緑 | 落ち 4 | 一致・一致 | 154・369・3・0 |
| M18 床の読み口 3 本と floor_at も新しい file へ移し adr.rs が再輸出する（床の木の型の取り込みは単体の歯の区画の組み立てだけに限る） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑**（779） | 一致・一致 | 155・369・4・0 |

出力の列は、変異の binary と base の binary で実の design-intent に `folio schema --check` と `folio check` を撃ち、標準出力と標準エラーと終了コードを比べた結果である。見え方の列は src 全体の 4 つの数（本便の後は 154・369・3・0）である（数え方は (i) の 3・参考値）。変異 18 本のうち、verify の 8 行のどれかが落ちなかったのは 8 本（M4・M7・M10・M11・M12・M13・M14・M18）である。

- **床の定数の中身の変化**（M1〜M3）は、判断の記録の命令の歯・生成区間の命令の歯・床の case・突き合わせの命令の歯・2 本の単体の歯のどれかが捉える。説明の注の 1 字（M2）は床の判定を変えないが、生成区間の命令の歯と床の木の導出の単体の歯が凍結 anchor との byte の違いとして捉える。
- **層の置き誤りと一覧の書き換え**（M6・M8・M9・M16）と、下の層の file が上の層の名を再輸出する形（M15）は、境界の歯が捉える。層 2 への置き誤り（M7）はどの歯も捉えない（(h) の 3）。
- **再輸出と見え方の広げ過ぎと super:: の名指し**（M10〜M14・M18）は、どの歯も捉えない。見え方の数がどれも本便の後の値（154・369・3・0）から動くので、(i) の 3 の数が捉える（(h) の 2 と 5）。
- **同じ定義を 2 か所に持つ形**のうち、写しを読む形（M4）はどの歯も見え方の数も捉えない（(h) の 4・区間の純度が捉える）。誰も使わない写し（M5）は clippy が捉える。
- **輪ごと外す弱め方**（M17）は、clippy（使われない定義）と判断の記録の命令の歯（図の節の場面）が捉える。

verify の 5（突き合わせの命令の歯）は、床の定数の中身の変化（M1・M3）だけで落ちた。それでも verify に置くのは、名指しを書き換える `link.rs` の道（改訂の来歴の欄の集合を新しい file から読む）を命令で通す歯がこの file だけだからである。M4 は区間の移動では生まれない形で、本便は定義を写さない。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。床の定数の中身の側には独立した凍結 anchor が在る（判断の記録の生成区間の anchor `tests/fixtures/schema/adr-region.txt` と床の case の fixture）。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない・ADR-15 決定 (4)(5)）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り floor_adr を層 1 で 1 行足す（50 行 → 51 行・並びは層 1 の中の字の順・`floor` の次） |
| まだ降ろしていない層が上がる辺の一覧 | 変えない（空のまま）。**行を足さない** |

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 51 区切りで一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 空の集合で一致（不変） |

**2. 単体の歯は動かない**（(b) の 2）。

**歯の本数は base の 779 本のまま**（参考値・本便は歯を足しも消しもしない）。歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）は base と 1 字も違わない（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと（ADR-15 決定 (5) の受入）

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下と `tests/floor_cases.yaml`）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。
   - 床の定数を読む出力: 欄の決まりの生成区間（`folio schema --dir design-intent --check`・9 file とも 一致・判断の記録の側 `adr/schema.yaml` は 24,202 byte・標準出力 464 byte）と床の判定（`folio check --dir design-intent`・合格・60 byte）は byte 一致・終了コード一致。
   - ほかの出力: 配信先の組み立て（`folio build --write`・23 file・1,316,521 byte）は `diff -r` で差 0。天井の材料の束（`folio ceiling --write`・観点 4・199 file・7,250,322 byte）は差 0。その束と持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check` を当てた出力（536 byte と 716 byte）・`--refute`（写しの 211 file・7,572,583 byte が差 0）・`--stamp`（印 7,860 byte を含む写しが差 0）・門 `--gate` 3 通りは byte 一致。相談窓口の質問（105 byte）・索引（38,207 byte）・導出した一覧の JSON の 1 行（1,400 byte）・部品の検査（組み立ての出力の 5 面・どちらも 合格）・`folio inject --check`（68 行・7,912 byte）は byte 一致。写しの外で撃った凍結の旗 3 つと版を上げた写しの旗 4 つ（判断の記録の床の定数を読む `--emit-amends` と `--freeze-anchor` を含む）も byte 一致・終了コード一致。
   - 凍結の旗: 写しの根（`design-intent/` と器の導出 file `contracts/schema.toml`）を作って git init と 1 commit を行い、4 つの場面（素の写し・憲法の版と P-1 の題を上げた写し・id の一覧の凍結 anchor を消した写し・版を上げずに P-1 の題だけを変えた写し）それぞれに、旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つを撃った。16 通りの標準出力と標準エラーの 32 本が byte で一致し、終了コードも一致し、撃った後の写しの中身は `diff -r` で差 0 だった。凍結の書き手と凍結 anchor の列の検査は床の読み口を通して床の木（凍結 anchor の欄の集合・写しの取り方・承認欄の欄）を読むので、この 16 通りは読み口から新しい file の床の木へのつながりを通る。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致。どれも終了コード 0（ADR-15 決定 (5) の受入の 2 つ目と 3 つ目）。このうち床と生成区間の一致は、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃つのは取り込みの審査である。verify の 3 と 6 は、判断の記録の側の生成区間が凍結 anchor と byte 一致することを歯として数える（実の design-intent の写しと床の木の導出の 2 つの道）。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **779 本すべて緑**、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。`cargo fmt --check`（edition 2024）の差分の数は、触る file のどれでも base と同じである（新しい file は 0）。

### (g) 大きさ（器の行数の上限 R-C4 と同じ式・ADR-15 決定 (1)）

size は **M**（見積 300 行）。本便の新しい file は正規化 501 行（参考値）で、S の見積 100 も M の見積 300 も超える。契約の型（`docs/design/delivery-template.md` の §4）は大きさに S か M を挙げるので、実際の増分に近い大きい方の M を選ぶ（便 110 と同じ）。

**受付が余地を測るのは `crates/folio/src/` の下の `+` と印の無い file だけで、行が減る `-` の file と歯の側の file は測らない。** `adr.rs` は本便で 1,390 → 902 行に減るので `-` を付ける。`adr.rs` の base の余地 110 は M の見積 300 を下回るので、`-` を付けずに write-set に入れると受付が余地の不足で断る。本便は `adr.rs` を開ける便なので、`-` の印が本便の受付の前提である。境界の歯の file は 1 行増えるので印を付けない。歯の側の file なので受付は余地を測らない（余地 1,187・参考値）。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | M の見積 300 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/adr.rs` | 1,390 | **110** | 受付は測らない（`-`） | 902（減る・余地 **598**） |
| floor_adr.rs（新設・`crates/folio/src/` の下） | base に無い | 1,500 | +1,200 | 501（余地 999） |
| `crates/folio/src/link.rs` | 657 | 843 | +543 | 658（余地 842） |
| `crates/folio/src/schema.rs` | 156 | 1,344 | +1,044 | 156 |
| `crates/folio/src/main.rs` | 589 | 911 | +611 | 590（余地 910） |

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 313 | 1,187 | 314（1 行足す・印なし・受付は測らない） |
| `crates/folio/tests/adr.rs` | 517 | 983 | 不変 |
| `crates/folio/tests/schema.rs` | 619 | 881 | 不変 |
| `crates/folio/tests/floor_cases.rs` | 760 | 740 | 不変 |
| `crates/folio/tests/link.rs` | 92 | 1,408 | 不変 |

本便が移動のほかに書く行は 45 行（参考値）である。新しい file の頭の注 4 行・空行 1 行・床の木の型の取り込み 1 行・見え方を広げた 26 行（区間の行の頭に印を足しただけで、ほかの字は同じ）、`adr.rs` の頭の注 1 行と取り込み 7 行、`link.rs` の 2 行、`schema.rs` の 1 行、`main.rs` の 1 行、境界の歯の file の 1 行である。消す行は 29 行で、見え方を広げる前の 26 行と、`adr.rs` の床の木の型の取り込み 1 行、`link.rs` と `schema.rs` の区切り名の 1 行ずつである。**ほかはすべて字を変えない移動**（区間の 409 行・撤退条件の種類の取り込み 1 行・空行 1 行の 411 行）である。

src 全体は 50 file・26,254 行から 51 file・26,269 行になり、余地 300 未満の src は 4 本から 3 本になる（参考値）。

**設計ノート §4-b との差**（参考値）: 移すもの・新しい file・層は §4-b の便 113 の行と揃う。違いは 4 点で、どれも理由がある。(1) `adr.rs` は §4-b の 1,390 → 897 に対し 1,390 → 902 である。§4-b の測りは glob の再輸出 1 行だったが、本便は再輸出を置かず 26 名の取り込み（6 行）と頭の注 1 行を足す。(2) 新しい file は §4-b の 498 に対し 501 である（頭の注の行数の違い）。(3) 見え方の拡大は §4-d の 25 + 2 に対し 24 + 2 である（日付の形を私のまま残す）。(4) 書き込み範囲に `link.rs`（再輸出を置かないため）と verify の歯の file 3 本（生成区間・床の case・突き合わせ）が入る。§4-b の測りは `cargo build` しか撃たず、歯の file を verify に名指す前の測りだった。

**便の diff の大きさ**は、写しの repo で base からの差を測って **71,203 byte**（参考値・6 file・足す 456 行・消す 440 行）で、器の門が測る上限 150,000 byte の下に収まる。

### (h) 本便が運ばないもの・言えないこと・撤退条件（ADR-15 撤退条件 (1)〜(4)）

**運ばないもの。**

- 判断の記録の欄の決まりの検査の本体と、床の読み口 3 本と道の辿り（`adr.rs` に残る）。区間の外の行は、頭の注 1 行と取り込みのほかは 1 字も動かない。
- `link.rs` と `schema.rs` の本体。区切り名の行と取り込みの行のほかは動かない。
- ほかの file の注の中の file 名の書き直し（凍結 anchor の列の検査と突き合わせの頭の注は 床の定数は `adr.rs` の `FLOOR` を読み口で読む と書く。読み口は `adr.rs` に残るので、注の 読み口で読む は正しいままで、床の木の置き場の file 名だけが古くなる）。移した注は字を変えずに運ぶ（移す前の置き場を指す file 名は区間の注に無い）。
- 床の定数の残り 5 本（天井・入口・相談窓口・規則の表・正本 7 file）と設計ノートの床の定数（便 114）。
- 塞がっている残り 3 file（設計ノートの床・入口の面・憲法の面）を開ける割り。便 114〜116 が運ぶ。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指し。本判断の外である（設計ノート §9 の 4 と 6）。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる（ADR-15 決定 (4)）。
2. 走査は**字面だけを見る**。再輸出を挟むと、歯が数える行き先は再輸出した file になり、真の定義の置き場は見えない。本便は再輸出を置かない。ADR-15 の起草役の台本の glob の再輸出の形（M11）と 1 名の再輸出の形（M12）と、読み口も移して再輸出する形（M18）は、verify の 8 行も workspace の歯も全部緑だった＝**再輸出を足す変化を捉えるのは歯ではなく、(i) の 3 の見え方の数（再輸出の行 3 → 3）である**。下の層の file が上の層の名を再輸出する形（M15）は、その file からの上向きの辺として境界の歯が捉える。**歯が捉えないのは、crate:: を書かずに super:: で区切りを名指す形である**（走査は crate:: と入口の接頭辞なしだけを数える）。新しい区切りを層 3 に置き、名指す 2 file を super:: の名指しに替えた変異（M10）は、上向きの辺が 2 本在るのに verify の 8 行も workspace の歯も全部緑だった。今の src の本文に super:: の名指しは `use super::*;` のほかに無い。**本便は super:: の名指しを 1 行も足さない。** 着地の時の確かめは (i) の 3 の見え方の数と、足した行に super:: が無いことである。
3. **層の置き誤りのうち、層 2 に置く誤りは歯が捉えない。** 起草役の変異で、新しい区切りを層 2 に置いても全部緑だった（M7）。新しい区切りを名指すのは層 2 の 2 file と層 3 の 1 file で、新しい区切り自身が名指すのは層 1 だけなので、層 2 では上向きの辺が生えない。層 0 に置く誤りは、新しい区切りが層 1 の 2 file を名指すので境界の歯が捉える（M6）。層 3 以上に置く誤りは、層 2 の 2 file からの上向きの辺として境界の歯が捉える（M8）。**層 2 への置き誤りは席の審査で見る**（層の割り当ての表の差分の 1 行）。
4. **同じ定義を 2 か所に持つ変化の一部は、どの歯も捉えない。** 値域の定数を `adr.rs` にも写し、`adr.rs` が自分の写しを読む変化（M4）は全部緑だった（条 P-6.3 に反する形・写しの値が同じ間は出力も変わらない）。写しが誰にも使われない場合（M5）は、clippy が使われない定義として捉える。着地の時の確かめは (i) の 3 の区間の純度（足した側に区間の定義が 2 度現れない）である。
5. **見え方の拡大を、どの歯も数えない。** 日付の形を広げる（M13）・床の木を道具の外へ向けた印にする（M14）変化は全部緑だった。本便の見え方の拡大 26 を確かめるのは (i) の 3 の見え方の数である。
6. **輪ごと外す弱め方は clippy が捉える。** 図の節の検査の呼び出しを消すと（M17）、検査の関数と図の節の欄の集合と図の型の一覧の置き場が誰にも使われない定義になり、clippy が落ちる。判断の記録の命令の歯も図の節の場面で落ちる。
7. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、本便の後の便 114〜116 は本便の着地を待って直列に運ぶ（設計ノート §5-a の 6）。便 112 は着地済み（main f9e48c6）なので、本便の前には待つ便が無い。

**撤退条件**（ADR-15 の撤退条件の逐語を引く。閾値は ADR-15 の値、比べる相手は直前の便〔便 112〕の着地後の値、判定は取り込みの審査が行い〔届かないときは持ち主〕、記帳は台帳 f2-648 に行う）。

- (1) 逐語: 責務で割っても受付の断りが減らない。数えるのは取り込みの審査で、台帳 f2-648 へ記帳する。11 本目の便が着地した後の 10 便で、実装の側の file の余地が足りずに受付で断られた回数を数える。3 回に達したら、決定 (2) の層の数と決定 (3) の降ろし方を捨てて測り直す判断の記録を起こす。歯の側の file が原因の断りはここに数えない（本判断は歯の側の境界を決めていない）。— ADR-15 の撤退条件 (1) は 11 本目の便の着地の後の 10 便で数えるので、本便は数えない（本便の後、余地 300 未満の src は 3 本・参考値）。
- (2) 逐語: 上向きの辺が増える。境界の歯が出す上向きの辺の集合を、取り込みの審査が直前の便の一覧と比べる（歯そのものは凍結した一覧との一致しか見ないので、便が辺を増やして一覧も同時に足せば歯は通る。増えたことを捉えるのは、審査が歯の file の差分を読む側である）。11 本の便のどれかの着地後に、上向きの辺が直前の便より増えていたら、決定 (5) の順を捨てて順を組み直す。増える便は計画に 1 本も無いことを写しの file で確かめてあるので、増えたときは実測か計画のどちらかが誤っていたということである。— 本便の直前の値は便 112 の着地後の 0 本、本便の計画値も 0 本（参考値）。審査は境界の歯の file の差分（凍結した一覧の行の増減）を読み、1 本でも在れば決定 (5) の順を組み直す。凍結した一覧は空のままなので、差分に一覧の行が現れないことが確かめである。本便で字面に現れない上向きの辺（(h) の言えないことの 2）を審査が数えられなかったときは (4) に当たる。
- (3) 逐語: 境界が責務で引けない場所が出る。判定するのは取り込みの審査（届かないときは持ち主）で、便を運ぶ席ではない。1 つの file を責務で割ろうとして、どちらの層にも属する部分が残り、割り方を決められないと審査が判じた便が 2 本に達したら、決定 (2) の層の定義を捨てて測り直す。— 床の定数（型付きのデータ）を層 1 に、床の読み口をそれを使う一番下の層（層 2・`adr.rs`）に残す割り方は、責務で引けていると見る（席の裁定・台帳 f2-648 notes 2026-09-23）。**起草の時点の見立て = 数えに入れない（数えは便 109〜112 の審査の裁定で 0 のまま）**。判じるのは取り込みの審査（届かないときは持ち主）で、着地の審査で改めて判じる。2 本に達したら決定 (2) の層の定義を捨てて測り直す（撤退条件 (3) の判定者・裁定は台帳 f2-648 notes）。
- (4) 逐語: 数えられない回が 2 回を超えたら、本判断の撤退条件の判定を「まだ分からない」に落として持ち主へ渡す。「同じ」とみなすのは（撤退条件の番号・数えられなかった理由・対象の便）の 3 つ組が一致する回である。この 2 回は本判断が自分で置く数であり、規則の表の行 R-7 の繰り返し上限とは別である（行 R-7 が言う 3 つ組は検査の id と判定と対象の道で、本判断が数えるものとは母集団が違う）。— 撤退条件 (4) は、本便の (2) か (3) を審査が数えられなかったときだけ当たる（本便でなりうるのは、(i) の 3 の見え方の数と super:: の確かめを審査が撃てなかったときの (2)。直前の便までの数えは 0 のまま）。

どの場合も区間は戻さない。戻すと `adr.rs` の余地が 110 に戻って塞がり、列の残り（便 114〜116）と後続の便の受付の前提が崩れるためである。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7（写しに区間を当てて組み立て、走査し直す）を本便の区間に当てたうえで、次の 6 つを数える。起草役の台本（区間を当てる 1 本・走査・行数・変異・出力の比較 2 本・受付の再現）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d113-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 50 file・243 辺（下 172・同じ層 71・上 0）、本便の後が 51 file・246 辺（下 173・同じ層 73・上 0）だった（参考値）。消える辺は 2（`adr.rs` から撤退条件の種類の型への下向き・`schema.rs` から `adr.rs` への下向き）、足す辺は 5（`adr.rs`・`link.rs`・`schema.rs` から新しい file への下向き 3、新しい file から床の木の型と撤退条件の種類の型への同じ層 2）。
3. **区間の純度と見え方と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（一致は 411 行＝区間の 435 行のうち見え方を広げなかった 409 行と、撤退条件の種類の取り込み 1 行と空行 1 行）。移動でない行は、足した側が (g) の 45 行、消した側が (g) の 29 行である。見え方を広げた 26 行は、印を外すと消した側の 26 行と字が一致する。見え方は src 全体で、道具の中で公開の印（`pub(crate) `）を含む行と、行頭の公開の印（空白の後の `pub `）の行と、再輸出（`pub use` と `pub(crate) use`）の行と、`use super::*;` のほかの super:: の名指しを base と比べる（128 → 154・369 → 369・3 → 3・0 → 0・参考値）。道具の中で公開の印の増分 26 は、定数 24 本と欄の集合の型の 2 欄（24 + 2）である。設計ノート §4-d の便 113 の値 25 + 2 との差 1 は日付の形で、床の木だけが使うので私のまま残した（§0 の 設計ノートからの逸脱）。足した行に super:: が無いことも diff で確かめる。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違い 0）。
4. **出力の byte**: base と本便の後の binary で、(f) の 1 の凍結の旗 16 通りと、`folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check`・`folio check`・`folio inject --check` を撃ち、`diff -r` と `cmp` で比べる。凍結の旗と `--refute` と `--stamp` は書くので、写しに当てる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`（9 file・判断の記録の側の byte 数）。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。

## 2. 範囲

- 入れる: 新しい src の file 1 本（判断の記録の床の定数・欄の集合の型・床の木）・`adr.rs` からの字を変えない移動（見え方の印 26 か所のほか）・`adr.rs` の頭の注 1 行と取り込みの行・名指しの 2 file（`link.rs`・`schema.rs`）の取り込みの行と区切り名・入口の区切りの宣言 1 行・境界の歯の file の層の表 1 行。
- 入れない: 判断の記録の検査の本体と床の読み口の置き場・`link.rs` と `schema.rs` の本体・再輸出の行と別名の取り込みと super:: の名指し・日付の形の見え方・ほかの file の注の中の file 名の書き直し・床の定数の残り 5 本と設計ノートの床の定数・既に在る歯の名や本数や本文の変更・境界の歯の凍結した一覧・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| floor_adr | 新しい src の file | `crates/folio/src/` の下の floor_adr.rs（責務の層 1 読む）。判断の記録の欄の集合の型・欄の決まりの定数・床の木を `adr.rs` から字を変えずに受ける。`adr.rs` の検査が読む定数 24 本と欄の集合の型の 2 欄を道具の中で公開にする |
| trim | 取り除き | `crates/folio/src/adr.rs` から移すものを外し、頭の注 1 行を足し、撤退条件の種類の取り込みを消し、床の木の型の取り込みから keys_floor を外し、新しい区切りの 26 名を取り込む（再輸出はしない） |
| rewire | 名指しの行 | 改訂の来歴の欄の集合を名指す `link.rs` と、床の木を名指す `schema.rs` の区切り名を新しい区切りへ向け直す（`link.rs` は取り込み 1 行も足す） |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に floor_adr を層 1 で 1 行足す。凍結した一覧は空のまま。歯の本文は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h)）ので、取り込みの審査は区間の純度と見え方の数と super:: の無さと歯の名を §1 (i) の 3 の手順で確かめ、床と生成区間の一致を §1 (f) の 2 の命令で撃つ。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる着地済みの便: 便 112（`folio check` の口が凍結の後始末を入口へ返す・main f9e48c6・行 `dk`・台帳 f2-648.157）・便 111（main ab39057・台帳 f2-648.156）・便 110（main 81f5f60・台帳 f2-648.155）・便 106（床の木の型を層 1 の `floor.rs` へ・main b8fb27f・台帳 f2-648.151）。本便と便 112 はどちらも入口 `main.rs` と境界の歯の file を書き換えるが、便 112 は着地済みなので書き込み範囲の重なりは無い。
- ADR-15 の列の便 114 以降は、どれも境界の歯の file を書き換えるので**本便の着地を待つ**。便 114（設計ノートの床の定数）は本便と同じ形で、`schema.rs` の名指しの行（本便の隣の行）を書き換える。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "dl"
title = "判断の記録の床の定数（欄の集合の型 Keys・欄の決まりの定数・床の木 FLOOR）を crates/folio/src/adr.rs から新しい file crates/folio/src/floor_adr.rs へ字を変えずに責務で降ろし、塞がっている adr.rs の余地を 300 以上へ開ける。これは判断の記録 ADR-15 の決定 (1)(2)(3)(5) が定めた 11 本の列の 8 本目（塞がっている 4 file を開ける 4 本の 1 本目）で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 113 の行がその中身を持つ。降ろす先は責務の層 1 読む で、区間が区間の外に名指すのは層 1 の床の木の型と keys_floor の書き方と撤退条件の種類の名の列だけなので、層が上がる辺は 0 本のまま増えない。adr.rs の検査が読む定数 24 本と欄の集合の型の 2 欄を道具の中で公開へ広げ（見え方の拡大 26）、日付の形は床の木だけが使うので私のまま残す。再輸出の行も別名の取り込みも super:: の名指しも 1 行も置かず、改訂の来歴の欄の集合を名指す link.rs と床の木を名指す schema.rs は新しい file を直に名指す。adr.rs は頭の注 1 行と取り込みの行を、link.rs と schema.rs は取り込みの行と区切り名だけを書き換え、入口 main.rs は区切りの宣言 1 行を足し、判断の記録の検査の本体と床の読み口 floor_strs・floor_val・floor_num は adr.rs に残り、ほかの行は 1 字も動かない。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の層の割り当ての表に floor_adr を層 1 で 1 行足す（凍結した一覧は空のまま・歯の本文は変えない）。歯は足さず消さず本文も変えず、adr.rs の単体の歯も名と本文が変わらず、folio check と folio schema の標準出力と標準エラーと終了コードも、欄の決まりの生成区間の byte も床の判定も、面・束・印・門・索引・部品の検査と凍結の旗の出力の byte も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR19", "FR5"]
section = "1"
write-set = ["+crates/folio/src/floor_adr.rs", "-crates/folio/src/adr.rs", "crates/folio/src/link.rs", "crates/folio/src/schema.rs", "crates/folio/src/main.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/adr.rs", "crates/folio/tests/schema.rs", "crates/folio/tests/floor_cases.rs", "crates/folio/tests/link.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test adr", "cargo nextest run -p folio --test schema", "cargo nextest run -p folio --test floor_cases", "cargo nextest run -p folio --test link", "cargo nextest run -p folio --bin folio adr_floor_derives_the_frozen_anchor_byte_for_byte", "cargo nextest run -p folio --bin folio floor_values_and_numbers_are_read_through_the_floor", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した空の一覧とちょうど一致する）、--test adr が走らせる判断の記録の命令の歯の全部が緑（案の数・承認欄・図の節・帰結の欄・改訂の欄・欄の決まりの写しのずれの違反を種類と場所と文言まで測る・参考値 20 本・本数と名は base の cargo nextest list と同じ）、--test schema が走らせる欄の決まりの生成区間の命令の歯の全部が緑（判断の記録の側の実の生成区間が凍結 anchor と byte で一致することを含む・参考値 18 本・本数と名は base の cargo nextest list と同じ）、--test floor_cases が走らせる歯の全部が緑（床の case の歯 1 本〔凍結 fixture の expected_cases が持つ件数の case を全部数え、全部の期待の終了コードと出力を満たす〕と、同じ file が取り込む yaml と sha256 の単体の歯・参考値 11 本）、--test link が走らせる判断の記録の突き合わせの命令の歯の全部が緑（改訂の来歴の欄の集合を読む道と撤退条件の種類の値域の突き合わせを含む・参考値 3 本・本数と名は base の cargo nextest list と同じ）、床の木の導出が凍結 anchor と byte で一致する単体の歯 1 本が緑、床の読み口が床の木の値と数を道で引ける単体の歯 1 本が緑、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い）で、workspace の nextest が全部緑（参考値 779 本・本数と名の一覧は base の cargo nextest list と同じ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
