# 設計: 便 115 — 入口の面の読み手（部品の一覧と枠・置き場の定数・読んだ中身と文脈の型・読みと数え・支度表の読み）を `face_index.rs` から新しい face_index_read.rs（層 4 面に出す・面の中の割り）へ字を変えずに責務で降ろし、塞がっている `face_index.rs` の余地を 300 以上へ開ける（参考値・base main 0d0b08a）（出力不変・FR4 / FR16 / FR9 / FR7）

- 要件: FR4（入口・憲法・要件書の面を 1 つの生成器と 1 組の design token で出す・第 1.31 版）/ FR16（判断の記録の面を正本から逐語で生成する・第 1.31 版）/ FR9（設計ノートを 1 つの型で生成する・第 1.31 版）/ FR7（組み立てて、見せる・第 1.31 版）。入口の面の読み手は、入口の面（FR4）の文脈を正本から組み、同時に判断の記録の並び（入口の棚から各ページへ辿る導線・FR16）と設計ノートの並び（設計ノートの面の前後の導線・FR9）と配信先の組み立ての面の並び（FR7）を返す。本便はどの要件の振る舞いも変えない。FR16・FR9・FR7 を挙げるのは、読み手の口を名指す 3 file（判断の記録の面・設計ノートの面・配信先の組み立て）の名指しの行を本便が書き換えるので、その 3 つの受入基準がこの便の 振る舞い不変 の対象になるからである。
- 条: P-2.1（人が読むページはすべて 1 つの生成器から出力する）/ P-2.4（部品・図の型・密度 profile は閉じた一覧で持つ）/ P-6.1（各文書の正本は YAML 1 file であり、人が読むページはそこから逐語で生成する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (1)(2)(3)(4)(5)（とくに決定 (3) の後段 **切り出す中身が面の口を呼ぶなら、下の層へ降ろさず同じ層の中で割る**）と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §1 の 面の中の割り・§2 の `face_index.rs` の行（余地 122）・§4-a と §4-b の **便 115 の行**・§4-b の直後の 便 115 の前提は便 105（着地済み）・§4-d（便 115 の見え方の拡大）・§5・§5-a の 4（再輸出を挟んだ名指しは見えない）と 5（区間の外に定義がある名の列挙）・§7・§8 の b-1-4（便 115・116 の新しい file を層 4 に置き、決定 (3) に面の中の割りを書き分ける）。本便は ADR-15 が定めた 11 本の列の 10 本目で、塞がっている 4 file を開ける 4 本（便 113〜116）の 3 本目、面の中の割りの 1 本目である。層が上がる辺は便 112 の後に 0 本で、本便は 0 本のまま運ぶ。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `dn` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 6 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 6 本〔入口の面の骨の歯・棚の行の歯・支度表の節の歯・判断の記録の面の歯・設計ノートの面の歯・配信先の組み立ての歯〕は本文を変えずに write-set に入る）。**起草役が write-set 13 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 設計ノートからの逸脱: 設計ノート §4-b の便 115 の行は、ADR-15 の起草役の台本の測り（区間 = 部品の一覧から文脈の型の impl まで〔読んだ正本の一覧 SOURCES・名札の表・dc・readable_faces を含む〕と、読みと数えの節の adr_number から updated までと、支度表の節の required だけ。文脈を組む context と支度表の読みは `face_index.rs` に残す。`face_index.rs` に新しい区切りの glob の再輸出を置き、判断の記録の面と設計ノートの面の取り込みの行を新しい区切りの別名 face_index に書き換え〔呼び出しの字は入口の面の名のまま〕、配信先の組み立て `site.rs` は書き換えずに再輸出を経由させる）だった（台本は写しの置き場の d115.py・起草役が実物の行を読んで確かめた）。本便は 3 点で違う。(1) **区間を ADR-15 決定 (3) の後段の字どおりに取る**: 正本を読んで面の文脈を組む部分＝文脈を組む context と支度表の読み（相談窓口の正本と支度表を読んで節の中身を返す）を区間に入れ、HTML を書く側だけが使う読んだ正本の一覧・名札の表・dc・readable_faces は `face_index.rs` に残す（(b) の 1・(c) の 1 行目）。(2) **再輸出も別名の取り込みも置かず、使う側が新しい file を直に名指す**（再輸出を挟んだ名指しは走査に見えない＝設計ノート §5-a の 4 の穴を避ける向き・便 113 / 114 と同じ席の裁定）。そのため配信先の組み立て `site.rs` が write-set に入る（§4-b の行に無い）。(3) 見え方の拡大は §4-d の 31 でなく **54** で、拡大が多い向きである。出どころは、書く側が文脈の型の欄と method を読むこと（区間の線が文脈の型の内と外を分ける）と、文脈を組む context と支度表の読みを区間に入れたことである（数の出どころは (b) の 3・(i) の 3）。§4-b の行は列の見積の材料で、判断の記録 ADR-15 が縛るのは層・上向きの辺の一覧（0 のまま）・便の本数と順・既存の歯を動かさないことである。この逸脱は席の裁定で受けた（取り込みの審査・台帳 f2-648 notes 2026-09-23）。(1) の区間は、読み手 = 正本から文脈を組む／書き手 = 文脈から HTML を出す の責務で引けており、書く側も使う枠 FRAME と部品の一覧 PARTS は読み手が正本の断りに使う閉じた一覧なので読み手に置く（(h) の撤退条件 (3)）。(2) の再輸出を置かない形は便 113 / 114 と同じ裁定で受けた。(3) の見え方の拡大 54 は、増分が全部 道具の中で公開 の印で、文脈の型の欄と method が読み手と書き手の線をまたぐのは読み手 / 書き手の割りに固有のものなので受けた（条件: 増分に行頭の公開の印 `pub ` が 1 行も無いことを (i) の 3 で数え、着地の審査で席が見る）。代わりの区間（context を書く側に残す台本の形）は決定 (3) の後段の字と食い違うので採らない。単体の歯のための `#[cfg(test)]` の取り込み 2 行も、既存の歯を動かさずに区間を移す最小の形として受けた（ADR-15 決定 (5)・(b) の 2）。
- 改訂: 改訂 c（2026-09-23・席）= 検証役（~/.local/share/folio2/handoff-2026-09-23/d115-verify.md・判定「受付に進んでよい」・blocking 0・文面だけ）の所見 5 点を反映 = 区間の 1 つ目の始まりの行番号・(甲) は super:: の名指しを数えないので (丙) と組で見る・新しい file に rustfmt を丸ごと当てない・頭の注の中身の列挙を実測に・台本の形そのものを当てた実測（X7）。区間・数・write-set・verify・size は変えない。揺れの歯（adr_figure_with_all_fields_passes）は検証役の単独 5 回・workspace 24 回で再現せず、原因は「まだ分からない」。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 114 の着地（main 0d0b08a・台帳 f2-648.159）を受けて、写しを実物の main 0d0b08a に取り直して全部を測り直し、表題の base・§0 の前の便・§1 (a) の辺の出発点・§1 (g) の受付が余地を測る範囲・§1 (h) の言えないことの 9 と撤退条件 (1)・§1 (i) の 7・§5 の依存を実物の形に直した。初稿の木（f9e48c6 + 便 113 と便 114 の起草役の実測 patch）と実物の 0d0b08a の違いは注の 10 行だけである（`adr.rs` と `note.rs` の頭の注 1 行ずつ・floor_adr.rs と floor_note.rs の頭の注 4 行ずつ）。本便の区間の file（入口の面・判断の記録の面・設計ノートの面・配信先の組み立て・入口の宣言・境界の歯の層の表）には掛からない。区間の台本は無修正で当たり、区間の差分は初稿の実測と 1 byte も違わない（50,801 byte・sha256 36819eb3…f66a が一致）。区間・write-set・verify・size・done・上向きの辺（0 のまま）・辺の計・相互の対（14 → 14）・歯の名の一覧（base と差 0）・見え方（189 → 243）・余地の表・src 全体の行数・出力の byte・凍結の旗の 16 通りの一致・門は 1 つも違わなかった。変異 25 本も実物で撃ち直した（結果は (d) の表と同じ。M19 の 1 回目の workspace で判断の記録の命令の歯 1 本〔図の欄の全部を持つ記録が通る〕が落ちたが、M19 が触らない道の歯で、M19 の撃ち直しで全部緑、その歯を単独で 3 回撃って 3 回緑だった＝揺れと見て表は変えない）。あわせて席の裁定 4 つ（台帳 f2-648 notes 2026-09-23）を文面に入れ、受付が余地を測る範囲（`crates/folio/src/` の下だけ・歯の側と `-` は測らない）を (g) に書き、ADR-15 の起草役の台本の形の記述を実物（glob の再輸出に加えて判断の記録の面と設計ノートの面は別名の取り込み・配信先の組み立ては書き換えない）に合わせた（§0 の逸脱の段・(c) の 2 行目・(d) の M11 と M13 の名・(h) の言えないことの 2）。測り直しの記録は `~/.local/share/folio2/handoff-2026-09-23/d115-remeasure.md`。
- 前の便: **便 114 着地済み（main 0d0b08a・設計ノートの床の定数を層 1 の floor_note.rs へ降ろした・行 `dm`・台帳 f2-648.159）**。便 113 も着地済み（main 89c668d・判断の記録の床の定数を層 1 の floor_adr.rs へ・行 `dl`・台帳 f2-648.158）、便 112 も着地済み（main f9e48c6・行 `dk`・台帳 f2-648.157）。起草役は初めに main f9e48c6 に便 113 と便 114 の起草役の実測 patch を当てた木で測り、便 114 の着地を受けて写しを 0d0b08a の実物に取り直した。本便の区間を台本で当てた差分は 2 つの木で 1 byte も違わない（改訂 b の行）。便 113 / 114 の区間と本便の区間は、同じ file では入口 `main.rs`（区切りの宣言）と境界の歯の file（層の表の行）の 2 本で重なるが、同じ行も隣の行も書き換えない（重なり無し）。**この契約の数はすべて 0d0b08a の実測（参考値）である。base は main 0d0b08a**（規則の表の行 D-13）。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り・ADR-15 決定 (1)(3)(5)）

入口の面の生成器 `crates/folio/src/face_index.rs`（層 4 面に出す）は、同じ層の中の 2 つの責務を同居させている。

1. **正本を読んで面の文脈を組む側**（base の 29〜57・71〜86・115〜259・297〜707 行の 4 区間・601 行）。部品の一覧 PARTS と枠 FRAME（面の名・正本の名・部品の一覧・前後の面へのリンク）／置き場の定数 3 本（設計ノートの dir・欄の決まりの file の名・相談窓口の正本の名）と判断の記録の状態の表／読んだ中身と文脈の型（判断の記録 1 本・設計ノート 1 本・読める文書の数・棚の文書・付録・関係・文脈 Ctx と、その method）／節 読みと数え（判断の記録の id の数・判断の記録と設計ノートを読んで並べる口・一覧の id を表と過不足なく照らす exact・憲法と要件書の札・文脈を組む context）／節 相談窓口の正本と支度表（節の見出しと支度表の中身の型・必須の欄の字面・節の見出しの読み・行き先の表・行き先の型・付録の型・支度表を読む口）。
2. **文脈から HTML を書く側**。面の口 derive（正本を読み込み、上の読み手を呼び、下の書き手を順に呼ぶ命令の口の本体）・読んだ正本の一覧 SOURCES（面の末尾だけが使う）・名札の表（数が 0 のときの字と節 支度表 の名札 12 本）・部品の名札の口 dc・読める面の数・節 骨格 以下（head・表紙・棚・棚の card と関係・状態の行・読む順番・相談窓口・節 支度表・末尾）・単体の歯の区画 4 本。

正規化で 1,376 行・余地 **124**（参考値）で、器の受付が size M（見積 300）の便を断る 2 file（便 114 の後）の 1 つである。

読み手の側の名を `face_index.rs` の外から名指すのは 3 file だけである。判断の記録の面 `face_adr.rs`（層 4・判断の記録の id の数を前後の面の並びに使う）、設計ノートの面 `face_note.rs`（層 4・設計ノートの並びとその 1 本の前後のリンクを使う）、配信先の組み立て `site.rs`（層 5・判断の記録と設計ノートの並びから面を 1 本 1 枚で組む）。`face.rs` は面の口 derive だけを呼ぶ。`crates/folio/tests/` の歯は 1 名も名指さない（歯は命令を撃つ）。

**区間の外に定義がある名の列挙**（ADR-15 決定 (5) の太字・設計ノート §5-a の 5）: 区間が使う名のうち区間の外に定義があるのは、部品目録の閉じた一覧の型 Component（層 1 `catalog.rs`）・憲法の段の閉じた一覧（層 1 `constitution_enums.rs`）・正本の cursor と字面の逃がし（`cursor::load`・R・X・esc・層 1 `cursor.rs`）・入口の棚の閉じた一覧と id の形と設計ノートの状態の表（`shelf::is_doc_id`・`shelf::STATUS`・ANNEXES・SHELF_DOCS・SHELF_RELATIONS・Shelf・層 1 `shelf.rs`）・yaml の値の型（層 1 `yaml.rs`）と、**面の口**（`face::link`・Frame・anchor・tier_of・層 4 `face.rs`）と、std の 3 つ（fs・Path・LazyLock）だけである。書く側の名（読んだ正本の一覧・名札の表・dc・readable_faces・面の口 derive と書き手の関数）を区間は 1 つも名指さない＝新しい file から `face_index.rs` への辺は生えず、相互に名指す対は増えない。確かめ方は、写しに区間を見え方を広げずに切り出し、元の取り込みのまま `cargo build --all-targets` を撃つことである（誤りは書く側の解けない名だけ・新しい file の側は使われない取り込みの警告だけだった）。

逆向き（区間の中に定義があり区間の外が使う名）は、`face_index.rs` の書く側が 16 名（判断の記録の状態の表・文脈 Ctx・棚の文書・枠 FRAME・相談窓口の正本の名・設計ノートの dir・設計ノート 1 本・判断の記録 1 本・支度表の中身と見出しの型・context・exact・判断の記録と設計ノートの並びの口・支度表の見出しと中身の口）と、単体の歯だけが使う部品の一覧 PARTS。ほかに `face_adr.rs` が判断の記録の id の数の 1 名、`face_note.rs` が設計ノートの並びの口と 1 本の前後のリンクの method、`site.rs` が判断の記録と設計ノートの並びの口 2 名（と、その中身の file と id の method）である。

便 114 の着地の後の層が上がる辺は **0 本**（参考値・便 112 で 0 に着き、便 113 と便 114 は 0 本のまま運んだ）で、境界の歯の file の凍結した一覧は空である。

### (b) 直す先 — 入口の面の読み手を新しい face_index_read.rs（層 4 面に出す）へ字を変えずに移す（ADR-15 決定 (3) の後段・設計ノート §1 の 面の中の割り）

**1. 新しい file** `crates/folio/src/` の下の face_index_read.rs へ、`face_index.rs` の 4 区間（(a) の 1 の 601 行）を字を変えずに、元の順のまま移す。区間どうしの間には空行を 1 行ずつ置く。新しい file に rustfmt を丸ごと当てない（区間の中の 1 か所を rustfmt が畳み、字を変えない移動でなくなる・(f) の末尾）。rustfmt の形にそろえるのは取り込みの行だけである。新しい file の頭の注 4 行は、何をどこから降ろしたか・面の口と HTML を書く側と名札の表は `face_index.rs` に残ること・面の口と字面の逃がしを呼ぶので層 1 に置かないこと・見え方だけを広げたこと・移した注の中の file 名は移す前の置き場を指すことを書く。取り込みは rustfmt の形で 10 行（std 3・空行 1・crate 6）。新しい file は 620 行・正規化 622 行（参考値）。

`face_index.rs` に残るのは (a) の 2 である。頭の注に 1 行（読み手を降ろした先・面の口と HTML を書く側はここに残すこと）を足し、取り込みを書く側が使うものだけに絞り（fs・LazyLock・憲法の段の一覧・Frame・anchor・tier_of・ANNEXES・SHELF_RELATIONS・Shelf を外す）、新しい区切りの 16 名の取り込み（rustfmt の形で 4 行）と、単体の歯のための部品の一覧の取り込み（`#[cfg(test)]` の付いた 2 行・下の 2）を足す。**再輸出の行は置かない**（下の 4）。

**2. 単体の歯は動かない。** `face_index.rs` の単体の歯 4 本（棚の文書の表の exact の照らし・読む順番の行き先の解決・読める面の数・部品の一覧が入口の面に置けること）は `face_index.rs` に残り、名も本文も変わらない（歯の名は `face_index::face_index_tests::` のまま）。歯は親の取り込みを `use super::*` で受ける。exact と readable_faces と棚の文書の表と読む順番の行き先の解決は書く側も使うので、ふだんの取り込みで届く。**部品の一覧 PARTS だけは書く側が使わない**（枠 FRAME の欄だけが使う）ので、その取り込みを単体の歯の区画を組み立てるときに限る（`#[cfg(test)]`）。印を付けずに取り込むと `cargo build` が使われない取り込みの警告を出し、取り込みを置かないと単体の歯の区画が組み立たない（(d) の変異）。移す区間に単体の歯の区画は無い。

**3. 見え方の拡大は 54 か所**（ADR-15 決定 (3) が契約に書けと言う数・参考値）。写しに区間を見え方を広げずに当てて組み立て、組み立ての誤り（私の項目・欄・method・型を別の file から名指した）と、型が欄より私だという警告で要ると言われたものだけを広げた。

| 広げたもの（私 → 道具の中で公開） | 数 | 理由 |
| --- | ---: | --- |
| 項目: 枠 FRAME・置き場の定数 2 本（設計ノートの dir・相談窓口の正本の名）・判断の記録の状態の表・棚の文書と文脈の型・exact・context・支度表の見出しと中身の型・支度表の見出しと中身の口 | 12 | 書く側が名指す（面の口 derive が context と支度表の 2 つの口を呼び、書き手が文脈を引数に取り、棚の凡例が exact を呼ぶ） |
| 型: 読める文書の数・付録・関係 | 3 | 文脈の欄の型として書く側に見える（書く側がその欄を読むと組み立てが型は私だと断り、欄だけ広げると型が欄より私だという警告が出る） |
| 欄: 判断の記録 1 本の 3（id・状態・日付）・設計ノート 1 本の 3（id・状態・生成日）・読める文書の数の 2・棚の文書の 6・付録の 4・関係の 4・文脈の 6・支度表の見出しの 3・支度表の中身の 3 | 34 | 書く側（棚の card と関係・状態の行・読む順番・節 支度表）が読む |
| 文脈の method（文書を id で引く・読める文書・まだ無い文書・面の数・読める型の列挙） | 5 | 書く側が呼ぶ |
| 既に道具の中で公開だったもの（判断の記録の id の数）と、既に公開だったもの（部品の一覧・判断の記録 1 本と設計ノート 1 本の型・2 つの並びの口・file と id と前後のリンクの method） | 0 | 印を変えない |

私のまま残すもの: 欄の決まりの file の名（設計ノートを読む口だけが使う）・判断の記録 1 本の id の数と設計ノート 1 本の題（並べる口と前後のリンクの method だけが読む）・必須の欄の字面・行き先の表と型・付録の型・憲法と要件書の札・更新の字（どれも区間の中だけが呼ぶ）。

行頭の公開の印（`pub `）は 1 つも足さない。**再輸出の行（`pub use` と `pub(crate) use`）も、別名の取り込みも、super:: の名指しも 1 行も足さない。** 起草役は src 全体で、道具の中で公開の印を含む行が 189 → 243（差 54 がちょうど上の表の 54）、行頭の公開の印の行が 369 → 369、再輸出の行が 3 → 3、`use super::*;` のほかの super:: の名指しが 0 → 0、`#[cfg(test)]` の直後の取り込みの行が 0 → 1 であることを数えた（参考値・数え方は (i) の 3）。設計ノート §4-d は便 115 を 31 と数えた。差の出どころは (i) の 3 に書く。

**4. 名指しの行だけを書き換える file**。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `face_adr.rs` | 4 | 取り込み 1 行と判断の記録の id の数の呼び出し 1 行の区切り名（入口の面 → 新しい区切り） |
| `face_note.rs` | 4 | 取り込み 1 行と設計ノートの並びの呼び出し 1 行と前後のリンクの method の道 1 行の区切り名 |
| `site.rs` | 5 | 区切りの括りの取り込み 1 行に新しい区切りを足し、判断の記録と設計ノートの並びの呼び出し 2 行の区切り名を書き換える（面の口 derive の呼び出しは入口の面のまま） |
| `main.rs` | 5 | 区切りの宣言 1 行（並びは字の順・入口の面の宣言の次） |

**この 4 file では、表の行のほかは 1 字も動かない。** 使う側が名指す先を新しい file へ直に向けるのは、再輸出も別名も置かないためである（(c) の 2〜4 行目）。`site.rs` の頭の注の中の 入口の面と同じ読み（`face_index::records`）の字は直さない（(h) の 運ばないもの）。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の後に運ぶ、塞がっている file を開ける 4 本の 3 本目である。先に便 107 が正本の cursor と字面の逃がしを、便 108 が部品目録の閉じた一覧を、便 109 が入口の棚の閉じた一覧と設計ノートの状態の表を層 1 へ降ろしたので、読み手を層 4 に切り出しても、読み手が名指す先は層 1 と層 4 の `face.rs` だけで、上の層を名指さない（区間の外の名は (a) の列挙）。歯の側の前提は便 105（入口の面の歯の file を 3 本に割り、`tests/face_index.rs` の余地を正に戻した・着地済み）である。

**層 4 に置く理由と、その限界**。読み手は面の口（`face::link`・anchor・tier_of）と字面の逃がしを呼び、枠 FRAME はリンク（HTML）も組む。層 1 の定義（正本の byte を型に直す）に当てはまらないので、ADR-15 決定 (3) の後段のとおり**同じ層 4 の中で割る**。向きの決まりは同じ層を許すので、塞がりは開き、上向きの辺は増えない。層 0〜3 に置くと、読み手から層 4 の `face.rs` への上向きの辺が生え、層 5 に置くと、層 4 の 3 file（入口の面・判断の記録の面・設計ノートの面）から読み手への上向きの辺が生える（(d) の変異でどれも境界の歯が捉える）。**読み手（正本を読んで面の文脈を組み、文脈と支度表の中身を型で返す）と書き手（文脈から HTML を書く）の割り方は、責務で引けていると見る**（席の裁定・台帳 f2-648 notes 2026-09-23・便 113 / 114 と同じ向き）。面の口 derive は、正本を読み込んで読み手と書き手を順に呼ぶ命令の口の本体なので `face_index.rs` に残す（ADR-15 決定 (3) の 元の file には命令の口の本体だけを残す）。枠 FRAME は書く側（head と部品の名札と末尾）も使うが、面の名・正本の名・部品の一覧・前後の面を持つ面の静的な文脈で、支度表の読み（付録の型が見つからないときの断りの字）も正本の名を読むので読み手に置く（設計ノート §4-b の便 115 の行も 部品の一覧・枠 を読み手に数える）。判定は取り込みの審査が行う（(h) の撤退条件 (3)）。

### (c) 採らなかった形（ADR-15 決定 (3)(4)）

| 形 | 退けた理由 |
| --- | --- |
| 設計ノート §4-b の測り（ADR-15 の起草役の台本）の区間: 部品の一覧から文脈の型の impl まで（読んだ正本の一覧・名札の表・dc・readable_faces を含む）と adr_number から updated までと required だけを移し、文脈を組む context と支度表の読みを `face_index.rs` に残す | ADR-15 決定 (3) の後段が名指す 正本を読んで面の文脈を組む部分 の本体（context）と、相談窓口の正本と支度表を読む口が書く側に残り、書く側だけが使う名札と dc と面の数が読み手に入る＝割りの線が責務と食い違う。本便は context と支度表の読みを読み手に入れ、名札・dc・readable_faces・読んだ正本の一覧を書く側に残す |
| `face_index.rs` に新しい区切りを丸ごと再輸出する 1 行（glob・台本の再輸出の行と同じ字）を置き、使う側 3 file を書き換えない（台本はこの再輸出に加えて判断の記録の面と設計ノートの面の取り込みを下の 4 行目の別名に書き換え、配信先の組み立てだけを再輸出経由のまま残した） | 辺の数は同じ 0 に着き書き込み範囲の src が 1 本減る（`site.rs` を触らない）が、再輸出の行が 1 つ増え（3 → 4）、新しい区切りの公開の名が全部 `face_index` の名でも道具の全体から名指せるようになる。走査は字面を見るので、使う側の真の行き先が辺に現れない（設計ノート §5-a の 4）。(d) の変異で、この形はどの歯も捉えないことを確かめた |
| 判断の記録の id の数 1 名だけを `face_index.rs` から再輸出し、`face_adr.rs` を書き換えない | 同上（再輸出の行が 1 つ増え、`face_adr.rs` から新しい file への辺が字面から消える） |
| 判断の記録の面と設計ノートの面が新しい区切りを別名 face_index で取り込む（台本の別名の行と同じ字） | 走査は `crate::` の後の区切り名を見るので辺は同じだが、本文の呼び出しの字が入口の面の名のままで、どの file を名指しているかが本文から読めない。(d) の変異で、この形も全部緑・見え方の数も動かないことを確かめた（捉えるのは diff を読む審査だけ）。本便は別名を置かず、呼び出しの字も新しい区切り名にする |
| 枠と部品の一覧を `face_index.rs` に残し、支度表の読みは読み手に入れる（読み手が付録の型の断りの字で `face_index.rs` の枠を名指す） | 同じ層の中の名指しなので境界の歯は通すが、入口の面と読み手が互いに名指す対が 1 つ増える（相互の対 14 → 15）。設計ノート §6 の見込み（便 116 の後も 14）から外れる。(d) の変異で、この形は歯も見え方の数も捉えず、相互の対の数だけが捉えることを確かめた |
| 枠と部品の一覧も支度表の節（見出しと中身の読み）も `face_index.rs` に残す | 相互の名指しは生えないが、相談窓口の正本と支度表を読んで節の中身を組む口が書く側に残る＝読みと書きが同じ file に同居したまま残り、割りの線が責務と食い違う |
| 読める面の数と読んだ正本の一覧も読み手へ | どちらも正本を読まず、書く側だけが使う（棚の見出しの数と末尾の sources の字）。広げる理由が無く、見え方の拡大が 2 増える |
| 単体の歯のための部品の一覧の取り込みを、印なしで置く／置かない／単体の歯を新しい file へ移す | 印なしは `cargo build` が使われない取り込みの警告を出し clippy が落ちる。置かないと単体の歯の区画が組み立たない。歯を移すと歯の名が変わる（既存の歯を動かさない・ADR-15 決定 (5)）。(d) の変異で前 2 つを確かめた |
| 新しい file を層 1（読む）に置く | 読み手が層 4 の `face.rs`（面の口）を名指すので上向きの辺が生える（ADR-15 決定 (3) の後段が退けた形・(d) の変異で境界の歯が落ちる） |
| 区間の私の項目と欄を全部広げる（台本の widen の形） | 区間の中だけが使う項目と欄まで道具の全体から名指せるようになる。(d) の変異で、どの歯も捉えず見え方の数だけが動くことを確かめた |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台と変異（P-10.1・P-10.2）

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。歯の本数と名の正本は `cargo nextest list` の出力で、この契約には写さない。

- `crates/folio/tests/face_index.rs`: 入口の面の骨の歯。凍結 fixture（`tests/fixtures/face/` の入口の面・支度表の無い側と在る側の 2 通り）との byte 一致・実の正本の面の部品の検査・正本を直に読んで数えた逐語と件数の census（生成側から独立した物差し）・check の 3 値・導出できない入力・凡例の並び・読む順番の行き先を測る。読み手の文脈（棚の文書・付録・関係・判断の記録の並び）の 1 欄の違いが面の byte に出れば落ちる。
- `crates/folio/tests/face_index_shelf.rs`: 棚の行の歯。判断の記録と設計ノートの card の在る側と無い側・状態が表に無い・番号の重なり・id と file 名のずれ・置き場の不在・棚の外が動かないことを、写しの正本で測る（読み手の 2 つの並びの口と状態の表を通る）。
- `crates/folio/tests/face_index_sheet.rs`: 節 支度表 の歯。支度表の在る側と無い側の census・導出できない 4 つ・付録の型を棚の付録に解くこと・承認の行・相談窓口からの 1 周を測る（読み手の支度表の読みを通る）。
- `crates/folio/tests/face_adr.rs`・`crates/folio/tests/face_note.rs`: 判断の記録の面と設計ノートの面の歯。凍結 fixture との byte 一致と、隣の記録と棚の順の隣へのリンク（読み手の判断の記録の id の数と設計ノートの並びの口を通る）を測る。
- `crates/folio/tests/site.rs`: 配信先の組み立ての歯。凍結 fixture との byte 一致と、実の正本の各面（判断の記録と設計ノートの面を 1 本 1 枚）の検査を測る（読み手の 2 つの並びの口を通る）。
- 単体の歯 4 本（`face_index.rs` の区画・`--bin folio` で名指す）: 棚の文書の表を exact で照らす・読む順番の行き先の解決・読める面の数・部品の一覧が入口の面に置けること。
- 境界の歯 2 本（(e) の 1）。

起草役の変異の実測（写しの repo・workspace の nextest を 1 回撃って落ちた歯を verify の行へ振り分け〔組み立たないときは verify の行を 1 本ずつ撃ち〕、clippy を撃ち、変異の binary で実の design-intent に `folio build --write`〔23 file を base の binary の出力と `diff -r`〕と `folio check` を撃って比べ、独立の走査で上向きの辺と相互の対を数えて戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 入口の面の骨・3 棚の行・4 支度表の節・5 判断の記録の面・6 設計ノートの面・7 配信先の組み立て・8〜11 単体の歯 4 本・12 clippy）である。落ちの数は落ちた歯の本数である。

| 変異 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8〜11 | 12 | workspace | 出力（build・check） | 走査の上向き | 相互の対 | 見え方（公開の印の行・行頭 pub・再輸出・super::・cfg(test) の取り込み） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ---: | ---: | --- |
| M1 面の文脈の欄を 1 つ落とす（context が関係の欄 relations を文脈へ入れない） | 緑 | **落ち 13** | **落ち 6** | **落ち 9** | 緑 | 緑 | **落ち 9** | 緑 | **落ち** | 落ち 66 | 差（生成が止まる）・一致 | 0 | 14 | 243・369・3・0・1 |
| M2 面の文脈の値を 1 つ変える（判断の記録の状態の名札 廃止 → 退役） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M25 面の文脈の値を 1 つ変える（判断の記録の状態の名札 発効 → 有効・実の正本に在る状態） | 緑 | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 差・一致 | 0 | 14 | 243・369・3・0・1 |
| M3 支度表の読みで付録の型を落とす（with を読むが並べない） | 緑 | **落ち 1** | 緑 | **落ち 3** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 5 | 差・一致 | 0 | 14 | 243・369・3・0・1 |
| M4 新しい区切りを層 0 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M5 新しい区切りを層 1 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M6 新しい区切りを層 2 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M7 新しい区切りを層 3 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M8 新しい区切りを層 5 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M9 層の表から新しい区切りの行を消す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M10 層 5 に置き、名指す側 4 file を super:: の名指しに替える | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・5・1 |
| M11 glob の再輸出 1 行（台本の再輸出の行と同じ字・使う側 3 file は face_index 経由のまま） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 244・369・4・0・0 |
| M12 1 名の再輸出（adr_number）・face_adr は face_index 経由のまま | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 244・369・4・0・1 |
| M13 別名の取り込み（台本の別名の行と同じ字・face_adr と face_note が use crate::face_index_read as face_index） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M14 区間の私の fn（required）を道具の中で公開へ | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 244・369・3・0・1 |
| M15 区間の top-level と欄を全部道具の中で公開へ（台本の widen の形） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 253・369・3・0・1 |
| M16 FRAME を pub へ（道具の外へ向けた印） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 242・370・3・0・1 |
| M17 区間の当て損ない（exact を face_index.rs に取り残し、文脈の組み立てが face_index を名指す） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 15 | 243・369・3・0・1 |
| M18 区間の当て損ない（枠 FRAME と PARTS を face_index.rs に取り残し、支度表の読みが face_index の FRAME を名指す） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 15 | 243・369・3・0・0 |
| M19 同じ定義を 2 か所（face_index.rs に ADR_STATUS の写しを置いて自分の写しを読む） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M20 同じ定義を 2 か所（face_index.rs に NOTE_SCHEMA の写し・誰も使わない） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち** | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・0・1 |
| M21 status-line を輪ごと外す（derive の status_line の呼び出しを消す） | 緑 | **落ち 2** | **落ち 4** | 緑 | 緑 | 緑 | **落ち 1** | 緑 | **落ち** | 落ち 8 | 差・一致 | 0 | 14 | 243・369・3・0・1 |
| M22 単体の歯のための取り込みを #[cfg(test)] なしにする | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち** | **全部緑** | 一致・一致 | 0 | 14 | 243・369・3・0・0 |
| M23 単体の歯のための取り込みを消す | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **組み立て不可** | **落ち** | 組み立て不可 | 一致・一致 | 0 | 14 | 243・369・3・0・0 |
| M24 新しい区切りが上の層の名を再輸出する（site の run） | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち** | 落ち 1 | 一致・一致 | 1 | 15 | 244・369・4・0・1 |

出力の列は、変異の binary と base の binary で実の design-intent に `folio build --write` と `folio check` を撃ち、配信先の 23 file と、標準出力と標準エラーと終了コードを比べた結果である。見え方の列は src 全体の 5 つの数（本便の後は 243・369・3・0・1）である（数え方は (i) の 3・参考値）。走査の列は起草役の独立の走査の上向きの辺の数（本便の後は 0）と、相互に名指す対の数（本便の後は 14）である。

変異 25 本のうち、verify の 12 行のどれも落ちなかったのは 11 本（M2・M10〜M19）である。

- **面の文脈の中身の変化**（M1・M3・M25）は、入口の面の骨の歯・支度表の節の歯・配信先の組み立ての歯のどれかが凍結 fixture との byte の違いや census として捉える。M1（文脈へ関係の欄を入れない）は、書く側が関係の欄を引いて生成が止まり、clippy も使われない可変の印として捉える。**実の正本にも凍結 fixture にも無い値の変化（M2・判断の記録の状態 廃止 の名札）は、どの歯も捉えない**（(h) の言えないことの 6）。実の正本に在る値の変化（M25・発効 の名札）は捉える。
- **層の置き誤りと一覧の書き換え**（M4〜M9）は、どの層に置いても境界の歯が捉える。新しい区切りは層 4 の `face.rs` を名指し、層 4 の 3 file と層 5 の `site.rs` から名指されるので、層 4 のほかのどこに置いても上向きの辺が生える（便 113 / 114 と違い、層の置き誤りに歯の穴は無い）。下の層の file が上の層の名を再輸出する形（M24）も、その file からの上向きの辺として境界の歯が捉える。
- **super:: の名指し・再輸出・別名の取り込み**（M10〜M13）は、どの歯も捉えない。M10〜M12 は見え方の数（super:: 0 → 5・再輸出 3 → 4）が捉える。**別名の取り込み（M13・台本の別名の行と同じ字）は見え方の数も相互の対も動かさない**＝捉えるのは diff を読む審査だけである（(h) の言えないことの 2）。台本の形そのもの（glob の再輸出と別名の取り込みの両方）は M11 と M13 を合わせた形で、どちらの変異も全部緑だった。両方を同時に当てた形も全部緑で、見え方の数は再輸出の行 3 → 4 だけが動いた（検証役の変異 X7）。
- **見え方の広げ過ぎ**（M14〜M16）は、どの歯も捉えない。見え方の数（公開の印の行 243 → 244・253、行頭 pub 369 → 370）が捉える（(h) の言えないことの 5）。
- **同じ層の中の区間の当て損ない**（M17・M18）は、どの歯も見え方の数も捉えない。新しい区切りから `face_index.rs` への同じ層の辺が生え、相互に名指す対が 14 → 15 になる＝**捉えるのは (i) の 3 の相互の対の数だけ**である（(h) の言えないことの 4）。
- **同じ定義を 2 か所に持つ形**のうち、写しを読む形（M19）はどの歯も見え方の数も捉えない（(h) の言えないことの 4・区間の純度が捉える）。誰も使わない写し（M20）は clippy が捉える。
- **輪ごと外す弱め方**（M21）は、clippy（使われない定義）と入口の面の骨の歯・棚の行の歯・配信先の組み立ての歯が捉える。
- **単体の歯のための取り込みの形**（M22・M23）は、印を外すと clippy が使われない取り込みとして捉え、取り込みを消すと単体の歯の区画が組み立たない（verify の 8〜11 と clippy が落ちる）。

独立の走査の列は、層の割り当てを本便の計画（新しい区切りは層 4）に固定して src の字面だけから数えるので、境界の歯の層の表だけを書き換える変異（M4〜M9）では 0 のままである。M19 と M20 は区間の移動では生まれない形で、本便は定義を写さない。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。面の中身の側には独立した凍結 anchor が在る（入口の面の 2 通り・判断の記録の面・設計ノートの面・配信先の組み立ての凍結 fixture と、正本を直に読む census）。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない・ADR-15 決定 (4)(5)）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り face_index_read を層 4 で 1 行足す（並びは層 4 の中の字の順・入口の面の行の次） |
| まだ降ろしていない層が上がる辺の一覧 | 変えない（空のまま）。**行を足さない** |

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 新しい区切りを含めて一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 空の集合で一致（不変） |

**2. 単体の歯は動かない**（(b) の 2）。

**歯は足しも消しもしない。** 歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）は base と 1 字も違わない（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと（ADR-15 決定 (5) の受入）

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下と `tests/floor_cases.yaml`）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。
   - 読み手を通る出力: 配信先の組み立て（`folio build --write`・23 file・1,316,521 byte・入口の面と判断の記録の面と設計ノートの面を全部含む）は `diff -r` で差 0。部品の検査（組み立ての出力の 5 面・どちらも 合格・60 byte）は byte 一致。
   - ほかの出力: 天井の材料の束（`folio ceiling --write`・観点 4・199 file・7,250,322 byte）は差 0。その束と持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check` を当てた出力（536 byte と 716 byte）・`--refute`（写しの 211 file・7,572,583 byte が差 0）・`--stamp`（印 7,860 byte を含む写しが差 0）・門 `--gate` 3 通りは byte 一致。相談窓口の質問（105 byte）・索引（38,207 byte）・導出した一覧の JSON の 1 行（1,400 byte）・欄の決まりの生成区間（`folio schema --check`・9 file とも 一致・464 byte）・床の判定（`folio check`・合格・60 byte）・`folio inject --check`（68 行・7,912 byte）は byte 一致。写しの外で撃った凍結の旗 3 つと版を上げた写しの旗 4 つも byte 一致・終了コード一致（23 対）。
   - 凍結の旗: 写しの根（`design-intent/` と器の導出 file `contracts/schema.toml`）を作って git init と 1 commit を行い、4 つの場面（素の写し・憲法の版と P-1 の題を上げた写し・id の一覧の凍結 anchor を消した写し・版を上げずに P-1 の題だけを変えた写し）それぞれに、旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つを撃った。16 通りの標準出力と標準エラーの 32 本が byte で一致し、終了コードも一致し、撃った後の写しの中身は `diff -r` で差 0 だった。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致。どれも終了コード 0（ADR-15 決定 (5) の受入の 2 つ目と 3 つ目）。このうち床と生成区間の一致は、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃つのは取り込みの審査である。入口の面の byte は、verify の 2 と 7 が凍結 fixture との byte 一致として歯で数える（`tests/fixtures/face/` の入口の面 2 通りと配信先の組み立て）。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **全部緑**（歯の名の一覧は base と同じ）、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り・本便の単体の歯のための取り込みはこの区画でだけ使われる）。`cargo fmt --check`（edition 2024）の差分の数は、触る file の合計で base と同じである（base の `face_index.rs` の読み手の側に在った 1 か所が、そのまま新しい file へ移る）。

### (g) 大きさ（器の行数の上限 R-C4 と同じ式・ADR-15 決定 (1)）

size は **M**（見積 300 行）。本便の新しい file は正規化 622 行（参考値）で、S の見積 100 も M の見積 300 も超える。契約の型（`docs/design/delivery-template.md` の §4）は大きさに S か M を挙げるので、実際の増分に近い大きい方の M を選ぶ（便 110・113・114 と同じ）。

**受付が余地を測るのは `crates/folio/src/` の下の `+` と印の無い file だけで、行が減る `-` の file と歯の側の file は測らない。** `face_index.rs` は本便で 1,376 → 774 行に減るので `-` を付ける。`face_index.rs` の base の余地 124 は M の見積 300 を下回るので、`-` を付けずに write-set に入れると受付が余地の不足で断る。本便は `face_index.rs` を開ける便なので、`-` の印が本便の受付の前提である。入口の宣言は 1 行増えるので印を付けず、測られる（余地 909）。境界の歯の file と verify の歯の file 6 本は歯の側の file なので、受付は余地を測らない（下の表は参考値）。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | M の見積 300 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/face_index.rs` | 1,376 | **124** | 受付は測らない（`-`） | 774（減る・余地 **726**） |
| face_index_read.rs（新設・`crates/folio/src/` の下） | base に無い | 1,500 | +1,200 | 622（余地 878） |
| `crates/folio/src/face_adr.rs` | 856 | 644 | +344 | 856 |
| `crates/folio/src/face_note.rs` | 1,011 | 489 | +189 | 1,011 |
| `crates/folio/src/site.rs` | 261 | 1,239 | +939 | 261 |
| `crates/folio/src/main.rs` | 591 | 909 | +609 | 592（余地 908） |

歯の側の余地（参考値・同じ base）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 315 | 1,185 | 316（1 行足す・印なし・歯の側なので受付は測らない） |
| `crates/folio/tests/face_index.rs` | 810 | 690 | 不変 |
| `crates/folio/tests/face_index_shelf.rs` | 516 | 984 | 不変 |
| `crates/folio/tests/face_index_sheet.rs` | 643 | 857 | 不変 |
| `crates/folio/tests/face_adr.rs` | 1,102 | **398** | 不変（歯の側なので受付は測らない） |
| `crates/folio/tests/face_note.rs` | 1,060 | 440 | 不変 |
| `crates/folio/tests/site.rs` | 727 | 773 | 不変 |

本便が移動のほかに書く行は 85 行（参考値）である。新しい file の頭の注 4 行・取り込み 6 行と空行 2 行・見え方を広げた 54 行（区間の行の頭に印を足しただけで、ほかの字は同じ）、`face_index.rs` の頭の注 1 行と取り込み 8 行（面の口の 1 行・新しい区切りの 4 行・単体の歯のための 2 行・棚の 1 行）、`face_adr.rs` の 2 行、`face_note.rs` の 3 行、`site.rs` の 3 行、`main.rs` の 1 行、境界の歯の file の 1 行である。消す行は 64 行で、見え方を広げる前の 54 行と、`face_index.rs` の取り込み 2 行、`face_adr.rs` の 2 行、`face_note.rs` の 3 行、`site.rs` の 3 行である。**ほかはすべて字を変えない移動**（554 行）である（数え方は (i) の 3）。

src 全体は 52 file・26,284 行から 53 file・26,305 行になり、余地 300 未満の src は 2 本から 1 本になる（参考値・残るのは憲法の面で、便 116 が開ける）。

**設計ノート §4-b との差**（参考値）: 新しい file と層は §4-b の便 115 の行と揃う。違いは 4 点で、どれも §0 の 設計ノートからの逸脱 に由来する。(1) `face_index.rs` は §4-b の 1,378 → 935 に対し 1,376 → 774 である（本便は文脈を組む context と支度表の読みも移し、名札・dc・readable_faces・読んだ正本の一覧を残し、再輸出の 1 行の代わりに 16 名の取り込み 4 行と単体の歯のための 2 行を置く）。(2) 新しい file は §4-b の 453 に対し 622 である（同じ理由）。(3) 見え方の拡大は §4-d の 31 に対し 54 である（(i) の 3）。(4) 書き込み範囲に `site.rs` と verify の歯の file 5 本（棚の行・支度表の節・判断の記録の面・設計ノートの面・配信先の組み立て）が入る。§4-b の測りは再輸出で使う側を書き換えず、`cargo build` しか撃たず、歯の file を verify に名指す前の測りだった。

**便の diff の大きさ**は、写しの repo で base からの差を測って **50,801 byte**（参考値・7 file・足す 639 行・消す 618 行）で、器の門が測る上限 150,000 byte の下に収まる。

### (h) 本便が運ばないもの・言えないこと・撤退条件（ADR-15 撤退条件 (1)〜(4)）

**運ばないもの。**

- 入口の面の書く側（面の口 derive・読んだ正本の一覧・名札の表・dc・読める面の数・節 骨格 以下の書き手）と単体の歯の区画（`face_index.rs` に残る）。区間の外の行は、頭の注 1 行と取り込みのほかは 1 字も動かない。
- 判断の記録の面・設計ノートの面・配信先の組み立ての本体。区切り名の行のほかは動かない。
- ほかの file の注の中の file 名と道の書き直し。`site.rs` の頭の注の 入口の面と同じ読み（`face_index::records` と `face_index::notes`）の字と、`shelf.rs` の頭の注の 読むのは…面の生成器（`face_index.rs`）の字は、本便の後は読み手の置き場を指さなくなるが、本便は直さない（注は走査が剥がすので辺に現れない・便 113 / 114 と同じ扱い）。移した注の中の file 名も字を変えずに運び、新しい file の頭の注がその file 名は移す前の置き場を指すと断る。
- 憲法の面の割り（便 116 が運ぶ）。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指しの解消。本判断の外である（設計ノート §9 の 4 と 6）。単体の歯（部品の一覧が入口の面に置けること）が読み手の一覧を書き手の file から `#[cfg(test)]` の取り込みで見ている状態も、設計ノート §9 の 6 の掃除の便（便 117 以降）で歯の置き場を正す後続に置く（席の裁定・台帳 f2-648 notes 2026-09-23）。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる（ADR-15 決定 (4)）。
2. 走査は**字面だけを見る**。再輸出を挟むと、歯が数える行き先は再輸出した file になり、真の定義の置き場は見えない。本便は再輸出を置かない。glob の再輸出の形（M11・台本の再輸出の行と同じ字）と 1 名の再輸出の形（M12）は、verify の 12 行も workspace の歯も全部緑だった＝**再輸出を足す変化を捉えるのは歯ではなく、(i) の 3 の見え方の数（再輸出の行 3 → 3）である**。**歯が捉えないのは、crate:: を書かずに super:: で区切りを名指す形である**（走査は crate:: と入口の接頭辞なしだけを数える）。新しい区切りを層 5 に置き、名指す 4 file を super:: の名指しに替えた変異（M10）は、上向きの辺が在るのに全部緑だった。今の src の本文に super:: の名指しは `use super::*;` のほかに無い。**本便は super:: の名指しを 1 行も足さない。** **別名の取り込み（M13・台本の別名の行と同じ字）は、歯も見え方の数も相互の対も動かさない**。本便は別名を 1 行も置かない。着地の時の確かめは (i) の 3 の見え方の数と、足した行に super:: と別名（`as`）の取り込みが無いことである。
3. **層の置き誤りは、層 4 のほかのどこでも境界の歯が捉える**（M4〜M8）。新しい区切りは層 4 の面の口を名指し、層 4 と層 5 から名指されるので、層 4 のほかでは必ず上向きの辺が生える。便 113 / 114 の 層 2 への置き誤りを歯が捉えない 穴は、本便には無い。ただし super:: の名指しと組むと捉えない（2 の M10）。
4. **同じ層の中の区間の当て損ないと、同じ定義を 2 か所に持つ変化は、どの歯も捉えない。** 向きの決まりは同じ層を許すので、区間の名を `face_index.rs` に取り残して読み手がそれを名指す形（M17 = exact・M18 = 枠と部品の一覧）は、境界の歯も見え方の数も通る。**着地の時の確かめは (i) の 3 の相互の対の数（14 → 14）と、新しい file に入口の面の区切りの名指しが無いことで、super:: の名指しは対の数に現れないので (丙) と組で見る。** 判断の記録の状態の表を `face_index.rs` にも写して書く側が自分の写しを読む変化（M19）も全部緑だった（条 P-6.3 に反する形・写しの値が同じ間は出力も変わらない）。着地の時の確かめは (i) の 3 の区間の純度（足した側に区間の定義が 2 度現れない・移動でない足した行が (g) の 85 行だけ）である。写しが誰にも使われない場合（M20）は、clippy が使われない定義として捉える。
5. **見え方の拡大を、どの歯も数えない。** 区間の中だけが使う fn を広げる（M14）・区間の項目と欄を全部広げる（M15・台本の widen の形）・枠を道具の外へ向けた印にする（M16）変化は全部緑だった。本便の見え方の拡大 54 を確かめるのは (i) の 3 の見え方の数である。
6. **実の正本にも凍結 fixture にも無い値を、どの歯も数えない。** 判断の記録の状態の表の 廃止 の名札を変えても（M2）全部緑で、組み立ての出力も同じだった（廃止の記録が実の正本にも入口の面の凍結 fixture にも棚の行の歯の写しにも無い）。これは本便の前から在る穴で、本便は運ばない（値を 1 字も変えないので、移動の正しさには掛からない）。
7. **輪ごと外す弱め方は clippy が捉える。** 状態の行の呼び出しを消すと（M21）、状態の行の関数が誰にも使われない定義になり、clippy が落ちる。入口の面の骨の歯・棚の行の歯・配信先の組み立ての歯も落ちる。
8. **単体の歯のための取り込みは、組み立ての 2 つの形が捉える。** 印を外すと clippy が使われない取り込みとして捉え（M22）、取り込みを消すと単体の歯の区画が組み立たない（M23）。`cargo build` だけでは後者を捉えない（(f) の末尾）。
9. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、本便の後の便 116 は本便の着地を待って直列に運ぶ（設計ノート §5-a の 6）。便 114 は着地済み（main 0d0b08a）なので、本便の前には待つ便が無い。

**撤退条件**（ADR-15 の撤退条件の逐語を引く。閾値は ADR-15 の値、比べる相手は直前の便〔便 114〕の着地後の値、判定は取り込みの審査が行い〔届かないときは持ち主〕、記帳は台帳 f2-648 に行う）。

- (1) 逐語: 責務で割っても受付の断りが減らない。数えるのは取り込みの審査で、台帳 f2-648 へ記帳する。11 本目の便が着地した後の 10 便で、実装の側の file の余地が足りずに受付で断られた回数を数える。3 回に達したら、決定 (2) の層の数と決定 (3) の降ろし方を捨てて測り直す判断の記録を起こす。歯の側の file が原因の断りはここに数えない（本判断は歯の側の境界を決めていない）。— ADR-15 の撤退条件 (1) は 11 本目の便の着地の後の 10 便で数えるので、本便は数えない（本便の後、余地 300 未満の src は 1 本・参考値）。
- (2) 逐語: 上向きの辺が増える。境界の歯が出す上向きの辺の集合を、取り込みの審査が直前の便の一覧と比べる（歯そのものは凍結した一覧との一致しか見ないので、便が辺を増やして一覧も同時に足せば歯は通る。増えたことを捉えるのは、審査が歯の file の差分を読む側である）。11 本の便のどれかの着地後に、上向きの辺が直前の便より増えていたら、決定 (5) の順を捨てて順を組み直す。増える便は計画に 1 本も無いことを写しの file で確かめてあるので、増えたときは実測か計画のどちらかが誤っていたということである。— 本便の直前の値は便 114 の着地後の 0 本、本便の計画値も 0 本（参考値）。数えは 0 のまま。審査は境界の歯の file の差分（凍結した一覧の行の増減）を読み、1 本でも在れば決定 (5) の順を組み直す。凍結した一覧は空のままなので、差分に一覧の行が現れないことが確かめである。本便で字面に現れない上向きの辺（言えないことの 2）を審査が数えられなかったときは (4) に当たる。
- (3) 逐語: 境界が責務で引けない場所が出る。判定するのは取り込みの審査（届かないときは持ち主）で、便を運ぶ席ではない。1 つの file を責務で割ろうとして、どちらの層にも属する部分が残り、割り方を決められないと審査が判じた便が 2 本に達したら、決定 (2) の層の定義を捨てて測り直す。— 区間を ADR-15 決定 (3) の後段の字どおり（正本を読んで面の文脈を組む部分 = context と支度表の読み）に取り、HTML を書く側だけが使う名札・dc・readable_faces・読んだ正本の一覧を書く側に残し、書く側も使う枠 FRAME と部品の一覧 PARTS を読み手に置く割り方は、責務（読み手 = 正本から文脈を組む／書き手 = 文脈から HTML を出す・閉じた一覧は読み手が正本の断りに使う）で引けていると見る（席の裁定・台帳 f2-648 notes 2026-09-23・(b) の 層 4 に置く理由と、その限界・(c)）＝**起草の時点の見立ては数えに入れない（数えは便 109〜114 の審査の裁定で 0 のまま）**。判じるのは取り込みの審査（届かないときは持ち主）で、2 本に達したら決定 (2) の層の定義を捨てて測り直す。
- (4) 逐語: 数えられない回が 2 回を超えたら、本判断の撤退条件の判定を「まだ分からない」に落として持ち主へ渡す。「同じ」とみなすのは（撤退条件の番号・数えられなかった理由・対象の便）の 3 つ組が一致する回である。この 2 回は本判断が自分で置く数であり、規則の表の行 R-7 の繰り返し上限とは別である（行 R-7 が言う 3 つ組は検査の id と判定と対象の道で、本判断が数えるものとは母集団が違う）。— 撤退条件 (4) は、本便の (2) か (3) を審査が数えられなかったときだけ当たる（本便でなりうるのは、(i) の 3 の見え方の数と相互の対の数と super:: と別名の確かめを審査が撃てなかったときの (2)。直前の便までの数えは 0 のまま）。

どの場合も区間は戻さない。戻すと `face_index.rs` の余地が 124 に戻って塞がり、列の残り（便 116）と後続の便の受付の前提が崩れるためである。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7（写しに区間を当てて組み立て、走査し直す）を本便の区間に当てたうえで、次の 7 つを数える。起草役の台本（区間を当てる 1 本と取り込みの塊 2 つと広げる行の表・走査・行数・変異・出力の比較 2 本・受付の再現）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d115-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 52 file・249 辺（下 174・同じ層 75・上 0）、本便の後が 53 file・256 辺（下 179・同じ層 77・上 0）だった（参考値）。消える辺は 3（判断の記録の面と設計ノートの面から入口の面への同じ層 2、入口の面から憲法の段の閉じた一覧への下向き 1）、足す辺は 10（入口の面・判断の記録の面・設計ノートの面から新しい file への同じ層 3、新しい file から面の口への同じ層 1、新しい file から層 1 の 5 file〔部品目録・憲法の段・cursor・入口の棚・yaml〕への下向き 5、配信先の組み立てから新しい file への下向き 1）。
3. **区間の純度と見え方と相互の対と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（一致は 554 行）。移動でない行は、足した側が (g) の 85 行、消した側が (g) の 64 行である。見え方を広げた 54 行は、印を外すと消した側の 54 行と字が一致する（多重集合は同じ字の欄の行を file をまたいで照らすので、file ごとの内訳は広げる行の表と照らして数える）。見え方は src 全体で、道具の中で公開の印（`pub(crate) `）を含む行と、行頭の公開の印（空白の後の `pub `）の行と、再輸出（`pub use` と `pub(crate) use`）の行と、`use super::*;` のほかの super:: の名指しと、`#[cfg(test)]` の直後の取り込みの行を base と比べる（189 → 243・369 → 369・3 → 3・0 → 0・0 → 1・参考値）。道具の中で公開の印の増分 54 は、項目 12 と型 3 と欄 34 と method 5 である。設計ノート §4-d の便 115 の値 31 との差（参考値）は、§4-d の測りが文脈を組む context と支度表の読みを入口の面に残した（文脈の型を組むのも入口の面の中だった）こと、再輸出を挟んだので使う側の名指しを数えなかったことと、台本の widen が struct の欄を型ごとに一律に広げ method を数えなかったことによる。本便は context と支度表の読みを読み手に入れたので、書く側が読む文脈の欄と method と支度表の型と欄が線をまたぐ（拡大が多い向き・§0 の 設計ノートからの逸脱）。**相互に名指す対**（互いを名指す 2 file の対）を走査の辺から数え、base と比べる（14 → 14・新しい file は相互の対に入らない・参考値）。足した行に super:: が無いことも diff で確かめる。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違い 0）。**境界の歯も見え方の数も捉えない 3 つ（別名の取り込み・super:: の名指し・同じ層の中の当て損ない）は、着地の審査で席が次の手順で実測する**（席の裁定・台帳 f2-648 notes 2026-09-23・(h) の言えないことの 2 と 4）: (甲) 相互に名指す対の数が base と同じ（14 → 14）で、新しい file に入口の面の区切りの名指し（crate:: を付けた形でも super:: を付けた形でも）が無い。相互の対の数は走査の辺から数えるので super:: の名指しは数えない（検証役の変異 X8 = 取り残した名を読み手が super:: で名指す形は、対の数 14 のまま (丙) の super:: の数だけが動く）。(甲) は (丙) と組で見る。(乙) 移動でない足した行に `use … as` の別名の取り込みが無い（移動の行の憲法の段の一覧の別名 ce と数の型の変換は base の `face_index.rs` から字を変えずに移ったもので数えない）。(丙) 足した行に `use super::*;` のほかの super:: が無い。あわせて (丁) 見え方の増分に行頭の公開の印 `pub ` が 1 行も無い（行頭 pub の行 369 → 369）ことを数える（見え方の拡大 54 を受けた条件・§0 の 設計ノートからの逸脱）。
4. **出力の byte**: base と本便の後の binary で、(f) の 1 の凍結の旗 16 通りと、`folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check`・`folio check`・`folio inject --check` を撃ち、`diff -r` と `cmp` で比べる。凍結の旗と `--refute` と `--stamp` は書くので、写しに当てる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`（9 file）。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。
7. **前提の便との重なり**: 本便の区間の台本を main 0d0b08a の写しに当てた差分と、初稿の木（main f9e48c6 に便 113 と便 114 の起草役の実測 patch を当てた木）に当てた差分を byte と sha256 で比べる（起草役の実測では一致・50,801 byte）。実測 patch（`~/.local/share/folio2/handoff-2026-09-23/d115-measured.patch`・0d0b08a 基準）は 0d0b08a に `git apply --check` で当たる。

## 2. 範囲

- 入れる: 新しい src の file 1 本（入口の面の読み手・部品の一覧と枠・置き場の定数と判断の記録の状態の表・読んだ中身と文脈の型とその method・読みと数え・支度表の読み）・`face_index.rs` からの字を変えない移動（見え方の印 54 か所のほか）・`face_index.rs` の頭の注 1 行と取り込みの行（単体の歯のための 2 行を含む）・名指しの 3 file（`face_adr.rs`・`face_note.rs`・`site.rs`）の区切り名・入口の区切りの宣言 1 行・境界の歯の file の層の表 1 行。
- 入れない: 入口の面の書く側と面の口 derive と名札の表と単体の歯の区画・判断の記録の面と設計ノートの面と配信先の組み立ての本体・再輸出の行と別名の取り込みと super:: の名指し・区間の中だけが使う項目と欄の見え方・ほかの file の注の中の file 名と道の書き直し・憲法の面の割り・既に在る歯の名や本数や本文の変更・境界の歯の凍結した一覧・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| face_index_read | 新しい src の file | `crates/folio/src/` の下の face_index_read.rs（責務の層 4 面に出す・面の中の割り）。入口の面の読み手（部品の一覧と枠・置き場の定数と判断の記録の状態の表・読んだ中身と文脈の型とその method・読みと数え・支度表の読み）を `face_index.rs` から字を変えずに受ける。使う側が名指す項目 12・型 3・欄 34・method 5 を道具の中で公開にする |
| trim | 取り除き | `crates/folio/src/face_index.rs` から移すものを外し、頭の注 1 行を足し、取り込みを書く側が使うものに絞り、新しい区切りの 16 名と、単体の歯のための部品の一覧（`#[cfg(test)]`）を取り込む（再輸出はしない） |
| rewire | 名指しの行 | 判断の記録の面・設計ノートの面・配信先の組み立ての区切り名を新しい区切りへ向け直す（別名を置かない） |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に face_index_read を層 4 で 1 行足す。凍結した一覧は空のまま。歯の本文は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h)）ので、取り込みの審査は区間の純度と見え方の数と相互の対の数と super:: の無さと歯の名を §1 (i) の 3 の手順で確かめ、床と生成区間の一致を §1 (f) の 2 の命令で撃つ。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる便: 便 114（設計ノートの床の定数を層 1 へ・行 `dm`・**着地済み main 0d0b08a・台帳 f2-648.159**）と便 113（判断の記録の床の定数を層 1 へ・行 `dl`・着地済み main 89c668d・台帳 f2-648.158）。どちらも入口 `main.rs` と境界の歯の file を書き換えたが、本便の区切りの宣言と層の表の行とは同じ行も隣の行も書き換えない（重なり無し）。ほかの着地済みの前提: 便 112（main f9e48c6・台帳 f2-648.157）・便 109（入口の棚の閉じた一覧と設計ノートの状態の表を層 1 の `shelf.rs` へ）・便 108（部品目録の閉じた一覧を層 1 の `catalog.rs` へ）・便 107（正本の cursor を層 1 の `cursor.rs` へ）・便 106（境界の歯）・便 105（入口の面の歯の file の割り）。
- ADR-15 の列の便 116 は、境界の歯の file と入口を書き換えるので**本便の着地を待つ**。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "dn"
title = "入口の面の読み手（部品の一覧 PARTS と枠 FRAME・置き場の定数と判断の記録の状態の表・読んだ中身と文脈の型 Ctx とその method・節 読みと数え〔判断の記録と設計ノートの並びの口・exact・憲法と要件書の札・文脈を組む context〕・節 相談窓口の正本と支度表の読み）を crates/folio/src/face_index.rs から新しい file crates/folio/src/face_index_read.rs へ字を変えずに責務で降ろし、塞がっている face_index.rs の余地を 300 以上へ開ける。これは判断の記録 ADR-15 の決定 (1)(2)(3)(5) が定めた 11 本の列の 10 本目（塞がっている 4 file を開ける 4 本の 3 本目・面の中の割りの 1 本目）で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 115 の行と §1 の 面の中の割り がその中身を持つ。読み手は面の口（face.rs の link・anchor・tier_of）と字面の逃がしを呼ぶので、ADR-15 決定 (3) の後段のとおり層 1 へ降ろさず同じ責務の層 4 面に出す の中で割る。区間が区間の外に名指すのは層 1 の 5 file（部品目録・憲法の段・正本の cursor・入口の棚・yaml）と層 4 の face.rs だけで、書く側の名は名指さないので、層が上がる辺は 0 本のまま増えず、相互に名指す対も増えない。書く側と使う側が名指す項目 12・型 3・欄 34・method 5 を道具の中で公開へ広げ（見え方の拡大 54）、区間の中だけが使うものは私のまま残す。面の口 derive と読んだ正本の一覧と名札の表と部品の名札の口と読める面の数と HTML を書く側は face_index.rs に残る。再輸出の行も別名の取り込みも super:: の名指しも 1 行も置かず、判断の記録の面 face_adr.rs と設計ノートの面 face_note.rs と配信先の組み立て site.rs は新しい file を直に名指す。face_index.rs は頭の注 1 行と取り込みの行（単体の歯だけが使う部品の一覧の取り込みは #[cfg(test)] に限る）を、face_adr.rs と face_note.rs と site.rs は区切り名の行だけを書き換え、入口 main.rs は区切りの宣言 1 行を足し、ほかの行は 1 字も動かない。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の層の割り当ての表に face_index_read を層 4 で 1 行足す（凍結した一覧は空のまま・歯の本文は変えない）。歯は足さず消さず本文も変えず、face_index.rs の単体の歯 4 本も名と本文が変わらず、入口の面・判断の記録の面・設計ノートの面と配信先の組み立ての byte も、束・印・門・索引・部品の検査・欄の決まりの生成区間・床の判定と凍結の旗の出力の byte も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR4", "FR16", "FR9", "FR7"]
section = "1"
write-set = ["+crates/folio/src/face_index_read.rs", "-crates/folio/src/face_index.rs", "crates/folio/src/face_adr.rs", "crates/folio/src/face_note.rs", "crates/folio/src/site.rs", "crates/folio/src/main.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/face_index.rs", "crates/folio/tests/face_index_shelf.rs", "crates/folio/tests/face_index_sheet.rs", "crates/folio/tests/face_adr.rs", "crates/folio/tests/face_note.rs", "crates/folio/tests/site.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test face_index", "cargo nextest run -p folio --test face_index_shelf", "cargo nextest run -p folio --test face_index_sheet", "cargo nextest run -p folio --test face_adr", "cargo nextest run -p folio --test face_note", "cargo nextest run -p folio --test site", "cargo nextest run -p folio --bin folio face_index_document_table_has_four_ids_and_rejects_others", "cargo nextest run -p folio --bin folio face_index_stop_anchor_resolves_only_the_face_anchors", "cargo nextest run -p folio --bin folio face_index_counts_readable_faces", "cargo nextest run -p folio --bin folio face_parts_are_all_allowed_on_the_index_face", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した空の一覧とちょうど一致する）、--test face_index が走らせる入口の面の骨の歯の全部が緑（凍結 fixture〔tests/fixtures/face/ の入口の面〕との byte 一致・実の正本の面の部品の検査・逐語と件数の census・check の 3 値・導出できない入力・凡例の並び・読む順番の行き先を含む・本数と名は base の cargo nextest list と同じ）、--test face_index_shelf が走らせる棚の行の歯の全部が緑（判断の記録と設計ノートの card の在る側と無い側・状態が表に無い・番号の重なり・id と file 名のずれ・棚の外が動かない・本数と名は base と同じ）、--test face_index_sheet が走らせる支度表の節の歯の全部が緑（支度表の在る側と無い側の census・導出できない 4 つ・付録の型を棚の付録に解く・承認の行・相談窓口からの 1 周・本数と名は base と同じ）、--test face_adr が走らせる判断の記録の面の歯の全部が緑（凍結 fixture との byte 一致と隣の記録へのリンクを含む・本数と名は base と同じ）、--test face_note が走らせる設計ノートの面の歯の全部が緑（凍結 fixture との byte 一致と棚の順の隣へのリンクを含む・本数と名は base と同じ）、--test site が走らせる配信先の組み立ての歯の全部が緑（凍結 fixture との byte 一致と実の正本の各面〔判断の記録と設計ノートの面を含む〕の検査を含む・本数と名は base と同じ）、face_index.rs の単体の歯 4 本（棚の文書の表の照らし・読む順番の行き先の解決・読める面の数・部品の一覧が入口の面に置けること）がそれぞれ緑、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い・単体の歯のための取り込みは歯の区画の組み立てでだけ使われる）で、workspace の nextest が全部緑（本数と名の一覧は base の cargo nextest list と同じ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
