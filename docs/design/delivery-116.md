# 設計: 便 116 — 憲法の面の読み手（条と文脈の型・読みと検査・欠番・関係の欄・改訂の段の読み）を `face_constitution.rs` から新しい face_constitution_read.rs（層 4 面に出す・面の中の割り）へ字を変えずに責務で降ろし、塞がっている `face_constitution.rs` の余地を 300 以上へ開ける（参考値・base main b81ae12）（出力不変・FR4 / FR7）

- 要件: FR4（入口・憲法・要件書の面を 1 つの生成器と 1 組の design token で出す・第 1.31 版）/ FR7（組み立てて、見せる・第 1.31 版）。憲法の面の読み手は、憲法の面（FR4）の文脈（段の順・条・参照の先）を正本 3 file から組み、正本の値域と数を検査し、欠番と関係の欄のリンクと改訂の段を返す。本便はどの要件の振る舞いも変えない。FR7 を挙げるのは、配信先の組み立てが憲法の面を 1 枚含み、その凍結 fixture との byte 一致を verify が名指すので、組み立ての受入基準もこの便の 振る舞い不変 の対象になるからである。
- 条: P-2.1（人が読むページはすべて 1 つの生成器から出力する）/ P-4.1（検査・生成が実行できなかった結果を異常なしとして扱わない）/ P-6.1（各文書の正本は YAML 1 file であり、人が読むページはそこから逐語で生成する）/ P-7.2（廃止は状態で表し、番号は空けたままにする）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-10.2（生成物どうしの突き合わせを唯一の合格判定にしない）/ N-3.1（規則の例外機構を足さない）/ P-5.4（台帳は規則を置かない）
- 出所: 判断の記録 **ADR-15**（実装の区切りの境界は責務で決め、器の行数の上限を境界の理由にしない・発効 2026-09-23 10:33 JST・持ち主の承認・逐語 承認する・PR #269）の決定 (1)(2)(3)(4)(5)（とくに決定 (3) の後段 **切り出す中身が面の口を呼ぶなら、下の層へ降ろさず同じ層の中で割る**）と、その材料の設計ノート `docs/design/module-map-2026-09-23.md` の §1 の 面の中の割り・§2 の `face_constitution.rs` の行・§4-a と §4-b の **便 116 の行**・§4-d（便 116 の見え方の拡大）・§5・§5-a の 4（再輸出を挟んだ名指しは見えない）と 5（区間の外に定義がある名の列挙）・§6（便 116 の後に余地 300 未満の src が 0）・§7・§8 の b-1-4（便 115・116 の新しい file を層 4 に置き、決定 (3) に面の中の割りを書き分ける）。本便は ADR-15 が定めた 11 本の列の 11 本目（最後）で、塞がっている 4 file を開ける 4 本（便 113〜116）の 4 本目、面の中の割りの 2 本目である。層が上がる辺は便 112 の後に 0 本で、本便は 0 本のまま運ぶ。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 `do` が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は `+`・行が減る file は `-`）。**新しい dir は作らず、file も消さない。**
- 門: 本便は `design-intent/` の下の正本を 1 file も読み書きしない（書き換えるのは `crates/folio/src/` の 3 本と `crates/folio/tests/modules.rs` の 1 本だけで、verify が名指す歯の file 3 本〔憲法の面と face の命令の共通の歯・憲法の面の枡と欠番と改訂来歴の歯・配信先の組み立ての歯〕は本文を変えずに write-set に入る）。**起草役が write-set 7 本をそのまま渡して実測し（base の binary と本便の後の binary の両方）、天井の門が 0（通す・断りの字は 設計文書の正本を書き換えない便）を返すことを確かめた**（2026-09-23・base は下の行が名指す木）。したがって本便は規則の表の開発規律行 D-12 の裁定を要しない。受付の手順はふだんどおり preflight → dispatch。
- 設計ノートからの逸脱: 設計ノート §4-b の便 116 の行は、ADR-15 の起草役の台本の測り（区間 = 条と文脈の型と、節 読みと検査 の全部〔文脈を組む context・値域の突き合わせ・数の突き合わせ・段の名を繋ぐ口・段ごとの条の数・札の凡例・章の名〕と、欠番と関係の欄と改訂の段の読み。`face_constitution.rs` に新しい区切りの glob の再輸出を置く）だった。本便は 3 点で違う。(1) **区間を ADR-15 決定 (3) の後段の字どおりに取る**: 正本を読んで面の文脈を組む部分（context と、正本の値域と数の突き合わせ）と、正本から節の中身を組む読み（欠番・関係の欄のリンクの列・改訂の段）を区間に入れ、HTML を書く側だけが使う名札と数の口 4 つ（段の名を繋ぐ口・段ごとの条の数・札の凡例の 1 行・章の名の表）は `face_constitution.rs` に残す（(b) の 1・(c) の 1 行目）。(2) **再輸出も別名の取り込みも置かず、書く側が新しい file を直に名指す**（再輸出を挟んだ名指しは走査に見えない＝設計ノート §5-a の 4 の穴を避ける向き・便 113〜115 と同じ席の裁定）。(3) 見え方の拡大は §4-d の 13 でなく **21** である。§4-d の 13 は台本が top-level の項目だけを数えた値で（欄と method は広げたが数えていない）、本便は欄も数える。項目だけで比べると本便は 8 で、§4-d より少ない（名札と数の口 4 つを残し、値域の突き合わせは私のまま・(i) の 3）。§4-b の行は列の見積の材料で、判断の記録 ADR-15 が縛るのは層・上向きの辺の一覧（0 のまま）・便の本数と順・既存の歯を動かさないことである。この逸脱は便 115 と同じ向きの席の裁定 4 つで受けた（区間の取り方と撤退条件 (3)・見え方の拡大 21・憲法の段の一覧の呼び名 ce の 1 行・歯が捉えない 3 つの審査の手順・取り込みの審査・台帳 f2-648 notes 2026-09-23）。(1) の区間は、読み手 = 正本から文脈と節の中身を組む／書き手 = 文脈から HTML を出す の責務で引けている（(h) の撤退条件 (3)）。関係の欄からリンクの字面を組む列は、設計ノート §1 の 面の中の割り の定義（面の口を呼び、関係の欄からはリンクも組む）の内なので読み手に入る。席の裁定の 書く側も使う閉じた一覧は読み手に置く は、本便の区間が閉じた一覧（部品の一覧・章の帯の表・枠・値の枡の閉じた表・前の裁定の区切り）を 1 つも使わないので当てはめが空で、閉じた一覧は全部、それを使う書く側に残る（(b) の 層 4 に置く理由と、その限界）。(3) の見え方の拡大 21 は、§4-d の 13 が項目だけの数で項目どうしでは 8 < 13 であり、増分が全部 道具の中で公開 の印で、条と文脈と改訂の段の型の欄が読み手と書き手の線をまたぐのは読み手 / 書き手の割りに固有のものなので受けた（条件: 増分に行頭の公開の印 `pub ` が 1 行も無いことを (i) の 3 で数え、着地の審査で席が見る）。
- 改訂: 改訂 b（2026-09-23・起草役）= 便 115 の着地（main b81ae12・台帳 f2-648.160）を受けて、写しを実物の main b81ae12 に取り直して全部を測り直し、表題の base・§0 の前の便・§1 (a) の辺の出発点・§1 (b) の 1 の rustfmt の扱い・§1 (f) の `--stamp` の標準出力の注と rustfmt の内訳・§1 (g) の src 全体の行数・§1 (h) の言えないことの 4 と 8 と撤退条件 (1)・§1 (i) の 3 の (甲) と 7・§5 の依存を実物の形に直した。区間の台本は無修正で当たり、区間の差分は前の木（main f9e48c6 + 便 113〜115 の起草役の実測 patch）と index 行まで同じである（22,424 byte・sha256 4fa6b732…f165 が一致）。参考値は全部同じで、src 全体の行数だけが便 113〜115 の注の書き直しで 1 減った（base 26,305 → 26,304・本便の後 26,318 → 26,317）。便 113〜115 の実物と patch の差は頭の注の 15 行だけで、本便の区間に掛からない。区間・write-set・verify・size・done・上向きの辺（0 のまま）・辺の計・相互の対（14 → 14）・歯の名の一覧（base と差 0）・見え方（243 → 264・行頭 pub 369 のまま・再輸出 3・super:: 0・`use … as` 7 → 8）・余地の表・出力の byte・凍結の旗の 16 通りの一致・門は 1 つも違わなかった。(d) の変異の表は前の木での実測で、測り直しの記録に撃ち直しは無い（区間の差分が index 行まで同じ）。あわせて便 115 の改訂 c の形に揃えた（(甲) は super:: の名指しを数えないので (丙) と組で見る・新しい file に rustfmt を丸ごと当てない・撤退条件 (1) の当てはめに本便の後の余地 300 未満の src の本数）。実測 patch は `~/.local/share/folio2/handoff-2026-09-23/d116-measured.patch`（b81ae12 基準・23,247 byte・sha256 038c92af…bde9・b81ae12 に `git apply --check` が通る）、測り直しの記録は同じ dir の `d116-remeasure.md`。
- 改訂: 改訂 b の前段（2026-09-23・起草役）= 席の裁定 4 つ（台帳 f2-648 notes 2026-09-23）を文面として先に入れた = §0 の 設計ノートからの逸脱の段（閉じた一覧の当てはめが空であること・関係の欄のリンクの列が面の中の割りの定義の内であること）・(h) の撤退条件 (3) の当てはめ・(i) の 3 の §4-d との数え方の違いと (乙) と (丁)。数・区間・write-set・verify・size は変えない。起草の後に便 113 が着地した（main 89c668d・台帳 f2-648.158）。起草役は実物の 89c668d に便 114・115・116 の実測 patch がこの順に無修正で当たることを確かめた（便 113 の着地の差は `adr.rs` と floor_adr.rs の注の行だけで、本便の区間の file に掛からない）。全部の測り直しは便 115 の着地の後の改訂 b で行う。
- 前の便: **便 115 着地済み（main b81ae12・入口の面の読み手を層 4 の face_index_read.rs へ降ろした・行 `dn`・台帳 f2-648.160）・重なり無し**。便 114 も着地済み（main 0d0b08a・設計ノートの床の定数を層 1 の floor_note.rs へ・行 `dm`・台帳 f2-648.159）、便 113 も着地済み（main 89c668d・判断の記録の床の定数を層 1 の floor_adr.rs へ・行 `dl`・台帳 f2-648.158）、便 112 も着地済み（main f9e48c6・行 `dk`・台帳 f2-648.157）。起草役は初めに main f9e48c6 に便 113〜115 の起草役の実測 patch をこの順に当てた木（写しの commit 99300a8・3 本とも無修正で当たった）で測り、便 115 の着地を受けて写しを b81ae12 の実物に取り直した。初稿の木と実物の b81ae12 の違いは頭の注の 15 行だけで（`adr.rs`・`note.rs`・`face_index.rs` の 1 行ずつと floor_adr.rs・floor_note.rs・face_index_read.rs の 4 行ずつ）、本便の区間の file（憲法の面の生成器・入口の区切りの宣言・境界の歯の層の表）には掛からない。本便の区間を台本で当てた差分は 2 つの木で index 行まで同じである（改訂 b の行）。便 113〜115 の区間と本便の区間は、同じ file では入口 `main.rs`（区切りの宣言）と境界の歯の file（層の表の行）の 2 本で重なるが、同じ行は書き換えない（重なり無し）。本便の宣言と層の表の行は、憲法の面の区切りの行の次に置く（字の順）。**この契約の数はすべて b81ae12 の実測（参考値）である。base は main b81ae12**（規則の表の行 D-13）。

## 1. 設計

### (a) いま起きていること（実測・数え方は (i)・参考値は base での測り・ADR-15 決定 (1)(3)(5)）

憲法の面の生成器 `crates/folio/src/face_constitution.rs`（層 4 面に出す）は、同じ層の中の 2 つの責務を同居させている。

1. **正本を読んで面の文脈と節の中身を組む側**（base の 93〜120・156〜271・504〜529・694〜746・927〜958 行の 5 区間・255 行）。条 1 本の型と文脈の型（段の順・条・要件書の id と題の表・規則の表の行 id の表）と、条 id から条を引く method／節 読みと検査（正本 3 file から文脈を組む context・読んでいる置き場の憲法の値域が組み立てた版と集合で一致するかの突き合わせ・meta の数が数えた条の数と一致するかの突き合わせ）／条の id の列の欠番／関係の欄から要件書・規則の表の行・条・節への参照を解いてリンクの列にする口／改訂の段 1 つの読み（段の番号・担当・持ち主の手番か・what の前後・名指す条）の型とその口。
2. **文脈から HTML を書く側**。部品の一覧 PARTS・章の帯の表 BANDS・枠 FRAME・部品の名札の口 dc・面の口 derive（正本を読み込み、上の読み手を呼び、下の書き手を順に呼ぶ命令の口の本体）・名札と数の口 4 つ（段の名を繋ぐ口・段ごとの条の数・札の凡例の 1 行・章の名の表）・節 骨格 以下（head・表紙・発効かの判定・章の見出し・目次・北極星・読み方・段の章・条の行・数値の表と値の枡と裁定の枡と状態の札・改訂の章・用語集・出所・承認欄・末尾）・単体の歯の区画 1 本。

正規化で 1,295 行・余地 **205**（参考値）で、器の受付が size M（見積 300）の便を断る最後の 1 file（便 115 の後）である。

読み手の側の名を `face_constitution.rs` の外から名指す file は無い。`face.rs`（層 4）と配信先の組み立て `site.rs`（層 5）は面の口 derive だけを呼び、`crates/folio/tests/` の歯は 1 名も名指さない（歯は命令を撃つ）。したがって名指しの行を書き換える file も無い。

**区間の外に定義がある名の列挙**（ADR-15 決定 (5) の太字・設計ノート §5-a の 5）: 区間が使う名のうち区間の外に定義があるのは、憲法の値域の閉じた一覧（値域の表 ENUMS と段の型・層 1 `constitution_enums.rs`）・正本の cursor と字面の逃がし（R・X・esc・層 1 `cursor.rs`）と、**面の口**（段の名札の型 Tier・anchor・section_anchor・split_dash・tier_label・tier_of・層 4 `face.rs`）だけである。std は使わない。書く側の名（部品の一覧・章の帯の表・枠・dc・面の口 derive・名札と数の口・書き手の関数・値の枡の閉じた表・前の裁定の区切り）を区間は 1 つも名指さない＝新しい file から `face_constitution.rs` への辺は生えず、相互に名指す対は増えない。確かめ方は、写しに区間を見え方を広げずに切り出し、元の取り込みのまま `cargo build --all-targets` を撃つことである（誤りは書く側の解けない名だけ・新しい file の側は使われない取り込みの警告だけだった）。

逆向き（区間の中に定義があり区間の外が使う名）は、`face_constitution.rs` の書く側だけが使う 8 名（条 1 本の型・文脈の型・改訂の段の型・context・数の突き合わせ・欠番・関係の欄のリンクの列・改訂の段の読み）である。

便 112 の後の層が上がる辺は **0 本**（参考値）で、境界の歯の file の凍結した一覧は空である。便 113〜115 もその 0 本のまま着地した（base b81ae12 で 0 本・参考値）。

### (b) 直す先 — 憲法の面の読み手を新しい face_constitution_read.rs（層 4 面に出す）へ字を変えずに移す（ADR-15 決定 (3) の後段・設計ノート §1 の 面の中の割り）

**1. 新しい file** `crates/folio/src/` の下の face_constitution_read.rs へ、`face_constitution.rs` の 5 区間（(a) の 1 の 255 行）を字を変えずに、元の順のまま移す。区間どうしの間には空行を 1 行ずつ置く。新しい file に rustfmt を丸ごと当てない（本便の区間は rustfmt の差分が 0 で、当てても字は変わらないが、字を変えない移動の確かめを rustfmt の出力に頼らない・(f) の末尾）。rustfmt の形にそろえるのは取り込みの行だけである。節の見出しの注 読みと検査 は、その節の 3 つの口（context・値域の突き合わせ・数の突き合わせ）と一緒に移る。新しい file の頭の注 4 行は、何をどこから降ろしたか・面の口 derive と HTML を書く側と枠と名札は `face_constitution.rs` に残ること・面の口と字面の逃がしを呼び関係の欄からリンクの列も組むので層 1 に置かないこと・見え方だけを広げたことを書く（移した注に置き場を指す file 名は無い）。取り込みは 3 行（憲法の段の一覧・cursor・面の口）。新しい file は 268 行・正規化 268 行（参考値）。

`face_constitution.rs` に残るのは (a) の 2 である。頭の注に 2 行（読み手を降ろした先・面の口と HTML を書く側はここに残すこと）を足し、面の口の取り込みの括りから書く側が使わなくなった 4 名（section_anchor・split_dash・tier_label・tier_of）を外し（括りの 2 行を 1 行に書き換える）、新しい区切りの 8 名の取り込み（rustfmt の形で 3 行）を足す。**再輸出の行は置かない**（下の 3）。

**2. 単体の歯は動かない。** `face_constitution.rs` の単体の歯 1 本（部品の一覧が憲法の面に置けること）は `face_constitution.rs` に残り、名も本文も変わらない（歯の名は `face_constitution::face_constitution_tests::` のまま）。歯が `use super::*` で受ける部品の一覧と部品目録の型は書く側に残るので、**単体の歯のための取り込みは要らない**（便 115 と違い、`#[cfg(test)]` の取り込みを 1 行も足さない）。移す区間に単体の歯の区画は無い。

**3. 見え方の拡大は 21 か所**（ADR-15 決定 (3) が契約に書けと言う数・参考値）。写しに区間を見え方を広げずに当てて組み立て、組み立ての誤り（私の項目・欄を別の file から名指した）で要ると言われたものだけを広げた。

| 広げたもの（私 → 道具の中で公開） | 数 | 理由 |
| --- | ---: | --- |
| 型: 条 1 本・文脈・改訂の段 1 つ | 3 | 書く側の関数の引数と戻り値に現れる（面の口 derive が文脈を受け、書き手が条と改訂の段を受ける） |
| fn: context・数の突き合わせ・欠番・関係の欄のリンクの列・改訂の段の読み | 5 | 書く側が呼ぶ（面の口 derive が context と数の突き合わせを、読み方の章が欠番を、条の行が関係の欄を、改訂の章が改訂の段の読みを呼ぶ） |
| 欄: 条 1 本の 5（正本の cursor・id・題・段の鍵・段の名札）・文脈の 2（段の順・条）・改訂の段の 6（番号・担当・持ち主の手番か・what の前・後・名指す条） | 13 | 書く側（表紙・目次・読み方・段の章・条の行・改訂の章・末尾）が読む |

私のまま残すもの: 値域の突き合わせ（context だけが呼ぶ）・文脈の要件書の id と題の表と規則の表の行 id の表（関係の欄のリンクの列だけが読む）・条 id から条を引く method（関係の欄と改訂の段の読みだけが呼ぶ）。

行頭の公開の印（`pub `）は 1 つも足さない。**再輸出の行（`pub use` と `pub(crate) use`）も、super:: の名指しも 1 行も足さず、別名の取り込みは、書く側と同じ字の憲法の段の一覧の呼び名 ce の 1 行（取り込む区切り名が字面に出るので走査に見える）のほかは足さない。** 起草役は src 全体で、道具の中で公開の印を含む行が 243 → 264（差 21 がちょうど上の表の 21）、行頭の公開の印の行が 369 → 369、再輸出の行が 3 → 3、`use super::*;` のほかの super:: の名指しが 0 → 0、`#[cfg(test)]` の直後の取り込みの行が 1 → 1（便 115 の 1 行・本便は足さない）であることを数えた（参考値・数え方は (i) の 3）。設計ノート §4-d は便 116 を 13 と数えた。差の出どころは (i) の 3 に書く。

**4. 名指しの行だけを書き換える file**。

| file | 層 | 書き換える行 |
| --- | --- | --- |
| `main.rs` | 5 | 区切りの宣言 1 行（並びは字の順・憲法の面の宣言の次） |

**ほかの file は 1 字も動かない。** 読み手の名を外から名指す file は無いので（(a)）、名指しの行を向け直す file も無い。

**順の理由**（ADR-15 決定 (5)）: 本便は層をまたいで使われるものを降ろす 7 本（便 106〜112）の後に運ぶ、塞がっている file を開ける 4 本の 4 本目である。先に便 107 が正本の cursor と字面の逃がしを、便 108 が部品目録の閉じた一覧を層 1 へ降ろしたので、読み手を層 4 に切り出しても、読み手が名指す先は層 1 の 2 file と層 4 の `face.rs` だけで、上の層を名指さない（区間の外の名は (a) の列挙）。

**層 4 に置く理由と、その限界**。読み手は面の口（段の名札・anchor・節の anchor・what の前後の割り）と字面の逃がしを呼び、関係の欄からリンクの列（HTML の字面）も組む。層 1 の定義（正本の byte を型に直す）に当てはまらないので、ADR-15 決定 (3) の後段のとおり**同じ層 4 の中で割る**。向きの決まりは同じ層を許すので、塞がりは開き、上向きの辺は増えない。層 0〜3 に置くと、読み手から層 4 の `face.rs` への上向きの辺が生え、層 5 に置くと、層 4 の `face_constitution.rs` から読み手への上向きの辺が生える（(d) の変異でどれも境界の歯が捉える）。**読み手（正本を読んで文脈と節の中身を組み、型と字面の列で返す）と書き手（文脈から HTML を書く）の割り方は、責務で引けていると見る**（席の裁定・台帳 f2-648 notes 2026-09-23・便 113〜115 と同じ向き）。面の口 derive は、正本を読み込んで読み手と書き手を順に呼ぶ命令の口の本体なので `face_constitution.rs` に残す（ADR-15 決定 (3) の 元の file には命令の口の本体だけを残す）。関係の欄のリンクの列は HTML の字面を返すが、正本の関係の欄の id を文脈の表（要件書・規則の表・条）と節の名に解き、解けなければ誤りで止まる読みなので読み手に置く（設計ノート §1 と §4-b の便 116 の行も 関係の欄 を読み手に数える）。名札と数の口 4 つは文脈を読むだけで正本を読まず、書く側だけが使う（面の名札と見出しの数と凡例の字）ので書く側に残す（便 115 が 読める面の数 と 名札の表 を書く側に残したのと同じ向き）。閉じた一覧（部品の一覧・章の帯の表・枠・値の枡の閉じた表・前の裁定の区切り）は読み手が 1 つも使わないので、読み手に置く理由が無い（便 115 は支度表の読みが枠の正本の名を読んだので枠と部品の一覧を読み手に置いた）。判定は取り込みの審査が行う（(h) の撤退条件 (3)）。

### (c) 採らなかった形（ADR-15 決定 (3)(4)）

| 形 | 退けた理由 |
| --- | --- |
| 設計ノート §4-b の測り（ADR-15 の起草役の台本）の区間: 節 読みと検査 を丸ごと（段の名を繋ぐ口・段ごとの条の数・札の凡例・章の名を含む）移す | 名札と数の口 4 つは正本を読まず、書く側だけが使う（表紙と章の見出しの数・札の凡例の HTML の 1 行・目次の章の名）。読み手に入れると割りの線が責務と食い違い、見え方の拡大が項目で 4 増える。本便はそれらを書く側に残す（便 115 が 読める面の数 と 名札の表 を書く側に残したのと同じ向き） |
| `face_constitution.rs` に新しい区切りを丸ごと再輸出する 1 行（glob）を置く（台本の形） | 辺の数は同じ 0 に着き、取り込みの行は 3 行から 1 行に減るが、再輸出の行が 1 つ増え（3 → 4）、新しい区切りの公開の名が全部 `face_constitution` の名でも道具の全体から名指せるようになる。走査は字面を見るので、再輸出を挟んだ名指しの真の行き先が辺に現れない（設計ノート §5-a の 4）。(d) の変異で、この形はどの歯も捉えないことを確かめた |
| 書く側が新しい区切りを別名で取り込む（区切りの名を別の名に置き換えて取り込み、その名から項目を取り込む） | 走査は `crate::` の後の区切り名を見るので辺は同じだが、本文から名指している file が読めない。(d) の変異で、この形は歯も見え方の数も相互の対も捉えないことを確かめた（捉えるのは diff を読む審査だけ） |
| 値域の突き合わせを `face_constitution.rs` に残す（context だけが呼ぶ） | 読み手の context が書く側の口を名指すので、書く側と読み手が互いに名指す対が 1 つ増える（相互の対 14 → 15）。(d) の変異で、この形は歯も見え方の数も捉えず、相互の対の数だけが捉えることを確かめた |
| 改訂の段の型を `face_constitution.rs` に残し、改訂の段の読みだけを読み手へ | 同上（読み手の改訂の段の読みが書く側の型を名指し、相互の対が 14 → 15 になる・(d) の変異） |
| 関係の欄のリンクの列を書く側に残す（HTML の字面を返すから） | 関係の欄の id を文脈の表に解き、解けなければ誤りで止まる読みが書く側に残る。文脈の要件書の id と題の表と規則の表の行 id の表を書く側が読むので、見え方の拡大が欄で 2 増え、条 id から条を引く method も広がる。設計ノート §1 と §4-b も関係の欄を読み手に数える |
| 発効かの判定（meta の状態を読んで発効かを返す口）も読み手へ | 表紙と承認欄だけが使う 1 行の判定で、同じ状態の表を head と承認欄（書く側）も読む。読み手に入れても書く側の文書の状態の読みは残り、見え方の拡大が 1 増える |
| 閉じた一覧（部品の一覧・章の帯の表・枠・値の枡の閉じた表・前の裁定の区切り）も読み手へ（書く側も使う閉じた一覧は読み手に置く向き） | 本便の区間はどれも使わない（書く側だけが使う）。読み手に移すと書く側が読み手の名を余計に名指し（枠・章の帯の表・値の枡の閉じた表・前の裁定の区切りは私なので見え方の拡大も要る）、単体の歯は部品の一覧を `#[cfg(test)]` の取り込みで受けることになる。読み手と書き手の両方が使う閉じた一覧は本便に無い |
| 新しい file を層 1（読む）に置く | 読み手が層 4 の `face.rs`（面の口）を名指すので上向きの辺が生える（ADR-15 決定 (3) の後段が退けた形・(d) の変異で境界の歯が落ちる） |
| 区間の私の項目と欄と method を全部広げる（台本の widen の形） | 区間の中だけが使う口と欄と method まで道具の全体から名指せるようになる。(d) の変異で、どの歯も捉えず見え方の数だけが動くことを確かめた |
| 境界の歯の一覧に、無効化の旗や 今回だけ の口を置く | 条 N-3.1 が拒む。歯 2 は集合の一致を見るので、辺を足しても減らしても落ちる |

### (d) 歯の土台と変異（P-10.1・P-10.2）

本便は新しい凍結の土台（fixture）を作らない。使うのは既に在る土台と歯である。歯の本数と名の正本は `cargo nextest list` の出力で、この契約には写さない。

- `crates/folio/tests/face.rs`: 憲法の面と face の命令の共通の歯。凍結 fixture（`tests/fixtures/face/` の憲法の面）との byte 一致・実の正本の面の部品の検査・正本を直に読んで数えた逐語と件数の census（生成側から独立した物差し）・check の 3 値・導出できない入力・組み立てた版とのずれ（読み手の値域の突き合わせを通る）・meta の数のずれ（読み手の数の突き合わせを通る）・札の凡例・数えの字・数値の表の種別の凡例を測る。読み手の文脈（段の順・条・関係の欄・改訂の段）の 1 欄の違いが面の byte に出れば落ちる。
- `crates/folio/tests/face_constitution.rs`: 憲法の面の歯。数値の表の値の枡と裁定の枡・欠番の行と、正本を直に読んだ独立の欠番の数え（読み手の欠番の口を通る）・改訂来歴の前の文・用語集の欄の名前の節・機構の札を、実の正本とその写しで測る。
- `crates/folio/tests/site.rs`: 配信先の組み立ての歯。凍結 fixture との byte 一致と、実の正本の各面（憲法の面を含む）の検査を測る。
- 単体の歯 1 本（`face_constitution.rs` の区画・`--bin folio` で名指す）: 部品の一覧が憲法の面に置けること。
- 境界の歯 2 本（(e) の 1）。

起草役の変異の実測（写しの repo・workspace の nextest を 1 回撃って落ちた歯を verify の行へ振り分け〔組み立たないときは verify の行を 1 本ずつ撃ち〕、clippy を撃ち、変異の binary で実の design-intent に `folio build --write`〔23 file を base の binary の出力と `diff -r`〕と `folio check` を撃って比べ、独立の走査で上向きの辺と相互の対を数えて戻した・参考値）。verify の列の番号は契約表の verify の並び（1 境界の歯・2 憲法の面と face の命令の共通の歯・3 憲法の面の歯・4 配信先の組み立て・5 単体の歯・6 clippy）である。落ちの数は落ちた歯の本数である。

| 変異 | 1 | 2 | 3 | 4 | 5 | 6 | workspace | 出力（build・check） | 走査の上向き | 相互の対 | 見え方（公開の印の行・行頭 pub・再輸出・super::・cfg(test) の取り込み） |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | ---: | ---: | --- |
| M1 面の文脈の欄を 1 つ落とす（context が規則の表の行 id を文脈の rule_ids へ入れない） | 緑 | **落ち 12** | **落ち 14** | **落ち 9** | 緑 | **落ち** | 落ち 58 | 差（生成が止まる）・一致 | 0 | 14 | 264・369・3・0・1 |
| M2 面の文脈の欄の中身を 1 つ落とす（context が要件書の目標の節を reqs へ入れない・関係の欄は目標を名指さない） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M3 改訂の段の読みの値を反転（持ち主の手番の判定を逆に） | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 落ち 3 | 差・一致 | 0 | 14 | 264・369・3・0・1 |
| M4 値域の突き合わせを弱める（数の一致を 超えない に） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M5 数の検査を弱める（meta の counts が数えた数より多いのを通す） | 緑 | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M6 関係の欄の節の群を読まない（sections の鍵を読み違える） | 緑 | **落ち 1** | 緑 | **落ち 1** | 緑 | 緑 | 落ち 3 | 差・一致 | 0 | 14 | 264・369・3・0・1 |
| M7 欠番の並びを逆に | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M8 新しい区切りを層 0 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M9 新しい区切りを層 1 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M10 新しい区切りを層 2 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M11 新しい区切りを層 3 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M12 新しい区切りを層 5 に置く | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M13 層の表から新しい区切りの行を消す | **落ち 1** | 緑 | 緑 | 緑 | 緑 | 緑 | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M14 層 5 に置き、書く側の取り込みを super:: の名指しに替える | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・1・1 |
| M15 glob の再輸出 1 行（ADR-15 の起草役の台本の形） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 265・369・4・0・1 |
| M16 別名の取り込み（use crate::face_constitution_read as read と use read::{…}） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M17 区間の私の fn（enum_skew）を道具の中で公開へ | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 265・369・3・0・1 |
| M18 区間の top-level と欄と method を全部道具の中で公開へ（台本の widen の形） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 268・369・3・0・1 |
| M19 文脈の型 Ctx を pub へ（道具の外へ向けた印） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 14 | 263・370・3・0・1 |
| M20 区間の当て損ない（enum_skew を face_constitution.rs に取り残し、文脈の組み立てが face_constitution を名指す） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 15 | 265・369・3・0・1 |
| M21 区間の当て損ない（改訂の段の型 Step を face_constitution.rs に取り残し、改訂の段の読み step が face_constitution の Step を名指す） | 緑 | 緑 | 緑 | 緑 | 緑 | 緑 | **全部緑** | 一致・一致 | 0 | 15 | 264・369・3・0・1 |
| M22 同じ定義を 2 か所（face_constitution.rs に missing_numbers の写しを置いて自分の写しを読む） | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち** | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M23 同じ定義を 2 か所（face_constitution.rs に missing_numbers の写し・誰も使わない） | 緑 | 緑 | 緑 | 緑 | 緑 | **落ち** | **全部緑** | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M24 数の検査を輪ごと外す（derive の check_counts の呼び出しを消す） | 緑 | **落ち 1** | 緑 | 緑 | 緑 | **落ち** | 落ち 1 | 一致・一致 | 0 | 14 | 264・369・3・0・1 |
| M25 新しい区切りが上の層の名を再輸出する（site の run） | **落ち 1** | 緑 | 緑 | 緑 | 緑 | **落ち** | 落ち 1 | 一致・一致 | 1 | 14 | 265・369・4・0・1 |

出力の列は、変異の binary と base の binary で実の design-intent に `folio build --write` と `folio check` を撃ち、配信先の 23 file と、標準出力と標準エラーと終了コードを比べた結果である。見え方の列は src 全体の 5 つの数（本便の後は 264・369・3・0・1）である（数え方は (i) の 3・参考値）。走査の列は起草役の独立の走査の上向きの辺の数（本便の後は 0）と、相互に名指す対の数（本便の後は 14）である。

変異 25 本のうち、verify の 6 行のどれも落ちなかったのは 11 本（M2・M4・M7・M14〜M21）である。workspace の歯だけが捉えて verify の 6 行が捉えない変異は無い（M3 と M6 は verify の外の印の歯も落ちるが、verify の 2 と 4 も落ちる）。

- **面の文脈と節の中身の変化**（M1・M3・M5・M6）は、憲法の面と face の命令の共通の歯・憲法の面の歯・配信先の組み立ての歯のどれかが凍結 fixture との byte の違いや census や導出できない入力として捉える。M1（文脈へ規則の表の行 id を入れない）は、関係の欄の規則の表の行が解けず生成が止まり、clippy も使われない可変の印として捉える。M5（meta の数が多いのを通す）は、数のずれの歯が捉える。**実の正本にも凍結 fixture にも無い値と場合の変化（M2・M4・M7）は、どの歯も捉えない**（(h) の言えないことの 6）。
- **層の置き誤りと一覧の書き換え**（M8〜M13）は、どの層に置いても境界の歯が捉える。新しい区切りは層 4 の `face.rs` を名指し、層 4 の `face_constitution.rs` から名指されるので、層 4 のほかのどこに置いても上向きの辺が生える。下の層の file が上の層の名を再輸出する形（M25）も、その file からの上向きの辺として境界の歯が捉える（clippy も使われない取り込みとして捉える）。
- **super:: の名指し・再輸出・区切りの別名の取り込み**（M14〜M16）は、どの歯も捉えない。M14 と M15 は見え方の数（super:: 0 → 1・再輸出 3 → 4）が捉える。**区切りの別名の取り込み（M16）は見え方の 5 つの数も相互の対も動かさない**＝捉えるのは diff を読む審査（(i) の 3 の (乙)・`use … as` の行が 8 → 9）だけである（(h) の言えないことの 2）。
- **見え方の広げ過ぎ**（M17〜M19）は、どの歯も捉えない。見え方の数（公開の印の行 264 → 265・268、行頭 pub 369 → 370）が捉える（(h) の言えないことの 5）。
- **同じ層の中の区間の当て損ない**（M20・M21）は、どの歯も見え方の数も捉えない。新しい区切りから `face_constitution.rs` への同じ層の辺が生え、相互に名指す対が 14 → 15 になる＝**捉えるのは (i) の 3 の相互の対の数だけ**である（(h) の言えないことの 4）。
- **同じ定義を 2 か所に持つ形**（M22・M23）は、clippy が捉える。書く側が自分の写しを読む形（M22）は、読み手の欠番の口が誰にも使われない定義になり、誰も使わない写し（M23）は写しが使われない定義になる（便 115 と違い、読み手の口を使うのが書く側 1 file だけなので、写しを読むと元が必ず使われなくなる）。
- **輪ごと外す弱め方**（M24）は、clippy（使われない定義）と、meta の数のずれの歯（憲法の面と face の命令の共通の歯）が捉える。

独立の走査の列は、層の割り当てを本便の計画（新しい区切りは層 4）に固定して src の字面だけから数えるので、境界の歯の層の表だけを書き換える変異（M8〜M13）では 0 のままである。M22 と M23 は区間の移動では生まれない形で、本便は定義を写さない。

境界の歯（(e)）の土台は、実装の側の file そのもの（`crates/folio/src/*.rs` と入口の宣言）である。**これは条 P-10.1 が求める独立した凍結 anchor ではない**（同じ便・同じ書き手が同じ書き込み範囲の中で書き直す）。ADR-15 はこれを 凍結した実測 と位置づけており、本便もその位置づけのまま運ぶ。面の中身の側には独立した凍結 anchor が在る（憲法の面と配信先の組み立ての凍結 fixture と、正本を直に読む census と欠番の独立の数え）。

### (e) 歯（新しくは足さない・既に在る歯は 1 本も落とさない・ADR-15 決定 (4)(5)）

**1. 境界の歯 2 本**（便 106 が置いた `crates/folio/tests/modules.rs`）の型付きデータを書き換える。歯の本文は 1 字も変えない。

| 書き換え | 中身 |
| --- | --- |
| 層の割り当ての表 | 新しい区切り face_constitution_read を層 4 で 1 行足す（並びは層 4 の中の字の順・憲法の面の行の次） |
| まだ降ろしていない層が上がる辺の一覧 | 変えない（空のまま）。**行を足さない** |

| 歯 | 何を数えるか | 本便の後 |
| --- | --- | --- |
| `p106_layers_cover_every_module` | 層の割り当ての表の区切りの集合が、入口の区切りの宣言の集合と src の file 名の集合にちょうど一致する | 新しい区切りを含めて一致 |
| `p106_edges_point_down` | 注釈と字面を剥がした src を走査した 層が上がる対の集合が、凍結した一覧とちょうど一致する | 空の集合で一致（不変） |

**2. 単体の歯は動かない**（(b) の 2）。

**歯は足しも消しもしない。** 歯の名の一覧（`cargo nextest list --workspace` を sort して比べる）は base と 1 字も違わない（起草役の実測）。

### (f) 凍結 anchor が動かないこと・出力が動かないこと（ADR-15 決定 (5) の受入）

本便は `design-intent/` の下も凍結の土台（`tests/fixtures/` の下と `tests/floor_cases.yaml`）も `crates/folio/build.rs` も **1 byte も触らない**。

1. **出力の byte が 1 つも動かない。** 起草役は base の binary と本便の後の binary で、同じ正本から次を作って突き合わせた（参考値）。
   - 読み手を通る出力: 配信先の組み立て（`folio build --write`・23 file・1,316,521 byte・憲法の面を含む）は `diff -r` で差 0。部品の検査（組み立ての出力の 5 面・どちらも 合格・60 byte）は byte 一致。
   - ほかの出力: 天井の材料の束（`folio ceiling --write`・観点 4・199 file・7,250,322 byte・面の写しに憲法の面を含む）は差 0。その束と持ち主の天井の周の置き場（2026-09-22 の 26 周目）の写しに `--check` を当てた出力（536 byte と 716 byte）・`--refute`（写しの 211 file・7,572,583 byte が差 0）・`--stamp`（印 7,860 byte を含む写しが差 0・標準出力は写しの置き場の path を含むので byte 数は置き場の字数で変わる〔改訂 b の写しで 198 byte〕が、base と本便の後は byte 一致）・門 `--gate` 3 通りは byte 一致。相談窓口の質問（105 byte）・索引（38,207 byte）・導出した一覧の JSON の 1 行（1,400 byte）・欄の決まりの生成区間（`folio schema --check`・9 file とも 一致・464 byte）・床の判定（`folio check`・合格・60 byte）・`folio inject --check`（68 行・7,912 byte）は byte 一致。写しの外で撃った凍結の旗 3 つと版を上げた写しの旗 4 つも byte 一致・終了コード一致（23 対）。
   - 凍結の旗: 写しの根（`design-intent/` と器の導出 file `contracts/schema.toml`）を作って git init と 1 commit を行い、4 つの場面（素の写し・憲法の版と P-1 の題を上げた写し・id の一覧の凍結 anchor を消した写し・版を上げずに P-1 の題だけを変えた写し）それぞれに、旗なし・`--emit-amends`・`--freeze-anchor`・`--freeze-ids` の 4 つを撃った。16 通りの標準出力と標準エラーの 32 本が byte で一致し、終了コードも一致し、撃った後の写しの中身は `diff -r` で差 0 だった。
2. **生成区間と床の判定が動かない。** `folio schema --dir design-intent --check` が 9 file とも 一致（byte 数まで base と同じ）・`folio check --dir design-intent` が 合格（違反 0・まだ分からない 0）・`folio inject --check` が 一致。どれも終了コード 0（ADR-15 決定 (5) の受入の 2 つ目と 3 つ目）。このうち床と生成区間の一致は、器の verify では撃てない（`.vessel.toml` の撃ってよい命令は cargo と git だけ）。撃つのは取り込みの審査である。憲法の面の byte は、verify の 2 と 4 が凍結 fixture との byte 一致として歯で数える（`tests/fixtures/face/` の憲法の面と配信先の組み立て）。
3. **天井の印も門も動かない。** 本便は設計文書の正本を 1 file も書き換えないので、門は 0（通す）を返す（§0 の 門 の行に実測）。

これに加えて、起草役は写しの repo で `cargo build --all-targets` が 0 警告、`cargo build` が 0 警告、`cargo nextest run --workspace` が **全部緑**（歯の名の一覧は base と同じ）、`cargo clippy --workspace --all-targets -- -D warnings` が **0 警告** であることを実測した（どれも参考値）。組み立ての確かめに `cargo build` だけを使わないのは、単体の歯の区画を組み立てないためである（便 106 の検証役の申し送り）。`cargo fmt --check`（edition 2024）の差分の数は、触る file の合計で base と同じである（base の `face_constitution.rs` の面の口の取り込みの括りが rustfmt の形でないのは base から同じで、本便はその括りの 2 行を 1 行に書き換えるだけで括りを整形し直さない）。file ごとの差分の数は `face_constitution.rs` 1 → 1・新しい file 0・`main.rs` 0・境界の歯の file 4 → 4 である（参考値）。

### (g) 大きさ（器の行数の上限 R-C4 と同じ式・ADR-15 決定 (1)）

size は **M**（見積 300 行）。本便の新しい file は正規化 268 行（参考値）で、S の見積 100 を超え M の見積 300 の下に収まる。

**受付が余地を測るのは `crates/folio/src/` の下の `+` と印の無い file だけで、行が減る `-` の file と歯の側の file は測らない。** `face_constitution.rs` は本便で 1,295 → 1,039 行に減るので `-` を付ける。`face_constitution.rs` の base の余地 205 は M の見積 300 を下回るので、`-` を付けずに write-set に入れると受付が余地の不足で断る。本便は `face_constitution.rs` を開ける便なので、`-` の印が本便の受付の前提である。入口の宣言は 1 行増えるので印を付けず、測られる（余地 908）。境界の歯の file も 1 行増えるので印を付けないが、歯の側の file なので受付は余地を測らない。

触る src それぞれの余地（参考値・base の実測・幅 120 正規化・上限 1,500）。

| file | 正規化行 | 余地 | M の見積 300 との差 | 本便の後 |
| --- | ---: | ---: | ---: | ---: |
| `crates/folio/src/face_constitution.rs` | 1,295 | **205** | 受付は測らない（`-`） | 1,039（減る・余地 **461**） |
| face_constitution_read.rs（新設・`crates/folio/src/` の下） | base に無い | 1,500 | +1,200 | 268（余地 1,232） |
| `crates/folio/src/main.rs` | 592 | 908 | +608 | 593（余地 907） |

歯の側の余地（参考値・同じ base・受付は測らない）。

| file | 正規化行 | 余地 | 本便の後 |
| --- | ---: | ---: | ---: |
| `crates/folio/tests/modules.rs` | 316 | 1,184 | 317（1 行足す・印なし） |
| `crates/folio/tests/face.rs` | 885 | 615 | 不変 |
| `crates/folio/tests/face_constitution.rs` | 567 | 933 | 不変 |
| `crates/folio/tests/site.rs` | 727 | 773 | 不変 |

本便が移動のほかに書く行は 37 行（参考値）である。新しい file の頭の注 4 行・空行 1 行・取り込み 3 行・見え方を広げた 21 行（区間の行の頭に印を足しただけで、ほかの字は同じ）、`face_constitution.rs` の頭の注 2 行と取り込み 4 行（面の口の括りの書き換えた 1 行とその閉じの 1 行・新しい区切りの取り込みの 2 行）、`main.rs` の 1 行、境界の歯の file の 1 行である。消す行は 23 行で、見え方を広げる前の 21 行と、`face_constitution.rs` の面の口の括りの 2 行である。**ほかはすべて字を変えない移動**（239 行）である（数え方は (i) の 3）。

src 全体は 53 file・26,304 行から 54 file・26,317 行になり、**余地 300 未満の src は 1 本から 0 本になる**（参考値・base b81ae12・設計ノート §6 の便 116 の後の値と揃う）。

**設計ノート §4-b との差**（参考値）: 新しい file と層は §4-b の便 116 の行と揃う。違いは 4 点で、どれも §0 の 設計ノートからの逸脱 に由来する。(1) `face_constitution.rs` は §4-b の 1,294 → 1,001 に対し 1,295 → 1,039 である（本便は名札と数の口 4 つを書く側に残し、再輸出の 1 行の代わりに 8 名の取り込み 3 行を置く）。(2) 新しい file は §4-b の 304 に対し 268 である（同じ理由）。(3) 見え方の拡大は §4-d の 13 に対し 21 である（(i) の 3）。(4) 書き込み範囲に verify の歯の file 2 本（憲法の面と face の命令の共通の歯・配信先の組み立て）が入る。§4-b の測りは再輸出で使う側を書き換えず、`cargo build` しか撃たず、歯の file を verify に名指す前の測りだった。

**便の diff の大きさ**は、写しの repo で base からの差を測って **22,424 byte**（参考値・4 file・足す 276 行・消す 262 行）で、器の門が測る上限 150,000 byte の下に収まる。

### (h) 本便が運ばないもの・言えないこと・撤退条件（ADR-15 撤退条件 (1)〜(4)）

**運ばないもの。**

- 憲法の面の書く側（部品の一覧・章の帯の表・枠・dc・面の口 derive・名札と数の口 4 つ・節 骨格 以下の書き手・値の枡の閉じた表・前の裁定の区切り）と単体の歯の区画（`face_constitution.rs` に残る）。区間の外の行は、頭の注 2 行と取り込みの行のほかは 1 字も動かない。
- ほかの file の注の中の file 名と道の書き直し。`face.rs` の頭の注の 憲法の面（`face_constitution.rs`）の字と、配信先の組み立ての頭の注の生成器の一覧の字は、面の口 derive の置き場を指し続けるので正しいままである。移した注に置き場を指す file 名は無い。
- 見え方を広げた箇所の絞り直しと、同じ層の中の相互の名指しの解消。本判断の外である（設計ノート §9 の 4 と 6）。便 115 が単体の歯のために足した `#[cfg(test)]` の取り込み 1 行も、設計ノート §9 の 6 の掃除の便（便 117 以降）で歯の置き場を正す後続に置く（席の裁定・台帳 f2-648 notes 2026-09-23）。本便はその形の行を足さない。
- 値域の突き合わせが 組み立てた版より値が少ない 場合を、欠番の検査が 同じ接頭辞の欠番が 2 つ以上 の場合を、歯が持たない穴（(d) の M4・M7）。本便の前から在る穴で、本便は運ばない（値と式を 1 字も変えないので、移動の正しさには掛からない）。
- `design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・外部の道具の増減・台帳への記帳・器の行数の上限を数える歯（ADR-15 決定 (1)）。

**この便の歯が言えないこと。**

1. 境界の歯は**独立した凍結 anchor ではない**。層の割り当ても辺の一覧も、実装と同じ便・同じ書き手が同じ書き込み範囲の中で書き直せる（ADR-15 決定 (4)）。
2. 走査は**字面だけを見る**。再輸出を挟むと、歯が数える行き先は再輸出した file になり、真の定義の置き場は見えない。本便は再輸出を置かない。台本の glob の再輸出の形（M15）は、verify の行も workspace の歯も全部緑だった＝**再輸出を足す変化を捉えるのは歯ではなく、(i) の 3 の見え方の数（再輸出の行 3 → 3）である**。**歯が捉えないのは、crate:: を書かずに super:: で区切りを名指す形である**（走査は crate:: と入口の接頭辞なしだけを数える）。新しい区切りを層 5 に置き、書く側の取り込みを super:: の名指しに替えた変異（M14）は、上向きの辺が在るのに全部緑だった。今の src の本文に super:: の名指しは `use super::*;` のほかに無い。**本便は super:: の名指しを 1 行も足さない。** **区切りを別の名で取り込む別名（M16）は、歯も見え方の数も相互の対も動かさない**。本便はその形の別名を 1 行も置かない。着地の時の確かめは (i) の 3 の見え方の数と、足した行に super:: と区切りの別名の取り込みが無いことである。
3. **層の置き誤りは、層 4 のほかのどこでも境界の歯が捉える**（M8〜M12）。新しい区切りは層 4 の面の口を名指し、層 4 の `face_constitution.rs` から名指されるので、層 4 のほかでは必ず上向きの辺が生える（便 113 / 114 の 層 2 への置き誤りを歯が捉えない 穴は、便 115 と同じく本便には無い）。ただし super:: の名指しと組むと捉えない（2 の M14）。
4. **同じ層の中の区間の当て損ないは、どの歯も捉えない。** 向きの決まりは同じ層を許すので、区間の名を `face_constitution.rs` に取り残して読み手がそれを名指す形（M20 = 値域の突き合わせ・M21 = 改訂の段の型）は、境界の歯も見え方の数も通る。**着地の時の確かめは (i) の 3 の相互の対の数（14 → 14）と、新しい file に憲法の面の区切りの名指しが無いことで、super:: の名指しは対の数に現れないので (丙) と組で見る。** 同じ定義を 2 か所に持つ変化は、本便では clippy が捉える（欠番の口の写しを書く側が読む M22 は読み手の元の口が、誰も使わない写しの M23 は写しが、使われない定義になる）。ただし、読み手の口を別の file も使うようになった後は、写しを読む形を clippy は捉えない（便 115 の M19）。着地の時の確かめは (i) の 3 の区間の純度（足した側に区間の定義が 2 度現れない・移動でない足した行が (g) の 37 行だけ）である。
5. **見え方の拡大を、どの歯も数えない。** 区間の中だけが使う fn を広げる（M17）・区間の項目と欄と method を全部広げる（M18・台本の widen の形）・文脈の型を道具の外へ向けた印にする（M19）変化は全部緑だった。本便の見え方の拡大 21 を確かめるのは (i) の 3 の見え方の数である。
6. **実の正本にも凍結 fixture にも無い値と場合を、どの歯も数えない。** 文脈の要件書の表から目標の節を落としても（M2）、関係の欄が目標を名指さないので全部緑で、組み立ての出力も同じだった。値域の突き合わせを 値が多すぎるときだけ断る 形に弱めても（M4）、欠番の並びを逆にしても（M7・実の正本の欠番は 1 つ）全部緑だった。これらは本便の前から在る穴で、本便は運ばない（値と式を 1 字も変えないので、移動の正しさには掛からない）。
7. **輪ごと外す弱め方は clippy が捉える。** 面の口 derive から数の突き合わせの呼び出しを消すと（M24）、数の突き合わせの口が誰にも使われない定義になり、clippy が落ちる。meta の数のずれの歯（憲法の面と face の命令の共通の歯）も落ちる。
8. 歯の file（境界の歯）は**辺を動かす便すべての合流点**になるので、本便は便 115 の着地を待って直列に運ぶ（設計ノート §5-a の 6・便 115 は着地済み〔main b81ae12〕で、本便の base はその実物）。本便の後に境界の歯の file を書き換える列の便は無い。

**撤退条件**（ADR-15 の撤退条件の逐語を引く。閾値は ADR-15 の値、比べる相手は直前の便〔便 115〕の着地後の値、判定は取り込みの審査が行い〔届かないときは持ち主〕、記帳は台帳 f2-648 に行う）。

- (1) 逐語: 責務で割っても受付の断りが減らない。数えるのは取り込みの審査で、台帳 f2-648 へ記帳する。11 本目の便が着地した後の 10 便で、実装の側の file の余地が足りずに受付で断られた回数を数える。3 回に達したら、決定 (2) の層の数と決定 (3) の降ろし方を捨てて測り直す判断の記録を起こす。歯の側の file が原因の断りはここに数えない（本判断は歯の側の境界を決めていない）。— 本便はその 11 本目の便なので本便そのものは数えず、数えの窓は本便の着地の後の 10 便に開く（本便の後、余地 300 未満の src は 0 本・参考値・設計ノート §6 の便 116 の後の値）。
- (2) 逐語: 上向きの辺が増える。境界の歯が出す上向きの辺の集合を、取り込みの審査が直前の便の一覧と比べる（歯そのものは凍結した一覧との一致しか見ないので、便が辺を増やして一覧も同時に足せば歯は通る。増えたことを捉えるのは、審査が歯の file の差分を読む側である）。11 本の便のどれかの着地後に、上向きの辺が直前の便より増えていたら、決定 (5) の順を捨てて順を組み直す。増える便は計画に 1 本も無いことを写しの file で確かめてあるので、増えたときは実測か計画のどちらかが誤っていたということである。— 本便の直前の値（便 115 の着地後）も本便の計画値も 0 本（参考値）で、審査は境界の歯の file の差分に凍結した一覧の行が現れないことを読み、字面に現れない上向きの辺（言えないことの 2）を数えられなかったときは (4) に当たる。
- (3) 逐語: 境界が責務で引けない場所が出る。判定するのは取り込みの審査（届かないときは持ち主）で、便を運ぶ席ではない。1 つの file を責務で割ろうとして、どちらの層にも属する部分が残り、割り方を決められないと審査が判じた便が 2 本に達したら、決定 (2) の層の定義を捨てて測り直す。— 区間を決定 (3) の後段の字どおり（context と読みと検査の節と、欠番・改訂の段の読みと、設計ノート §1 の 面の中の割り の定義の内に在る関係の欄からリンクの字面を組む列）に取り、名札と数の口 4 つと閉じた一覧を書く側に残す割り方は責務（読み手 = 正本から文脈と節の中身を組む／書き手 = 文脈から HTML を出す）で引けているという席の裁定（台帳 f2-648 notes 2026-09-23・(b) の 層 4 に置く理由と、その限界・(c)）により**起草の時点の見立ては数えに入れず（数えは便 109〜115 の審査の裁定で 0 のまま）**、判じるのは取り込みの審査（届かないときは持ち主）で、2 本に達したら決定 (2) の層の定義を捨てて測り直す。
- (4) 逐語: 数えられない回が 2 回を超えたら、本判断の撤退条件の判定を「まだ分からない」に落として持ち主へ渡す。「同じ」とみなすのは（撤退条件の番号・数えられなかった理由・対象の便）の 3 つ組が一致する回である。この 2 回は本判断が自分で置く数であり、規則の表の行 R-7 の繰り返し上限とは別である（行 R-7 が言う 3 つ組は検査の id と判定と対象の道で、本判断が数えるものとは母集団が違う）。— 撤退条件 (4) は本便の (2) か (3) を審査が数えられなかったときだけ当たり、本便でなりうるのは (i) の 3 の見え方の数と相互の対の数と super:: と別名の確かめを審査が撃てなかったときの (2) である（直前の便までの数えは 0 のまま）。

どの場合も区間は戻さない。戻すと `face_constitution.rs` の余地が 205 に戻って塞がり、後続の便の受付の前提が崩れるためである。

### (i) 数え直す手順（誰でも撃ち直せる形・規則の表の行 D-13）

本便の契約が挙げる数は全部 **参考値**で、正本は実測である。組み直す手順は設計ノート `docs/design/module-map-2026-09-23.md` の §7（写しに区間を当てて組み立て、走査し直す）を本便の区間に当てたうえで、次の 7 つを数える。起草役の台本（区間を当てる 1 本と取り込みの塊 2 つと広げる行の表・走査・行数・変異・出力の比較 2 本・受付の再現）と実測 patch は `~/.local/share/folio2/handoff-2026-09-23/` の起草の記録 `d116-draft.md` が場所と撃ち方を持つ。

1. **正規化行と余地**: 空行を含む全行を数え、1 行の字数（Unicode の字の数）が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す。余地 = 1,500 − その値。
2. **辺と層の向き**: 設計ノートの冒頭の走査の式に字面の剥がしを足したもの（注釈と字面を剥がし、絶対の道と括り書きを解き、入口だけ接頭辞なしも数え、自分自身は数えない）を当て、層の割り当てで 下向き / 同じ層 / 上向き に分ける。境界の歯そのものがこの式を持つので、`cargo nextest run -p folio --test modules` が数え直しである。起草役の独立の走査では、base が 53 file・256 辺（下 179・同じ層 77・上 0）、本便の後が 54 file・260 辺（下 181・同じ層 79・上 0）だった（参考値）。消える辺は 0（書く側は憲法の段の一覧と cursor と面の口を使い続ける）、足す辺は 4（書く側から新しい file への同じ層 1、新しい file から面の口への同じ層 1、新しい file から層 1 の 2 file〔憲法の段・cursor〕への下向き 2）。
3. **区間の純度と見え方と相互の対と歯の名**: base と本便の後の diff の足した行と消した行を多重集合で照らす（一致は 239 行）。移動でない行は、足した側が (g) の 37 行、消した側が (g) の 23 行である。見え方を広げた 21 行は、印を外すと消した側の 21 行と字が一致する。見え方は src 全体で、道具の中で公開の印（`pub(crate) `）を含む行と、行頭の公開の印（空白の後の `pub `）の行と、再輸出（`pub use` と `pub(crate) use`）の行と、`use super::*;` のほかの super:: の名指しと、`#[cfg(test)]` の直後の取り込みの行を base と比べる（243 → 264・369 → 369・3 → 3・0 → 0・1 → 1・参考値）。道具の中で公開の印の増分 21 は、型 3 と fn 5 と欄 13 である。設計ノート §4-d の便 116 の値 13 との差（参考値）は数え方の違いで、§4-d は 項目だけ を数え（台本の widen は欄と method も広げたが数えなかった）、本便は 項目 + 型 + 欄 + method を数える（本便の method の増分は 0）。項目どうしで比べると本便は 8 で、§4-d の 13 より 5 少ない（名札と数の口 4 つを書く側に残し、値域の突き合わせを私のまま残した）。欄は台本が区間の欄を全部（15）広げたのに対し、本便は書く側が読む 13 だけを広げた（文脈の要件書の表と規則の表の行 id の表は私のまま）。**相互に名指す対**（互いを名指す 2 file の対）を走査の辺から数え、base と比べる（14 → 14・新しい file は相互の対に入らない・参考値）。歯の名は `cargo nextest list --workspace` を base と並べ、sort して比べる（違い 0）。**境界の歯も見え方の数も捉えない 3 つ（区切りの別名の取り込み・super:: の名指し・同じ層の中の当て損ない）は、着地の審査で席が次の手順で実測する**（席の裁定・台帳 f2-648 notes 2026-09-23・(h) の言えないことの 2 と 4）: (甲) 相互に名指す対の数が base と同じ（14 → 14）で、新しい file に憲法の面の区切りの名指し（crate:: を付けた形でも super:: を付けた形でも）が無い。相互の対の数は走査の辺から数えるので super:: の名指しは数えない（取り残した名を読み手が super:: で名指す形は、対の数 14 のまま (丙) の super:: の数だけが動く・便 115 の検証役の変異 X8 と同じ形）。(甲) は (丙) と組で見る。(乙) 移動でない足した行の `use … as` が、書く側と同じ字の憲法の段の一覧の呼び名（constitution_enums を ce と呼ぶ 1 行）のほかに無い（この 1 行は src の 8 file が同じ字で持つ呼び名で、取り込む区切り名が字面に出るので走査に見える＝席の裁定の 別名の取り込みを置かない が塞ぐ形〔新しい区切りを別の名で取り込む M16〕の外・`use … as` の行は 7 → 8・参考値・移動の行の数の型の変換 `as u64` は base の `face_constitution.rs` から字を変えずに移ったもので数えない）。(丙) 足した行に `use super::*;` のほかの super:: が無い。あわせて (丁) 見え方の 5 つの数（道具の中で公開の印の行・行頭 pub・再輸出・super::・`#[cfg(test)]` の直後の取り込み）を base と比べ、増分に行頭の公開の印 `pub ` が 1 行も無い（行頭 pub の行 369 → 369）ことを数える（見え方の拡大 21 を受けた条件・§0 の 設計ノートからの逸脱）。
4. **出力の byte**: base と本便の後の binary で、(f) の 1 の凍結の旗 16 通りと、`folio build --write`・`folio ceiling --write`・`--check`・`--refute`・`--stamp`・`--gate`・`folio intake --print`・`folio graph --print`・`folio parts --print`・`folio parts --check`・`folio check`・`folio inject --check` を撃ち、`diff -r` と `cmp` で比べる。凍結の旗と `--refute` と `--stamp` は書くので、写しに当てる。
5. **生成区間の byte**: `folio schema --dir design-intent --check`（9 file）。
6. **便の diff の大きさ**: base の commit と便の commit の差を byte で測る。
7. **前提の便との重なり**: 本便の区間の台本を main b81ae12 の写しに当てた差分と、初稿の木（main f9e48c6 に便 113〜115 の起草役の実測 patch を順に当てた木）に当てた差分を byte と sha256 で比べる（起草役の実測では index 行まで一致・22,424 byte）。実測 patch（`~/.local/share/folio2/handoff-2026-09-23/d116-measured.patch`・b81ae12 基準）は b81ae12 に `git apply --check` で当たる。便 113〜115 の区間と同じ行は書き換えない（入口の宣言と境界の歯の層の表では文脈の行だけが重なる）。

## 2. 範囲

- 入れる: 新しい src の file 1 本（憲法の面の読み手・条と文脈と改訂の段の型・読みと検査の 3 つの口・欠番・関係の欄のリンクの列・改訂の段の読み）・`face_constitution.rs` からの字を変えない移動（見え方の印 21 か所のほか）・`face_constitution.rs` の頭の注 2 行と取り込みの行・入口の区切りの宣言 1 行・境界の歯の file の層の表 1 行。
- 入れない: 憲法の面の書く側と面の口 derive と名札と数の口と閉じた一覧と単体の歯の区画・再輸出の行と区切りの別名の取り込みと super:: の名指しと `#[cfg(test)]` の取り込み・区間の中だけが使う項目と欄と method の見え方・ほかの file の注の中の file 名と道の書き直し・既に在る歯の名や本数や本文の変更・境界の歯の凍結した一覧・`design-intent/` の下の全 file・凍結の土台・`Cargo.toml`・CI の yml・新しい dir・外部の道具・台帳への記帳・上限を数える歯。

## 3. 部品

| id | 名 | 役 |
| --- | --- | --- |
| face_constitution_read | 新しい src の file | `crates/folio/src/` の下の face_constitution_read.rs（責務の層 4 面に出す・面の中の割り）。憲法の面の読み手（条と文脈と改訂の段の型・読みと検査の 3 つの口・欠番・関係の欄のリンクの列・改訂の段の読み）を `face_constitution.rs` から字を変えずに受ける。書く側が名指す型 3・fn 5・欄 13 を道具の中で公開にする |
| trim | 取り除き | `crates/folio/src/face_constitution.rs` から移すものを外し、頭の注 2 行を足し、面の口の取り込みから書く側が使わなくなった 4 名を外し、新しい区切りの 8 名を取り込む（再輸出はしない） |
| declare | 区切りの宣言 | `crates/folio/src/main.rs` に新しい区切りの宣言を 1 行足す（並びは字の順） |
| modules | 境界の歯の型付きデータ | `crates/folio/tests/modules.rs` の層の割り当ての表に face_constitution_read を層 4 で 1 行足す。凍結した一覧は空のまま。歯の本文は変えない |

## 4. 検査（歯）

§1 (e) のとおり。本便は歯を足さず、既に在る歯は 1 本も名を変えず 1 本も消さず、本文も変えない。境界の歯 2 本は型付きデータだけが変わる。区間の変化を捉える歯の範囲と限界は §1 (d) と §1 (h) に書いた。verify の行で落ちない変化があるので、共通の検証の `.vessel.toml` の common-verify（workspace 全体の nextest と clippy）を合わせて判定する。**common-verify でも落ちない変化がある**（§1 (h)）ので、取り込みの審査は区間の純度と見え方の数と相互の対の数と super:: と区切りの別名の無さと歯の名を §1 (i) の 3 の手順で確かめ、床と生成区間の一致を §1 (f) の 2 の命令で撃つ。

## 5. 依存

- 外部の道具は増やさない。`Cargo.toml` も `Cargo.lock` も触らない。
- 前提になる便: 便 115（入口の面の読み手を層 4 へ・行 `dn`・**着地済み**・main b81ae12・台帳 f2-648.160）と、その前提の便 114（行 `dm`・着地済み・main 0d0b08a・台帳 f2-648.159）と便 113（行 `dl`・着地済み・main 89c668d・台帳 f2-648.158）。本便と便 113〜115 は、入口 `main.rs` と境界の歯の file を書き換える（区切りの宣言と層の表の行で、同じ行は書き換えない）＝**重なり無し**。本便は便 115 の着地を待つ直列の前提（設計ノート §5-a の 6）を満たし、base は main b81ae12 である。着地済みの前提: 便 112（main f9e48c6・台帳 f2-648.157）・便 108（部品目録の閉じた一覧を層 1 の `catalog.rs` へ）・便 107（正本の cursor を層 1 の `cursor.rs` へ）・便 106（境界の歯）。
- 本便は ADR-15 の列の最後の便である。本便の後に境界の歯の file と入口を書き換える列の便は無い。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "do"
title = "憲法の面の読み手（条 1 本と文脈と改訂の段の型・節 読みと検査〔文脈を組む context・組み立てた版との値域の突き合わせ・meta の数と数えた条の数の突き合わせ〕・条の id の欠番・関係の欄のリンクの列・改訂の段の読み）を crates/folio/src/face_constitution.rs から新しい file crates/folio/src/face_constitution_read.rs へ字を変えずに責務で降ろし、塞がっている face_constitution.rs の余地を 300 以上へ開ける。これは判断の記録 ADR-15 の決定 (1)(2)(3)(5) が定めた 11 本の列の 11 本目（最後・塞がっている 4 file を開ける 4 本の 4 本目・面の中の割りの 2 本目）で、設計ノート docs/design/module-map-2026-09-23.md の §4-b の便 116 の行と §1 の 面の中の割り がその中身を持ち、本便の後に余地 300 未満の src は 0 本になる。読み手は面の口（face.rs の段の名札・anchor・節の anchor・what の前後の割り）と字面の逃がしを呼び、関係の欄からリンクの列も組むので、ADR-15 決定 (3) の後段のとおり層 1 へ降ろさず同じ責務の層 4 面に出す の中で割る。区間が区間の外に名指すのは層 1 の 2 file（憲法の段の一覧・正本の cursor）と層 4 の face.rs だけで、書く側の名は名指さないので、層が上がる辺は 0 本のまま増えず、相互に名指す対も増えない。書く側が名指す型 3・fn 5・欄 13 を道具の中で公開へ広げ（見え方の拡大 21）、区間の中だけが使うもの（値域の突き合わせ・文脈の要件書の表と規則の表の行 id の表・条 id から条を引く method）は私のまま残す。面の口 derive と部品の一覧と章の帯の表と枠と部品の名札の口と名札と数の口 4 つ（段の名を繋ぐ口・段ごとの条の数・札の凡例・章の名）と HTML を書く側と閉じた一覧は face_constitution.rs に残る。再輸出の行も区切りの別名の取り込みも super:: の名指しも #[cfg(test)] の取り込みも 1 行も置かず、書く側は新しい file を直に名指す。face_constitution.rs は頭の注 2 行と取り込みの行を書き換え、入口 main.rs は区切りの宣言 1 行を足し、ほかの行は 1 字も動かない（読み手の名を外から名指す file は無い）。あわせて ADR-15 の決定 (4) の境界の歯の file crates/folio/tests/modules.rs の層の割り当ての表に face_constitution_read を層 4 で 1 行足す（凍結した一覧は空のまま・歯の本文は変えない）。歯は足さず消さず本文も変えず、face_constitution.rs の単体の歯 1 本も名と本文が変わらず、憲法の面と配信先の組み立ての byte も、束・印・門・索引・部品の検査・欄の決まりの生成区間・床の判定と凍結の旗の出力の byte も凍結 anchor も 1 つも動かず、design-intent の下と凍結の土台と組み立ての script と Cargo.toml と CI の yml は 1 file も触らず、新しい dir も作らず file も消さない"
req = ["FR4", "FR7"]
section = "1"
write-set = ["+crates/folio/src/face_constitution_read.rs", "-crates/folio/src/face_constitution.rs", "crates/folio/src/main.rs", "crates/folio/tests/modules.rs", "crates/folio/tests/face.rs", "crates/folio/tests/face_constitution.rs", "crates/folio/tests/site.rs"]
verify = ["cargo nextest run -p folio --test modules", "cargo nextest run -p folio --test face", "cargo nextest run -p folio --test face_constitution", "cargo nextest run -p folio --test site", "cargo nextest run -p folio --bin folio face_parts_are_all_allowed_on_the_constitution_face", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "境界の歯 2 本が緑（層の割り当ての表の区切りの集合が入口の区切りの宣言の集合と src の file 名の集合にちょうど一致し、層が上がる対の集合が凍結した空の一覧とちょうど一致する）、--test face が走らせる憲法の面と face の命令の歯の全部が緑（凍結 fixture〔tests/fixtures/face/ の憲法の面〕との byte 一致・実の正本の面の部品の検査・逐語と件数の census・check の 3 値・組み立てた版とのずれ・札の凡例・数えの字・数値の表の種別の凡例を含む・本数と名は base の cargo nextest list と同じ）、--test face_constitution が走らせる憲法の面の歯の全部が緑（数値の表の値の枡と裁定の枡・欠番の行と独立の数え・改訂来歴の前の文・用語集の欄の名前の節・機構の札を含む・本数と名は base と同じ）、--test site が走らせる配信先の組み立ての歯の全部が緑（凍結 fixture との byte 一致と実の正本の各面〔憲法の面を含む〕の検査を含む・本数と名は base と同じ）、face_constitution.rs の単体の歯 1 本（部品の一覧が憲法の面に置けること）が緑、clippy が 0 警告（移した先にも元の file にも使われない定義や使われない取り込みが無い）で、workspace の nextest が全部緑（本数と名の一覧は base の cargo nextest list と同じ・本便は歯を足さない）で CI が通る"
<!-- contracts:end -->
