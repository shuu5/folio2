# 設計: 便 31 — 設計ノートの面に図の本体（SVG）を埋め、図の class を folio の design token で塗る（FR15 / NFR2・ADR-4 決定 (3)）

- 要件: FR15（文書を生成するとき、図の節の型付き記述から図の本体だけを部品として埋め、意味の属性は落とさない・通らない図は生成せず前の生成物も上書きしない）/ NFR2（部品目録に無い class は rules 行 R-3 の値 0・色は design token 由来 100%）/ FR9（設計ノートの面）
- 条: P-2.1（生成器は folio 1 つ・道具は内側の部品を作る）/ P-2.3（色・字の大きさ・線の太さの元の値は 1 か所）/ P-2.4（部品と図の型は閉じた一覧）/ P-4.1（図が導出できなければ面も出さない）/ P-10.1（凍結 anchor）
- 判断の記録: ADR-4 決定 (3)（図の本体だけを取り、意味を表す class を部品目録に載せ、色・字の大きさ・線の太さは folio の design token で 1 か所に定義する・閲覧の仕掛けは捨てる・意味の属性は残す）。ADR-5 決定 (3)（手書きの生成器・外部の部品を足さない）。便 30（f2-648.45）の後に直列で置く。
- 裁定: 持ち主 2026-09-19「承認する」（A-3.1・便 30 の受け皿）。「設計ノートはさすがに図がないとだめだと思うが。」（2026-09-19・便 29 の walk の保留の理由）。walk 承認（AC5 の形・図の入った設計ノートの面と入口の棚）は本便の着地後に planner が tailnet の内側で取る（本便の契約の外）。
- 置き場: この文書は folio2 の設計ノート（M1 で YAML 正本へ移す）。契約表は末尾の区間。審査の材料は行 af が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（Declared 形・新規は無い）。

## 1. 目的と中身

便 30 で設計ノートの図 1 枚を図の本体（SVG）として書けるようになった（`folio figure`）。本便は (1) 設計ノートの面（`folio face --face note`）の図の章を、図ごとの図の枠（figure-panel）に図の本体を逐語で埋めた形にし、(2) 図の本体の意味の class（部品目録の figure_body_classes・33 語）を folio.css で design token から塗り、(3) 部品目録の figure-panel を設計ノートの面にも置けるようにし、(4) 面と組み立ての写しに図の道具を足して歯と凍結 fixture を更新する、の 4 つ。図の生成そのもの（便 30）と道具の写し（受け皿）は変えない。

planner の実測（2026-09-19・main cf6d28c）: figure.rs（476 行・便 30）の関数 derive は dir・文書 id・図 id を取り、設計ノートを読み直して図の行を探し、型の表（5 組）・spec の JSON 書き出し・道具の子の処理・svg の抽出を行う。実の正本の fig-1 の図の本体は 7,775 byte・99 改行・class 17 語（a-default・c-backend・c-external・c-frontend・c-grid・c-mask・m-dashed・m-default・m-emphasis・m-security・s-backend・s-external・s-frontend・semantic-sigil・sigil-fill・t-muted・t-primary）で全部 parts.json の figure_body_classes に在り、行内の様式 0・色の直書き 0・style 要素 0・script 要素 0・引用符の無い属性 0・要素は circle・defs・desc・g・marker・path・pattern・polygon・rect・svg・text・title の 12 種・stroke-width と font-size と font-weight は属性で持ち font-family は持たない（頁の字体を継ぐ）・svg の属性に lang の値 en と英語の desc「An architecture diagram generated by Archify.」が在る（道具の出力の一部・読み上げ用・画面には出ない）。道具の 1 回は 0.22 秒。folio.css（正規化 804 行）の figure-panel の規則は 358〜363 行と 476〜477・618 行で、svg を塗る規則と `.c-*` 等の class は 1 つも無い（parts --check は class を folio.css の選択子から集めるので、図の本体の 33 語は folio.css に無ければ R-3 の違反になる）。道具の template.html の様式は class ごとに fill と stroke を道具の変数（--backend-fill 等）で塗り、`.a-dashed` は stroke-dasharray 4,4・`.a-security` は 5,5・`.c-lane` は 6,6・`.semantic-sigil` は stroke currentColor と stroke-width 1.35・`.s-<種別>` は color。face_note.rs（正規化 1,002 行・余地 498）の関数 figures_chapter は section-lead-callout の中に card（class「card accent warn」）を図ごとに出し、cd に定数 FIGURE_CD「図の生成はまだ無い（要件書 FR15 の便で足す）＝まだ分からない。」を置く。PARTS は 7 部品で figure-panel を含まない。parts.json の figure-panel の faces は index・constitution・srs（note 無し）で、凍結目録 tests/fixtures/floor/parts-catalog.json も同じ。face_srs.rs の関数 figure_open は figure の要素（figure-panel の dc）に data-role の値 diagram と id を付け fig-title（fn・題・fig-tools の zoom-btn・zoom-close）を出し、関数 figure_close は figcaption の ver を出す（folio-ui.js の拡大は data-role diagram の figure を対象にする）。tests/face_note.rs（正規化 523・歯 16）の fixture_copy は正本 4 file + design-note/full.yaml を src/ へ、導出 file を親 dir の contracts/ へ写す（図の道具は写さない）。tests/site.rs と tests/serve.rs の写し（便 29）も同じ形で図の道具は無い。tests/figure.rs の写しは repo の vendor/archify/ を親 dir へ再帰で写す（43 file）。full.yaml の fig-1（2 箱・layout 済み）は道具を通る（受け皿の実測）。expected-note.html（便 28 の凍結）は図の card 1 を持つ。

(a) 図の生成器の口（`crates/folio/src/figure.rs`）。関数 derive の「図の行を探した後」の部分を関数 render（pub・引数は dir・図 id・図の型の字・spec の X・戻り値は図の本体の String）に切り出し、derive は行を探して render を呼ぶ形にする（振る舞いと文言は不変・便 30 の歯は期待不変で緑）。

(b) 設計ノートの面の図の章（`crates/folio/src/face_note.rs`）。関数 figures_chapter を次に改める。帯（h2「図 <数> 枚」・kicker「図」）と chapbody は同じ。chapbody の中は section-lead-callout と card でなく、図ごとに図の枠: figure の要素（figure-panel の dc・data-role の値 diagram・id の値は図の id を escape したもの）→ fig-title（fn「図 <i>」（i は 1 から）・題は caption の逐語・fig-tools に zoom-btn「拡大」・zoom-close「✕ 閉じる」・face_srs.rs の figure_open と同じ字面で凡例の hint は無し）→ 図の本体（figure.rs の関数 render の戻り値をそのまま 1 行で・escape しない・前後に改行 1 つ）→ figcaption（span の ver「図 <i> · <型の名札> · <図の id> · 根拠: <refs の各 id のリンクを「・」で>」・refs が無いか空なら「根拠:」以降を出さない）→ `</figure>`。型の名札は閉じた表（β・5 組）: archify-architecture →「構成図（architecture）」・archify-workflow →「手順図（workflow）」・archify-sequence →「順序図（sequence）」・archify-dataflow →「流れ図（dataflow）」・archify-lifecycle →「状態図（lifecycle）」（字面は parts.json の figure_body_classes.type_ids と同じ・歯で突き合わせる）。表に無い型（既存 3 型を含む）は関数 render が 2 にする（「図の道具の型でない」）ので面も 2。図が 1 枚でも導出できなければ面全体が 2（全部か無しか・前の面は残る・FR15）。定数 FIGURE_CD と card・section-lead-callout の使用は図の章から消す（section-lead-callout は面の他の章で使わないので PARTS から外す・item-row 等は不変）。PARTS は figure-panel を足して 7 → 7（section-lead-callout を外し figure-panel を足す）。toc の図の項（k「図」・t「<数> 枚」）と cover-meta の図「<数> 枚」は不変。図の章が無いとき（figures が 0）の形は不変。
- 図の本体の中の id は道具が付ける（archify-diagram-title 等）。同じ面に図が 2 枚以上あると道具の id が重複しうるが、本便は道具の出力を変えない（意味の属性は落とさない・ADR-4 決定 (3)）＝重複は面の検査（parts --check）の対象外で、閲覧に影響しない（aria の参照だけ）。図の id（正本の id）は figure の id 属性に置き、節の anchor（s<n>）や approval と同じ形の字（英小文字・数字・ハイフン）なので衝突は正本の側で防ぐ（床の own-id-space）。

(c) 図の class の様式（`design-intent/preview/folio.css`）。design token を 1 か所（既存の最初の `:root` 区間の末尾）に足す。値は既存の token を指す（明るい画面の `:root` 区間には足さない＝指した token が切り替わる）: `--fig-backend: var(--ok)` と `--fig-backend-fill: var(--ok-tint)`・frontend → brand / brand-tint・external → neutral / neutral-tint・database → violet / violet-tint・cloud → warn / warn-tint・messagebus → ink-soft / paper-3・security → bad / bad-tint・lane → line / paper-2・`--fig-grid: var(--line-soft)`・`--fig-mask: var(--paper-2)`・`--fig-arrow: var(--ink-soft)`・`--fig-arrow-emphasis: var(--brand)`・`--fig-text: var(--ink)`・`--fig-text-muted: var(--ink-soft)`・`--fig-text-dim: var(--ink-faint)`。規則（figure-panel の規則の直後に 1 区間・選択子は figure-panel の data-component の属性選択子の下）: 箱 7 種 `.c-<種別>` は fill が `--fig-<種別>-fill`・stroke が `--fig-<種別>`（種別 = backend・cloud・database・external・frontend・messagebus・security）・`.c-lane` は fill が lane-fill・stroke が lane・stroke-dasharray 6,6・`.c-grid` は stroke が grid・fill none・`.c-mask` は fill が mask・stroke none／線 `.a-default` は stroke が arrow・`.a-emphasis` は arrow-emphasis・`.a-dashed` は database・stroke-dasharray 4,4・`.a-security` は security・5,5（fill は none）／矢じり `.m-default` `.m-emphasis` `.m-dashed` `.m-security` は fill が同じ順の token／文字 `.t-primary` は fill が text・`.t-muted` は text-muted・`.t-dim` は text-dim・`.t-backend` `.t-messagebus` `.t-security` は fill が種別の token／印 `.s-<種別>`（7 種）は color が種別の token・`.semantic-sigil` は fill none・stroke currentColor・stroke-width 1.35・stroke-linecap round・stroke-linejoin round・`.semantic-sigil .sigil-fill` は fill currentColor・stroke none／`svg` は display block・width 100%・height auto／`svg text` は font-family が `--font-sans`。33 語すべてが選択子に現れる（parts --check の母集団）。色の直書きは 0（token だけ・R-3）。印刷の区間（618 行）は触らない。

(d) 部品目録（`design-intent/preview/parts.json`・`tests/fixtures/floor/parts-catalog.json`）。figure-panel の faces に note を足す（index・constitution・srs・note の順）。凍結目録も同じ 1 か所。他は変えない（build.rs が目録から生成する部品の faces がこれで note を持つ）。

(e) 写し。tests/face_note.rs の fixture_copy・tests/site.rs の fixture_copy・tests/serve.rs の built_site に、repo の `vendor/archify/` を一時 dir（src/ の親）の `vendor/archify/` へ再帰で写す（tests/figure.rs の写しと同じ形）。面の導出が図の道具を呼ぶため。

(f) 凍結 fixture。`tests/fixtures/face/expected-note.html` を (b) の形で再凍結する（差分は図の章だけ＝他の章は byte 不変を歯で見る・図の本体は full.yaml の fig-1 を道具が描いたもの）。組み立ての歯（tests/site.rs）の note-full.html の期待は同じ file なので追従。図の凍結 anchor（tests/fixtures/figure/anchor/）は変えない。

(g) 歯。`crates/folio/tests/face_note.rs`（関数名は face_note を含める・`--test face_note` の scope）: 既存 16 本は写しの変化に追従（凍結の歯は再凍結・census の歯は「図の card 1」「図の cd 1」の代わりに次を見る）。
1. 実の正本: figure-panel が figures の数（1）だけ在り data-role diagram を持つ ∧ `<svg` が図の数だけ ∧ fig-title に caption の逐語 ∧ figcaption の ver に「構成図（architecture）」と図の id ∧「図の生成はまだ無い」が 0 ∧ card accent warn が 0 ∧ 型の名札 5 組が parts.json の figure_body_classes.type_ids と字面一致（歯の中で parts.json を読む）。
2. 実の正本の面で parts --check 合格（既存の歯・図の class 33 語のうち面に出る 17 語が folio.css に在る）∧ 面の class のうち `c-` `a-` `m-` `t-` `s-` で始まる語と semantic-sigil・sigil-fill が全部 parts.json の figure_body_classes の 5 一覧（node_kind・edge_kind・arrowhead・text_role・sigil）の和に在る。
3. 写しの fig-1 の spec から layout を消す（道具の検査に落ちる形）→ 面を --write = 2 ∧ stderr に「図の道具の検査を通らない」∧ 先に書いてあった面の byte が不変（FR15 の「前の生成物を上書きしない」の面の側）。
4. 写しの親 dir の vendor/archify/bin/archify.mjs を消す → 2 ∧「図の道具が無い」。
5. 写しの fig-1 の type を pipeline-rail に → 2 ∧「図の道具の型でない」。
6. 写しの figures を空の一覧にした面で、図の章と figure-panel が 0 ∧ toc に図の項が無い（既存の形の確認）。
7. 写しで 2 度 --write して同じ byte（図を含めて決定的）。
8. 再凍結の面と、写しの figures を空にした面の 2 面で、図の章（id が s7 の section の開始から終了まで）と cover-meta の図の数と toc の図の項を除いた残りが byte で同じ（前の版の凍結を歯に埋め込まない）。
`crates/folio/tests/site.rs`（関数名は site を含める）・`crates/folio/tests/serve.rs`（関数名は serve を含める）: 写しの追加だけ・期待は不変（site の凍結の歯は expected-note.html に追従）。`crates/folio/tests/parts.rs`（関数名は parts を含める）: 本文不変・凍結目録の note を足して緑。`crates/folio/tests/figure.rs` と `crates/folio/tests/parts.rs`: 本文も期待も変えない（verify の scope の置き場として write-set に載せる・便 29 の教訓）。

(h) 便 30 までの形との接続: 新規 file は無い。`crates/folio/src/face_note.rs` は (b)（見積 +40 行・card の分が減る・余地 498）・`crates/folio/src/figure.rs` は (a)（+15）・`design-intent/preview/folio.css` は (c)（+45 行）・`design-intent/preview/parts.json` と `tests/fixtures/floor/parts-catalog.json` は (d) の 1 語・`crates/folio/tests/face_note.rs` は (g)（+120）・`crates/folio/tests/site.rs` と `crates/folio/tests/serve.rs` は (e)（各 +10）・`crates/folio/tests/figure.rs` と `crates/folio/tests/parts.rs` は不変（verify の scope）・`tests/fixtures/face/expected-note.html` は再凍結。`face.rs`・`face_srs.rs`・`face_adr.rs`・`face_index.rs`・`site.rs`・`parts.rs`・`note.rs`・`folio-ui.js`・`vendor/`・`tests/fixtures/figure/`・`full.yaml`・`example.yaml`・`.github/workflows/` は触らない。外部 crate は増えない。正規表現は使わない。size は M = face_note.rs と tests/face_note.rs の増分が中・他は小。

## 2. 範囲

- 入れる: 設計ノートの面の図の章に図の本体・figure-panel の note 対応・folio.css の図の token と 33 語の規則・写し 3 か所に図の道具・expected-note.html の再凍結・歯。
- 入れない: 図の生成器の振る舞いの変更（便 30）・図の閲覧の部品（箱の強調・経路の追跡＝意味の属性を読む folio の部品・別の便）・既存 3 型の図の道具化・道具の出力の加工（lang・desc・id は道具のまま）・入口の棚と build の変更（面に埋まるので file は増えない）・walk 承認（着地後・契約の外）。

## 3. 部品

| id | 名 | 役 |
|---|---|---|
| chapter | 図の章 | figure-panel + 図の本体 + figcaption |
| token | 図の token | `--fig-*` を既存 token から |
| rules | 図の規則 | 33 語の class を token で塗る |
| copies | 写し | 面と組み立ての写しに図の道具 |

## 4. 検査（歯）

§1 (g) のとおり。共通の検証は `.vessel.toml` の common-verify（便 0〜便 30 の歯は期待不変で全部回る・CI は setup-node 20）。

## 5. 依存

外部 crate は増やさない（clap / yaml-rust2 のまま）。図の道具の写しと Node.js は受け皿（便 30・A-3.1 承認 2026-09-19）のまま。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "af"
title = "設計ノートの面に図の本体（SVG）を figure-panel で埋め、図の class 33 語を folio.css の design token で塗る（通らない図は面ごと 2・前の面を残す）"
req = ["FR15", "NFR2", "FR9"]
section = "1"
write-set = ["crates/folio/src/face_note.rs", "crates/folio/src/figure.rs", "design-intent/preview/folio.css", "design-intent/preview/parts.json", "tests/fixtures/floor/parts-catalog.json", "crates/folio/tests/face_note.rs", "crates/folio/tests/site.rs", "crates/folio/tests/serve.rs", "crates/folio/tests/figure.rs", "crates/folio/tests/parts.rs", "tests/fixtures/face/expected-note.html"]
verify = ["cargo nextest run -p folio --test face_note face_note", "cargo nextest run -p folio --test figure figure", "cargo nextest run -p folio --test site site", "cargo nextest run -p folio --test serve serve", "cargo nextest run -p folio --test parts parts", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "face_note の歯（再凍結・実の正本の figure-panel と名札・parts 合格と class の母集団・通らない図で 2 と前の面・道具の不在・型外・図なし・決定的・図の章の外は不変）が緑、figure の歯が期待不変で緑、site の歯が expected-note.html に追従して緑、serve の歯が期待不変で緑、parts の歯が凍結目録の note で緑、clippy が 0 警告で CI が通る"
<!-- contracts:end -->
