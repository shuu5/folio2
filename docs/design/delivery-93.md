# 設計: 便 93 — 規則の表の行 R-17 を folio check が機械で数える床の歯（NFR3 / FR5）

- 要件: NFR3（文書間の参照 id の未解決は行 R-4 の値にする＝参照は必ずつながる）/ FR5（folio は構造の床を実行し、結果を 合格・不合格・まだ分からない の 3 値で返す）
- 条: P-3.1（機械で決定的に検査できる項目は床に置く）/ P-4.2（判定できないものは「まだ分からない」として表に出す）/ P-5.1（規則・閾値・型の一覧は型付きデータに置く）/ P-5.2（文書と台帳からは規則を id で参照する）/ P-6.3（同じ内容を 2 つの面が持つとき一方を正本とし他方は導出する）/ P-10.1（検査は独立した凍結 anchor を 1 本以上持つ）/ P-17.1（id を再利用・改番しない）/ N-2.1（散文にしか無い規則を規則として扱わない）/ N-3.1（規則の例外機構を足さない）
- 出所: 規則の表の行 R-17（裁定 = 持ち主 2026-09-22 10:2x JST・逐語「それで進めて」／行の追加の承認 = 同 11:42 JST・逐語「全部承認する」・どちらも台帳 f2-648 notes・対話面 R-8）。設計ノート docs/design/graph-and-incremental-ceiling.md §8 の便の列の 3 本目（G0'）。後付けの設計ノート docs/design/edge-retrofit-2026-09-22.md §9 の 1 と、独立の検証役の申し送り（歯を書くときは 否定・対象外・帰結 の 3 語形を数えない）。判断の記録 ADR-13 決定 (3-b)。
- 置き場: この文書は folio2 の設計ノート。契約表は末尾の区間。審査の材料は行 cp が指す §1 だけなので、判定に要る材料は §1 に全部置く。write-set は手書き（新しい file は crates/folio/src/mentions.rs の 1 本で先頭に + を付ける・縮む file は無い）。
- 門: 本便は design-intent の下の正本（rules.yaml・adr/ADR-13.yaml）を書き換えるので天井の門の対象である。席が実測した folio ceiling --gate は 2（まだ分からない・印が古い）を返す。持ち主の裁定 D-12（2026-09-21 23:45 JST・逐語「推奨で進めて」）により門の外で受ける。
- 前の便: 便 90（要件の欄 adrs）・便 91（規則の表の行の欄 refs）・便 92（判断の記録の帰結の欄 produced）。3 本とも着地済み（main 7942194）。受け皿が揃うまで歯を入れないという順は設計ノート §8 が固定しており、本便はその最後の 1 本である。
- 改訂 b（2026-09-22 14:1x JST・run f2-648.131-20260922T050042Z の質問 about:write-set への回答）: 実装側が crates/folio/tests/adr.rs の f92_ の歯 3 本（ADR-1 の写しの produced: [ADR-2] を produced: [P-6] ／ [FR16] ／ ADR-2 に変異させる）が、ADR-1 の帰結の散文が ADR-2 を指したままになるため、狙いの違反に加えて種別 R-17 の違反を 1 件出して落ちると報告した。歯の意図は produced の欄の形の検査であり、ADR-2 を外す必要は無い。そこで write-set に crates/folio/tests/adr.rs を足し、3 本の変異を ADR-2 を残す形（produced: [ADR-2, P-6] ／ [ADR-2, FR16] ／ 一覧でない値）に直す。歯の数と期待の字面は変えない。これ以外の変更は無い（(i) の運ばないものから adr.rs を外した）。
- 改訂 c（2026-09-22 14:4x JST・run f2-648.131-20260922T051910Z の門の不合格への応答）: 門は歯 5 の done（行 R-17 を消すと参照 id の違反ちょうど 1 件）を実装が裁定なしに弱めたと判定した。事実は席が実測した＝行 R-17 を落とすと参照 id の違反は 2 件（憲法 N-2 の関係の欄と、要件書の承認の来歴 meta の印が R-17 を名指す分）。契約の側が事実を取り違えていたので、歯 5 と done を ちょうど 2 件 に直す。併せて改訂 b の (h) の 3 本目の歯の名を f92_an_adr_that_does_not_exist_is_a_violation に正す。これ以外は変えない。

## 1. 目的と中身

規則の表の行 R-17 は 2026-09-22 に発効したが、機械の検査を持たない。行の注が自分でそう書いている（歯は便で足す・それまでこの行の判定は「まだ分からない」）。本便はその歯を足す。**行そのものは 1 字も変えない**（行の変更は裁定 id を要する・P-17.1）。歯は行の what と population を機械の読みに落とし、実装の型付きの定数（P-5.1）でその読みを閉じる。

歯を入れると床が赤くなる分は、本便の write-set の中で設計文書の側を直して **0 にしてから着地させる**。直すのは 2 か所だけで、どちらも「本物の依存なのに型付きの欄に書き写されていなかった」対である。

### (a) 実測（2026-09-22・main 7942194・便 91 / 92 の着地後）

- 器の受付の行数の式（空行を含む全行を数え、1 行の字数が 120 を超える行は 切り上げ(字数 ÷ 120) − 1 だけ足す）で測った値と余地（1500 − 値）。src = check.rs 861・余地 **639**／rules.rs 295・余地 1205／refs.rs 431・余地 **1069**／link.rs 657・余地 843／main.rs 542・余地 958／adr.rs 1287・余地 213。歯 = tests/check.rs 1024・余地 **476**／tests/floor_cases.rs 760・余地 740。本便が触る src は新しい file（余地 1500）と check.rs と main.rs と link.rs で、**どれも余地が M の見積 300 を上回る**。adr.rs は 1 字も触らない。
- **id を拾う口は既に在る。** crates/folio/src/refs.rs の scan_ids（202 行・pub）が 条と規範文・要件書の 5 接頭辞・規則の表の行 を正規表現を使わず字の走査で拾う。判断の記録の id は crates/folio/src/link.rs の scan_adr_ids（276 行）が同じ形で拾う。後者だけが private なので **pub(crate) にする（1 語の変更・振る舞いは 1 つも変わらない）**。新しい走査も新しい正規表現も足さない。
- **節点の索引を組む材料も既に在る。** 憲法の条と規範文・規則の表の 2 節・要件書の 7 節・判断の記録の記録は、床が既に読んでいる木（check.rs の Sources と adr::check_adr の返り値）にそのまま在る。席が数えた節点は **198**（条 27・規範文 67・規則の表の行 29・要件書の 7 節の行 62・判断の記録 13）。
- 席の数えの口は、後付けの 1 回目が使った席の実験 ~/.local/share/folio2/handoff-2026-09-21/grill/experiment_prose_edges.py の走査を写し、対の判定を行の単位に変えた script（scratchpad・repo には置かない）。以下の数はすべてその script を main の作業ツリーに当てて出したものである。
- 囲む行の id が取れて、指す先も節点である言及 = **704**。うち受け皿の表（(b) 4）で外れるもの = **21**。数える言及 = **683**。
- 683 のうち、その行の型付きの欄（無向・(b) 5）で満たされないもの = **31**。うち語形の閉じた一覧（(e)）で外れるもの = **22**。残り **9** が (d) の書き写しの対象である。
- 歯の関数名の接頭辞。grep -rn 'fn f93_' crates/folio/tests は今 **0 本**（使われている最大は f92_）。

### (b) 数え方（実装の型付きの定数に置く 5 つの閉じた一覧）

行 R-17 の what は 設計文書の中で id を持つ行の散文の欄に現れた、その行以外の id のうち、その行の型付きの欄に無いものの数 で、値は 0 件 である。機械の読みは次の 5 つで閉じる。どれも新しい file crates/folio/src/mentions.rs の定数に置く。

1. **対象の file（9 種）** = 憲法・規則の表・要件書・語彙・天井の正本・入口の棚・相談窓口・判断の記録（adr/ の記録）・設計ノート（design-note/ の正本）。行 R-17 の population が名指す 9 つと 1 つも違わない。**支度表（intake-sheet.yaml）は population に無いので入れない。** 席の実測では語彙・天井・入口・相談窓口・設計ノートに id の形の行は 1 つも無く、この 5 file からの言及は 0 である（走査は掛けるが数は出ない）。
2. **id を持つ行** = id の欄を持つ表のうち、その値が 1 の索引に在るもの。条・規範文・規則の表の行・要件書の 7 節の行・判断の記録の記録の 5 種で、行の種類はそのまま受け皿の表の鍵になる。
3. **散文の欄** = 型付きの欄の閉じた一覧（21 語 = article・articles・amended_by・amends・ac・adrs・basis・figures・goals・produced・reads・ref・refs・relations・req・reqs・rules・sections・target・verifies・verify）の補集合から、さらに **最上位の 2 節（meta と schema）** と **来歴と反対側からの確認の欄 6 つ（approval・date・grill・ruled_at・ruling・source）** を落としたもの。後の 2 つは行 R-17 の population が 数えない言及 として名指す 承認と版の来歴（承認の欄と meta の節）・生成区間（schema の節）・反対側からの確認（grill）と反証の記録 の機械の読みである。21 語には便 90 の adrs・便 91 の refs・便 92 の produced を含める。
4. **受け皿の表** = 出所の行の種類ごとに、その種類が既に持つ型付きの欄が受けられる指す先の種類。行 R-17 の population の末尾（書き写す先は、その行の種類が既に持つ型付きの欄だけ）を機械に持たせたものである。**書き写す先が無い組は数えない。** 表は次のとおり。
   - 条 → 全部（relations の 4 名前空間と amended_by）
   - 規範文 → 無し（規範文の行は型付きの欄を持たない・席の実測では出所になった規範文は 0）
   - 規則の表の行 → 全部（article と、便 91 の refs）
   - 判断の記録 → 全部（basis と、便 92 の produced と amends）
   - 要件・非機能要件 → 条・規範文・目的・規則の表の行・受入基準・判断の記録（basis・goals・rules・verify.ac・便 90 の adrs）
   - 制約 → 条・規範文・規則の表の行（basis と rules）
   - 受入基準 → 要件・非機能要件（verifies）
   - 目的・関係者・出力 → 無し
5. **満たされたかの判定は対を無向で見る。** その行の型付きの欄に指す先が在るか、**指す先の行の型付きの欄にその行が在るか**のどちらかなら満たされたとする。規範文の id は親の条へ丸めてから照合する（行 R-17 の出所の後付けの設計ノート §3.1 が定めた書き方に合わせる）。
   - 無向にする理由は 3 つある。① 後付けの 1 回目の数え（279 対）が対を無向で数えており、行 R-17 はその数えの上に立っている。② 便 90 は 逆向きの辺が既に在る 5 対も書かない を席の裁定として置き、その便は着地している（有向で読むとその裁定と正面から食い違う）。③ 天井の増分が使う近傍は無向で、逆向きの辺が 1 本在れば近傍は同じだけ広がる。
   - 席は有向でも数えた。(d) の書き写しの後で **有向 44 件・語形の後に 21 件が残り、21 とも逆向きの型付きの辺が既に在る対**である（例: 要件 FR16 の注が判断の記録 ADR-7 を指し、ADR-7 の根拠に FR16 が在る）。この 21 本を足しても近傍は 1 つも広がらない。
   - **行 R-17 の what の字（その行の型付きの欄に無いもの）は有向に読める。** 本便は無向で実装し、その食い違いを (i) の🔴 に出す（行の改訂は裁定 id を要するので本便では触らない）。

### (c) 行 R-17 そのものを歯の入り口にする

歯は検査のたびに rules.yaml の 閾値の行から **id が R-17 の行を探す**。

- 行が無ければ **歯は何もしない**（その文書の集合にこの規則は無い）。行の value が 0 件 でなければ **まだ分からない** を 1 件出す（P-4.2）。値の正本は行で、実装は写しを持たない（P-5.1）。
- これは行 R-16（散文の門）の値を検査のたびに rules.yaml から読む既に在る形（crates/folio/src/prose.rs の gate）と同じ作りである。
- **行を消して黙らせる口にはならない。** 憲法の条 N-2 の関係の欄が行 R-17 を指しているので、行を消すと参照 id が解けなくなり refs.rs が 種別 参照 id の違反を 1 件出す（席が実測した）。(h) の歯 5 がこれを押さえる。
- 凍結した土台 tests/fixtures/floor_base/design-intent/ の規則の表には行 R-17 が無い（席が実測）。したがって **既存の床の事例 141 件は 1 件も動かない**。

### (d) main の現物で数えた 31 件と、0 にするための書き写し 9 件

語形（(e)）で外れない 9 件は次のとおりで、9 件とも本物の依存である。本便の write-set で型付きの欄へ書き写し、**着地の時点で違反 0 にする**。

| 出所 | 指す先 | 出所の欄と字 | 書き写す先 |
| --- | --- | --- | --- |
| ADR-13 | A-2 | decision（条文の改訂が要ると分かったら、その時点で本判断を止めて条 A-2 の手順へ移す） | basis に A-2 |
| ADR-13 | A-2.3 | note（【敵対的検証】条 A-2.3 の検証は 2026-09-22 に実施済み） | 同上（親の条 A-2 で届く） |
| ADR-13 | P-11.1 | retreat.condition（数えられない回が行 R-7 の回数を超えたら…持ち主へ渡す〔条 P-4.2・P-11.1〕） | basis に P-11 |
| ADR-13 | P-12.2 | note（逐語と日付は承認の欄に置き、台帳の notes にも同じ逐語で記帳する〔条 P-12.2〕） | basis に P-12 |
| ADR-13 | P-14.3 | decision（CLAUDE.md には載せない〔行 R-2 の上限を超えるため・条 P-14.3〕） | basis に P-14 |
| ADR-13 | P-17.1 | consequences 2 件（裁定 id と時刻を行に付ける・条 P-17.1） | basis に P-17 |
| R-16 | R-9 | population（憲法・rules・要件書の規範文には掛けない〔R-9 / R-12 の領分〕） | refs に R-9 |
| R-16 | R-12 | 同上 | refs に R-12 |

- **ADR-13 の根拠の欄**は 5 つの条 id を足す。並びは同じ file 群の慣例（判断の記録 ADR-11 の根拠と同じ並び = 条 P を番号順 → A → N → R → D → 要件書 → 判断の記録）に合わせ、`basis` は次の 35 個になる: P-3, P-4, P-5, P-6, P-7, P-8, P-10, **P-11**, **P-12**, P-13, **P-14**, **P-17**, **A-2**, N-2, N-3, N-5, R-2, R-4, R-7, R-8, D-12, FR11, FR13, FR14, FR17, FR18, FR19, FR20, NFR1, AC17, AC18, ADR-3, ADR-8, ADR-9, ADR-11。**決定・帰結・撤退条件・承認の欄は 1 字も触らない**（発効済みの判断の記録の本文は動かさない・書き写しだけを行う。後付けの 1 回目が 12 本の記録に対して行ったのと同じ操作である）。
- **R-16 の refs** は `[P-6.3, P-10.1, ADR-3]` から `[P-6.3, P-10.1, R-9, R-12, ADR-3]` になる（便 91 が定めた並び = 条と規範文 → 要件書 → 規則の表の行 → 判断の記録）。R-16 の母集団の文は 自分の掛からない範囲を R-9 と R-12 の領分として名指して切っている＝後付けの設計ノート §3.3 が 数える と定めた 境界を名指す非拡大の宣言である。便 91 はこの 2 対を population の欄から拾えておらず、本便が補う。**行 R-16 の what・value・status・ruling・population・note は 1 字も触らない**（refs の一覧に 2 個足すだけ）。
- **受け皿が無くて数えない 21 件**（(b) 4 で外れる分）は 要件 → 要件・制約 が 16 件、受入基準 → 判断の記録・規範文 が 3 件、制約 → 要件 が 2 件である。判断の記録 ADR-13 決定 (3-b) は 要件書の項がほかの要件・制約を指す欄（12 対）を保留としており、本便はその保留に従う（欄を発明しない）。
- **要件書（srs.yaml）は 1 byte も触らない。** 書き写しの対象が 1 件も無いためである。

### (e) 数えない言及の語形（閉じた一覧 10 語）

行 R-17 の population は、機械では文の意味を読まないと決められない 3 つの除外を名指している（その行に掛からないと述べた言及〔対象外・本判断の外〕／その行が生んだもの／先例の引き合い〔同じ運び方・同形にする〕）。括弧の中はその語形そのものである。歯は **言及を含む 1 文（区切りは 。）にこの一覧のどれかが在れば数えない**。一覧は実装の型付きの定数に置く。

| 語形 | 行 R-17 が名指す種別 | main で外れる言及 |
| --- | --- | --- |
| 対象外 | 掛からないと述べた言及 | 6（ADR-3 / 4 / 5 / 6 / 7 / 8 → A-2.3・どれも 本判断は条文を改訂しないので A-2.3 の対象外） |
| 本判断の外 | 同上 | 0（行が名指す語形なので一覧に入れる） |
| 同じ運び方 | 先例の引き合い | 1（ADR-8 → ADR-6） |
| 同形 | 先例の引き合い | 1（ADR-11 → NFR1） |
| と同じく | 先例の引き合い | 2（ADR-1 → P-1 / FR1・番号の付け方の例示） |
| と揃う | 先例の引き合い | 3（R-12 → R-9 / R-10 / R-11） |
| 審査の記帳 | 承認と版の来歴 | 4（NFR2 → ADR-7・NFR3 → R-1 / R-9 / R-16） |
| 根拠から | 承認と版の来歴 | 3（FR19 → P-14 / P-14.3・NFR1 → P-14・どれも 根拠から条 P-14 を外した の文） |
| へ移した | 承認と版の来歴 | 1（R-12 → P-14・指す条を P-14 から P-5 へ移した の文） |
| と書いたら | id の形の例示 | 1（NFR3 → P-2・平易文の書き方の例） |

- **行き過ぎの実測。** 10 語のどれかを含む文に在る言及は main で 32 件で、そのうち 22 件が上の表の分、残り 10 件は **既に型付きの欄で満たされている言及**である（外しても違反の数は変わらない）。席はその 10 件の文を全部読み、10 件とも 先例の引き合い か 版の来歴 の文であることを確かめた（例: ADR-6 → ADR-5 の 判断の記録 ADR-5 決定 (2) と同じ運び方）。**種別を取り違えて外している文は 1 つも無い。**
- **この一覧は閉じる方（fail closed）に効く。** 同じ種別を別の言い方で書いた文は語形に当たらないので違反として出る＝書き手は 型付きの欄へ書き写す か 語形の一覧に足す（実装の定数の変更＝この設計ノートに根拠を書く）かのどちらかを選ぶ。黙って通る口は持たない（N-3.1）。
- **id の形の例示 は行 R-17 の population が名指していない種別である。** 本便は機械の読みとして 2 語（と同じく・と書いたら）を置くが、行の側に種別が無い＝実装が行より狭い。この食い違いは (i) の🔴 に出す。

### (f) 置き場と大きさ

- 新しい file **crates/folio/src/mentions.rs**（+）に置く。設計ノート §8 の G0' は refs.rs か新しい mentions.rs のどちらでもよいとしており、席は新しい file を選ぶ。理由は、refs.rs が持つ行 R-4 の数え（全欄の参照 id のうち実在しないもの）と R-17 の数え（散文の言及のうち型付きの欄に無いもの）は母集団も向きも別で、同じ file に置くと 2 つの規則の閉じた一覧が混ざるからである。行 R-4 の式と値と母集団は 1 字も変えない。
- **crates/folio/src/main.rs** に mod の 1 行、**crates/folio/src/check.rs** の check_dir に呼び出しを 1 群（判断の記録を読めたときだけ呼ぶ・note.rs の呼び出しの隣）、**crates/folio/src/link.rs** の scan_adr_ids を pub(crate) にする 1 語。
- 見積は mentions.rs が 290 行前後、ほかの 3 file が合わせて 15 行前後。**size は M**（触る src の余地は 新しい file 1500・check.rs 639・link.rs 843・main.rs 958 で、どれも M の見積 300 を上回る）。外部 crate は増やさない。新しい dir も作らない。

### (g) 床の事例（凍結 anchor・P-10.1）

凍結した土台 tests/fixtures/floor_base/design-intent/ は憲法 第 1.0 版の時点の写しで、**行 R-17 より半年ぶん前に書かれている＝生成側からも検査側からも独立している**。席がこの土台に同じ数えを当てると、数える言及 408（受け皿なしで外した 8）のうち **75 件が型付きの欄に無く、語形の一覧を掛けた後で 65 件**が残る。歯が本当に散文の言及を拾っていることは、この独立の土台で確かめる。

tests/floor_cases.yaml に事例を 1 件足す（**土台の file は 1 byte も触らない**・段の中で行を足す）。

- 段 1: rules.yaml の thresholds の末尾に行 R-17 を足す（id: R-17・article: N-2・kind: build-check・status: 仮・value: 0 件・stage: post・ruling は同じ file の共通の裁定の錨・what と population は英字の語を含まない日本語だけで書く〔行 R-9 の語彙の数えは英字の語だけを見る〕・what と population に id を 1 つも書かない〔この行自身が数えの対象になるため〕）。
- 段 2: constitution.yaml の 条 N-2 の関係の欄の rules に R-17 を足す（行 R-4 の逆参照の数えを満たすため）。
- 期待: 終了コード 1・出力に **R-3 の散文が AC2 を指すのに** が在る（土台の行 R-3 の注は M0 受入条件（AC2） と書きながら、行 R-3 は AC2 を受ける型付きの欄を持っていない）。件数は固定しない（土台には同じ形の言及が 65 件在るため）。
- 事例の総数 expected_cases を 141 から **142** にする。

### (h) 歯（関数名は f93_ で始める・置き場は crates/folio/tests/check.rs）

tests/check.rs の Work は実の design-intent を一時 dir へ写し、器の導出 file を根に置き、git init と 1 commit を行う。写しの rules.yaml を字面で変異させる口 mutate_rules（133 行・当て先がちょうど 1 か所でなければ自分で落ちる）も既に在る。本便の歯はその写しを使う（新しい歯の file も新しい dir も作らない）。**変異の当て先は R-16 の行の refs の 1 か所**で、字は `refs: [P-6.3, P-10.1, R-9, R-12, ADR-3]` とする（本便の後の実の rules.yaml にこの字は 1 回だけ現れる）。

1. **f93_the_real_sources_leave_no_prose_edge** — 実の正本の写しで素の床が 終了コード 0・違反 0・まだ分からない 0。写しの ADR-13 の根拠の欄が P-11 と P-12 と P-14 と P-17 と A-2 を持ち、写しの R-16 の行の refs が R-9 と R-12 を持つこと。本便の前の main にはその 7 個が 1 つも無いので **赤い歯**。
2. **f93_a_prose_id_outside_the_typed_fields_is_a_violation** — 当て先から R-9 を外して `refs: [P-6.3, P-10.1, R-12, ADR-3]` にして素の床 → 終了コード 1・違反ちょうど 1 件で、種別が R-17、行に R-16 と R-9 と 型付きの欄 が出る。当て先が無いので **赤い歯**。
3. **f93_an_excluded_phrase_is_not_counted** — 2 と同じ変異に加えて、R-16 の母集団の文の （R-9 / R-12 の領分） を （R-9 / R-12 の領分・対象外） に替えて素の床 → 終了コード 0・違反 0（語形の一覧が効く）。当て先が無いので **赤い歯**。
4. **f93_a_kind_without_a_receptacle_is_not_counted** — 実の写しの要件 FR12 の注が要件 FR11 と FR13 と FR14 を指すのに、要件の行には要件を受ける型付きの欄が無い（受け皿の表）ので違反にならないことを、素の床の 違反 0 と、その 3 個が FR12 の行のどの型付きの欄にも現れないことで見る。本便の前の main には歯が無く FR12 の欄も変わらないので **赤くない歯**（受け皿の表が効いていることの回帰）。
5. **f93_deleting_the_rule_row_is_not_a_silent_escape** — 2 と同じ変異に加えて、写しの rules.yaml から行 R-17 の行を落として素の床 → 終了コード 1・違反は全部 種別 参照 id で行に **id R-17 が実在しない** を持ち、**ちょうど 2 件**（憲法 N-2 の関係の欄が指す先が消えた 1 件と、要件書の承認の来歴 meta の印が規則の行 R-17 を名指す 1 件。席が main af7ae26 の写しから行 R-17 を落として実測した）。R-17 の違反は 1 件も出ない（歯が黙る）。当て先が無いので **赤い歯**。

回帰は verify の 2 行目で見る。tests/check.rs（本便の前の 46 本 ＋ f93_ の 5 本 = 51 本）と tests/floor_cases.rs（1 本・回す事例が 141 から 142 になる）。器の受付は verify の旗 --bin の次の語を filter 語と読むので、verify の行に --bin は書かない。

改訂 b で足した分: crates/folio/tests/adr.rs の f92_an_article_id_is_a_violation ／ f92_an_id_in_the_basis_is_a_violation ／ f92_an_adr_that_does_not_exist_is_a_violation の 3 本は、ADR-1 の写しの produced: [ADR-2] を当て先に変異させる（f92_produced_that_is_not_a_list_is_a_violation は一覧でない値の検査が先に落ちるので R-17 まで届かず緑のまま）。ADR-1 の帰結の散文は ADR-2 を指すので、ADR-2 を落とす変異は本便の歯が種別 R-17 の違反を 1 件足し、3 本が assert_figure_violation の ちょうど 1 件 で落ちる。直しは変異の値に ADR-2 を残すこと（produced: [ADR-2, P-6] ／ produced: [ADR-2, FR16] ／ produced: [ADR-2, ADR-99]）。期待の字面（id の形でない・自分の id か根拠・produced が一覧でない）と歯の本数は変えない。回帰は共通の検証（workspace 全体の nextest）で見る。

### (i) 本便が運ばないもの・宣言する食い違い・撤退条件

**運ばないもの。**

- **機械の読みの写しを設計文書へ導出すること（P-5.6 / 行 D-11）。** (b) の 5 つの閉じた一覧と (e) の語形の一覧は、床が違反と まだ分からない を出す判定に使う閉じた一覧なので、写しを人が読める型付きデータへ決定的に導出する義務が掛かる。置き場は rules.yaml の生成区間（rules.rs の床の木に群を足せば `folio schema` の対象は増えない＝要件 FR19 の文も schema.rs も触らない）だが、**導出の結果（生成区間の行数・byte 数・要約値・凍結 anchor tests/fixtures/schema/rules-region.txt）は席の権能では実測できない**（席は crates/ を編集できない）ので、測れる側が運ぶ次の便に回す。本便はその義務を (i) に明記し、台帳へ後続として起票する。
- 要件書（srs.yaml）・憲法の条文・語彙・ほかの判断の記録の本文・ほかの 7 file の生成区間・面の生成器・refs.rs・rules.rs・schema.rs・土台 tests/fixtures/floor_base/・id の一覧の凍結 anchor・CI の yml。
- 要件書の項がほかの要件・制約を指す欄（(d) の 21 件のうち 16 件）。判断の記録 ADR-13 決定 (3-b) が保留としている。

**宣言する食い違い（次の一括の🔴 に出す・行の改訂は裁定 id を要するので本便では触らない）。**

1. 行 R-17 の population の末尾（書き写す先は…＝この行のために欄を増やさない）は、便 90 / 91 / 92 で欄が 3 つ増えた事実と食い違う。歯の受け皿の表は **増えた後の欄**で組む。
2. 行 R-17 の what（その行の型付きの欄に無いもの）は有向に読めるが、歯は無向で数える（(b) 5 の理由 3 つ・有向で読むと逆向きの辺が既に在る 21 対を重ねて書くことになる）。
3. 行 R-17 の population は 数えない言及 に id の形の例示 を持たないが、歯は (e) の 2 語でそれを外す。
4. 行 R-17 の note（歯は便で足す・それまでこの行の判定は「まだ分からない」）は、本便の着地で古くなる。

**撤退条件。** 語形の閉じた一覧が 15 語を超えたとき、または語形の取り違えで本物の依存を 1 件でも取りこぼしたと分かったときは、この歯を止める。止め方は、行 R-17 を rules.yaml から落とすのではなく（それは (c) のとおり参照 id の違反になる）、mentions.rs の呼び出しを check.rs から外して歯を 1 本の まだ分からない に落とす便を 1 本出す（P-4.2・行の判定は測れない に戻る）。便 1 本で戻せる。

## 2. 範囲

- 入れる: 新しい file mentions.rs（数え方の 5 つの閉じた一覧と語形の一覧と判定）・main.rs の mod の 1 行・check.rs の呼び出し 1 群・link.rs の 1 語の可視性・ADR-13 の根拠の欄に条 5 個・R-16 の行の refs に規則の表の行 2 個・床の事例 1 件と総数の 1 行・f93_ の歯 5 本。
- 入れない: 機械の読みの写しの導出（生成区間・凍結 anchor・凍結の定数）・要件書・憲法の条文・語彙・ほかの判断の記録の本文・行 R-17 と行 R-16 の what と値と母集団・行 R-4 の式・面の生成器・adr.rs と refs.rs と rules.rs と schema.rs・土台 tests/fixtures/floor_base/・新しい dir・外部 crate。

## 3. 部品

| id | 名 | 役 |
|---|---|---|
| tooth | 床の歯 | crates/folio/src/mentions.rs（対象の file・id を持つ行・散文の欄・受け皿の表・無向の判定・語形の一覧） |
| wire | 結線 | main.rs の mod・check.rs の呼び出し・link.rs の scan_adr_ids の可視性 |
| switch | 入り口 | rules.yaml の行 R-17 を検査のたびに読む（行が無ければ黙る・値が 0 件 でなければ まだ分からない） |
| copy | 書き写し | design-intent/adr/ADR-13.yaml の根拠 5 個と design-intent/rules.yaml の R-16 の refs 2 個 |
| case | 床の事例 | tests/floor_cases.yaml の 1 件（凍結した土台に行 R-17 を足して数える・P-10.1） |
| teeth | 歯 | crates/folio/tests/check.rs の f93_ 5 本 |

## 4. 検査（歯）

§1 (h) のとおり。共通の検証は .vessel.toml の common-verify（workspace 全体の nextest と clippy）。

## 5. 依存

外部 crate は増やさない。

<!-- contracts:begin -->
schema = 1

[[contract]]
id = "cp"
title = "規則の表の行 R-17（設計文書の中で id を持つ行の散文の欄に現れた、その行以外の id のうち、その行の型付きの欄に無いものの数・値 0 件）を folio check が機械で数える床の歯を新しい file crates/folio/src/mentions.rs に足す。数え方（対象の 9 file・id を持つ行・散文の欄 = 型付きの欄 21 語と来歴 6 語と最上位 2 節の補集合・受け皿の表・数えない言及の語形 10 語）は実装の型付きの定数で閉じ、対は無向で見て規範文の id は親の条へ丸める。歯の入り口は rules.yaml の行 R-17 そのもので、行が無ければ黙り（凍結した土台には行が無いので既存の床の事例 141 件は動かない）、値が 0 件 でなければ まだ分からない を出す。main の現物で残る 9 件は判断の記録 ADR-13 の根拠に条 5 個・規則の表の行 R-16 の refs に行 2 個を書き写して 0 にしてから着地させる（行の what と値と母集団は 1 字も変えない）"
req = ["NFR3", "FR5"]
section = "1"
write-set = ["+crates/folio/src/mentions.rs", "crates/folio/src/main.rs", "crates/folio/src/check.rs", "crates/folio/src/link.rs", "design-intent/adr/ADR-13.yaml", "design-intent/rules.yaml", "tests/floor_cases.yaml", "crates/folio/tests/check.rs", "crates/folio/tests/floor_cases.rs", "crates/folio/tests/adr.rs"]
verify = ["cargo nextest run -p folio --test check f93_", "cargo nextest run -p folio --test check --test floor_cases", "cargo clippy --workspace --all-targets -- -D warnings"]
size = "M"
done = "f93_ の歯 5 本（実の正本の写しで素の床が終了コード 0・違反 0・まだ分からない 0 で ADR-13 の根拠に条 5 個と R-16 の refs に行 2 個が在る／R-16 の refs から R-9 を外すと種別 R-17 の違反ちょうど 1 件／同じ文に 対象外 を足すと違反 0／要件が要件を指す言及は受け皿が無いので違反 0／行 R-17 を消すと R-17 の違反は 0 で参照 id の違反ちょうど 2 件〔憲法 N-2 の関係の欄と要件書の承認の来歴〕）が全部緑、tests/check.rs の既存の歯 46 本と tests/floor_cases.rs の 142 件が全部緑、tests/adr.rs の f92_ の歯 3 本が ADR-2 を残した変異で緑、clippy が 0 警告で CI が通る"
<!-- contracts:end -->
