# 設計ノート: 辺の後付け — 散文にしか無かった依存を型付きの欄へ書き写す（2026-09-22）

- 位置: 持ち主の裁定 2026-09-22 10:2x JST（対話面 R-8・逐語「それで進めて」・台帳 f2-648 notes）。設計ノート `docs/design/graph-and-incremental-ceiling.md` 第 2 版 §2.2 の「その代わり、書き写す便を便の列に入れる（§8 の G0）」を、G0 を待たずに文書の側だけで先に済ませる便である。
- 読む人: orchestrator 席（次の便の契約を書く）・天井の 4 観点（増分の読む範囲が変わる）・持ち主（要件書 第 1.26 版の承認）。
- 状態: 起草（未承認）。要件書は v1.26 を起草し、発効は持ち主の承認を待つ。**第 2 版（2026-09-22）**: 独立の検証役の報告（`~/.local/share/folio2/handoff-2026-09-21/retrofit-verify.md`・判定 条件付き）の直し 8 点を全部反映した。差分は §9 に置く。
- 触っていないもの: **憲法の条文**（前文・条・規範文・平易文・機構・出所は 1 字も変えていない）・規則の表の行 D-12・すべての生成区間（`schema` 節）・実装（`crates/`）・凍結 anchor・歯（tests/）。要件 FR20 は**不変ではない**（関係の欄に `rules: [D-12]` を足した。規範文・平易文・確かめ方・注は不変で、D-12 の行そのものも不変）。憲法で変えたのは条 N-2 の `relations.rules` に行 R-17 を足した 1 行だけで、これは R-4 の双方向が要求する逆参照である（判断の記録 ADR-3 の帰結が「憲法の関係の欄に逆参照を置く（R-4 の双方向。条文の改訂ではない）」と書いた形・条の改訂の範囲〔A-2.2〕の外）。
- 本ノートは規則を持たない（N-2）。この便が足した規則は規則の表の行 R-17 だけで、値と母集団の正本はその行にある（ここには写さない・P-5.1）。

---

## 1. 目的

持ち主の問い（2026-09-22 01:1x）は「つなげるべき節点がつながっていないだけではないか」だった。席の実験（`~/.local/share/folio2/handoff-2026-09-21/grill/experiment_prose_edges.py`）は、散文の中の id の言及を辺に昇格すると、整合の所見が半径 2 に収まる割合が 64.8% から 79.5% へ上がることを示した。

昇格は走査で救い上げる手であって、条 N-2 の向き（散文にしか無い規則を規則として扱わない）に反する。**本当に依存なら、走査で拾うのではなく型付きの欄に書き写すべきである。** 本便はその書き写しを 1 回やり切り、残差を数え、同じことが次から起きないように床の規則を 1 本置く。

---

## 2. 対の内訳（実測）

数えの口は席の実験を写した `~/.local/share/folio2/handoff-2026-09-21/grill/retrofit_pairs.py`（元の script は触っていない）で、各対の出所（file・欄の道・前後 60 字）を TSV に落とす。

| | 値 |
| --- | --- |
| 走査した正本 | 憲法・規則の表・要件書・語彙・天井・入口・相談窓口・支度表・判断の記録 12 本・設計ノートの正本 |
| 囲む id が在る散文の言及 | 811 |
| 型付きの辺が無い（出所, 指す先）の対 | **279** |
| そのうち書き写した | **47 対** |
| 何もしなかった（親の条を含め既に欄に在る） | 113 対 |
| 欄の意味に合わないので除外 | **119 対** |
| 実際に欄へ足した id | **45**（判断の記録 37・要件書 8） |
| 型付きの辺の増分 | **+47**（45 の id ＋ 行 R-17 自身の配線 2 本） |

対と id が食い違うのは、1 つの行の複数の言及が同じ親の条へ丸まるからである（例: `ADR-2` の `P-17.2` / `P-17.4` / `P-17` の 3 対は `P-17` の 1 id になる）。**「書き写した 47」は対の数、「足した 45」は id の数、「+47」は辺の増分で、3 つは別の数である**（第 1 版はこの 3 つを「71 対」の 1 つに混ぜていた・検証役の指摘 6）。

出所の種類 → 指す先の種類（279 対の内訳・多い順）: 判断の記録 → 規範文 123・規則行 → 規範文 23・判断の記録 → 規則行 20・要件 → 判断の記録 12・規則行 → 規則行 10・判断の記録 → 要件 10・判断の記録 → 判断の記録 10・要件 → 要件 9・要件 → 規範文 7・判断の記録 → 条 6・判断の記録 → 受入基準 6・ほか 43。

**出所に憲法の条は 1 つも無い**（憲法は指される側だけ）。これは実測であって仮定ではない。

---

## 3. 欄への写し方の決まり（種類ごと）

**欄は増やさない。** 書き写す先は、出所の行の種類が既に持つ型付きの欄だけである。欄が受け付ける id の形は、欄の決まり（`adr/schema.yaml`・`srs.yaml` の `schema` 節・`rules.yaml` の `schema` 節）と、**面の生成器が実際に解く形**の両方に従う。後者は歯が守っており、破ると面が「まだ分からない」に落ちる。

| 出所の種類 | 欄 | 受け付ける id の形 | 出所 |
| --- | --- | --- | --- |
| 判断の記録 | `basis` | 条・要件書の 7 節・規則の表の行・判断の記録 | `adr.rs` の `is_basis_id`・`adr/schema.yaml` の `basis_note` |
| 要件・非機能要件 | `basis` | **条の id だけ** | `face_srs.rs` の `article()`（条の題を引く） |
| 要件・非機能要件 | `rules` | 規則の表の行の id | `face_srs.rs` の `rule()` |
| 制約 | `basis` | **条の id だけ** | 同上 |
| 条 | `relations.rules` ほか | 規則の表の行・条・要件書の id | `refs.rs` の `reverse()` |

### 3.1 規範文の id は親の条に落とす

`basis` は「条の id」を持つ欄である。要件書の 77 項はすべて条の id で、判断の記録 12 本の項のうち条を指すものも 1 つの例外なく条の粒である（規範文の id を持つ項は 1 つも無い）。言及が規範文の id（`P-6.4`）のときは親の条（`P-6`）を書く。規範文 ↔ 条 の辺（in-article）が構造として在るので、親の条を書けば規範文へは 1 段で届く。**123 対の「判断の記録 → 規範文」の大半は、親の条が既に `basis` に在ったので何もしていない**（113 件の「既に欄に在る」の主因）。

### 3.2 要件書の `basis` は条の id しか受けない（実装の振る舞い）

起草の途中で、要件書の項の `basis` に判断の記録や他の要件の id を書いたところ、歯 29 本が落ちた。字面は次のとおり。

```
folio build: まだ分からない: srs.yaml.requirements[6].basis[1]: 条 id「FR5」が無い
```

要件書の面の生成器は `basis` の各項を条として解き、解けなければ面ごと「まだ分からない」に落とす。これは**実装の振る舞いであって設計文書の字面ではない**ので、歯を直さず、その対を除外へ回した（§4 の E3b・26 対）。歯は 1 本も直していない（703 件すべて緑）。

### 3.3 出所の文が肯定の依存であること

**書き写すのは、出所の文が指す先に依っていると読めるときだけである。** 次の 3 つの形は id が出ていても辺にしない。

1. **否定・対象外**: 「本判断は条文を改訂しないので A-2.3 の**対象外**」「それらの扱いは**本判断の外**」。掛からないことを述べた文で、その id は根拠ではない。
2. **後ろ向き**: その判断が**生んだもの**（発効で生まれた要件・受入基準・規則の行・後続の判断）。判断の記録の面は `basis` を章 04「**根拠**と撤退条件」に描く（`face_adr.rs` の `CHAPTERS`）ので、生んだものを根拠に書くと読み手を誤らせる。
3. **先例の引き合い**: 「X と同じ運び方」「X と同形にする」（§4 の E6 と同じ型）。

**残すのは非変更・非拡大の宣言である。** 「rules 行 R-4 / R-9 の行と母集団は**変えない**」（ADR-1）・「母集団（NFR3・AC6）は**広げない**」（ADR-7）は、その行や要件を**境界として名指し、その値に依って自分の範囲を切っている**。影響しないことを確かめた先も欄に書く。掛からないことを述べた 1 の形とは別に数える。

### 3.4 欄が空のときは足す

`rules` の欄を持っていなかった要件 2 本（FR6・FR20）には、欄の決まりの任意の欄として `rules` を足した。欄名は新設ではない（`srs.yaml` の `schema.requirement_row.optional` に在る）。

---

## 4. 除外の一覧（119 対）

| 種類 | 件数 | 理由 | 例 |
| --- | --- | --- | --- |
| **E3 受け皿の欄が無い（規則の表の行）** | 33 | 規則の表の行が別の id を指せる型付きの欄は `article` 1 つだけで、値は 1 つ（`refs.rs` が `as_str` で読む）、しかも R-4 の双方向でその条に固定されている。閾値の行の `basis` は散文の欄（行 R-2 は実測の文を置いている）で、作法の行には `basis` が無い | `R-12 → R-2 / R-9 / R-10 / R-11`（注が 4 本の行を指す）・`D-12 → ADR-8 / FR20` |
| **E3b 受け皿の欄が無い（要件書の項）** | 26 | §3.2 のとおり `basis` は条の id しか受けない。要件から判断の記録・他の要件・制約を指す欄が無い | `FR9 → ADR-3`・`FR12 → FR11 / FR13 / FR14`・`CON9 → FR6 / FR14` |
| **E8 後ろ向きの辺（帰結の欄が要る）** | 16 | §3.3 の 2。その判断が生んだ要件・受入基準・規則の行・後続の判断を `basis` に入れると、面の「根拠」の章に帰結が混ざる。**受け皿は「帰結の欄」で、いまは無い**ので §4.1 の 4 へ回す | `ADR-8 → FR17 / FR18 / FR20 / AC15 / AC16 / AC18 / D-12`（どれも ADR-8 の発効で生まれた）・`ADR-4 → FR15 / R-15`（R-15 の裁定は「ADR-4 発効承認」） |
| **E7 否定・対象外の言及** | 7 | §3.3 の 1。掛からないことを述べた文 | `ADR-3 / 4 / 5 / 6 / 7 / 8 → A-2`（唯一の出所が「本判断は条文を改訂しないので A-2.3 の対象外」）・`ADR-9 → ADR-10`（「本判断の外」） |
| **E1 承認と裁定の来歴** | 17 | 承認欄（`approval.surface` の値そのもの・`approval.ruling` の逐語）・規則の表の `ruling`・制約の `source` は「誰がいつ決めたか」の記録であって、その行が何に依っているかではない | `R-1 → P-17.3`（発効承認の行）・`ADR-3 → ADR-4`（1 つの承認で 2 本発効した記録） |
| **E6 版の来歴と先例の引き合い** | 11 | 「第 N 版で根拠から外した」「N から移した」「N と同じ語に揃えた」「N と同形にする」。**外した根拠を書き戻すのは天井の裁定の巻き戻しになる** | `FR19 → P-14`（天井の 16 周目 整合 F-3 で外した）・`NFR1 → P-14`（23 周目 整合 F-2 で外した）・`NFR3 → R-1 / R-9 / R-16`（語を揃えた来歴）・`ADR-8 → ADR-6`（「ADR-6 の intake.yaml と同じ運び方」） |
| **E5 id の形の例示** | 4 | 番号の書き方の例として並べているだけ | `ADR-1 → P-1 / FR1`（「番号は P-1・R-7・FR1 と同じくゼロ詰めしない」）・`NFR3 → FR3 / P-2`（「『FR3 を見よ』『P-2 による』と書いたら」） |
| **E4 id の言及ではない** | 3 | 受入基準の固定の材料の `path` の綴りを走査が拾った | `AC14 → ADR-1 / ADR-2`（`tests/fixtures/face/adr/`） |
| **E2 反対側からの確認（grill）と反証の記録** | 2 | 検証で何を言われたかの記録で、その判断が何に依るかではない | `ADR-10 → N-3.1`・`ADR-9 → P-2` |

### 4.1 席へ返すこと（欄が要る）

**E3・E3b・E8 の 75 対は「欄が足りない」で落ちている。** 新しい欄は本便では発明していない。要るかどうかの判断は席と持ち主の手番で、材料は次のとおり。

1. **要件書の項が判断の記録を指す欄**（14 対）。`FR9 / FR10 / FR11 / FR12 / FR13 / FR14 → ADR-3`・`FR15 → ADR-4`・`FR16 → ADR-5`・`FR17 / FR18 / FR20 → ADR-8`・`FR19 → ADR-9`・`NFR1 → ADR-4`・`NFR2 → ADR-7`。どの要件も注の 1 文目が「判断の記録 ADR-n 決定 (k)」で始まっており、依存は明白で、向きも一定である。**逆向きの辺で代わりにはならない。** 走査は対を無向で数える（`retrofit_pairs.py` の `tuple(sorted(...))`）ので、逆辺が既に在る対はそもそも 279 の母集団に入らない＝この 14 対は**どちら向きの辺も無い**（便の前に判断の記録 → 要件の辺は 0 本・検証役の実測）。第 1 版の「逆向きの辺は既に `basis` で引ける」は誤りだった。
2. **要件書の項が他の要件・制約を指す欄**（12 対）。`FR12 → FR11 / FR13 / FR14`（撤去の前提）・`FR7 / FR9 → FR5`（構造の床に寄せている）・`CON9 → FR6 / FR14`。
3. **規則の表の行が条以外を指す欄**（33 対）。`article` を一覧にするか、`basis` を id の一覧の欄に作り直すか、どちらも欄の決まりと床の関数の改訂を伴う。R-4 の双方向の式に触るので、判断の記録が要る。
4. **判断の記録が生んだものを指す欄（帰結の欄）**（16 対）。`ADR-1 → ADR-2 / ADR-7`・`ADR-2 → ADR-11`・`ADR-3 → ADR-11 / ADR-9 / FR12 / R-16`・`ADR-4 → FR15 / R-15`・`ADR-8 → FR17 / FR18 / FR20 / AC15 / AC16 / AC18 / D-12`。`basis`（根拠）には入れられないが、辺としては本物で、**天井の増分がいちばん取りこぼす向き**（判断を直したときに、その判断が生んだ要件が読み直されない）。受け皿は `adr/schema.yaml` に新しい欄（例: `produced`）を足すか、要件の側の 1 の欄で逆から引くかのどちらかで、欄の決まりの改訂として判断の記録が要る。

---

## 5. 足した規則の行

規則の表に閾値の行 **R-17** を 1 本足した（条 N-2・`kind: build-check`・`status: 仮`・`stage: post`・`value: 0 件`）。行の本文と母集団と注はその行が持つ（ここには写さない・P-6.3）。裁定は持ち主 2026-09-22 10:2x JST（逐語「それで進めて」・f2-648 notes）。

- 数えない言及の正本はこの行の母集団の欄で、第 2 版で 3 つ足した（掛からないと述べた言及・その行が生んだもの・先例の引き合い）。境界を名指す非変更と非拡大の宣言は数える（§3.3）。
- 条を **N-2**（文章にしか無い規則を、規則として扱わない）に付けたのは、この行が止めるものが「散文にしか無い依存」だからである。設計ノート `graph-and-incremental-ceiling.md` §2.2 の 3 点目が同じ向きを述べている。
- **機械の検査（folio check の歯）はまだ無い。** 便で足すまで、この行の判定は「まだ分からない」（P-4.2）で、床の値ではなく書き手の作法として働く。行の注にそう書いた。
- 憲法 N-2 の `relations.rules` に `R-17` を足した（R-4 の双方向。条文の改訂ではない）。

---

## 6. 再計測（before / after）

`experiment_prose_edges.py` を 4 観点で、この便の前（`origin/docs/batch11`）と後（本 branch）に当てた。数えるのは**型付きの辺だけ**の側である（散文の言及を昇格した側は参考）。

| 観点 | 所見 | 2 段以内 before | 2 段以内 after | 3 段以上の止める before | after |
| --- | --- | --- | --- | --- | --- |
| 整合 | 88 | 63.6% | **65.9%** | 4 | 4 |
| 忠実さ | 80 | 75.0% | **80.0%** | 1 | 1 |
| 読みやすさ | 34 | 76.5% | 76.5% | 0 | 0 |
| 実態 | 87 | 72.4% | **78.2%** | 5 | 5 |

| | before | after |
| --- | --- | --- |
| 節点 | 268 | 269（行 R-17） |
| 型付きの辺（有向） | 719 | **766**（+47・うち 2 本は行 R-17 自身の配線） |
| 昇格でまだ増える対 | 266 | **236** |
| 半径 2 の近傍の大きさ（中央値 / 平均 / 最大） | 16 / 29 / 133 | 16 / 32 / 142 |

**読み方。**

- 整合は 63.6% → 65.9%。昇格の上限（81.8%）までの差 18.2 点のうち、**2.3 点を型付きの辺で取った**。残りの差は §4 の除外が持っている。うち受け皿の欄が無い 59 対と後ろ向きの 16 対が最大の塊で、**欄を足せばさらに取れる**（§4.1）。
- 第 1 版（検証役へ出した版）はここが 69.3% だった。差の 3.4 点は、§9 で外した 24 対が稼いでいたものである。**外した分だけ数は下がるが、下がった数の方が正しい。**
- 忠実さと実態も上がった（+5.0 点・+5.8 点）。書き写しの 37 件が判断の記録に入り、判断の記録が 4 観点すべての読む文書だからである。
- 読みやすさは動かない。語彙の語と天井の観点に置かれた所見が主で、それらは型付きの辺を 1 本も持たない（設計ノート §3.1 が床に語彙を入れた理由と同じ）。
- 近傍の中央値は 16 のままで、平均だけ 29 → 32 に太った。昇格の側（中央値 48）に比べれば小さい。**辺を選んで足す方が、走査で全部拾うより安い。**
- 「昇格でまだ増える対」が 266 → 236 としか減らないのは、親の条を書いても規範文の id の言及そのものは残るからである（走査は `P-6.4` を見て `P-6` は見ない）。この差は R-17 の歯を書くときに、走査の側で親の条に丸める式を置けば消える。

数え直し: `REPO=<作業場所> python3 ~/.local/share/folio2/handoff-2026-09-21/grill/experiment_prose_edges.py --vp <観点>`。対の一覧は同じ置き場の `retrofit_pairs.py`（出力 `retrofit_pairs.tsv`）。

---

## 7. 検査

| 検査 | 結果 |
| --- | --- |
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | 一致 8 file・終了コード 0 |
| `folio inject --check --dir design-intent` | 68 行 / 7912 byte 一致・終了コード 0 |
| `cargo nextest run --workspace --no-fail-fast` | 703 件すべて緑 |

（第 2 版でやり直した結果。数値は第 1 版と同じ。）

**歯は 1 本も直していない。** 途中で落ちた 29 本（§3.2）は、設計文書の字面ではなく実装の振る舞い（面の生成器が `basis` を条として解く）を守っていた歯なので、歯ではなく書き写しの側を直し、その対を除外へ回した。

---

## 8. 検証役の指摘と直し（第 2 版）

独立の検証役（読み取りのみ・報告 `~/.local/share/folio2/handoff-2026-09-21/retrofit-verify.md`）が、第 1 版（commit `c029f7b`）を 8 観点で検証した。判定は **条件付き**（憲法・規則の行 R-17・除外の妥当性・検査・再計測の追試は 可。書き写しの正しさと版上げの記述が 条件付き）。直しは 8 点で、全部反映した。

| # | 指摘 | 直し |
| --- | --- | --- |
| 1 | 「A-2.3 の**対象外**」の 1 文だけを出所に `A-2` を根拠へ入れた 6 件（ADR-3 / 4 / 5 / 6 / 7 / 8）は誤り | 6 件とも外した。同じ文の後半（「対話面 R-8 で取り」）は実依存なので `R-8` は残した |
| 2 | `ADR-9 → ADR-10` は出所が承認の逐語（自分で除外した型）と「**本判断の外**」 | 外した |
| 3 | 後ろ向きの辺 12 件（その判断が生んだ要件・規則の行・後続の判断を根拠に書いた） | 外した。**検証役の機械の式が拾わなかった同じ文の 4 件（`ADR-8 → AC15 / AC16 / AC18 / D-12`）も同じ理由で外した**ので、外した後ろ向きは合計 16 件 |
| 4 | 「行と母集団は**変えない**」「**広げない**」は根拠として弱い（推奨） | 残す方を選び、§3.3 に決まりとして書いた（非変更・非拡大は境界の名指し＝依存。掛からないことを述べた 1 の形とは別に数える） |
| 5 | §4.1 の「逆向きの辺は既に `basis` で引ける」は誤り（便の前に 0 本） | 直した。走査は対を無向で数えるので、逆辺の在る対はそもそも母集団に入らない |
| 6 | 「書き写した 71 対」は辺の増分（うち 2 本は行 R-17 自身の配線）で、実際に足した id は 69 | §2 の表を「対 47・id 45・辺の増分 +47」の 3 つに分けた（1〜3 の直しの後の数） |
| 7 | 「触っていないもの」に FR20 を挙げたのは食い違い（`rules: [D-12]` を得ている） | FR20 を外し、何が動いて何が不変かを書いた |
| 8 | 要件書の `status_note` の「71 対」 | 直した数に揃えた |

第 1 版との差分は **外した 24 対**（否定・対象外 7・後ろ向き 16・先例の引き合い 1）だけで、足した対は 1 つも増やしていない。検証役の「除外の妥当性 15/15 妥当」「意図して外した根拠の書き戻しは無い」「憲法の変更は 1 行だけ」「R-17 の欄は過不足なし」「検査 4 本とも再現」は第 2 版でも変わらない。

検証役が自分で走らせて再現した数（節点 268 → 269・辺 719・昇格の上限 81.8%・279 対の総数・出所に条 0）は第 1 版のまま正しい。**辺の after だけが第 2 版で 790 → 766 に下がる**（§6）。

検証役の申し送り 1 件（後続の便へ）: **R-17 の歯を書くときは「否定・対象外・帰結」の 3 語形を数えないと、この誤りを増やす向きに働く。**

---

## 9. 後続

1. **床の歯**（台帳 f2-648 の後続の便）。行 R-17 を `folio check` の歯にする。走査の範囲（型付きの欄の外・除外の種類）を実装の定数として閉じ、規範文の id は親の条に丸め、**「否定・対象外」「後ろ向き（その判断が生んだもの）」「先例の引き合い」の 3 語形を数えない**（検証役の申し送り）。それまで R-17 は「まだ分からない」。
2. **欄を足すかの判断**（§4.1）。要件書の項から判断の記録・他の要件を指す欄・規則の表の行から条以外を指す欄・判断の記録から生んだものを指す欄（帰結の欄）。欄の決まりと床の関数を触るので判断の記録が要る。**75 対**が待っている。検証役は「要件書の側（14 対・欄の名は `adrs` 推奨・大きさ S）だけを先に、判断の記録つきで出す」ことを推している。
3. 設計ノート `graph-and-incremental-ceiling.md` の §10.2 の 🔴 5 つは本便では 1 つも動かしていない。索引の口（便 G1）も起こしていない。
