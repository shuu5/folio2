# 設計ノート: 実装の区切りの図（2026-09-23）— 責務の 6 層・依存の向き・割り直しの便 106〜116

- 状態: **改訂 b**（反対側からの確認〔grill〕への応答）。判断の記録 **ADR-15**（提案中）の材料であり、判断そのものは ADR-15 が持つ。この文書は実測と割り方の案だけを持つ。
- 出所: 総点検の設計ノート `docs/design/audit-2026-09-23.md` の §3-c・§3-d と §4 の 🔴 3（持ち主の承認 2026-09-23 08:13 JST・対話面 R-8・逐語「問いは全部推奨で」）。
- 反対側からの確認: `~/.local/share/folio2/handoff-2026-09-23/adr15-grill.md`（独立の審査役・支持 2・退ける 5・まだ分からない 2・判定「直してから」）。改訂 b で改めた点は §8。
- 基準点: `origin/main` = **aff1b32**。実装の側 `crates/folio/src/` 44 file・正規化 26,160 行（審査役が独立に再現済み）。
- 走査の式（改訂 b で改めた）: 注釈（`//` と `/* */`）を剥がしてから、`crate::<区切り名>` と `use crate::{a, b}` の括り書きを解いて相異なる名を取る。区切りを宣言する入口 `main.rs` だけは接頭辞の無い `<区切り名>::` も数える（`mod` で宣言した名は接頭辞なしで解けるため）。自分自身は数えない。
- 正規化行 = 全行 + 各行の `ceil(字数 ÷ 120) − 1`（器 scribe2 の規則 `R-C4.line-width` = 120 と同じ式）。余地 = 1,500 − 正規化行。
- 置き場: folio2 の設計ノート。規則を置かない（P-5.4・N-2.1）。ここで決まったことは ADR-15 と便の契約表へ写す。

## 1. 責務の 6 層

| 層 | 責務 | 何を知っているか |
| --- | --- | --- |
| 0 土台 | 文書を何も知らない共通の型と計算 | 何も知らない |
| 1 読む | 正本の byte を型に直す。閉じた一覧・値域・床の木の型を持つ | 正本の形 |
| 2 検査する | 型に直したものを決まりに照らして数える | 正本の形 + 決まり |
| 3 導出する | 正本から別の型付きの出力を作る（生成区間・索引・束・印・anchor・支度表） | 上記 + 出力の形 |
| 4 面に出す | 人が読むページ（HTML）と図の本体（SVG）を書く | 上記 + 見た目 |
| 5 配る | 命令の入口と、生成物を置き・見せ・知らせるところ | 全部 |

**向きの決まり**: file は自分より下の層だけを名指す。同じ層どうしの名指しは許す。

総点検の設計ノート §3-d は 5 層（読む → 検査する → 導出する → 面に出す → 配る）を挙げた。土台を 1 つ足したのは、`verdict.rs`（判定の 3 値と終了コード）と `sha256.rs`（要約値の計算）が正本を 1 字も読まず、文書の形を何も知らないためである。読む層に入れると、その層の責務を「正本を型に直す」と言えなくなる。

層 5 の責務に「命令の入口」を足したのは、`main.rs` が置きも見せも知らせもしないのに層 5 に在るという食い違いを消すためである（審査役の直し 1）。

**層 1 の定義と実測が合う時点**: 「閉じた一覧を持つ」という層 1 の定義に実測が追いつくのは、§4 の便 108（部品目録の閉じた一覧）と便 109（入口の棚と設計ノートの状態の閉じた一覧）の後である。それまでは 2 つの閉じた一覧が検査の層と面の層に在る（審査役の直し 2）。

## 2. 区切りの図（層 × file × 責務 × 正規化行 × 余地）

太字の余地は 300 未満（size M の便が受付で断られる）。

| 層 | file | 責務（file の頭の注釈から） | 正規化行 | 余地 |
| --- | --- | --- | ---: | ---: |
| 0 土台 | `sha256.rs` | 要約値（sha256）の手書き。外部 crate を足さない | 123 | 1377 |
| 0 土台 | `verdict.rs` | 判定（3 値と終了コード）。実行できなかった検査は合格にしない | 94 | 1406 |
| 1 読む | `yaml.rs` | 正本の読み手。低水準の event から木を組み、重複キーを拾う | 975 | 525 |
| 1 読む | `constitution_enums.rs` | 憲法の値域の置き場（正本から組み立て時に導出した型） | 86 | 1414 |
| 2 検査する | `adr.rs` | 判断の記録（adr/）の欄の決まりの検査 + その床の定数 | 1390 | **110** |
| 2 検査する | `note.rs` | 設計ノートの正本の形の検査 + その床の定数 | 1362 | **138** |
| 2 検査する | `findings.rs` | 天井の所見 file と起動の記録の検査（--check） | 1096 | 404 |
| 2 検査する | `anchor.rs` | 凍結 anchor の列（anchors/）の検査 | 1004 | 496 |
| 2 検査する | `check.rs` | 正本 7 file の形の床と、床の共通の道具（重複 id・非空・行 id・未知の節） | 879 | 621 |
| 2 検査する | `lineage.rs` | 凍結 anchor の列の区間の消し込み | 863 | 637 |
| 2 検査する | `parts.rs` | 部品目録の閉じた一覧（catalog）と、面が使った部品の検査 | 672 | 828 |
| 2 検査する | `link.rs` | 判断の記録と正本 4 file・凍結 anchor の列の突き合わせ | 657 | 843 |
| 2 検査する | `ceiling.rs` | 天井の正本（ceiling.yaml）の形の検査 + 天井の床の定数 | 576 | 924 |
| 2 検査する | `refs.rs` | 参照 id の解決（R-4）と規則の表の行の逆参照・憲法の件数 | 431 | 1069 |
| 2 検査する | `gitcheck.rs` | 凍結 anchor の列の版管理（git）との照合 | 395 | 1105 |
| 2 検査する | `entrance.rs` | 入口の正本（index.yaml）の形の検査 | 378 | 1122 |
| 2 検査する | `mentions.rs` | 規則の表の行 R-17 の床（散文の言及と型付きの欄の対） | 341 | 1159 |
| 2 検査する | `prose.rs` | 設計ノートの散文の門（R-16） | 327 | 1173 |
| 2 検査する | `ids.rs` | 要件・判断・受入基準の id の消失と改番 | 316 | 1184 |
| 2 検査する | `vocab.rs` | 語彙の検査（規則の表の行 R-9・本文の英字の語） | 297 | 1203 |
| 2 検査する | `rules.rs` | 規則の表の欄の決まりの床 + その床の定数 | 295 | 1205 |
| 2 検査する | `gate.rs` | 天井の門（--gate）。印と今の正本から 3 値を返す | 252 | 1248 |
| 2 検査する | `intake.rs` | 相談窓口の正本（intake.yaml）の形の検査 | 252 | 1248 |
| 3 導出する | `graph.rs` | 設計文書の索引（節点と辺）を正本から組み直す | 738 | 762 |
| 3 導出する | `bundle.rs` | 天井の材料の束を組む（--write）+ 天井の正本の読み手 | 730 | 770 |
| 3 導出する | `sheet.rs` | 相談窓口の支度表を書く（folio intake） | 571 | 929 |
| 3 導出する | `schema.rs` | 欄の決まりの生成区間の導出（folio schema）+ 床の木の型 Floor | 476 | 1024 |
| 3 導出する | `freeze.rs` | 凍結 anchor と id の凍結を書く（--freeze-anchor / --emit-amends）+ 実行の様態の型 | 357 | 1143 |
| 3 導出する | `stamp.rs` | 天井の判定の印を書く（--stamp） | 343 | 1157 |
| 3 導出する | `inject.rs` | 憲法の前文と規範文を CLAUDE.md の生成区間へ書く | 263 | 1237 |
| 4 面に出す | `face_index.rs` | 入口の面の生成器（棚・支度表の節） | 1378 | **122** |
| 4 面に出す | `face_constitution.rs` | 憲法の面の生成器 | 1294 | **206** |
| 4 面に出す | `face.rs` | 面の生成の入口と共通の部品（表紙・章帯・カード・図の枠）+ 正本の cursor | 1138 | 362 |
| 4 面に出す | `face_note.rs` | 設計ノートの面の生成器 | 1023 | 477 |
| 4 面に出す | `face_srs.rs` | 要件書の面の生成器（章 01・02・09 と骨格） | 1023 | 477 |
| 4 面に出す | `face_adr.rs` | 判断の記録の面の生成器 | 855 | 645 |
| 4 面に出す | `figure.rs` | 型付き記述から図の本体（SVG）を描く（folio figure） | 699 | 801 |
| 4 面に出す | `face_srs_items.rs` | 要件書の面の章 03〜06 | 389 | 1111 |
| 4 面に出す | `face_labels.rs` | 面の共有の名札（憲法の値域の型への網掛け） | 326 | 1174 |
| 4 面に出す | `face_srs_rtm.rs` | 要件書の面の章 07・08 | 114 | 1386 |
| 5 配る | `main.rs` | 命令の入口（副命令 12 本・区切りの宣言 43 本） | 574 | 926 |
| 5 配る | `serve.rs` | 配信（folio serve）。接続先を loopback か tailnet に限る | 419 | 1081 |
| 5 配る | `site.rs` | 配信先の組み立て（folio build） | 261 | 1239 |
| 5 配る | `hello.rs` | 気づかせる 1 行（folio hello） | 128 | 1372 |

| 層 | file 数 | 正規化行 |
| --- | ---: | ---: |
| 0 土台 | 2 | 217 |
| 1 読む | 2 | 1061 |
| 2 検査する | 19 | 11783 |
| 3 導出する | 7 | 3478 |
| 4 面に出す | 10 | 8239 |
| 5 配る | 4 | 1382 |
| **計** | **44** | **26160** |

歯の側（`crates/folio/tests/`）は 38 file・22,134 行で、上限を超えているものが 2 本ある（`tests/face.rs` 1,785・余地 **−285**／`tests/face_index.rs` 1,528・余地 **−28**）。この 2 本の割り直しは総点検の設計ノート §3-c の便 105 で、入口の面を割る便（便 115）の前提である。

## 3. 依存の辺の実測

| 向き | 本数 |
| --- | ---: |
| 下向き（自分より下の層を名指す） | 127 |
| 同じ層 | 75 |
| **上向き（決まりに反する）** | **20** |
| 辺の計 | 222 |

節点は 44（`main.rs` を含む）。改訂 a は注釈を剥がさず `main.rs` の接頭辞なしの名指しも数えなかったので 115 / 72 / 20（207 辺・43 節点の図）だった。**上向きの 20 本は改訂 a と 1 本も違わない。**

### 3-a. 上向きの辺 20 本（全数）

| use する側 | 層 | use される側 | 層 | 名指しているもの |
| --- | --- | --- | --- | --- |
| `adr.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor`・`floor_diff`・`keys_floor`・`strip_notes`・`derive` |
| `note.rs` | 2 検査 | `schema.rs` | 3 導出 | 同上 |
| `ceiling.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor`・`floor_diff`・`strip_notes`・`derive` |
| `entrance.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor`・`derive` |
| `rules.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor`・`derive` |
| `check.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor` |
| `intake.rs` | 2 検査 | `schema.rs` | 3 導出 | `Floor` |
| `bundle.rs` | 3 導出 | `face.rs` | 4 面 | `R`（正本の cursor の結果の型） |
| `findings.rs` | 2 検査 | `face.rs` | 4 面 | `R` |
| `gate.rs` | 2 検査 | `face.rs` | 4 面 | `R` |
| `stamp.rs` | 3 導出 | `face.rs` | 4 面 | `R` |
| `sheet.rs` | 3 導出 | `face.rs` | 4 面 | `R`・`X`・`load` |
| `entrance.rs` | 2 検査 | `face.rs` | 4 面 | `SHELF_DOCS`・`SHELF_RELATIONS`・`ANNEXES`・`SHELF_LEGEND`（`face_labels.rs` からの再輸出） |
| `parts.rs` | 2 検査 | `face.rs` | 4 面 | `FIGURE_LABELS`・`MAX_PER_BAND`・`MAX_RAIL_NODES`・`MAX_STATE_NODES`（`parts::catalog` の再輸出） |
| `anchor.rs` | 2 検査 | `freeze.rs` | 3 導出 | `Flag`・`State` |
| `check.rs` | 2 検査 | `freeze.rs` | 3 導出 | `Flag`・`After`・`after` |
| `ids.rs` | 2 検査 | `freeze.rs` | 3 導出 | `Flag`・`After`・`not_frozen_by` |
| `findings.rs` | 2 検査 | `bundle.rs` | 3 導出 | `Ceiling`・`Rules`・`Viewpoint`・`Files`・`load`・`read_dir_names`・`digest_text`・`build_one` |
| `gate.rs` | 2 検査 | `bundle.rs` | 3 導出 | `Ceiling`・`load`・`read_dir_names` |
| `gate.rs` | 2 検査 | `stamp.rs` | 3 導出 | `STAMP_FILE`（印の file 名の定数） |

### 3-b. 20 本の出どころは 7 つの file の「同居」

改訂 a は 5 つと書いた。実測を進めると 7 つだった（審査役の直し 4）。

| # | 出どころ | 同居しているもの（層をまたいで使われる側） | 同居しているもの（その file 固有の口） | 辺 |
| ---: | --- | --- | --- | ---: |
| 1 | `schema.rs` | 床の木の型 `Floor`・`floor_diff`・`strip_notes`・`keys_floor`・`derive` | `folio schema` の口（`TARGETS`・`run`・`run_one`・`region_of`） | 7 |
| 2 | `face.rs` | 正本の cursor（`R`・`X`・`load`） | 面の生成の入口と共通の部品（`Frame`・`card`・`figure_panel`・`esc` ほか） | 5 |
| 3 | `parts.rs` | 部品目録の閉じた一覧 `catalog`（組み立て時に導出した型） | `folio parts` の検査 | 1 |
| 4 | `face_labels.rs`・`face_note.rs` | 入口の棚の閉じた一覧（`SHELF_*`・`ANNEXES`）と設計ノートの状態の閉じた一覧 | 面の名札と設計ノートの面の生成 | 1 |
| 5 | `bundle.rs`・`stamp.rs` | 天井の正本の読み手と型（`Ceiling`・`Rules`・`Viewpoint`・`Files`・`load`）・置き場の走査・束の組み直し（`build_one`・`digest_text`）・印の file 名 | `folio ceiling --write` と `--stamp` の本体（`run`・`write_all`） | 3 |
| 6 | `freeze.rs` | 凍結の実行の様態の型（`Flag`・`State`・`After`）と言い換え（`not_frozen`・`not_frozen_by`） | `--freeze-anchor` / `--emit-amends` の本体 | 2 |
| 7 | `check.rs` | （同居しているのは口の側）床の定数と床の共通の道具 | `folio check` の口 `check_dir` が凍結の後始末 `freeze::after` を呼ぶ | 1 |

**上向きの辺はすべて左の列を名指している。命令の口の本体を名指す上向きの辺は 1 本も無い**（例外は 7 番で、そこは口の側が下の層に居座っている形である）。

### 3-c. 同じ層の中の絡み

相互に名指す対は **30** で、うち **17 対**が同じ層の中に在る（層 2 が 6・層 4 が 10・層 3 が 1）。向きの決まりは同じ層を許すので、この 17 対には何も掛からない。

11 本の便の後は **14 対**（層 2 が 6・層 4 が 8）が残り、すべて同じ層の中である。そのうち 2 対（`face_srs` ↔ `face_srs_items`・`face_srs` ↔ `face_srs_rtm`）は、**上限だけを理由に割った便（便 100・便 35）の跡そのもの**である。この跡を解くのは本判断の外で、同じ責務の線（読み手と書き手）を後から当てる後続に置く（審査役の直し 5）。

## 4. 割り直しの便（106〜116・11 本）

原則: **1 便 = 既存の src 1 本 + 新設の src 1 本**（ほかの file は名指しの行だけ）。出力を変えず、既存の歯を 1 本も動かさない。歯は便 106 で境界の歯 2 本を足すだけで、以後は増えない。

**順**: 層をまたいで使われるものを降ろす 7 本（便 106〜112）が先、塞がっている 4 file を開ける 4 本（便 113〜116）が後。改訂 a は逆順で、その理由を「先に開けないと受付で断られる」と書いていたが、器の受付は 見積 が 余地 を超えたときだけ断るので偽だった（§8）。

### 4-a. 上向きの辺の列（写しの file へ機械的に当てた実測）

| 便 | 何 | 上向きの辺 |
| --- | --- | ---: |
| — | 基準（origin/main aff1b32） | **20** |
| 106 | 床の木の型 → `floor.rs`（層 1） | 13 |
| 107 | 正本の cursor → `cursor.rs`（層 1） | 8 |
| 108 | 部品目録の閉じた一覧 → `catalog.rs`（層 1） | 7 |
| 109 | 入口の棚と設計ノートの状態の閉じた一覧 → `shelf.rs`（層 1） | 6 |
| 110 | 天井の正本の読み手・束の組み直し・印の名 → `ceiling_src.rs`（層 1） | 3 |
| 111 | 凍結の実行の様態の型 → `phase.rs`（層 1） | 1 |
| 112 | `folio check` の口が凍結の後始末を入口へ返す | **0** |
| 113 | 判断の記録の床の定数 → `floor_adr.rs`（層 1） | 0 |
| 114 | 設計ノートの床の定数 → `floor_note.rs`（層 1） | 0 |
| 115 | 入口の面の読み手 → `face_index_read.rs`（層 1） | 0 |
| 116 | 憲法の面の読み手 → `face_constitution_read.rs`（層 1） | 0 |

**1 本も増えない。** 実測の手順は §7。逆順（塞がりを先に開ける）にすると、開けた先の新しい file が層をまたいで名指すので上向きの辺が増える（審査役が便 113 相当を当てて 20 → 21 を実測した）。

### 4-b. 便ごとの中身と書き込み範囲

| 便 | 移すもの（行は基準点の番号） | 書き込み範囲 | 元の余地 → 後の余地 | 新設の行 |
| --- | --- | --- | --- | ---: |
| 106 | `schema.rs` の床の木の型（50〜289・`keys_floor!` の中の道も書き換える） | `schema.rs`・`+ floor.rs`・`main.rs`・名指しの行だけ 8 file（`adr`・`note`・`ceiling`・`entrance`・`rules`・`check`・`intake`・`graph`）・`+ tests/modules.rs`・`tests/schema.rs` | 476 → 238（1,024 → 1,262） | 242 |
| 107 | `face.rs` の正本の cursor（28〜29・137〜300） | `face.rs`・`+ cursor.rs`・`main.rs`・名指しの行だけ 12 file・`tests/modules.rs`・`tests/face.rs` | 1,138 → 973（362 → 527） | 169 |
| 108 | `parts.rs` の部品目録の閉じた一覧（15〜20） | `parts.rs`・`+ catalog.rs`・`main.rs`・名指しの行だけ 11 file・`tests/modules.rs`・`tests/parts.rs` | 672 → 668 | 7 |
| 109 | `face_labels.rs` の棚の閉じた一覧（224〜290）と `face_note.rs` の状態の閉じた一覧 | `face_labels.rs`・`face_note.rs`・`+ shelf.rs`・`main.rs`・`entrance.rs`・`face.rs`・`face_index.rs`・`tests/modules.rs` | 326 → 260 | 71 |
| 110 | `bundle.rs` の天井の正本の読み手と束の組み直し（33〜129・147〜304・417〜519・589〜641）と `stamp.rs` の印の file 名 | `bundle.rs`・`stamp.rs`・`+ ceiling_src.rs`・`main.rs`・`findings.rs`・`gate.rs`・`graph.rs`・`tests/modules.rs`・`tests/bundle.rs` | 730 → 321 | 416 |
| 111 | `freeze.rs` の実行の様態の型と言い換え（19〜63・148〜161） | `freeze.rs`・`+ phase.rs`・`main.rs`・`anchor.rs`・`check.rs`・`ids.rs`・`site.rs`・`tests/modules.rs`・`tests/freeze.rs` | 357 → 300 | 61 |
| 112 | `check.rs` の口が凍結の後始末を呼ばず、状態を返して入口が呼ぶ（署名の変更・出力は同じ） | `check.rs`・`main.rs`・`tests/modules.rs`・`tests/check.rs` | 879 → 879 | — |
| 113 | `adr.rs` の床の定数（23〜458） | `adr.rs`・`+ floor_adr.rs`・`main.rs`・`schema.rs`（名指し 1 行）・`tests/modules.rs`・`tests/adr.rs` | 1,390 → 898（**110 → 602**） | 496 |
| 114 | `note.rs` の床の定数（28〜467） | `note.rs`・`+ floor_note.rs`・`main.rs`・`schema.rs`（名指し 1 行）・`tests/modules.rs`・`tests/note.rs` | 1,362 → 886（**138 → 614**） | 481 |
| 115 | `face_index.rs` の読み手（118〜263・302〜592） | `face_index.rs`・`+ face_index_read.rs`・`main.rs`・`face_adr.rs`・`face_note.rs`・`tests/modules.rs`・`tests/face_index.rs` | 1,378 → 944（**122 → 556**） | 442 |
| 116 | `face_constitution.rs` の読み手（93〜121・157〜311・505〜529・694〜746・927〜958） | `face_constitution.rs`・`+ face_constitution_read.rs`・`main.rs`・`tests/modules.rs`・`tests/face_constitution.rs` | 1,294 → 1,002（**206 → 498**） | 296 |

**便 115 の前提**は便 105（歯の `tests/face_index.rs` の余地を正に戻す）。**便 107 の前提**も同じく便 105（`tests/face.rs`）。

### 4-c. 各便の受入

歯だけでは足りない（審査役の直し 8）。どの便も次の 3 つを受入に置く。

1. 既存の歯が全部通る（`cargo nextest run` が 777 本中 777 本・境界の歯を足した便は 779 本）。
2. `folio check --dir design-intent` が **合格（違反 0・まだ分からない 0）**。
3. `folio schema --check` が **9 file とも一致**（byte 数まで同じ）。

### 4-d. 「1 字も変えない」ではない

審査役が便 113 相当を実際に当てたところ、区間を移すだけでは組み立てが通らず、次が要った。

- 私（非公開）の定数 **25 本**を同じ道具の中で公開する（`ID_PATTERN`・`RULING_PATTERN`・`OWNER`・`RECORD`・`STATUS`・`OPTION`・`RETREAT`・`APPROVAL` ほか）。区間の 28 の項目のうち 25 が私で、26 が区間の外から使われている。
- `struct Keys` の欄 `required`・`optional` も公開する（欄が私のままだと組み立てが止まる）。
- 元の file に再輸出の 1 行を置く（`schema.rs` の `crate::adr::FLOOR` が解けるようにする。`schema.rs` は名指しの行だけの書き換えに留める）。

つまり見え方は **27 か所**で広がる。歯は 1 本も落ちないので、**「歯の本数不変・既存の歯が全部緑」はこの変化を検出しない**。副作用は本判断の向きと逆で、25 の定数が道具の全体から名指せるようになる。便ごとに実測して契約に書く（審査役の直し 7）。

### 4-e. 床の定数を 2 本しか降ろさない理由

責務の線（床の定数は読む層・検査は検査の層）は**床の定数 9 本すべてに一様に掛かる**。本判断が運ぶのは 2 本（判断の記録・設計ノート）で、選んだ理由は**余地が 300 未満だから**である。残り 5 本（天井・入口・相談窓口・規則の表・正本 7 file）は余地が足りている（498 以上）ので本判断では運ばず、後続に置く。**どれを先に運ぶかを余地で決めることと、境界を余地で決めることは別である**（審査役の直し 3）。

## 5. 境界の歯（`crates/folio/tests/modules.rs`・便 106 で新設）

層の割り当てと、まだ降ろしていない上向きの辺の一覧は、**歯の側**が型付きデータで持つ。実装の側に置いて歯がそれを読む形にすると、実装が自分で自分を採点することになる。

```rust
/// 層の割り当て（責務・ADR-15 決定 (2)）。新しい区切りは必ずここに 1 行を持つ。
const LAYERS: &[(&str, u8)] = &[
    ("verdict", 0), ("sha256", 0),
    ("yaml", 1), ("constitution_enums", 1), ("floor", 1),
    // ... 44 行から始めて 54 行まで増える ...
];

/// まだ降ろしていない上向きの辺（ADR-15 決定 (4)）。凍結した実測であって許可ではない。
/// 行を足す書き換えは認めない（足すなら ADR-15 を改訂する）。
const REMAINING_UPWARD: &[(&str, &str)] = &[
    ("adr", "schema"), ("note", "schema"), /* ... 20 対から 0 対へ ... */
];
```

| 歯 | 何を数えるか | 落ちるとき |
| --- | --- | --- |
| `p106_layers_cover_every_module` | `LAYERS` の file 名の集合が、`src/main.rs` の区切りの宣言（`mod <名>;`）の集合と一致する | 新しい file が層を持たない／消えた file が表に残っている |
| `p106_edges_point_down` | 注釈を剥がした `src/*.rs` を走査して辺を集め、層が上がる対の集合が `REMAINING_UPWARD` と**一致**する | 上向きの辺が 1 本でも増えた／減ったのに一覧を直していない |

2 本目は集合の一致を見るので、辺を足しても減らしても落ちる。無効化の旗も「今回だけ」の口も持たない（N-3.1）。

### 5-a. この歯が言えないこと（限界）

1. **独立した anchor ではない。** 層の割り当ても辺の一覧も、実装の側と**同じ便・同じ書き手**が同じ書き込み範囲の中で書き直す。file が違うだけで、条 P-10.1 が求める「生成側からも検査側からも独立した」凍結 anchor にはならない。ADR-15 はこれを「凍結した実測」と位置づけ、独立性の主張は取り下げている（審査役の直し 9・18）。
2. **注釈を剥がさないと偽の辺が出る。** 今の写しでは剥がしても辺の計は変わらないが、区間を降ろした先の file の説明文が偽の辺を作る場面を、写しでの適用で実測 2 件見た（`phase.rs` の説明文の `anchor::check_anchor`・`ceiling_src.rs` の説明文の `findings::refute`）。歯は剥がしてから数える。
3. **区切りを宣言する入口の辺は接頭辞なしで書かれる。** `main.rs` は 43 本を宣言し、本文で 16 の区切りを接頭辞なしで名指すが、`crate::` は 1 回しか書かない。数えないと 43 節点の図の上の「0」になる。歯は `main.rs` だけ接頭辞なしも数える（審査役の直し 10）。
4. **再輸出を挟んだ名指しは見えない。** 今の写しに 1 例ある（`face.rs:483` が `face_labels.rs` を丸ごと再輸出し、呼ぶ側の真の行き先は `face_labels.rs`）。便 113 自身も再輸出を使う。歯は字面を見るので、再輸出を挟めば上向きの辺を書かずに同じ依存を作れる。**この歯が言えるのは「字面の上で上向きの辺が無い」までである。**
5. **全部の便の合流点になる。** 辺を動かす便はどれも `tests/modules.rs` を書き込み範囲に入れるので、並行して運ぶ便が直列になる。ADR-15 が見込む「重なりが減る」に逆向きに働く（審査役の直し 11・20）。

## 6. 数の一覧（この便の列で動く値）

| 何 | いま | 便 112 の後 | 便 116 の後 |
| --- | ---: | ---: | ---: |
| 実装の側の file 数 | 44 | 50 | 54 |
| 余地 300 未満の src | 4 | 4 | **0** |
| 上向きの辺 | 20 | **0** | 0 |
| 下向きの辺 | 127 | — | 172 |
| 同じ層の辺 | 75 | — | 73 |
| 相互に名指す対（うち同じ層） | 30（17） | — | 14（14） |
| 歯の file | 38 | 39 | 39 |
| 歯の本数 | 777 | 779 | 779 |

## 7. 実測の手順（誰でも撃ち直せる形）

1. 基準点の `crates/folio/src/*.rs` を作業の置き場へ写す。
2. 層の割り当て表（§2）と §1 の向きの決まりで、注釈を剥がした走査（冒頭の式）を当てて 下向き / 同じ層 / 上向き を数える。
3. 便 106 から順に、§4-b の行範囲を新しい file へ切り出し、区切りの宣言を入口に 1 行足し、名指しの行を書き換える。各便のあとで 2 を撃ち直す。
4. 数が 1 便でも増えたら順を組み替える（改訂 b はこの手順で改訂 a の順を退けた）。

台本は `/tmp/claude-1001/…/scratchpad/`（`simulate.py`・`scan.py`・`cycles.py`）に置いた。作業の置き場の写しなので版管理には入れない。組み立てが通ることまでは確かめていない（§4-d のとおり、見え方を広げる直しが要る）。実測したのは**辺の図と行数**であり、便が実際に運べることは便の契約と受付が受け持つ。

## 8. 改訂 b で改めた点（反対側からの確認への応答）

| # | 審査役の直し | 改めた先 |
| ---: | --- | --- |
| 6・13・21 | 順を入れ替える（「受付で断られる」は偽・撤退条件が計画自身で発動する） | 便の順を 106〜112 →113〜116 に。§4-a の列を実測で置いた |
| 7 | 「1 字も変えない」は偽 | §4-d を新設 |
| 12 | 便ごとの上向きの値が推測で誤り | §4-a を実測に置き換え |
| 10 | 走査の限界（`main.rs` の 16 辺・再輸出・注釈） | 冒頭の式と §5-a |
| 1・2 | 層 5 の責務・層 1 の定義と実測の食い違い | §1 |
| 3 | 床の定数を 2 本しか降ろさない理由 | §4-e |
| 4 | 同居は 5 つでなく 7 つ | §3-b |
| 5 | 同じ層の循環 17 対が読めない | §3-c と §6 |
| 8 | 受入が歯だけでは足りない | §4-c |
| 11・20 | 境界の歯の file が合流点になる負債 | §5-a の 5 |
| 9・14〜19・22・23 | 独立性の主張・撤退条件・規則の表の行 D-11 の理由・条 P-18・平易文・根拠の欄 | 判断の記録 ADR-15 の側で直した |

**歯の失敗 4 本の再現**: 審査役の写しでは 777 本中 4 本が落ちた。本枝の作業ツリーで `cargo nextest run --no-fail-fast` を撃つと **777 本中 777 本が通る**。落ちた 4 本（`check_canonical_design_intent_passes`・`f77_top_level_is_closed_on_the_three_files`・`parts_check_passes_on_the_three_generated_faces`・`site_on_the_real_sources_passes_parts_check_and_face_check`）はどれも実の設計文書を版管理ごと読む歯で、審査役の写しに版管理が無かったことによる環境差である。本物の不具合ではない。

## 9. 未決（ADR-15 の発効までに答えが要るもの）

1. 反対側からの確認（grill）は改訂 a に対して 1 回行った。改訂 b に対して当て直すかは席の手番。
2. 便 105（歯の file 2 本の割り直し）は本ノートの外で、便 107 と便 115 の前提。
3. 「実装の型付きの定数」が歯を含むかという読みは本判断では決めない（まだ分からない・P-4.2）。決めるなら条の改訂（A-2 の手続き）が要る。
4. §3-c の同じ層の循環 14 対（うち 2 対は上限で割った跡）を解く便は本判断の外。
5. 床の定数の残り 5 本を降ろす後続の便は起票していない。
