# 要件書 版 B（第 1.34 版）の起草 — M3 の範囲の節と、骨格・列の根・始まりの凍結・値域の部分集合の要件と受入基準

**状態: 起草・承認待ち（main c33ee44 の上・一括 15 と便 119 の着地後）。** 来歴 = 2026-09-24 07:0x JST 起草・07:1x JST 改訂 b = 分け方の問いを席の裁定に替えて問いから外し、天井の正本 v0.14〔3 観点の読む欄に scope_m3〕を同じ枝に足した・改訂 f = 便 119 の着地〔main c33ee44・09:14 JST〕に追随して枝を載せ替え、P-6 の注・FR11 の契機と注・本 doc の「名は便で決める・未実装」を命令の名と着地の事実に改めた（09:4x JST）・改訂 c = 歯を性質で当てる席の PR〔main 41ed484〕を取り込み、FR22 の注に行 R-17 の 1 文を戻した・07:2x JST 改訂 d = 独立の検証 ~/.local/share/folio2/handoff-2026-09-24/srs-vB-verify.md の直す 5 と問う 3 を席の裁定どおりに当てた＝範囲の節の写しの欠けと足した字の一覧・入口の版の番号を外す・骨格と始まりの凍結の確かめ方を版管理の中の置き場に・始まりの凍結の断る場合に「まだ分からない」・受入基準 AC26 と AC27 を足す・AC25 の注・07:2x JST 改訂 e = 憲法の条 P-6 の機構の注に導出物の検査の命令の 1 句〔commit a227af0 の題は「改訂 d の追加」〕・07:3x JST 改訂 f = 再検証の直す 2＝AC26 の禁止字を承認欄の who の全字と版付きのモデルの名と verbatim〔床の定数と雛形の文に現れない字〕に限り、生成区間と雛形の相談窓口の文を数えの外に／命令の名を「名は便で決める（案 folio derive --check）」に・07:5x JST 組み直し = 一括 15 が main 73a6bf7 に着地したので、枝 docs/srs-vB の版 B の差分〔design-intent と本書〕を main の上の新しい枝 docs/srs-vB2 に当て直した。要件書の meta は main の側を土台に第 1.34 版の字を載せた）。 下の 🔴 問 1 が持ち主への承認要求である（1 つだけ）。承認の記帳（要件書と天井の正本の承認欄と `effective_version`・状態の 1 枚と台帳）は、持ち主が答えた後に席が同じ枝の別の commit で書く（「承認の後に席が書く欄」）。

起草 2026-09-24・orchestrator 席（起草は起草役〔opus 5.5〕に委ねた。正本の作成の行は席を責任者として「orchestrator 席（AI・fable 5.1）」と書く）・作業場所 `.worktrees/planner-srs-vB2`（branch `docs/srs-vB2`・`origin/main` 73a6bf7〔一括 15 の着地〕から切った）。改訂 f までの起草と検証は旧枝 `docs/srs-vB`（`origin/docs/batch15` ab04c4f から切り `origin/main` 0b0aafd と 41ed484 を取り込んだ枝・最後の commit 27819fb）で行い、その差分〔`git diff 9d83c73 27819fb -- design-intent docs/design`〕を `git apply --3way` で当てた。

出所は判断の記録 **ADR-16**（発効 2026-09-23 17:39 JST・逐語「裁定は両方承認する」）の決定 (7)「版 B は ① の後に出す = M3 の範囲の節・骨格の命令と列の根の表と始まりの凍結の要件と受入基準（持ち主の承認）」である。① は便 117（床の定数 SRS_TOP_LEVEL に scope_m3・main a34c6b2）で着地し、面が scope_m3 を描く口は便 118（main 0b0aafd）で着地した。

## この版で足したもの・直したもの

| 置き場 | id / 欄 | 中身 | ADR-16 の出所 |
|---|---|---|---|
| 最上位の節 | `scope_m3`（build 6 行・not_build 9 行・note） | M3 の範囲・入口・出口の判定点 ①〜③ と前提と記帳の持ち主・道具の口の指し先・M3 に入れないもの・契約表を持たない設計文書の移行の時機 | 決定 (1)。置き替えた字と決定 (1) の外から足した字は起草で決めたことの 1 と scope_m3 の注に全部並べた |
| 要件 | **FR22** 骨格の命令（`folio init --dir`） | 正本 7 file と判断の記録・設計ノートの置き場と欄の決まりを書く・行 R-8 / R-16 と最小の条と最初の判断の記録（提案中）・正本が 1 本でも在れば何も書かずに非 0・folio2 の口座名と持ち主の逐語を写さない・憲法の正本が無い置き場では案内の 1 行がこの命令を案内する | 決定 (3)・(2)(オ)(カ) |
| 要件 | **FR23** 列の根の表 | 列の根の要約値を憲法の名で床の定数の表から引いて照らす・表に無い名は凍結の命令で全桁を出して断り、既存の列は床が違反・写しは置き場の名の行だけ・folio2 の行は不変 | 決定 (2)(ア) |
| 要件 | **FR24** 始まりの凍結の命令 | 憲法の列と id の一覧がどちらも 0 本で、ほかの検査が違反 0・まだ分からない 0 のときだけ 2 つを同時に書く・どの検査も数えから外さない | 決定 (2)(イ) |
| 要件 | **FR25** 値域の部分集合の床 | 置き場の憲法の値域の各鍵が組み立てた値域の部分集合かを数え、部分集合でなければ「まだ分からない」 | 決定 (2)(ウ) |
| 受入基準 | **AC19〜AC22** | FR22〜FR25 の歯（固定の材料はまだ置いていない＝口が未実装・置き場は便で決める。AC9 / AC11 と同じ書き方） | 同上 |
| 受入基準 | **AC26 / AC27**（改訂 d） | FR22 の 2 つの規範文の歯＝骨格の命令が書いた file に folio2 の承認欄の字（who と verbatim・席とモデルの名）が 1 回も出ない／憲法の正本が無い置き場でセッションの始まりの 1 行が骨格の命令を名指す（字の数えで決定的・条 P-3.1・材料は最小の手書き） | 決定 (3) |
| 受入基準 | **AC23 / AC24 / AC25** | M3 の出口の判定点 ① / ② / ③。利用者の台帳の記録が在れば着地、無ければ「まだ分からない」の測定値（folio2 の歯は持たない） | 決定 (1)(5)・ADR-3 決定 (5) と同じ形 |
| 要件の注 | FR3 の注に 1 文 | 憲法の正本が無い置き場で 1 行が案内する先（骨格の命令）は FR22 が持つ | 決定 (3) |
| 要件の注 | FR10 の注に 1 文 | 器の導出 file（contracts/schema.toml）を探す場所を置き場を含む版管理の根へ改める（便 ④・未実装） | 決定 (2)(キ) |
| 要件の確かめ方 | FR11 の `verify.ac` | [AC9] → [AC9, AC24, AC25]（AC24 / AC25 の `verifies` と向きを揃える。今の要件書は両向きが全部揃っている） | — |
| meta | `title`・`version`・`status_note`・`counts`・`approval` | 題に「＋ M3・外部の利用者の置き場で床に掛ける」・v1.34・書き出しの 1 文と来歴の末尾・fr 25 / ac 27・作成の行 1 つ | — |
| 天井の正本 | `design-intent/ceiling.yaml` を v0.14（起草）に | 忠実さ・読みやすさ・文書どうしの整合の 3 観点の読む欄（`viewpoints[].reads`・人が書く欄・生成区間の外）の要件書の行に `scope_m3` を足す＝scope か scope_m1 を読んでいた 3 観点に同じ形で（忠実さと整合は scope_m1 の後・読みやすさは scope の後）。頭の注釈（v0.14 起草／v0.13 発効）・`meta.version`・承認欄の作成の行。実態の観点は scope も scope_m1 も読まないので足さない | 便 117 §1 (h) の 3 の穴 |
| 憲法 | 条 P-6 の機構の注（`mechanism.note`・人が書く欄・条 A-2.2 の外）に 1 句（改訂 e） | 再生成して差分 0 を数える命令の列に、導出物の検査の命令〔要件 FR11 の契機・配信の組み立てから切り離した独立の命令 folio derive --check・便 119 で 2026-09-24 に着地〕を足す。条文・規範文・段・縛る相手・schema 節・前文は 1 字も変えない（`folio inject --check` は一致のまま） | 決定 (5)（便 119 の検証役の指摘） |
| 語彙 | `identifiers` の「命令と旗」に `init` | FR22 の契機の命令名が行 R-9 で落ちた（下の床の節）ので命令名として足した | — |

**既存の要件の規範文・平易文と、受入基準の判定の式は 1 字も変えていない。** 要件と受入基準の番号は新しい番号だけ（条 P-7.1）。

## 起草で決めたこと（迷った点）

1. **scope_m3 は 3 欄のまま。** 余分な欄は床も面も数えず黙って落ちる（便 118 の検証役の実測）ので、入口と出口を build の行に、CON8 の時機を not_build の行に置いた。**置き替えた字**は 4 つ＝「本判断」→「判断の記録 ADR-16」・「決定 (6)」→「判断の記録 ADR-16 決定 (6)」・「M3 に入れないもの」の 1 文を not_build の行に割った・CON8 の時機の文を not_build の 1 行に。**決定 (1) の外から足した字**は 6 つ＝build の 1 行目の出所の括弧・出口の行の受入基準の id（AC23・AC24・AC25）・build の 6 行目の全体（道具の口と「1 つの組み立て」＝決定 (2) の字）・note の便 117 と便 118 の着地の文・note の参照 id の形の便の位置の文（決定 (6)(7) の字）・note の最後の文（便の列の置き場）。改訂 d で、落としていた決定 (1) の 1 文「列の根の表と値域は組み立ての中に在るので、組み立てを特定しないと、どの版で検査したかが合否の一部になる」を build の 4 行目の末尾に写し（原文の並びどおり）、入口の行の「（第 1.34 版）」を外した（原文に番号は無い・一括 15 の問 7 の案 B と番号を取り合わない）。
2. **`effective_version` は v1.33 のまま触らない。** 発効しているのは一括 15 で承認された第 1.33 版（2026-09-24）で、第 1.34 版は承認の後に席が上げる（「承認の後に席が書く欄」の 1）。書き出しの 1 文は「いま発効しているのは 第 1.33 版（2026-09-24）で、第 1.34 版は起草・承認待ちです。」にした。旧枝では一括 15 が未承認だったので v1.32 のままにしていた。
3. **決定 (2)(キ)（器の導出 file を版管理の根で解く）は要件にせず、FR10 の注に置いた。** FR10 の規範文は読む場所を定めていないので、規範文を変えずに書ける。独立の要件（FR26）にしない裁定は下の「席の裁定」の 1。
4. **案内の 1 行（決定 (3) の末尾）は FR22 の規範文の最後の 1 文に置き、FR3 の規範文は変えない。** FR3 の規範文は案内の中身を持たないので、中身は FR22 が持ち、FR3 の注から指す。
5. **判定点 ①〜③ は受入基準 3 本に分けた。** 判定点ごとに記帳が別で、② と ③ は要件 FR11 を、① は FR22〜FR25 を確かめる。3 本のままにする裁定は下の「席の裁定」の 2。
6. **FR22 の注の行 R-17 の 1 文は、一度外して改訂 c で戻した。** 1 度目の起草で「散文の言及の歯の行 R-17 は骨格に入れず、利用者が要れば利用者の持ち主の裁定で足す」（決定 (2)(オ)）を注に書き、欄 rules に R-17 を足したところ、歯 `f93_deleting_the_rule_row_is_not_a_silent_escape`（行 R-17 を消したときの参照 id の違反を実の設計文書でちょうど 3 件と pin）が落ちたので、言い換えずに文ごと外していた。席の PR（台帳 f2-648.162・main 41ed484）がこの歯と `f90_the_real_srs_carries_the_adrs_field` を性質で当てる形に直したので、改訂 c で同じ字の 1 文と欄 rules の R-17 を戻した。
7. **受入基準 AC19〜AC22 の固定の材料は「まだ置いていない・置き場は便で決める」とした。** 新しい dir の名を要件書で先に決めない（AC9 / AC11 は予定の path を書いているが、ここでは名を便へ回した）。

## 持ち主へ問う項

### 🔴 問 1. 要件書 第 1.34 版（版 B）と天井の正本 v0.14 を発効する

**やりたいこと。** M3 で何を作り、どこで「終わった」とするかを要件書に書く。判断の記録 ADR-16 が決めたことを、要件と受入基準の形に移すだけである。

1. 要件書に「M3 で作る／作らない」の節を足す（中身は ADR-16 決定 (1) の写し）。
2. 新しい要件 4 本（FR22 骨格の命令・FR23 列の根の表・FR24 始まりの凍結・FR25 値域の部分集合）と、受入基準 9 本（AC19〜AC22 = 4 本の歯・AC23〜AC25 = M3 の出口の判定点 ①〜③・AC26 と AC27 = FR22 の 2 つの規範文の歯）を足す。
3. FR3 と FR10 の注に 1 文ずつ、FR11 の確かめ方の指し先に AC24 / AC25、表紙の題に M3 を足す。語彙の命令名の一覧に `init` を足す。
4. 天井の正本を v0.14 にする＝要件書の scope か scope_m1 を読む 3 観点（忠実さ・読みやすさ・文書どうしの整合）が scope_m3 も読む。足さないと、天井の審査役に渡す材料の束から M3 の範囲の節が「落とした節」として外れ、4 観点のどれも M3 の範囲を読まない。
5. 憲法の条 P-6 の機構の注 1 か所に、導出物の検査の命令（要件 FR11 の独立の命令 folio derive --check・便 119 で 2026-09-24 09:14 JST に着地〔main c33ee44〕）を名指す 1 句を足す。改訂 f で、FR11 の契機（when）の「名は便で決める」を命令の名 folio derive --check に置き換え、FR11 の注に着地の事実を足し、語彙の「命令と旗」に derive を足した（便 119 の着地に追随・要件の状態は変えない）。条文は変えないので条文の改訂（条 A-2）ではない。

**理由。** ADR-16 決定 (7) が「版 B は ① の後に出す（持ち主の承認）」と決めており、① にあたる便 117 と、面が節を描く便 118 が着地した。版 B が無いと、便 ②〜④（列の根の表・始まりの凍結・値域・骨格の命令）が契約表の要件の欄に指す要件を持てない。判定点 ③ は folio2 の外の事実なので、利用者の台帳の記録が在れば着地・無ければ「まだ分からない」とする（ADR-16 決定 (5)）。

**代替案。**
- **(A) 範囲の節だけを先に発効し、要件は便ごとに足す。** 承認の回数が便の数だけ増え、便 ② の契約が指す要件がその時点で無い。
- **(B) 判定点を受入基準にせず、範囲の節の字だけで持つ。** 判定点が機械の読む行（受入基準の id）を持たず、便の契約や台帳から id で指せない（条 P-5.2）。

**コスト。** 承認 1 回（要件書と天井の正本を同じ承認で）。**既存の要件の規範文・平易文と受入の判定の式は 1 字も動かず、番号は新しいものだけ。** 床は合格で、歯は全部通る（下の床の節）。

**推奨 = 1〜5 をまとめて発効する。** ADR-16 の承認で決めたことの写しで、新しい決めごとを持ち込まない。

## 席の裁定（持ち主に問わない分け方）

順序や分け方だけを問う問いは持ち主へ出さない（`.beads/PRIME.md`）。次の 2 つは席が決めた（裁定 id = 席・2026-09-24 07:1x JST・改訂 b の依頼）。

1. **決定 (2)(キ)（器の導出 file を版管理の根で解く）は FR26 にせず、FR10 の注の 1 文に留める。** 理由: ADR-16 決定 (2)(キ) が「この形は便 ④ の契約に書く」と定めており、要件の規範文でなく便の契約が持つ形だから。
2. **判定点 ①〜③ は受入基準 3 本（AC23〜AC25）のまま。** 理由: 判定点 ③ は folio2 の外の測定値で、① や ② と形が違うので 1 本に畳まない。

改訂 d で、独立の検証の問う 3 つも席が決めた（裁定 id = 席・2026-09-24 07:2x JST・改訂 d の依頼）。

3. **scope_m3 の入口の文から版の番号を外す。** 理由: 決定 (1) の原文に番号は無く、一括 15 の問 7 が案 B なら第 1.34 版の番号がそちらに要る。
4. **判定点 ③ の測定値は AC25 の字（器の受付が導出物を読んで通した記録・利用者の台帳）を正とする。** 理由: 決定 (1) ③ の字で、器の受け口（s2-07l.473）の着地は前提であって測定値ではない。AC25 の注にその 1 文を足した。一括 15 の側（問 7 の案 A の測定値の字）は席が直す。
5. **FR22 の 2 つの規範文の歯を今の版で受入基準 AC26・AC27 として足す。** 理由: 字の出現の数と 1 行の中身は機械で決定的に数えられる（条 P-3.1）ので、便 ④ の契約へ回さず要件書に置く。FR22 の確かめ方の指し先にも足し、両向きの対称を揃えた。

## 承認の後に席が書く欄

| # | file | 欄 | 何を書くか |
|---|---|---|---|
| 1 | `design-intent/srs.yaml` | `meta.effective_version` | v1.33 → v1.34 |
| 2 | `design-intent/srs.yaml` | `meta.status_note` の先頭の文 | 「いま発効しているのは 第 1.34 版（<承認の日付>）です。」 |
| 3 | `design-intent/srs.yaml` | `meta.status_note` の来歴の v1.34 | 「v1.34 = 2026-09-24 起草（…承認待ち。…）」を「v1.34 = <日付> 発効（逐語「…」・版 B = …）」に |
| 4 | `design-intent/srs.yaml` | `meta.approval` | 承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・版 B〔問 1〕の承認要求への回答）", verbatim: <逐語>, version: v1.34}`） |
| 4-2 | `design-intent/ceiling.yaml` | 頭の注釈の状態の行・`meta.approval` | 「v0.14 は起草・承認待ち」を「v0.14 は発効（<日付 時刻> JST 持ち主「<逐語>」・版 B と同じ承認）」に・承認の行 1 つ（`{role: 承認, who: 持ち主（shuu5）, when: <日付>, stamp: "発効（f2-648 notes <時刻> JST・対話面 R-8・要件書 第 1.34 版〔版 B〕の問 1 への回答）", verbatim: <逐語>, version: v0.14}`） |
| 5 | `docs/design/srs-vB.md` | 頭の状態の行 | 「承認済み・発効（…逐語…）」に |
| 6 | 台帳（`bd`） | f2-648 notes | 承認の逐語と日付・版 B の発効・便 ②〜④ の契約が指す要件 id（FR22〜FR25） |
| 7 | 台帳（`bd`） | 歯の負債（f2-648.162） | 歯 f90 / f93 を性質で当てる直しは main 41ed484 で着地済みで、FR22 の注の 1 文は改訂 c で戻した。台帳の記帳だけが残る |
| 8 | `docs/design/state.md` | 要件書の版の行・次の一手 | 第 1.34 版（版 B）発効・次 = 便 ②（列の根の表と始まりの凍結） |

憲法の条 P-6 の機構の注の 1 句は、この表に行を持たない。機構の注は承認欄を持たず、一括 14 の条 P-2 の注と同じ扱いで、承認は問 1 の逐語が兼ねる（台帳の notes に記帳する行 6 に含める）。

索引の欄の決まり（graph.yaml）はこの表に行を要らない（folio graph --digest が新しい要件と受入基準の節点を毎回数え直す）。語彙は承認欄を持たず、要件書の承認で運ぶ。

## 床の結果（新しい枝 docs/srs-vB2 の HEAD〔main 73a6bf7 の上に組み直した時点の実測（c33ee44 への載せ替え後は 改訂 f の記録どおり床 4 本 合格）〕で実測・2026-09-24・main root の binary で撃った）

| 検査 | 結果 |
|---|---|
| `folio check --dir design-intent` | 合格（違反 0・まだ分からない 0） |
| `folio schema --check --dir design-intent` | rc 0（9 file 一致） |
| `folio inject --check --dir design-intent` | 68 行 / 7,912 byte 一致 |
| `cargo nextest run --workspace --no-tests=fail --no-fail-fast` | **786 / 786 PASS**（新しい枝で撃ち直した。旧枝の改訂 b までは `f90_the_real_srs_carries_the_adrs_field` が落ちていた＝歯の直しは main 41ed484 で着地） |
| `folio build --dir design-intent --out <scratch> --write` | 床 合格・23 file 書けた（新しい枝で撃ち直した）。要件書の面の章 02 に「M3 で作る／M3 では作らない」の塊と注が scope_m1 の直後に出る・FR22〜FR25 と AC19〜AC27 に M3 の札が付く（計 13＝要件 4 と受入基準 9・改訂 f の後の実測）・受入基準の章に AC26 と AC27 が出て、範囲の節に決定 (1) の写した 1 文が出ることを HTML の字で確かめた |
| `folio ceiling --dir design-intent --faces <site> --out <scratch> --write` | 束を組んだ（観点 4・file 200・落とした節 499・正本に無い節 0・新しい枝で撃ち直した）。忠実さ・読みやすさ・整合の `sources/srs.yaml` に `scope_m3:` の節が 1 つずつ入り、3 観点の「落とした節」の行から scope_m3 が消えた。実態の観点は今までどおり scope・scope_m1・scope_m3 を落とす |

**歯の経緯。** 改訂 b までは `crates/folio/tests/check.rs` の `f90_the_real_srs_carries_the_adrs_field`（欄 adrs の行の数を 14 で pin・新しい 4 行で 18）が落ち、起草の途中では `f93_deleting_the_rule_row_is_not_a_silent_escape`（参照 id の違反を 3 件で pin）も落ちた。どちらも席の PR（main 41ed484）で性質で当てる形になり、改訂 c で 786 / 786 になった。

**行 R-9 の実測。** 1 度目の床で「要件書 FR22 when: 語彙に無い英字の語「init」」の違反 1 件が出た。命令名なので語彙の identifiers の「命令と旗」に `init` を足した（語彙は要件書の承認で運ぶ）。旗の `--dir` は数えられなかった。

## まだ分からない点

1. **要件書の面の鮮度の札が起草中の版を「発効・拘束力あり」と出す。** 札は `meta.version` と `meta.status` を読み、`effective_version` を読まない（この枝では「v1.34（発効・拘束力あり）」）。一括 15 の起草中も同じ形で v1.33 と出ており、本版の変更ではない。

天井の観点が scope_m3 を読まない点は、改訂 b で天井の正本 v0.14 を足して閉じた（`folio ceiling --write` の束で、忠実さ・読みやすさ・整合の 3 観点の `sources/srs.yaml` に scope_m3 の節が入り「落とした節」から外れた）。面の確認は生成した HTML の字の抜き出しで行った（席の裁定で足りるとした）。一括 15 の問 7 と版の番号の取り合い・一括 15 の後の rebase の 2 点は、一括 15 が main 73a6bf7 に第 1.33 版で着地し、本版を main の上に組み直したので閉じた。
