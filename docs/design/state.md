# 状態（復元の 1 枚）

**席が毎回上書きする。履歴は版管理（git）が持つ。ここには規則を置かない**（P-5.4・N-2.1）。規則の正本は憲法・規則の表・要件書・判断の記録で、この 1 枚はそれらを指すだけである。台帳 `f2-648` の notes の【次の一手と未決】は、以後この 1 枚を指す 1 行にする（総点検の設計ノート `docs/design/audit-2026-09-23.md` の §3-a の L5）。

- 書いた時点: 2026-09-23（総点検の裁定の後・本 1 枚を新設した PR の枝）
- 基準: 統合先 `main` の `c7379ee`（退役の残骸の撤去・PR #267）

## 1. 今の状態

| 何 | 値 |
| --- | --- |
| 統合先 `main` | `c7379ee`（退役の残骸の撤去・その 1 つ前の `1289164` が総点検の設計ノート `docs/design/audit-2026-09-23.md`・持ち主 承認済み） |
| 最後に着地した便 | 便 104（台帳 `f2-648.142`・天井の印の `sources` を正本から測る・契約 `docs/design/delivery-104.md` 行 `db`） |
| 天井の印 | `design-intent/preview/ceiling-stamp.yaml`・周 `2026-09-22-round26`・判定 **合格**（4 観点とも 合格・止める 0）・門は **通す** |
| 憲法 | 第 1.2 版（発効） |
| 要件書 | 第 1.30 版（発効・未実装の要件の状態を「保留」と明示した） |
| 規則の表 | 第 1.0 版。開発規律の行は D-1 〜 D-13（D-13 は上と同じ PR で足す） |
| 天井の正本 | 第 0.12 版 |
| 判断の記録 | ADR-1 〜 ADR-14（発効）。ADR-15（module の境界）は未起草 |
| 器（scribe2） | binary は `a6ffeea`。置き場は `~/.cache/folio2-orchestrator/r86/new/bin/scribe2`。`.vessel.toml` に `entrance-flip = "unmeasured"` |

## 2. 次の一手

総点検の設計ノート `docs/design/audit-2026-09-23.md` の §5 の段取りに従う。持ち主の裁定（2026-09-23 08:13 JST・逐語「問いは全部推奨で」・🔴 1〜6 とも推奨どおり）は済んでいる。退役の残骸の撤去（🔴 4）は `c7379ee` で着地した。

1. **いま走っている PR**: 要件書 第 1.30 版（🔴 2）＋ 規則の表の行 D-13（🔴 6）＋ 契約の型 `docs/design/delivery-template.md` ＋ この 1 枚。承認は済んでいるので、床が通れば席が取り込む。
2. **判断の記録 ADR-15 の起草**（🔴 3）: 「module の境界は責務で決め、器の行数の上限を境界の理由にしない」と module 図。反対側からの確認（grill）を先に行い、発効の承認を取る。
3. **便 105**: `crates/folio/tests/face.rs` と `crates/folio/tests/face_index.rs` を面ごとに割る（振る舞い不変・歯の本数不変）。これを先にしないと面の歯を触る便が受付で断られる。
4. **便 106 〜 109**: src の割り直し（`adr.rs`・`note.rs`・`face_index.rs`・`face_constitution.rs`）。ADR-15 の発効が前提。
5. **便 110 〜**: 数や字面で固定している歯を、性質で当てる形へ。
6. **一括 14 → 天井の 27 周目**: 26 周目の直す 14 件と便 102 の 🔴 1 点をまとめて 1 回の承認で運ぶ。

## 3. 未決

| # | 何 | 誰の手番 |
| --- | --- | --- |
| 1 | 一括 14（天井の 26 周目の直す 14 件 + 便 102 の 🔴 1 点） | 席（総点検の便の列の後） |
| 2 | 要件 FR11（導出物の差分 0）と FR13（着地は印だけから読む）= 台帳 `f2-648.143` / `.144`。器の口 s2-07l.473 の着地待ち | 器（scribe2）→ M3 の入口で再判定 |
| 3 | 非機能要件 NFR1（AI へ常時渡す説明文の量）= 量を数える口が無い | 席（M3 の入口で再判定） |
| 4 | ADR-11 決定 (4) ⑥⑦⑧ = 台帳 `f2-648.74` / `.75` / `.76`。⑦ は規則の表に行を足すので裁定が要る | 席 → 持ち主（⑦ だけ） |
| 5 | ADR-13 の未消化の決定 4 本 = 台帳 `f2-648.145`（周の引き金）/ `.146`（門の単位）/ `.147`（注意の先）/ `.148`（実装の状態） | 席（便の列の後） |
| 6 | 憲法の条の廃止（status）の機構の時機 = 台帳 `f2-648.100` | 席 → 持ち主 |
| 7 | 器の暫定の散文の門の撤去の要求 = 台帳 `f2-648.93` | 席 → 器 |
| 8 | 索引の anchor の定数の写しを生成区間へ = 台帳 `f2-648.126` | 席 |
| 9 | 器の binary の置き場を実行 path の 1 本に統一する（持ち主の問い 2026-09-23 08:13 JST「毎回手で変えるのは何故か・他の project と入れ方が違う」） | 席 |

## 4. 使う口

| 何をしたいか | 撃つもの |
| --- | --- |
| 床を回す | `folio check --dir design-intent`・`folio schema --check --dir design-intent`・`folio inject --check --dir design-intent` |
| 全体像を見る | `folio graph --digest --dir design-intent` |
| 天井の門を撃つ | `folio ceiling --dir design-intent --gate --write-set <書き換える file>` |
| 台帳を読む | `bd --readonly show f2-648` |
| 便を出す | 総点検の設計ノートと契約の型 `docs/design/delivery-template.md` の順に従う |
