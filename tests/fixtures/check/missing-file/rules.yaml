# folio check の歯の fixture（4 file 形・crates/folio/tests/check.rs）。変異 = この組は constitution.yaml を欠く → 2。正本の形だけを持つ最小の rules。
schema:
  top_level: [schema, thresholds, discipline]
thresholds:
  - id: R-1
    article: P-1
    what: 説明文の合計
    value: "2,000 字 以下"
discipline:
  - {id: D-1, article: P-1, what: 並列の内側は opus まで}
